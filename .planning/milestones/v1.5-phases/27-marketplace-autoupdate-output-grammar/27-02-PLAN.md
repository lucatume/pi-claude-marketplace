---
phase: 27-marketplace-autoupdate-output-grammar
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
  - tests/shared/notify-v2.test.ts
  - tests/orchestrators/marketplace/list.test.ts
autonomous: true
requirements: [UXG-01]
must_haves:
  truths:
    - "The `marketplace list` surface no longer renders the `<last-updated <iso>>` token on any header"
    - "`MarketplaceDetails.lastUpdatedAt` remains a valid field in state and in the type (only rendering stops)"
    - "catalog-uat + notify-v2 + the orchestrator list test all assert the token-free list header"
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "renderMpHeader list-surface SUB-BRANCH B with lastUpdatedToken removed from the join array"
      contains: "renderMpHeader"
    - path: "docs/output-catalog.md"
      provides: "list-surface header shape + mixed-scopes block without `<last-updated>`"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "mixed-scopes fixture without lastUpdatedAt"
  key_links:
    - from: "renderMpHeader SUB-BRANCH B (notify.ts:659-669)"
      to: "the .filter/.join token array"
      via: "removal of lastUpdatedToken from the array (not emptying it)"
      pattern: "ICON_INSTALLED.*mp.name.*scope.*autoupdateToken"
---

<objective>
Drop the `<last-updated <iso>>` marker from the `marketplace list` surface header (UXG-01, closes UAT Finding 1). The raw ISO timestamp is noise and meaningless for path-source marketplaces. The `MarketplaceDetails.lastUpdatedAt?` field STAYS in the type (notify.ts:285) and in state persistence (state-io.ts:70) -- only the renderer stops emitting it. This avoids touching `notify-types.test.ts:221-232` (`_MarketplaceDetailsExpected` asserts `lastUpdatedAt?` is present).

Purpose: A lockstep grammar change across renderer + catalog + the byte-equality gate + the per-variant byte test + the dedicated orchestrator test. All edits land in ONE commit so the catalog-uat gate never sees an intermediate RED (the gate reads the live catalog at test time).

Output: list header renders without `<last-updated>`; `<autoupdate>` marker on the list surface is unchanged; all four test surfaces GREEN.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-marketplace-autoupdate-output-grammar/27-RESEARCH.md
@.planning/phases/27-marketplace-autoupdate-output-grammar/27-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop the `<last-updated>` token from the renderer + catalog + all three byte tests (one atomic commit)</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, docs/output-catalog.md, tests/architecture/catalog-uat.test.ts, tests/shared/notify-v2.test.ts, tests/orchestrators/marketplace/list.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts:648-670 (renderMpHeader `case undefined:` SUB-BRANCH B -- the `lastUpdatedToken` composition at :665-666 and the join array at :667)
    - extensions/pi-claude-marketplace/shared/notify.ts:596-603 (the renderMpHeader block-comment documenting the dropped token -- must be updated)
    - extensions/pi-claude-marketplace/shared/notify.ts:285 (the `MarketplaceDetails.lastUpdatedAt?` field -- STAYS, do NOT remove)
    - docs/output-catalog.md:25-29 (the "Marketplace header shape" table row L27 for `details.lastUpdatedAt` + the marker prose L29)
    - docs/output-catalog.md:696-724 (the `## /claude:plugin marketplace list` section: prose L698, `mixed-scopes` fenced block L712-722 with the `<last-updated 2026-05-25T00:00:00Z>` token at L715, and the explainer prose L724)
    - tests/shared/notify-v2.test.ts: the list-surface byte tests asserting `<last-updated>` (per 27-RESEARCH.md the assertion + mini-spec at L70 / L664-686 region) -- grep the file for `last-updated` to locate the exact assertions
    - tests/architecture/catalog-uat.test.ts:1127-1147 (`mixed-scopes` fixture -- the `lastUpdatedAt: "2026-05-25T00:00:00Z"` in alpha/project's `details` at :1134)
    - tests/orchestrators/marketplace/list.test.ts:154-189 (the dedicated `ML-V2 / D-16-12` test asserting `● test-mp [project] <autoupdate> <last-updated 2026-05-25T00:00:00Z>` at :188)
    - 27-RESEARCH.md § "UXG-01 -- Drop `<last-updated <iso>>`" + § "The Lockstep Contract" UXG-01 row + § "Pitfall 2" (whitespace-exact gate)
  </read_first>
  <action>
    Edit the renderer, catalog, and all three byte tests in ONE commit (the lockstep contract -- catalog-uat reads the live catalog at test time):

    1. **Renderer (notify.ts SUB-BRANCH B):** In `renderMpHeader`'s `case undefined:` SUB-BRANCH B, REMOVE the `lastUpdatedToken` local (the `mp.details.lastUpdatedAt === undefined ? "" : \`<last-updated ${...}>\`` composition) AND remove `lastUpdatedToken` from the `.filter(...).join(" ")` token array entirely -- drop the array element, do NOT just empty it (per Pitfall 2: an empty trailing slot risks a double-space; the `.filter` already collapses it, but removing the element is cleaner and matches the catalog). The array becomes `[ICON_INSTALLED, mp.name, \`[${mp.scope}]\`, autoupdateToken]`. The `autoupdateToken` (`<autoupdate>` iff `details.autoupdate === true`) STAYS. Do NOT touch `MarketplaceDetails.lastUpdatedAt?` (notify.ts:285) -- the field remains.

    2. **Renderer block-comment (notify.ts:596-603):** Remove the SUB-BRANCH B bullet describing the `<last-updated ${mp.details.lastUpdatedAt}>` token; keep the `<autoupdate>` bullet. Keep the comment accurate to the new behavior.

    3. **Catalog (output-catalog.md):** (a) Delete the table row L27 (`undefined`, `details.lastUpdatedAt` set → `● M [S] <last-updated <iso>>`). (b) In the marker prose L29, remove references to `<last-updated <iso>>` being emitted on the list surface (keep the `<autoupdate>` marker statement). (c) In the `## /claude:plugin marketplace list` section: update the prose L698 to drop the `<last-updated <iso>>` mention; edit the `mixed-scopes` fenced block L715 from `● alpha [project] <autoupdate> <last-updated 2026-05-25T00:00:00Z>` to `● alpha [project] <autoupdate>`; update the explainer L724 to drop the `<last-updated>` clause.

    4. **catalog-uat fixture (catalog-uat.test.ts:1134):** Remove `lastUpdatedAt: "2026-05-25T00:00:00Z"` from alpha/project's `details` so it reads `details: { autoupdate: true }` (matching the corrected `mixed-scopes` byte form). The `examples.length >= 30` count is unaffected (no state added/removed).

    5. **notify-v2 byte test:** Edit the list-surface assertion(s) that expect `<last-updated>` so the expected string drops the token (header becomes `... <autoupdate>` only when autoupdate is true; bare otherwise). Update the file-header mini-spec at L70 if it documents `<last-updated>`.

    6. **orchestrator list test (list.test.ts:154-189):** Update the `ML-V2` test: the expected byte form at :188 changes from `● test-mp [project] <autoupdate> <last-updated 2026-05-25T00:00:00Z>` to `● test-mp [project] <autoupdate>`. The test FIXTURE may keep setting `lastUpdatedAt` on the record (proving the field persists but is no longer rendered) -- adjust the test name/comment to reflect "field persists, token not rendered" rather than "emits the marker". Per CLAUDE.md IL-2 all output still flows through `shared/notify.ts`; no output-channel change.

    Mirror the existing `.filter`/`joinTokens` whitespace discipline exactly -- a stray double-space fails the byte-equality gate (Pitfall 2).
  </action>
  <verify>
    <automated>node --test tests/shared/notify-v2.test.ts tests/architecture/catalog-uat.test.ts tests/orchestrators/marketplace/list.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/shared/notify-v2.test.ts tests/architecture/catalog-uat.test.ts tests/orchestrators/marketplace/list.test.ts` exits 0.
    - catalog-uat `mixed-scopes` fixture renders `● alpha [project] <autoupdate>` (no `<last-updated>` token) byte-equal to the catalog block.
    - `grep -n "last-updated" docs/output-catalog.md` returns no list-surface emission claims (the marker is no longer rendered; any remaining mention must be historical/cross-surface prose only -- confirm none assert the list emits it).
    - `grep -n "lastUpdatedAt" extensions/pi-claude-marketplace/shared/notify.ts` still matches the `MarketplaceDetails.lastUpdatedAt?` field declaration (the field is retained; only the renderer emission is removed).
    - The `<autoupdate>` list-surface marker is unchanged (alpha/project + zeta/project still render `<autoupdate>`).
  </acceptance_criteria>
  <done>
    `<last-updated>` dropped from the list renderer + catalog + all three byte tests in one commit; `lastUpdatedAt` field + state persistence intact; `notify-types.test.ts` untouched and GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full-suite GREEN gate for UXG-01</name>
  <files>(no source change -- GREEN gate only)</files>
  <read_first>
    - 27-VALIDATION.md § "Sampling Rate" (per-wave merge runs `npm run check`)
    - tests/architecture/notify-types.test.ts:221-232 (`_MarketplaceDetailsExpected` -- must stay GREEN because `lastUpdatedAt?` is retained)
  </read_first>
  <action>
    Run `npm run check` to confirm typecheck (the retained `lastUpdatedAt?` field keeps `notify-types.test.ts` compile proofs GREEN) + ESLint + Prettier + the full `node --test` suite. If Prettier reports markdown drift on the edited catalog, run the project formatter and re-run. Record the UXG-01 closure in the SUMMARY (renderer token removed, field retained, 3 byte surfaces updated).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0.
    - `tests/architecture/notify-types.test.ts` GREEN (the `MarketplaceDetails.lastUpdatedAt?` shape proof is unaffected -- field retained).
  </acceptance_criteria>
  <done>
    `npm run check` GREEN; UXG-01 closed with the field retained in state/type and only the list-surface emission removed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | UXG-01 removes a render-time token from the list-surface header. No untrusted input, disk mutation, network, or new parsing surface. The dropped `lastUpdatedAt` value originates from already-validated state (state-io.ts) and is now simply not rendered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02 | Information disclosure | list-surface header | accept | Removing the ISO timestamp from output reduces (not increases) disclosure surface. The value is non-secret bookkeeping; no security relevance. |
| T-27-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (internal source + doc + test edits only); no Package Legitimacy Audit required. |

Security surface is marginal per 27-RESEARCH.md § Security Domain (grammar phase, no auth/session/secret surface).
</threat_model>

<verification>
- `node --test tests/shared/notify-v2.test.ts tests/architecture/catalog-uat.test.ts tests/orchestrators/marketplace/list.test.ts` GREEN.
- `npm run check` GREEN.
- `notify-types.test.ts` untouched and GREEN (field retained).
</verification>

<success_criteria>
- UXG-01 ROADMAP success criterion: `marketplace list` no longer renders `<last-updated <iso>>` on any marketplace header; `MarketplaceDetails.lastUpdatedAt` may remain in state (it does).
- catalog-uat byte-equality GREEN at the commit boundary.
- The `<autoupdate>` list-surface marker behavior is unchanged.
</success_criteria>

<output>
Create `.planning/phases/27-marketplace-autoupdate-output-grammar/27-02-SUMMARY.md` when done.
</output>
