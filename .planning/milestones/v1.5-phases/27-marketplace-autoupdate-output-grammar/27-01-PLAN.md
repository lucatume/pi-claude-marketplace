---
phase: 27-marketplace-autoupdate-output-grammar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
autonomous: true
requirements: [UXG-06]
must_haves:
  truths:
    - "The catalog no longer claims github-source `marketplace add` defaults autoupdate ON"
    - "The catalog autoupdate heading names the real `autoupdate` / `noautoupdate` verbs"
    - "catalog-uat byte-equality stays GREEN (the FIXTURES section key matches the renamed heading byte-for-byte)"
  artifacts:
    - path: "docs/output-catalog.md"
      provides: "Corrected github-source prose + renamed autoupdate heading"
      contains: "noautoupdate"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "FIXTURES section key matching the renamed heading"
  key_links:
    - from: "tests/architecture/catalog-uat.test.ts FIXTURES key"
      to: "docs/output-catalog.md `## ` heading"
      via: "loadCatalogExamples section-name derivation (sectionRe at catalog-uat.test.ts:82)"
      pattern: "marketplace autoupdate"
---

<objective>
Correct two documentation defects in `docs/output-catalog.md` (UXG-06, closes UAT Finding 6):
1. The github-source `marketplace add` prose falsely states GitHub marketplaces default autoupdate ON. Verified-false: `add.ts` writes NO `autoupdate` field for any source kind (github at add.ts:235-244, path at add.ts:311-320); only `bootstrap` enables it.
2. The autoupdate command heading reads `<enable|disable>`, but the real edge verbs are `autoupdate` / `noautoupdate` (router.ts:74-75, 179-182 -- there is no `disable` subcommand).

Purpose: This is the first plan in the phase because the heading rename has a HIDDEN test coupling -- the catalog-uat driver keys its `FIXTURES` map by the `##` heading text. Landing UXG-06 first establishes the renamed heading + key so UXG-04 (which rewrites the same autoupdate fixtures) does not double-touch the heading. Doc-only per the requirement, EXCEPT the FIXTURES key edit forced by the driver coupling.

Output: Corrected catalog prose + renamed heading + byte-matching FIXTURES key; catalog-uat GREEN.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-marketplace-autoupdate-output-grammar/27-RESEARCH.md
@.planning/phases/27-marketplace-autoupdate-output-grammar/27-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct github-source prose + rename the autoupdate heading + sync the FIXTURES key (one atomic commit)</name>
  <files>docs/output-catalog.md, tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - docs/output-catalog.md:744-750 (the `github-source` fenced block + the false prose at L750)
    - docs/output-catalog.md:843-845 (the `## ` autoupdate heading + its preamble; note the heading is backtick-wrapped: `` ## `/claude:plugin marketplace autoupdate <enable|disable> <name>` ``)
    - tests/architecture/catalog-uat.test.ts:74-120 (`loadCatalogExamples` + `sectionRe` at :82 -- confirms the section key is the inner backtick text of the `## ` heading)
    - tests/architecture/catalog-uat.test.ts:1285 (the `FIXTURES` map key literal `"/claude:plugin marketplace autoupdate <enable|disable> <name>"` that MUST match the renamed heading byte-for-byte)
    - 27-RESEARCH.md § "UXG-06 -- Doc-only catalog correction + heading rename" + § "Pitfall 3" (the FIXTURES-key coupling) + § "Common Pitfalls / Pitfall 3"
  </read_first>
  <action>
    Edit `docs/output-catalog.md` and `tests/architecture/catalog-uat.test.ts` together in ONE commit (per UXG-06; the heading rename forces the test key edit):

    1. **github-source prose (catalog L750):** Rewrite the sentence that begins "GitHub-source marketplaces default to autoupdate ON; the persisted record stores `autoupdate: true`." Replace with prose stating that `marketplace add` never enables autoupdate for ANY source (github or path); autoupdate is opt-in via `bootstrap` or an explicit `marketplace autoupdate`. The byte form of the `github-source` fenced block at L747 (`● claude-plugins-official [user] (added)`) does NOT change -- prose only. Keep the existing reload-hint clause unchanged.

    2. **Heading rename (catalog L843):** Rename the backtick-wrapped `## ` heading from `/claude:plugin marketplace autoupdate <enable|disable> <name>` to the real-verb form `/claude:plugin marketplace autoupdate|noautoupdate <name>` (names both verbs; drops the non-existent `disable` subcommand). Keep the backtick wrapping (the driver's `sectionRe` requires it). Update the preamble at L845 if it references the `<enable|disable>` form in prose, and verify the cross-reference anchor link to `## /claude:plugin marketplace list` at L845 still resolves (that link targets the list heading, not the autoupdate heading, so it is unaffected -- confirm by inspection).

    3. **FIXTURES key (catalog-uat.test.ts:1285):** Change the `FIXTURES` map key string from `"/claude:plugin marketplace autoupdate <enable|disable> <name>"` to the EXACT byte-identical inner text of the renamed heading: `"/claude:plugin marketplace autoupdate|noautoupdate <name>"`. The 5 nested state fixtures (enable-fresh, disable-fresh, enable-idempotent, disable-idempotent, failure-not-found) stay unchanged in THIS plan -- UXG-04 rewrites their byte forms later.

    Do NOT change any renderer code. Do NOT change the autoupdate state fixtures' message payloads or byte forms (that is UXG-04 / Plan 03). Per CLAUDE.md IL-2 there is no output-channel change here.
  </action>
  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/architecture/catalog-uat.test.ts` exits 0 (no `[MISSING FIXTURE]` for any autoupdate state; `examples.length >= 30` still holds).
    - `grep -n "default to autoupdate ON" docs/output-catalog.md` returns nothing (the false claim is gone).
    - `grep -n "autoupdate|noautoupdate" docs/output-catalog.md` matches the renamed heading.
    - The `FIXTURES` key in catalog-uat.test.ts is byte-identical to the renamed heading's inner backtick text (the driver's `loadCatalogExamples` derives the section name from the heading; a mismatch produces `[MISSING FIXTURE]`).
    - The `github-source` fenced byte form `● claude-plugins-official [user] (added)` is unchanged.
  </acceptance_criteria>
  <done>
    Catalog github-source prose corrected, autoupdate heading renamed to the real verbs, FIXTURES key synced byte-for-byte, catalog-uat GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full-suite GREEN gate for UXG-06</name>
  <files>(no source change -- GREEN gate only)</files>
  <read_first>
    - 27-VALIDATION.md § "Sampling Rate" (per-wave merge runs `npm run check`)
    - .planning/REQUIREMENTS.md:25 (UXG-06 acceptance: doc-only, no renderer change)
  </read_first>
  <action>
    Run `npm run check` to confirm typecheck + ESLint + Prettier + the full `node --test` suite stay GREEN after the doc + FIXTURES-key edit. No source change is expected from this task; it is the GREEN gate that proves UXG-06 introduced no regression. If Prettier reports formatting drift on the edited markdown, apply `npm run format` (or the project's documented formatter) and re-run. Record the UXG-06 closure in the plan SUMMARY (heading + FIXTURES key + github prose).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 (typecheck + ESLint + Prettier + tests all GREEN).
    - No renderer (`shared/notify.ts`) or orchestrator file changed (UXG-06 is doc + 1-line-test only).
  </acceptance_criteria>
  <done>
    `npm run check` GREEN; UXG-06 closed doc-only with the forced FIXTURES-key sync.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | UXG-06 edits documentation prose + one test-fixture key string. No untrusted input crosses any boundary; no disk-mutation, network, or parsing surface is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information disclosure | catalog prose accuracy | accept | Marginal surface -- a doc correction reduces (not increases) misinformation; the byte-contract gate (catalog-uat) prevents drift between doc and renderer. No security-relevant data. |
| T-27-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase (internal source + doc edits only); no Package Legitimacy Audit required (27-RESEARCH.md § Package Legitimacy Audit). |

Security surface is marginal per 27-RESEARCH.md § Security Domain: this is a grammar/doc phase with no auth/session/secret surface.
</threat_model>

<verification>
- `node --test tests/architecture/catalog-uat.test.ts` GREEN (byte-equality + no missing-fixture).
- `npm run check` GREEN end-to-end.
- Manual grep confirmations of the corrected prose + renamed heading + matching key.
</verification>

<success_criteria>
- UXG-06 ROADMAP success criterion: the catalog no longer claims github `add` auto-enables autoupdate, and the heading names the real `autoupdate`/`noautoupdate` verbs.
- catalog-uat byte-equality GREEN at the commit boundary (FIXTURES key matches the renamed heading).
- No renderer change (doc-only requirement honored, modulo the driver-forced 1-line test key).
</success_criteria>

<output>
Create `.planning/phases/27-marketplace-autoupdate-output-grammar/27-01-SUMMARY.md` when done.
</output>
