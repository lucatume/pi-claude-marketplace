---
phase: 28-severity-routing-label-discipline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/shared/snm-uxg03-label-color-spike.test.ts
  - .planning/phases/28-severity-routing-label-discipline/UXG-03-FINDING.md
  - .planning/v1.4-MILESTONE-UAT.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - UXG-03
must_haves:
  truths:
    - "A feasibility spike is RUN against the @earendil-works/pi-coding-agent host notify surface and its outcome is recorded as evidence."
    - "The spike confirms the host notify(message, type?) signature has NO color-only / label-suppression parameter (label + color both derive from `type`)."
    - "If feasibility is refuted (the strongly-evidenced expected outcome), UXG-03 resolves as an UPSTREAM-TRACKED FINDING -- no colorless in-extension workaround is shipped (D-28-10)."
    - "The deliverable mirrors the SNM-39 / G-MIL-07 precedent: in-repo finding with exact host line refs + spike evidence, a UAT note, a REQUIREMENTS note, and a STATE.md deferral row (D-28-12)."
    - "Filing the actual upstream issue is left to the operator (not auto-filed, not auto-drafted) (D-28-12)."
  artifacts:
    - path: ".planning/phases/28-severity-routing-label-discipline/UXG-03-FINDING.md"
      provides: "the in-repo upstream-tracked finding with host line refs + spike evidence + contingent D-28-13 policy"
      min_lines: 30
    - path: "tests/shared/snm-uxg03-label-color-spike.test.ts"
      provides: "the spike harness asserting the host label/color coupling (evidence lock)"
    - path: ".planning/v1.4-MILESTONE-UAT.md"
      provides: "UAT note recording UXG-03 resolution (defer-with-finding)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "UXG-03 line annotated with the resolution"
    - path: ".planning/STATE.md"
      provides: "deferral row for the UXG-03 upstream finding"
  key_links:
    - from: "UXG-03-FINDING.md"
      to: "node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/types.d.ts:75"
      via: "exact host line ref for the notify(message, type?) signature"
      pattern: "types.d.ts:75"
    - from: "UXG-03-FINDING.md"
      to: "dist/main.js Error:/Warning: label literals"
      via: "host bundle label-derives-from-type evidence"
      pattern: "Warning: |Error: "
---

<objective>
Run the UXG-03 feasibility spike: confirm whether the Pi host
(`@earendil-works/pi-coding-agent`) can render the severity COLOR on a
multi-line cascade WITHOUT the `Error:`/`Warning:` label prefix (which breaks
the 0/2 indent ladder and duplicates the inline per-row status). The deliverable
is EVIDENCE + a RESOLUTION RECORD, not shipped extension code -- D-28-10 forbids
shipping a colorless in-extension workaround, and D-28-11 rejects the only
in-extension lever (forcing `info`) as self-defeating.

Purpose: UXG-03's requirement text already anticipates this is "likely an
upstream capability". The phase carries a spike before committing the approach.
The strongly-evidenced expected outcome (host `notify(message, type?)` has no
color-only param; label+color both derive from `type`) is that feasibility is
REFUTED, in which case UXG-03 resolves as an upstream-tracked finding mirroring
the SNM-39 / G-MIL-07 precedent.

Output: a spike harness that locks the host label/color coupling as evidence, an
in-repo `UXG-03-FINDING.md` with exact host line refs, and the four-part
resolution record (UAT note + REQUIREMENTS note + STATE.md deferral row +
finding doc). Filing the upstream issue is the operator's call (D-28-12).
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-severity-routing-label-discipline/28-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the spike -- capture host label/color coupling evidence and lock it in a harness test</name>
  <files>tests/shared/snm-uxg03-label-color-spike.test.ts</files>
  <read_first>
    - node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/types.d.ts lines 70-80 (the `notify(message, type?: "info"|"warning"|"error")` signature -- line 75; no options/color-only param)
    - tests/shared/snm37-behavioral-smoke.test.ts (the SNM-37 pre-tui `ctx.ui.notify` boundary-capture rig to reuse/extend; `makeCtx`/`mock.fn` pattern, lines 60-164)
    - scripts/pi.sh (the source-load sandbox harness; lines 74-110 show the `-e` extension load + `notify` delivery path)
    - .planning/phases/28-severity-routing-label-discipline/28-CONTEXT.md decisions D-28-10/11/13 and §"UXG-03 spike" canonical refs
  </read_first>
  <action>
    Run the feasibility spike against the INSTALLED runtime host
    `@earendil-works/pi-coding-agent` (NOT the `@mariozechner/...` peer-dep
    contract -- the two namespaces matter per CONTEXT §Specific Ideas; the
    installed host renders label+color). Gather and record exact evidence:
    (a) the host public notify signature at
    `node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/types.d.ts:75`
    is `notify(message: string, type?: "info" | "warning" | "error"): void` --
    NO options object, NO color-only / label-suppression parameter; (b) in the
    host bundle `node_modules/@earendil-works/pi-coding-agent/dist/main.js` the
    literal `Warning: ` / `Error: ` label prefixes are emitted as a function of
    `type` (grep the bundle for the label literals and capture the surrounding
    derivation so label and color are shown to share the single `type` arg).
    Harness design is Claude's Discretion (D-28 §Claude's Discretion): the
    cheapest faithful form is a `tests/shared/`-located node:test that READS the
    host `types.d.ts` line + greps the host bundle for the label literals and
    ASSERTS the coupling (no color-only param exists; label literals are present
    and `type`-derived). This is a read-only evidence lock -- do NOT modify any
    host file, do NOT add a host dependency, do NOT change `shared/notify.ts`.
    Place the test under `tests/shared/` so it runs inside `npm test` /
    `npm run check` (mirrors the SNM-37 placement rationale). If -- contrary to
    the strong expectation -- the spike DISCOVERS a color-only/label-suppression
    capability, STOP and record that surprising positive outcome in the SUMMARY
    instead (it would change Task 2's resolution from defer-with-finding to a
    follow-up implementation plan); do NOT ship any colorless workaround under
    D-28-10 regardless.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace; node --test tests/shared/snm-uxg03-label-color-spike.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/shared/snm-uxg03-label-color-spike.test.ts` exists and is GREEN under `node --test`.
    - The test asserts the host `notify` signature at `types.d.ts:75` carries no color-only / label-suppression parameter (only `message` + optional `type`).
    - The test asserts the host bundle `dist/main.js` contains the `Warning: ` / `Error: ` label literals (proving label is host-produced from `type`).
    - No host file modified; no dependency added; `shared/notify.ts` untouched.
  </acceptance_criteria>
  <done>The spike ran; a GREEN harness test locks the host label/color coupling (no color-only param; label literals present and type-derived) as reproducible evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Record UXG-03 resolution -- finding doc + UAT note + REQUIREMENTS note + STATE.md deferral row</name>
  <files>.planning/phases/28-severity-routing-label-discipline/UXG-03-FINDING.md, .planning/v1.4-MILESTONE-UAT.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/v1.4-MILESTONE-UAT.md lines 778-846 (the G-MIL-07 / SNM-39 defer-with-finding entry -- the exact precedent SHAPE to mirror: truth / status / root_cause / artifacts / missing / "opening the upstream issue is the user's call")
    - .planning/STATE.md lines 190-198 (the existing "Additional v1.4.1-scope deferrals" table -- the `upstream_finding` row format to mirror for the new deferral row)
    - .planning/REQUIREMENTS.md line 21 (the UXG-03 definition to annotate)
    - .planning/phases/28-severity-routing-label-discipline/28-CONTEXT.md decisions D-28-10/11/12/13
  </read_first>
  <action>
    Write `.planning/phases/28-severity-routing-label-discipline/UXG-03-FINDING.md`
    as an upstream-tracked finding mirroring the SNM-39 / G-MIL-07 precedent
    (D-28-12). It MUST contain: (1) the truth UXG-03 asserted (multi-line
    cascades render color WITHOUT the host label, single-line keep it); (2) the
    spike method + the Task-1 harness test path as evidence; (3) the EXACT host
    line refs --
    `node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/types.d.ts:75`
    (`notify(message, type?)`, no color-only param) and the `dist/main.js`
    `Warning: ` / `Error: ` label literals derived from `type`; (4) the
    root-cause statement: label + color are both produced by the host from the
    single `type` arg, and the only in-extension lever (forcing `info` to drop
    the label) ALSO drops the color AND nullifies UXG-02's warning/error routing
    -- REJECTED per D-28-11; (5) the resolution: DEFER-WITH-FINDING; do NOT ship
    a colorless in-extension workaround (D-28-10); filing the actual upstream
    `@earendil-works/pi-coding-agent` issue is the OPERATOR's call -- not
    auto-filed, not auto-drafted (D-28-12); (6) the contingent D-28-13 policy
    recorded for intent: IF a future host label-suppression capability lands,
    the discriminator is ENTRYPOINT-based -- `notify()` (structured cascade)
    suppresses the label, `notifyUsageError()` keeps it (NOT a line-count test:
    `notifyUsageError` emits `message` then a blank line then `usage` at
    notify.ts:169, so a literal newline test would wrongly strip the label).

    Add a UXG-03 entry to `.planning/v1.4-MILESTONE-UAT.md` in the same
    defer-with-finding shape as G-MIL-07 (truth / status: defer-with-finding /
    root_cause / artifacts pointing to UXG-03-FINDING.md + the spike test /
    "opening the upstream issue is the operator's call").

    Annotate UXG-03 in `.planning/REQUIREMENTS.md` (line ~21) with a resolution
    note: spike RUN, feasibility REFUTED (host couples label+color to `type`,
    no color-only param), resolved as upstream-tracked finding per D-28-12 (link
    UXG-03-FINDING.md). Set the requirement checkbox per the project's
    convention for defer-with-finding closures (match how SNM-39 was marked).

    Add a deferral row to `.planning/STATE.md` "Additional v1.4.1-scope
    deferrals" table (or a new v1.5 deferrals subsection if cleaner) with
    category `upstream_finding`, the host line refs, "defer-with-finding;
    recorded in-repo (UXG-03-FINDING.md + spike test + UAT). Opening an upstream
    `@earendil-works/pi-coding-agent` issue is the operator's call (D-28-12)",
    and the deferral date.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace; grep -q "types.d.ts:75" .planning/phases/28-severity-routing-label-discipline/UXG-03-FINDING.md; echo "ref-exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/28-severity-routing-label-discipline/UXG-03-FINDING.md` exists and contains the exact host line ref `types.d.ts:75` plus the label-literal evidence and the statement that filing the upstream issue is the OPERATOR's call (D-28-12).
    - The finding records the root cause (label+color both derive from the single host `type` arg; forcing `info` is rejected per D-28-11) and the contingent entrypoint-based D-28-13 policy.
    - `.planning/v1.4-MILESTONE-UAT.md` has a UXG-03 entry with `status: defer-with-finding` mirroring the G-MIL-07 shape.
    - `.planning/REQUIREMENTS.md` UXG-03 line is annotated with the spike-refuted resolution and links UXG-03-FINDING.md.
    - `.planning/STATE.md` has an `upstream_finding` deferral row for UXG-03 with the host line refs.
    - No colorless in-extension workaround is shipped; `extensions/pi-claude-marketplace/shared/notify.ts` is unmodified by this plan.
  </acceptance_criteria>
  <done>UXG-03 resolved as a defer-with-finding upstream record: finding doc + UAT note + REQUIREMENTS note + STATE.md deferral row all present; no extension code shipped; operator owns filing the upstream issue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension -> host (read-only) | The spike READS the installed `@earendil-works/pi-coding-agent` host type declarations and bundle to gather evidence. No data crosses INTO the host; nothing is written to host files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Information Disclosure | spike harness reading host internals | accept | The spike only reads already-installed `node_modules` host files (public type decls + bundle) and existing in-repo planning docs. No secrets, no PII, no network. Evidence is recorded in-repo only. |
| T-28-05 | Tampering | host bundle / extension code | accept | Read-only spike: no host file is modified, no dependency added, `shared/notify.ts` is untouched (D-28-10/11 forbid a colorless workaround). The only writes are an in-repo finding doc + planning-doc notes + a test file. |
| T-28-06 | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. `package.json` untouched; no new host dependency. |

No `high`-severity threats introduced. The spike is read-only against host internals; the only writes are in-repo documentation + a read-only evidence-lock test.
</threat_model>

<verification>
- `tests/shared/snm-uxg03-label-color-spike.test.ts` GREEN: locks the host label/color coupling (no color-only param at `types.d.ts:75`; label literals present in the bundle).
- `UXG-03-FINDING.md` exists with exact host line refs, root cause, resolution, and the contingent D-28-13 entrypoint policy.
- UAT + REQUIREMENTS + STATE.md all carry the defer-with-finding resolution (four-part record per D-28-12).
- `extensions/pi-claude-marketplace/shared/notify.ts` is unmodified -- no colorless in-extension workaround shipped (D-28-10).
- `npm run check` GREEN (the new spike test runs inside the suite; no production-code change to regress).
</verification>

<success_criteria>
- The UXG-03 feasibility spike was RUN; its outcome (host couples label+color to the single `type` arg; no color-only param) is recorded as reproducible evidence.
- UXG-03 resolves as an upstream-tracked finding (the strongly-evidenced expected outcome) -- NOT a shipped colorless workaround (D-28-10).
- Four-part resolution record present: finding doc + UAT note + REQUIREMENTS note + STATE.md deferral row (D-28-12).
- Filing the actual upstream issue is explicitly left to the operator.
- `npm run check` GREEN.
</success_criteria>

<output>
Create `.planning/phases/28-severity-routing-label-discipline/28-02-SUMMARY.md` when done
</output>
