---
milestone: v1.12
status: passed
tested: 2026-06-11
environment: sandboxed Pi runtime via `scripts/pi.sh --home tmp/pi-uat --cd tmp/work` (source-loaded extension; bootstrapped with claude-plugins-official)
---

# Milestone v1.12 Runtime UAT — Marketplace and Plugin Config Files

Conversational runtime UAT performed before phase archival (operator-guided,
2026-06-11). All tests run against a sandboxed Pi home; real scopes untouched
(pre-verified mirrored and backed up at `~/.pi/agent/uat-backup-20260611-082414/`).

## Tests

| # | Test | Requirement | Result |
|---|------|-------------|--------|
| 1 | First-launch migration + silent steady state | MIG-01/02, RECON-05 | PASS |
| 2 | `preview` empty advisory | DIFF-01 | PASS |
| 3 | Hand-edit config → reload installs; second reload silent | RECON-01/05 | PASS (install applied; see UAT-02 for cascade visibility) |
| 3b | Startup uninstall of undeclared managed plugin, cascade visible | RECON-02 | PASS |
| 4 | Command write-back: install patches base config | WB-01 | PASS |
| 5 | Disable: artefacts removed, entry+pin kept, `(disabled)` on list | ENBL-01/02/04 | PASS (cascade token amended — UAT-03) |
| 6 | Enable: cache re-materialization, pin preserved | ENBL-03 | PASS (header amended — UAT-04) |
| 7 | `--local` install: local file only, base byte-identical | WB-02 | PASS (shadowing fixed — UAT-05) |
| 8 | 0-byte config at startup: error block, nothing uninstalled, state byte-identical, file not overwritten | CFG-03 | PASS |

## Findings

| ID | Finding | Resolution |
|----|---------|------------|
| UAT-01 | Empty-but-present state.json spawned an empty `claude-plugins.json` in every scope root | FIXED `58ece5c` — migration skips when there is nothing to capture (`migrated: false, reason: "empty-state"`); verified live |
| UAT-02 | Reconcile cascade invisible on `/reload`: pi's `rebuildChatFromMessages()` erases extension notifications emitted during the reload pipeline (any severity); visible at startup | ACCEPTED as host limitation (pi is not our fork; operator decided no upstream issue) — registered in `.planning/BACKLOG.md` with candidate directions; workaround: `preview` before / `list` after reload |
| UAT-03 | Fresh-disable cascade row rendered `(uninstalled)` instead of `(disabled)` | FIXED `e7c04e9` (operator decision 1a) — row is now `⊘ <name> v<version> (disabled)`, inventory-identical; reload trailer preserved via new `disable-cascade` kind in the hint ladder; verified live |
| UAT-04 | Fresh-enable cascade header showed `(added)` for an existing marketplace | FIXED `e7c04e9` — bare SUB-BRANCH A header; row keeps `(installed)` (operator decision 2b); verified live |
| UAT-05 | `--local` install re-declared a base-declared marketplace into the local file, shadowing base settings (merged `autoupdate` flipped to false) via the CFG-02 wholesale override | FIXED `2a1195e` — adopted-marketplace declaration now gated on the scope's MERGED view at all three call sites (install + both enable-disable arms); 4 regression tests |

## Quality gate at UAT close

`npm run check` GREEN: 1810 unit + 10 integration tests (1804 at milestone-complete; +6 from UAT fixes).

## How to re-run this UAT

Sandboxed harness: `./scripts/pi.sh --home $(pwd)/tmp/pi-uat --cd $(pwd)/tmp/work`
(`--home` redirects PI_CODING_AGENT_DIR/sessions; `--cd` sets the project scope).
Bootstrap once, then drive the test table above. Startup-path tests (cascade
visibility, CFG-03) require quit→edit→relaunch rather than `/reload` (UAT-02).
