---
phase: 31-credential-subprocess-layer-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/architecture/no-shell-out.test.ts
  - package.json
autonomous: true
requirements:
  - AUTH-06
  - AUTH-08
  - AUTH-09
must_haves:
  truths:
    - "tests/architecture/no-shell-out.test.ts permits node:child_process imports in extensions/pi-claude-marketplace/platform/git-credential.ts and only that file"
    - "tests/architecture/no-shell-out.test.ts contains a second assertion that the whitelist is exactly one file, so silent widening is caught in CI"
    - "npm test discovers and runs tests/platform/**/*.test.ts (the dir is currently typechecked but skipped by node --test)"
    - "npm run check exits 0 before any new platform/git-credential.ts content is authored"
  artifacts:
    - path: "tests/architecture/no-shell-out.test.ts"
      provides: "Narrowed D-21 architecture gate (ALLOWED_CHILD_PROCESS_FILES whitelist + exact-membership assertion)"
      contains: "ALLOWED_CHILD_PROCESS_FILES"
    - path: "package.json"
      provides: "test/test:coverage:unit globs include tests/platform/"
      contains: "platform"
  key_links:
    - from: "tests/architecture/no-shell-out.test.ts"
      to: "extensions/pi-claude-marketplace/platform/git-credential.ts"
      via: "ALLOWED_CHILD_PROCESS_FILES set (skip-by-relative-path)"
      pattern: "ALLOWED_CHILD_PROCESS_FILES"
---

<objective>
Narrow the D-21 no-shell-out architecture gate so the Phase 31 production file
(extensions/pi-claude-marketplace/platform/git-credential.ts) can import
node:child_process under an explicit whitelist, AND fold tests/platform/ into
the npm test glob so the Phase 31 unit + architecture tests authored in Plan
31-02 actually run in CI. Without these two amendments, Plan 31-02's first
commit would either fail the architecture gate or write tests that never
execute against npm test.

Purpose: Open the gate before walking through it. Tests in tests/platform/
are currently typechecked (tsconfig include) but not executed (the npm test
glob lists architecture,bridges,domain,edge,helpers,orchestrators,persistence,
shared,transaction -- platform is absent). The AUTH-09 architecture test +
the new platform/git-credential subprocess test must run under npm test or the
phase gate is not real.

Output:
- tests/architecture/no-shell-out.test.ts amended with the
  ALLOWED_CHILD_PROCESS_FILES whitelist (per RESEARCH.md Pitfall 1) + a second
  test asserting the whitelist has exactly one entry.
- package.json test and test:coverage:unit globs widened to include the
  tests/platform/ directory.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-credential-subprocess-layer-auth/31-CONTEXT.md
@.planning/phases/31-credential-subprocess-layer-auth/31-RESEARCH.md
@tests/architecture/no-shell-out.test.ts
@package.json
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| architecture test -> filesystem | The amended test reads .ts files from the extension tree; it does not write or execute anything outside the test harness |
| npm test glob -> CI | A glob that silently omits a directory is a latent test-coverage gap; this plan closes it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-01 | Tampering | tests/architecture/no-shell-out.test.ts whitelist | mitigate | Second test asserts the whitelist is exactly one entry (per RESEARCH.md Example 3); future widening requires an explicit edit + a phase justification, caught in PR review and CI |
| T-31-01-02 | Information Disclosure | npm test glob | accept | Globs are public repo text; not sensitive |
| T-31-01-03 | Repudiation | architecture gate bypass via SECOND child_process import | mitigate | The walk-all-files loop continues after the whitelist skip; any non-whitelisted file with a forbidden pattern still fails the assertion (defense in depth) |
| T-31-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies are added in this plan; package.json edit is glob-only (RESEARCH.md "Installation: None. No new packages.") |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Narrow no-shell-out architecture gate with whitelist + exact-membership assertion</name>
  <files>tests/architecture/no-shell-out.test.ts</files>
  <read_first>
    - tests/architecture/no-shell-out.test.ts (current 64-line D-21 gate; understand existing walkTsFiles + FORBIDDEN_PATTERNS + assert.deepEqual shape)
    - .planning/phases/31-credential-subprocess-layer-auth/31-RESEARCH.md (Pitfall 1 + Example 3 -- the canonical narrowing shape verified against the repo)
    - extensions/pi-claude-marketplace/platform/git.ts (confirms platform-tier file naming convention; provides D-13 boundary precedent)
  </read_first>
  <action>
    Amend tests/architecture/no-shell-out.test.ts to (1) add a module-level
    readonly Set named ALLOWED_CHILD_PROCESS_FILES whose sole entry is the
    repo-relative path "extensions/pi-claude-marketplace/platform/git-credential.ts";
    (2) inside the existing test "no child_process imports anywhere in
    extensions/pi-claude-marketplace/ (D-21)", before each FORBIDDEN_PATTERNS
    iteration compute rel = path.relative(REPO_ROOT, file) and skip via
    `if (ALLOWED_CHILD_PROCESS_FILES.has(rel)) continue;` (push the skip BEFORE
    readFile to avoid reading the whitelisted file at all); (3) rename the
    test title to "no child_process imports outside the Phase 31 whitelist
    (D-21 + Phase 31 narrowing)"; (4) extend the docstring header to record
    the AUTH-06 / AUTH-08 / AUTH-09 reason for the narrowing -- preserve the
    existing W-8 / D-21 / MA-7 narrative and append the Phase 31 paragraph
    that names the three reasons git-credential differs from a clone shell-out
    (no clone/fetch/pull/etc; OS keychain access has no pure-JS equivalent;
    missing-git is non-fatal degrade); (5) add a SECOND test named
    "Phase 31 whitelist: exactly one file may import node:child_process" that
    asserts `[...ALLOWED_CHILD_PROCESS_FILES].sort()` deep-equals
    `["extensions/pi-claude-marketplace/platform/git-credential.ts"]` (this is
    the silent-widening guard per RESEARCH.md Example 3). Do NOT change the
    existing FORBIDDEN_PATTERNS regex set; do NOT change walkTsFiles. The
    assert.deepEqual failure message MUST continue to mention "(MA-7's git CLI
    not found ...)" so the existing failure narrative is preserved -- amend it
    to read "outside the Phase 31 whitelist" instead of "in the extension
    tree". The amendment lands BEFORE the production file exists, so the test
    must still pass against the current tree (which has zero child_process
    imports anywhere); both new arrangements are forward-compatible no-ops
    until Plan 31-02 writes the whitelisted file.
  </action>
  <verify>
    <automated>node --test "tests/architecture/no-shell-out.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - tests/architecture/no-shell-out.test.ts grep `ALLOWED_CHILD_PROCESS_FILES` returns at least 3 hits (declaration, skip-check inside walk loop, exact-membership assertion in the second test).
    - tests/architecture/no-shell-out.test.ts grep `extensions/pi-claude-marketplace/platform/git-credential.ts` returns at least 2 hits (the Set initializer + the exact-membership assertion).
    - Two `test(` declarations exist in the file (one renamed gate test, one new whitelist-membership test); grep `^test\(` (or `\btest\(\s*['"]`) shows two matches.
    - `node --test tests/architecture/no-shell-out.test.ts` exits 0 with 2 passing tests against the current tree (no production file yet -- gate is forward-compatible).
    - The docstring header still cites D-21, W-8, MA-7, and now ADDITIONALLY cites AUTH-06, AUTH-08, AUTH-09 by ID (grep for `AUTH-06` returns >=1 hit; same for AUTH-08, AUTH-09).
    - npm run typecheck exits 0.
  </acceptance_criteria>
  <done>
    The D-21 gate skips extensions/pi-claude-marketplace/platform/git-credential.ts
    via an explicit whitelist; a sibling test pins the whitelist to exactly
    that one path; both tests pass against the current (production-file-absent)
    tree; the docstring records the AUTH-06/08/09 reason for the narrowing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fold tests/platform/ into the npm test glob</name>
  <files>package.json</files>
  <read_first>
    - package.json (current scripts.test + scripts.test:coverage:unit; understand the exact existing glob ordering -- the new entry must be inserted alphabetically between "persistence" and "shared")
    - tests/platform/pi-api.test.ts (confirms there is already content under tests/platform/ that should be discovered)
    - tsconfig.json (confirms "tests/**/*.ts" is already in the include set so no tsconfig change is required)
  </read_first>
  <action>
    Edit package.json scripts.test and scripts.test:coverage:unit so the
    brace-expansion glob lists are alphabetically extended to include
    `platform`. Change scripts.test value from
    `"node --test \"tests/{architecture,bridges,domain,edge,helpers,orchestrators,persistence,shared,transaction}/**/*.test.ts\""`
    to
    `"node --test \"tests/{architecture,bridges,domain,edge,helpers,orchestrators,persistence,platform,shared,transaction}/**/*.test.ts\""`.
    Apply the same insertion to scripts.test:coverage:unit (same brace list,
    `platform` between `persistence` and `shared`). Do NOT change
    test:coverage:e2e, test:coverage:integration, test:e2e, test:e2e:nightly,
    or test:integration -- those use disjoint roots (tests/e2e, tests/integration)
    that already operate independently. Do NOT reorder any other key in
    package.json (preserve alphabetical key order, trailing newline, exact
    indentation -- prettier --check will fail on whitespace drift).

    Decision context (D-31-A): the existing tests/platform/pi-api.test.ts has
    been in the tree since Phase 17.2 but the glob never included it; adding
    `platform` here closes the latent gap surfaced by Phase 31's need for the
    git-credential unit test + AUTH-09 architecture test (the architecture
    test goes under tests/architecture, but the SUBPROCESS unit tests need a
    home that mirrors platform/git-credential.ts -- tests/platform/ is the
    correct mirror).
  </action>
  <verify>
    <automated>npm run format:check &amp;&amp; node -e "const p=require('./package.json'); const ok=p.scripts.test.includes('platform')&amp;&amp;p.scripts['test:coverage:unit'].includes('platform'); process.exit(ok?0:1);" &amp;&amp; npm test 2>&amp;1 | grep -E "platform pi-api|tests/platform" || (echo "platform tests not discovered" &amp;&amp; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `node -p "require('./package.json').scripts.test"` output contains the substring `,platform,shared,`.
    - `node -p "require('./package.json').scripts['test:coverage:unit']"` output contains the substring `,platform,shared,`.
    - `npm test` discovers and runs tests/platform/pi-api.test.ts: the output shows at least one "✔ platform pi-api ..." line (the existing 6 tests in pi-api.test.ts).
    - `npm test` exits 0 (test count rises by 6 from the pre-amendment baseline; no regression in other suites).
    - `npm run format:check` exits 0 (no whitespace drift in package.json).
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>
    npm test discovers tests/platform/**/*.test.ts; the pre-existing
    pi-api.test.ts 6 tests now appear in the suite (proves the glob works);
    npm run check stays green. The directory is ready to receive Plan 31-02's
    new git-credential.test.ts.
  </done>
</task>

</tasks>

<verification>
End-to-end: `npm run check` exits 0 with two new architecture-test assertions
passing (whitelist exists; whitelist is exactly one file) and tests/platform/
now contributing to the test count. The phase-gate file
extensions/pi-claude-marketplace/platform/git-credential.ts does NOT yet exist
(Plan 31-02 creates it); the architecture gate is forward-compatible until it
does.
</verification>

<success_criteria>
1. tests/architecture/no-shell-out.test.ts contains
   ALLOWED_CHILD_PROCESS_FILES with sole entry
   "extensions/pi-claude-marketplace/platform/git-credential.ts" and a sibling
   exact-membership assertion test; both new tests pass.
2. package.json scripts.test and scripts.test:coverage:unit include `platform`
   in the brace-expansion alphabetically between `persistence` and `shared`;
   `npm test` discovers tests/platform/pi-api.test.ts (existing 6 tests run).
3. `npm run check` exits 0; no regression in pre-existing test count
   (1168/1168 baseline + 6 newly-discovered + 1 newly-added whitelist test).
4. No production code changes in this plan; Plan 31-02 is unblocked to
   author extensions/pi-claude-marketplace/platform/git-credential.ts.
</success_criteria>

<output>
Create `.planning/phases/31-credential-subprocess-layer-auth/31-01-SUMMARY.md`
when done. Record: (a) the exact new test-count delta from the glob widening
(pi-api.test.ts contributes 6) plus the 1 new whitelist-membership test; (b)
confirmation that no production file under
extensions/pi-claude-marketplace/platform/ has been added yet; (c) the
ALLOWED_CHILD_PROCESS_FILES whitelist contents (one entry) for Plan 31-02 to
mirror in its file path.
</output>
