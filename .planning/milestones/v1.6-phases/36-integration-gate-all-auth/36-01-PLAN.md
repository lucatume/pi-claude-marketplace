---
phase: 36-integration-gate-all-auth
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/auth-e2e.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - AUTH-08
  - AUTH-09
  - AUTH-10

must_haves:
  truths:
    - "npm run check exits 0; test count is >= 1312 (Phase 35 baseline); no regressions"
    - "npm run test:integration includes auth-e2e.test.ts and all 3 tests pass"
    - "Full add-path E2E: buildAuthCallbacks + initiateDeviceFlow + CredentialOps closure chain works end-to-end from the orchestrator seam (AUTH-01)"
    - "Silent reuse: fill-hit returns stored cred; no Device Flow notify fires (AUTH-02 / AUTH-08)"
    - "Reject-evict-reflow: onAuthFailure evicts cred, subsequent onAuth call triggers fresh Device Flow (AUTH-07)"
    - "REQUIREMENTS.md shows AUTH-01 through AUTH-10 as [x] satisfied"
  artifacts:
    - path: "tests/integration/auth-e2e.test.ts"
      provides: "Cross-phase integration tests for the full v1.6 auth stack"
      min_lines: 80
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement status"
      contains: "[x] **AUTH-01**"
  key_links:
    - from: "tests/integration/auth-e2e.test.ts"
      to: "extensions/pi-claude-marketplace/platform/git.ts::buildAuthCallbacks"
      via: "direct import"
      pattern: "buildAuthCallbacks"
    - from: "tests/integration/auth-e2e.test.ts"
      to: "extensions/pi-claude-marketplace/domain/github-auth.ts::initiateDeviceFlow"
      via: "direct import"
      pattern: "initiateDeviceFlow"
    - from: "tests/integration/auth-e2e.test.ts"
      to: "tests/helpers/credential-mock.ts::makeMockCredentialOps"
      via: "import"
      pattern: "makeMockCredentialOps"
    - from: "tests/integration/auth-e2e.test.ts"
      to: "tests/helpers/device-flow-mock.ts::makeMockDeviceFlowHttp"
      via: "import"
      pattern: "makeMockDeviceFlowHttp"
    - from: "tests/integration/auth-e2e.test.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts::makeRawNotifyFn"
      via: "import"
      pattern: "makeRawNotifyFn"
---

<objective>
Close the v1.6 AUTH integration gate: write a cross-phase integration test that
exercises the full auth stack end-to-end (buildAuthCallbacks + initiateDeviceFlow
+ CredentialOps + makeRawNotifyFn in a single test file), and mark all AUTH-01
through AUTH-10 requirements as satisfied in REQUIREMENTS.md.

Purpose: Phases 31-35 delivered each layer of the v1.6 auth stack in isolation.
This gate confirms the closure chain is wired correctly end-to-end and marks
the 10 AUTH requirements as complete.

Output:
- tests/integration/auth-e2e.test.ts (3 integration tests)
- .planning/REQUIREMENTS.md (AUTH-01..AUTH-10 marked [x])
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/acolomba/src/pi-claude-marketplace/.planning/PROJECT.md
@/Users/acolomba/src/pi-claude-marketplace/.planning/ROADMAP.md
@/Users/acolomba/src/pi-claude-marketplace/.planning/STATE.md
@/Users/acolomba/src/pi-claude-marketplace/.planning/REQUIREMENTS.md
@/Users/acolomba/src/pi-claude-marketplace/.planning/phases/35-orchestrator-call-sites-output-catalog-auth/35-01-SUMMARY.md
@/Users/acolomba/src/pi-claude-marketplace/.planning/phases/35-orchestrator-call-sites-output-catalog-auth/35-02-SUMMARY.md
@/Users/acolomba/src/pi-claude-marketplace/.planning/phases/33-git-ts-auth-wiring-auth/33-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/integration/auth-e2e.test.ts with 3 integration tests</name>
  <files>tests/integration/auth-e2e.test.ts</files>

  <read_first>
    - tests/helpers/credential-mock.ts -- MockCredentialOpsHandle, makeMockCredentialOps, MockCredentialState.store (Map-based), fillCalls, approveCalls, rejectCalls
    - tests/helpers/device-flow-mock.ts -- MockDeviceFlowHttpHandle, makeMockDeviceFlowHttp, MockDeviceFlowState.pollQueue, deviceCode defaults (user_code: "ABCD-1234", verification_uri: "https://github.com/login/device", interval: 5)
    - extensions/pi-claude-marketplace/platform/git.ts -- buildAuthCallbacks(opts: BuildAuthCallbacksOpts): { onAuth, onAuthFailure }; BuildAuthCallbacksOpts: { credentialOps, host, onAuthRequired }; AuthAttemptResult discriminated union; OnAuthRequiredFn
    - extensions/pi-claude-marketplace/domain/github-auth.ts -- initiateDeviceFlow(opts: InitiateDeviceFlowOpts): Promise<DeviceFlowResult>; InitiateDeviceFlowOpts: { host, credentialOps, notifyFn, http? }; DeviceFlowResult: { ok: true; cred; authAttempted: true } | { ok: false; reason; authAttempted: true }; PollResult kinds: "success", "pending", "slow_down", "access_denied", "expired_token"
    - extensions/pi-claude-marketplace/shared/notify.ts -- makeRawNotifyFn(ctx: ExtensionContext): (message, severity?) => void; line 1530
    - tests/integration/concurrent-install.test.ts -- pattern for integration test file structure (imports, test() calls, node:test usage)
    - tests/orchestrators/marketplace/add.test.ts -- AUTH-01 tests (lines 654-813) for the exact pattern of wiring buildAuthCallbacks + initiateDeviceFlow; observe how makeMockCredentialOps and makeMockDeviceFlowHttp are used together
  </read_first>

  <action>
Create tests/integration/auth-e2e.test.ts with 3 tests that exercise the full
v1.6 auth stack in one file. This is a cross-phase observability point: it
wires the closure chain (buildAuthCallbacks -> onAuth -> initiateDeviceFlow ->
CredentialOps.approve) without going through the full orchestrator.

File header: a brief module comment explaining this is the Phase 36 integration
gate, with references to AUTH-01, AUTH-02, AUTH-07.

Imports (all type-only where possible; use full relative paths from tests/integration/):
- import assert from "node:assert/strict"
- import test from "node:test"
- import { buildAuthCallbacks } from "../../extensions/pi-claude-marketplace/platform/git.ts"
- import type { OnAuthRequiredFn } from "../../extensions/pi-claude-marketplace/platform/git.ts"
- import { initiateDeviceFlow } from "../../extensions/pi-claude-marketplace/domain/github-auth.ts"
- import { makeRawNotifyFn } from "../../extensions/pi-claude-marketplace/shared/notify.ts"
- import { makeMockCredentialOps } from "../helpers/credential-mock.ts"
- import { makeMockDeviceFlowHttp } from "../helpers/device-flow-mock.ts"

For the makeRawNotifyFn call you need a mock ExtensionContext. Construct a
minimal inline mock -- do NOT import the full Pi API type. Use this shape:
  const notifyCalls: Array<{ message: string; severity?: string }> = [];
  const mockCtx = { ui: { notify: (msg: string, sev?: string) => { notifyCalls.push({ message: msg, ...(sev !== undefined && { severity: sev }) }); } } } as unknown as Parameters<typeof makeRawNotifyFn>[0];

Then: const notifyFn = makeRawNotifyFn(mockCtx);

Test 1 -- "AUTH-01 add path E2E: fill-miss triggers Device Flow, token stored via approve":
  - Build a fresh empty makeMockCredentialOps() (no pre-seeded store).
  - Build a makeMockDeviceFlowHttp with pollQueue: [{ kind: "success", accessToken: "gho_test_token_e2e", tokenType: "bearer", scope: "repo" }], deviceCode.interval set to 0.
  - Construct onAuthRequired using initiateDeviceFlow: const onAuthRequired: OnAuthRequiredFn = () => initiateDeviceFlow({ host: "github.com", credentialOps: credOps, notifyFn, http });
  - Build callbacks: const { onAuth, onAuthFailure } = buildAuthCallbacks({ credentialOps: credOps, host: "github.com", onAuthRequired });
  - Simulate a fill-miss: call onAuth("https://github.com/some-repo.git") and await the result.
  - Assert the return value has username ("x-access-token") and password ("gho_test_token_e2e").
  - Assert notifyCalls has exactly one entry with message "Open https://github.com/login/device and enter: ABCD-1234" and severity "info" (AUTH-03 byte form).
  - Assert credOps.approveCalls has exactly one entry with host "github.com" and cred.password "gho_test_token_e2e" (AUTH-06).
  - Assert credOps.fillCalls.length === 1 (the initial fill that returned null).

Test 2 -- "AUTH-02 / AUTH-08 silent reuse: fill-hit returns stored cred, no Device Flow":
  - Build makeMockCredentialOps with store pre-seeded: new Map([["github.com", { username: "x-access-token", password: "stored_token_e2e" }]]).
  - Build an empty makeMockDeviceFlowHttp (default poll queue; unreachable in this test).
  - Reset notifyCalls to [].
  - Construct onAuthRequired and build callbacks same as Test 1.
  - Call onAuth("https://github.com/other-repo.git") and await.
  - Assert return value has password "stored_token_e2e".
  - Assert notifyCalls.length === 0 (no Device Flow prompt, AUTH-02 / AUTH-08).
  - Assert credOps.approveCalls.length === 0 (no approve; stored token reused).
  - Assert credOps.fillCalls.length === 1 with host "github.com".

Test 3 -- "AUTH-07 reject-evict-reflow: onAuthFailure evicts, next fill-miss re-triggers Device Flow":
  - Build makeMockCredentialOps with store pre-seeded with host "github.com" -> { username: "x-access-token", password: "stale_token" }.
  - Build makeMockDeviceFlowHttp with pollQueue: [{ kind: "success", accessToken: "gho_fresh_token_e2e", tokenType: "bearer", scope: "repo" }], deviceCode.interval set to 0.
  - Reset notifyCalls to [].
  - Construct onAuthRequired and build callbacks same as Test 1.
  - Simulate onAuthFailure call: call onAuthFailure("https://github.com/repo.git", { username: "x-access-token", password: "stale_token" }) and await result.
  - Assert onAuthFailure result has cancel property equal to true (CP-9: always cancels).
  - Assert credOps.rejectCalls has exactly one entry with host "github.com" (AUTH-07 eviction via git credential reject).
  - Assert the store no longer contains "github.com" (credential evicted from in-memory store, mirrors OS keychain eviction per AUTH-07).
  - Assert notifyCalls.length === 0 (onAuthFailure does not emit Device Flow prompt; it merely evicts and cancels).
  - Then simulate a second round: call onAuth("https://github.com/repo.git") and await.
  - Assert notifyCalls has exactly one Device Flow prompt entry with message "Open https://github.com/login/device and enter: ABCD-1234" and severity "info" (Device Flow re-triggered automatically after eviction per AUTH-07).
  - Assert credOps.approveCalls has exactly one entry with cred.password "gho_fresh_token_e2e".

AUTH-09 inline guard in each test: assert that no notifyCalls entry contains
"access_token" or "gho_" in the message field. This is an inline guard, not a
separate architecture test.

ESLint discipline: no direct ctx.ui.notify calls (mockCtx inline object is not a
ctx.ui.notify call at the production layer; the test is test-tier code outside
the ESLint restricted-syntax scope). No barrel imports; use direct module paths.
Use exactOptionalPropertyTypes-safe patterns: conditional spreads for optional
fields (e.g. `...(sev !== undefined && { severity: sev })`).
  </action>

  <verify>
    <automated>node --test tests/integration/auth-e2e.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - tests/integration/auth-e2e.test.ts exists and has exactly 3 test() blocks
    - node --test tests/integration/auth-e2e.test.ts exits 0 with pass 3, fail 0
    - npm run test:integration exits 0 (includes auth-e2e.test.ts alongside existing integration tests)
    - npm run check exits 0 with test count >= 1312 (auth-e2e.test.ts is NOT in the main test glob, so check count is unaffected)
    - npx tsc --noEmit exits 0 (no type errors in new file)
    - npx eslint tests/integration/auth-e2e.test.ts exits 0
    - Test 1 asserts: notifyCalls contains "Open https://github.com/login/device and enter: ABCD-1234" at severity "info"
    - Test 2 asserts: notifyCalls.length === 0 (silent reuse)
    - Test 3 asserts: onAuthFailure result has cancel: true; rejectCalls has 1 entry; second onAuth re-triggers Device Flow prompt
  </acceptance_criteria>

  <done>3 integration tests pass; npx tsc --noEmit and npx eslint both exit 0; npm run check stays GREEN at >= 1312 tests</done>
</task>

<task type="auto">
  <name>Task 2: Mark AUTH-01 through AUTH-10 as [x] in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md -- current content; all 10 AUTH-* lines currently show "[ ]"
  </read_first>

  <action>
Update .planning/REQUIREMENTS.md to mark all 10 AUTH requirements as satisfied.

Change every instance of "- [ ] **AUTH-" to "- [x] **AUTH-" for AUTH-01 through AUTH-10.

Also update the Traceability table at the bottom: change all "Pending" entries to "Satisfied" for AUTH-01 through AUTH-10.

The deferred requirements section (AUTH-D01, AUTH-D02, AUTH-D03) and the Out of
Scope table are unchanged.

After editing, the file should contain:
  - [x] **AUTH-01**: User can run `marketplace add <private-github-url>`...
  - [x] **AUTH-02**: User can run `marketplace update <name>`...
  - [x] **AUTH-03**: During Device Flow, user is shown a one-time code...
  - [x] **AUTH-04**: Device Flow polling respects the server-specified interval...
  - [x] **AUTH-05**: Device Flow timeout or access_denied produces a clear...
  - [x] **AUTH-06**: Successful Device Flow stores the token in the OS keychain...
  - [x] **AUTH-07**: A rejected stored token is evicted...
  - [x] **AUTH-08**: Subsequent add/update against the same host reuse the stored token...
  - [x] **AUTH-09**: The access token never appears in state.json...
  - [x] **AUTH-10**: `npm run check` stays green; duplicate GitCredentials type...

Traceability table: change all 10 rows from "Pending" to "Satisfied".

Do NOT change anything else in the file (header, deferred requirements, out-of-scope table, coverage statistics).
  </action>

  <verify>
    <automated>grep -c '\- \[x\] \*\*AUTH-' .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - grep -c '\- \[x\] \*\*AUTH-' .planning/REQUIREMENTS.md outputs 10
    - grep -c '\- \[ \] \*\*AUTH-' .planning/REQUIREMENTS.md outputs 0
    - grep -c 'Satisfied' .planning/REQUIREMENTS.md outputs 10 (one per traceability row)
    - grep -c 'Pending' .planning/REQUIREMENTS.md outputs 0
    - The deferred requirements (AUTH-D01, AUTH-D02, AUTH-D03) are unchanged
    - The Out of Scope table is unchanged
  </acceptance_criteria>

  <done>All 10 AUTH requirements show [x] in REQUIREMENTS.md; Traceability table shows Satisfied for all 10; no other content changed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> production module | Test imports production auth modules directly; no network, no subprocess |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Information Disclosure | auth-e2e.test.ts | accept | Test tokens ("gho_test_token_e2e") are clearly non-production mock values; no real tokens used. AUTH-09 inline guard in each test asserts no production token strings in notifyCalls. |
| T-36-SC | Tampering | npm installs | accept | No new npm dependencies; this plan adds only test code and a documentation update. |
</threat_model>

<verification>
## Phase Gate Checks

1. `npm run check` exits 0 with >= 1312 tests (baseline from Phase 35)
2. `npm run test:integration` exits 0 (all 3 auth-e2e tests pass; existing integration tests unaffected)
3. `grep -c '\- \[x\] \*\*AUTH-' .planning/REQUIREMENTS.md` outputs 10
4. `grep -c 'Pending' .planning/REQUIREMENTS.md` outputs 0
5. `npx tsc --noEmit` exits 0 (no type errors introduced)
</verification>

<success_criteria>
1. `npm run check` exits 0; test count is >= 1312; no regressions from Phase 35 baseline.
2. `npm run test:integration` exits 0; auth-e2e.test.ts contributes 3 passing tests.
3. Test 1 confirms AUTH-01: fill-miss triggers Device Flow; token stored via approve; prompt is byte-exact "Open https://github.com/login/device and enter: ABCD-1234" at info severity.
4. Test 2 confirms AUTH-02/AUTH-08: stored cred reused silently; zero Device Flow notify calls.
5. Test 3 confirms AUTH-07: onAuthFailure evicts cred (rejectCalls.length === 1, store empty); subsequent onAuth re-triggers Device Flow (notifyCalls has prompt entry; approveCalls has fresh token).
6. REQUIREMENTS.md: all 10 AUTH requirements are [x]; Traceability table shows Satisfied for all 10.
</success_criteria>

<output>
Create `.planning/phases/36-integration-gate-all-auth/36-01-SUMMARY.md` when done.
</output>
