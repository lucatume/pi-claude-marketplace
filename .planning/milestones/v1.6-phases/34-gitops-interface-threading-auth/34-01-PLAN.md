---
phase: 34-gitops-interface-threading-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
  - tests/orchestrators/marketplace/shared.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "GitOps.clone and GitOps.fetch each accept an optional auth bundle of shape { credentialOps: CredentialOps; host: string; onAuthRequired: OnAuthRequiredFn }"
    - "DEFAULT_GIT_OPS.clone and DEFAULT_GIT_OPS.fetch forward the optional auth bundle to platform/git.ts so buildAuthCallbacks fires when present"
    - "refreshGitHubClone accepts an optional auth bundle (5th positional parameter) and forwards it into gitOps.fetch"
    - "All existing refreshGitHubClone call sites in update.ts (marketplace + plugin) keep compiling without modification (auth is optional)"
    - "All existing makeMockGitOps consumers keep compiling without modification (auth is optional on the interface)"
    - "npm run check exits 0"
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts"
      provides: "GitOps interface with optional auth on clone/fetch; refreshGitHubClone with optional auth bundle; DEFAULT_GIT_OPS verified pass-through"
      contains: "auth?:"
    - path: "tests/orchestrators/marketplace/shared.test.ts"
      provides: "Auth-threading unit tests proving refreshGitHubClone forwards the auth bundle into gitOps.fetch"
      contains: "auth"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts"
      to: "extensions/pi-claude-marketplace/platform/git.ts"
      via: "type import of CredentialOps + OnAuthRequiredFn for the GitOps auth bundle"
      pattern: "from \"\\.\\./\\.\\./platform/git(-credential)?\\.ts\""
    - from: "tests/orchestrators/marketplace/shared.test.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts"
      via: "import of refreshGitHubClone and GitOps for type-asserting the auth seam"
      pattern: "refreshGitHubClone"
---

<objective>
Widen the `GitOps` interface in `orchestrators/marketplace/shared.ts` so `clone` and `fetch` each accept an optional `auth?: { credentialOps; host; onAuthRequired }` bundle matching the shape already accepted by `platform/git.ts` `CloneOptions.auth?` / `FetchOptions.auth?` (Phase 33). Verify `DEFAULT_GIT_OPS` passes the bundle through, and add an optional `auth?` 5th parameter to `refreshGitHubClone` that is forwarded into `gitOps.fetch`. Add unit tests that lock the auth-threading behavior on `refreshGitHubClone` without changing any existing call site.

Purpose: AUTH-01 / AUTH-02. Phase 35 orchestrators (`add.ts`, `update.ts`) need to inject an auth callback through the orchestrator-tier git surface without violating the D-13 boundary (no isomorphic-git import in orchestrator code) or breaking existing public-only call sites. This phase widens the contract; Phase 35 wires the closures at the orchestrator call sites.

Output:
- `orchestrators/marketplace/shared.ts`: `GitOps.clone` + `GitOps.fetch` accept `auth?`; `DEFAULT_GIT_OPS` verified; `refreshGitHubClone` adds optional `auth?` parameter and threads it into `gitOps.fetch`.
- `tests/orchestrators/marketplace/shared.test.ts`: New tests cover (a) `refreshGitHubClone` without `auth` calls `gitOps.fetch` without an `auth` field (backward-compat) and (b) `refreshGitHubClone` with an `auth` bundle forwards the same bundle into `gitOps.fetch`.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Phase 33 landed the platform/git.ts auth callbacks + types Phase 34 imports
@extensions/pi-claude-marketplace/platform/git.ts

# Phase 31 landed the CredentialOps interface referenced via the auth bundle
@extensions/pi-claude-marketplace/platform/git-credential.ts

# Current state of the file Phase 34 modifies
@extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts

# Existing test infrastructure
@tests/orchestrators/marketplace/shared.test.ts
@tests/helpers/git-mock.ts
@tests/helpers/credential-mock.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen GitOps + refreshGitHubClone auth seam (per AUTH-01, AUTH-02)</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts (full file -- lines 1-484)
    - extensions/pi-claude-marketplace/platform/git.ts (lines 39-75 for CloneOptions/FetchOptions auth shape; lines 246-282 for AuthAttemptResult / OnAuthRequiredFn / BuildAuthCallbacksOpts type definitions)
    - extensions/pi-claude-marketplace/platform/git-credential.ts (lines 58-65 for CredentialOps interface)
  </read_first>
  <action>
    In `extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts`:

    1. Add a type-only import for `CredentialOps` and `OnAuthRequiredFn` from the platform layer. Put them with the existing `type` imports near the top (after the existing `import type { ScopedLocations } from "../../persistence/locations.ts";` block). Exact import statements:
       - `import type { CredentialOps } from "../../platform/git-credential.ts";`
       - `import type { OnAuthRequiredFn } from "../../platform/git.ts";`
       The existing value import `import * as defaultGit from "../../platform/git.ts";` stays as-is.

    2. Define and export a type alias `GitAuthBundle` immediately above the `GitOps` interface (so both the interface and `refreshGitHubClone` can reuse a single name; do NOT inline the literal twice). Exact shape:
       ```
       /**
        * Phase 34 (v1.6): optional auth bundle passed through GitOps.clone /
        * GitOps.fetch and refreshGitHubClone. Mirrors the shape accepted by
        * platform/git.ts `CloneOptions.auth?` / `FetchOptions.auth?`. When
        * undefined, every call site behaves identically to the pre-v1.6
        * public-only path.
        *
        * D-13 boundary: this re-exports only TYPES from the platform tier
        * (`CredentialOps`, `OnAuthRequiredFn`) -- no isomorphic-git symbol
        * crosses into the orchestrator tier.
        */
       export interface GitAuthBundle {
         readonly credentialOps: CredentialOps;
         readonly host: string;
         readonly onAuthRequired: OnAuthRequiredFn;
       }
       ```

    3. Widen `GitOps.clone` and `GitOps.fetch` to accept an optional `auth?: GitAuthBundle` field on their opts argument. The two method signatures, after the change, MUST be:
       ```
       clone(opts: {
         dir: string;
         url: string;
         ref?: string;
         singleBranch?: boolean;
         auth?: GitAuthBundle;
       }): Promise<void>;
       fetch(opts: {
         dir: string;
         remote?: string;
         ref?: string;
         auth?: GitAuthBundle;
       }): Promise<void>;
       ```
       Do NOT touch `forceUpdateRef`, `checkout`, `resolveRef`, or `currentBranch` -- those primitives never go to the network (D-14 step 2/3 + read-only ref resolution) and do not need auth callbacks.

    4. Update the `GitOps` JSDoc above the interface (currently lines 62-76 in the file) to note the new opt-in `auth?` seam without removing the existing D-12/D-13 / no-`pull` notes. Append a paragraph that reads exactly:
       ```
       Phase 34 (v1.6): `clone` and `fetch` each accept an optional `auth`
       bundle. When provided, `DEFAULT_GIT_OPS` forwards it to
       `platform/git.ts`, which builds the isomorphic-git
       `onAuth`/`onAuthFailure` callbacks via `buildAuthCallbacks`. When
       omitted, both primitives behave identically to the pre-v1.6
       public-only path (NFR-5 surfaces untouched).
       ```

    5. Verify `DEFAULT_GIT_OPS` pass-through. The current implementation is:
       ```
       export const DEFAULT_GIT_OPS: GitOps = {
         clone: defaultGit.clone,
         fetch: async (o): Promise<void> => {
           await defaultGit.fetch(o);
         },
         ...
       };
       ```
       Because `defaultGit.clone` and `defaultGit.fetch` already declare `auth?` on their `CloneOptions` / `FetchOptions` (Phase 33), structural typing means the widened `GitOps` interface assignment is already correct -- the bound function references propagate the new optional field transparently. CONFIRM by reading the platform module (already done in read_first) and leaving the `DEFAULT_GIT_OPS` definition byte-identical. Add a single in-line comment immediately above `clone: defaultGit.clone,` reading:
       ```
       // Phase 34 (v1.6): the `auth?` field added to GitOps.clone / .fetch is
       // structurally compatible with platform/git.ts CloneOptions.auth? /
       // FetchOptions.auth?. No wrapper code change is required -- the
       // bound function references already accept the widened opts shape.
       ```

    6. Extend `refreshGitHubClone`'s signature to accept an optional `auth?: GitAuthBundle` as a NEW 5th positional parameter (after `onFetchSucceeded?`), and forward it into the single `gitOps.fetch(...)` call (`refreshGitHubClone` does NOT call `gitOps.clone` -- the clone happens in `add.ts`, so only `fetch` needs the auth bundle). The final signature MUST be:
       ```
       export async function refreshGitHubClone(
         cloneDir: string,
         storedRef: string | undefined,
         gitOps: GitOps,
         onFetchSucceeded?: () => void,
         auth?: GitAuthBundle,
       ): Promise<void>
       ```
       Update the `gitOps.fetch` call so the spread emits `auth` only when set:
       ```
       await gitOps.fetch({
         dir: cloneDir,
         remote: "origin",
         ...(storedRef !== undefined && { ref: storedRef }),
         ...(auth !== undefined && { auth }),
       });
       ```
       Do NOT change any other line in `refreshGitHubClone`. Do NOT change ANY existing call site of `refreshGitHubClone` in this phase (Phase 35 owns the call-site updates in `orchestrators/marketplace/update.ts` and `orchestrators/plugin/update.ts`). Both existing call sites pass 3 or 4 args; the new optional 5th parameter is backward-compatible.

    7. Refresh the JSDoc block immediately above `refreshGitHubClone` (currently lines 127-136) by appending a paragraph that reads exactly:
       ```
       Phase 34 (v1.6): the optional `auth` parameter is forwarded to
       `gitOps.fetch` so private-repository refreshes can trigger Device
       Flow on a credential miss. `gitOps.clone` is not called from here
       (Phase 35's `add.ts` is the only caller of clone); the auth bundle
       therefore only flows into the fetch primitive within this helper.
       ```

    Do NOT touch any other helper in `shared.ts` (e.g. `cascadeUnstagePlugin`, `resolveScopeFromState`, `applyAutoupdateFlipInPlace`, `loadVisibleMarketplaces`, `renderPartition`, the error classes). The change is scoped to the GitOps interface, DEFAULT_GIT_OPS comment, and `refreshGitHubClone`.

    AUTH-09 reminder: do NOT add any logging or notify hook around the auth bundle in this file. The bundle is forwarded by reference into `gitOps.fetch`; downstream `platform/git.ts::buildAuthCallbacks` already drops credential fields from error messages on the floor (CP-10).
  </action>
  <acceptance_criteria>
    - `grep -n "export interface GitAuthBundle" extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` returns exactly 1 line.
    - `grep -n "auth?: GitAuthBundle" extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` returns at least 3 lines (one each in GitOps.clone, GitOps.fetch, refreshGitHubClone signature).
    - `grep -n "from \"\\.\\./\\.\\./platform/git-credential.ts\"" extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` returns exactly 1 line and is a type-only import (`import type`).
    - `grep -n "OnAuthRequiredFn" extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` returns at least 1 line and the import is `import type`.
    - `grep -c "isomorphic-git" extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` returns 0 (D-13 boundary preserved).
    - `grep -n "refreshGitHubClone" extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` shows the function signature has 5 parameters with `auth?: GitAuthBundle` as the last.
    - `grep -n "...(auth !== undefined && { auth })" extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` returns exactly 1 line (auth forwarded into the gitOps.fetch call inside refreshGitHubClone).
    - `DEFAULT_GIT_OPS` body remains byte-identical except for the new comment line referenced in action step 5; the property bindings `clone: defaultGit.clone,` and the arrow-wrapped `fetch:` remain unchanged.
    - `grep -n "gitOps.clone" extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` returns 0 lines inside refreshGitHubClone (proves the helper does not call clone; only the GitOps interface mentions clone).
    - `npx tsc --noEmit` (typecheck only) exits 0 -- the widened interface compiles cleanly against unchanged call sites in `orchestrators/marketplace/update.ts`, `orchestrators/plugin/update.ts`, `tests/helpers/git-mock.ts`, and `tests/orchestrators/marketplace/shared.test.ts`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && npx tsc --noEmit</automated>
  </verify>
  <done>
    `shared.ts` declares `GitAuthBundle`, widens `GitOps.clone`/`fetch` with `auth?: GitAuthBundle`, threads `auth?` through `refreshGitHubClone` into `gitOps.fetch`, leaves `DEFAULT_GIT_OPS` structurally pass-through (verified), and TypeScript compiles cleanly with NO modification to existing call sites.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lock auth-threading behavior with refreshGitHubClone unit tests (per AUTH-01, AUTH-02)</name>
  <files>tests/orchestrators/marketplace/shared.test.ts</files>
  <read_first>
    - tests/orchestrators/marketplace/shared.test.ts (full file -- lines 1-134; the existing `makeStubGitOps` pattern is the template to extend)
    - tests/helpers/credential-mock.ts (full file -- lines 1-40 already read; need full file for `makeMockCredentialOps` signature)
    - extensions/pi-claude-marketplace/platform/git.ts (lines 246-282 for `AuthAttemptResult` / `OnAuthRequiredFn` shape)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts (the version just modified by Task 1 -- to know the new `GitAuthBundle` export name)
  </read_first>
  <action>
    Append THREE new tests to `tests/orchestrators/marketplace/shared.test.ts` (do NOT rewrite or remove the 3 existing tests). The tests must lock the auth-threading contract on `refreshGitHubClone`.

    First, extend the local `makeStubGitOps` so each call records its `opts` verbatim. The existing `fetchCalls` log only records a counter (`log.fetch: number`). Replace it with an array of `{ dir: string; remote?: string; ref?: string; auth?: GitAuthBundle }` records so the new tests can assert on the auth field. Update the `fetch` stub method body to:
    ```
    async fetch(args): Promise<void> {
      log.fetch.push({ ...args });
      await Promise.resolve();
    },
    ```
    And update the `CallLog` interface to:
    ```
    interface CallLog {
      fetch: { dir: string; remote?: string; ref?: string; auth?: GitAuthBundle }[];
      ...
    }
    ```
    Then update the 3 EXISTING tests' assertions on `log.fetch` from `assert.equal(log.fetch, 1)` to `assert.equal(log.fetch.length, 1)`. This is a mechanical rewrite -- no behavior change in the existing assertions, just the shape of the counter.

    Import `GitAuthBundle` as a type alongside `GitOps` and `refreshGitHubClone` at the top of the file:
    ```
    import type {
      GitAuthBundle,
      GitOps,
    } from "../../../extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts";
    ```
    Import the test helper for credentials:
    ```
    import { makeMockCredentialOps } from "../../helpers/credential-mock.ts";
    ```
    (Verify the export name in the credential-mock file during read_first; if the helper is named differently, use the actual export.)

    Now add these THREE new tests AFTER the existing three:

    Test A -- "refreshGitHubClone without auth omits the auth field on gitOps.fetch":
    - Build the stub with `resolveRefReturns: "abc...".repeat(...)` (any 40-hex string).
    - Call `await refreshGitHubClone("/tmp/clone-dir", "main", gitOps);` (4-arg form, no auth -- proves the existing pre-v1.6 calling convention is preserved).
    - Assert `log.fetch.length === 1`.
    - Assert the recorded fetch call has `auth === undefined` (i.e. the spread did NOT add the field).
    - Use `assert.equal(log.fetch[0]?.auth, undefined)` to lock the absent-field semantics.

    Test B -- "refreshGitHubClone with auth bundle forwards the same bundle into gitOps.fetch":
    - Build a real auth bundle: `const credentialOps = makeMockCredentialOps().credentialOps;` (or whatever the helper actually returns -- adjust to its actual signature).
    - Build an `onAuthRequired` stub that returns `Promise.resolve<AuthAttemptResult>({ ok: false, reason: "not invoked", authAttempted: true })` (the stub never runs since the fetch stub does not consult the bundle; it is purely passed through).
    - Build the auth bundle: `const auth: GitAuthBundle = { credentialOps, host: "github.com", onAuthRequired };`
    - Build the stub gitOps with `resolveRefReturns: "<some 40-hex>"`.
    - Call `await refreshGitHubClone("/tmp/clone-dir", "main", gitOps, undefined, auth);` (5-arg form with `onFetchSucceeded` left undefined, `auth` passed).
    - Assert `log.fetch.length === 1`.
    - Assert the recorded fetch call's `auth` field is `===` (reference-equal) to the `auth` bundle passed in -- proves the bundle was forwarded BY REFERENCE without re-bundling. `assert.strictEqual(log.fetch[0]?.auth, auth)`.
    - Assert `log.fetch[0]?.auth?.host === "github.com"` and `log.fetch[0]?.auth?.credentialOps === credentialOps` and `log.fetch[0]?.auth?.onAuthRequired === onAuthRequired` (locks each component).
    - Assert the `onAuthRequired` stub was NEVER called (the fetch stub does not consult the bundle; this is a pure threading test).

    Test C -- "refreshGitHubClone with auth bundle invokes onFetchSucceeded after gitOps.fetch":
    - Build the same auth bundle as Test B.
    - Build the stub gitOps with `resolveRefReturns: "<some 40-hex>"`.
    - Build an `onFetchSucceeded` spy: `let fetchSucceededCount = 0; const onFetchSucceeded = () => { fetchSucceededCount++; };`.
    - Call `await refreshGitHubClone("/tmp/clone-dir", "main", gitOps, onFetchSucceeded, auth);`.
    - Assert `fetchSucceededCount === 1` (proves passing both the 4th and 5th optional args together still works).
    - Assert `log.fetch.length === 1` and the recorded auth bundle is the same reference as before.

    AUTH-09 reminder: do NOT put any literal password / token string into the auth bundle in these tests. The `credentialOps` is a mock that does not need to be exercised. If the existing `makeMockCredentialOps` helper requires initial credentials, either pass an empty store or fabricate a non-sensitive placeholder like `username: "test-user"` (no `password` field) -- the test never invokes `.fill()`, so the placeholder value never leaves the test.

    Do NOT add tests against `gitOps.clone` here. The current `refreshGitHubClone` does not call clone (verified in Task 1 read_first), so clone-side auth threading would have nothing to assert against this helper. Clone-side auth is exercised in Phase 33's `tests/platform/git-auth-callbacks.test.ts` and will be re-exercised in Phase 35 `add.ts` tests.
  </action>
  <acceptance_criteria>
    - `grep -c "refreshGitHubClone:" tests/orchestrators/marketplace/shared.test.ts` returns at least 6 (3 original tests + 3 new tests).
    - `grep -n "without auth omits the auth field" tests/orchestrators/marketplace/shared.test.ts` returns exactly 1 line.
    - `grep -n "forwards the same bundle" tests/orchestrators/marketplace/shared.test.ts` returns exactly 1 line.
    - `grep -n "invokes onFetchSucceeded" tests/orchestrators/marketplace/shared.test.ts` returns exactly 1 line.
    - `grep -n "GitAuthBundle" tests/orchestrators/marketplace/shared.test.ts` returns at least 2 lines (one in the type-import, one in the CallLog type).
    - `grep -n "makeMockCredentialOps" tests/orchestrators/marketplace/shared.test.ts` returns at least 1 line (used in Tests B and C).
    - `grep -E "log\\.fetch === 1|assert\\.equal\\(log\\.fetch, 1\\)" tests/orchestrators/marketplace/shared.test.ts` returns 0 lines (the old counter form is fully replaced with `log.fetch.length`).
    - `assert.strictEqual(log.fetch[0]?.auth, auth)` appears in Test B (proves reference equality, not deep equality).
    - The `npm test` command exits 0 with the new tests passing.
    - The 3 existing tests still pass (no regression on the pre-v1.6 surface).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && node --test tests/orchestrators/marketplace/shared.test.ts</automated>
  </verify>
  <done>
    `tests/orchestrators/marketplace/shared.test.ts` contains 6 tests (3 original + 3 new auth-threading tests); `node --test` against the file is GREEN; the auth bundle is asserted to be forwarded by reference into `gitOps.fetch` and the `onAuthRequired` callback is verified to NOT fire during a pure threading test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator → platform | Orchestrator-tier code (`shared.ts`) calls into `platform/git.ts` via the structural `GitOps` interface; an auth bundle (`credentialOps`, `host`, `onAuthRequired`) crosses here. The bundle's `credentialOps` holds shell-out authority (Phase 31), but only `platform/git-credential.ts` is whitelisted by D-21 to invoke `node:child_process` -- the orchestrator only forwards the reference. |
| platform → isomorphic-git | `platform/git.ts::buildAuthCallbacks` (Phase 33) is the only seam that hands credentials to isomorphic-git; orchestrator code never touches the library directly (D-13). |
| test → production code | `tests/orchestrators/marketplace/shared.test.ts` injects a stub `GitOps` (no real fetch/clone) and a mock `CredentialOps` (no real subprocess) -- the developer's OS keychain and the network are both untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Information Disclosure | `refreshGitHubClone` forwarding `auth.credentialOps` / token-bearing `GitCredentials` | mitigate | The auth bundle is forwarded BY REFERENCE without serialization, interpolation into Error / log lines, or notify calls. No `console.log`, `JSON.stringify`, or template literal in `shared.ts` references the bundle. Acceptance grep `grep -c "auth.credentialOps\\|auth.onAuthRequired" extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` returns 0 outside the type definition and the single forwarding spread. AUTH-09 holds. |
| T-34-02 | Information Disclosure | Test fixtures embedding a real token in `auth` bundle | mitigate | Test bodies use the `makeMockCredentialOps` mock and never construct a `GitCredentials` with a `password` field. Test C is a pure threading test -- the stub `onAuthRequired` returns `{ ok: false, reason: "not invoked" }` and is asserted to NEVER be invoked. The string `"not invoked"` is the only literal in the auth bundle and carries no credential material. |
| T-34-03 | Tampering | Backward-compatibility regression: existing call sites silently break when interface widens | mitigate | The `auth?` field is optional on every signature change (interface methods + `refreshGitHubClone` parameter). Acceptance criteria run `npx tsc --noEmit` against the unchanged Phase 33 / Phase 4 call sites in `update.ts` (both marketplace and plugin) and `tests/helpers/git-mock.ts` to prove the widening is non-breaking. The architecture test `tests/architecture/no-shell-out.test.ts` is NOT modified -- it already whitelists `platform/git-credential.ts` and forbids `node:child_process` everywhere else (Phase 31). |
| T-34-04 | Elevation of Privilege | A future caller passes a mismatched `host` so a token is leaked across hosts | accept | Phase 34 only widens the seam; the binding of `host` happens at the orchestrator call site in Phase 35 (`addGithubInGuard`, `refreshRecord`). Phase 35's threat model owns the per-call-site `host` normalization audit. |
| T-34-SC | Tampering | Package legitimacy | n/a | No npm install in this phase -- pure source-code changes to two existing TypeScript files. The `## Package Legitimacy Audit` requirement does not apply (no `npm install` task). |
</threat_model>

<verification>
- `npm run check` exits 0 (typecheck + ESLint + Prettier + tests) -- the canonical project-wide gate.
- Existing call sites in `orchestrators/marketplace/update.ts` (lines 116 + 315) and `orchestrators/plugin/update.ts` (lines 91 + 267) compile and continue to pass their existing tests without modification.
- Existing `makeMockGitOps` consumers in `tests/orchestrators/marketplace/add.test.ts`, `tests/orchestrators/marketplace/update.test.ts`, `tests/orchestrators/marketplace/list.test.ts`, etc. compile without modification (the widening is structurally additive).
- The new 3 tests added to `tests/orchestrators/marketplace/shared.test.ts` all pass via `node --test`.
- No new `node:child_process` import is introduced anywhere -- D-21 boundary preserved.
- No `isomorphic-git` import is introduced into `orchestrators/marketplace/shared.ts` -- D-13 boundary preserved.
- AUTH-09: `grep -E "console\\.(log|warn|error)|notify\\(|\\$\\{.*auth.*\\}|JSON\\.stringify\\(auth" extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` returns 0 lines (no logging or interpolation of the auth bundle).
</verification>

<success_criteria>
1. `GitOps.clone` and `GitOps.fetch` signatures each accept the optional `auth?: GitAuthBundle` field.
2. `GitAuthBundle` is exported from `orchestrators/marketplace/shared.ts` as `{ credentialOps: CredentialOps; host: string; onAuthRequired: OnAuthRequiredFn }` with type-only imports from the platform tier (D-13 preserved).
3. `DEFAULT_GIT_OPS.clone` and `DEFAULT_GIT_OPS.fetch` pass the optional `auth` bundle through to `platform/git.ts` via structural typing -- byte-identical wrapper code except for the explanatory comment.
4. `refreshGitHubClone(cloneDir, storedRef, gitOps, onFetchSucceeded?, auth?)` -- the function signature gains a 5th optional `auth?: GitAuthBundle` parameter and threads it into the single `gitOps.fetch(...)` call.
5. All existing call sites of `refreshGitHubClone` compile and pass tests WITHOUT modification.
6. 3 new unit tests in `tests/orchestrators/marketplace/shared.test.ts` lock the auth-threading contract: absent-bundle backward compat, by-reference forwarding, and concurrent `onFetchSucceeded + auth` threading. All pass.
7. `npm run check` exits 0.
8. AUTH-09 is preserved: no log line, error message, or notify call in `shared.ts` references the auth bundle.
</success_criteria>

<output>
Create `.planning/phases/34-gitops-interface-threading-auth/34-01-SUMMARY.md` when done.
</output>
