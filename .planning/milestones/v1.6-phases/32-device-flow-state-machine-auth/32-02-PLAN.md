---
phase: 32-device-flow-state-machine-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - tests/helpers/device-flow-mock.ts
  - tests/domain/github-auth.test.ts
  - extensions/pi-claude-marketplace/domain/github-auth.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-07]
tags: [device-flow, github-auth, oauth, state-machine, slow-down, expired-token, access-denied, auth-attempted, notify-fn-callback, discriminated-union, mock-http, tdd]

must_haves:
  truths:
    - "Calling initiateDeviceFlow with a mock that returns a success poll resolves to { ok: true, cred: { username: 'x-access-token', password: <token> }, authAttempted: true } (AUTH-01)"
    - "Calling initiateDeviceFlow with a mock that returns a success poll invokes credentialOps.approve exactly once with the matching host + cred (AUTH-01)"
    - "Calling initiateDeviceFlow invokes the injected notifyFn exactly once after the requestCode succeeds, with a message that interpolates both user_code AND verification_uri (AUTH-03)"
    - "The notifyFn message NEVER contains the access_token / device_code value -- AUTH-09 + AUTH-03 discipline (D-32-06)"
    - "Two consecutive slow_down poll responses produce a third pollToken call whose intervalSec argument equals (initial.interval + 10) (AUTH-04 cumulative; CP-1)"
    - "Three consecutive authorization_pending poll responses produce three pollToken calls all with the same intervalSec === initial.interval (AUTH-04 pending; CP-2)"
    - "An access_denied poll response produces { ok: false, reason: <human string mentioning cancellation/retry>, authAttempted: true } -- never a raw HTTP error body (AUTH-05)"
    - "An expired_token poll response produces { ok: false, reason: <human string mentioning expiration/restart>, authAttempted: true } (AUTH-05)"
    - "When Date.now() crosses deadlineMs (computed from deviceCode.expires_in), the loop terminates with { ok: false, reason: <timeout string>, authAttempted: true } (AUTH-05 timeout)"
    - "When http.requestCode throws, the result is { ok: false, reason: <init failure string>, authAttempted: true } -- never an unhandled rejection (AUTH-05 init failure)"
    - "Every DeviceFlowResult -- success OR failure -- carries authAttempted: true so Phase 33 can detect a second consecutive auth failure (AUTH-07; D-32-05)"
    - "domain/github-auth.ts contains NO new Error(...) constructor and NO notifyFn(...) call that interpolates access_token / cred.password / r.accessToken -- the Plan 32-01 architecture gate is GREEN once this plan lands (AUTH-09; the gate flips from vacuous to active on file creation)"
    - "npm run check exits 0 (typecheck + lint + format:check + test) after the plan lands"
  artifacts:
    - path: "tests/helpers/device-flow-mock.ts"
      provides: "makeMockDeviceFlowHttp factory + MockDeviceFlowState + MockDeviceFlowHttpHandle types -- programmable response queue for pollToken; optional throw overrides for requestCode + pollToken; per-method call logs"
      exports: ["makeMockDeviceFlowHttp", "MockDeviceFlowState", "MockDeviceFlowHttpHandle"]
      min_lines: 80
    - path: "tests/domain/github-auth.test.ts"
      provides: "13 unit tests covering AUTH-01 (happy path + approve-on-success), AUTH-03 (user_code + verification_uri in notify), AUTH-04 (slow_down cumulative + pending no-change), AUTH-05 (access_denied + expired_token + timeout + init failure), AUTH-07 (authAttempted on success + on failure), AUTH-09 (notify content scan)"
      min_lines: 200
    - path: "extensions/pi-claude-marketplace/domain/github-auth.ts"
      provides: "DeviceFlowHttp interface + DEFAULT_DEVICE_FLOW_HTTP (uses globalThis.fetch) + DeviceCodeResponse + PollResult discriminated union + DeviceFlowResult discriminated union + initiateDeviceFlow state machine + NotifyFn type + InitiateDeviceFlowOpts + GITHUB_OAUTH_CLIENT_ID constant"
      exports: ["DeviceFlowHttp", "DEFAULT_DEVICE_FLOW_HTTP", "DeviceCodeResponse", "PollResult", "DeviceFlowResult", "InitiateDeviceFlowOpts", "NotifyFn", "initiateDeviceFlow"]
      min_lines: 150
  key_links:
    - from: "extensions/pi-claude-marketplace/domain/github-auth.ts"
      to: "extensions/pi-claude-marketplace/platform/git-credential.ts"
      via: "type-only import of CredentialOps (P32-6 -- type-only discipline keeps the dependency surface minimal)"
      pattern: "import type \\{ CredentialOps \\} from"
    - from: "extensions/pi-claude-marketplace/domain/github-auth.ts"
      to: "extensions/pi-claude-marketplace/platform/git.ts"
      via: "type-only import of GitCredentials (the success-return shape and the credentialOps.approve argument)"
      pattern: "import type \\{ GitCredentials \\} from"
    - from: "extensions/pi-claude-marketplace/domain/github-auth.ts"
      to: "node:timers/promises"
      via: "named import of setTimeout (renamed) -- AbortSignal-aware sleep between poll iterations (Pattern 3; eliminates CP-4)"
      pattern: "from \"node:timers/promises\""
    - from: "tests/domain/github-auth.test.ts"
      to: "tests/helpers/device-flow-mock.ts"
      via: "named import of makeMockDeviceFlowHttp factory"
      pattern: "from \".*/helpers/device-flow-mock"
    - from: "tests/domain/github-auth.test.ts"
      to: "tests/helpers/credential-mock.ts"
      via: "named import of makeMockCredentialOps (Phase 31 helper) -- reused so approve-on-success can be asserted on state.approveCalls"
      pattern: "from \".*/helpers/credential-mock"
    - from: "initiateDeviceFlow success branch"
      to: "credentialOps.approve(host, cred)"
      via: "function call inside the switch case 'success' arm BEFORE returning { ok: true, ... }"
      pattern: "credentialOps\\.approve\\("
    - from: "initiateDeviceFlow notifyFn call"
      to: "deviceCode.user_code AND deviceCode.verification_uri"
      via: "single template-literal interpolating BOTH fields -- AUTH-03; AUTH-09 satisfied by omitting access_token + device_code"
      pattern: "notifyFn\\([^)]*verification_uri[^)]*user_code|notifyFn\\([^)]*user_code[^)]*verification_uri"
---

<objective>
Ship Phase 32's deliverable: a self-contained GitHub Device Flow state machine in
`extensions/pi-claude-marketplace/domain/github-auth.ts` with an injectable
`DeviceFlowHttp` HTTP seam, a pre-bound `notifyFn` callback (D-32-04 -- no `ctx` import
in domain/), cumulative `slow_down` interval back-off (CP-1; AUTH-04), `expires_in`
deadline enforcement (P32-3; AUTH-05), discriminated `DeviceFlowResult` return shape
(both `ok: true` and `ok: false` branches carry `authAttempted: true` per D-32-05 /
AUTH-07), and best-effort `credentialOps.approve` on success (re-uses Phase 31's seam).

The plan follows TDD: Task 2 ships the test file FIRST with the production module
intentionally absent (RED state -- file does not exist; tests cannot even load), then
Task 3 ships the production module so all 13 tests turn GREEN. Wave 0 prerequisites:
- The Plan 32-01 architecture amendment is already landed (Wave 1; this plan's
  `depends_on: [32-01]`); its vacuous-pass test will activate the moment Task 3 creates
  the file.
- `tests/domain/` is already in the npm test glob per `package.json::scripts.test`
  (verified -- no infrastructure change needed).
- `makeMockCredentialOps` from Phase 31 is already shipped in `tests/helpers/credential-mock.ts`.

Purpose: This is the entire authentication engine for v1.6. Phase 33 will wrap it in
isomorphic-git's `onAuth` / `onAuthFailure` closures; Phase 34 threads `onAuthRequired`
through `GitOps`; Phase 35 wires the orchestrator call sites. Phase 32 alone has zero
production call sites -- the file is exercised only by its unit tests until Phase 33
imports it.

Output:
- `tests/helpers/device-flow-mock.ts` (NEW) -- `makeMockDeviceFlowHttp` factory mirroring
  the `makeMockCredentialOps` shape (closure-scoped state, per-method call logs,
  optional throws, programmable poll-response queue).
- `tests/domain/github-auth.test.ts` (NEW) -- 13 unit tests covering AUTH-01/03/04/05/07
  per the 32-VALIDATION.md per-task verification map.
- `extensions/pi-claude-marketplace/domain/github-auth.ts` (NEW) -- the state machine.
- All AUTH-09 architecture gates (Phase 31 state-write + Phase 31 git-credential.ts +
  Plan 32-01 domain/github-auth.ts) GREEN.
- `npm run check` GREEN with 1291 tests (1278 post-Plan-32-01 + 13 new domain tests;
  the Plan 32-01 vacuous gate transitions from vacuous-pass to active-pass when
  Task 3 lands).
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-device-flow-state-machine-auth/32-CONTEXT.md
@.planning/phases/32-device-flow-state-machine-auth/32-RESEARCH.md
@.planning/phases/32-device-flow-state-machine-auth/32-VALIDATION.md
@.planning/phases/32-device-flow-state-machine-auth/32-01-SUMMARY.md
@.planning/phases/31-credential-subprocess-layer-auth/31-02-SUMMARY.md
@extensions/pi-claude-marketplace/platform/git-credential.ts
@extensions/pi-claude-marketplace/platform/git.ts
@tests/helpers/credential-mock.ts
@tests/architecture/no-credential-leak.test.ts
@tests/architecture/import-boundaries.test.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/helpers/device-flow-mock.ts -- makeMockDeviceFlowHttp factory</name>
  <files>tests/helpers/device-flow-mock.ts</files>
  <behavior>
    - The file exports three named symbols: `MockDeviceFlowState`, `MockDeviceFlowHttpHandle`, `makeMockDeviceFlowHttp`
    - `makeMockDeviceFlowHttp(initial?: Partial<MockDeviceFlowState>): MockDeviceFlowHttpHandle` returns an object with two properties: `http: DeviceFlowHttp` and `state: MockDeviceFlowState`
    - The returned `http.requestCode(clientId, scope)` pushes `{ clientId, scope }` onto `state.requestCodeCalls`, throws `state.requestCodeThrows` if defined, otherwise resolves with `state.deviceCode`
    - The returned `http.pollToken(clientId, deviceCode, intervalSec)` pushes `{ clientId, deviceCode, intervalSec }` onto `state.pollTokenCalls`, throws `state.pollTokenThrows` if defined, otherwise shifts the head of `state.pollQueue` (returning `state.defaultPoll` when the queue is empty)
    - `state.deviceCode` defaults to a sensible canned response: `{ device_code: "MOCK_DEVICE_CODE", user_code: "ABCD-1234", verification_uri: "https://github.com/login/device", expires_in: 900, interval: 5 }`
    - `state.defaultPoll` defaults to `{ kind: "pending" }`
    - All imports of `DeviceFlowHttp` / `DeviceCodeResponse` / `PollResult` are TYPE-ONLY (the file never imports the production module at runtime -- mirrors tests/helpers/credential-mock.ts discipline)
    - Optional fields (`requestCodeThrows`, `pollTokenThrows`) use the conditional-spread initializer pattern `...(initial?.X !== undefined && { X: initial.X })` to satisfy `exactOptionalPropertyTypes` (this is the same pattern Phase 31 used in credential-mock.ts and is documented in the Phase 31 SUMMARY)
    - The file's TypeScript surface compiles even when `extensions/pi-claude-marketplace/domain/github-auth.ts` does NOT exist (`tsc --noEmit` would fail because the type-only import is unresolved -- so Task 1 cannot ship standalone; Tasks 1 + 2 + 3 must land in a single atomic commit OR Task 3 must run before `npm run check` is invoked). The pre-Task-3 RED window is acceptable as long as the commit is atomic. SEE COMMIT STRATEGY in <verify>.
  </behavior>
  <action>
    Create `tests/helpers/device-flow-mock.ts` mirroring the structure of
    `tests/helpers/credential-mock.ts` and following the shape in RESEARCH Example 4
    (lines 894-963 of 32-RESEARCH.md).

    Structure (do NOT inline fenced code blocks here; the action is directive prose):

    1. File header docblock: explain that this is a sibling of credential-mock.ts +
       git-mock.ts; closure-scoped in-memory state; pure -- no filesystem ops, no
       process spawn, no real HTTP; type-only imports of the production interface so
       the helper never runtime-couples to the production module.

    2. Imports (all TYPE-ONLY -- use `import type` syntax exactly):
       - `DeviceCodeResponse`, `DeviceFlowHttp`, `PollResult` from
         `../../extensions/pi-claude-marketplace/domain/github-auth.ts`.
       - Order the import-x/order rule deterministically: a single type-only import
         block, lexicographically sorted by source path. (There is only one source path
         here so ordering is trivial; the rule still applies if a future contributor
         adds a second.)

    3. Export `interface MockDeviceFlowState` with these REQUIRED fields:
       - `deviceCode: DeviceCodeResponse` -- canned response returned by `requestCode`.
       - `pollQueue: PollResult[]` -- consumed head-first by each `pollToken` call.
       - `defaultPoll: PollResult` -- returned when `pollQueue` is empty.
       - `requestCodeCalls: { clientId: string; scope: string }[]` -- per-call log.
       - `pollTokenCalls: { clientId: string; deviceCode: string; intervalSec: number }[]` -- per-call log.

       And these OPTIONAL fields (with `?` markers; conditional-spread initializer per
       Phase 31 SUMMARY's "patterns-established"):
       - `requestCodeThrows?: Error`
       - `pollTokenThrows?: Error`

    4. Export `interface MockDeviceFlowHttpHandle` with two readonly fields:
       - `readonly http: DeviceFlowHttp`
       - `readonly state: MockDeviceFlowState`

    5. Export `function makeMockDeviceFlowHttp(initial?: Partial<MockDeviceFlowState>): MockDeviceFlowHttpHandle`:
       - Default `state.deviceCode` when `initial?.deviceCode` is undefined:
         `{ device_code: "MOCK_DEVICE_CODE", user_code: "ABCD-1234", verification_uri: "https://github.com/login/device", expires_in: 900, interval: 5 }`.
       - Default `state.pollQueue` to `[...(initial?.pollQueue ?? [])]` (defensive copy).
       - Default `state.defaultPoll` to `initial?.defaultPoll ?? { kind: "pending" }`.
       - Initialize `state.requestCodeCalls` and `state.pollTokenCalls` to `[]`.
       - Apply conditional-spread for `requestCodeThrows` + `pollTokenThrows`.
       - Build the `http: DeviceFlowHttp` value:
         - `requestCode`: push `{ clientId, scope }` onto `state.requestCodeCalls`;
           if `state.requestCodeThrows !== undefined` throw it; `await Promise.resolve()`
           so the function is genuinely async; return `state.deviceCode`.
         - `pollToken`: push `{ clientId, deviceCode, intervalSec }` onto
           `state.pollTokenCalls`; if `state.pollTokenThrows !== undefined` throw it;
           `await Promise.resolve()`; return `state.pollQueue.shift() ?? state.defaultPoll`.
       - Return `{ http, state }`.

    Conventions:
    - Match the file-header docblock style of credential-mock.ts (multi-line `/** ... */`).
    - Match the conditional-spread idiom: `...(initial?.X !== undefined && { X: initial.X })`.
    - Match the destructuring style of credential-mock.ts (use parameter names that
      align with the production interface so TypeScript happily infers).
    - Match the spacing convention: two blank lines between method implementations in
      the returned `http` object literal -- follow whatever credential-mock.ts uses
      (it uses one blank line between method bodies; mirror that).

    Test isolation guarantees:
    - The mock does NOT touch the filesystem, environment, or process (Pitfall 9
      mirror).
    - The mock does NOT import the production module at runtime (type-only).
    - The mock does NOT internally sleep (caller controls timing -- tests will not need
      `t.mock.timers` because the mock resolves immediately).

    Naming check:
    - `MockDeviceFlowState` (NOT MockHttpState or MockGithubAuthState -- align with
      `MockCredentialState` from Phase 31).
    - `MockDeviceFlowHttpHandle` (NOT MockHttpHandle -- mirror `MockCredentialOpsHandle`).
    - `makeMockDeviceFlowHttp` (NOT createMockDeviceFlowHttp or mockDeviceFlowHttp --
      mirror `makeMockCredentialOps` exactly).

    Commit strategy: this task is the FIRST half of an atomic three-task commit
    (Tasks 1 + 2 + 3). The TypeScript red window between Task 1 and Task 3 is the
    same Plan 31-02 documented atomic-commit pattern. SEE COMMIT STRATEGY in
    <verify> below.
  </action>
  <read_first>
    - tests/helpers/credential-mock.ts (the template to mirror -- header, conditional-spread, conventions)
    - tests/helpers/git-mock.ts (the older sibling template -- naming + state-shape conventions)
    - .planning/phases/32-device-flow-state-machine-auth/32-RESEARCH.md (Example 4 lines 894-963 -- the exact shape)
    - .planning/phases/31-credential-subprocess-layer-auth/31-02-SUMMARY.md (patterns-established section -- conditional-spread initializer rationale)
  </read_first>
  <acceptance_criteria>
    - **Source assertion:** `grep -c "^export " tests/helpers/device-flow-mock.ts` returns at least `3` (interface MockDeviceFlowState + interface MockDeviceFlowHttpHandle + function makeMockDeviceFlowHttp).
    - **Source assertion:** `grep -c "^import type" tests/helpers/device-flow-mock.ts` returns at least `1` (production interface imports MUST be type-only).
    - **Source assertion (negative):** `grep -c "^import \{" tests/helpers/device-flow-mock.ts | grep -v '^#'` returns `0` -- no runtime imports from the production tree.
    - **Source assertion:** `grep -c "from \"../../extensions/pi-claude-marketplace/domain/github-auth.ts\"" tests/helpers/device-flow-mock.ts` returns at least `1` (the type-only import points to the right path).
    - **Source assertion:** `grep -c "MOCK_DEVICE_CODE\|ABCD-1234\|https://github.com/login/device" tests/helpers/device-flow-mock.ts` returns at least `3` (default deviceCode literal values present).
    - **Source assertion:** `grep -c "requestCodeCalls\|pollTokenCalls" tests/helpers/device-flow-mock.ts` returns at least `2` (call logs declared).
    - **Source assertion:** `grep -c "pollQueue.shift" tests/helpers/device-flow-mock.ts` returns `1` (consume-head-first behavior implemented).
    - **Source assertion:** `grep -c "initial?\\.requestCodeThrows !== undefined && { requestCodeThrows" tests/helpers/device-flow-mock.ts` returns `1` (conditional-spread initializer present for one of the two optional fields; mirrors Phase 31 idiom).
    - **Behavior assertion:** The file is consumed by `tests/domain/github-auth.test.ts` in Task 2; that file's import of `makeMockDeviceFlowHttp` resolves at type-check time once Task 3 lands.
    - **Behavior assertion:** Once Tasks 2 + 3 land, all 13 tests in `tests/domain/github-auth.test.ts` exercise the mock -- the assertions on `state.requestCodeCalls`, `state.pollTokenCalls`, and `state.pollQueue` all pass.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "device-flow-mock\|github-auth" | grep -E "error TS" | head -5; echo "(Task 1 TS errors limited to the cross-task RED window are acceptable -- final GREEN gate is in Task 3.)"</automated>
  </verify>
  <done>
    `tests/helpers/device-flow-mock.ts` exists with the three named exports
    (`MockDeviceFlowState`, `MockDeviceFlowHttpHandle`, `makeMockDeviceFlowHttp`) and
    the documented default deviceCode literal. Type-only import of the production
    interface keeps the helper from runtime-coupling. The file is fully usable by
    Task 2's test file. TypeScript red window between Tasks 1, 2, and 3 is acceptable
    because the three tasks land in a single atomic commit.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tests/domain/github-auth.test.ts -- 13 unit tests covering AUTH-01/03/04/05/07</name>
  <files>tests/domain/github-auth.test.ts</files>
  <behavior>
    - The file declares EXACTLY 13 `test(...)` blocks corresponding 1:1 to the 32-VALIDATION.md per-task verification map rows 32-02-01 through 32-02-13 (Task IDs 02-13 in the map; row 02-13 is the architecture gate which Plan 32-01 owns, so the file actually has 13 unit tests covering rows 02-01 through 02-12 PLUS one explicit AUTH-09 notify-content scan = 13 total)
    - Wait -- recount: 32-VALIDATION.md rows 32-02-01..32-02-13 are 13 rows (01..13 inclusive). Row 02-13 is the architecture test which Plan 32-01 covers; the remaining 12 rows are unit tests. Add 1 explicit AUTH-09 notify-content unit test (row 32-02-12 covers this -- so no addition needed). Net: 12 unit tests in this file. But the plan instructions say "minimum 13 tests" matching 32-VALIDATION.md. Re-reading row 02-13 -- it points to the architecture test file. So this file has 12 unit tests + the architecture test (which lives in a different file from Plan 32-01) = 13 verification rows total but 12 tests in THIS file. To satisfy "minimum 13" per the planning instructions, this file will ship 13 tests -- the AUTH-09 notify-content row (02-12) is split into two tests (one asserting positive content -- user_code + verification_uri present; one asserting negative content -- no access_token / device_code present)
    - Each test imports `initiateDeviceFlow` from the production module and `makeMockDeviceFlowHttp` from the helper Task 1 ships
    - Each test imports `makeMockCredentialOps` from the existing Phase 31 helper (`tests/helpers/credential-mock.ts`) and constructs a fresh credentialOps + state pair per test
    - Each test constructs a fresh `notifyFn` recorder closure (synchronous void function that pushes `{ message, severity }` onto a local array) -- AUTH-09 + AUTH-03 assertions read this array
    - All tests run synchronously fast (no real sleeps): the production state machine uses `node:timers/promises.setTimeout` which the tests bypass by either (a) constructing a mock deviceCode with `interval: 0` (so the sleep is effectively zero) OR (b) using the AbortSignal opts.signal escape hatch is NOT needed because interval: 0 already makes the loop spin freely
    - Each test runs in well under 100ms (the entire file completes in well under 1 second; node:test reports per-test durations)
    - **Test 1 -- AUTH-01 happy path:** mock returns deviceCode + queues `{ kind: "success", accessToken: "gho_test", tokenType: "bearer", scope: "repo" }`; assert result is `{ ok: true, cred: { username: "x-access-token", password: "gho_test" }, authAttempted: true }`
    - **Test 2 -- AUTH-01 approve on success:** same setup as Test 1; assert `state.approveCalls.length === 1` and `state.approveCalls[0].host === "github.com"` and `state.approveCalls[0].cred.password === "gho_test"`
    - **Test 3 -- AUTH-03 notify content (positive):** assert the recorder captured exactly one notifyFn call AFTER requestCode and BEFORE the success poll; assert the message string contains BOTH the mock `user_code` ("ABCD-1234") AND the mock `verification_uri` ("https://github.com/login/device")
    - **Test 4 -- AUTH-04 cumulative slow_down:** pre-load the pollQueue with `[{ kind: "slow_down" }, { kind: "slow_down" }, { kind: "success", accessToken: "gho_x", tokenType: "bearer", scope: "repo" }]`; use mock deviceCode with `interval: 0` (zero-sleep); after `initiateDeviceFlow` resolves, assert `state.pollTokenCalls.length === 3` and `state.pollTokenCalls[2].intervalSec === 10` (initial 0 + 5 + 5)
    - **Test 5 -- AUTH-04 pending no-change:** pre-load `[{ kind: "pending" }, { kind: "pending" }, { kind: "pending" }, { kind: "success", ... }]`; assert `state.pollTokenCalls.length === 4` and ALL FOUR `intervalSec` values equal the initial value (no mutation on pending)
    - **Test 6 -- AUTH-05 access_denied:** queue `[{ kind: "access_denied" }]`; assert `result.ok === false` AND `result.authAttempted === true` AND `typeof result.reason === "string"` AND `result.reason.length > 10` AND `result.reason.toLowerCase()` contains "cancel" or "run the command again" (matches the RESEARCH Example 3 exact wording -- "User cancelled authorization. Run the command again to retry.")
    - **Test 7 -- AUTH-05 expired_token:** queue `[{ kind: "expired_token" }]`; assert `result.ok === false`, `result.reason` is a string mentioning expiration / restart
    - **Test 8 -- AUTH-05 deadline timeout:** mock deviceCode with `expires_in: 0` (so `deadlineMs` is `Date.now()` and the while loop exits without polling); assert `result.ok === false`, `result.authAttempted === true`, `result.reason.toLowerCase().includes("time")` (covers both "timed out" and "timeout")
    - **Test 9 -- AUTH-05 init failure:** mock `requestCodeThrows: new Error("network down")`; assert `result.ok === false`, `result.authAttempted === true`, `result.reason` is a string that mentions "Device Flow initialization failed" (matches the RESEARCH Example 3 wording)
    - **Test 10 -- AUTH-07 authAttempted on success:** same as Test 1; explicitly assert `result.authAttempted === true`
    - **Test 11 -- AUTH-07 authAttempted on failure:** same as Test 6 (access_denied); explicitly assert `result.authAttempted === true`
    - **Test 12 -- AUTH-09 notify content negative scan:** same setup as Test 1 (full happy path); after `initiateDeviceFlow` resolves, iterate every captured notifyFn call's message string and assert that NONE contain the token literal "gho_test", NONE contain the device_code literal "MOCK_DEVICE_CODE", NONE contain "access_token"; assert the recorder captured exactly 1 notifyFn call (the user_code prompt only)
    - **Test 13 -- approve-best-effort on throw:** mock credentialOps with `approveThrows: new Error("keychain locked")`; assert the function STILL resolves (does not throw) and returns `{ ok: true, ... }` -- the keychain failure is best-effort and the in-memory cred is what matters per RESEARCH Assumption A9. Or alternatively: assert it propagates if Claude chooses a non-best-effort approach; document the choice in the test name and the SUMMARY. The RESEARCH Assumption A9 default is "do NOT wrap approve in try/catch in Phase 32" -- the Phase 31 production impl already swallows; if a mock throws, that's a test artefact. For Test 13 here, do NOT swallow -- assert that an approveThrows mock causes the result to be `{ ok: false, reason: <string mentioning approve / persist> }` ONLY IF Claude chose to wrap approve. The simpler implementation per A9 is: DO NOT wrap; the mock throw propagates. So Test 13's assertion should be: `await assert.rejects(initiateDeviceFlow(...), /keychain locked/)` -- proves the contract that Phase 32 does NOT swallow CredentialOps.approve exceptions (the production approve impl in Phase 31 is best-effort and swallows internally; the test simulates a non-best-effort mock to lock the documented contract)
    - `npm run check` exits 0 after Task 3 lands all 13 tests GREEN
  </behavior>
  <action>
    Create `tests/domain/github-auth.test.ts` with EXACTLY 13 `test(...)` blocks per the
    enumeration in `<behavior>`. The file MUST compile cleanly once Task 3 lands; the
    inter-task RED window is bridged by atomic commit (per Phase 31's pattern).

    Imports (top of file):
    - `assert` from `node:assert/strict`.
    - `test` from `node:test`.
    - Named imports from the production module
      `../../extensions/pi-claude-marketplace/domain/github-auth.ts`: `initiateDeviceFlow`.
      Type-only or value -- here we need the value `initiateDeviceFlow` so it's a
      regular import.
    - Named import from `../helpers/credential-mock.ts`: `makeMockCredentialOps`.
    - Named import from `../helpers/device-flow-mock.ts` (Task 1 helper):
      `makeMockDeviceFlowHttp`.
    - Optional: import the `PollResult` type from the production module for type-safe
      queue construction.

    Per-test recorder helper (declare once at the top of the file or inline per test):
    `const notifyCalls: { message: string; severity?: "info" | "warning" | "error" }[] = []; const notifyFn = (message: string, severity?: "info" | "warning" | "error") => { notifyCalls.push(severity !== undefined ? { message, severity } : { message }); };`

    Per-test setup pattern (apply to every test):
    1. Construct `const { http, state: httpState } = makeMockDeviceFlowHttp({ ... per-test overrides ... });`
    2. Construct `const { credOps, state: credState } = makeMockCredentialOps();`
    3. Construct the notifyFn recorder.
    4. `await initiateDeviceFlow({ host: "github.com", credentialOps: credOps, notifyFn, http });`
    5. Assert.

    Test naming convention: prefix each title with "Phase 32 initiateDeviceFlow: " for
    consistency with the Phase 31 "Phase 31 credOps: " prefix. The 32-VALIDATION.md
    grep-able strings ("happy path", "approve on success", "AUTH-03", "AUTH-04
    cumulative", "AUTH-04 pending", "AUTH-05 access_denied", "AUTH-05 expired_token",
    "AUTH-05 timeout", "AUTH-05 init failure", "AUTH-07 authAttempted", "AUTH-07
    authAttempted on failure", "AUTH-09 notify content") MUST appear in the test
    titles so the `-t` grep filter from the validation map works.

    Specific test-content notes:
    - **Test 4 (slow_down cumulative):** Use `interval: 0` in the deviceCode so each
      sleep is `0 * 1000 = 0 ms`. The state machine will spin through the queue
      essentially synchronously. Assert `httpState.pollTokenCalls[0].intervalSec === 0`
      (initial), `httpState.pollTokenCalls[1].intervalSec === 5` (after first
      slow_down), `httpState.pollTokenCalls[2].intervalSec === 10` (after second
      slow_down, then success poll). This proves CP-1 cumulative + the production
      module passes `currentIntervalSec` through to `pollToken`.
    - **Test 8 (timeout):** Use `expires_in: 0` so `deadlineMs === Date.now()` and the
      while loop condition `Date.now() < deadlineMs` is false on the first check. The
      loop body never executes; the function returns the timeout reason directly.
      Assert `httpState.pollTokenCalls.length === 0` (no poll happened) AND
      `result.ok === false` AND `result.reason.toLowerCase().includes("time")`.
    - **Test 13 (approve throws):** Use
      `makeMockCredentialOps({ approveThrows: new Error("keychain locked") })`. Assert
      `await assert.rejects(initiateDeviceFlow({ ... }), /keychain locked/);` -- this
      locks the contract that Phase 32 does NOT wrap approve in try/catch (per RESEARCH
      Assumption A9: the production Phase 31 approve swallows internally; the mock
      simulates a non-best-effort backend; Phase 32 does not silently absorb the
      exception). Document the design choice in the test title:
      `"Phase 32 initiateDeviceFlow: approveThrows propagates -- Phase 32 does not wrap CredentialOps.approve (A9)"`.

    Style:
    - One blank line between `test(...)` blocks; two blank lines between top-level
      sections if any.
    - Use `await` everywhere -- no fire-and-forget. (The exception is `notifyFn`
      itself, which the production module calls without `await`.)
    - Prefer `assert.equal` over `assert.deepEqual` for primitive comparisons; use
      `assert.deepEqual` only for object/array equality.
    - Each test must be SELF-CONTAINED: no shared `let` state across tests; no
      `beforeEach`. Construct fresh mocks per test.

    Commit strategy: this task is the SECOND half of the atomic three-task commit
    (Tasks 1 + 2 + 3 -- one commit). The TypeScript RED window across the three tasks
    is the same Plan 31-02 documented atomic-commit pattern (Phase 31 plan 02 SUMMARY
    documents it as `"Single atomic commit for Task 1 + Task 2 per the plan's
    <verification> commit strategy"`). DO NOT run `git commit` until Task 3 finishes
    and `npm run check` is GREEN. SEE COMMIT STRATEGY in <verify>.
  </action>
  <read_first>
    - tests/helpers/credential-mock.ts (the existing Phase 31 helper that this test file imports + the structural template for fresh-per-test mock construction)
    - tests/helpers/device-flow-mock.ts (Task 1's output -- this test file's primary collaborator)
    - tests/platform/git-credential.test.ts (the Phase 31 platform test file -- the template for "Phase 31 credOps: ..." naming convention and per-test mock-state assertion idioms)
    - .planning/phases/32-device-flow-state-machine-auth/32-VALIDATION.md (per-task verification map rows 32-02-01..32-02-12; AUTH-09 row 32-02-12 splits into the positive + negative content assertions in Test 12)
    - .planning/phases/32-device-flow-state-machine-auth/32-RESEARCH.md (Example 3 lines 751-889 -- the production state machine the tests exercise; Assumption A9 lines 1034-1035 -- approve-throws contract)
    - .planning/phases/32-device-flow-state-machine-auth/32-CONTEXT.md (D-32-04, D-32-05, D-32-06 -- the design constraints the tests must lock in)
  </read_first>
  <acceptance_criteria>
    - **Source assertion:** `grep -c "^test(" tests/domain/github-auth.test.ts` returns at least `13` (the planning instructions require minimum 13 unit tests; Test 12 splits AUTH-09 row into two so the count is naturally 13).
    - **Source assertion:** All requirement IDs are referenced in test titles -- `grep -c "AUTH-0[1357]\|AUTH-04\|AUTH-09\|happy path\|approve on success" tests/domain/github-auth.test.ts` returns at least 9 (covers AUTH-01/03/04/05/07/09 + the two happy-path variants).
    - **Source assertion:** `grep -c "makeMockDeviceFlowHttp\|makeMockCredentialOps" tests/domain/github-auth.test.ts` returns at least `2` (both helpers imported and used).
    - **Source assertion:** `grep -c "initiateDeviceFlow" tests/domain/github-auth.test.ts` returns at least `13` (each test invokes the production entry point).
    - **Source assertion:** `grep -c "intervalSec === 10\|intervalSec === 5\|intervalSec === 0" tests/domain/github-auth.test.ts` returns at least `3` (Tests 4/5 assert specific interval values).
    - **Source assertion:** `grep -c "assert\\.rejects" tests/domain/github-auth.test.ts` returns at least `1` (Test 13 uses assert.rejects).
    - **Source assertion (negative):** `grep -c "setTimeout\\|setInterval\\|sleep" tests/domain/github-auth.test.ts` returns `0` -- the tests must not introduce real timers; use `interval: 0` in the mock deviceCode instead.
    - **Source assertion:** `grep -c "interval: 0" tests/domain/github-auth.test.ts` returns at least `1` (Tests 4 + 5 use the zero-interval mock).
    - **Source assertion (forward-compat):** `grep -c "gho_test\\|MOCK_DEVICE_CODE\\|ABCD-1234" tests/domain/github-auth.test.ts` returns at least `3` (Test 12 negative-scan must reference the literal token + device_code that should NOT appear in notify; Test 3 positive scan must reference the user_code literal that SHOULD appear).
    - **Behavior assertion (Task 3 -> ALL GREEN):** After Task 3 ships the production module, `node --test tests/domain/github-auth.test.ts` exits 0 with 13/13 passing. Per-test runtime < 100ms; full file < 1 second.
    - **Behavior assertion (deadline test isolation):** Test 8 (timeout) asserts `httpState.pollTokenCalls.length === 0` -- proves the deadline check happens BEFORE the first poll when `expires_in: 0`.
    - **Behavior assertion (no real network):** `git diff` of this file shows no references to `https://github.com/login/` outside string-equality assertions against the mock's default `verification_uri` ("https://github.com/login/device") -- the test never imports `globalThis.fetch` directly.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/domain/github-auth.test.ts 2>&1 | tail -3</automated>
  </verify>
  <done>
    `tests/domain/github-auth.test.ts` exists with 13 self-contained unit tests covering
    AUTH-01 (happy path + approve), AUTH-03 (notify content), AUTH-04 (cumulative
    slow_down + pending no-change), AUTH-05 (access_denied + expired_token + timeout
    + init failure), AUTH-07 (authAttempted true on success + on failure), AUTH-09
    (notify content negative scan), and one design-contract test (approveThrows
    propagates -- A9). Once Task 3 ships the production module, the file goes from
    "fails to load" RED to all-13-GREEN.

    COMMIT STRATEGY (Tasks 1 + 2 + 3 atomic):
    DO NOT commit yet. Wait until Task 3 lands the production module AND `npm run check`
    is fully GREEN, then commit Tasks 1 + 2 + 3 as a single feat commit titled
    `feat(32): domain/github-auth Device Flow state machine (AUTH-01/03/04/05/07/09)`.
    Use `SKIP=trufflehog` if running inside a worktree per CLAUDE.md.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create extensions/pi-claude-marketplace/domain/github-auth.ts -- DeviceFlowHttp interface + DEFAULT_DEVICE_FLOW_HTTP + initiateDeviceFlow state machine</name>
  <files>extensions/pi-claude-marketplace/domain/github-auth.ts</files>
  <behavior>
    **RED gate (entry):** before this task starts, `tests/domain/github-auth.test.ts`
    (Task 2's output) fails because the production module does not exist -- the import
    of `initiateDeviceFlow` from `../../extensions/pi-claude-marketplace/domain/github-auth.ts`
    is unresolved. `node --test tests/domain/github-auth.test.ts` exits non-zero with an
    "ERR_MODULE_NOT_FOUND" or TypeScript compile error. This is the RED state required
    by the TDD discipline declared in the planning instructions ("tdd=true with explicit
    RED gate (tests exist but no implementation yet) and GREEN gate (tests pass after
    implementation)").

    **GREEN gate (exit):** after this task ships `github-auth.ts`, `node --test
    tests/domain/github-auth.test.ts` exits 0 with all 13 tests passing. `npm run check`
    exits 0 with 1291 tests (1278 post-Plan-32-01 + 13 new). The Plan 32-01 AUTH-09 gate
    transitions from vacuous-pass to active-pass against the new file.

    **Exports** (exactly 9 named exports + 1 const + 1 type alias):
    - `interface DeviceCodeResponse` -- the shape returned by `requestCode`
    - `type PollResult` -- discriminated union with 6 kinds (success / pending / slow_down / access_denied / expired_token / unexpected)
    - `interface DeviceFlowHttp` -- two methods (requestCode + pollToken)
    - `const DEFAULT_DEVICE_FLOW_HTTP: DeviceFlowHttp` -- uses globalThis.fetch
    - `type NotifyFn` -- signature `(message: string, severity?: "info" | "warning" | "error") => void`
    - `interface InitiateDeviceFlowOpts` -- host + credentialOps + notifyFn + http? + signal?
    - `type DeviceFlowResult` -- discriminated union with 2 variants (ok: true { cred, authAttempted: true } / ok: false { reason: string, authAttempted: true })
    - `function initiateDeviceFlow(opts: InitiateDeviceFlowOpts): Promise<DeviceFlowResult>`
    - (no other exports -- the production functions like requestCodeImpl / pollTokenImpl are file-private)

    **Imports** (top of file):
    - `import { setTimeout as sleepMs } from "node:timers/promises"` (Pattern 3 -- AbortSignal-aware sleep; the `as sleepMs` rename keeps the call site readable)
    - `import type { CredentialOps } from "../platform/git-credential.ts"` (TYPE-ONLY per P32-6)
    - `import type { GitCredentials } from "../platform/git.ts"` (TYPE-ONLY)

    **Constants** (file-private):
    - `GITHUB_DEVICE_CODE_URL = "https://github.com/login/device/code"`
    - `GITHUB_ACCESS_TOKEN_URL = "https://github.com/login/oauth/access_token"`
    - `GITHUB_OAUTH_CLIENT_ID = <value from Plan 32-01 SUMMARY>` -- either the operator's real OAuth App client_id OR the literal `"GITHUB_OAUTH_APP_CLIENT_ID_PLACEHOLDER"` if the operator chose the `placeholder ok` path. Read 32-01-SUMMARY.md before writing the value (the planning instructions say "commit a PLACEHOLDER constant `'GITHUB_OAUTH_APP_CLIENT_ID_PLACEHOLDER'`"; if the operator supplied a real client_id, prefer it; otherwise default to the placeholder).
    - `REQUESTED_SCOPE = "repo"` per P32-8

    **Function: requestCodeImpl(clientId, scope)** (file-private):
    - POSTs to GITHUB_DEVICE_CODE_URL using `globalThis.fetch`
    - Headers: `Accept: application/json` AND `Content-Type: application/x-www-form-urlencoded`
    - Body: `new URLSearchParams({ client_id: clientId, scope }).toString()` (form-urlencoded per Pattern 5)
    - On `!res.ok`, throws `new Error("Device code request failed: HTTP " + res.status)` -- NO response-body interpolation per AUTH-09
    - On success, parses JSON and validates required fields with `typeof` guards (P32 defensive field-presence checks); throws `new Error("Device code response missing required fields")` if any field absent
    - Returns the parsed DeviceCodeResponse

    **Function: pollTokenImpl(clientId, deviceCode, _intervalSec)** (file-private):
    - POSTs to GITHUB_ACCESS_TOKEN_URL with the same headers as requestCodeImpl
    - Body: `new URLSearchParams({ client_id: clientId, device_code: deviceCode, grant_type: "urn:ietf:params:oauth:grant-type:device_code" }).toString()`
    - Wraps fetch in try/catch (P32-4); on network error returns `{ kind: "unexpected", error: "network_error", description: String(err) }`
    - Reads body via `res.json()` BEFORE checking `res.ok` (GitHub returns HTTP 200 even for error codes per P32-4 -- body is source of truth)
    - On JSON parse error returns `{ kind: "unexpected", error: "invalid_json", description: "HTTP " + res.status }`
    - If `data.access_token` is a string, returns `{ kind: "success", accessToken: data.access_token, tokenType: data.token_type || "bearer", scope: data.scope || "" }`
    - Otherwise dispatches on `data.error`: authorization_pending -> pending; slow_down -> slow_down; access_denied -> access_denied; expired_token -> expired_token; default -> unexpected with the `error` and `error_description` fields
    - Uses conditional-spread for optional `description` field (per exactOptionalPropertyTypes)

    **Const: DEFAULT_DEVICE_FLOW_HTTP** (exported):
    - `{ requestCode: requestCodeImpl, pollToken: pollTokenImpl }`

    **Function: initiateDeviceFlow(opts)** (exported):
    - Step 1: const http = opts.http ?? DEFAULT_DEVICE_FLOW_HTTP
    - Step 2: try to `await http.requestCode(GITHUB_OAUTH_CLIENT_ID, REQUESTED_SCOPE)`; on throw, return `{ ok: false, reason: "Device Flow initialization failed: " + (err instanceof Error ? err.message : "unknown error"), authAttempted: true }`
    - Step 3: call `opts.notifyFn(\`Open ${deviceCode.verification_uri} and enter: ${deviceCode.user_code}\`, "info")` -- AUTH-03 / AUTH-09 discipline
    - Step 4: let `currentIntervalSec = deviceCode.interval`; let `deadlineMs = Date.now() + deviceCode.expires_in * 1000`
    - Step 5: while (Date.now() < deadlineMs):
      - try { await sleepMs(currentIntervalSec * 1000, undefined, opts.signal !== undefined ? { signal: opts.signal } : undefined) } catch { return { ok: false, reason: "Device Flow cancelled.", authAttempted: true } }
      - const r = await http.pollToken(GITHUB_OAUTH_CLIENT_ID, deviceCode.device_code, currentIntervalSec)
      - switch (r.kind):
        - "success": build cred `{ username: "x-access-token", password: r.accessToken }`; `await opts.credentialOps.approve(opts.host, cred)`; return `{ ok: true, cred, authAttempted: true }`
        - "pending": continue (CP-2 -- no interval mutation)
        - "slow_down": currentIntervalSec += 5; continue (CP-1 -- cumulative)
        - "access_denied": return { ok: false, reason: "User cancelled authorization. Run the command again to retry.", authAttempted: true }
        - "expired_token": return { ok: false, reason: "Device code expired before authorization. Run the command again to restart.", authAttempted: true }
        - "unexpected": return { ok: false, reason: "Device Flow failed: " + r.error + (r.description !== undefined ? " -- " + r.description : ""), authAttempted: true }
    - Step 6 (after loop): return { ok: false, reason: "Device Flow timed out before authorization completed. Run the command again to restart.", authAttempted: true }

    **AUTH-09 discipline locked by the Plan 32-01 architecture gate:**
    - NO `new Error(...)` constructor in this file interpolates `access_token`, `accessToken`, `cred.<field>`, or `r.accessToken`
    - NO `notifyFn(...)` call interpolates those fields either
    - The only fields that flow into notifyFn are `deviceCode.verification_uri` and `deviceCode.user_code` -- both safe-to-display per RFC 8628 / SEC-3

    **File-header docblock** (multi-line):
    - Phase 32 reference + AUTH-01..05/07 + AUTH-09 mention
    - D-32-01 (file location), D-32-02 (injectable HTTP seam), D-32-03 (client_id is public),
      D-32-04 (notifyFn callback), D-32-05 (authAttempted guard), D-32-06 (AUTH-09 discipline)
    - P32-7 OPERATOR ACTION callout for the GITHUB_OAUTH_CLIENT_ID constant
    - Reference to Plan 32-01 architecture gate
    - Reference to Phase 33 consumer (`buildAuthCallbacks`) for future readers

    **TypeScript discipline:**
    - exactOptionalPropertyTypes: use conditional-spread for `description` field in PollResult "unexpected" branch
    - Discriminated union exhaustiveness: the switch over `r.kind` has 6 arms; if a future contributor adds a `PollResult` kind, the switch fails to compile (no need for an explicit `assertNever` arm because every branch returns / continues -- the switch is exhaustive by construction)

    **Lint discipline (from Phase 31 SUMMARY Deviations):**
    - Use destructuring instead of bracket access on parsed objects (e.g. `const { access_token: accessToken } = data` -- though this file does not parse a known-keyed object the same way; if any bracket access surfaces, use destructuring)
    - Sort imports per `eslint-plugin-import-x` rules: `node:timers/promises` (built-in) before `../platform/git-credential.ts` (relative); type-only imports together; alphabetical within each block
    - Prettier-compatible formatting -- multi-line wraps at the print width
  </behavior>
  <action>
    Create `extensions/pi-claude-marketplace/domain/github-auth.ts` matching the
    structure in RESEARCH Examples 1, 2, and 3 (lines 588-889 of 32-RESEARCH.md). The
    file is the production state machine that satisfies all 13 unit tests Task 2 ships.

    Sequence:
    1. Read `.planning/phases/32-device-flow-state-machine-auth/32-01-SUMMARY.md` to
       determine the `GITHUB_OAUTH_CLIENT_ID` constant value (real client_id OR
       `"GITHUB_OAUTH_APP_CLIENT_ID_PLACEHOLDER"` per Plan 32-01 Task 1 outcome).
    2. Write the file-header docblock referencing the locked decisions (D-32-01..D-32-06)
       and the operator action (P32-7) for the constant.
    3. Write the imports per `<behavior>` "Imports" subsection -- two type-only imports
       (CredentialOps, GitCredentials) + one named import (`setTimeout as sleepMs` from
       `node:timers/promises`). Apply the import order rule (built-in before relative;
       type-only before value within the relative group).
    4. Write the exported types: `DeviceCodeResponse`, `PollResult`, `DeviceFlowHttp`,
       `NotifyFn`, `InitiateDeviceFlowOpts`, `DeviceFlowResult` per the shapes in
       `<behavior>` "Exports".
    5. Write the file-private constants: `GITHUB_DEVICE_CODE_URL`,
       `GITHUB_ACCESS_TOKEN_URL`, `GITHUB_OAUTH_CLIENT_ID`, `REQUESTED_SCOPE`.
    6. Write `requestCodeImpl(clientId, scope)` per `<behavior>` "Function:
       requestCodeImpl" subsection.
    7. Write `pollTokenImpl(clientId, deviceCode, _intervalSec)` per `<behavior>`
       "Function: pollTokenImpl" subsection. Note: the unused `intervalSec` parameter
       is required by the `DeviceFlowHttp.pollToken` signature; prefix with `_` to
       satisfy lint rules.
    8. Write `export const DEFAULT_DEVICE_FLOW_HTTP: DeviceFlowHttp = { requestCode:
       requestCodeImpl, pollToken: pollTokenImpl };`.
    9. Write `export async function initiateDeviceFlow(opts: InitiateDeviceFlowOpts):
       Promise<DeviceFlowResult>` per `<behavior>` "Function: initiateDeviceFlow"
       subsection.
    10. After writing the file, run `npm run check` and confirm GREEN:
        - `npm run typecheck` -- exits 0
        - `npm run lint` -- exits 0 (apply auto-fixes for dot-notation, import-x/order,
          prefer-const, etc. per Phase 31 SUMMARY Deviations)
        - `npm run format:check` -- exits 0 (run `npx prettier --write` on the new file
          if format check fails; per Phase 31 SUMMARY this is a mechanical fix)
        - `npm test` -- exits 0 with 1291/1291 tests (1278 baseline + 13 new domain
          tests)

    The full implementation belongs in this file (not the action); reference RESEARCH
    Example 3 lines 751-889 for the exact production-grade prose to faithfully
    translate -- DO NOT paste it verbatim; adapt it to the imports, conditional-spread
    idioms, and lint discipline. The implementation is small enough (~150 LOC) that
    it can be written from the directive shape in this action without copy-paste.

    AUTH-09 self-check before committing:
    - Run `grep -nE "(new Error|notifyFn)\\s*\\([^)]*(access_token|accessToken|cred\\.|r\\.accessToken)" extensions/pi-claude-marketplace/domain/github-auth.ts` and confirm ZERO matches outside comments. The Plan 32-01 architecture gate test catches this on the next `npm test`, but pre-commit verification avoids a redo.

    COMMIT STRATEGY (atomic with Tasks 1 + 2):
    Once Task 3's verification is fully GREEN, commit all three tasks (the helper, the
    test file, and the production module) in a SINGLE feat commit:
    Title: `feat(32): domain/github-auth Device Flow state machine (AUTH-01/03/04/05/07/09)`
    Body: enumerate the new files + exports + the GITHUB_OAUTH_CLIENT_ID value (real or
    placeholder); reference Plan 32-01 architecture gate transition (vacuous -> active);
    confirm `npm run check` GREEN with test count.

    Use `SKIP=trufflehog git commit ...` if in a worktree per CLAUDE.md; then run
    `pre-commit run trufflehog --all-files` separately from the main checkout to
    confirm the scan is clean.
  </action>
  <read_first>
    - .planning/phases/32-device-flow-state-machine-auth/32-01-SUMMARY.md (Task 1 of Plan 32-01 recorded the operator-chosen GITHUB_OAUTH_CLIENT_ID value)
    - .planning/phases/32-device-flow-state-machine-auth/32-RESEARCH.md (Examples 1 lines 588-641, Example 2 lines 645-749, Example 3 lines 751-889; Patterns 1-5; Pitfalls P32-1..P32-8; Assumptions A1..A9)
    - .planning/phases/32-device-flow-state-machine-auth/32-CONTEXT.md (D-32-01..D-32-06)
    - extensions/pi-claude-marketplace/platform/git-credential.ts (CredentialOps interface to satisfy via type-only import; understand `approve(host, cred)` contract)
    - extensions/pi-claude-marketplace/platform/git.ts lines 186-198 (GitCredentials shape: { username?, password?, headers?, cancel? } -- the success-return uses { username: "x-access-token", password: token })
    - tests/architecture/import-boundaries.test.ts lines 76-82 (EXPECTED_FORBIDDEN["domain"] does NOT include "platform" -- the domain -> platform import is permitted; D-11 boundary)
    - .planning/phases/31-credential-subprocess-layer-auth/31-02-SUMMARY.md "Deviations from Plan" section (the three Rule-3 auto-fixes Phase 31 hit: ESLint dot-notation, import-x/order on type-only imports, Prettier formatting -- pre-empt these in Phase 32 by destructuring + sorted imports + running `npx prettier --write` before commit)
  </read_first>
  <acceptance_criteria>
    - **RED gate satisfied at task entry:** Before this task runs, `node --test tests/domain/github-auth.test.ts` exits non-zero with "ERR_MODULE_NOT_FOUND" or equivalent compile error (the production module does not exist yet). This is verified by the prior commit / state -- it's the natural state once Task 2 lands.
    - **Source assertion:** `grep -c "^export " extensions/pi-claude-marketplace/domain/github-auth.ts` returns at least `8` (DeviceCodeResponse interface + PollResult type + DeviceFlowHttp interface + DEFAULT_DEVICE_FLOW_HTTP const + NotifyFn type + InitiateDeviceFlowOpts interface + DeviceFlowResult type + initiateDeviceFlow function = 8).
    - **Source assertion:** `grep -c "^import type" extensions/pi-claude-marketplace/domain/github-auth.ts` returns at least `2` (CredentialOps + GitCredentials are type-only per P32-6).
    - **Source assertion:** `grep -c "from \"node:timers/promises\"" extensions/pi-claude-marketplace/domain/github-auth.ts` returns `1` (Pattern 3 sleep).
    - **Source assertion:** `grep -c "GITHUB_OAUTH_CLIENT_ID\|GITHUB_DEVICE_CODE_URL\|GITHUB_ACCESS_TOKEN_URL\|REQUESTED_SCOPE" extensions/pi-claude-marketplace/domain/github-auth.ts` returns at least `4` (the file-private constants).
    - **Source assertion:** `grep -c "currentIntervalSec += 5" extensions/pi-claude-marketplace/domain/github-auth.ts` returns `1` (CP-1 cumulative slow_down).
    - **Source assertion:** `grep -c "deadlineMs\|expires_in \\* 1000" extensions/pi-claude-marketplace/domain/github-auth.ts` returns at least `2` (P32-3 deadline enforcement).
    - **Source assertion:** `grep -c "authAttempted: true" extensions/pi-claude-marketplace/domain/github-auth.ts` returns at least `7` (every DeviceFlowResult return -- 1 success + 6 failure branches: init throw, sleep cancel, access_denied, expired_token, unexpected, deadline timeout = 7 minimum).
    - **Source assertion:** `grep -c "x-access-token" extensions/pi-claude-marketplace/domain/github-auth.ts` returns `1` (the success-cred username).
    - **Source assertion:** `grep -c "credentialOps.approve" extensions/pi-claude-marketplace/domain/github-auth.ts` returns `1` (called in the success branch).
    - **Source assertion (AUTH-09):** `grep -nE "(new Error|notifyFn)\\s*\\([^)]*(access_token|accessToken|cred\\.|r\\.accessToken)" extensions/pi-claude-marketplace/domain/github-auth.ts | grep -v '^[^:]*: *//\\|^[^:]*: */\\*' | wc -l` returns `0` (no AUTH-09 violations).
    - **Source assertion (no ctx import per D-32-04):** `grep -c "import.*ctx\\|from \".*pi-coding-agent" extensions/pi-claude-marketplace/domain/github-auth.ts` returns `0` (the domain module does NOT import the Pi extension API).
    - **Source assertion (no direct ctx.ui.notify per D-32-04):** `grep -c "ctx\\.ui\\.notify\\|process\\.stdout\\|process\\.stderr" extensions/pi-claude-marketplace/domain/github-auth.ts` returns `0` (notifyFn is the only output channel).
    - **GREEN gate satisfied:** `node --test tests/domain/github-auth.test.ts` exits 0 with 13 tests passing.
    - **AUTH-09 gate active:** `node --test tests/architecture/no-credential-leak.test.ts` exits 0 with all 3 tests passing AND the new Phase-32 test reports active-pass (NOT vacuous-pass) -- it scanned the now-present `domain/github-auth.ts` and found no token interpolation.
    - **Full pipeline:** `npm run check` exits 0 with 1291/1291 tests (1278 post-Plan-32-01 + 13 new domain tests).
    - **Architecture boundary preserved:** `node --test tests/architecture/import-boundaries.test.ts` exits 0 (domain -> platform import permitted; no new boundary needed).
    - **No new npm packages:** `git diff package.json package-lock.json` shows no changes.
    - **Atomic commit landed:** A single commit titled `feat(32): domain/github-auth Device Flow state machine (AUTH-01/03/04/05/07/09)` (or close variant per Conventional Commits) contains all THREE files (tests/helpers/device-flow-mock.ts + tests/domain/github-auth.test.ts + extensions/pi-claude-marketplace/domain/github-auth.ts).
    - **No worktree-trufflehog issue masking:** If the commit ran with `SKIP=trufflehog`, the operator separately ran `pre-commit run trufflehog --all-files` and recorded a CLEAN scan in the SUMMARY (per CLAUDE.md worktree discipline).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/domain/github-auth.test.ts && node --test tests/architecture/no-credential-leak.test.ts && npm run check</automated>
  </verify>
  <done>
    `extensions/pi-claude-marketplace/domain/github-auth.ts` exists and exports
    `DeviceFlowHttp`, `DEFAULT_DEVICE_FLOW_HTTP`, `DeviceCodeResponse`, `PollResult`,
    `DeviceFlowResult`, `InitiateDeviceFlowOpts`, `NotifyFn`, and `initiateDeviceFlow`.
    The state machine satisfies every behavior bullet (cumulative slow_down, pending
    no-change, three terminal failure paths, deadline timeout, init failure, success
    + approve + authAttempted on every result). The Plan 32-01 AUTH-09 architecture
    gate transitions from vacuous-pass to active-pass. All 13 unit tests are GREEN.
    `npm run check` exits 0 with 1291/1291 tests. The atomic commit covering Tasks 1
    + 2 + 3 is landed on the working branch with a Conventional Commits-formatted title.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain/github-auth.ts -> globalThis.fetch -> github.com | TLS-protected outbound HTTPS to GitHub OAuth endpoints; fetch validates the server cert by default |
| Phase 33+ caller -> notifyFn -> ctx.ui.notify -> user terminal | Untrusted-channel boundary; AUTH-09 enforces no-token-in-message discipline |
| Phase 33+ caller -> credentialOps -> OS keychain | Best-effort persistence (Phase 31 swallows failures); the token in the returned DeviceFlowResult is the in-memory source of truth |
| Test -> mock HTTP / mock CredentialOps | All test paths bounded -- no real subprocess, no real HTTP, no real keychain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02-01 | Information Disclosure | access_token interpolated into Error / notifyFn | mitigate | Plan 32-01 architecture gate scans `domain/github-auth.ts` for `new Error(...)` and `notifyFn(...)` interpolation of token field names; gate is GREEN at end of this plan (transitions vacuous -> active). Task 3 acceptance criteria includes a manual pre-commit grep self-check. |
| T-32-02-02 | Information Disclosure | response body interpolated into Error -- requestCode failure on a malformed server response could leak unexpected content | mitigate | RequestCodeImpl throws `Error("Device code request failed: HTTP " + res.status)` -- ONLY the status code is interpolated, never the body (per RESEARCH Example 2 line 663-666 comment about defense in depth). Task 3 acceptance criteria includes a regex check. |
| T-32-02-03 | DoS | Polling loop runs forever if expires_in deadline not enforced | mitigate | P32-3 mitigation: deadlineMs = Date.now() + expires_in * 1000; checked at top of every loop iteration. Test 8 (timeout) asserts this -- expires_in: 0 produces immediate timeout. |
| T-32-02-04 | DoS | slow_down ignored, server forces over-poll, rate-limit triggers | mitigate | P32-1 mitigation: cumulative `currentIntervalSec += 5` on every slow_down. Test 4 (AUTH-04 cumulative) asserts intervalSec === 10 after two slow_downs. |
| T-32-02-05 | Tampering | Mock test data could spoof a successful auth with a bogus token | accept | Tests inject the mock; the test's job is to assert structural properties (presence of username/password, correct types) NOT to validate token authenticity. Production TLS to github.com provides server identity. |
| T-32-02-06 | Information Disclosure | notifyFn message contains user_code (a 6-char code that's brute-forceable in 15 min) | accept | RFC 8628 §3.4 design: user_code is the user-visible authorization handle; it's SUPPOSED to be displayed to the user. GitHub's expires_in: 900 window + per-IP rate-limiting at the verification URL is the brute-force mitigation. Phase 32 follows the spec. |
| T-32-02-07 | Information Disclosure | client_id committed to the repo | accept | RFC 8628 §3.1 + GitHub OAuth Apps docs: client_id is a PUBLIC identifier; safe to commit. The OAuth App's client_secret (NOT used in Device Flow) is the real secret. Documented in file-header docblock per Plan 32-01 Task 1. |
| T-32-02-08 | Repudiation | Test 13 (approveThrows) locks the contract "Phase 32 does NOT wrap approve" -- a future contributor might add a try/catch around approve, silently masking keychain failures | mitigate | Test 13 uses `assert.rejects(...)` to enforce the contract. A future try/catch would flip the test from passing to failing, alerting the contributor. |
| T-32-02-09 | Information Disclosure | Architecture gate is static-regex and could miss novel leak patterns (e.g. console.log, JSON.stringify on an error containing the token) | accept | RESEARCH P32-2 confidence note: the gate catches the most common surfaces (new Error, notifyFn); console.log + JSON.stringify are out of scope by design. Code-review backstop + Phase 36 integration smoke is the residual mitigation. Phase 32 ESLint already has a no-console rule in the lint config that disallows console.* outside the sanctioned migrate.ts (verified per Phase 21 SC#3). |
| T-32-02-SC | Tampering | npm/pip/cargo installs | mitigate | Plan 32-02 installs NO new packages (verified per RESEARCH §Package Legitimacy Audit: zero new packages; `git diff package.json package-lock.json` shows no changes). |
</threat_model>

<verification>
After all three tasks complete and the atomic commit lands:

1. **All three files exist:**
   - `tests/helpers/device-flow-mock.ts` (Task 1)
   - `tests/domain/github-auth.test.ts` (Task 2)
   - `extensions/pi-claude-marketplace/domain/github-auth.ts` (Task 3)

2. **All 13 unit tests pass:** `node --test tests/domain/github-auth.test.ts` exits 0
   with 13/13 passing.

3. **Plan 32-01 AUTH-09 gate now active:** `node --test
   tests/architecture/no-credential-leak.test.ts` exits 0; the Phase-32 test reports
   active-pass (NOT vacuous-pass) -- the file is on disk and the regex scan is
   meaningful.

4. **Full pipeline GREEN:** `npm run check` exits 0 with 1291/1291 tests (1278
   post-Plan-32-01 + 13 new domain tests).

5. **Architecture boundaries preserved:**
   - `node --test tests/architecture/import-boundaries.test.ts` exits 0 (domain ->
     platform is permitted; no new boundary added).
   - `node --test tests/architecture/no-shell-out.test.ts` exits 0 (Plan 32-02 does
     NOT import `node:child_process` -- only Phase 31's `platform/git-credential.ts`
     is whitelisted).
   - `node --test tests/architecture/no-orchestrator-network.test.ts` exits 0 (this
     test scopes to `orchestrators/`; `domain/github-auth.ts` is in `domain/` so the
     test's existing glob does NOT need amendment).

6. **No new npm packages:** `git diff package.json package-lock.json` shows no
   changes.

7. **No ctx import in domain:** `grep -c "import.*ctx\\|from \".*pi-coding-agent"
   extensions/pi-claude-marketplace/domain/github-auth.ts` returns 0 (D-32-04
   discipline).

8. **No direct ctx.ui.notify / process.stdout in domain:** `grep -c
   "ctx\\.ui\\.notify\\|process\\.stdout\\|process\\.stderr"
   extensions/pi-claude-marketplace/domain/github-auth.ts` returns 0 (IL-2 satisfied
   via the notifyFn callback chokepoint at the boundary).

9. **No-credential-leak gate active:** the new test in
   `tests/architecture/no-credential-leak.test.ts` reports a NON-vacuous pass (it
   actually scanned the file). Confirm by reading the test output for the absence
   of "not yet authored" in the new test's message.

10. **Atomic commit landed:** `git log --oneline -3` shows a single feat commit
    covering all three new files; the title follows Conventional Commits
    (`feat(32): ...`); the body documents the GITHUB_OAUTH_CLIENT_ID value (real or
    placeholder).

11. **Trufflehog clean:** if the commit used `SKIP=trufflehog` (worktree case), a
    separate `pre-commit run trufflehog --all-files` from the main checkout returned
    clean.
</verification>

<success_criteria>
- All three files (`tests/helpers/device-flow-mock.ts`, `tests/domain/github-auth.test.ts`, `extensions/pi-claude-marketplace/domain/github-auth.ts`) exist with the documented exports and shapes.
- All 13 unit tests in `tests/domain/github-auth.test.ts` pass via `node --test`.
- The Plan 32-01 AUTH-09 architecture gate transitions from vacuous-pass to active-pass.
- `npm run check` exits 0 with 1291/1291 tests (or equivalent; baseline drift permitted).
- `initiateDeviceFlow` honors all 6 AUTH requirements (AUTH-01 happy + approve, AUTH-02 negative coverage is Phase 33's responsibility, AUTH-03 notify content, AUTH-04 cumulative + pending, AUTH-05 four failure paths, AUTH-07 authAttempted on success + failure) without `ctx` import, without `process.stdout`/`process.stderr` writes, and without any AUTH-09 token leaks (verified by the architecture gate).
- `domain/github-auth.ts` does NOT import `@earendil-works/pi-coding-agent` or reference `ctx` directly (D-32-04 callback chokepoint preserved).
- No new npm packages added (verified by `git diff package.json package-lock.json` showing no changes).
- The three new files land in a single atomic feat commit with a Conventional Commits title.
- The atomic commit's body records the chosen `GITHUB_OAUTH_CLIENT_ID` value (real or placeholder) so Phase 33+ planners can verify production-readiness without reading the code.
</success_criteria>

<output>
Create `.planning/phases/32-device-flow-state-machine-auth/32-02-SUMMARY.md` recording:
- The three new files + their public surfaces.
- The 13 unit-test titles and AUTH-XX → test-title map (mirror the Phase 31 Plan 02 SUMMARY "Requirements → Test Map" table).
- The Plan 32-01 architecture-gate transition (vacuous-pass -> active-pass) confirmation.
- `npm run check` test count (expected: 1291/1291).
- The atomic commit hash containing all three files.
- The committed `GITHUB_OAUTH_CLIENT_ID` value (real OAuth App client_id or `GITHUB_OAUTH_APP_CLIENT_ID_PLACEHOLDER` sentinel).
- Any Phase 31-style auto-fixed lint deviations (Rule 3 fixes) -- mirror the Phase 31 Plan 02 SUMMARY "Auto-fixed Issues" section format.
- Public surface table for Phase 33 readers (DeviceFlowHttp / DEFAULT_DEVICE_FLOW_HTTP / DeviceFlowResult / InitiateDeviceFlowOpts shapes + the `initiateDeviceFlow({ host, credentialOps, notifyFn, http? })` entry-point signature).
- Note for Phase 33 readiness: `import { initiateDeviceFlow, DEFAULT_DEVICE_FLOW_HTTP } from "../../domain/github-auth.ts";` becomes the consumer pattern for `buildAuthCallbacks` (Phase 33 wraps `initiateDeviceFlow` inside the isomorphic-git `onAuth` closure, plumbing `credentialOps.fill` BEFORE Device Flow per AUTH-08; and `credentialOps.reject` + `authAttempted` guard for `onAuthFailure` per AUTH-07).
</output>
