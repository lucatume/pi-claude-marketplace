---
phase: 32-device-flow-state-machine-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/architecture/no-credential-leak.test.ts
autonomous: false
requirements: [AUTH-09]
tags: [auth-09, no-credential-leak, oauth-app, operator-checkpoint, device-flow, github-auth]

must_haves:
  truths:
    - "Operator has registered (or confirmed) the pi-claude-marketplace GitHub OAuth App with Device Flow enabled and has a public client_id value documented for Plan 32-02 to substitute (P32-7)"
    - "tests/architecture/no-credential-leak.test.ts scans extensions/pi-claude-marketplace/domain/github-auth.ts for token interpolation inside new Error(...) constructors AND inside notifyFn(...) calls (D-32-06)"
    - "The Phase 32 AUTH-09 gate test passes vacuously while domain/github-auth.ts does NOT yet exist (Plan 32-01 lands before Plan 32-02) and activates automatically when Plan 32-02 creates the file"
    - "npm run check exits 0 after the amendment lands (1277 baseline tests still GREEN + new Phase-32 AUTH-09 test added as 1278)"
  artifacts:
    - path: "tests/architecture/no-credential-leak.test.ts"
      provides: "Amended AUTH-09 gate -- new test case 'AUTH-09 (Phase 32): domain/github-auth.ts never interpolates a token in an Error or notifyFn message' covering domain/github-auth.ts"
      contains: "domain/github-auth.ts"
  key_links:
    - from: "tests/architecture/no-credential-leak.test.ts"
      to: "extensions/pi-claude-marketplace/domain/github-auth.ts"
      via: "static file read + regex scan inside a new test() block"
      pattern: "domain/github-auth\\.ts"
    - from: "Operator action (OAuth App registration)"
      to: "Plan 32-02 Task 3 (GITHUB_OAUTH_CLIENT_ID constant)"
      via: "documented checkpoint output -- operator records client_id for Plan 32-02 to substitute (or confirms placeholder is acceptable for unit tests)"
      pattern: "GITHUB_OAUTH_CLIENT_ID"
---

<objective>
Land the AUTH-09 architecture-gate amendment that extends the Phase 31 no-credential-leak
test to also scan the upcoming `extensions/pi-claude-marketplace/domain/github-auth.ts`
file (Plan 32-02 deliverable), AND surface the GitHub OAuth App registration as an
explicit operator checkpoint (P32-7 from RESEARCH).

The amendment is forward-compatible: the new test passes vacuously while
`domain/github-auth.ts` does not exist on disk, and activates the moment Plan 32-02
creates the file -- the same pattern Phase 31 used for the original Error-interpolation
gate.

Purpose: Phase 32 cannot ship without (1) a static gate proving the new domain module
respects AUTH-09 token-non-interpolation discipline -- the only way to lock the
discipline in for the lifetime of the codebase -- and (2) a registered OAuth App
client_id. The amendment is intentionally scoped to AUTH-09 only; the state machine
itself lands in Plan 32-02.

Output:
- Amended `tests/architecture/no-credential-leak.test.ts` (1 new test added; existing
  tests UNCHANGED) per D-32-06.
- Operator checkpoint record (no file artifact -- the checkpoint task documents the
  operator action and what to record for Plan 32-02).
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-device-flow-state-machine-auth/32-CONTEXT.md
@.planning/phases/32-device-flow-state-machine-auth/32-RESEARCH.md
@.planning/phases/32-device-flow-state-machine-auth/32-VALIDATION.md
@.planning/phases/31-credential-subprocess-layer-auth/31-02-SUMMARY.md
@tests/architecture/no-credential-leak.test.ts
@extensions/pi-claude-marketplace/platform/git-credential.ts
@extensions/pi-claude-marketplace/platform/git.ts
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: OPERATOR CHECKPOINT -- register (or confirm) GitHub OAuth App for Device Flow</name>
  <what-built>
    Nothing automated yet. This task surfaces P32-7: Phase 32's `GITHUB_OAUTH_CLIENT_ID`
    constant in `extensions/pi-claude-marketplace/domain/github-auth.ts` (Plan 32-02
    Task 3) must hold the registered OAuth App client_id before the extension can hit
    real GitHub. The client_id is a PUBLIC OAuth identifier (RFC 8628 §3.1) -- safe to
    commit to the repository; the client_secret (NOT used in Device Flow) is the real
    secret.

    The unit tests Plan 32-02 ships use mock HTTP and do NOT care about the constant
    value -- the placeholder ships safely into the repository. The operator action
    matters for production runtime (Phase 36 integration smoke + first real `marketplace
    add <private-github-url>` invocation).
  </what-built>
  <how-to-verify>
    1. Visit https://github.com/settings/developers (Settings -> Developer settings -> OAuth Apps).
    2. Confirm whether an OAuth App for `pi-claude-marketplace` is already registered.
       If yes, skip to step 5. If no, click "New OAuth App".
    3. Register the new app with:
       - Application name: `pi-claude-marketplace`
       - Homepage URL: https://github.com/acolomba/pi-claude-marketplace
       - Authorization callback URL: any placeholder (e.g. http://localhost) -- Device
         Flow does NOT use a callback URL, but the field is required for OAuth App
         creation.
       - Click "Register application".
    4. On the new app's page, scroll to "Device Flow" and CHECK the "Enable Device Flow"
       box. Save.
    5. Copy the "Client ID" value (a hex-like string, e.g. `Iv1.abc123...`).
    6. Record the client_id here as the resume-signal text (it is public; safe to commit
       to PLAN summary / commit message). If you prefer to defer real-GitHub usage until
       Phase 36, type "placeholder ok" -- Plan 32-02 will ship the constant
       `"GITHUB_OAUTH_APP_CLIENT_ID_PLACEHOLDER"` per the planning instructions and the
       operator will substitute it before the first production run.

    Expected outcomes:
    - Operator records either a real client_id (e.g. `Iv1.abc123def456...`) OR the
      sentinel string "placeholder ok" indicating Plan 32-02 should ship the
      `GITHUB_OAUTH_APP_CLIENT_ID_PLACEHOLDER` literal.
    - Either outcome unblocks Plan 32-02; the mock-HTTP unit tests do not depend on
      the real client_id.
  </how-to-verify>
  <read_first>
    - .planning/phases/32-device-flow-state-machine-auth/32-CONTEXT.md (D-32-03)
    - .planning/phases/32-device-flow-state-machine-auth/32-RESEARCH.md (Pitfall P32-7 + Assumption A5)
  </read_first>
  <acceptance_criteria>
    - Operator records ONE of: (a) a real OAuth App client_id string, or (b) the
      sentinel `placeholder ok`.
    - The chosen value is captured in this plan's SUMMARY.md so Plan 32-02 Task 3 can
      substitute the constant deterministically.
    - The recorded value is treated as public (operator does NOT need to mask it; the
      OAuth App client_id is a public identifier per RFC 8628 §3.1).
  </acceptance_criteria>
  <resume-signal>Reply with one of: a real OAuth App client_id string (e.g. `Iv1.abc123...`) or the literal `placeholder ok`. Either value unblocks Plan 32-02.</resume-signal>
  <done>
    The operator has either (1) confirmed an existing OAuth App + recorded its client_id,
    OR (2) registered a new OAuth App + recorded the new client_id, OR (3) explicitly
    deferred real-GitHub usage by typing `placeholder ok`. Plan 32-02 Task 3 can read
    this plan's SUMMARY to determine which constant value to commit.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Amend tests/architecture/no-credential-leak.test.ts with Phase-32 AUTH-09 gate</name>
  <files>tests/architecture/no-credential-leak.test.ts</files>
  <behavior>
    - A new test block titled "AUTH-09 (Phase 32): domain/github-auth.ts never interpolates a token in an Error or notifyFn message" exists in the file
    - The new test reads `extensions/pi-claude-marketplace/domain/github-auth.ts` from disk if present, strips comments with the existing `stripComments` helper, and asserts a forbidden-pattern regex does NOT match
    - The forbidden pattern catches template-literal OR string-concatenation interpolation of `access_token` / `accessToken` / `cred.<field>` / `r.accessToken` INSIDE either `new Error(...)` constructors OR `notifyFn(...)` calls
    - When `domain/github-auth.ts` does NOT exist on disk (i.e. before Plan 32-02 lands the file), the test passes vacuously with an explanatory `assert.ok(true, "...")` (mirrors the Phase 31 vacuous-pass pattern)
    - The two existing tests (state-write field gate; Phase 31 git-credential.ts Error-interpolation gate) are NOT modified -- additive change only
    - `node --test tests/architecture/no-credential-leak.test.ts` exits 0 with 3 tests passing (2 existing + 1 new); the new test is vacuous-pass while domain/github-auth.ts is absent
    - `npm run check` exits 0 with 1278/1278 tests (1277 baseline + 1 new)
  </behavior>
  <action>
    Add ONE new `test(...)` block to `tests/architecture/no-credential-leak.test.ts` per
    D-32-06 and RESEARCH Example 5. Do NOT modify the two existing tests, the
    `STATE_WRITE_FILES` constant, the `FORBIDDEN_STATE_FIELDS` regex, the
    `GIT_CREDENTIAL_FILE` constant, the `stripComments` helper, or any imports.

    The new test block must:
    1. Declare a new local constant `GITHUB_AUTH_FILE = "extensions/pi-claude-marketplace/domain/github-auth.ts"` (place it next to the existing `GIT_CREDENTIAL_FILE` constant for symmetry).
    2. Use the test title exactly: `"AUTH-09 (Phase 32): domain/github-auth.ts never interpolates a token in an Error or notifyFn message"`.
    3. Use `access(...)` from `node:fs/promises` (already imported) to detect file presence; on absence, `assert.ok(true, "domain/github-auth.ts not yet authored; AUTH-09 Phase-32 gate inactive until Plan 32-02")` and `return` (vacuous pass -- mirrors the Phase 31 Test 2 pattern at lines 68-82).
    4. On presence, read the file with `readFile(...)` + `"utf8"`, call `stripComments(src)`, and apply a forbidden-pattern regex that catches BOTH `new Error(...)` AND `notifyFn(...)` interpolation of token field names.
    5. The regex must catch (combined into one pattern, per RESEARCH Example 5 lines 1000-1001): template-literal `${...access_token|accessToken|cred.<lowercase_field>|r.accessToken...}` OR string-concatenation `... + access_token|accessToken|cred.<lowercase_field>|r.accessToken` INSIDE a `new Error(...)` constructor opener OR a `notifyFn(...)` call opener. Use the case-insensitive `/i` flag.
    6. Assert `equal(regex.test(stripped), false, "Error or notifyFn in domain/github-auth.ts interpolates a token field (AUTH-09 violation)")`.

    Insertion point: append the new `test(...)` block at the end of the file (after the existing Phase 31 git-credential.ts Error-interpolation gate at line 95). Do NOT reshuffle existing code.

    Do NOT add new imports -- everything the new test needs (`access`, `readFile`, `path`, `test`, `assert`, `REPO_ROOT`, `stripComments`) is already imported / defined in the file.

    Naming + style: follow the existing file's two-blank-lines-between-tests convention; reuse `REPO_ROOT` constant; reuse `stripComments` helper.

    Verification after the edit:
    - `node --test tests/architecture/no-credential-leak.test.ts` exits 0 with 3 tests (2 existing GREEN + 1 new vacuous-GREEN).
    - `npm run check` exits 0; the new test count is 1278 (1277 baseline + 1 added).
    - `grep -c "^test(" tests/architecture/no-credential-leak.test.ts` returns `3`.
    - `grep -c "domain/github-auth.ts" tests/architecture/no-credential-leak.test.ts` returns at least `1` (the new `GITHUB_AUTH_FILE` constant).

    Commit message (per CLAUDE.md Conventional Commits + worktree SKIP=trufflehog discipline):
    `test(32): extend AUTH-09 gate to domain/github-auth.ts (Plan 32-01)`
  </action>
  <read_first>
    - tests/architecture/no-credential-leak.test.ts (the file being amended; understand existing structure, stripComments helper, REPO_ROOT constant, vacuous-pass pattern at lines 68-82)
    - .planning/phases/32-device-flow-state-machine-auth/32-RESEARCH.md (Example 5 lines 969-1007 -- the exact regex shape and vacuous-pass pattern)
    - .planning/phases/32-device-flow-state-machine-auth/32-CONTEXT.md (D-32-06 -- AUTH-09 discipline in Device Flow)
    - extensions/pi-claude-marketplace/platform/git-credential.ts (Phase 31 reference -- the Error-interpolation discipline the Phase 32 gate mirrors)
    - .planning/phases/31-credential-subprocess-layer-auth/31-02-SUMMARY.md (Architecture Gates Status table -- confirms how the Phase 31 gate transitioned from vacuous to active)
  </read_first>
  <acceptance_criteria>
    - **Source assertion:** `grep -c "^test(" tests/architecture/no-credential-leak.test.ts` returns exactly `3` (two existing + one new).
    - **Source assertion:** `grep -n "AUTH-09 (Phase 32)" tests/architecture/no-credential-leak.test.ts` returns at least one match.
    - **Source assertion:** `grep -n "domain/github-auth.ts" tests/architecture/no-credential-leak.test.ts` returns at least one match (the new GITHUB_AUTH_FILE constant).
    - **Source assertion:** `grep -n "notifyFn" tests/architecture/no-credential-leak.test.ts` returns at least one match (the new regex covers notifyFn calls).
    - **Source assertion:** `grep -c "STATE_WRITE_FILES" tests/architecture/no-credential-leak.test.ts` is unchanged from baseline (the existing test 1 constant is untouched).
    - **Source assertion:** `grep -c "platform/git-credential.ts never interpolates" tests/architecture/no-credential-leak.test.ts` returns `1` (Phase 31 Test 2 title still present, unmodified).
    - **Behavior assertion (vacuous-pass active):** `node --test tests/architecture/no-credential-leak.test.ts` exits 0 with 3 passing tests; the new test reports a vacuous-pass message mentioning "not yet authored" while `extensions/pi-claude-marketplace/domain/github-auth.ts` is absent.
    - **Test command:** `npm run check` exits 0 with 1278/1278 tests passing (or 1277+1 -- the +1 is the new test).
    - **Test command:** The new test contains an `access(...)` call against `extensions/pi-claude-marketplace/domain/github-auth.ts` (use `grep -n "access(.*github-auth" tests/architecture/no-credential-leak.test.ts` to confirm).
    - **Negative source assertion:** The two existing tests are byte-for-byte unchanged -- run `git diff tests/architecture/no-credential-leak.test.ts` and confirm only NEW lines are added (no deletions or modifications inside the existing test bodies, the helpers, or the imports).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/no-credential-leak.test.ts && grep -c "^test(" tests/architecture/no-credential-leak.test.ts | grep -v '^#' | grep -q "^3$" && npm run check</automated>
  </verify>
  <done>
    A third test block exists in `tests/architecture/no-credential-leak.test.ts` titled
    `"AUTH-09 (Phase 32): domain/github-auth.ts never interpolates a token in an Error
    or notifyFn message"`. It is vacuous-pass while `domain/github-auth.ts` is absent.
    It will activate automatically when Plan 32-02 lands the production file. The two
    existing tests are unchanged. `npm run check` exits 0 with 1278/1278 tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> repo (OAuth App registration) | Operator interacts with GitHub UI; records public client_id; no secret material crosses into the repo |
| test file -> filesystem (architecture gate read) | Test reads source files at test-runtime via `fs/promises.readFile`; read-only; no execution of read content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01-01 | Information Disclosure | OAuth App `client_id` recorded in SUMMARY.md / commit message | accept | client_id is a PUBLIC OAuth identifier per RFC 8628 §3.1; safe to commit. Documented in Task 1 how-to-verify step 6. |
| T-32-01-02 | Information Disclosure | OAuth App `client_secret` accidentally recorded by operator | mitigate | Task 1 how-to-verify explicitly references CLIENT_ID only; never asks for client_secret. Device Flow does NOT use client_secret -- the operator has no reason to copy it. |
| T-32-01-03 | Tampering | Architecture test file amended in a way that weakens the existing Phase-31 gate | mitigate | Task 2 acceptance criteria includes a negative source assertion verifying byte-for-byte preservation of the two existing tests via `git diff`. The action explicitly forbids modifying the existing constants / helpers. |
| T-32-01-04 | Repudiation | Vacuous-pass logic could mask a real authoring failure (file present but unreadable) | accept | Phase 31 already uses the same vacuous-pass pattern (lines 68-82 of the file being amended); same false-negative envelope. The gate activates as soon as the file becomes readable; the very next test run after Plan 32-02 lands will catch any real violation. |
| T-32-01-05 | Information Disclosure | A future contributor adds a `console.log(accessToken)` in `domain/github-auth.ts` without going through `new Error(...)` or `notifyFn(...)` | accept | Out of scope for AUTH-09 gate by design (RESEARCH Pitfall P32-2 confidence note -- the gate catches the most common surfaces, not all possible surfaces; code-review backstop is required). Phase 36 integration smoke is the runtime backstop for residual leaks. |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | Plan 32-01 installs NO new packages (verified per RESEARCH §Package Legitimacy Audit: zero new packages). Phase 32 baseline `package.json` is unchanged by this plan. |
</threat_model>

<verification>
After both tasks complete:

1. **Operator checkpoint outcome captured:** Plan SUMMARY.md records either a real
   client_id string or the sentinel `placeholder ok` from Task 1.
2. **AUTH-09 gate extended:** `tests/architecture/no-credential-leak.test.ts` contains
   exactly 3 `test(...)` blocks (the 2 existing + 1 new Phase-32 block).
3. **Vacuous-pass active:** `node --test tests/architecture/no-credential-leak.test.ts`
   exits 0 with all 3 tests passing; the new test reports vacuous-pass
   ("not yet authored") because `domain/github-auth.ts` does not yet exist.
4. **Existing tests preserved:** `git diff tests/architecture/no-credential-leak.test.ts`
   shows ONLY additions (no modifications inside the existing test bodies or helpers).
5. **Pipeline GREEN:** `npm run check` exits 0 with 1278/1278 tests (baseline 1277 + 1
   new).
6. **No new packages:** `git diff package.json` shows no changes (verified per
   RESEARCH §Package Legitimacy Audit -- zero new packages).
7. **Commit landed:** A single commit titled
   `test(32): extend AUTH-09 gate to domain/github-auth.ts (Plan 32-01)` (or close
   variant per Conventional Commits) is present on the working branch.
</verification>

<success_criteria>
- `tests/architecture/no-credential-leak.test.ts` has 3 test blocks (2 existing UNCHANGED + 1 new Phase-32 block).
- The new test passes vacuously now AND will activate when Plan 32-02 creates `domain/github-auth.ts`.
- Operator has registered the OAuth App OR explicitly chosen the placeholder path for Plan 32-02 to use.
- The operator's chosen client_id (real or sentinel) is recorded in the Plan 32-01 SUMMARY.md so Plan 32-02 can substitute deterministically.
- `npm run check` exits 0 with 1278/1278 tests passing.
- No new npm packages added.
- Commit message follows Conventional Commits + worktree SKIP=trufflehog discipline per CLAUDE.md.
</success_criteria>

<output>
Create `.planning/phases/32-device-flow-state-machine-auth/32-01-SUMMARY.md` recording:
- The operator's recorded client_id (or `placeholder ok` sentinel) for Plan 32-02 to substitute.
- Confirmation of the new AUTH-09 test block (vacuous-pass state).
- `npm run check` test count (expected: 1278/1278).
- Commit hash of the AUTH-09 amendment commit.
</output>
