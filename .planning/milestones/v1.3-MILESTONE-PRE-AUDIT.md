---
milestone: v1.3
audited: 2026-05-24T00:00:00Z
status: gaps_found
scores:
  requirements: 34/38
  phases: 2/3
  integration: 7/10
  flows: 3/5
gaps:
  requirements:
    - id: "CMC-13"
      status: "partial"
      phase: "Phase 13 (sub-wave 2a / import)"
      claimed_by_plans: ["13-01-01-PLAN.md", "13-02a-01-PLAN.md", "13-02b-01-PLAN.md", "13-02d-01-PLAN.md"]
      completed_by_plans: ["13-01-01-SUMMARY.md", "13-02a-01-SUMMARY.md", "13-02b-01-SUMMARY.md", "13-02c-01-SUMMARY.md", "13-02d-01-SUMMARY.md"]
      verification_status: "passed (phase) / partial (integration)"
      evidence: "orchestrators/import/execute.ts:474-475 hard-codes declaresAgents=false / declaresMcp=false for every installed-plugin row on `/claude:plugin import`. Source comment acknowledges this as a 'follow-up (sub-wave 2b finalization)'. Soft-dep markers `{requires pi-subagents}` / `{requires pi-mcp}` therefore never fire on the import surface, breaking MSG-SD-1..3 on this command. Phase 13 VERIFICATION.md marked CMC-13 SATISFIED; integration check disagrees."
    - id: "CMC-16"
      status: "partial"
      phase: "Phase 13 (sub-wave 2a-02)"
      claimed_by_plans: ["13-01-02-PLAN.md", "13-02a-02-PLAN.md"]
      completed_by_plans: ["13-01-02-SUMMARY.md", "13-02a-02-SUMMARY.md"]
      verification_status: "passed (phase) / unwired (integration)"
      evidence: "presentation/manual-recovery.ts::renderManualRecovery + ManualRecoveryLine RowSpec exist and are tested in isolation via the catalog UAT fixture, but NO production orchestrator constructs a ManualRecoveryLine. orchestrators/plugin/reinstall.ts (lines 498-548) maps a propagated ManualRecoveryError to a PluginCascadeRow{status:'failed', reasons:['rollback partial']} cascade row instead of emitting the separate manual-recovery anchor line. orchestrators/marketplace/remove.ts:73,91-96 keeps the import via `void renderManualRecovery;` (dead-code seam). The catalog binding for `(manual recovery)` row state is therefore aspirational rather than verified end-to-end."
    - id: "CMC-34"
      status: "partial"
      phase: "Phase 13 (sub-wave 2b/2c)"
      claimed_by_plans: ["13-01-01-PLAN.md", "13-02b-01-PLAN.md", "13-02c-01-PLAN.md"]
      completed_by_plans: ["13-01-01-SUMMARY.md", "13-02b-01-SUMMARY.md", "13-02c-01-SUMMARY.md"]
      verification_status: "passed (phase) / blocker (integration)"
      evidence: "6 edge handlers route argument-validation failures through `notifyError(ctx, msg + '\\n' + USAGE)` instead of `notifyUsageError(ctx, msg, USAGE)`: edge/handlers/plugin/{list.ts:40,57,65, reinstall.ts:34,44,52,86, update.ts:36,48,61, bootstrap.ts:37,42,48}, edge/handlers/marketplace/{list.ts:28 (via args-schema callback), autoupdate.ts:37 (via callback)}. Both wrappers produce severity='error', but `notifyUsageError` is contractually mandated to compose `${message}\\n\\n${usageBlock}` (blank-line separator per MSG-NC-2). The 6 handlers emit a single newline instead, violating the MSG-NC-2 byte-exact user contract. tests/edge/router.test.ts:70-86 only asserts severity + USAGE-block presence, not the `\\n\\n` separator, so the regression is not caught."
    - id: "CMC-38"
      status: "unsatisfied"
      phase: "Phase 14 (not started)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Phase 14 directory does not exist under .planning/phases/. ROADMAP.md marks Phase 14 as `[ ] Not started`. REQUIREMENTS.md CMC-38 row marked Pending. No drift-guard test suite reads style-guide YAML frontmatter as the binding contract today; future contributors can hand-roll status tokens / reasons / wrong wrappers without `npm run check` failing. The 3 partial CMCs above (CMC-13, CMC-16, CMC-34) are exactly the surfaces CMC-38 would have caught structurally."
  integration:
    - finding: "renderManualRecovery is orphan in production"
      severity: "BLOCKER"
      affected_reqs: ["CMC-16", "CMC-25", "CMC-34"]
      file: "extensions/pi-claude-marketplace/presentation/manual-recovery.ts"
      note: "Composer exists + tested in isolation; no orchestrator emits ManualRecoveryLine. reinstall.ts reroutes ManualRecoveryError into the rollback-partial cascade row instead."
    - finding: "import surface hard-codes soft-dep predicates to false"
      severity: "BLOCKER"
      affected_reqs: ["CMC-13", "CMC-27"]
      file: "extensions/pi-claude-marketplace/orchestrators/import/execute.ts:474-475"
      note: "declaresAgents/Mcp never propagate from PluginInstalledOutcome to PluginCascadeRow on import."
    - finding: "6 edge handlers route USAGE errors through notifyError"
      severity: "BLOCKER"
      affected_reqs: ["CMC-22", "CMC-25", "CMC-26", "CMC-28", "CMC-29", "CMC-33", "CMC-34"]
      files:
        - "edge/handlers/plugin/list.ts:40,57,65"
        - "edge/handlers/plugin/reinstall.ts:34,44,52,86"
        - "edge/handlers/plugin/update.ts:36,48,61"
        - "edge/handlers/plugin/bootstrap.ts:37,42,48"
        - "edge/handlers/marketplace/list.ts:28"
        - "edge/handlers/marketplace/autoupdate.ts:37"
      note: "MSG-NC-2 mandates `\\n\\n` separator between message and Usage block; these handlers emit `\\n`. Existing router tests verify presence, not byte-shape."
    - finding: "transaction/rollback.ts hand-composes (failed) {rollback partial} literal"
      severity: "WARNING"
      affected_reqs: ["CMC-17", "CMC-38"]
      file: "extensions/pi-claude-marketplace/transaction/rollback.ts:56-62"
      note: "Bypasses renderRow + renderRollbackPartial. Documented intentional (transaction layer has no plugin/scope/marketplace context). Token strings correct today but structurally unprotected -- exactly the surface CMC-38 drift guard would cover."
    - finding: "MARKETPLACE_LABEL_PROBE constant duplicated across 3 files"
      severity: "WARNING"
      affected_reqs: []
      note: "DRY drift risk; unprotected by current tests."
  flows:
    - name: "reinstall partial-failure → manual recovery + rollback-partial + cause-chain + reload-hint"
      status: "DEGRADED"
      reason: "Manual-recovery anchor line is never emitted (see CMC-16). User sees `(failed) {rollback partial}` cascade row instead of the separate anchor line documented in the catalog."
    - name: "/claude:plugin import → per-row soft-dep markers"
      status: "DEGRADED"
      reason: "Soft-dep markers `{requires pi-subagents}` / `{requires pi-mcp}` never fire on the import surface (see CMC-13)."
tech_debt:
  - phase: "12-messaging-foundations-renderer-primitives"
    items:
      - "presentation/reload-hint.ts:50 TODO (Phase 13, MSG-RH-1) -- intentional deferral; landed in Phase 13"
      - "shared/grammar/reasons.ts comment block documents v1.3 additions over V1 set; reconciliation against 24→23 reasons count completed (no debt)"
  - phase: "13-conformance-refactor-es-5-supersession"
    items:
      - "transaction/rollback.ts:57 hand-composed `(failed) {rollback partial}` literal bypasses renderRow (documented intentional; unprotected without CMC-38)"
      - "MARKETPLACE_LABEL_PROBE constant duplicated across 3 files (DRY drift risk)"
      - "IN-02 + IN-03 review findings skipped with documented rationale (architectural decision + doc-only on historical artefacts) per 13-REVIEW-FIX.md"
nyquist:
  compliant_phases: [13]
  partial_phases: [12]
  missing_phases: [14]
  overall: "INCOMPLETE -- Phase 12 VALIDATION.md has nyquist_compliant: false / wave_0_complete: false; Phase 14 has no VALIDATION.md."
phase_14_status: "NOT STARTED -- Phase 14 directory does not exist; CMC-38 unsatisfied; milestone definition of done not met."
---

# v1.3 Consistent Messaging -- Milestone Audit Report

**Audited:** 2026-05-24
**Status:** `gaps_found`
**Phases verified:** 12 (PASSED), 13 (PASSED), 14 (NOT STARTED)
**HEAD:** `0e55ead` (branch `gsd/v1.3-replan-catalog`)

## Executive Summary

The v1.3 milestone is **NOT complete**. Phase 14 (Drift Guard & Test Alignment) has not been started, so its single requirement (CMC-38) is unsatisfied. Additionally, the integration check surfaced three integration-layer gaps that Phase 12 / Phase 13 VERIFICATION.md files independently marked satisfied:

1. **CMC-13** (per-row soft-dep predicate) -- degraded on the `/claude:plugin import` surface (hard-coded `false`).
2. **CMC-16** (manual-recovery line) -- composer is orphan; no production orchestrator constructs `ManualRecoveryLine`.
3. **CMC-34** (entity vs usage errors) -- 6 edge handlers route USAGE errors through `notifyError(..., msg + "\n" + USAGE)` instead of `notifyUsageError(...)`, violating the MSG-NC-2 blank-line separator.

The 3 cross-phase gaps are exactly the kind of structural drift that **Phase 14 / CMC-38 was designed to catch**. The milestone cannot close until Phase 14 lands AND the 3 partial CMCs are remediated.

## Score

| Dimension | Score | Notes |
|---|---|---|
| Requirements | 34/38 | CMC-13, CMC-16, CMC-34 partial; CMC-38 unsatisfied |
| Phases | 2/3 | Phase 14 not started |
| Integration | 7/10 | 3 BLOCKER + 2 WARNING findings |
| Flows | 3/5 | reinstall manual-recovery and import soft-dep flows degraded |

## Phase Verifications

| Phase | Status | Score | Verified | Critical Gaps | Tech Debt |
|---|---|---|---|---|---|
| 12 | passed | 7/7 truths | 2026-05-22 | None | reload-hint.ts:50 TODO (resolved in Phase 13) |
| 13 | passed | 5/5 truths | 2026-05-24 | None claimed; integration check found 3 | transaction/rollback.ts literal; label-probe dup; IN-02/03 skipped with rationale |
| 14 | **MISSING** | -- | -- | Entire phase not started | -- |

## Requirements Coverage (3-Source Cross-Reference)

Phase 12 SUMMARY frontmatter `requirements-completed` ∪ Phase 13 SUMMARY frontmatter `requirements-completed` covers CMC-01..37 (37 entries). REQUIREMENTS.md traceability table marks 37/38 as `Complete` (CMC-38 pending). Phase VERIFICATION.md files mark all 37 SATISFIED. **However**, the integration check downgrades CMC-13, CMC-16, and CMC-34 to PARTIAL based on production wiring evidence.

### Unsatisfied Requirements

| REQ-ID | Phase | Final Status | Root Cause |
|---|---|---|---|
| CMC-13 | Phase 13 | partial | `orchestrators/import/execute.ts:474-475` hard-codes `declaresAgents=false / declaresMcp=false` for imported plugins; soft-dep markers never fire on `/claude:plugin import`. |
| CMC-16 | Phase 13 | partial / unwired | `renderManualRecovery` + `ManualRecoveryLine` exist + tested in isolation, but no production orchestrator constructs the row. `reinstall.ts` reroutes `ManualRecoveryError` into the cascade row. |
| CMC-34 | Phase 13 | partial / blocker | 6 edge handlers emit `msg + "\n" + USAGE` instead of using `notifyUsageError` (which composes `msg + "\n\n" + USAGE`). MSG-NC-2 blank-line separator contract violated. |
| CMC-38 | Phase 14 | unsatisfied | Phase 14 not started. No drift-guard suite reads style-guide YAML frontmatter as the binding contract. |

### Satisfied Requirements (34)

CMC-01, CMC-02, CMC-03, CMC-04, CMC-05, CMC-06, CMC-07, CMC-08, CMC-09, CMC-10, CMC-11, CMC-12, CMC-14, CMC-15, CMC-17, CMC-18, CMC-19, CMC-20, CMC-21, CMC-22*, CMC-23, CMC-24, CMC-25*, CMC-26*, CMC-27*, CMC-28*, CMC-29*, CMC-30, CMC-31, CMC-32, CMC-33*, CMC-35, CMC-36, CMC-37.

*Marked satisfied on the cascade / catalog-conformance happy path but transitively affected by the CMC-34 edge-handler routing gap when their command is invoked with bad arguments. Not re-classified because the structural contract on the success / cascade path is met; the affected surface is purely the argument-error path.

## Cross-Phase Integration

| Finding | Severity | Affected REQs |
|---|---|---|
| `renderManualRecovery` orphan in production | BLOCKER | CMC-16, CMC-25, CMC-34 |
| `import/execute.ts` hard-codes soft-dep predicates | BLOCKER | CMC-13, CMC-27 |
| 6 edge handlers route USAGE errors through `notifyError` | BLOCKER | CMC-22, CMC-25, CMC-26, CMC-28, CMC-29, CMC-33, CMC-34 |
| `transaction/rollback.ts` hand-composed `(failed) {rollback partial}` literal | WARNING | CMC-17, CMC-38 |
| `MARKETPLACE_LABEL_PROBE` duplicated across 3 files | WARNING | -- |

## End-to-End Flows

| # | Flow | Status | Notes |
|---|---|---|---|
| 1 | `install` → success → reload-hint → `list` reflects | WIRED | -- |
| 2 | `marketplace add --scope project` → adoption-on-`list` round-trip | WIRED | fold-adoption integration test green |
| 3 | `reinstall` partial-failure → rollback-partial + manual-recovery + cause-chain + reload-hint | **DEGRADED** | Manual-recovery anchor line never emitted (CMC-16) |
| 4 | `marketplace remove` with dependent plugins → dual trailer (reload above retry) | WIRED | -- |
| 5 | `import` cross-scope → per-scope headers + cascade children + per-row soft-dep | **DEGRADED** | Soft-dep markers hard-coded `false` (CMC-13 on `import`) |

## Nyquist Validation Coverage

| Phase | VALIDATION.md | Compliant | Action |
|---|---|---|---|
| 12 | exists | false (wave_0_complete: false) | `/gsd:validate-phase 12` |
| 13 | exists | true | None -- green |
| 14 | missing | -- | Phase not started; validation lands with Phase 14 |

Phase 12's VALIDATION.md is in draft status with `nyquist_compliant: false` and `wave_0_complete: false`. Run `/gsd:validate-phase 12` to close the validation gap retroactively (the Phase 12 implementation passed verification; Nyquist coverage is the open item).

## Tech Debt Aggregated

**Phase 12:**
- `presentation/reload-hint.ts:50` -- `TODO (Phase 13, MSG-RH-1)` -- resolved in Phase 13 per design.

**Phase 13:**
- `transaction/rollback.ts:57` -- hand-composed `(failed) {rollback partial}` literal bypasses `renderRow`. Documented intentional in source; unprotected without CMC-38.
- `MARKETPLACE_LABEL_PROBE` constant duplicated across 3 files.
- `IN-02` + `IN-03` review findings skipped with rationale in `13-REVIEW-FIX.md` (architectural decision + doc-only on historical artefacts).

## Gap Closure Plan

1. **Phase 14 (Drift Guard)** -- must land. CMC-38 covers structural enforcement against:
   - Hand-rolled status tokens outside `STATUS_TOKENS`
   - Hand-rolled reasons outside `REASONS`
   - Wrong wrapper for usage errors (would catch the CMC-34 violation today)
   - Dual-trailer order inversion
   - `transaction/rollback.ts` literal drift
   - Hand-composed `(failed)` / `(skipped)` strings
2. **CMC-13 closure** -- plumb `declaresAgents` / `declaresMcp` through `PluginInstalledOutcome` → `import/execute.ts` cascade-row mapping. Add catalog UAT fixture covering `(installed) {requires pi-subagents}` on `import`.
3. **CMC-16 closure** -- choose between (a) emit `ManualRecoveryLine` from `reinstall.ts` when `ManualRecoveryError` propagates, OR (b) document that the manual-recovery anchor only fires from `bridges/*/stage.ts` migration failures (which currently appears to be the actual call path), and remove the catalog binding for the orphan state. Update `marketplace/remove.ts:73,91-96` to either consume the composer or drop the dead-code import seam.
4. **CMC-34 closure** -- migrate the 6 edge handlers to `notifyUsageError`. Add a router test asserting the `\n\n` separator byte-exactly (the existing test only checks presence).

The natural sequencing is **(2, 3, 4) before Phase 14**, so that when CMC-38's drift guard lands it does not retroactively fail `npm run check` on the existing partials. Alternatively, Phase 14 can ingest the 3 partials as its initial green-baseline failures and close them as part of the phase scope.
