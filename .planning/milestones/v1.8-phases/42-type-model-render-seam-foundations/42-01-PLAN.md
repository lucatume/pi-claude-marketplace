---
phase: 42-type-model-render-seam-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - tests/architecture/notify-types.test.ts
  - tests/shared/notify-v2.test.ts
  - tests/architecture/catalog-uat.test.ts
  - docs/output-catalog.md
autonomous: true
requirements:
  - INFO-04
  - INFO-08
tags:
  - notify
  - discriminated-union
  - atomic-supersession
  - closed-set
must_haves:
  truths:
    - "NotificationMessage is a 3-arm discriminated union (cascade | marketplace-info | plugin-info) reachable from the existing exported type name"
    - "REASONS tuple contains the literal 'not added' as a new entry"
    - "Rendering a payload with reason 'not added' produces the exact byte form '⊘ <name> [<scope>] (failed) {not added}' at column 0 with severity 'error'"
    - "wrapDescription(text, 4, 66) wraps on word boundaries with no ellipsis, indenting each emitted line with 4 spaces"
    - "All v1.0-v1.7 cascade catalog UAT fixtures remain byte-equal (zero behavior change for any non-info call site)"
    - "npm run check exits 0 at the single atomic-supersession commit boundary"
    - "The REASONS extension, type variants, wrapDescription helper, render arms, first catalog state, catalog-uat fixture, length-lock test, and per-status unit tests land in ONE commit (SC#5)"
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "MarketplaceInfoMessage + PluginInfoMessage variant types; CascadeNotificationMessage variant alias; 'not added' REASON; file-private wrapDescription helper; file-private renderMarketplaceInfo + renderPluginInfo functions; notify() dispatcher rewritten to switch on message.kind with assertNever default"
      contains: "not added"
    - path: "tests/architecture/notify-types.test.ts"
      provides: "Compile-time proofs for the new variants (shape + kind discriminator + componentsResolved discriminator); REASONS length-lock at 29; closed-set proof asserting 'not added' membership"
      contains: "not added"
    - path: "tests/shared/notify-v2.test.ts"
      provides: "Per-status unit tests for wrapDescription (≥6 tests covering empty/short/exact/long/over-length-single-word/whitespace-normalization edge cases); byte-form unit test for '{not added}' row rendering; byte-form unit tests for renderMarketplaceInfo (github + path variants) and renderPluginInfo (componentsResolved true + false variants)"
      contains: "not added"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "New FIXTURES outer-map entry for '/claude:plugin marketplace info <name>' containing the 'scope-mismatch-not-added' inner fixture that maps to the NotificationMessage payload constructed for the {not added} catalog state"
      contains: "scope-mismatch-not-added"
    - path: "docs/output-catalog.md"
      provides: "New H2 section '## `/claude:plugin marketplace info <name>`' containing at least one fenced byte block annotated '<!-- catalog-state: scope-mismatch-not-added -->' that demonstrates the {not added} {scope-mismatch} byte form"
      contains: "scope-mismatch-not-added"
  key_links:
    - from: "extensions/pi-claude-marketplace/shared/notify.ts"
      to: "tests/architecture/notify-types.test.ts"
      via: "imported REASONS / MarketplaceInfoMessage / PluginInfoMessage / NotificationMessage type symbols proven via compile-time type assertions"
      pattern: "import .* from .*shared/notify"
    - from: "extensions/pi-claude-marketplace/shared/notify.ts"
      to: "tests/shared/notify-v2.test.ts"
      via: "notify() entry point + NotificationMessage construction with kind: 'marketplace-info' and kind: 'plugin-info' + direct wrapDescription import for unit tests"
      pattern: "notify\\(|wrapDescription\\("
    - from: "docs/output-catalog.md"
      to: "tests/architecture/catalog-uat.test.ts"
      via: "loadCatalogExamples sectionRe + stateRe pairing: H2 section name 'marketplace info <name>' MUST equal the FIXTURES outer-map key byte-for-byte; '<!-- catalog-state: scope-mismatch-not-added -->' MUST equal the FIXTURES inner-map key byte-for-byte"
      pattern: "scope-mismatch-not-added"
---

<objective>
Land the complete v1.8 info-surface type model in `extensions/pi-claude-marketplace/shared/notify.ts`: two new discriminated-union variants (`MarketplaceInfoMessage` and `PluginInfoMessage`), one new `REASONS` closed-set entry (`"not added"`), one file-private `wrapDescription` renderer helper, two new file-private render functions (`renderMarketplaceInfo`, `renderPluginInfo`), and the `notify()` dispatcher rewritten to switch on a `kind` discriminator with `assertNever` exhaustiveness -- all delivered in ONE atomic commit alongside the first catalog state and UAT fixture that consume the new `"not added"` REASON. This satisfies INFO-04 (`--scope` mismatch byte form) and INFO-08 (closed-set extension via atomic supersession) per the ROADMAP Phase 42 success criteria.

Purpose: The v1.3 retrospective lesson (`c4d87d4`, `dbd149a`) forbids splitting a closed-set extension across commits -- intermediate states either fail catalog-UAT byte-equality or fail typecheck. Phase 42 is the contract phase for v1.8; Phases 43 and 44 build their command surfaces on the contract delivered here without any further closed-set churn.

Output: One commit on the active feature branch containing the type model extensions, the new REASON, the new helper + render arms, the new dispatcher, the new catalog state, the new catalog-uat fixture, the REASONS length-lock test, and the per-status unit tests for `wrapDescription` and the new render arms. Zero behavior change for any v1.0-v1.7 cascade call site.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-type-model-render-seam-foundations/42-CONTEXT.md
@.planning/phases/42-type-model-render-seam-foundations/42-RESEARCH.md
@./CLAUDE.md
@extensions/pi-claude-marketplace/shared/notify.ts
@tests/architecture/notify-types.test.ts
@tests/shared/notify-v2.test.ts
@tests/architecture/catalog-uat.test.ts
@docs/output-catalog.md
@docs/messaging-style-guide.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend type model + closed set + compile-time proofs (no commit yet)</name>
  <files>
    extensions/pi-claude-marketplace/shared/notify.ts,
    tests/architecture/notify-types.test.ts
  </files>
  <read_first>
    extensions/pi-claude-marketplace/shared/notify.ts,
    tests/architecture/notify-types.test.ts,
    .planning/phases/42-type-model-render-seam-foundations/42-RESEARCH.md,
    docs/adr/v2-001-structured-notify.md,
    extensions/pi-claude-marketplace/shared/types.ts
  </read_first>
  <behavior>
    - Test: `REASONS["length"] extends 29 ? true : never` proof passes (length-lock at 29; current length is 28).
    - Test: set-equality proof asserts the new literal `"not added"` is a member of `(typeof REASONS)[number]`.
    - Test: `NotificationMessage` accepts payloads with `kind: "cascade"` (explicit) AND payloads with `kind` omitted (cascade default per RESEARCH Migration Strategy #2 / A4) AND payloads with `kind: "marketplace-info"` AND payloads with `kind: "plugin-info"`.
    - Test: `Extract<NotificationMessage, { kind: "marketplace-info" }>` resolves to a shape with `readonly name: string`, `readonly scope: Scope`, `readonly details: MarketplaceDetails`, `readonly source: { sourceKind: "github"; owner: string; repo: string; ref?: string } | { sourceKind: "path"; absPath: string }`, `readonly lastUpdated?: string`, `readonly description?: string`.
    - Test: `Extract<NotificationMessage, { kind: "plugin-info" }>` resolves to a shape carrying `readonly marketplaceName: string`, `readonly marketplaceScope: Scope`, `readonly marketplaceDetails: MarketplaceDetails`, `readonly plugin: PluginInfoRow`.
    - Test: `Extract<PluginInfoRow, { componentsResolved: true }>` has `components: { agents?: readonly string[]; commands?: readonly string[]; mcp?: readonly string[]; skills?: readonly string[] }` and `dependencies?: readonly string[]`; `Extract<PluginInfoRow, { componentsResolved: false }>` has NO `components` field.
    - Test: PLUGIN_STATUSES length stays exactly 11; MARKETPLACE_STATUSES length stays exactly 7; DEPENDENCIES length stays exactly 2 (unchanged -- guard against accidental drift).
    - Until the renderer arms exist (Task 2) and the dispatcher narrows on `kind`, the typecheck will fail on the renamed top-level switch. Task 2 closes the gap; do NOT commit at the end of Task 1.
  </behavior>
  <action>
    PRE-FLIGHT VERIFICATION (RESEARCH Wave 0 Gaps):

    (a) Confirm no existing REASONS length-lock test exists. Run `git grep -nE "REASONS[^a-zA-Z_]?\\.?\\[?[\"']?length" extensions/pi-claude-marketplace tests/` and verify zero hits (other than the one you are about to add). If a hit is found, reconcile with RESEARCH Pitfall 2 by updating in place instead of adding new.

    (b) Confirm no existing `notify()` call site in `extensions/pi-claude-marketplace/orchestrators/**` or `extensions/pi-claude-marketplace/edge/**` passes a `kind` field to the NotificationMessage payload (orchestrator-local `kind:` discriminators on internal types like `ReinstallPluginsTarget` and `PluginShapeError` are unrelated and unaffected). Run `git grep -nE "notify\\(.*\\{[^}]*kind:" extensions/pi-claude-marketplace/orchestrators/ extensions/pi-claude-marketplace/edge/` and verify zero hits. This validates RESEARCH Assumption A4 (optional `kind?` migration strategy is safe).

    TYPE MODEL CHANGES in `extensions/pi-claude-marketplace/shared/notify.ts`:

    1. Extend the `REASONS` closed-set tuple at line 63 by appending the new entry `"not added"` as the last member. The tuple is `as const`. Do NOT reorder existing entries. After the change, `REASONS.length === 29`. Document the addition in the inline block comment that precedes the tuple (note "Phase 42 / INFO-04 / INFO-08: added 'not added' for --scope mismatch on info surfaces").

    2. Rename the existing `NotificationMessage` interface declaration around line 596 to `CascadeNotificationMessage` and add a `readonly kind?: "cascade"` optional discriminator field. Keep the `readonly marketplaces: readonly MarketplaceNotificationMessage[]` field byte-identical. Add a JSDoc note explaining that `kind` is optional on the cascade variant so v1.0-v1.7 call sites do not need to be migrated (RESEARCH Strategy #2).

    3. Add a new exported interface `MarketplaceInfoMessage` with the exact shape recommended in RESEARCH Pattern 1:
       - `readonly kind: "marketplace-info"`
       - `readonly name: string`
       - `readonly scope: Scope`
       - `readonly details: MarketplaceDetails`
       - `readonly source: { readonly sourceKind: "github"; readonly owner: string; readonly repo: string; readonly ref?: string } | { readonly sourceKind: "path"; readonly absPath: string }`
       - `readonly lastUpdated?: string` (ISO8601; github sources only per INFO-01)
       - `readonly description?: string` (marketplace.json description, optional)

    4. Add a new exported interface `PluginInfoMessage` with the exact shape recommended in RESEARCH Pattern 1:
       - `readonly kind: "plugin-info"`
       - `readonly marketplaceName: string`
       - `readonly marketplaceScope: Scope`
       - `readonly marketplaceDetails: MarketplaceDetails`
       - `readonly plugin: PluginInfoRow`

    5. Add a new exported type `PluginInfoRow = (PluginInfoRowBase & PluginInfoComponentsResolved) | (PluginInfoRowBase & PluginInfoComponentsUnresolved)` and the three supporting interfaces from RESEARCH Pattern 1:
       - `PluginInfoRowBase`: `readonly status: "installed" | "available" | "unavailable" | "failed"`, `readonly name: string`, `readonly version?: string`, `readonly scope?: Scope`, `readonly description?: string`, `readonly reasons?: readonly Reason[]` (populated when status is `"unavailable"` or `"failed"`).
       - `PluginInfoComponentsResolved`: `readonly componentsResolved: true`, `readonly components: { readonly agents?: readonly string[]; readonly commands?: readonly string[]; readonly mcp?: readonly string[]; readonly skills?: readonly string[] }`, `readonly dependencies?: readonly string[]` ("<plugin>@<marketplace>" string form).
       - `PluginInfoComponentsUnresolved`: `readonly componentsResolved: false`.
       - Add JSDoc on `PluginInfoComponentsResolved.components` stating the sort precondition: the renderer assumes pre-sorted input; Phase 44 orchestrator MUST sort by kind name and by per-kind name before constructing the message (RESEARCH Pitfall 5 / Don't Hand-Roll: Sorting components arrays).

    6. Replace the exported `NotificationMessage` type alias to be the 3-arm discriminated union: `export type NotificationMessage = CascadeNotificationMessage | MarketplaceInfoMessage | PluginInfoMessage`. Do not delete `CascadeNotificationMessage` -- it remains exported as part of the union.

    7. Do NOT modify any existing call site in `orchestrators/**`, `edge/**`, or any test fixture file. The optional `kind?` on the cascade variant guarantees the cascade path stays byte-identical (RESEARCH Pitfall 1).

    COMPILE-TIME PROOFS in `tests/architecture/notify-types.test.ts`:

    Add the following type-level assertions following the existing `_l1` / `_l2` / `_l3` length-lock pattern around the `PLUGIN_STATUSES` / `MARKETPLACE_STATUSES` / `DEPENDENCIES` assertions:

    (i) `REASONS` length-lock at 29: `type _Assert_ReasonsLen = (typeof REASONS)["length"] extends 29 ? true : never; export const _l4: _Assert_ReasonsLen = true;` plus an explicit set-membership proof: `type _Assert_NotAddedMember = "not added" extends (typeof REASONS)[number] ? true : never; export const _l4b: _Assert_NotAddedMember = true;`.

    (ii) `NotificationMessage` 3-arm proof: `type _Assert_NotifKinds = Extract<NotificationMessage, { kind: "marketplace-info" }> extends never ? never : Extract<NotificationMessage, { kind: "plugin-info" }> extends never ? never : true; export const _l5: _Assert_NotifKinds = true;`.

    (iii) Cascade-arm backward-compat proof: a value satisfying `{ readonly marketplaces: readonly MarketplaceNotificationMessage[] }` (NO `kind`) MUST still type-check as a `NotificationMessage`. Use `type _Assert_CascadeNoKind = { readonly marketplaces: readonly MarketplaceNotificationMessage[] } extends NotificationMessage ? true : never; export const _l6: _Assert_CascadeNoKind = true;`. This locks Migration Strategy #2.

    (iv) `MarketplaceInfoMessage` shape proof: build an `_MarketplaceInfoExpected` type literal mirroring the shape from step 3 (using `_MarketplaceDetailsExpected` pattern from the existing file) and assert it `extends` the exported type and vice-versa. Cover BOTH source-kind variants (github with optional ref + path).

    (v) `PluginInfoMessage` shape proof: same approach as (iv); build `_PluginInfoExpected` mirroring the shape from step 4. Cover both `componentsResolved: true` and `componentsResolved: false` arms (use `Extract<PluginInfoRow, { componentsResolved: true }>` / `Extract<PluginInfoRow, { componentsResolved: false }>`); assert the resolved arm has the four optional component-kind arrays and the optional dependencies array, and assert the unresolved arm has NO `components` property (use `keyof` exclusion proof).

    (vi) Drift guards (unchanged but explicit): re-state the existing PLUGIN_STATUSES/MARKETPLACE_STATUSES/DEPENDENCIES length-lock assertions verbatim (do NOT modify or remove them). Add a comment noting Phase 42 verified they remain 11 / 7 / 2 respectively.

    DO NOT COMMIT at the end of Task 1. The typecheck WILL be RED until Task 2 lands the dispatcher rewrite + renderer arms. The single atomic commit happens at the end of Task 2 (SC#5).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "shared/notify\\.ts|notify-types\\.test\\.ts" | grep -vE "renderMarketplaceInfo|renderPluginInfo|wrapDescription|message\\.kind"</automated>
  </verify>
  <acceptance_criteria>
    - File `extensions/pi-claude-marketplace/shared/notify.ts` contains a `REASONS` tuple with the literal string `"not added"` appended after the previous last entry, and the inline comment around the tuple cites "Phase 42 / INFO-04 / INFO-08".
    - File `extensions/pi-claude-marketplace/shared/notify.ts` exports `CascadeNotificationMessage` with `readonly kind?: "cascade"` and `readonly marketplaces: readonly MarketplaceNotificationMessage[]`.
    - File `extensions/pi-claude-marketplace/shared/notify.ts` exports `MarketplaceInfoMessage` with `readonly kind: "marketplace-info"` and the six fields specified in action step 3.
    - File `extensions/pi-claude-marketplace/shared/notify.ts` exports `PluginInfoMessage` with `readonly kind: "plugin-info"` and the four fields specified in action step 4, plus `PluginInfoRow` discriminated on `componentsResolved: true | false`.
    - File `extensions/pi-claude-marketplace/shared/notify.ts` declares `export type NotificationMessage = CascadeNotificationMessage | MarketplaceInfoMessage | PluginInfoMessage`.
    - Command `grep -c '"not added"' extensions/pi-claude-marketplace/shared/notify.ts` returns at least 1.
    - File `tests/architecture/notify-types.test.ts` contains a type assertion of the form `(typeof REASONS)["length"] extends 29 ? true : never` AND a type assertion of the form `"not added" extends (typeof REASONS)[number] ? true : never`.
    - File `tests/architecture/notify-types.test.ts` contains all six type-level proofs (i)-(vi) from the action section above; existing PLUGIN_STATUSES/MARKETPLACE_STATUSES/DEPENDENCIES length locks remain at 11 / 7 / 2 byte-identical.
    - Pre-flight grep audit (action step a) produced zero hits for an existing REASONS length-lock; pre-flight grep audit (action step b) produced zero hits for an existing `notify(...)` call with an inline `kind:` field.
    - NO commit is created at the end of Task 1. Working tree has staged or unstaged changes only.
  </acceptance_criteria>
  <done>
    Type model + closed-set + compile-time proofs are in place; typecheck failures (if any) are limited to references to `renderMarketplaceInfo`, `renderPluginInfo`, `wrapDescription`, and `message.kind` switching -- all of which Task 2 will introduce. No commit has been created.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add renderer arms + helper + dispatcher + catalog state + UAT fixture + per-status tests; land ONE atomic-supersession commit</name>
  <files>
    extensions/pi-claude-marketplace/shared/notify.ts,
    tests/shared/notify-v2.test.ts,
    tests/architecture/catalog-uat.test.ts,
    docs/output-catalog.md
  </files>
  <read_first>
    extensions/pi-claude-marketplace/shared/notify.ts,
    tests/shared/notify-v2.test.ts,
    tests/architecture/catalog-uat.test.ts,
    docs/output-catalog.md,
    docs/messaging-style-guide.md,
    .planning/phases/42-type-model-render-seam-foundations/42-RESEARCH.md
  </read_first>
  <behavior>
    - Test (wrapDescription empty): `wrapDescription("", 4, 66)` returns `[]`.
    - Test (wrapDescription short): `wrapDescription("Hello world.", 4, 66)` returns `["    Hello world."]` (4-space indent, single line).
    - Test (wrapDescription exact-fit): A 66-char text on word boundary returns `["    " + text]` (single line).
    - Test (wrapDescription long): A text whose first-line fit is 60 chars and next 30 chars wrap to a second line returns two strings, both prefixed by `    `.
    - Test (wrapDescription over-length single word): `wrapDescription("supercalifragilisticexpialidociousandevenlongerwithanotherwordtoexceed", 4, 66)` returns `["    supercalifragilisticexpialidociousandevenlongerwithanotherwordtoexceed"]` -- the over-length word is emitted on its own line at indent (NO truncation, NO ellipsis per INFO-02).
    - Test (wrapDescription whitespace normalization): `wrapDescription("  hello\t\tworld\n\nfoo  ", 4, 66)` returns `["    hello world foo"]` (single line; multiple whitespace collapses to single space).
    - Test ({not added} byte-form): A `PluginInfoMessage` payload constructed with `kind: "plugin-info"`, plugin `{ status: "failed", name: "my-mp", scope: "user", reasons: ["not added"], componentsResolved: false }` invoked through `notify(ctx, pi, msg)` produces the body string starting with `⊘ my-mp [user] (failed) {not added}` at column 0. Severity passed to `ctx.ui.notify` MUST be `"error"`.
    - Test (renderMarketplaceInfo github with ref): A `MarketplaceInfoMessage` for a github source with `ref: "main"` and a description renders the per-block byte form `● <name> [<scope>] <autoupdate-marker>\ngithub: owner/repo#main\nlast_updated: <iso>\ndescription: ...`.
    - Test (renderMarketplaceInfo path no description): A `MarketplaceInfoMessage` for a path source omits the `last_updated` and `description` lines and renders `path: /abs/path`.
    - Test (renderPluginInfo componentsResolved=true): A `PluginInfoMessage` with `status: "installed"`, version `"1.0.0"`, description, `componentsResolved: true`, sorted components, and dependencies renders the marketplace-header form followed by the indented plugin row, then the wrapped description block, then the alphabetized-by-kind component lists, then the dependencies line.
    - Test (renderPluginInfo componentsResolved=false): A `PluginInfoMessage` with `componentsResolved: false` renders the marker line `components: not resolved` instead of any component lists.
    - Test (cascade backward-compat): An existing-shape payload `{ marketplaces: [...] }` (NO `kind`) still produces byte-identical output to v1.7 baseline for at least one representative cascade (smoke-check; the full 60+ fixture catalog UAT is the binding gate).
    - Test (catalog UAT byte-equality): The new `scope-mismatch-not-added` fenced block in `docs/output-catalog.md` pairs with the FIXTURES entry and byte-equals the rendered output.
  </behavior>
  <action>
    RENDERER + DISPATCHER CHANGES in `extensions/pi-claude-marketplace/shared/notify.ts`:

    1. Add a file-private helper `wrapDescription(text: string, indentCol: number, wrapCol: number): string[]` per RESEARCH Pattern 2. The algorithm: split on `/\\s+/`, filter empty tokens, greedy-accumulate words into lines whose TEXT length (not counting indent) does not exceed `wrapCol`; prepend `" ".repeat(indentCol)` to each emitted line; return `string[]` (caller `.join("\\n")`s). Empty / whitespace-only input returns `[]`. A single token longer than `wrapCol` is emitted on its own line at indent (NO truncation, NO ellipsis). Add JSDoc citing INFO-02 and noting that `wrapCol` is the TEXT width per RESEARCH Pitfall 4 (matching the existing `truncateDescription` / `DESCRIPTION_MAX_COLS = 66` convention). File-private -- do NOT export (RESEARCH Anti-Pattern: Adding wrapDescription to public exports).

    2. Add a file-private function `renderMarketplaceInfo(message: MarketplaceInfoMessage, probe: SoftDepStatus): string`. Behavior:
       - Compose the marketplace header `● <name> [<scope>] <autoupdate-marker>` by reusing the EXISTING header composition primitives (`ICON_INSTALLED`, `renderMpHeader`-shared autoupdate-marker helper). Do NOT modify `renderMpHeader` (RESEARCH Pitfall 1).
       - Append a source-kind line: `github: <owner>/<repo>` (with `#<ref>` suffix only when `source.ref` is defined per INFO-01) OR `path: <absPath>`. Switch on `message.source.sourceKind` with `assertNever` default.
       - When `message.lastUpdated` is defined AND `source.sourceKind === "github"`, append `last_updated: <iso>` on its own line.
       - When `message.description` is defined, append `description: <text>` on its own line (NO wrapping -- `description:` is a single attribute line on the marketplace info surface per INFO-01; only `plugin info` description bodies get `wrapDescription` per INFO-02). NOTE if the catalog-state byte form Phase 43 ships disagrees, the renderer will be tightened then; Phase 42 ships the inline single-line shape because INFO-04's only catalog state (`scope-mismatch-not-added`) does not exercise description rendering and INFO-01 lives in Phase 43.
       - `probe` parameter is unused on info surfaces (info messages do not emit soft-dep markers per RESEARCH Anti-Patterns); accept it for signature parity with `composeMarketplaceBlock` and `_ = probe` to silence unused warnings if necessary.

    3. Add a file-private function `renderPluginInfo(message: PluginInfoMessage, probe: SoftDepStatus): string`. Behavior:
       - Always-marketplace-header form: render the marketplace header at column 0 (same shape as `renderMarketplaceInfo` header) using `message.marketplaceName`, `message.marketplaceScope`, `message.marketplaceDetails`.
       - Append the plugin row at 2-space indent. Compose via existing primitives where possible: status glyph (`ICON_INSTALLED` for `installed`, `ICON_AVAILABLE` for `available`, `ICON_UNINSTALLABLE` for `unavailable` and `failed`), name, optional `[scope]` bracket via `renderScopeBracket(plugin.scope, message.marketplaceScope)`, optional `vX.Y.Z` via `renderVersion`, status token (`(installed)` / `(available)` / `(unavailable)` / `(failed)`), and `composeReasons(plugin.reasons, false, false, probe)` (pass `false, false` for the two soft-dep declares flags per RESEARCH Anti-Pattern: Hand-rolling composeReasons for info messages).
       - When `plugin.description` is defined, append `wrapDescription(plugin.description, 4, 66).join("\\n")` after the plugin row (4-space indent, 66-col TEXT width per RESEARCH Pitfall 4).
       - Switch on `plugin.componentsResolved`:
         - `true` branch: For each of the four component kinds (`agents`, `commands`, `mcp`, `skills`) in alphabetical order, when the array is defined and non-empty emit a 4-space-indented line `    <kind>: <comma-separated names>` (the orchestrator pre-sorts per Pitfall 5; renderer does NOT sort). When `dependencies` is defined and non-empty, append `    dependencies: <comma-separated entries>` as the LAST line.
         - `false` branch: Emit the single line `    components: not resolved` per INFO-05.
         - Default: `assertNever(plugin)` (the PluginInfoRow discriminator is exhaustive -- the default arm is unreachable but locks NFR-7 per RESEARCH Pitfall 3).

    4. Rewrite the public `notify(ctx, pi, message)` body at line 1472 to dispatch on `message.kind`:
       - `switch (message.kind ?? "cascade")` -- default-narrows the cascade arm so v1.0-v1.7 call sites do not require migration.
       - `case "cascade":` -- execute the EXISTING cascade body verbatim (probe → blocks → body → hint → severity → `ctx.ui.notify(...)`). Move the existing implementation INTO this arm; do NOT extract it into a helper (minimizes diff churn).
       - `case "marketplace-info":` -- call `renderMarketplaceInfo(message, softDepStatus(pi))`; severity is `computeSeverity(message)` (extend `computeSeverity` per step 5); `ctx.ui.notify(body, severity)` if severity defined, else `ctx.ui.notify(body)`. NO reload-hint trailer for info messages (read-only surface; RESEARCH Don't Hand-Roll: Reload-hint trailer). NO summary line for info messages (RESEARCH Don't Hand-Roll: Summary line on info messages / A6).
       - `case "plugin-info":` -- call `renderPluginInfo(message, softDepStatus(pi))`; severity logic same as `marketplace-info` arm.
       - `default:` -- `assertNever(message); return;` (RESEARCH Pitfall 3).

    5. Extend `computeSeverity(message: NotificationMessage)` at line 1157 to handle the new kinds:
       - First-match ladder (preserve existing arm ordering per Phase 28 / D-28-06): when `message.kind === "marketplace-info"`, return `undefined` (no failure can be expressed on a marketplace-info message itself; `{not added}` failures use `plugin-info` per INFO-04's byte form `⊘ <name> [<scope>] (failed) {not added}`).
       - When `message.kind === "plugin-info"`, return `"error"` if `message.plugin.status === "failed"`, else `undefined`.
       - Cascade arm executes the EXISTING body. Place the new arms at the TOP of the function so the cascade arm only sees cascade payloads (or use a `switch (message.kind ?? "cascade")` for symmetry with `notify()`).

    6. Extend `shouldEmitReloadHint(message)` at line 1319 to short-circuit `false` on info-message kinds (info messages NEVER trigger reload per RESEARCH Don't Hand-Roll: Reload-hint trailer). Add an explicit arm `if (message.kind === "marketplace-info" || message.kind === "plugin-info") return false;` at the top.

    7. Extend `buildSummaryLine(message, severity)` at line 1274 to handle info-message kinds. Recommendation per RESEARCH A6: the dispatcher already short-circuits the summary-line invocation for info messages (only the cascade arm in `notify()` invokes `buildSummaryLine`). Add a defensive `if (message.kind === "marketplace-info" || message.kind === "plugin-info") return "";` arm at the top of `buildSummaryLine` so a future mistaken call still returns benign output.

    CATALOG STATE in `docs/output-catalog.md`:

    8. Add a new H2 section EXACTLY titled (byte-for-byte to satisfy `sectionRe` at `tests/architecture/catalog-uat.test.ts:82`): ``## `/claude:plugin marketplace info <name>` ``. Choose the section location alphabetically near the existing marketplace H2 sections.

    9. Inside that section, add ONE fenced byte block preceded by the annotation `<!-- catalog-state: scope-mismatch-not-added -->` on its own line. The fenced block contains the EXACT byte form (newline-separated, no trailing newline inside the fence):
       ```
       Error: 1 plugin operation failed

       ⊘ my-mp [user] (failed) {not added}
       ```
       NOTE: the `Error:` summary line is emitted by `buildSummaryLine` when severity is `"error"` AND the message is a cascade -- but per step 7 we short-circuit summary for info kinds. The `scope-mismatch-not-added` byte form is the bare `⊘ my-mp [user] (failed) {not added}` row (no summary line). UPDATE the fenced block contents to be:
       ```
       ⊘ my-mp [user] (failed) {not added}
       ```
       (single line; no summary line; severity `"error"` is delivered to the host as the second argument to `ctx.ui.notify` and produces the host's own `Error:` prefix, which is NOT part of the catalog byte form per the existing catalog convention.) Verify against the existing catalog conventions (read the first ~100 lines of `docs/output-catalog.md`) before finalizing.

    10. Add a narrative paragraph above the fenced block explaining the trigger: "Surfaced when `marketplace info <name> --scope <wrong-scope>` is invoked against a marketplace present only in the OTHER scope (e.g., requesting `--scope user` when `my-mp` lives only in `project`). The new `{not added}` REASON (Phase 42 / INFO-04) distinguishes this from a truly-absent marketplace name."

    CATALOG-UAT FIXTURE in `tests/architecture/catalog-uat.test.ts`:

    11. Extend the `FIXTURES` map at line 228 with a new top-level entry whose key MUST equal the H2 section captured by `sectionRe` byte-for-byte: `"/claude:plugin marketplace info <name>"`. Under that entry, add a nested fixture keyed `"scope-mismatch-not-added"` whose value is the `NotificationMessage` payload that, when run through `notify(ctx, pi, ...)`, produces the byte form fenced in step 9. Recommended payload shape:
       ```ts
       {
         kind: "plugin-info",
         marketplaceName: "my-mp",
         marketplaceScope: "user",
         marketplaceDetails: { /* minimal MarketplaceDetails per existing fixtures */ },
         plugin: {
           status: "failed",
           name: "my-mp",
           scope: "user",
           reasons: ["not added"],
           componentsResolved: false,
         },
       } satisfies NotificationMessage
       ```
       Reuse the existing `makeCtx()` / `piWithBothLoaded()` helpers -- do NOT introduce new test scaffolding.

    PER-STATUS UNIT TESTS in `tests/shared/notify-v2.test.ts`:

    12. Add at LEAST 6 per-status tests for `wrapDescription` covering the edge cases in the `<behavior>` section: empty, short, exact-fit, long, over-length single word, whitespace normalization. Use the existing `test(...)` + `assert.deepEqual(...)` pattern from the file. Import `wrapDescription` via a controlled test seam: since the helper is file-private (NOT exported), add the tests by importing the public `notify` and asserting end-to-end via a `PluginInfoMessage` with a description that exercises each wrap case. (DO NOT export `wrapDescription` per Anti-Pattern; lock behavior through end-to-end byte tests.) If the end-to-end approach makes wrap-edge-case isolation impractical, the planner may export `wrapDescription` ONLY behind a `/* @internal */` JSDoc-tagged export and re-confirm in code review -- but the default is end-to-end testing through `notify()`.

    13. Add a per-status byte test for the `{not added}` row: construct the payload from step 11, invoke `notify(ctx, pi, msg)`, assert `ctx.ui.notify.mock.calls[0].arguments[0]` equals `⊘ my-mp [user] (failed) {not added}` AND `arguments[1]` equals `"error"`.

    14. Add at LEAST 2 per-status byte tests for `renderMarketplaceInfo`: one github source with `ref` + `lastUpdated` + `description`, one path source without `lastUpdated` and without `description`. Lock the full body bytes including the marketplace header line.

    15. Add at LEAST 2 per-status byte tests for `renderPluginInfo`: one `componentsResolved: true` with sorted components + dependencies + a description that wraps, one `componentsResolved: false` showing the `components: not resolved` marker line. Lock the full body bytes.

    16. Add ONE cascade backward-compat smoke test: a payload `{ marketplaces: [{ name: "x", scope: "user", plugins: [] }] }` (NO `kind` field) invoked through `notify()` produces byte-identical output to a payload `{ kind: "cascade", marketplaces: [...] }` with the same marketplaces array. Lock that the optional `kind?` strategy is honored.

    FINAL ATOMIC-SUPERSESSION COMMIT (per SC#5 and CLAUDE.md):

    17. Run `npm run check`. Verify it exits 0 (all 1156+ tests GREEN, catalog UAT byte-equality GREEN for the new fixture AND for ALL existing 60+ cascade fixtures, typecheck GREEN, ESLint GREEN, Prettier GREEN). If RED, iterate inside the worktree until GREEN -- do NOT commit a RED state. Specifically watch for byte-form drift on any pre-existing cascade fixture (Pitfall 1 warning sign).

    18. Run `pre-commit run --files` against the changed files. Iterate until clean per CLAUDE.md ("a failed pre-commit hook means the commit did NOT happen").

    19. Stage exactly these files (DO NOT use `git add -A`):
       - `extensions/pi-claude-marketplace/shared/notify.ts`
       - `tests/architecture/notify-types.test.ts`
       - `tests/shared/notify-v2.test.ts`
       - `tests/architecture/catalog-uat.test.ts`
       - `docs/output-catalog.md`

    20. Create ONE commit. If working in a worktree, prefix with `SKIP=trufflehog` per CLAUDE.md. Commit title (≤72 chars, Conventional Commits): `feat(42-01): add info-message variants + "not added" REASON (atomic)`. Body (≤80 cols/line): summarize the five SC items, cite INFO-04 + INFO-08, note "Atomic-supersession commit per v1.3 retrospective lesson and Phase 42 SC#5", reference RESEARCH file. Do NOT use `--no-verify`. Do NOT use `--amend`.

    21. After the commit, run `git status` to verify the working tree is clean and `git log -1 --stat` to confirm the file footprint. If trufflehog was skipped via `SKIP=trufflehog`, run `pre-commit run trufflehog --all-files` separately and confirm GREEN.

    22. The plan ships ONE commit containing the contract foundation. Phases 43 and 44 build on top -- no other changes are permitted in this commit (SC#5: "nothing else").
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 after the commit (the bash verify command runs this; non-zero exit fails the gate).
    - File `extensions/pi-claude-marketplace/shared/notify.ts` defines a file-private function whose name starts with `function wrapDescription(` and signature `(text: string, indentCol: number, wrapCol: number): string[]`. The function is NOT exported (no `export` keyword on its declaration).
    - File `extensions/pi-claude-marketplace/shared/notify.ts` defines file-private functions `renderMarketplaceInfo` and `renderPluginInfo`; neither is exported.
    - The exported `notify` function body contains a `switch` whose discriminant is `message.kind ?? "cascade"` (or equivalent narrowing) with arms `"cascade"`, `"marketplace-info"`, `"plugin-info"`, and a `default` that calls `assertNever`.
    - File `extensions/pi-claude-marketplace/shared/notify.ts` `computeSeverity` and `shouldEmitReloadHint` both handle `message.kind === "marketplace-info"` and `message.kind === "plugin-info"` arms; `shouldEmitReloadHint` returns `false` for both info kinds; `computeSeverity` returns `"error"` for `plugin-info` with `plugin.status === "failed"` and `undefined` otherwise for info kinds.
    - File `docs/output-catalog.md` contains a new H2 line matching the regex `^## \`/claude:plugin marketplace info <name>\`\s*$` followed by an HTML comment `<!-- catalog-state: scope-mismatch-not-added -->` and a fenced byte block whose only content is the single line `⊘ my-mp [user] (failed) {not added}`.
    - File `tests/architecture/catalog-uat.test.ts` `FIXTURES` map contains an outer key whose string value equals `"/claude:plugin marketplace info <name>"` byte-for-byte AND an inner key `"scope-mismatch-not-added"`.
    - File `tests/shared/notify-v2.test.ts` contains at least 6 tests whose names mention `wrapDescription`, at least 1 test asserting the byte form `⊘ my-mp [user] (failed) {not added}` with severity `"error"`, at least 2 tests for `renderMarketplaceInfo` (github + path source kinds), at least 2 tests for `renderPluginInfo` (`componentsResolved: true` + `componentsResolved: false`), and at least 1 cascade backward-compat smoke test verifying that a payload WITHOUT a `kind` field still routes through the cascade arm.
    - Command `git log -1 --pretty=%s` returns a title starting with `feat(42-01):` and length ≤ 72.
    - Command `git log -1 --name-only --pretty=` lists EXACTLY these five files: `extensions/pi-claude-marketplace/shared/notify.ts`, `tests/architecture/notify-types.test.ts`, `tests/shared/notify-v2.test.ts`, `tests/architecture/catalog-uat.test.ts`, `docs/output-catalog.md`. No other files in the commit (SC#5 "nothing else"). If `docs/messaging-style-guide.md` or `docs/adr/v2-001-structured-notify.md` were optionally amended per RESEARCH Open Questions, they MAY be included -- but no orchestrator, edge handler, or non-info catalog file is modified.
    - Command `git status` reports a clean working tree after the commit.
    - Pre-commit hooks ran GREEN (CLAUDE.md mandate); no `--no-verify` was used; if trufflehog was skipped via `SKIP=trufflehog` in a worktree, a separate `pre-commit run trufflehog --all-files` confirmed GREEN.
    - `grep -c "not added" docs/output-catalog.md` returns at least 2 (the catalog-state annotation + the rendered REASON inside the fence).
    - All 60+ pre-existing cascade catalog UAT fixtures remain byte-equal (verified by `npm run check`; a regression here triggers Pitfall 1 and the commit must be unwound before retry).
  </acceptance_criteria>
  <done>
    The atomic-supersession commit lands on the active feature branch. `npm run check` exits 0. The REASONS tuple has the new `"not added"` member. The new info-message variants are reachable from `NotificationMessage` via the `kind` discriminator. `wrapDescription` works per all 6 edge cases. The first catalog state and UAT fixture pair byte-equally. Zero behavior change for any v1.0-v1.7 cascade call site. Phase 42 contract is shipped -- Phases 43 and 44 can now consume it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → renderer | INFO command argument values (`<name>`, `<plugin>@<marketplace>`, `--scope`) eventually flow into `MarketplaceInfoMessage.name` / `PluginInfoMessage.marketplaceName` / `plugin.name` / `plugin.scope`. Phase 42 ships the type contract; Phases 43/44 own the input-validation seam. |
| Plugin manifest → message | Description / components / dependencies pulled from `plugin.json` or `marketplace.json` files on disk could carry control characters or unusual whitespace. Phase 42's `wrapDescription` tokenizer (`/\s+/`) collapses newlines/tabs into single spaces, providing basic display normalization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering (display confusion) | `wrapDescription` text input | mitigate | `/\s+/` tokenization collapses CR/LF/tab/multiple-space into single spaces, neutralizing basic injection of newlines that would otherwise break the indent ladder. Deeper escaping (ANSI control sequences, zero-width chars) is Phase 43/44's orchestrator responsibility per the contract boundary. |
| T-42-02 | Information disclosure | `componentsResolved: false` discriminator on external-source plugins | accept | The discriminator carries NO data -- it is a structural marker that signals "do not attempt a network fetch" (preserves NFR-5). The marker is precisely the mitigation against a higher-severity threat (unintentional network access during read-only info). |
| T-42-03 | Tampering (closed-set drift) | `REASONS` tuple extension | mitigate | Atomic-supersession commit lands the new `"not added"` REASON in lockstep with the length-lock test (line lock at 29) and the closed-set membership proof. Any future drift (rename, removal) triggers a compile-time failure in `notify-types.test.ts`. CMC-11 closed-set discipline preserved. |
| T-42-04 | Tampering (intermediate-state catalog drift) | Multi-commit split of the type + REASON + catalog state | mitigate | Plan structure forbids intermediate commits: Task 1 stages changes without committing; Task 2 stages additional changes and creates the SINGLE atomic-supersession commit. Pre-commit hook + `npm run check` enforce GREEN at the commit boundary (CLAUDE.md mandate + SC#5). |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | Phase 42 installs zero packages (RESEARCH Package Legitimacy Audit: N/A). No supply-chain surface introduced. |
</threat_model>

<verification>
Phase-level verification (after the single atomic commit):

1. `npm run check` exits 0 (typecheck + ESLint + Prettier + ALL tests including catalog-uat byte-equality).
2. `git log -1 --name-only --pretty=` lists exactly the 5 expected files (or 5-7 if optional ADR / style-guide amendments per RESEARCH Open Questions are included); no orchestrator, edge handler, or unrelated catalog section modified.
3. `git grep -nE "\"not added\"" extensions/pi-claude-marketplace/shared/notify.ts` returns at least 1 hit in the REASONS tuple.
4. `git grep -nE "kind: \"marketplace-info\"|kind: \"plugin-info\"" extensions/pi-claude-marketplace/shared/notify.ts tests/` returns hits in BOTH the source file (variant definitions + dispatcher arms) AND test files (fixture construction + per-status tests).
5. The pre-existing 60+ catalog-uat cascade fixtures all remain GREEN (proven transitively by `npm run check` exit 0; if any regress, Pitfall 1 was hit and the commit must be unwound).
6. Pre-commit hook chain ran GREEN at the commit boundary (CLAUDE.md mandate). Trufflehog confirmed clean (either inline or via separate `pre-commit run trufflehog --all-files` if SKIP-prefixed in a worktree).
</verification>

<success_criteria>
Phase 42 is complete when ALL of the following are true:

1. **SC#1 (variants reachable):** `NotificationMessage` is a 3-arm discriminated union (`CascadeNotificationMessage | MarketplaceInfoMessage | PluginInfoMessage`) reachable from the exported type name; the `componentsResolved` discriminator on `PluginInfoMessage.plugin` is exhaustive via `assertNever` per NFR-7.
2. **SC#2 (REASON + atomic supersession):** `REASONS` tuple contains the literal `"not added"`; the length-lock + closed-set proof in `notify-types.test.ts` are updated to the new count (29) and the new member; rendering a row with `reason: "not added"` produces `⊘ <name> [<scope>] (failed) {not added}` at column 0 with severity `error`; the change lands in ONE commit alongside the catalog state in `docs/output-catalog.md` AND the matching `catalog-uat.test.ts` fixture.
3. **SC#3 (wrapDescription):** A new file-private `wrapDescription(text, indentCol, wrapCol): string[]` hard-wraps at the requested column count with no ellipsis, preserves word boundaries, and indents each line at `indentCol`; per-status unit tests lock the behavior at the 4-col-indent / 66-col-total used by `plugin info`.
4. **SC#4 (zero behavior change):** The renderer switch in `notify()` has new arms for both info variants gated by exhaustive `assertNever` defaults; the existing 10-arm plugin status + 7-arm marketplace status surfaces are byte-unchanged for all non-info call sites (zero regression on the 60+ pre-existing cascade catalog UAT fixtures).
5. **SC#5 (single atomic commit):** `npm run check` exits 0; catalog UAT byte-equality remains GREEN; the atomic-supersession commit contains exactly the REASON tuple addition, the new types, the new helper, the new fixture(s) for `--scope` mismatch, and the catalog state(s) -- nothing else (optional ADR / style-guide amendments per RESEARCH Open Questions are permitted but no orchestrator, edge handler, or non-info catalog file).

INFO-04 and INFO-08 marked as Phase 42 outcomes in REQUIREMENTS.md traceability after `/gsd-verify-work` confirms the SC items above.
</success_criteria>

<output>
Create `.planning/phases/42-type-model-render-seam-foundations/42-01-SUMMARY.md` when done. Summary MUST cite the atomic commit SHA, list the 5 (or 5-7 if optional docs amended) files in the commit footprint, and confirm `npm run check` GREEN at the commit boundary.
</output>
