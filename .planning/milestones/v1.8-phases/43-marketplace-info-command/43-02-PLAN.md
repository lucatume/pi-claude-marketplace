---
phase: 43-marketplace-info-command
plan: 02
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
autonomous: true
requirements:
  - INFO-07
tags:
  - catalog
  - uat
  - byte-equality
  - info-surface
must_haves:
  truths:
    - "`docs/output-catalog.md`'s `## /claude:plugin marketplace info <name>` H2 section enumerates every reachable state for the marketplace info command surface: github single-scope (with autoupdate + lastUpdatedAt + description), github single-scope no-ref (no description, no lastUpdatedAt), path single-scope (no lastUpdatedAt), `<no autoupdate>` marker (single scope), both-scopes fan-out (project-first), absent-from-both `{not added}` (no scope bracket), and the Phase-42-landed `--scope` mismatch `{not added}` (with scope bracket)"
    - "Every catalog state under that H2 is paired with a `<!-- catalog-state: STATE -->` annotation matching the FIXTURES inner-map key byte-for-byte (per `sectionRe` / `stateRe` in `tests/architecture/catalog-uat.test.ts:82-83`)"
    - "Every annotated catalog state has a corresponding FIXTURE entry in `tests/architecture/catalog-uat.test.ts`'s `FIXTURES[\"/claude:plugin marketplace info <name>\"]` inner map; the FIXTURE's `message: NotificationMessage` payload, when run through `notify(mockCtx, mockPi, message)`, byte-equals the fenced expected output"
    - "The catalog UAT byte-equality test (`catalog UAT: every <!-- catalog-state: --> annotation pairs byte-equal with notify()`) is GREEN against the expanded fixture set"
    - "The pre-existing Phase 42 `scope-mismatch-not-added` fixture is preserved byte-identical -- the Plan 43-02 additions are PURELY additive within the same H2 section; no Phase 42 fixture is renamed, reordered, or modified"
    - "Severity routing on every new fixture is correct: success states omit `expectedSeverity` (info -- no second arg to `ctx.ui.notify`); `{not added}` rows set `expectedSeverity: \"error\"` and assert the magic-string severity arg"
    - "`npm run check` exits 0; the assertion that `examples.length >= 30` at `catalog-uat.test.ts:1518` still holds (with Phase 43's additions the count grows further -- enforces the floor, never the ceiling)"
  artifacts:
    - path: "docs/output-catalog.md"
      provides: "Expanded `## /claude:plugin marketplace info <name>` H2 section with one fenced catalog-state block per reachable info-surface state plus narrative prose explaining the trigger / severity / reload-hint behavior for each. Existing Phase 42 `scope-mismatch-not-added` block preserved verbatim and reordered ONLY if necessary to keep the section's prose flow coherent (no byte change to its content)."
      contains: "github-single-scope-full"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "Expanded `FIXTURES[\"/claude:plugin marketplace info <name>\"]` inner map with one fixture per new catalog state. Pre-existing Phase 42 `scope-mismatch-not-added` fixture preserved byte-identical. Pi-mock selection uses `piWithBothLoaded()` (info surface emits NO soft-dep markers). Severity arg locked per state."
      contains: "github-single-scope-full"
  key_links:
    - from: "docs/output-catalog.md"
      to: "tests/architecture/catalog-uat.test.ts"
      via: "`loadCatalogExamples` + `sectionRe` + `stateRe` parser: H2 section name `/claude:plugin marketplace info <name>` MUST equal the FIXTURES outer-map key byte-for-byte (already set by Phase 42); every new `<!-- catalog-state: STATE -->` annotation MUST equal a FIXTURES inner-map key byte-for-byte"
      pattern: "/claude:plugin marketplace info <name>"
    - from: "tests/architecture/catalog-uat.test.ts FIXTURES"
      to: "extensions/pi-claude-marketplace/shared/notify.ts"
      via: "Each FIXTURE constructs a `NotificationMessage` (cascade | marketplace-info | marketplace-info-cascade | plugin-info) and routes it through the public `notify()` entry; byte equality vs. the catalog fenced block is the binding gate"
      pattern: "notify\\(ctx as never, fixture\\.pi as never, fixture\\.message\\)"
---

<objective>
Close INFO-07 by enumerating every byte form the `marketplace info` command surface can emit in `docs/output-catalog.md` and pairing each with a programmatic FIXTURE in `tests/architecture/catalog-uat.test.ts`'s byte-equality runner. Depends on Plan 43-01 having landed the orchestrator / handler / completion / `MarketplaceInfoCascadeMessage` type so the fixtures drive against a working production surface.

Purpose: The catalog UAT is the v1.4 user-contract gate (SNM-31). Until INFO-07's fixtures land, the `marketplace info` byte form is only locked at the per-status `notify-v2` unit-test level -- the higher-fidelity catalog-vs-renderer byte-equality gate has no coverage for the new surface. Plan 43-02 adds that coverage in a single atomic-supersession-aware commit (catalog narrative + fenced blocks + FIXTURES map all move together per RESEARCH Pitfall 6 / catalog-uat coupling).

Output: An expanded H2 section in `docs/output-catalog.md` plus an expanded FIXTURES entry in `tests/architecture/catalog-uat.test.ts`, with `npm run check` GREEN.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-marketplace-info-command/43-CONTEXT.md
@.planning/phases/43-marketplace-info-command/43-01-PLAN.md
@.planning/phases/42-type-model-render-seam-foundations/42-RESEARCH.md
@./CLAUDE.md
@extensions/pi-claude-marketplace/shared/notify.ts
@docs/output-catalog.md
@docs/messaging-style-guide.md
@tests/architecture/catalog-uat.test.ts
@tests/shared/notify-v2.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enumerate `marketplace info` catalog states + paired FIXTURES; lockstep with byte-equality UAT</name>
  <files>
    docs/output-catalog.md,
    tests/architecture/catalog-uat.test.ts
  </files>
  <read_first>
    docs/output-catalog.md,
    tests/architecture/catalog-uat.test.ts,
    extensions/pi-claude-marketplace/shared/notify.ts,
    .planning/phases/43-marketplace-info-command/43-01-PLAN.md
  </read_first>
  <behavior>
    - Test (catalog UAT GREEN end-to-end): `npm test -- --test-name-pattern="catalog UAT"` exits 0. The new `<!-- catalog-state: ... -->` annotations under the `## /claude:plugin marketplace info <name>` H2 each pair byte-equal with their FIXTURE.
    - Test (catalog floor preserved): the `examples.length >= 30` assertion at `catalog-uat.test.ts:1518` still holds (Plan 43-02 grows it; the floor never fails).
    - Test (Phase 42 fixture preserved byte-identical): the `scope-mismatch-not-added` fixture continues to render the bare `⊘ my-mp [user] (failed) {not added}` row with severity `"error"`. Regress this if anything in Plan 43-02 modifies it; reject the change.
    - Test (severity routing correctness): success states (every github / path / fan-out fixture) emit ONLY one arg to `ctx.ui.notify` (no severity); `{not added}` states emit two args with the second being `"error"`. Enforced by the existing catalog-uat severity-mismatch detector (`tests/architecture/catalog-uat.test.ts:1578-1596`).
    - Test (loadCatalogExamples parses every new state): each new `<!-- catalog-state: STATE -->` annotation under the info-surface H2 produces an entry in the `examples` array; verifiable by counting `grep -cE '^&lt;!-- catalog-state:' docs/output-catalog.md` and asserting that the test driver successfully resolves every annotation to a fixture.
  </behavior>
  <action>
    PRE-FLIGHT: re-read `docs/output-catalog.md:819-835` (Phase 42's `## /claude:plugin marketplace info <name>` H2 section as landed). DO NOT modify the `scope-mismatch-not-added` block bytes -- it is the Phase 42 anchor. Plan 43-02 EXTENDS the section; the existing block stays.

    CATALOG STATE MATRIX -- enumerate these states under the existing H2. For each, add prose explaining the trigger / severity / reload-hint behavior, then a `<!-- catalog-state: STATE -->` annotation, then a fenced text block with the exact byte form. STATE names are lowercase + hyphen-separated to match the parser's `stateRe = /^&lt;!-- catalog-state: ([a-z0-9-]+) -->\s*$/`. Recommended STATE name + payload outline:

    1. **`github-single-scope-full`** -- github source, `--scope user`, autoupdate enabled, with `lastUpdatedAt` + `description`. Body:

       ```text
       ● claude-plugins-official [user] <autoupdate>
       github: anthropics/claude-plugins-official#main
       last_updated: 2026-06-03T00:00:00Z
       description: Official Claude plugin marketplace.
       ```

       FIXTURE payload (single `MarketplaceInfoMessage`):

       ```ts
       {
         kind: "marketplace-info",
         name: "claude-plugins-official",
         scope: "user",
         details: { autoupdate: true, lastUpdatedAt: "2026-06-03T00:00:00Z" },
         source: { sourceKind: "github", owner: "anthropics", repo: "claude-plugins-official", ref: "main" },
         description: "Official Claude plugin marketplace.",
       } satisfies NotificationMessage
       ```

       No `expectedSeverity` (info).

    2. **`github-single-scope-minimal`** -- github source, `--scope user`, autoupdate off, no `ref`, no `lastUpdatedAt`, no `description`. Body:

       ```text
       ● community-mp [user] <no autoupdate>
       github: someuser/community-mp
       ```

       FIXTURE: `MarketplaceInfoMessage` with `details: { autoupdate: false }`, `source: { sourceKind: "github", owner: "someuser", repo: "community-mp" }` (no `ref`), no `description`. No `expectedSeverity`. This block shows that omitting `ref` drops the `#&lt;ref&gt;` suffix from the `github:` line (INFO-01 contract), and omitting `lastUpdatedAt` drops the `last_updated:` line.

    3. **`path-single-scope`** -- path source, `--scope project`, autoupdate off, no description. Body:

       ```text
       ● local-mp [project] <no autoupdate>
       path: /home/user/marketplaces/local-mp
       ```

       FIXTURE: `MarketplaceInfoMessage` with `details: { autoupdate: false }`, `source: { sourceKind: "path", absPath: "/home/user/marketplaces/local-mp" }`. No `description`. No `expectedSeverity`. The block emphasizes: path sources NEVER emit `last_updated:` (INFO-01 gates that on the github arm).

    4. **`path-single-scope-with-description`** -- path source, `--scope user`, autoupdate enabled, with description. Body:

       ```text
       ● dev-mp [user] <autoupdate>
       path: /home/user/src/dev-mp
       description: Local development marketplace; experimental plugins.
       ```

       FIXTURE: `MarketplaceInfoMessage` with `details: { autoupdate: true }`, `source: { sourceKind: "path", absPath: "/home/user/src/dev-mp" }`, `description: "Local development marketplace; experimental plugins."`. No `expectedSeverity`. Shows that the `description:` line is INDEPENDENT of source kind (it appears on both github + path arms when `marketplace.json` carries one).

    5. **`both-scopes-fan-out`** -- INFO-03 fan-out, no `--scope`, marketplace present in BOTH scopes. Project block FIRST, user block SECOND, separated by ONE blank line. Body:

       ```text
       ● my-mp [project] <autoupdate>
       path: /repo/path/my-mp

       ● my-mp [user] <no autoupdate>
       github: someuser/my-mp
       ```

       FIXTURE: `MarketplaceInfoCascadeMessage` with `blocks: [projectBlock, userBlock]`:

       ```ts
       {
         kind: "marketplace-info-cascade",
         blocks: [
           {
             kind: "marketplace-info",
             name: "my-mp",
             scope: "project",
             details: { autoupdate: true },
             source: { sourceKind: "path", absPath: "/repo/path/my-mp" },
           },
           {
             kind: "marketplace-info",
             name: "my-mp",
             scope: "user",
             details: { autoupdate: false },
             source: { sourceKind: "github", owner: "someuser", repo: "my-mp" },
           },
         ],
       } satisfies NotificationMessage
       ```

       No `expectedSeverity` (info). Narrative MUST cite MSG-GR-3 / INFO-03 / Phase 18's compareByNameThenScope project-first tie-break.

    6. **`absent-from-both`** -- marketplace name not present in EITHER scope, no `--scope` filter. Body:

       ```text
       ⊘ ghost-mp (failed) {not added}
       ```

       FIXTURE: `PluginInfoMessage` with `status: "failed"`, `name: "ghost-mp"`, `reasons: ["not added"]`, `componentsResolved: false`, NO `plugin.scope` field (so the renderer's bracket short-circuit emits no `[scope]` token). `marketplaceScope: "user"` and `marketplaceDetails: { autoupdate: false }` are non-rendered placeholders (the Phase 42 INFO-04 carve-out skips the header for `{not added}` rows). `expectedSeverity: "error"`. Narrative MUST distinguish this from `scope-mismatch-not-added`: the absent-from-both state has NO scope bracket because the marketplace is in neither scope; the scope-mismatch state DOES have a bracket because the user asked for a specific scope.

    7. **`scope-mismatch-not-added`** -- THE PHASE 42 EXISTING BLOCK. Do NOT modify. Verify byte-for-byte preservation. If the existing block lives at the END of the section, leave it there; if a more natural narrative ordering puts the failure states at the END, the existing block can be repositioned WITHIN the section but its bytes (annotation + fenced block) MUST NOT change.

    NARRATIVE INTRO -- expand the existing intro prose (currently `docs/output-catalog.md:823`) to enumerate the new states. Keep the existing "Read-only detail surface (Phases 42-43)" framing; remove the "lands in Phase 43" deferral language (Phase 43 IS landing it). Recommended structure:

    - Brief intro (carry forward from Phase 42, refresh language).
    - Sub-section: "Success states" -- with the four success fixtures (`github-single-scope-full`, `github-single-scope-minimal`, `path-single-scope`, `path-single-scope-with-description`).
    - Sub-section: "Multi-scope fan-out" -- the `both-scopes-fan-out` fixture with prose citing MSG-GR-3 / INFO-03 project-first ordering.
    - Sub-section: "Failure states" -- the two `{not added}` fixtures (`absent-from-both`, `scope-mismatch-not-added`) with prose distinguishing them.

    For each fixture: ONE narrative paragraph followed by the `<!-- catalog-state: -->` annotation and the fenced ```text block.

    FIXTURES EXTENSION in `tests/architecture/catalog-uat.test.ts`:

    Locate the existing `"/claude:plugin marketplace info <name>"` outer-map entry (line 1263). KEEP the existing `"scope-mismatch-not-added"` inner entry byte-identical. Add the SIX new inner entries in the same order the catalog presents them. Each entry follows the existing fixture pattern (CatalogFixture: `message` + `pi` + optional `expectedSeverity`).

    The `pi` field for ALL info-surface fixtures is `piWithBothLoaded()` -- info messages never emit soft-dep markers (Phase 42 design; the probe arg is accepted for signature parity but unused per `renderMarketplaceInfo`'s JSDoc at `shared/notify.ts:1740-1743`).

    For the `expectedSeverity` field: omit it for the four success states + the fan-out state (info severity, no second arg); set `"error"` for the two `{not added}` failure states.

    Cite the source plan in a section-header comment immediately above the new fixtures: `// Phase 43 / Plan 43-02 / INFO-07: full catalog state coverage for marketplace info`.

    SANITY: run `npm test -- --test-name-pattern="catalog UAT"` after each fixture is added -- if a single byte mismatches, the test failure points exactly to the diverging fixture / catalog block. Iterate until GREEN. Common drift causes: trailing newline inside the fenced block (the parser does `fenceBody.join("\n")` so trailing `\n` issues are typically tooling-introduced; check that the fence's closing ``` lives on its own line with no leading whitespace).

    FINAL GATE: `npm run check` GREEN. ESLint MAY flag `tests/architecture/catalog-uat.test.ts` if the new fixtures' `satisfies NotificationMessage` clauses break a max-LoC rule; if so, the planner extracts each fixture's message into a typed `const` ABOVE the FIXTURES map and references it inside the map. This is a presentation refactor, not a behavior change.

    PRE-COMMIT: per CLAUDE.md, run `pre-commit run --files docs/output-catalog.md tests/architecture/catalog-uat.test.ts` BEFORE `git commit`. Iterate to clean. If working in a worktree, prefix the commit with `SKIP=trufflehog` and run `pre-commit run trufflehog --all-files` separately.

    COMMIT: ONE commit per CLAUDE.md atomic-ish (this plan is NOT subject to Phase 42's atomic-supersession strictness, but bundling catalog + UAT moves keeps `npm run check` GREEN at every commit boundary). Title (≤72 chars): `feat(43-02): catalog states + UAT fixtures for marketplace info`. Body cites INFO-07 + INFO-01 + INFO-03 + the FIXTURES count delta.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - Command `grep -cE "^&lt;!-- catalog-state: " docs/output-catalog.md` returns at least 64 (baseline is 58 pre-Phase 43; Plan 43-02 adds 6 new states for a floor of 64).
    - Command `grep -nE "^&lt;!-- catalog-state: (github-single-scope-full|github-single-scope-minimal|path-single-scope|path-single-scope-with-description|both-scopes-fan-out|absent-from-both|scope-mismatch-not-added) -->" docs/output-catalog.md` returns exactly 7 hits (the 6 new states + the preserved Phase 42 state).
    - Command `grep -nE '"(github-single-scope-full|github-single-scope-minimal|path-single-scope|path-single-scope-with-description|both-scopes-fan-out|absent-from-both|scope-mismatch-not-added)"' tests/architecture/catalog-uat.test.ts` returns at least 7 hits.
    - The `"/claude:plugin marketplace info &lt;name&gt;"` outer-map entry in `tests/architecture/catalog-uat.test.ts` contains exactly 7 inner keys (1 pre-existing + 6 new). The pre-existing `"scope-mismatch-not-added"` key + payload + `expectedSeverity: "error"` is byte-identical to Phase 42's landed shape.
    - `npm test -- --test-name-pattern="catalog UAT"` exits 0; no `[MISSING FIXTURE]` or `[BYTE MISMATCH]` or `[SEVERITY MISMATCH]` failures.
    - `npm run check` exits 0.
    - Catalog narrative under `## /claude:plugin marketplace info <name>` no longer contains the "lands in Phase 43" deferral language (search: `grep -nE "lands in Phase 43" docs/output-catalog.md` returns no hits within the info-surface section).
    - The four success fixtures (`github-single-scope-full`, `github-single-scope-minimal`, `path-single-scope`, `path-single-scope-with-description`) omit `expectedSeverity` (info / no second arg).
    - The fan-out fixture (`both-scopes-fan-out`) omits `expectedSeverity` (info).
    - The two `{not added}` fixtures (`absent-from-both`, `scope-mismatch-not-added`) set `expectedSeverity: "error"`.
    - Pre-commit hooks GREEN; trufflehog clean.
  </acceptance_criteria>
  <done>
    INFO-07 closed: every reachable byte form of the `marketplace info` command surface is enumerated under `## /claude:plugin marketplace info <name>` and locked byte-equal via the catalog UAT. The Phase 42 `scope-mismatch-not-added` anchor is preserved untouched. Phase 43 is ready for `/gsd-verify-work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog narrative ↔ fixture map | The catalog UAT depends on byte-equality between the fenced text block and the live `notify()` output for each `(section, state)` tuple. A typo in either side surfaces as a `[BYTE MISMATCH]` failure -- not silently. |
| Documentation ↔ implementation | Plan 43-02 modifies docs + tests only; no production code path changes. Any drift between the catalog narrative and the actual Phase 43-01 implementation surfaces as a UAT failure that points at the specific state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02-01 | Tampering (catalog drift) | `docs/output-catalog.md` H2 section vs. FIXTURES outer-map key | mitigate | The catalog-uat parser fails fast with `[MISSING FIXTURE]` if the H2 string and the outer-map key diverge by even one character. Plan 43-02 keeps the existing Phase 42 outer-map key string intact; new states only extend the inner map. |
| T-43-02-02 | Tampering (anchor regression) | Phase 42 `scope-mismatch-not-added` fixture preservation | mitigate | Plan 43-02 explicitly forbids modifying the existing fixture's annotation, fence body, FIXTURE payload, or `expectedSeverity` field. The acceptance-criteria byte-identity check is enforced via `grep` for the exact existing key string. |
| T-43-02-SC | Tampering | npm/pip/cargo installs | accept | Plan 43-02 installs zero packages. No supply-chain surface introduced. |
</threat_model>

<verification>
Phase-level verification after Plan 43-02 lands:

1. `npm run check` exits 0.
2. `grep -cE "^<!-- catalog-state: " docs/output-catalog.md` returns at least 64.
3. The catalog UAT test (`catalog UAT: every <!-- catalog-state: --> annotation pairs byte-equal with notify()`) is GREEN.
4. Pre-commit hooks GREEN at commit boundary; trufflehog clean (separate run if SKIP-prefixed in worktree).
5. Phase 42's `scope-mismatch-not-added` fixture preserved byte-identical (verified by diff against the pre-Plan-43-02 fixture body).
</verification>

<success_criteria>
Plan 43-02 is complete when ALL of the following are true:

1. The `## /claude:plugin marketplace info <name>` H2 in `docs/output-catalog.md` enumerates 7 catalog states: 4 success states (github-full / github-minimal / path / path-with-description), 1 fan-out (both-scopes), and 2 failure states (absent-from-both / scope-mismatch-not-added).
2. Every annotated catalog state has a matching FIXTURE in `tests/architecture/catalog-uat.test.ts`'s `FIXTURES["/claude:plugin marketplace info <name>"]` inner map; severity routing is correct per state.
3. Phase 42's `scope-mismatch-not-added` block (catalog narrative + annotation + fenced text + FIXTURE payload + `expectedSeverity: "error"`) is preserved byte-identical.
4. `npm run check` exits 0; the catalog UAT byte-equality runner produces zero `[MISSING FIXTURE]` / `[BYTE MISMATCH]` / `[SEVERITY MISMATCH]` failures.
5. The catalog narrative under the info-surface H2 no longer carries Phase 42's "lands in Phase 43" deferral language.

INFO-07 marked as Phase 43 outcome in REQUIREMENTS.md traceability after `/gsd-verify-work` confirms.
</success_criteria>

<output>
Create `.planning/phases/43-marketplace-info-command/43-02-SUMMARY.md` when done. Summary MUST cite the commit SHA, list the 2 files in the commit footprint (`docs/output-catalog.md`, `tests/architecture/catalog-uat.test.ts`), confirm `npm run check` GREEN, and note the final `examples.length` value (Phase 42 floor was 30; Phase 43-02 grows the actual count -- record the new total).
</output>
