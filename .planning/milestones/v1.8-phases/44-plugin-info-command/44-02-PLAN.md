---
phase: 44-plugin-info-command
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
autonomous: true
requirements:
  - INFO-02
  - INFO-05
tags:
  - catalog
  - uat
  - byte-equality
  - info-surface
  - plugin-info
must_haves:
  truths:
    - "`docs/output-catalog.md` gains a NEW H2 section `## /claude:plugin info <plugin>@<marketplace>` enumerating every reachable byte form for the plugin info command surface: installed-single-scope (with resolved components + description), available-single-scope (with description), unavailable-single-scope (with `{hooks}` or `{lsp}` reason), installed-both-scopes (INFO-03 fan-out, project-first), components-not-resolved (INFO-05 external-source marker), missing-plugin-in-manifest (`{not in manifest}` REASON at 2-space indent under marketplace header), missing-marketplace (`{not added}` at column 0 with NO scope bracket per D-03 absent-from-both), and `--scope` mismatch (`{not added}` at column 0 WITH `[scope]` bracket)"
    - "Every catalog state under the NEW H2 is paired with a `<!-- catalog-state: STATE -->` annotation matching the FIXTURES inner-map key byte-for-byte (per `sectionRe` / `stateRe` in `tests/architecture/catalog-uat.test.ts:82-83`)"
    - "Every annotated catalog state has a corresponding FIXTURE entry in `tests/architecture/catalog-uat.test.ts`'s `FIXTURES[\"/claude:plugin info <plugin>@<marketplace>\"]` inner map; the FIXTURE's `message: NotificationMessage` payload, when run through `notify(mockCtx, mockPi, message)`, byte-equals the fenced expected output"
    - "The catalog UAT byte-equality test (`catalog UAT: every <!-- catalog-state: --> annotation pairs byte-equal with notify()`) is GREEN against the expanded fixture set"
    - "ALL pre-existing Phase 42 + Phase 43 catalog states are preserved byte-identical -- Plan 44-02 additions are PURELY additive under a NEW H2; no Phase 42 or Phase 43 fixture is renamed, reordered, or modified"
    - "Severity routing on every new fixture is correct: success states (installed / available / unavailable / installed-both-scopes / components-not-resolved) OMIT `expectedSeverity` (info -- no second arg to `ctx.ui.notify`); `(failed)` rows (`{not added}` missing-marketplace, `{not added}` --scope mismatch, `{not in manifest}` missing-plugin) SET `expectedSeverity: \"error\"`"
    - "`npm run check` exits 0; the assertion that `examples.length >= 30` at `catalog-uat.test.ts:1632` still holds (with Plan 44-02's additions the count grows from 64 to at least 72 -- enforces the floor, never the ceiling)"
  artifacts:
    - path: "docs/output-catalog.md"
      provides: "New H2 section `## /claude:plugin info <plugin>@<marketplace>` with one fenced catalog-state block per reachable plugin-info-surface state plus narrative prose explaining the trigger / severity / reload-hint behavior for each. Placed AFTER the Phase 43 `## /claude:plugin marketplace info <name>` H2 section (around docs/output-catalog.md:908) and BEFORE the existing `## /claude:plugin marketplace remove <name>` section."
      contains: "installed-single-scope"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "New `FIXTURES[\"/claude:plugin info <plugin>@<marketplace>\"]` outer-map entry with one fixture per new catalog state. Pi-mock selection uses `piWithBothLoaded()` (info surface emits NO soft-dep markers). Severity arg locked per state. Placed AFTER the Phase 43 `\"/claude:plugin marketplace info <name>\"` outer-map entry (around line 1399) and BEFORE the existing `\"/claude:plugin marketplace remove <name>\"` entry."
      contains: "installed-single-scope"
  key_links:
    - from: "docs/output-catalog.md"
      to: "tests/architecture/catalog-uat.test.ts"
      via: "`loadCatalogExamples` + `sectionRe` + `stateRe` parser: the NEW H2 section name `/claude:plugin info <plugin>@<marketplace>` MUST equal the FIXTURES outer-map key byte-for-byte; every `<!-- catalog-state: STATE -->` annotation MUST equal a FIXTURES inner-map key byte-for-byte"
      pattern: "/claude:plugin info <plugin>@<marketplace>"
    - from: "tests/architecture/catalog-uat.test.ts FIXTURES"
      to: "extensions/pi-claude-marketplace/shared/notify.ts"
      via: "Each FIXTURE constructs a `NotificationMessage` (plugin-info | plugin-info-cascade) and routes it through the public `notify()` entry; byte equality vs. the catalog fenced block is the binding gate"
      pattern: "notify\\(ctx as never, fixture\\.pi as never, fixture\\.message\\)"
---

<objective>
Close the INFO-07 catalog UAT coverage for the plugin-info surface by enumerating every byte form the `plugin info` command can emit in `docs/output-catalog.md` and pairing each with a programmatic FIXTURE in `tests/architecture/catalog-uat.test.ts`'s byte-equality runner. Depends on Plan 44-01 having landed the orchestrator / handler / completion / `PluginInfoCascadeMessage` type so the fixtures drive against a working production surface.

Purpose: The catalog UAT is the v1.4 user-contract gate (SNM-31). Until INFO-07's plugin-info fixtures land, the `plugin info` byte form is only locked at the per-status `notify-v2` unit-test level -- the higher-fidelity catalog-vs-renderer byte-equality gate has no coverage for the new surface. Plan 44-02 adds that coverage in a single atomic commit (catalog narrative + fenced blocks + FIXTURES map all move together per RESEARCH Pitfall 6 / catalog-uat coupling). Closes the v1.8 milestone.

Output: A new H2 section in `docs/output-catalog.md` plus a new FIXTURES outer-map entry in `tests/architecture/catalog-uat.test.ts`, with `npm run check` GREEN.

NOTE: INFO-07 was assigned to Phase 43 in the REQUIREMENTS.md traceability table because that's when catalog UAT coverage for the info surface first lands user-observably (the `marketplace info` H2 section + fixtures). Plan 44-02 EXTENDS the INFO-07 catalog coverage to the second info surface (`plugin info`) without re-closing the requirement. The `requirements:` frontmatter lists INFO-02 + INFO-05 (the Phase 44-owned requirements) because that's what this plan's byte forms encode -- INFO-07's traceability table row stays attributed to Phase 43.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-plugin-info-command/44-CONTEXT.md
@.planning/phases/44-plugin-info-command/44-01-PLAN.md
@.planning/phases/43-marketplace-info-command/43-02-PLAN.md
@.planning/phases/43-marketplace-info-command/43-VERIFICATION.md
@.planning/phases/42-type-model-render-seam-foundations/42-RESEARCH.md
@./CLAUDE.md
@extensions/pi-claude-marketplace/shared/notify.ts
@docs/output-catalog.md
@docs/messaging-style-guide.md
@tests/architecture/catalog-uat.test.ts
@tests/shared/notify-v2.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enumerate `plugin info` catalog states + paired FIXTURES; lockstep with byte-equality UAT</name>
  <files>
    docs/output-catalog.md,
    tests/architecture/catalog-uat.test.ts
  </files>
  <read_first>
    docs/output-catalog.md,
    tests/architecture/catalog-uat.test.ts,
    extensions/pi-claude-marketplace/shared/notify.ts,
    .planning/phases/44-plugin-info-command/44-01-PLAN.md,
    .planning/phases/43-marketplace-info-command/43-02-PLAN.md
  </read_first>
  <behavior>
    - Test (catalog UAT GREEN end-to-end): `npm test -- --test-name-pattern="catalog UAT"` exits 0. The new `<!-- catalog-state: ... -->` annotations under the `## /claude:plugin info <plugin>@<marketplace>` H2 each pair byte-equal with their FIXTURE.
    - Test (catalog floor preserved): the `examples.length >= 30` assertion at `catalog-uat.test.ts:1632` still holds (Plan 44-02 grows it from 64 to at least 72; the floor never fails).
    - Test (Phase 42 + Phase 43 fixtures preserved byte-identical): every pre-existing catalog state (the 64 landed through Phase 43) continues to render byte-identical. Regress this if anything in Plan 44-02 modifies them; reject the change.
    - Test (severity routing correctness): the five success states (installed-single-scope / available-single-scope / unavailable-single-scope / installed-both-scopes / components-not-resolved) emit ONLY one arg to `ctx.ui.notify` (no severity); the three failure states (`{not added}` missing-marketplace, `{not added}` --scope mismatch, `{not in manifest}` missing-plugin) emit two args with the second being `"error"`. Enforced by the existing catalog-uat severity-mismatch detector (`tests/architecture/catalog-uat.test.ts` severity check, around line 1578).
    - Test (loadCatalogExamples parses every new state): each new `<!-- catalog-state: STATE -->` annotation under the plugin-info-surface H2 produces an entry in the `examples` array; verifiable by counting `grep -cE '^<!-- catalog-state:' docs/output-catalog.md` -- baseline is 64 (post-Phase-43); Plan 44-02 adds at least 8 new states for a floor of 72.
  </behavior>
  <action>
    PRE-FLIGHT: re-read the Phase 43 H2 section at `docs/output-catalog.md:821-906` to confirm the `## /claude:plugin marketplace info <name>` block layout AND the placement (it lives BETWEEN the `## /claude:plugin marketplace remove <name>` section and the older marketplace blocks per the post-Phase-43 catalog ordering). Re-read the Phase 43 FIXTURES outer-map entry at `tests/architecture/catalog-uat.test.ts:1278-1399`. DO NOT modify Phase 43's H2 section or fixtures -- this plan ADDS a new sibling H2 section + a new sibling FIXTURES outer-map entry.

    PLACEMENT: Add the new H2 section `## /claude:plugin info <plugin>@<marketplace>` IMMEDIATELY AFTER the Phase 43 `## /claude:plugin marketplace info <name>` section (so the two info H2 sections are adjacent). The order should be: marketplace info -> plugin info -> marketplace remove (existing). Add the matching FIXTURES outer-map entry in the SAME relative position in `catalog-uat.test.ts` (after the marketplace-info entry, before the marketplace-remove entry).

    CATALOG STATE MATRIX -- enumerate the following 8 states under the new H2. For each, add a narrative paragraph (1-2 sentences citing the trigger / severity / reload-hint behavior), then a `<!-- catalog-state: STATE -->` annotation, then a fenced ```text block with the EXACT byte form the renderer produces. STATE names are lowercase + hyphen-separated to match the parser's `stateRe = /^<!-- catalog-state: ([a-z0-9-]+) -->\s*$/`.

    Recommended STATE name + payload outline (use these exact STATE names so the SUMMARY can cross-reference -- the planner may rename if a more descriptive name emerges, BUT must keep the names UNIQUE across the file's 64+ existing catalog states):

    1. **`installed-single-scope`** -- installed plugin, `--scope user`, with resolved components + description + dependencies. Body:
       ```
       ● claude-plugins-official [user] <autoupdate>
         ● commit-commands v1.2.0 (installed)
           Helpful git commit commands for everyday use.
           agents: review-bot
           commands: c1, c2
           skills: commit-summary
       ```
       FIXTURE: `PluginInfoMessage` with `kind: "plugin-info"`, `marketplaceName: "claude-plugins-official"`, `marketplaceScope: "user"`, `marketplaceDetails: { autoupdate: true }`, `plugin: { status: "installed", name: "commit-commands", version: "1.2.0", description: "Helpful git commit commands for everyday use.", componentsResolved: true, components: { agents: ["review-bot"], commands: ["c1", "c2"], skills: ["commit-summary"] } }`. NO `expectedSeverity` (info). NO `mcp` array (omit when empty per `appendResolvedComponentLines` notify.ts:1900-1906 which gates on `names.length > 0`). NO `dependencies` field (separate fixture for that).

    2. **`installed-single-scope-with-dependencies`** -- same as above but with a `dependencies` field. Body has an additional 4-space-indented `dependencies: <plugin>@<mp>, ...` line AT THE END. FIXTURE adds `dependencies: ["helper@utils-mp"]` to the plugin row.

    3. **`available-single-scope`** -- AVAILABLE plugin (in marketplace.json, not installed in scope), `--scope user`, with description + version. Body:
       ```
       ● community-mp [user] <no autoupdate>
         ○ chat-helper v0.5.0 (available)
           Quick chat helper plugin; experimental.
           commands: chat
           skills: chat-init
       ```
       FIXTURE: `plugin.status: "available"`, glyph is `○` (per `pluginInfoStatusGlyph` notify.ts:1856-1859). NO `expectedSeverity` (info).

    4. **`unavailable-single-scope`** -- UNAVAILABLE plugin (declares `hooks` per the v1.4 grammar; resolver narrowed via `narrowResolverNotes` to `"hooks"` REASON). Body:
       ```
       ● community-mp [user] <no autoupdate>
         ⊘ legacy-plugin v0.1.0 (unavailable) {hooks}
           Old plugin that declares hooks; not installable in Pi.
       ```
       FIXTURE: `plugin.status: "unavailable"`, `reasons: ["hooks"]`, glyph is `⊘`. NO `expectedSeverity` (info -- per `computeSeverity` notify.ts:1416-1418, the plugin-info severity is `"error"` ONLY when `plugin.status === "failed"`; `"unavailable"` is info, just like the list surface). Components are typically omitted when the plugin is unavailable (no `componentsResolved: true` shape because resolver rejected it); use `componentsResolved: false` AND NO `components: not resolved` line in the rendered body? Re-check: per `renderPluginInfo` at notify.ts:2003-2014 the switch on `componentsResolved` ALWAYS emits either per-kind lines or the `components: not resolved` marker. SO this fixture's `componentsResolved: false` WILL render the marker line. Adjust the body to include it:
       ```
       ● community-mp [user] <no autoupdate>
         ⊘ legacy-plugin v0.1.0 (unavailable) {hooks}
           Old plugin that declares hooks; not installable in Pi.
           components: not resolved
       ```
       Verify by reading the renderer's switch before writing the fixture.

    5. **`installed-both-scopes-fan-out`** -- INFO-03 fan-out, no `--scope`, plugin present in BOTH scopes (e.g., same plugin installed under user-scope `mp` AND under project-scope `mp`). Project block FIRST, user block SECOND, separated by ONE blank line. Each block has its own marketplace header. Body:
       ```
       ● mp [project] <autoupdate>
         ● foo v1.0.0 (installed)
           skills: s1

       ● mp [user] <no autoupdate>
         ● foo v2.0.0 (installed)
           agents: a1
       ```
       FIXTURE: `kind: "plugin-info-cascade"` with `blocks: [projectBlock, userBlock]`. Each block is a full `PluginInfoMessage`. NO `expectedSeverity` (info). Narrative MUST cite MSG-GR-3 / INFO-03 / project-first ordering.

    6. **`components-not-resolved`** -- INFO-05, single scope, plugin whose source kind is external (`npm` / `git-subdir` / `url`). The renderer emits the marker line in place of per-kind component lists. Body:
       ```
       ● remote-mp [user] <no autoupdate>
         ● remote-plugin v1.0.0 (installed)
           Remote plugin sourced from an external npm package.
           components: not resolved
       ```
       FIXTURE: `plugin.status: "installed"`, `componentsResolved: false`, plus the description. NO `expectedSeverity` (info). Narrative MUST cite INFO-05 + NFR-5 (the orchestrator deliberately does NOT fetch external sources).

    7. **`missing-plugin-not-in-manifest`** -- `{not in manifest}` failure: marketplace exists in the scope but the plugin name is not in its manifest. Body:
       ```
       ● mp [user] <no autoupdate>
         ⊘ ghost-plugin (failed) {not in manifest}
       ```
       FIXTURE: `plugin.status: "failed"`, `reasons: ["not in manifest"]`, `componentsResolved: false`, no `version` (plugin doesn't exist; no version to display). `expectedSeverity: "error"`. Narrative MUST cite the existing `{not in manifest}` REASON (used by `update.ts` post-Phase 29 / UXG-08 for the same failure semantics) AND distinguish from `{not added}` (missing-marketplace).

    8. **`missing-marketplace-not-added-absent-from-both`** -- `{not added}` failure, no `--scope` filter, marketplace name is not present in EITHER scope. Body:
       ```
       ⊘ ghost-mp (failed) {not added}
       ```
       FIXTURE: `PluginInfoMessage` with `plugin.status: "failed"`, `plugin.name: "ghost-mp"` (NB: this is the MARKETPLACE name, not the plugin name -- the orchestrator emits the missing-marketplace name per the carve-out per 44-01 Task 2 behavior), `reasons: ["not added"]`, `componentsResolved: false`, NO `plugin.scope` field (D-03 absent-from-both: no scope bracket). Non-rendered placeholders: `marketplaceName: "ghost-mp"`, `marketplaceScope: "user"`, `marketplaceDetails: { autoupdate: false }`. `expectedSeverity: "error"`. Narrative MUST cite INFO-04 + D-03 (no scope bracket because the marketplace is in neither scope) AND distinguish from the `--scope`-mismatch variant.

    9. **`missing-marketplace-not-added-scope-mismatch`** -- `{not added}` failure WITH explicit `--scope`, marketplace is in the OTHER scope. Body:
       ```
       ⊘ ghost-mp [user] (failed) {not added}
       ```
       FIXTURE: same as above but `plugin.scope: "user"` (the renderer emits the `[user]` bracket). `expectedSeverity: "error"`. Narrative MUST cite INFO-04 AND distinguish from the absent-from-both variant. This is the plugin-info-surface MIRROR of Phase 42's `scope-mismatch-not-added` state under `marketplace info`.

    NARRATIVE STRUCTURE -- the new H2 section should follow Phase 43's layout pattern (intro paragraph + sub-sections):

    - Brief intro: "Read-only detail surface (Phase 44). Renders the install-cascade always-marketplace-header form (mirrors `install`'s shape per INFO-02) with a per-plugin row at 2-space indent, optional description block hard-wrapped at col 4 / 66-col text width, then either per-kind component lists (sorted: `agents`, `commands`, `mcp`, `skills`) with optional `dependencies:` line OR the `components: not resolved` marker (INFO-05). Phase 44 / INFO-02 + INFO-05 + INFO-07 lock the full state set below."
    - Severity routing paragraph: "Every success state (installed / available / unavailable / installed-both-scopes / components-not-resolved) is `info` severity (no second arg to `ctx.ui.notify`); the three `(failed)` states (`{not added}` missing-marketplace, `{not added}` --scope mismatch, `{not in manifest}` missing-plugin) route to `error`. No reload-hint fires on any state (info surfaces are read-only per SNM-33)."
    - Sub-section: "Success -- installed single scope" -- the `installed-single-scope` fixture, narrative explains what "single scope" means + which components render.
    - Sub-section: "Success -- installed single scope with dependencies" -- the `installed-single-scope-with-dependencies` fixture, narrative explains the `dependencies:` line placement (LAST after all component lists per INFO-02).
    - Sub-section: "Success -- available single scope" -- the `available-single-scope` fixture, narrative explains the `○` glyph + that `(available)` is info severity.
    - Sub-section: "Success -- unavailable single scope" -- the `unavailable-single-scope` fixture, narrative explains the `⊘` glyph + reason brace + that `(unavailable)` is info severity (vs `(failed)` which is error severity).
    - Sub-section: "Multi-scope fan-out -- both scopes hold the plugin" -- the `installed-both-scopes-fan-out` fixture, narrative cites MSG-GR-3 / INFO-03 project-first ordering AND that each block carries its OWN marketplace header (mirrors the install-cascade `\n\n` join).
    - Sub-section: "Components not resolved (external source)" -- the `components-not-resolved` fixture, narrative cites INFO-05 + NFR-5.
    - Sub-section: "Failure -- plugin not in manifest" -- the `missing-plugin-not-in-manifest` fixture, narrative cites the existing `{not in manifest}` REASON.
    - Sub-section: "Failure -- missing marketplace (no --scope filter)" -- the `missing-marketplace-not-added-absent-from-both` fixture, narrative cites D-03.
    - Sub-section: "Failure -- missing marketplace (--scope mismatch)" -- the `missing-marketplace-not-added-scope-mismatch` fixture, narrative distinguishes from the absent-from-both variant.

    Each fixture: ONE narrative paragraph followed by the `<!-- catalog-state: -->` annotation and the fenced ```text block.

    FIXTURES EXTENSION in `tests/architecture/catalog-uat.test.ts`:

    Locate the existing `"/claude:plugin marketplace info <name>"` outer-map entry (line 1278-1399). Add a NEW outer-map entry `"/claude:plugin info <plugin>@<marketplace>"` IMMEDIATELY AFTER it (BEFORE the `"/claude:plugin marketplace remove <name>"` entry at line 1404). Each inner entry follows the existing fixture pattern (CatalogFixture: `message` + `pi` + optional `expectedSeverity`).

    The `pi` field for ALL info-surface fixtures is `piWithBothLoaded()` -- info messages never emit soft-dep markers (Phase 42 design; the probe arg is accepted for signature parity but unused per `renderPluginInfo`'s JSDoc).

    For the `expectedSeverity` field: omit it for the five success states (states 1-6 above except the failure states); set `"error"` for the three `(failed)` states (states 7-9).

    Cite the source plan in a section-header comment immediately above the new outer-map entry: `// Phase 44 / Plan 44-02 / INFO-02 + INFO-05 + INFO-07: full catalog state coverage for plugin info`.

    SANITY: run `npm test -- --test-name-pattern="catalog UAT"` after each fixture is added -- if a single byte mismatches, the test failure points exactly to the diverging fixture / catalog block. Iterate until GREEN. Common drift causes:
    - Trailing newline inside the fenced block (the parser does `fenceBody.join("\n")` so trailing `\n` issues are typically tooling-introduced; check that the fence's closing ``` lives on its own line with no leading whitespace).
    - Component name order: the renderer requires PRE-SORTED arrays (PR-5 precondition); a fixture with `commands: ["c2", "c1"]` will render `commands: c2, c1`, which will NOT match a catalog block written as `commands: c1, c2`. Sort the FIXTURE arrays alphabetically before writing both halves to match.
    - Kind order: the renderer fixes the per-kind output order at `agents, commands, mcp, skills` (notify.ts:1881-1886). Catalog blocks MUST use this order.

    FINAL GATE: `npm run check` GREEN. ESLint MAY flag `tests/architecture/catalog-uat.test.ts` if the new fixtures' `satisfies NotificationMessage` clauses break a max-LoC rule; if so, the executor extracts each fixture's message into a typed `const` ABOVE the FIXTURES map and references it inside the map (mirrors what Phase 43's Plan 43-02 may have done if needed -- the policy is a presentation refactor, not a behavior change).

    PRE-COMMIT: per CLAUDE.md, run `pre-commit run --files docs/output-catalog.md tests/architecture/catalog-uat.test.ts` BEFORE `git commit`. Iterate to clean. If working in a worktree, prefix the commit with `SKIP=trufflehog` and run `pre-commit run trufflehog --all-files` separately.

    COMMIT: ONE commit bundling catalog + UAT additions keeps `npm run check` GREEN at the commit boundary. Title (≤72 chars): `feat(44-02): catalog + UAT fixtures for plugin info (INFO-02/05)`. Body cites INFO-02 + INFO-05 + the INFO-07 catalog-coverage extension + the FIXTURES count delta + the v1.8 milestone close.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - Command `grep -cE "^<!-- catalog-state: " docs/output-catalog.md` returns at least 72 (baseline is 64 post-Phase-43; Plan 44-02 adds at least 8 new states).
    - Command `grep -nE "^## .claude:plugin info <plugin>@<marketplace>." docs/output-catalog.md` returns exactly 1 hit (the new H2).
    - Command `grep -nE "^<!-- catalog-state: (installed-single-scope|installed-single-scope-with-dependencies|available-single-scope|unavailable-single-scope|installed-both-scopes-fan-out|components-not-resolved|missing-plugin-not-in-manifest|missing-marketplace-not-added-absent-from-both|missing-marketplace-not-added-scope-mismatch) -->" docs/output-catalog.md` returns exactly 9 hits (one per new state).
    - Command `grep -nE '"(installed-single-scope|installed-single-scope-with-dependencies|available-single-scope|unavailable-single-scope|installed-both-scopes-fan-out|components-not-resolved|missing-plugin-not-in-manifest|missing-marketplace-not-added-absent-from-both|missing-marketplace-not-added-scope-mismatch)"' tests/architecture/catalog-uat.test.ts` returns at least 9 hits.
    - The `"/claude:plugin info <plugin>@<marketplace>"` outer-map entry in `tests/architecture/catalog-uat.test.ts` contains exactly 9 inner keys.
    - `npm test -- --test-name-pattern="catalog UAT"` exits 0; no `[MISSING FIXTURE]` or `[BYTE MISMATCH]` or `[SEVERITY MISMATCH]` failures.
    - `npm run check` exits 0.
    - The five success fixtures (`installed-single-scope`, `installed-single-scope-with-dependencies`, `available-single-scope`, `unavailable-single-scope`, `installed-both-scopes-fan-out`, `components-not-resolved`) OMIT `expectedSeverity` (info / no second arg). NOTE: this is 6 success states. Verify by grep that none of these 6 contain `expectedSeverity` within their inner-map entry.
    - The three `(failed)` fixtures (`missing-plugin-not-in-manifest`, `missing-marketplace-not-added-absent-from-both`, `missing-marketplace-not-added-scope-mismatch`) SET `expectedSeverity: "error"`.
    - Pre-existing Phase 42 + Phase 43 fixtures preserved byte-identical (verified by `git diff tests/architecture/catalog-uat.test.ts` showing only ADDITIONS to the FIXTURES map; verified by Phase 42's `scope-mismatch-not-added` block + Phase 43's 6 new states all continuing to pair byte-equal).
    - Pre-commit hooks GREEN; trufflehog clean.
  </acceptance_criteria>
  <done>
    INFO-02 + INFO-05 + INFO-07 (plugin-info extension) closed: every reachable byte form of the `plugin info` command surface is enumerated under `## /claude:plugin info <plugin>@<marketplace>` and locked byte-equal via the catalog UAT. The Phase 42 + Phase 43 anchors are preserved untouched. Phase 44 is ready for `/gsd-verify-work`; v1.8 milestone closes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog narrative ↔ fixture map | The catalog UAT depends on byte-equality between the fenced text block and the live `notify()` output for each `(section, state)` tuple. A typo in either side surfaces as a `[BYTE MISMATCH]` failure -- not silently. |
| Documentation ↔ implementation | Plan 44-02 modifies docs + tests only; no production code path changes. Any drift between the catalog narrative and the actual Phase 44-01 implementation surfaces as a UAT failure that points at the specific state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02-01 | Tampering (catalog drift) | `docs/output-catalog.md` new H2 section vs. FIXTURES outer-map key | mitigate | The catalog-uat parser fails fast with `[MISSING FIXTURE]` if the H2 string and the outer-map key diverge by even one character. Plan 44-02 introduces both halves in lockstep within a single commit so they cannot drift at commit time; future-drift detection is the standing UAT gate. |
| T-44-02-02 | Tampering (anchor regression) | Phase 42 + Phase 43 fixture preservation | mitigate | Plan 44-02 explicitly forbids modifying any pre-existing fixture's annotation, fence body, FIXTURE payload, or `expectedSeverity` field. The acceptance-criteria byte-identity check is enforced via `git diff` review + the catalog UAT's transitive byte-equality gate. |
| T-44-02-03 | Tampering (component sort drift) | per-kind component arrays in plugin-info fixtures | mitigate | The renderer's `appendResolvedComponentLines` requires pre-sorted input (PR-5 precondition documented at notify.ts:1888-1894). The acceptance criteria + the per-fixture byte-equality check enforce sorted arrays in BOTH the fenced block AND the FIXTURE payload; a future fixture authored with unsorted components will fail the byte gate. |
| T-44-02-SC | Tampering | npm/pip/cargo installs | accept | Plan 44-02 installs zero packages. No supply-chain surface introduced. |
</threat_model>

<verification>
Phase-level verification after Plan 44-02 lands:

1. `npm run check` exits 0 (typecheck + ESLint + Prettier + tests).
2. `grep -cE "^<!-- catalog-state: " docs/output-catalog.md` returns at least 72.
3. The catalog UAT test (`catalog UAT: every <!-- catalog-state: --> annotation pairs byte-equal with notify()`) is GREEN.
4. Pre-commit hooks GREEN at commit boundary; trufflehog clean (separate run if SKIP-prefixed in worktree).
5. Phase 42 + Phase 43 fixtures preserved byte-identical (verified by `git diff` against the pre-Plan-44-02 file showing only ADDITIONS to the FIXTURES map AND additions of the new H2 block to the catalog doc).
6. The Phase 44 success criteria SC#1-5 from the ROADMAP (line 832-836) are all user-observable end-to-end through the catalog UAT byte-equality gate.
</verification>

<success_criteria>
Plan 44-02 is complete when ALL of the following are true:

1. The new H2 section `## /claude:plugin info <plugin>@<marketplace>` in `docs/output-catalog.md` enumerates 9 catalog states: 6 success states (installed / installed-with-dependencies / available / unavailable / installed-both-scopes-fan-out / components-not-resolved) + 3 failure states (`{not in manifest}`, `{not added}` absent-from-both, `{not added}` --scope mismatch).
2. Every annotated catalog state has a matching FIXTURE in `tests/architecture/catalog-uat.test.ts`'s new `FIXTURES["/claude:plugin info <plugin>@<marketplace>"]` outer-map entry; severity routing is correct per state (6 omit `expectedSeverity` for info; 3 set `"error"`).
3. Phase 42 + Phase 43 fixtures (including the `scope-mismatch-not-added` Phase 42 anchor and all 6 Phase 43 `marketplace info` states) are preserved byte-identical.
4. `npm run check` exits 0; the catalog UAT byte-equality runner produces zero `[MISSING FIXTURE]` / `[BYTE MISMATCH]` / `[SEVERITY MISMATCH]` failures.
5. The catalog narrative under the plugin-info-surface H2 clearly distinguishes the three `(failed)` states from each other (`{not in manifest}` vs `{not added}` absent-from-both vs `{not added}` scope-mismatch).

INFO-02 + INFO-05 closed end-to-end (production wiring in Plan 44-01 + catalog UAT byte gate in Plan 44-02). INFO-07 plugin-info catalog coverage extension complete (the requirement itself was attributed to Phase 43 in the traceability table; Plan 44-02 extends its surface to the second info command). v1.8 milestone closes.
</success_criteria>

<output>
Create `.planning/phases/44-plugin-info-command/44-02-SUMMARY.md` when done. Summary MUST cite the commit SHA, list the 2 files in the commit footprint (`docs/output-catalog.md`, `tests/architecture/catalog-uat.test.ts`), confirm `npm run check` GREEN, note the final `examples.length` value (Phase 43 baseline was 64; Plan 44-02 grows the actual count -- record the new total), and explicitly state that the v1.8 milestone is ready for `/gsd-verify-work` -> milestone close.
</output>
</content>
</invoke>
