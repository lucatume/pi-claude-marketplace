---
phase: 44-plugin-info-command
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/index.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts
  - extensions/pi-claude-marketplace/edge/router.ts
  - extensions/pi-claude-marketplace/edge/register.ts
  - extensions/pi-claude-marketplace/edge/completions/provider.ts
  - extensions/pi-claude-marketplace/edge/completions/data.ts
  - tests/architecture/notify-types.test.ts
  - tests/shared/notify-v2.test.ts
  - tests/orchestrators/plugin/info.test.ts
  - tests/edge/handlers/plugin/info.test.ts
  - tests/edge/completions/provider.test.ts
  - tests/edge/router.test.ts
autonomous: true
requirements:
  - INFO-02
  - INFO-05
tags:
  - plugin-info
  - orchestrator
  - edge-handler
  - completion
  - fan-out
must_haves:
  truths:
    - "Running `/claude:plugin info <plugin>@<marketplace> --scope user` against an INSTALLED plugin in user scope renders the install-cascade always-marketplace-header form (header at column 0, plugin row at 2-space indent with `(installed)` status), followed by an optional description hard-wrapped at col 4 / 66-col text width via `wrapDescription`, then per-kind component lists at 4-space indent (`agents`, `commands`, `mcp`, `skills` -- alphabetical by kind, alphabetical within each kind), then an optional `dependencies: <plugin>@<marketplace>, ...` line LAST (per D-01 implementing INFO-02)"
    - "Running the same command against an AVAILABLE plugin (declared in marketplace.json but not installed in the scope) renders the same shape with `(available)` status; an UNAVAILABLE plugin renders `(unavailable)` plus a reasons brace ({hooks} / {lsp} / {unsupported source} / etc.) sourced from the resolver narrowing (parallel to `list.ts::narrowResolverNotes`)"
    - "Running the same command against a plugin present in BOTH scopes (no `--scope` filter) renders TWO plugin-info blocks separated by ONE blank line; project block FIRST, user block SECOND (per D-02 implementing INFO-03 fan-out -- mirrors Phase 43's `MarketplaceInfoCascadeMessage` structural decision via a NEW `PluginInfoCascadeMessage` variant)"
    - "Running the same command against a plugin whose resolver returns `installable: false` AND whose `plugin.json` is unreadable / external (separate git repo, npm, http source per `domain/source.ts`'s `url | git-subdir | npm | unknown` ParsedSource arms in the plugin entry's `source` field) renders the marker line `    components: not resolved` at 4-space indent instead of per-kind component lists (per INFO-05; encoded as the Phase 42 `componentsResolved: false` discriminator on `PluginInfoRow`)"
    - "Running `/claude:plugin info <plugin>@<missing-marketplace>` renders `⊘ <missing-marketplace> (failed) {not added}` -- the Phase 42 INFO-04 carve-out reused: when the marketplace itself is not added in the requested scope, the orchestrator emits the bare `{not added}` row (no `[scope]` bracket when `--scope` was omitted; bracket present when a specific `--scope` was requested) with severity `error`"
    - "Running `/claude:plugin info <missing-plugin>@<known-marketplace>` (marketplace IS added but the plugin name is not in its manifest) renders `(failed) {not in manifest}` AT 2-space indent under the marketplace header at column 0 (uses the existing `not in manifest` REASON from the REASONS tuple -- already a member; sourced from the same `narrowResolverNotes` path as `update.ts`'s `(failed) {not in manifest}` byte form per Phase 29 / UXG-08); severity `error`"
    - "Argument completion for `plugin info <TAB>` returns `<plugin>@<marketplace>` combos for ALL known plugins (installed + available + unavailable) across BOTH scopes via a NEW `\"info\"` mode of the TC-6 plugin-ref completer; the union is built by extending `getPluginToMarketplacesMap` with an `info` mode that yields every plugin row (no status filter) AND does NOT exclude already-installed plugins in the target scope (the existing install-mode exclusion is a CMP-7 install-time UX choice and does NOT apply to info -- info surfaces ALL plugins regardless of install state)"
    - "Argument completion for `plugin info --scope user <TAB>` returns the SAME union (the scope filter does NOT narrow the completion candidate set -- TC-6 contract; the orchestrator handles scope-mismatch at execution time via the same INFO-04 `{not added}` row used by marketplace info)"
    - "The orchestrator reads ONLY local state + resolves against ON-DISK marketplace manifests (`loadState` + `loadMarketplaceManifest` + `resolveStrict`); NO network access; NO `gitOps` import (NFR-5 preserved -- structurally enforced by a grep-gate test mirroring `marketplace/info.ts`'s gate)"
    - "Every `notify()` invocation calls `ctx.ui.notify` exactly ONCE (IL-2 single-site discipline preserved); fan-out is composed by the new `PluginInfoCascadeMessage` variant joining `PluginInfoMessage` blocks with `\\n\\n` before the single `ctx.ui.notify` call"
    - "`npm run check` exits 0 with all new tests GREEN"
    - "Phase 42 + Phase 43 byte-equality preserved: `composeMarketplaceBlock`, `renderMpHeader`, `renderPluginRow`, `composePluginLines`, `joinTokens`, `composeReasons`, `renderMarketplaceInfo`, `composeMpInfoHeader`, `renderMarketplaceInfoCascade`, `renderPluginInfo`, `wrapDescription`, `pluginInfoStatusGlyph`, `appendResolvedComponentLines` bodies are byte-unchanged (the new variant is composed via REUSE of `renderPluginInfo`, not by editing it)"
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "New `PluginInfoCascadeMessage` discriminated-union variant (`kind: 'plugin-info-cascade'`) carrying `blocks: readonly PluginInfoMessage[]`; new file-private `renderPluginInfoCascade` dispatcher arm composing via `.map(renderPluginInfo).join('\\n\\n')`; extended `dispatchInfoMessage` helper switch to handle the 4th info kind; extended `notify()` dispatcher info short-circuit to include `plugin-info-cascade`; extended `computeSeverity` + `shouldEmitReloadHint` + `buildSummaryLine` short-circuits; `NotificationMessage` union extended to 5 arms (cascade + marketplace-info + plugin-info + marketplace-info-cascade + plugin-info-cascade)"
      contains: "plugin-info-cascade"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts"
      provides: "New `getPluginInfo(opts)` orchestrator: read-only (NO `withStateGuard`; NO `gitOps` import); per scope loads `state.json`, locates the requested marketplace record, walks installed plugin records + manifest entries (`loadMarketplaceManifest`) to classify the plugin as installed/available/unavailable/failed, resolves component lists via `resolveStrict` (sorted per INFO-02), detects `componentsResolved: false` for external/non-path source kinds per INFO-05, fans out across both scopes via the new `PluginInfoCascadeMessage` when `--scope` omitted, emits the Phase 42 INFO-04 `{not added}` carve-out for missing-marketplace, emits the existing `{not in manifest}` REASON for missing-plugin-in-known-marketplace"
      exports:
        - "getPluginInfo"
        - "GetPluginInfoOptions"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/index.ts"
      provides: "Re-exports `getPluginInfo` + `GetPluginInfoOptions` alongside existing plugin orchestrators"
    - path: "extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts"
      provides: "`makePluginInfoHandler(pi)` thin-shim factory mirroring `makeInstallHandler` + `makeUninstallHandler`; parses one required `<plugin>@<marketplace>` positional via `splitPluginMarketplaceRef` + optional `--scope`; routes parse failures (missing positional / invalid `@` form / unknown long flag / bad `--scope` value) through `notifyUsageError`; delegates to `getPluginInfo`"
      exports:
        - "makePluginInfoHandler"
    - path: "extensions/pi-claude-marketplace/edge/router.ts"
      provides: "`TOP_LEVEL_SUBCOMMANDS` tuple extended with `\"info\"` (BEFORE `\"marketplace\"`); `TOP_LEVEL_USAGE` extended with the `info <plugin>@<marketplace> [--scope user|project]` usage line (placed AFTER the `list` row); `SubcommandHandlers` interface gains a `pluginInfo: (args, ctx) => Promise<void>` member (named `pluginInfo` to disambiguate from `marketplaceInfo` per the existing `marketplaceList` / `list` naming pattern); `routeClaudePlugin` switch gains an `\"info\"` case dispatching to the new handler"
    - path: "extensions/pi-claude-marketplace/edge/register.ts"
      provides: "`registerClaudePluginCommand` wires `pluginInfo: makePluginInfoHandler(pi)` into the `handlers` record (alongside the other top-level handlers, BEFORE the `marketplace*` block)"
    - path: "extensions/pi-claude-marketplace/edge/completions/provider.ts"
      provides: "TC-6 dispatcher's `pluginRefBranchConfig` extended with an `\"info\"` arm returning `{ mode: 'info', allowMarketplaceOnly: false, ...(explicitScope !== undefined && { targetScope: explicitScope }) }`; `PluginRefMode` type alias widened to include `\"info\"`; the existing dispatch `if (pluginRefConfig !== null && positionals.length === 1)` arm fires for the new mode unchanged"
      contains: "\"info\""
    - path: "extensions/pi-claude-marketplace/edge/completions/data.ts"
      provides: "`PluginRefCompletionMode` widened to include `\"info\"`; new internal helper `getInfoPluginToMarketplacesMap(resolver, explicitScope?)` walking BOTH scopes' marketplaces and YIELDING every (plugin, marketplace) row regardless of `row.status` (no install-state exclusion -- info surfaces installed + available + unavailable in one union); `getPluginToMarketplacesMap` extended with a leading `if (mode === 'info') return getInfoPluginToMarketplacesMap(...)` branch BEFORE the install-mode branch"
    - path: "tests/architecture/notify-types.test.ts"
      provides: "Compile-time proofs for the new `PluginInfoCascadeMessage` variant: `Extract<NotificationMessage, { kind: \"plugin-info-cascade\" }>` non-`never` proof (new `_l10` export const) + a shape proof asserting `blocks: readonly PluginInfoMessage[]` (new `_l10a` export const) + a 5-arm union-arity proof confirming all four info-kind extractions AND the cascade-via-marketplaces narrow (new `_l10b` export const). Naming continues the Phase 43 `_l9*` sequence -- use `_l10*` since `_l9*` is taken."
      contains: "plugin-info-cascade"
    - path: "tests/shared/notify-v2.test.ts"
      provides: "Per-status byte tests for the new `PluginInfoCascadeMessage` (mirrors Phase 43's `MarketplaceInfoCascadeMessage` test set): empty-blocks (`\"\"`), single-block (byte-identical to `renderPluginInfo` for the lone block), 2-block fan-out (project-first body, `\\n\\n` separator), severity always info (no failure expressible -- the orchestrator routes `{not added}` through `PluginInfoMessage` instead), one installed-with-resolved-components fan-out arm, one components-not-resolved fan-out arm (INFO-05). MINIMUM 6 new tests."
    - path: "tests/orchestrators/plugin/info.test.ts"
      provides: "Orchestrator integration tests with hermetic HOME + tmp cwd + `saveState` fixtures + on-disk path-source marketplace dirs carrying a real `plugin.json` (so `resolveStrict` returns componentPaths): (a) single-scope installed with resolved components + description, (b) single-scope available with description, (c) single-scope unavailable with `{hooks}` reason (mirrors list.ts narrowResolverNotes), (d) single-scope with external source (`url`/`npm`/`git-subdir`) -> componentsResolved: false marker, (e) both-scopes fan-out (project-first ordering), (f) `--scope` mismatch -> Phase 42 INFO-04 `{not added}` row with `[scope]` bracket + severity error, (g) missing-plugin-in-known-marketplace -> `(failed) {not in manifest}` row at 2-space indent under marketplace header + severity error, (h) NFR-5 import discipline: orchestrator does not import `platform/git` / `DEFAULT_GIT_OPS` / `refreshGitHubClone`, (i) component list sort ordering: per-kind arrays delivered to the renderer are alphabetically sorted (PR-5 precondition validated at orchestrator boundary -- the renderer does NOT defensively sort), (j) dependencies field: when the manifest entry declares `dependencies` (opaque per PI-13), the orchestrator surfaces them as `dependencies: <plugin>@<marketplace>, ...` lastline. MINIMUM 10 tests."
    - path: "tests/edge/handlers/plugin/info.test.ts"
      provides: "Thin-shim handler tests mirroring `tests/edge/handlers/plugin/install.test.ts`: (a) no positional emits usage error via `notifyUsageError`, (b) `info foo@mp` delegates with `scope: undefined`, (c) `info foo@mp --scope user` delegates with `scope: 'user'`, (d) `info foo@mp --scope project` delegates with `scope: 'project'`, (e) bad `--scope` value routes through `notifyUsageError`, (f) malformed `<plugin>@<marketplace>` ref (no `@`, leading `@`, trailing `@`) routes through `notifyUsageError`, (g) unknown long flag routes through `notifyUsageError`. MINIMUM 7 tests."
    - path: "tests/edge/completions/provider.test.ts"
      provides: "Three new TC-6 `info`-mode tests + one TC-1 test extension. (a) `info <TAB>` returns the union of installed + available + unavailable plugin-refs across BOTH scopes (the TC-1 keyword completion fires first for the bare `info` slot, so the test exercises `info ` with a trailing space to trigger the TC-6 branch with `positionalHead === 'info'`); (b) `info foo@<TAB>` narrows to marketplaces carrying `foo`; (c) `info --scope project <TAB>` returns the SAME union (filter does not narrow set). Extension: the existing TC-1 top-level keyword test (`tokens.length === 0`) is amended to include `\"info\"` in the expected sorted set."
    - path: "tests/edge/router.test.ts"
      provides: "Extend the existing `makeHandlers` factory's `SubcommandHandlers` literal with `pluginInfo: mk(\"pluginInfo\")` (required by the type extension -- otherwise TS2741); add two new AP-3 dispatch assertions: (a) `routeClaudePlugin(\"info foo@mp\", handlers, ctx)` invokes `handlers.pluginInfo(\"foo@mp\", ctx)` exactly once; (b) `TOP_LEVEL_SUBCOMMANDS` includes `\"info\"` and `TOP_LEVEL_USAGE` substring contains `info <plugin>@<marketplace>`. This file is enumerated up-front (avoids the Phase 43 IN-04 oversight where it was added during execution due to the SubcommandHandlers type extension forcing the touch)."
  key_links:
    - from: "extensions/pi-claude-marketplace/edge/router.ts"
      to: "extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts"
      via: "`TOP_LEVEL_SUBCOMMANDS` tuple `\"info\"` entry + `routeClaudePlugin` case routes to `handlers.pluginInfo`"
      pattern: "case \"info\":\\s*\\n\\s*return handlers\\.pluginInfo"
    - from: "extensions/pi-claude-marketplace/edge/register.ts"
      to: "extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts"
      via: "`makePluginInfoHandler(pi)` factory wired into `handlers` record at the `pluginInfo:` key"
      pattern: "pluginInfo: makePluginInfoHandler"
    - from: "extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts"
      via: "`getPluginInfo` import + delegation after `splitPluginMarketplaceRef` + `parseArgs` success"
      pattern: "getPluginInfo\\("
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts"
      via: "Constructs `PluginInfoMessage` (single-scope), `PluginInfoCascadeMessage` (both-scopes), or `PluginInfoMessage` `{not added}` (Phase 42 carve-out) and routes through the single public `notify()` entry"
      pattern: "notify\\(opts\\.ctx, opts\\.pi"
    - from: "extensions/pi-claude-marketplace/edge/completions/provider.ts"
      to: "extensions/pi-claude-marketplace/edge/completions/data.ts"
      via: "`PluginRefMode` `\"info\"` value routes through `getPluginRefCompletions(\"info\", ...)` -> `getPluginToMarketplacesMap(\"info\", ...)` -> `getInfoPluginToMarketplacesMap(...)`"
      pattern: "case \"info\":"
---

<objective>
Wire `/claude:plugin info <plugin>@<marketplace> [--scope user|project]` end-to-end on top of the Phase 42 type model + Phase 43 fan-out pattern: extend `NotificationMessage` with a new `PluginInfoCascadeMessage` fan-out variant (INFO-03 multi-scope rendering for plugin info, parallel to `MarketplaceInfoCascadeMessage`); ship a read-only `getPluginInfo` orchestrator that projects local state + on-disk manifest resolution into the Phase 42 `PluginInfoMessage` shape (NFR-5 preserved -- NO network); ship the edge handler shim mirroring `makeInstallHandler` / `makeUninstallHandler`; register `info` as a top-level subcommand in `router.ts` + `register.ts`; extend the TC-6 completion dispatcher in `provider.ts` so `plugin info <TAB>` surfaces `<plugin>@<marketplace>` combos for installed + available + unavailable plugins across both scopes. Closes INFO-02 (plugin-info byte form + per-scope rendering + completion plumbing reuse) and INFO-05 (`components: not resolved` marker for external sources). INFO-07 catalog states + UAT fixtures land in Plan 44-02 once the production surface is wired and reachable.

Purpose: Phase 42 landed the `PluginInfoMessage` variant + `renderPluginInfo` arm + `wrapDescription` helper + `componentsResolved: true|false` discriminator but no plugin-info command surface emits them yet. Phase 43 wired `marketplace info` end-to-end and established the cascade-via-separate-variant pattern (`MarketplaceInfoCascadeMessage`). This plan is the production-code lift that makes `plugin info` user-reachable; Plan 44-02's catalog UAT then locks every byte form against the implementation that this plan ships. The two plans are tightly coupled and ship in successive waves so the catalog fixtures can drive against working code. Closes v1.8.

Output: The new orchestrator + edge handler + completion extension + the `PluginInfoCascadeMessage` type model addition with per-status byte tests + orchestrator/handler/completion/router integration tests, all behind a GREEN `npm run check`. No catalog UAT additions in this plan (Plan 44-02 owns them after the surface is reachable).
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-plugin-info-command/44-CONTEXT.md
@.planning/phases/43-marketplace-info-command/43-01-PLAN.md
@.planning/phases/43-marketplace-info-command/43-02-PLAN.md
@.planning/phases/43-marketplace-info-command/43-VERIFICATION.md
@.planning/phases/43-marketplace-info-command/43-REVIEW.md
@.planning/phases/42-type-model-render-seam-foundations/42-RESEARCH.md
@.planning/phases/42-type-model-render-seam-foundations/42-VERIFICATION.md
@./CLAUDE.md
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/index.ts
@extensions/pi-claude-marketplace/edge/handlers/marketplace/info.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts
@extensions/pi-claude-marketplace/edge/router.ts
@extensions/pi-claude-marketplace/edge/register.ts
@extensions/pi-claude-marketplace/edge/completions/provider.ts
@extensions/pi-claude-marketplace/edge/completions/data.ts
@extensions/pi-claude-marketplace/edge/args-schema.ts
@extensions/pi-claude-marketplace/edge/args.ts
@extensions/pi-claude-marketplace/persistence/state-io.ts
@extensions/pi-claude-marketplace/persistence/locations.ts
@extensions/pi-claude-marketplace/domain/manifest.ts
@extensions/pi-claude-marketplace/domain/resolver.ts
@extensions/pi-claude-marketplace/domain/source.ts
@extensions/pi-claude-marketplace/shared/errors.ts
@tests/architecture/notify-types.test.ts
@tests/shared/notify-v2.test.ts
@tests/orchestrators/marketplace/info.test.ts
@tests/orchestrators/plugin/list.test.ts
@tests/edge/handlers/marketplace/info.test.ts
@tests/edge/handlers/plugin/install.test.ts
@tests/edge/completions/provider.test.ts
@tests/edge/router.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend notify.ts with `PluginInfoCascadeMessage` fan-out variant + compile-time proofs + per-status byte tests</name>
  <files>
    extensions/pi-claude-marketplace/shared/notify.ts,
    tests/architecture/notify-types.test.ts,
    tests/shared/notify-v2.test.ts
  </files>
  <read_first>
    extensions/pi-claude-marketplace/shared/notify.ts,
    tests/architecture/notify-types.test.ts,
    tests/shared/notify-v2.test.ts,
    .planning/phases/43-marketplace-info-command/43-01-PLAN.md,
    .planning/phases/42-type-model-render-seam-foundations/42-VERIFICATION.md
  </read_first>
  <behavior>
    - Test (notify-types: extract): `Extract<NotificationMessage, { kind: "plugin-info-cascade" }>` resolves to a non-`never` type whose shape carries `readonly kind: "plugin-info-cascade"` and `readonly blocks: readonly PluginInfoMessage[]`. Add an `_l10` proof in the existing `_l*` length-lock + variant-shape pattern (Phase 43 used `_l9`/`_l9a`/`_l9b` for marketplace-info-cascade -- continue at `_l10`).
    - Test (notify-types: union arity): a new sibling proof `_l10b` confirms the union now exposes ALL FIVE variants (`Extract` for `"cascade"` via the existing `marketplaces` structural narrow + the FOUR info arms via `kind` discriminator: `"marketplace-info"`, `"plugin-info"`, `"marketplace-info-cascade"`, `"plugin-info-cascade"`).
    - Test (notify-types: shape): a `_l10a` proof using a `_PluginInfoCascadeExpected` type literal that bidirectionally extends `PluginInfoCascadeMessage` (mirror Phase 43's `_l9a` pattern).
    - Test (notify-v2: empty blocks): a `PluginInfoCascadeMessage` with `blocks: []` renders the empty string `""` (no sentinel -- plugin-info is per-name, not a list; the orchestrator MUST NOT construct an empty fan-out for the user-facing path, but the renderer keeps the edge case deterministic).
    - Test (notify-v2: single-block fan-out): a `PluginInfoCascadeMessage` with `blocks: [<installed single-scope>]` renders byte-identical to what `renderPluginInfo` would emit for that single block alone (no extra blank line, no header decoration).
    - Test (notify-v2: 2-block fan-out project-first): a `PluginInfoCascadeMessage` with `blocks: [<project installed>, <user installed>]` renders `<project body>\n\n<user body>` (single blank line separator; mirrors the cascade `composeMarketplaceBlock` `\n\n` join AND Phase 43's `MarketplaceInfoCascadeMessage` join).
    - Test (notify-v2: severity always info): `notify(ctx, pi, { kind: "plugin-info-cascade", blocks: [...] })` calls `ctx.ui.notify(body)` with ONE argument (no severity); reload-hint NEVER fires (read-only info surface, same as the three sibling info arms).
    - Test (notify-v2: installed with resolved components): single-block fan-out where the lone `PluginInfoMessage` carries `componentsResolved: true` + `components: { agents: ["a1", "a2"], commands: ["c1"], skills: ["s1"] }` + `dependencies: ["dep@mp"]` renders the standard install-cascade form (mp header + 2-space-indent plugin row + 4-space-indent per-kind component lines in `agents, commands, mcp, skills` order + 4-space-indent `dependencies:` line). Covers INFO-02 happy path through the new fan-out wrapper (the single-block case is the SAME byte form as the bare `plugin-info` variant).
    - Test (notify-v2: components-not-resolved): single-block fan-out with `componentsResolved: false` renders `    components: not resolved` at 4-space indent in place of the per-kind component lines. Covers INFO-05 through the new fan-out wrapper.
  </behavior>
  <action>
    Pre-flight: re-confirm Phase 42 landed `PluginInfoMessage` at `extensions/pi-claude-marketplace/shared/notify.ts:673-679` with `PluginInfoRow` at lines 690-714 carrying the `PluginInfoComponentsResolved | PluginInfoComponentsUnresolved` arms at lines 728-748. Re-confirm Phase 43 landed `MarketplaceInfoCascadeMessage` at lines 766-769 + `renderMarketplaceInfoCascade` at lines 1835-1840 + `dispatchInfoMessage` at lines 2048-2078. If any of these shapes differ from the cited line ranges, do NOT silently ignore the drift; reconcile against the actual landed shape and document the deviation in the SUMMARY.

    TYPE MODEL EXTENSION in `extensions/pi-claude-marketplace/shared/notify.ts`:

    1. Add a new exported interface immediately after `MarketplaceInfoCascadeMessage` (around line 770):
       - Name: `PluginInfoCascadeMessage`.
       - Shape: `readonly kind: "plugin-info-cascade"` + `readonly blocks: readonly PluginInfoMessage[]`.
       - JSDoc: cite Phase 44 / INFO-02 + INFO-03 fan-out; explain iteration order is the orchestrator's responsibility (project-first per MSG-GR-3 / INFO-03); reload-hint NEVER fires; severity is always info (no failure can be expressed on a fan-out payload -- the orchestrator routes the INFO-04 `{not added}` failure surface through the sibling `PluginInfoMessage` variant); empty `blocks` renders to the empty string. Mirror the JSDoc structure used for `MarketplaceInfoCascadeMessage`.

    2. Extend the exported `NotificationMessage` union (line 781-785) to include the new variant as the 5th arm, preserving the existing 4 arms in order:
       cascade | marketplace-info | plugin-info | marketplace-info-cascade | plugin-info-cascade.

    3. Add a new file-private function `renderPluginInfoCascade(message: PluginInfoCascadeMessage, probe: SoftDepStatus): string` immediately after `renderMarketplaceInfoCascade` (around line 1840). Implementation: `return message.blocks.map((b) => renderPluginInfo(b, probe)).join("\n\n");`. JSDoc cites INFO-02/03 + the `\n\n` join precedent set by `renderMarketplaceInfoCascade`. DO NOT modify `renderPluginInfo`.

    4. Extend the `dispatchInfoMessage` helper switch (around line 2057-2069) with a NEW `case "plugin-info-cascade"` arm BEFORE the `default: assertNever(message)` arm:
       ```
       case "plugin-info-cascade":
         body = renderPluginInfoCascade(message, probe);
         break;
       ```
       The helper signature MUST widen its `message` parameter union to include `PluginInfoCascadeMessage` (current signature is `MarketplaceInfoMessage | PluginInfoMessage | MarketplaceInfoCascadeMessage` per Phase 43 -- add `| PluginInfoCascadeMessage`). The `assertNever(message)` exhaustiveness gate will then narrow correctly.

    5. Extend the `notify()` dispatcher's info short-circuit predicate (lines 2105-2109) to include the new kind:
       ```
       if (
         message.kind === "marketplace-info" ||
         message.kind === "plugin-info" ||
         message.kind === "marketplace-info-cascade" ||
         message.kind === "plugin-info-cascade"
       ) {
         dispatchInfoMessage(ctx, message, probe);
         return;
       }
       ```
       The trailing switch on `message.kind` (lines 2128-2136) then narrows to `CascadeNotificationMessage` correctly -- the `case undefined:` / `case "cascade":` arms remain unchanged.

    6. Extend `computeSeverity` (line 1402-1418): the FIRST predicate (line 1412) already short-circuits on `marketplace-info` + `marketplace-info-cascade`; ADD `plugin-info-cascade` to that predicate (which routes to `undefined` / info -- no failure can be expressed on a plugin-info fan-out wrapper; the orchestrator routes `{not added}` through the sibling `PluginInfoMessage` instead). The existing `plugin-info` arm (line 1416-1418) stays unchanged.

    7. Extend `shouldEmitReloadHint` (line 1601): the existing info-kind short-circuit (around lines 1602-1610) MUST also short-circuit on `plugin-info-cascade` -- read-only info surface NEVER triggers reload.

    8. Extend `buildSummaryLine` (line 1541-1574): the existing defensive short-circuit (lines 1549-1555) MUST also short-circuit on `plugin-info-cascade` returning `""` (mirrors the existing three info-kind short-circuits; the `notify()` dispatcher does not invoke `buildSummaryLine` for info kinds, but the defensive guard mirrors the existing pattern and keeps `buildSummaryLine` exhaustive).

    9. Verify the 5-arm `notify()` dispatcher's `assertNever(message)` default arm still narrows correctly: after the four explicit info arms return, TS narrows `message` to `CascadeNotificationMessage`; the cascade body runs; any future 6th variant would compile-error at `assertNever`.

    COMPILE-TIME PROOFS in `tests/architecture/notify-types.test.ts`:

    10. Add a `_l10` block immediately after Phase 43's `_l9*` proofs (search for `_l9` to find the right neighborhood):
        - `type _Assert_PluginInfoCascadeKind = Extract<NotificationMessage, { kind: "plugin-info-cascade" }> extends never ? never : true;`
        - `export const _l10: _Assert_PluginInfoCascadeKind = true;`

    11. Add a shape proof asserting `PluginInfoCascadeMessage.blocks` is exactly `readonly PluginInfoMessage[]` (use a `_PluginInfoCascadeExpected` type literal mirroring Phase 43's `_MarketplaceInfoCascadeExpected` proof). Export as `_l10a`.

    12. Add a 5-arm union-arity proof `_l10b` confirming all five `NotificationMessage` variants are reachable. Mirror Phase 43's `_l9b` structure.

    PER-STATUS UNIT TESTS in `tests/shared/notify-v2.test.ts`:

    13. Add tests near the existing Phase 43 `MarketplaceInfoCascadeMessage` tests (search for `kind: "marketplace-info-cascade"` to find the right neighborhood). Mirror the existing `makeCtx()` + `piWithBothLoaded()` helpers; do NOT introduce new scaffolding.

    14. Seven tests minimum (one per behavior bullet above): empty blocks, single block, 2-block fan-out (project-first), severity always info, installed-with-resolved-components, components-not-resolved fan-out arm. Lock byte form via `assert.equal(callArgs[0], expected)` and severity via `assert.equal(callArgs.length, 1)`.

    DO NOT MODIFY `composeMarketplaceBlock`, `renderMpHeader`, `renderPluginRow`, `composePluginLines`, `joinTokens`, `composeReasons`, `renderVersion`, `renderScopeBracket`, `composeVersionArrow`, `renderMarketplaceInfo`, `renderMarketplaceInfoCascade`, `renderPluginInfo`, `composeMpInfoHeader`, `wrapDescription`, `pluginInfoStatusGlyph`, `appendResolvedComponentLines` (Phase 42 SC#4 + Phase 43 byte-equality is carried forward; the new variant is composed via REUSE of `renderPluginInfo`, not by editing it). Catalog UAT byte-equality MUST remain GREEN -- both the 60+ cascade fixtures AND the 64 catalog states landed through Phase 43.
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern="notify-types|notify-v2|catalog UAT"</automated>
  </verify>
  <acceptance_criteria>
    - Command `grep -c 'kind: "plugin-info-cascade"' extensions/pi-claude-marketplace/shared/notify.ts` returns at least 2 (interface declaration + dispatchInfoMessage switch arm).
    - Command `grep -nE 'export interface PluginInfoCascadeMessage' extensions/pi-claude-marketplace/shared/notify.ts` returns exactly 1 hit.
    - Command `grep -nE 'PluginInfoCascadeMessage' extensions/pi-claude-marketplace/shared/notify.ts | grep -v '^[^:]*://' | grep -cE 'PluginInfoCascadeMessage'` returns at least 4 (interface, union member, dispatcher narrowing in `dispatchInfoMessage` signature, `renderPluginInfoCascade` signature).
    - File `tests/architecture/notify-types.test.ts` contains `export const _l10` and a corresponding `_Assert_PluginInfoCascadeKind` type; the new `_l10a` shape proof references `_PluginInfoCascadeExpected`; the new `_l10b` 5-arm proof references all five variants.
    - File `tests/shared/notify-v2.test.ts` contains at least 6 NEW `test(...)` calls whose body references `kind: "plugin-info-cascade"`.
    - `npm test -- --test-name-pattern="notify-types|notify-v2|catalog UAT"` exits 0; in particular every pre-existing Phase 42 + Phase 43 catalog-UAT fixture (64 states + 60+ cascade fixtures) remains GREEN.
    - `composeMarketplaceBlock`, `renderMpHeader`, `renderPluginRow`, `renderMarketplaceInfo`, `renderMarketplaceInfoCascade`, `renderPluginInfo`, `composeMpInfoHeader`, `wrapDescription`, `pluginInfoStatusGlyph`, `appendResolvedComponentLines` bodies are byte-unchanged (verify via `git diff --stat extensions/pi-claude-marketplace/shared/notify.ts` -- only additions should appear beyond the new interface, new render function, dispatcher widening, and short-circuit predicate extensions).
    - The `notify()` dispatcher contains a SINGLE info-kind short-circuit `if` predicate covering all four info kinds (marketplace-info, plugin-info, marketplace-info-cascade, plugin-info-cascade); the trailing switch on `message.kind` carries the existing `undefined | "cascade"` arms + `assertNever` default arm UNCHANGED.
  </acceptance_criteria>
  <done>
    `PluginInfoCascadeMessage` exists as a 5th discriminated-union arm; per-status byte tests lock the empty / single / 2-block fan-out shapes plus the installed-with-resolved-components + components-not-resolved fan-out byte forms; severity is always info; Phase 42 + Phase 43 byte-equality for 60+ cascade fixtures + 64 catalog states is preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `getPluginInfo` orchestrator + orchestrator tests (read-only, NFR-5 preserved)</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/plugin/info.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/index.ts,
    tests/orchestrators/plugin/info.test.ts
  </files>
  <read_first>
    extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/list.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/install.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/index.ts,
    extensions/pi-claude-marketplace/shared/notify.ts,
    extensions/pi-claude-marketplace/persistence/state-io.ts,
    extensions/pi-claude-marketplace/persistence/locations.ts,
    extensions/pi-claude-marketplace/domain/manifest.ts,
    extensions/pi-claude-marketplace/domain/resolver.ts,
    extensions/pi-claude-marketplace/domain/source.ts,
    tests/orchestrators/marketplace/info.test.ts,
    tests/orchestrators/plugin/list.test.ts
  </read_first>
  <behavior>
    - Test (single-scope installed, with resolved components + description): `saveState` a user-scope marketplace `mp` with `plugins["foo"]` having `resources: { skills: ["s1"], commands: ["c1"], agents: ["a1"], mcpServers: ["m1"] }` + `version: "1.2.3"`; write a real `marketplace.json` on disk under the marketplace dir whose `plugins[]` entry for `foo` carries `description: "Foo plugin"` + `source: "./foo"`; write a real `<marketplaceRoot>/foo/.claude-plugin/plugin.json` describing skills + commands + agents + mcpServers directories so `resolveStrict` returns installable componentPaths. Call `getPluginInfo({ ctx, pi, name: "foo", marketplace: "mp", scope: "user", cwd })`. Assert one `ctx.ui.notify` call. Assert the body contains: header `● mp [user] <no autoupdate>` at column 0, a 2-space-indented `● foo v1.2.3 (installed)` row, a 4-space-indented `Foo plugin` wrapped-description line, then per-kind component lines at 4-space indent in order `agents:` -> `commands:` -> `mcp:` -> `skills:` (the renderer's COMPONENT_KINDS tuple), all sorted alphabetically within each kind. No `dependencies:` line (no dependencies in the manifest entry). No severity arg.
    - Test (single-scope available, with description): same setup but `foo` is in the manifest, NOT installed in state. Assert body has `○ foo v1.0.0 (available)` row (or no version if manifest omits one). Description wrapped at col 4 / 66.
    - Test (single-scope unavailable with `{hooks}` reason): manifest entry declares `hooks: { path: "..." }`; assert body has `⊘ foo v... (unavailable) {hooks}` row at 2-space indent. Reasons sourced via the same `narrowResolverNotes` pattern used by `list.ts:269-297`.
    - Test (components-not-resolved -- external source / INFO-05): manifest entry's `source` field parses as `npm` or `git-subdir` or `url` (NOT `path`); assert body has the 4-space-indented marker line `    components: not resolved` AND NO per-kind component lists. This is the `componentsResolved: false` discriminator arm.
    - Test (both-scopes fan-out, no `--scope`): `saveState` `foo@mp` under BOTH scopes (different versions + descriptions); call with `scope` undefined; assert ONE `ctx.ui.notify` call (single fan-out body); the body equals `<project-block-body>\n\n<user-block-body>` -- project FIRST per MSG-GR-3 / INFO-03. Each block has its OWN marketplace header (header repeated, separated by blank line -- mirrors the install-cascade `composeMarketplaceBlock` `\n\n` join semantics).
    - Test (`--scope` mismatch -- marketplace in project only, requested user): `saveState` `mp` under project scope only; call with `scope: "user"`; assert body equals `⊘ mp [user] (failed) {not added}` + severity `"error"`. Phase 42 INFO-04 carve-out reused -- the orchestrator emits a `PluginInfoMessage` with `plugin.status: "failed"` + `plugin.scope: "user"` + `reasons: ["not added"]` + `componentsResolved: false`. The `marketplace` lookup key in the `{not added}` row is the MARKETPLACE name (`mp`), NOT the plugin name, because the marketplace itself is what's missing (mirrors Phase 43 `marketplace/info.ts:155-186` carve-out where the bare row's `name` is the marketplace name).
    - Test (`--scope` mismatch absent-from-both, no `--scope`): no state saved; call with `scope` undefined; assert body equals `⊘ mp (failed) {not added}` (NO `[scope]` bracket -- the marketplace is in neither scope). Mirrors the `marketplace/info.ts` D-03 acceptance.
    - Test (missing plugin in known marketplace -- `{not in manifest}`): `saveState` `mp` under user scope with a `marketplace.json` whose `plugins[]` does NOT contain `foo`; call with `name: "foo"`, `marketplace: "mp"`, `scope: "user"`. Assert body equals `● mp [user] <no autoupdate>\n  ⊘ foo (failed) {not in manifest}` -- the marketplace header at column 0 + the failed plugin row at 2-space indent + the existing `{not in manifest}` REASON. Severity `"error"`. The REASONS tuple at `shared/notify.ts:79+` already contains `"not in manifest"` (used by update.ts post-Phase 29 / UXG-08); confirm by `grep -n '"not in manifest"' extensions/pi-claude-marketplace/shared/notify.ts` BEFORE writing the test.
    - Test (component list sort precondition / PR-5): construct a manifest entry + plugin.json whose component declarations are NOT sorted (e.g. skills `["zeta", "alpha"]`); assert the orchestrator sorts them before passing into `PluginInfoMessage` (the rendered body has `skills: alpha, zeta`, NOT `skills: zeta, alpha`). The renderer's `appendResolvedComponentLines` JSDoc at notify.ts:1888-1894 explicitly states "the orchestrator pre-sorts; the renderer does NOT" -- this test locks the orchestrator's responsibility.
    - Test (dependencies field): manifest entry declares `dependencies: ["dep1@mp2", "dep2@mp3"]`; assert the body contains a 4-space-indented `dependencies: dep1@mp2, dep2@mp3` line LAST (after all per-kind component lines). PI-13 keeps dependencies opaque; the info surface stringifies them as `<plugin>@<marketplace>` pairs if the manifest carries them in that form -- if the manifest carries opaque objects, fall back to JSON.stringify per element. The exact dependencies-rendering policy is at planner-implementer's discretion within the INFO-02 byte form: the test locks the resulting line presence + sort order (manifest order preserved, OR sorted -- choose one consistent policy and lock it in the test).
    - Test (NFR-5 import discipline): the source file `extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` MUST NOT contain the strings `platform/git`, `DEFAULT_GIT_OPS`, `refreshGitHubClone` (excluding comments). Strip comments before grepping (mirror the `marketplace/info.ts` grep-gate test at `tests/orchestrators/marketplace/info.test.ts` -- comment-stripped to avoid false positives from the file header that documents the prohibition).
    - Test (orchestrator `index.ts` re-export): `import { getPluginInfo } from "../../../extensions/pi-claude-marketplace/orchestrators/plugin/index.ts"` resolves -- the new export is wired into the barrel.
  </behavior>
  <action>
    Create `extensions/pi-claude-marketplace/orchestrators/plugin/info.ts`. Top-of-file comment cites INFO-02 + INFO-03 + INFO-04 (Phase 42 carve-out reuse) + INFO-05 + NFR-5 (no network) + IL-2 (single notify per invocation). Mirror `orchestrators/marketplace/info.ts` structure (header comment, imports, interface, exported async function, file-private helpers, NO `platform/git`/`DEFAULT_GIT_OPS`/`refreshGitHubClone`).

    Per Phase 43 WR-01: the file header comment MUST accurately enumerate the reachable code paths AND the source kinds that reach the orchestrator. Phase 43's WR-01 finding was that the `marketplace/info.ts` header overstated the path-fallback's reach -- avoid the same drift here. Specifically: enumerate ALL `ParsedSource.kind` values per `domain/source.ts:70-76` (`path | github | url | git-subdir | npm | unknown`) AND state which ones produce `componentsResolved: true` vs `false` (path -> resolved when plugin.json reachable; everything else -> unresolved per INFO-05 -- because the `plugin.json` lives at an unsynced external source the orchestrator cannot fetch).

    Per Phase 43 WR-02: the `found.length === 0 | 1 | 2` branch MUST be unambiguous. Either an explicit `if (sole === undefined) throw new Error("unreachable")` guard, OR a destructure-based pattern like `const [sole, ...rest] = found; if (rest.length === 0 && sole !== undefined)`. Do NOT structure the branches so that a defensive guard silently falls through to a different variant (Phase 43's WR-02 hazard).

    Per Phase 43 IN-01: prefer a typed `switch (src.kind)` over `ParsedSource` with `assertNever` exhaustiveness for source-kind dispatch rather than a loose `as { kind?: unknown; ... }` cast. Import `ParsedSource` from `domain/source.ts`.

    IMPORTS (verify on read; adjust if barrel names differ):

    - `loadMarketplaceManifest, type MarketplaceManifest` from `domain/manifest.ts`
    - `resolveStrict, type ResolvedPlugin` from `domain/resolver.ts`
    - `parsePluginSource, type ParsedSource` from `domain/source.ts` (for the entry's `source` field classification per INFO-05)
    - `locationsFor` from `persistence/locations.ts`
    - `loadState, type ExtensionState` from `persistence/state-io.ts`
    - `errorMessage` from `shared/errors.ts`
    - `notify` from `shared/notify.ts`
    - type imports: `ExtensionAPI, ExtensionContext` from `platform/pi-api.ts`; `NotificationMessage, PluginInfoMessage` from `shared/notify.ts`; `Scope` from `shared/types.ts`
    - `assertNever` from `shared/exhaustiveness.ts` (verify exact path by `grep -rn 'export function assertNever' extensions/`)

    INTERFACE:
    ```
    export interface GetPluginInfoOptions {
      readonly ctx: ExtensionContext;
      readonly pi: ExtensionAPI;
      readonly marketplace: string;
      readonly plugin: string;
      readonly scope?: Scope;
      readonly cwd: string;
    }
    ```

    ORCHESTRATOR BODY (logical flow -- planner refactors for readability; behavior is the contract):

    1. Compute candidate scope set: `const scopes: readonly Scope[] = opts.scope === undefined ? ["project", "user"] : [opts.scope];`. Project-first per MSG-GR-3 / INFO-03.

    2. For each candidate scope: `loadState(locationsFor(scope, opts.cwd).extensionRoot)`. Collect `mpRecord = state.marketplaces[opts.marketplace]` and the resolved `(scope, mpRecord)` tuple where `mpRecord !== undefined`.

    3. Branch on the collected marketplaces:

       (a) **Zero marketplaces found** -- the marketplace is not in any of the requested scopes. Construct the Phase 42 INFO-04 `{not added}` carve-out `PluginInfoMessage` with `plugin.name = opts.marketplace` (NOT `opts.plugin` -- the user-facing failure is "the marketplace is not added", not "the plugin doesn't exist"; this mirrors `orchestrators/marketplace/info.ts:155-186` where the bare row's `name` is the marketplace name). `plugin.scope` set if `opts.scope !== undefined`, omitted otherwise (D-03). `reasons: ["not added"]`. `componentsResolved: false`. Single `notify()` call; return.

       (b) **One marketplace found** -- emit a single `PluginInfoMessage` via the `buildBlock` helper below.

       (c) **Two marketplaces found (both scopes)** -- emit a `PluginInfoCascadeMessage` with `blocks: [projectBlock, userBlock]` in project-first order. The new Task-1-landed variant.

    4. **Helper `buildBlock(opts, scope, mpRecord): Promise<PluginInfoMessage>`** (file-private):

       a. Load the marketplace manifest: `const manifest = await loadMarketplaceManifest(mpRecord.manifestPath)` (catch ENOENT / parse errors -> emit a `{failed}` row with the closed-set REASON via `narrowProbeError` pattern from `list.ts:314-316` -- e.g. `unparseable` / `unreadable`).

       b. Find the manifest entry for `opts.plugin`: `const entry = manifest.plugins.find((p) => p.name === opts.plugin)`. If `undefined`: emit the `{not in manifest}` row at 2-space indent under the marketplace header (construct `PluginInfoMessage` with `marketplaceName: opts.marketplace`, `marketplaceScope: scope`, `marketplaceDetails: { autoupdate: mpRecord.autoupdate ?? false }`, `plugin: { status: "failed", name: opts.plugin, reasons: ["not in manifest"], componentsResolved: false }`). The renderer's standard path renders this as `● <mp> [<scope>] <autoupdate-marker>\n  ⊘ <plugin> (failed) {not in manifest}`.

       c. Check the installed bucket: `const installed = mpRecord.plugins[opts.plugin]` may be `undefined`. If defined, status is `"installed"`; if not, run `resolveStrict(entry, { marketplaceRoot: mpRecord.marketplaceRoot })` to compute `"available"` (resolveStrict succeeded + `installable: true`) or `"unavailable"` (resolveStrict succeeded + `installable: false`; reasons via `narrowResolverNotes` from list.ts).

       d. Determine `componentsResolved` -- INFO-05 key gate: parse the entry's `source` field via `parsePluginSource(entry.source as string)` (the entry's source is `Type.Unknown()` per `domain/components/plugin.ts:52` and may be a string OR an object; consult `domain/source.ts:240-298` for the parser contract). Switch on `src.kind`:
          - `"path"`: components ARE resolvable. Build the resolved arm: read `resolveStrict`'s `componentPaths` (skills/commands/agents arrays) AND read the entry's `mcpServers` field (or the standalone plugin.json's `mcpServers`); convert each kind's path list to a sorted array of NAMES (the basename of each path, or the manifest-declared name -- planner discretion within the INFO-02 byte form; the test locks the rendered byte form, not the internal representation). `componentsResolved: true` + `components: { agents: [...], commands: [...], mcp: [...mcpKeys], skills: [...] }` -- all four arrays optional (only emit a key when non-empty per `appendResolvedComponentLines` at notify.ts:1900-1906 which gates on `names.length > 0`).
          - `"github"` | `"url"` | `"git-subdir"` | `"npm"`: the plugin.json lives at an unsynced external source; per INFO-05 + NFR-5 the orchestrator MUST NOT fetch. Build the unresolved arm: `componentsResolved: false`.
          - `"unknown"`: forward-compat tail; default to `componentsResolved: false` (the orchestrator cannot reason about unknown source kinds -- emit the marker line, do not attempt resolution).
          - `default`: `assertNever(src)` (exhaustiveness gate per Phase 43 IN-01).

       e. Build description: `entry.description` (when present, hard-wrapped by the renderer's `wrapDescription(text, 4, 66)` -- the orchestrator passes the raw string, the renderer wraps).

       f. Build dependencies: if `entry.dependencies` is a non-empty array of strings, pass through to the message; if it's an opaque shape (PI-13), stringify each element via JSON.stringify (or skip the field entirely -- choose one policy and document it inline). For the INFO-02 happy path, pass `entry.dependencies as readonly string[]` when type-narrowed.

       g. Compose the `PluginInfoMessage` payload:
          ```
          return {
            kind: "plugin-info",
            marketplaceName: opts.marketplace,
            marketplaceScope: scope,
            marketplaceDetails: { autoupdate: mpRecord.autoupdate ?? false },
            plugin: {
              status,                         // "installed" | "available" | "unavailable" | "failed"
              name: opts.plugin,
              ...(version !== undefined && { version }),
              ...(description !== undefined && { description }),
              ...(reasons !== undefined && { reasons }),
              ...componentsField,             // either { componentsResolved: true, components: {...}, dependencies?: [...] } or { componentsResolved: false }
            },
          };
          ```
          PR-5 PRECONDITION: every component-name array AND the dependencies array MUST be pre-sorted at this construction site (the renderer asserts the input is sorted at notify.ts:1888-1894 / 1900-1906). Sort each array via `[...arr].sort((a, b) => a.localeCompare(b))`.

    5. **Single dispatch site:** exactly ONE `notify(opts.ctx, opts.pi, message)` call per `getPluginInfo` invocation (IL-2; the fan-out wrapper handles multi-scope rendering via `\n\n` join inside `notify`).

    UPDATE `extensions/pi-claude-marketplace/orchestrators/plugin/index.ts`: add `export { getPluginInfo, type GetPluginInfoOptions } from "./info.ts";` alongside the existing exports. Do NOT reorder existing exports. The existing `index.ts` already aggregates `install/uninstall/update/reinstall/list/import/bootstrap` -- add `info` in alphabetical position.

    TESTS in `tests/orchestrators/plugin/info.test.ts`: mirror the structure of `tests/orchestrators/marketplace/info.test.ts` AND `tests/orchestrators/plugin/list.test.ts`. Reuse the `withHermeticHome` + `makeCtx` patterns verbatim. Write at least the 10 tests enumerated in `<behavior>` above. For tests that need an on-disk path-source marketplace, create a real directory tree under the hermetic cwd:
    - `<root>/marketplace.json` with `{ name: "mp", plugins: [{ name: "foo", source: "./foo", description: "Foo plugin" }] }`
    - `<root>/foo/.claude-plugin/plugin.json` describing skills/commands/agents directories AND mcpServers
    - `<root>/foo/skills/<s1>/...`, `<root>/foo/commands/<c1>.md`, etc. -- create empty files / dirs to satisfy `resolveStrict`'s `statKind` probe.

    For the NFR-5 grep-gate test, read the source file with comment-stripping (mirror the marketplace/info.ts gate -- the file header comment WILL document the NFR-5 prohibition, so a naive `/platform\/git/.test(source)` would false-positive). Use the SAME `stripComments` helper that `tests/orchestrators/marketplace/info.test.ts` uses, OR replicate its body (single-line `//` strip + multi-line `/* ... */` strip).
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern="orchestrators/plugin/info\\.test" 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - File `extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` exists and exports `getPluginInfo` + `GetPluginInfoOptions`.
    - Command `grep -E "platform/git|DEFAULT_GIT_OPS|refreshGitHubClone" extensions/pi-claude-marketplace/orchestrators/plugin/info.ts | grep -vE '^\\s*(//|\\*)'` returns NO matches (exit code 1). NFR-5 preserved (comment-stripped).
    - File `extensions/pi-claude-marketplace/orchestrators/plugin/index.ts` re-exports `getPluginInfo` and `GetPluginInfoOptions`.
    - File `tests/orchestrators/plugin/info.test.ts` exists with at least 10 `test(...)` blocks covering the behavior bullets above.
    - `npm test -- --test-name-pattern="orchestrators/plugin/info\\.test"` exits 0; all new orchestrator tests pass.
    - Single-scope installed test asserts byte form: `● mp [user] <no autoupdate>\n  ● foo v1.2.3 (installed)\n    Foo plugin\n    agents: a1\n    commands: c1\n    mcp: m1\n    skills: s1` (sorted alphabetically within each kind; kind order is fixed by COMPONENT_KINDS as `agents, commands, mcp, skills`).
    - Both-scopes test asserts ONE `ctx.ui.notify` call and a body containing the project block FIRST and the user block SECOND, joined by `\n\n`. Both blocks carry their own marketplace header at column 0.
    - `{not added}` --scope-mismatch test asserts severity `"error"` (second arg to `ctx.ui.notify`) AND that the failed row's `name` is the MARKETPLACE name (not the plugin name).
    - `{not in manifest}` test asserts the marketplace header is present at column 0 AND the failed row at 2-space indent carries the `{not in manifest}` REASON brace (existing REASON, used by update.ts post-Phase 29).
    - INFO-05 components-not-resolved test asserts the body contains the literal substring `    components: not resolved` at 4-space indent.
    - PR-5 sort test asserts a `["zeta", "alpha"]`-shaped input emits `skills: alpha, zeta` (sorted) -- locks the orchestrator's responsibility for pre-sort.
    - The branch structure for `found.length === 0 | 1 | 2` is unambiguous (Phase 43 WR-02 fix): either explicit unreachable-throw guard OR destructure-based pattern; no silent fall-through between the single-block and fan-out paths.
    - The source-kind dispatch uses a typed `switch (src.kind)` over `ParsedSource` with `assertNever` exhaustiveness (Phase 43 IN-01 follow-through).
    - The file header comment block accurately enumerates ALL `ParsedSource.kind` values and which ones produce `componentsResolved: true` vs `false` (Phase 43 WR-01 follow-through).
  </acceptance_criteria>
  <done>
    The orchestrator is the sole site that projects local marketplace + plugin state into the Phase 42 `PluginInfoMessage` variant. NFR-5 is structurally enforced via the import grep-gate. All ten behaviors are byte-locked through orchestrator-integration tests against hermetic fixtures. INFO-05 (`components: not resolved`) is driven by the source-kind dispatch and verified end-to-end.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add edge handler + router/register wiring + TC-6 `info` completion mode + handler/completion/router tests</name>
  <files>
    extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts,
    extensions/pi-claude-marketplace/edge/router.ts,
    extensions/pi-claude-marketplace/edge/register.ts,
    extensions/pi-claude-marketplace/edge/completions/provider.ts,
    extensions/pi-claude-marketplace/edge/completions/data.ts,
    tests/edge/handlers/plugin/info.test.ts,
    tests/edge/completions/provider.test.ts,
    tests/edge/router.test.ts
  </files>
  <read_first>
    extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts,
    extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts,
    extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts,
    extensions/pi-claude-marketplace/edge/handlers/marketplace/info.ts,
    extensions/pi-claude-marketplace/edge/router.ts,
    extensions/pi-claude-marketplace/edge/register.ts,
    extensions/pi-claude-marketplace/edge/completions/provider.ts,
    extensions/pi-claude-marketplace/edge/completions/data.ts,
    extensions/pi-claude-marketplace/edge/args.ts,
    extensions/pi-claude-marketplace/edge/args-schema.ts,
    tests/edge/handlers/plugin/install.test.ts,
    tests/edge/handlers/marketplace/info.test.ts,
    tests/edge/completions/provider.test.ts,
    tests/edge/router.test.ts
  </read_first>
  <behavior>
    - Test (shim: missing positional -> usage error): `handler("", ctx)` produces a `notifyUsageError`-shaped notification (body contains the usage string `Usage: /claude:plugin info <plugin>@<marketplace> [--scope user|project]`) with severity `"error"`. The orchestrator is NOT invoked.
    - Test (shim: malformed ref -- no `@`): `handler("foo", ctx)` routes through `notifyUsageError` with `"Invalid <plugin>@<marketplace> ref: \"foo\"."` (mirrors install.ts:78-83); orchestrator NOT invoked.
    - Test (shim: malformed ref -- leading `@`): `handler("@mp", ctx)` routes through `notifyUsageError`; orchestrator NOT invoked.
    - Test (shim: malformed ref -- trailing `@`): `handler("foo@", ctx)` routes through `notifyUsageError`; orchestrator NOT invoked.
    - Test (shim: `info foo@mp` -> orchestrator called with `scope: undefined`): handler delegates to `getPluginInfo` with `marketplace: "mp"`, `plugin: "foo"`, `scope: undefined`.
    - Test (shim: `info foo@mp --scope user` -> orchestrator called with `scope: "user"`): handler delegates.
    - Test (shim: `info foo@mp --scope project` -> orchestrator called with `scope: "project"`): symmetric.
    - Test (shim: bad `--scope` value -> usage error): `handler("info foo@mp --scope bogus", ctx)` routes through `notifyUsageError`; orchestrator NOT invoked.
    - Test (shim: unknown long flag -> usage error): `handler("info foo@mp --bogus", ctx)` routes through `notifyUsageError`; orchestrator NOT invoked.
    - Test (router: `TOP_LEVEL_SUBCOMMANDS` contains `"info"`): import the constant and assert membership.
    - Test (router: `TOP_LEVEL_USAGE` contains `info <plugin>@<marketplace>`): import the constant and assert the substring.
    - Test (router: `routeClaudePlugin("info foo@mp", handlers, ctx)` invokes `handlers.pluginInfo("foo@mp", ctx)` exactly once): mirror existing AP-3 dispatch tests in `tests/edge/router.test.ts`.
    - Test (completion TC-1: `<TAB>` returns top-level keywords INCLUDING `info`): the existing TC-1 test is extended to expect `info` in the sorted set. UPDATE the existing test rather than adding a new one (Phase 43 Rule 1: type-extension-forced test updates are mechanical and symmetric).
    - Test (completion TC-6 info mode: `info <TAB>` returns plugin-refs across both scopes): with `state: { user: { mp: { plugins: { foo: {...}, bar: {...} } } }, project: { mp2: { plugins: { baz: {...} } } } }` AND each marketplace's `marketplace.json` carrying entries for `foo`, `bar`, `baz` PLUS one additional available plugin `qux`, `info ` (with trailing space) returns plugin-refs for ALL of `foo@mp`, `bar@mp`, `baz@mp2`, `qux@mp` (installed + available union; no install-state exclusion).
    - Test (completion TC-6 info mode: `info foo@<TAB>`): narrows to marketplaces carrying `foo`.
    - Test (completion TC-6 info mode: `info --scope project <TAB>`): returns the SAME union as the unfiltered case (the scope filter does NOT narrow the candidate set -- the orchestrator handles scope mismatch via the INFO-04 `{not added}` row at execution time).
  </behavior>
  <action>
    EDGE HANDLER -- create `extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts`. Verbatim mirror of `handlers/plugin/install.ts` modulo:

    - Module header cites `/claude:plugin info <plugin>@<marketplace> [--scope user|project]`.
    - Imports `getPluginInfo` from `../../../orchestrators/plugin/info.ts` (NOT `index.ts` -- the existing handlers import the named module directly per the marketplace/info.ts pattern).
    - `USAGE = "Usage: /claude:plugin info <plugin>@<marketplace> [--scope user|project]"` (NO `--map-model` flag -- info is read-only, no installation semantics).
    - Factory name: `makePluginInfoHandler(pi: ExtensionAPI)`.
    - Parse args via `parseArgs` (mirror install.ts:47-54) -- DO NOT use `parsePositionalsWithFlags` because there are no boolean flags. Inline the unknown-flag check: if any token in `parsed.positional` starts with `--` AND is not a recognized boolean flag (there are none for `info`), emit `notifyUsageError({ message: 'Unknown option: "<token>".', usage: USAGE })`.
    - Validate exactly ONE non-flag positional; on mismatch, emit `notifyUsageError({ message: "info requires exactly one <plugin>@<marketplace> argument.", usage: USAGE })`.
    - Split via `splitPluginMarketplaceRef(positional)` (from `handlers/plugin/shared.ts`); on `undefined`, emit `notifyUsageError({ message: 'Invalid <plugin>@<marketplace> ref: "<positional>".', usage: USAGE })`.
    - Delegate body:
      ```
      await getPluginInfo({
        ctx,
        pi,
        marketplace: ref.marketplace,
        plugin: ref.plugin,
        cwd: ctx.cwd,
        ...(parsed.scope !== undefined && { scope: parsed.scope }),
      });
      ```

    ROUTER -- edit `extensions/pi-claude-marketplace/edge/router.ts`:

    1. Add `"info"` to `TOP_LEVEL_SUBCOMMANDS` (line 52-62). Place AFTER `"import"` and BEFORE `"marketplace"` to keep verbs grouped semantically (info is read-only like `list`; alphabetic placement is acceptable too). Update the `as const` tuple.
    2. Extend `TOP_LEVEL_USAGE` (line 80-89) with a new line `"  info <plugin>@<marketplace> [--scope user|project]"`. Place AFTER the `list` row.
    3. Extend `SubcommandHandlers` (line 31-46) with `pluginInfo: (args: string, ctx: ExtensionCommandContext) => Promise<void>;`. Name MUST be `pluginInfo` (NOT `info`) to disambiguate from `marketplaceInfo` -- mirrors the existing `list` (plugin list) vs `marketplaceList` naming pattern, BUT note that for `list` the type field is unprefixed because there's no nested-vs-top conflict -- since `info` DOES conflict (marketplaceInfo exists), use the prefixed `pluginInfo` form.
    4. Extend `routeClaudePlugin`'s switch (line 132-153) with `case "info": return handlers.pluginInfo(rest, ctx);`. Place AFTER the `case "import":` line.

    REGISTER -- edit `extensions/pi-claude-marketplace/edge/register.ts`:

    1. Import: `import { makePluginInfoHandler } from "./handlers/plugin/info.ts";`.
    2. Add `pluginInfo: makePluginInfoHandler(pi),` to the `handlers` record in `registerClaudePluginCommand` (around line 77-92). Mirror the placement of other top-level handlers; place BEFORE the `marketplace*` block (since this is a top-level handler, NOT a marketplace handler).

    COMPLETION (provider.ts) -- edit `extensions/pi-claude-marketplace/edge/completions/provider.ts`:

    1. Widen the `PluginRefMode` type alias (line 176) to include `"info"`:
       ```
       type PluginRefMode = "install" | "uninstall" | "update" | "reinstall" | "info";
       ```
    2. Extend `pluginRefBranchConfig` (line 184-212) with a new `case "info":` arm:
       ```
       case "info":
         return {
           mode: "info",
           allowMarketplaceOnly: false,  // `info @<mp>` is NOT a valid command -- info requires both halves
           ...(explicitScope !== undefined && { targetScope: explicitScope }),
         };
       ```
       Place AFTER `case "reinstall":` and BEFORE `default: return null;`.
    3. Update the file header comment block (line 18-29) to add `info` to the TC-6 branch enumeration: "TC-6 -- head in {install, uninstall, update, reinstall, info} && tokens.length === 1".

    COMPLETION (data.ts) -- edit `extensions/pi-claude-marketplace/edge/completions/data.ts`:

    1. Widen `PluginRefCompletionMode` (line 37) to include `"info"`:
       ```
       type PluginRefCompletionMode = "install" | "uninstall" | "update" | "reinstall" | "info";
       ```
    2. Add a new internal helper `getInfoPluginToMarketplacesMap(resolver, explicitScope?)` analogous to `getInstalledPluginToMarketplacesMap` (line 313-339) but WITHOUT the `row.status !== "installed"` filter -- info surfaces ALL rows (installed + available + unavailable). The implementation walks both scopes' marketplaces (or the explicit-scope-only set when `explicitScope !== undefined`), iterates each marketplace's plugin index rows via `getPluginIndex`, and adds every plugin name -> marketplace mapping regardless of `row.status`.
    3. Extend `getPluginToMarketplacesMap` (line 346-356) with a leading `if (mode === "info") return getInfoPluginToMarketplacesMap(resolver, options.targetScope);` branch BEFORE the existing `if (mode === "install")` branch.

    HANDLER TESTS in `tests/edge/handlers/plugin/info.test.ts`: mirror `tests/edge/handlers/plugin/install.test.ts` structure. Reuse `withHermeticHome` + `makePi` + `makeCtx` patterns verbatim. Nine tests minimum -- one per `<behavior>` bullet under "shim:".

    COMPLETION TESTS in `tests/edge/completions/provider.test.ts`: append three new `test(...)` blocks AFTER the existing TC-6 tests (search for `TC-6 :: install <here>` or similar headers). Mirror the existing TC-6 test pattern. Always call `__resetCacheForTests()` at the top of each new test (consistent with the existing tests in the file). Also amend the existing TC-1 top-level keyword test to include `"info"` in the expected sorted set (Phase 43 Rule 1 follow-through -- type-extension-forced symmetric update).

    ROUTER TESTS -- edit `tests/edge/router.test.ts`:

    1. Extend the `makeHandlers` factory (around line 43-67 per Phase 43's IN-04) by adding `pluginInfo: mk("pluginInfo"),` alongside the other top-level handlers (e.g., between `list:` and `import:` or wherever fits the existing alphabetical order). Required by the `SubcommandHandlers` type extension -- otherwise TS2741.
    2. Add a new dispatch test: `routeClaudePlugin("info foo@mp", handlers, ctx)` invokes `handlers.pluginInfo` exactly once with `args === "foo@mp"`.
    3. Add a router-constant assertion: `TOP_LEVEL_SUBCOMMANDS` includes `"info"`; `TOP_LEVEL_USAGE` substring contains `info <plugin>@<marketplace>`.

    FINAL GATE: run `npm run check`. The full suite MUST exit 0 with the new tests GREEN and zero regression on the 1419+ pre-existing tests (Phase 43 baseline).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - File `extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts` exists and exports `makePluginInfoHandler`.
    - Command `grep -nE '"info"' extensions/pi-claude-marketplace/edge/router.ts` returns hits both in `TOP_LEVEL_SUBCOMMANDS` and in `routeClaudePlugin`'s switch.
    - Command `grep -nE 'pluginInfo:' extensions/pi-claude-marketplace/edge/register.ts` returns 1 hit (the handler wiring).
    - Command `grep -nE 'pluginInfo' extensions/pi-claude-marketplace/edge/router.ts` returns at least 2 hits (interface member + dispatch case).
    - Command `grep -nE 'case "info":' extensions/pi-claude-marketplace/edge/completions/provider.ts` returns 1 hit (the new pluginRefBranchConfig arm).
    - Command `grep -nE 'getInfoPluginToMarketplacesMap' extensions/pi-claude-marketplace/edge/completions/data.ts` returns at least 2 hits (declaration + call site).
    - File `tests/edge/handlers/plugin/info.test.ts` exists with at least 9 `test(...)` blocks.
    - File `tests/edge/completions/provider.test.ts` contains at least 3 new tests whose names reference `info` mode AND the existing TC-1 test expects `"info"` in the sorted top-level-keyword set.
    - File `tests/edge/router.test.ts`'s `makeHandlers` factory includes `pluginInfo: mk("pluginInfo")` AND there's a new dispatch test for `info foo@mp`.
    - `npm run check` exits 0; no regression on pre-existing tests.
    - The `pluginInfo` handler position in `register.ts` is grouped with other top-level handlers (NOT placed under the `marketplace*` block).
    - The `info` case in `routeClaudePlugin` is positioned grouped with other top-level subcommands (placement near `list` / `import` is recommended for readability; any consistent placement within the top-level switch is acceptable).
    - The `data.ts` `getInfoPluginToMarketplacesMap` helper does NOT filter rows by `status` (verified via test: the union returned includes installed + available + unavailable rows for the same marketplace).
  </acceptance_criteria>
  <done>
    `/claude:plugin info <plugin>@<marketplace> [--scope user|project]` is routable end-to-end from a slash command keystroke: TC-1 surfaces `info` at the top-level prompt; TC-6 surfaces `<plugin>@<marketplace>` combos at `info <TAB>` for installed + available + unavailable plugins across both scopes; the shim delegates to the orchestrator; the orchestrator emits one of (a) `PluginInfoMessage` (single scope), (b) `PluginInfoCascadeMessage` (both scopes), or (c) `PluginInfoMessage` `{not added}` / `{not in manifest}` (failure). The full `npm run check` is GREEN at the post-Wave-1 commit boundary.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> handler | `<plugin>` + `<marketplace>` from the `@`-split positional + `--scope` value flow into `getPluginInfo({ plugin, marketplace, scope })`. The names are used as state.marketplaces / state.marketplaces[mp].plugins lookup keys (no path traversal at this layer; the state-io layer's containment + the resolver's `assertPathInside` are the gate). |
| Disk -> renderer (plugin.json description) | `description` field read from the manifest entry. Could contain newlines / control characters that the renderer's `wrapDescription(text, 4, 66)` collapses via `/\s+/` tokenization (Phase 42 T-42-01 mitigation). Multi-line bodies normalize to single-space-separated text before wrapping. |
| Disk -> renderer (plugin.json components / mcpServers) | `componentPaths` from `resolveStrict` + `mcpServers` keys from the entry/manifest are passed verbatim into the per-kind component arrays. The arrays render comma-joined; pathological names with commas or newlines would break the indent ladder visually but cannot escalate to code execution (Pi's notify is text). Per Phase 43 T-43-01 contract: orchestrators own deeper escaping; this plan ships only the read seam. |
| Network exposure | NONE. NFR-5 is structurally enforced by the orchestrator-source grep-gate test + the absence of `platform/git` / `DEFAULT_GIT_OPS` / `refreshGitHubClone` imports. The orchestrator reads only state.json + on-disk marketplace.json + on-disk plugin.json; even GITHUB-source marketplaces are surfaced from their LOCAL CLONE (the persisted `marketplaceRoot`), never re-fetched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering (display confusion) | description / component-name field in `renderPluginInfo` | accept | INFO-02 description hard-wraps via `wrapDescription` which collapses whitespace via `/\s+/`. Component names (paths from `resolveStrict`) are basenames extracted from path lists; a pathological basename with embedded commas would break the comma-separated rendering. Deeper sanitization is out of scope (RESEARCH Phase 42 T-42-01 contract: orchestrators own deeper escaping). |
| T-44-02 | Information disclosure | absolute paths in the marketplace header's `path:` line (when path-source) AND the plugin's component paths if surfaced anywhere | accept | The plugin-info surface does NOT emit the marketplace's `path: <abs>` line (that's `marketplace-info`'s surface, Phase 43); plugin-info emits only the marketplace header `● <mp> [<scope>] <autoupdate>` + plugin row + components by NAME (not path). No new disclosure surface vs. v1.5 or Phase 43. |
| T-44-03 | Denial of service | `loadMarketplaceManifest` + `resolveStrict` blocking on disk read | mitigate | The orchestrator catches manifest read failures and emits a `(failed)` row with the closed-set REASON (`unreadable` / `unparseable` / `permission denied` / `source missing`) via the `narrowProbeError` pattern from `list.ts:314-316`. A pathological manifest (gigabytes) would still consume memory; treated as out-of-band concern -- the existing list orchestrator has the same exposure. |
| T-44-04 | Tampering (closed-set drift) | `PluginInfoCascadeMessage` discriminated-union extension | mitigate | Adding a 5th `kind` literal to `NotificationMessage` triggers a compile-time error at `notify()`'s `assertNever(message)` default arm + at `dispatchInfoMessage`'s `assertNever` exhaustiveness gate if not handled. Tests `_l10` + `_Assert_PluginInfoCascadeKind` + the 5-arm `_l10b` union-arity proof lock the variant at compile time. Phase 42 + Phase 43 catalog UAT byte-equality for the 60+ cascade + 64 catalog states stays GREEN structurally. |
| T-44-05 | Information disclosure (external sources) | a `componentsResolved: false` marker for `npm` / `url` / `git-subdir` sources DELIBERATELY hides component lists from the user | accept | This is the INFO-05 user-contract: the orchestrator MUST NOT fetch external sources (NFR-5 preserved). The marker line `components: not resolved` is the user-visible signal that the plugin's components live at an unsynced external source. Future enhancement (out of v1.8 per CONTEXT.md Deferred Ideas + REQUIREMENTS Out of Scope) could surface metadata from an offline cache; v1.8 ships the marker. |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | Plan 44-01 installs zero packages. No supply-chain surface introduced. |
</threat_model>

<verification>
Phase-level verification after Plan 44-01 lands:

1. `npm run check` exits 0 (typecheck + ESLint + Prettier + tests).
2. `grep -cE 'kind: "plugin-info-cascade"' extensions/pi-claude-marketplace/shared/notify.ts` returns at least 2.
3. `grep -E '"info"' extensions/pi-claude-marketplace/edge/router.ts` shows membership in `TOP_LEVEL_SUBCOMMANDS` AND in `routeClaudePlugin`'s switch.
4. `grep -E 'pluginInfo:' extensions/pi-claude-marketplace/edge/register.ts` returns 1 hit.
5. `grep -E 'platform/git|DEFAULT_GIT_OPS|refreshGitHubClone' extensions/pi-claude-marketplace/orchestrators/plugin/info.ts | grep -vE '^\s*(//|\*)'` returns no hits (NFR-5, comment-stripped).
6. Phase 42 catalog UAT byte-equality remains GREEN; Phase 43 catalog UAT byte-equality remains GREEN; proven transitively by `npm run check`.
7. Pre-commit hooks GREEN on commit; trufflehog clean (separate run if SKIP-prefixed in a worktree).
8. The new `PluginRefCompletionMode = "install" | "uninstall" | "update" | "reinstall" | "info"` is consumed at both call sites (`provider.ts` `pluginRefBranchConfig` + `data.ts` `getPluginToMarketplacesMap`).
9. Per-row component arrays are pre-sorted by the orchestrator (verified by the PR-5 sort test).
</verification>

<success_criteria>
Plan 44-01 is complete when ALL of the following are true:

1. `PluginInfoCascadeMessage` exists as a 5th `NotificationMessage` union arm; the `notify()` dispatcher routes it via `dispatchInfoMessage` -> `renderPluginInfoCascade(message, probe)` and emits exactly one `ctx.ui.notify` call (IL-2); severity is always info; reload-hint never fires.
2. `getPluginInfo({ ctx, pi, marketplace, plugin, scope?, cwd })` projects local state + on-disk manifest resolution into the Phase 42 `PluginInfoMessage` variant: single `PluginInfoMessage` for one-scope hits, `PluginInfoCascadeMessage` (blocks: [project, user]) for both-scopes fan-out, `PluginInfoMessage` `{not added}` for marketplace-absent (with `plugin.scope` set when a specific scope was requested, undefined when neither scope held the marketplace), `PluginInfoMessage` `{not in manifest}` for missing-plugin-in-known-marketplace, `componentsResolved: false` for plugins whose source kind is non-`path`. NFR-5 preserved: no `platform/git` / `DEFAULT_GIT_OPS` / `refreshGitHubClone` import.
3. `/claude:plugin info <plugin>@<marketplace> [--scope user|project]` is routable from a slash command keystroke: `TOP_LEVEL_SUBCOMMANDS` includes `"info"`, `routeClaudePlugin` dispatches the new case, `register.ts` wires `pluginInfo: makePluginInfoHandler(pi)` into the `handlers` record, `makePluginInfoHandler` is a thin-shim factory mirroring `makeInstallHandler`.
4. TC-6 completion surfaces plugin-refs at `plugin info <TAB>` and `plugin info --scope user <TAB>` -- `PluginRefMode` + `PluginRefCompletionMode` now include `"info"`, `pluginRefBranchConfig` returns the `"info"` arm, `getInfoPluginToMarketplacesMap` builds the union (installed + available + unavailable; no install-state exclusion); completion tests assert union-across-scopes behavior, plugin-half-narrowing behavior, and scope-narrow-no-filter behavior.
5. `npm run check` exits 0; new orchestrator / handler / completion / router / notify-v2 / notify-types tests GREEN; pre-existing Phase 42 + Phase 43 catalog UAT byte-equality preserved.

INFO-02 and INFO-05 are user-observable end-to-end; INFO-07 plugin-info catalog states + UAT fixtures are closed by Plan 44-02.
</success_criteria>

<output>
Create `.planning/phases/44-plugin-info-command/44-01-SUMMARY.md` when done. Summary MUST cite the commit SHA(s) (one or more allowed -- this plan is NOT subject to Phase 42's atomic-supersession constraint since INFO-07 plugin-info catalog states land in Plan 44-02), list the files touched, confirm `npm run check` GREEN, and note any deviations from the recommended implementation (e.g., if a different naming for the new `pluginInfo` SubcommandHandlers field was chosen, or if the dependencies-rendering policy diverged from the manifest-order convention).
</output>
</content>
</invoke>
