---
phase: quick-260522-c80
plan: 01
subsystem: planning-docs
tags: [project-md, requirements-traceability, cmc, d-30, messaging-style-guide]

# Dependency graph
requires:
  - phase: planning-foundation
    provides: REQUIREMENTS.md CMC-01..38 + docs/messaging-style-guide.md §15 + D-30 (PROJECT.md Key Decisions row)
provides:
  - PROJECT.md Active checklist that explicitly lists CMC-09 / CMC-21 / CMC-37 / CMC-38 and clarifies CMC-13 D-13 expansion
  - PROJECT.md Context bullet that flags D-30 supersession and enumerates the five v1.3 ES-5 replacements per style guide §15
affects: [phase-12-messaging-foundations, phase-13-conformance-refactor, phase-14-drift-guard]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Planning-doc human-readable summary mirrors the normative REQUIREMENTS.md CMC-* text plus the locked Key Decisions D-30 supersession"

key-files:
  created: []
  modified:
    - ".planning/PROJECT.md"

key-decisions:
  - "Honor D-30 supersession boundary in PROJECT.md context, not just in the Key Decisions table"
  - "Encode CMC-09/13/21/37/38 as first-class Active checklist items (parenthesised MSG-* IDs for traceability)"
  - "Preserve PRD §6.12 ES-5 historical baseline note while declaring the style guide canonical for v1.3"

patterns-established:
  - "When normative requirements (REQUIREMENTS.md) and Key Decisions (D-* rows) update, sweep PROJECT.md Active checklist + Context bullets in the same commit to prevent drift in downstream phase planning"

requirements-completed: [CMC-09, CMC-13, CMC-21, CMC-37, CMC-38]

# Metrics
duration: ~3min
completed: 2026-05-22
---

# Quick Task 260522-c80: Patch PROJECT.md to close requirements-summary gaps

**Aligned the human-readable PROJECT.md Active checklist and Context bullet with the normative REQUIREMENTS.md CMC-09/13/21/37/38 text and the locked D-30 ES-5 supersession, so future v1.3 phase planning reads a consistent picture across PROJECT.md, REQUIREMENTS.md, and docs/messaging-style-guide.md.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-05-22T12:52:35Z
- **Completed:** 2026-05-22T12:54:58Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Three new Active-checklist items added (CMC-09 upgradable list-only discipline, CMC-37 IL-3 inline disable comment preservation, CMC-38 drift guard test suite as Phase 14 milestone gate)
- Two existing Active-checklist items edited in place (per-scope rendering line now carries CMC-21 adoption; soft-dep markers line now carries CMC-13 D-13 per-row reasons expansion across `list` / `import` / `update` / `reinstall` cascades)
- Context "Stable user-contract strings" bullet rewritten to flag D-30 supersession + style guide §15 / MSG-04 canonical contract, enumerate the five v1.3 replacement strings, and preserve the PRD §6.12 historical baseline note (scope explicitly limited to ES-5; ES-1..ES-4 unchanged)

## Task Commits

Each task was committed atomically:

1. **Task 1: Patch PROJECT.md Active checklist + Context bullet** -- `39f6611` (docs)

**Plan metadata:** _(deferred to orchestrator Step 8 -- quick task per protocol)_

## Files Created/Modified

- `.planning/PROJECT.md` -- Active checklist gains CMC-09 / CMC-37 / CMC-38 items; per-scope rendering and soft-dep markers items edited in place for CMC-21 and CMC-13 D-13; Context "Stable user-contract strings" bullet rewritten to cross-reference D-30 supersession + style guide §15 with five replacement strings and the preserved PRD §6.12 historical baseline note

### What changed where

**Active checklist items added (3):**

| # | Line | Anchor                                                                              |
| - | ---- | ----------------------------------------------------------------------------------- |
| 1 | 56   | `CMC-09 upgradable list-only discipline` (MSG-PL-4)                                 |
| 2 | 67   | `CMC-37 IL-3 inline disable comment preservation` (MSG-LC-2)                        |
| 3 | 70   | `CMC-38 drift guard test suite (Phase 14 milestone gate)` (frontmatter-driven)      |

**Active checklist items edited in place (2):**

| # | Line | Anchor                                                                                                                            |
| - | ---- | --------------------------------------------------------------------------------------------------------------------------------- |
| 1 | 59   | Per-scope rendering line gains `CMC-21 adoption` (orphan project-scoped plugins adopted when project-scope marketplace later added) |
| 2 | 62   | Soft-dep markers line gains `MSG-SD-1..3, CMC-12/13` + per-row `{}` reasons across `list` / `import` / `update` / `reinstall` cascades and `D-13 expansion` reference |

**Context bullet rewritten (1):**

| Line | Anchor                                                                                                          |
| ---- | --------------------------------------------------------------------------------------------------------------- |
| 98   | `superseded by D-30; v1.3 canonical contract is docs/messaging-style-guide.md §15 / MSG-04` + five v1.3 replacement rows + PRD §6.12 historical-baseline note + explicit ES-5-scope-only clarifier |

## Decisions Made

- **Mirror D-30 in the Context bullet, not just the Key Decisions table.** The Context bullet is what phase-planning agents read first when assembling the v1.3 conformance picture; leaving it pointing at PRD §6.12 ES-5 as canonical would silently re-introduce the superseded baseline as the live contract.
- **Use parenthesised MSG-* IDs on new checklist items** to match the existing convention (e.g. `(MSG-PL-4)`, `(MSG-LC-2)`), giving every CMC checklist item a normative cross-reference into the style guide.
- **Preserve PRD §6.12 ES-5 as historical baseline.** Style guide §15 explicitly says the PRD row remains in the PRD as baseline; the canonical contract for v1.3 is the style guide. The rewritten Context bullet states this verbatim and limits the supersession scope to ES-5.

## Deviations from Plan

None -- plan executed exactly as written.

The plan's pre-commit guidance was followed precisely:

- `pre-commit run --files .planning/PROJECT.md` was run before `git commit`.
- All non-TruffleHog hooks passed.
- TruffleHog failed under the worktree sandbox with `failed to read index file: not a directory` (the documented worktree limitation in `CLAUDE.md`). Per CLAUDE.md, the scan was re-run separately from the main checkout (`cd /home/acolomba/pi-claude-marketplace && pre-commit run trufflehog --all-files`) and reported **Passed** -- i.e., the scan itself is clean; only the in-worktree hook spawn is broken.
- The commit was created with `SKIP=trufflehog` prefix, as documented in `CLAUDE.md`. `SKIP=` was not extended to any other hook. `--no-verify` was not used.

## Issues Encountered

- **Worktree base drift:** the worktree's HEAD was at `887c29f` (origin/main) instead of the expected base `b386a7b`. The startup `<worktree_branch_check>` Step 2 corrected this via `git reset --hard b386a7b` after Step 1 confirmed HEAD was on the per-agent branch `worktree-agent-a2191a8fed5267d76` (i.e. the protected-ref guard had already passed). The PLAN file was preserved across the reset because it was committed in `b386a7b`.

## User Setup Required

None -- doc-only edit; no environment, dependency, or external-service changes.

## Verification

Plan's automated verification checks all passed against the post-commit state:

- `grep -n "CMC-09 upgradable list-only discipline" .planning/PROJECT.md` → line 56
- `grep -n "CMC-37 IL-3 inline disable" .planning/PROJECT.md` → line 67
- `grep -n "CMC-38 drift guard test suite" .planning/PROJECT.md` → line 70
- `grep -n "CMC-21 adoption" .planning/PROJECT.md` → line 59
- `grep -n "D-13 expansion" .planning/PROJECT.md` → line 62
- `grep -n "superseded by D-30" .planning/PROJECT.md` → line 98
- `grep -n "requires pi-subagents" .planning/PROJECT.md` → line 99 (plus existing line 30 in milestone target features, untouched)
- `grep -n "requires pi-mcp" .planning/PROJECT.md` → line 100 (plus existing line 30, untouched)
- `grep -n "rollback partial" .planning/PROJECT.md` → line 103
- `grep -n "manual recovery" .planning/PROJECT.md` → line 102
- `grep -n "pick up changes" .planning/PROJECT.md` → line 101
- `git diff --stat HEAD` after commit → empty (tree clean)
- `git log -1 --stat` → single-file commit (`.planning/PROJECT.md`, 1 file changed, 12 insertions, 3 deletions)
- Active block `- [ ]` count is 17 (was 14 pre-edit; net delta +3 matches the three new CMC items; the two in-place edits do not change the count)
- Commit title: 72 chars (at the ≤72 limit); max body line: 69 chars (≤80 limit)

No files outside `.planning/PROJECT.md` were modified. The milestone Goal / spec-inputs / target-features block (lines 13-37), Key Decisions table, Evolution section, Out of Scope section, Constraints section, Validated section, and trailing "Last updated" footer entries were all left untouched.

## Self-Check: PASSED

- File `.planning/PROJECT.md` present and modified at the expected lines.
- Commit `39f6611` present on branch `worktree-agent-a2191a8fed5267d76` (verified via `git log -1 --stat`).
- Working tree clean post-commit; `git status --short` empty.

---
*Quick task: 260522-c80-patch-project-md-to-close-requirements-summary-gaps*
*Completed: 2026-05-22*
