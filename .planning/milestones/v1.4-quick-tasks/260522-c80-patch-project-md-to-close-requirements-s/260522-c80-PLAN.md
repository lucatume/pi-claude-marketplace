---
phase: quick-260522-c80
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements:
  - CMC-09
  - CMC-13
  - CMC-21
  - CMC-37
  - CMC-38

must_haves:
  truths:
    - "PROJECT.md Active checklist explicitly lists CMC-09 (upgradable list-only discipline)"
    - "PROJECT.md Active checklist explicitly lists CMC-37 (IL-3 inline disable comment preservation)"
    - "PROJECT.md Active checklist explicitly lists CMC-38 (drift guard test suite -- milestone gate)"
    - "PROJECT.md per-scope rendering checklist line reflects CMC-21 adoption behavior (orphan plugins adopted when project-scope marketplace later added)"
    - "PROJECT.md soft-dep markers checklist line reflects D-13 expansion: markers also emitted on `list` (available)/(installed) rows AND per-row inside `import`/`update`/`reinstall` cascades; aggregated single-trailer emission replaced by per-row `{}` reasons"
    - "PROJECT.md Context §Stable user-contract strings bullet flags D-30 supersession and lists all five v1.3 replacements per style guide §15"
    - "PRD §6.12 historical baseline note preserved on the same bullet (legacy strings remain in PRD as baseline; style guide is canonical v1.3 contract)"
    - "No content outside PROJECT.md is modified"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Patched Active checklist + Context bullet aligned with REQUIREMENTS.md CMC-09/13/21/37/38 and style guide §15"
      contains: "CMC-09, CMC-21, CMC-37, CMC-38, requires pi-subagents, requires pi-mcp, rollback partial, manual recovery, /reload to pick up changes, D-30"
  key_links:
    - from: ".planning/PROJECT.md Active section"
      to: ".planning/REQUIREMENTS.md CMC-09/13/21/37/38"
      via: "human-readable summary mirrors normative requirement text"
      pattern: "CMC-(09|13|21|37|38)"
    - from: ".planning/PROJECT.md Context §Stable user-contract strings"
      to: ".planning/PROJECT.md Key Decisions §D-30 + docs/messaging-style-guide.md §15"
      via: "supersession cross-reference"
      pattern: "D-30|MSG-04|section 15"
---

<objective>
Patch `.planning/PROJECT.md` to close hygiene gaps surfaced during Phase 12 `/gsd:discuss-phase`. The Active checklist is missing items for CMC-09, CMC-37, and CMC-38; the existing CMC-21 line under-specifies adoption; the existing soft-dep markers line under-specifies CMC-13's D-13 expansion. The Context bullet for stable user-contract strings still cites PRD §6.12 ES-5 as canonical even though D-30 supersedes it.

Purpose: Bring the human-readable PROJECT.md summary back into alignment with the normative REQUIREMENTS.md (CMC-01..38) and the locked D-30 supersession, so future phase planning reads a consistent picture across PROJECT.md, REQUIREMENTS.md, and ROADMAP.md.

Output: One edited file (`.planning/PROJECT.md`) and one atomic Conventional Commits commit.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@docs/messaging-style-guide.md
@CLAUDE.md

<interfaces>
<!-- Key reference anchors for the edit. Pre-extracted so executor needs no additional discovery. -->

## Current PROJECT.md Active section -- "v1.3 Consistent Messaging:" block (lines 52-67)

The block is a flat list of `- [ ]` markdown checklist items. Existing convention starts each item with **`<bold-token>`** prose (e.g. `- [ ] Universal line grammar (MSG-GR-1) applied at every notify callsite ...`). Some existing items already include parenthesised MSG-* IDs; new items SHOULD include the parenthesised normative ID for traceability.

Items that need to be CHANGED IN PLACE (not appended):
- Line 58 (per-scope rendering): "Per-scope marketplace and plugin rendering (MSG-GR-3) with name-primary / scope-secondary sort and the plugin-list orphan fold rule"
- Line 61 (soft-dep markers): "Soft-dep markers (MSG-SD-1..2) emitted on installed / updated / reinstalled rows when companion extensions are unloaded; excluded from uninstalled rows"

Items that need to be ADDED (anywhere in the block; place near semantically related items):
- CMC-09: upgradable list-only discipline (MSG-PL-4)
- CMC-37: IL-3 inline disable comment preservation (MSG-LC-2)
- CMC-38: drift guard test suite as Phase 14 milestone gate

## Current PROJECT.md Context bullet -- "Stable user-contract strings" (line 95)

The bullet currently reads (line 95):
> **Stable user-contract strings (PRD §6.12 ES-5):** `pi-subagents is not loaded; …`, `pi-mcp-adapter is not loaded; …`, `Run /reload to <verb> …`, `MANUAL RECOVERY REQUIRED: …`, `(rollback partial: [<phase>] <msg>; …)`. These cannot drift without a contract break.

This is superseded by D-30 (line 136). Replace with a bullet that:
1. Flags D-30 supersession + style guide §15 replacement table.
2. Lists each legacy string alongside its v1.3 replacement.
3. Preserves the historical baseline note (legacy strings remain in PRD §6.12 as historical baseline; style guide is canonical v1.3 contract).

## Authoritative source: CMC normative text (REQUIREMENTS.md)

CMC-09 (line 373): "(upgradable) is computed per PRD PL-5 string compare and rendered only by list; it MUST NOT appear on install / update / uninstall / reinstall result rows (MSG-PL-4)."

CMC-13 (line 380): "Soft-dep markers fire on installed / updated / reinstalled rows, on list-rendering (available) and (installed) rows, and per-row inside import / update / reinstall cascades whenever (a) the plugin declares the corresponding resource AND (b) the companion extension is unloaded; markers MUST NOT appear on (uninstalled) rows (MSG-SD-1..3). Today's aggregated single-trailer emission is replaced by per-row {} reasons."

CMC-21 (line 400): "Marketplaces render per scope on every surface (no collapse). On the plugin-list surface, orphan project-scoped plugins of a marketplace name fold under a <marketplace> [user] header when no <marketplace> [project] header exists; the plugin row's [<scope>] always reflects the plugin's actual install scope. When a project-scope marketplace is later added, previously-folded project-scoped plugins are ADOPTED by the new <marketplace> [project] header at adoption time."

CMC-37 (line 422): "The eslint discipline at the IL-3 call site is preserved: inline `eslint-disable-next-line no-restricted-syntax, no-console -- IL-3: <rationale>` comment on the line directly above the console.warn(...); no config-file override added. Other console.* calls remain forbidden by no-restricted-syntax + no-console (MSG-LC-2)."

CMC-38 (line 426): "A test suite reads the style-guide frontmatter (status_tokens:, reasons:, markers:, pattern_classes:) plus the normative MSG-* IDs as the binding drift-guard contract; npm run check fails when a callsite emits a token outside the closed sets or violates an MSG-* rule."

## Authoritative source: style guide §15 replacement table (docs/messaging-style-guide.md lines 515-527)

| ES-5 legacy marker                       | v1.3 replacement                                                                                              |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `pi-subagents is not loaded; …`          | `{requires pi-subagents}` reason on the affected line (§6 MSG-SD-1)                                           |
| `pi-mcp-adapter is not loaded; …`        | `{requires pi-mcp}` reason on the affected line (§6 MSG-SD-1)                                                 |
| `Run /reload to <verb> …`                | `/reload to pick up changes` (single canonical trailer, blank line above) (§5 MSG-RH-1)                       |
| `MANUAL RECOVERY REQUIRED: …`            | `⊘ <resource> (manual recovery) {<reason>}` as a separate top-level line (§7 MSG-MR-1 / MSG-MR-2)             |
| `(rollback partial: [<phase>] <msg>; …)` | `{rollback partial}` reason on the failed line + per-phase indented children (§8 MSG-RP-1)                    |

§15 also notes: "The PRD section 6.12 ES-5 row REMAINS in the PRD as historical baseline but is NO LONGER the canonical contract for these five user-facing surfaces -- this guide is."

## Authoritative source: D-30 (PROJECT.md line 136)

D-30 already exists in the Key Decisions table and locks `docs/messaging-style-guide.md` v1.0 + `docs/output-catalog.md` as the v1.3 user contract, superseding PRD §6.12 ES-5. The Context bullet edit should cross-reference D-30, not restate it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch PROJECT.md Active checklist + Context bullet</name>
  <files>.planning/PROJECT.md</files>
  <action>
Make TWO sets of edits to `.planning/PROJECT.md` using the Edit tool. Do NOT touch any other file. Do NOT touch the "Current Milestone" Goal/spec-inputs/target-features section (lines 13-37), the Key Decisions table, the Evolution section, the Out of Scope section, the Constraints section, the Validated section, or the trailing "Last updated" footer entries.

### Edit Set A -- Active checklist (lines ~52-67, "v1.3 Consistent Messaging:" block)

1. **Edit in place: per-scope rendering line** -- replace the existing line:
   `- [ ] Per-scope marketplace and plugin rendering (MSG-GR-3) with name-primary / scope-secondary sort and the plugin-list orphan fold rule`
   with:
   `- [ ] Per-scope marketplace and plugin rendering (MSG-GR-3) with name-primary / scope-secondary sort, the plugin-list orphan fold rule, and CMC-21 adoption: orphan project-scoped plugins fold under user-scope headers and are adopted when a project-scope marketplace is later added`

2. **Edit in place: soft-dep markers line** -- replace the existing line:
   `- [ ] Soft-dep markers (MSG-SD-1..2) emitted on installed / updated / reinstalled rows when companion extensions are unloaded; excluded from uninstalled rows`
   with:
   `- [ ] Soft-dep markers (MSG-SD-1..3, CMC-12/13) emitted per row -- on installed / updated / reinstalled rows, on `list`-rendering `(available)` and `(installed)` rows, and per-row inside `import` / `update` / `reinstall` cascades when companion extensions are unloaded; excluded from `(uninstalled)` rows. The aggregated single-trailer emission is replaced by per-row `{}` reasons (D-13 expansion)`

3. **Add new line: CMC-09 upgradable list-only discipline** -- insert as a new `- [ ] ...` item in the block, placed immediately after the plugin-row icon rule item (current line 55):
   `- [ ] CMC-09 upgradable list-only discipline: `(upgradable)` is computed per PRD PL-5 string compare and rendered ONLY by `list`; never on install / update / uninstall / reinstall result rows (MSG-PL-4)`

4. **Add new line: CMC-37 IL-3 inline disable comment preservation** -- insert as a new `- [ ] ...` item, placed immediately after the existing "Empty-result and legacy-migrate patterns; section 14.1 wording" item (current line 65):
   `- [ ] CMC-37 IL-3 inline disable comment preservation: the `eslint-disable-next-line no-restricted-syntax, no-console -- IL-3: <rationale>` comment stays inline directly above the sanctioned `console.warn`; no config-file rule widening; no second `console.warn` callsite introduced (MSG-LC-2)`

5. **Add new line: CMC-38 drift guard test suite (Phase 14 milestone gate)** -- insert as the FINAL `- [ ] ...` item in the block, after the per-command conformance item (current line 67):
   `- [ ] CMC-38 drift guard test suite (Phase 14 milestone gate): a frontmatter-driven test suite reads `docs/messaging-style-guide.md` YAML frontmatter (`status_tokens:`, `reasons:`, `markers:`, `pattern_classes:`) plus the normative MSG-* IDs as the binding contract; `npm run check` fails when a callsite emits an out-of-set token or violates an MSG-* rule`

Match the existing markdown convention: each item is a single line beginning with `- [ ] ` and ending without a trailing period (existing items omit terminal periods). Preserve backtick formatting on identifiers (`(upgradable)`, `list`, `install`, `console.warn`, etc.) and double-dash (`--`) separators as already used elsewhere in the block.

### Edit Set B -- Context bullet (line 95, "Stable user-contract strings")

Replace the single existing bullet at line 95:
`- **Stable user-contract strings (PRD §6.12 ES-5):** \`pi-subagents is not loaded; …\`, \`pi-mcp-adapter is not loaded; …\`, \`Run /reload to <verb> …\`, \`MANUAL RECOVERY REQUIRED: …\`, \`(rollback partial: [<phase>] <msg>; …)\`. These cannot drift without a contract break.`

with this expanded bullet (still a single Context list item; preserve the surrounding list structure):

```
- **Stable user-contract strings (superseded by D-30; v1.3 canonical contract is `docs/messaging-style-guide.md` §15 / MSG-04):** The five PRD §6.12 ES-5 strings have v1.3 replacements per style guide §15:
  - `pi-subagents is not loaded; …` → `{requires pi-subagents}` reason on the affected line (§6 MSG-SD-1)
  - `pi-mcp-adapter is not loaded; …` → `{requires pi-mcp}` reason on the affected line (§6 MSG-SD-1)
  - `Run /reload to <verb> …` → `/reload to pick up changes` (single canonical trailer, blank line above) (§5 MSG-RH-1)
  - `MANUAL RECOVERY REQUIRED: …` → `⊘ <resource> (manual recovery) {<reason>}` as a separate top-level line (§7 MSG-MR-1 / MSG-MR-2)
  - `(rollback partial: [<phase>] <msg>; …)` → `{rollback partial}` reason on the failed line + per-phase indented children (§8 MSG-RP-1)
  The legacy strings remain in PRD §6.12 as historical baseline but are no longer the canonical contract for these five user-facing surfaces -- the style guide is. ES-1..ES-4 from PRD §6.12 are unchanged; this supersession is scoped strictly to ES-5.
```

Use a 2-space-indented nested bullet list for the five replacements (matches the surrounding Context list nesting convention).

### Pre-commit + commit

After applying both edit sets:

1. Stage only `.planning/PROJECT.md`: `git add .planning/PROJECT.md`
2. Run pre-commit on the staged file: `pre-commit run --files .planning/PROJECT.md` -- fix any formatting failures (Prettier/markdownlint may rewrap or reformat) and re-stage as needed, repeating until clean. Do NOT use `--no-verify`.
3. Commit with a Conventional Commits message. Title MUST be ≥5 and ≤72 chars; body lines MUST be ≤80 chars. Suggested:
   - Title: `docs(planning): close PROJECT.md gaps vs REQUIREMENTS CMC-09/13/21/37/38`
   - Body: brief 2-3 line explanation noting (a) Active checklist now lists CMC-09/CMC-37/CMC-38 and clarifies CMC-13 D-13 expansion + CMC-21 adoption; (b) Context bullet flags D-30 supersession and lists v1.3 replacements per style guide §15.

This task is doc-only and PROJECT.md is not a worktree-introduced file, so the trufflehog `SKIP=` prefix from CLAUDE.md does not apply here unless the working tree is itself a worktree -- check `git rev-parse --is-inside-work-tree` vs `git worktree list` and apply `SKIP=trufflehog` only if running from inside `.worktrees/`.
  </action>
  <verify>
    <automated>grep -n "CMC-09 upgradable list-only discipline" .planning/PROJECT.md && grep -n "CMC-37 IL-3 inline disable" .planning/PROJECT.md && grep -n "CMC-38 drift guard test suite" .planning/PROJECT.md && grep -n "CMC-21 adoption" .planning/PROJECT.md && grep -n "D-13 expansion" .planning/PROJECT.md && grep -n "superseded by D-30" .planning/PROJECT.md && grep -n "requires pi-subagents" .planning/PROJECT.md && grep -n "requires pi-mcp" .planning/PROJECT.md && grep -n "rollback partial" .planning/PROJECT.md && grep -n "manual recovery" .planning/PROJECT.md && grep -n "pick up changes" .planning/PROJECT.md && git diff --stat HEAD -- .planning/PROJECT.md | grep -E "1 file changed" && git diff --name-only HEAD | grep -qxE "\.planning/PROJECT\.md" && [ "$(git diff --name-only HEAD | wc -l)" = "0" ]</automated>
  </verify>
  <done>
    - All five new/changed checklist anchors present in PROJECT.md (`CMC-09 upgradable list-only discipline`, `CMC-37 IL-3 inline disable`, `CMC-38 drift guard test suite`, `CMC-21 adoption`, `D-13 expansion`).
    - Context bullet contains `superseded by D-30`, all five v1.3 replacement tokens (`requires pi-subagents`, `requires pi-mcp`, `pick up changes`, `manual recovery`, `rollback partial`), and the historical-baseline note.
    - Exactly one file changed in the commit (`.planning/PROJECT.md`); `git status` reports clean.
    - `pre-commit run --files .planning/PROJECT.md` passes.
    - Commit message follows Conventional Commits with title ≤72 chars and body lines ≤80 chars.
  </done>
</task>

</tasks>

<verification>
After task completion:

1. `grep -c "^- \[ \] " .planning/PROJECT.md` shows the Active "v1.3 Consistent Messaging:" block has gained exactly 3 new items vs the pre-edit state (CMC-09, CMC-37, CMC-38), while two existing items (per-scope rendering, soft-dep markers) have been edited in place (net `- [ ]` count delta = +3).
2. `git log -1 --stat` shows a single-file commit touching only `.planning/PROJECT.md`.
3. Manual readability check: open PROJECT.md and confirm the Active block reads cleanly and the Context bullet's nested list renders correctly in markdown.
</verification>

<success_criteria>
- PROJECT.md Active section has explicit checklist items for CMC-09, CMC-21 adoption (in-place edit), CMC-37, CMC-38, and CMC-13 D-13 expansion (in-place edit on the soft-dep markers line).
- PROJECT.md Context §Stable user-contract strings flags D-30 supersession and lists all five v1.3 replacements per style guide §15, with the PRD §6.12 historical-baseline note preserved.
- No changes outside PROJECT.md.
- Pre-commit hooks pass; commit follows Conventional Commits format.
- ROADMAP.md, REQUIREMENTS.md, the v1.3 milestone goal text (lines 13-37), Key Decisions table, Evolution section, and Out of Scope section are untouched.
</success_criteria>

<output>
Create `.planning/quick/260522-c80-patch-project-md-to-close-requirements-s/260522-c80-SUMMARY.md` when done summarising:
- Which checklist items were added vs edited in place.
- That the Context bullet now cross-references D-30 + style guide §15.
- Confirmation that no files outside `.planning/PROJECT.md` were modified.
- The commit SHA.
</output>
