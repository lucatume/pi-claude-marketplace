---
phase: quick-260530-7pk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/fold-adoption.test.ts
  - tests/e2e/import-command.test.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/shared/errors.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/types.ts
  - eslint.config.js
  - tests/shared/errors.test.ts
  - extensions/pi-claude-marketplace/transaction/phase-ledger.ts
  - extensions/pi-claude-marketplace/transaction/rollback.ts
  - extensions/pi-claude-marketplace/shared/constants/marketplace-label-probe.ts
  - extensions/pi-claude-marketplace/platform/pi-api.ts
  - extensions/pi-claude-marketplace/shared/markers.ts
  - tests/architecture/scope-order-drift.test.ts
  - tests/architecture/catalog-uat.test.ts
  - tests/shared/notify-v2.test.ts
  - tests/orchestrators/plugin/list.test.ts
  - docs/prd/pi-claude-marketplace-prd.md
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [IL-2, IL-3, AS-7, NFR-4, PU-4, AS-6, MU-5, SNM-01, SNM-09, SNM-10]

must_haves:
  truths:
    - "CI `npm run test:integration` and `PI_CM_E2E_REF=pinned npm run test:e2e` are GREEN against the V2 folded renderer output"
    - "A first-run bootstrap GitHub clone failure surfaces a `ctx.ui.notify` failed-marketplace row, not a raw stack trace (IL-2)"
    - "`marketplace update` and `marketplace autoupdate/noautoupdate` failures surface the underlying error cause, not a bare `(failed)`"
    - "Manual-recovery rows name the leaked file paths in the rendered output (AS-7)"
    - "NFR-4 states Node >= 20.19.0 in PRD, CLAUDE.md, and PROJECT.md, matching package.json engines"
    - "No source/test comment narrates GSD process/phase/decision/version history; requirement-ID citations are retained"
  artifacts:
    - path: "tests/shared/errors.test.ts"
      provides: "causeChainTrailer MAX_DEPTH=5 truncation unit tests"
      contains: "truncated"
  key_links:
    - from: "edge/handlers/plugin/bootstrap.ts"
      to: "shared/notify.ts::notify"
      via: "handler-level catch routes expected failures through the V2 notify path"
      pattern: "catch.*notify"
    - from: "orchestrators/marketplace/update.ts"
      to: "shared/notify.ts::PluginFailedMessage"
      via: "synthetic failed-row carrying cause: err"
      pattern: "cause: err"
---

<objective>
Fix all actionable findings from the PR #22 review on branch `gsd/v1.3-replan-catalog`:
merge-blocking CI test failures, dropped error diagnostics, a closed-set type
tightening, a missing truncation test, a repo-wide comment-rot sweep, the NFR-4
Node floor correction, and a PRD-only PU-4/AS-6 reconciliation.

Purpose: The PR #22 branch deleted the `presentation/` layer and consolidated
rendering into `shared/notify.ts` (the V2 `notify(ctx, pi, NotificationMessage)`
model). Test assertions, comments, and one doc requirement still describe the
pre-refactor world; CI is red on the integration + e2e jobs the final gate
`needs:`.

Output: GREEN `npm run check`, `npm run test:integration`, and
`PI_CM_E2E_REF=pinned npm run test:e2e`; corrected diagnostics, comments, and
NFR-4 docs; one atomic commit per task.

GROUND TRUTH (verified during planning): the V2 renderer output IS the intended
contract -- `tests/architecture/catalog-uat.test.ts` passes byte-for-byte against
`docs/output-catalog.md`. Where a test assertion disagrees with rendered output,
the ASSERTION is stale; reconcile it to the V2 output (the actual rendered bytes
are captured inline in each task below). The extension source lives under
`extensions/pi-claude-marketplace/`; the scope's file:line refs omit that prefix.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@docs/prd/pi-claude-marketplace-prd.md
@docs/output-catalog.md
@docs/messaging-style-guide.md
</context>

<constraints>
- Conventional Commits: title 5-72 chars, body lines <= 80 cols. One atomic
  commit per task. Stay on `gsd/v1.3-replan-catalog`; never commit to main.
- Run `pre-commit run --files <changed files>` before EVERY `git commit`; fix
  failures, restage, re-run until clean. Never `--no-verify`.
- IL-2: all user-visible output flows through the V2 `notify(ctx, pi, ...)` /
  `notifyUsageError` path; no direct `process.stdout`/`process.stderr`. The only
  sanctioned `console.warn` is the load-time migration save failure (IL-3).
- Closed-set grammar model: when touching `shared/notify.ts`, reasons must be
  `Reason` (REASONS members); do not widen to bare `string`.
- Do NOT change dependency versions (`typescript@^6`, `write-file-atomic@^8`/`^7`
  stay as-is). Do NOT restore the PU-4/AS-6 cleanup-leak warning in code/tests.
- Comment policy (verbatim user intent): "make all comments about the code and
  not the history or the phases; it's ok for a comment to reference a
  requirement, but not the gsd process itself." KEEP requirement IDs
  (`NFR-*`, `IL-*`, `PU-*`, `AS-*`, `CMC-*`, `ES-*`, `PI-*`, `SNM-*`, `AG-*`,
  `MSG-*`, `MU-*`, `MA-*`, `MR-*`). REMOVE decision/task/plan/phase/wave refs
  (`D-NN-NN`, `T-NN-NN`, `Plan 14-06`, `Phase 21`, `Wave 1`), version-history
  narration (`V1 did X / V2 does Y`), provenance/tombstones (`GONE`, `REMOVED`,
  `canonicalised here in Phase 21`), and dead refs to deleted modules/symbols.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1 (A1): Reconcile the failing integration + e2e assertions to V2 folded output</name>
  <files>tests/integration/fold-adoption.test.ts, tests/e2e/import-command.test.ts</files>
  <action>
Update stale test assertions to the V2 folded renderer shape (the renderer is
correct; the assertions predate the fold). Exact actual rendered bytes were
captured during planning -- match these.

`tests/integration/fold-adoption.test.ts` ~line 201-205: the install-success
check asserts the flat form `● alpha@official [project] v1.0.0 (installed)`.
Actual V2 output is the folded marketplace header + indented child:
`● official [project]` then `  ● alpha v1.0.0 (installed)` then a blank line then
`/reload to pick up changes`. Reconcile the `installOk` predicate to match the
folded child row (`/  ● alpha v1\.0\.0 \(installed\)/`) AND, if needed, the header
(`/● official \[project\]/`). Keep `n.severity === undefined`.

`tests/e2e/import-command.test.ts` has THREE failing tests (lines 131, 181, 198);
reconcile each to the captured actual output:

- Test "imports enabled Claude settings across both scopes" (~131): the assertion
  expects a `/Claude plugin import summary/` header the V2 renderer no longer
  emits. Child rows fold to 2-space indent with NO `[scope]` bracket and NO
  `@marketplace` suffix. Replace the stale header/flat-row assertions with the
  folded shapes actually emitted, e.g.
  `/● claude-plugins-official \[project\] \(added\)/`,
  `/  ● official-plugin \(installed\)/`,
  `/  ⊘ preinstalled-plugin \(skipped\) \{already installed\}/`,
  `/  ⊘ unavailable-plugin \(unavailable\) \{no longer installable\}/`, and the
  trailing `/\/reload to pick up changes/`.

- Test "--scope project narrows writes to project scope" (~181): expects flat
  `/● local-plugin \[project\] \(installed\)/`. Actual is folded
  `  ● local-plugin (installed)` under the `● directory-marketplace [project]
  (added)` header. Update to `/  ● local-plugin \(installed\)/` (no bracket).

- Test "reports source mismatches and skips dependent plugins" (~198-219): three
  stale assertions. The source-mismatch cascade now renders a FAILED marketplace
  header with a FAILED folded child (NOT skipped, NO `[project]` bracket on the
  child, NO "Existing marketplace source" trailer):
  `⊘ directory-marketplace [project] (failed)` then
  `  ⊘ local-plugin (failed) {source mismatch}`. Replace the three asserts with:
  `/⊘ directory-marketplace \[project\] \(failed\)/` and
  `/  ⊘ local-plugin \(failed\) \{source mismatch\}/`. Drop the
  `/⊘ local-plugin \[project\] \(skipped\) \{source mismatch\}/` and
  `/Existing marketplace source/` asserts (the V2 renderer emits neither).

Prefer anchoring on the canonical shapes the catalog UAT uses. Do NOT touch the
renderer -- the catalog-uat byte-equality gate confirms the output is correct.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run test:integration 2>&1 | grep -E "# (pass|fail)" && PI_CM_E2E_REF=pinned node --test tests/e2e/import-command.test.ts 2>&1 | grep -E "# (pass|fail)"</automated>
  </verify>
  <done>`npm run test:integration` reports 0 fail; the three import-command e2e tests pass. No renderer files changed.</done>
</task>

<task type="auto">
  <name>Task 2 (A2): Restore the bootstrap handler-level catch (IL-2)</name>
  <files>extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts</files>
  <action>
The handler dropped the `try { await bootstrapClaudePlugin(...) } catch` boundary.
`orchestrators/plugin/bootstrap.ts:108-110` rethrows every error except
`MarketplaceDuplicateNameError`, and `addMarketplace`
(`orchestrators/marketplace/add.ts:104,108,135,204,257`) signals failures by
THROWING (it does NOT notify). So a first-run GitHub clone failure currently
escapes as a raw uncaught rejection / stack trace -- an IL-2 violation.

The `main` branch caught this with `notifyError(ctx, errorMessage(err), err)`,
but `notifyError` was removed in the V2 refactor. Route expected failures through
the V2 notify surface instead: wrap the `await bootstrapClaudePlugin(...)` call in
a try/catch; in the catch, emit a marketplace-level failed row via
`notify(ctx, pi, { marketplaces: [{ name: <bootstrap marketplace name>, scope:
"user", status: "failed", plugins: [] }] })`. Use the bootstrap marketplace name
constant exported by `orchestrators/plugin/bootstrap.ts`
(`BOOTSTRAP_MARKETPLACE_NAME`); import `notify` from `shared/notify.ts`. notify()
computes `error` severity for a failed marketplace row (per the closed-set
severity ladder). This mirrors the established marketplace-failure recipe in
`orchestrators/marketplace/autoupdate.ts:153-162` and
`orchestrators/marketplace/update.ts:623-625`.

ALSO correct the now-false inline comment at lines 68-71 (and the header comment
at 15-23) claiming `addMarketplace` "emits V2 failed notifications" -- it throws.
Rewrite the comment to describe the actual behavior: the handler catch routes the
thrown failure through the V2 notify path; strip the `D-18-02` / `D-20-03` /
`Plan 20-03` decision-and-plan narration per the comment policy (a bare `IL-2`
citation is fine).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && grep -q "catch" extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts && grep -q "notify(" extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts && ! grep -qE "D-2[0-9]-|Plan 20|D-18-02" extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts && npm run check 2>&1 | tail -3</automated>
  </verify>
  <done>A bootstrap failure is caught at the handler and emitted via `notify` as a failed marketplace row; no decision/plan IDs remain in the file; `npm run check` exit 0.</done>
</task>

<task type="auto">
  <name>Task 3 (B1+B2): Surface dropped error causes on marketplace update + autoupdate failures</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts</files>
  <action>
B1 -- `orchestrators/marketplace/update.ts` `refreshOneMarketplace` (~607-627) has
a bindingless `catch {}` that discards the `MarketplaceUpdateError` (its `cause`
+ `retryHint`) and emits a bare `(failed)` marketplace header. The
`MarketplaceNotificationMessage` shape has no `cause` field (cause is confined to
plugin-level variants per SNM-10), so surface the cause via the established
synthetic-row pattern: bind the error (`catch (err)`), then emit the failed
marketplace header WITH a synthetic `PluginFailedMessage` child carrying
`cause: err` so the renderer appends the depth-5 cause-chain trailer. Mirror
`orchestrators/plugin/reinstall.ts:354-362` (the `(reinstall)` synthetic-failed
recipe): build a `PluginFailedMessage` with `status: "failed"`, a name
identifying the failure, `reasons` (use `reasonsFromTypedError(err)` if available,
else a permissive closed-set default), and `cause: err instanceof Error ? err :
new Error(errorMessage(err))`. Place it in the marketplace's `plugins: [failedRow]`.
Confirm the exact `notifyDirectFailure` helper signature in
`orchestrators/plugin/update.ts` and the reinstall recipe before writing, to keep
the cause-chain trailer (carrying `MarketplaceUpdateError.retryHint` via its
`.message`/cause) visible to the user. Never leave the catch bindingless.

B2 -- `orchestrators/marketplace/autoupdate.ts` `!shouldCollectNotFound` branch
(~136-164) emits `(failed)` and returns WITHOUT surfacing `err` (notably
`StateLockHeldError`, whose `.message` carries an actionable retry hint). Apply
the SAME synthetic-failed-child pattern: attach a `PluginFailedMessage` carrying
`cause: err` to the failed marketplace row so the cause chain reaches the user.

Rewrite both catch-block comments to describe the cause-surfacing behavior; strip
the `D-18-01`/`D-18-02`/`D-16-08`/`Plan 18-05` narration (keep bare requirement
IDs like `MU-5` / `SNM-10`).

NOTE: do NOT touch the SEPARATE cache-cleanup `catch {}` at update.ts:633-644
(that is the deliberate PU-4/AS-6 post-commit drop handled doc-only in Task 11).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && grep -q "cause: err" extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts && grep -q "cause: err" extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts && npm run check 2>&1 | tail -3</automated>
  </verify>
  <done>Both failure paths bind `err` and attach a synthetic `PluginFailedMessage` with `cause: err`; no bindingless catch remains in the two failure branches; `npm run check` exit 0.</done>
</task>

<task type="auto">
  <name>Task 4 (B3): Name the leaked paths on manual-recovery rows (AS-7)</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/shared/errors.ts, tests/shared/notify-v2.test.ts</files>
  <action>
`ManualRecoveryError.leaks` (`shared/errors.ts:309-314`) carries the leaked file
paths, but the manual-recovery renderer path in `shared/notify.ts` (the
`"manual recovery"` row at ~1044-1054 and the cause-chain trailer composition at
~1237-1242 via `renderIndentedCauseChain`) reads only the underlying-error
trailer -- the leaked paths never reach the user. AS-7 requires the leaked files
be NAMED.

Choose ONE approach (prefer (b) for locality):
  (a) Render `err.leaks` on the manual-recovery row in `shared/notify.ts` (a new
      indented child line listing the leaked paths when the `cause` is a
      `ManualRecoveryError` with non-empty `leaks`); or
  (b) Have `ManualRecoveryError` embed the joined leak list into its `.message`
      (mirroring `appendLeakToError`/`appendLeaks` at `shared/errors.ts:120-137`)
      so the existing depth-5 cause-chain trailer surfaces the paths verbatim.

Whichever approach, confirm the leaked paths appear in the final rendered string
for a manual-recovery row. Add/adjust a test in `tests/shared/notify-v2.test.ts`:
construct a notify payload with a `manual recovery` plugin row whose `cause` is a
`ManualRecoveryError` carrying known leak paths, render it, and assert the leak
paths appear in the output. Keep any comment that cites `AS-7`; strip decision-ID
narration.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/shared/notify-v2.test.ts 2>&1 | grep -E "# (pass|fail)"</automated>
  </verify>
  <done>A manual-recovery row renders the leaked paths from `ManualRecoveryError.leaks`; a new/adjusted notify-v2 test asserts the paths in output and passes.</done>
</task>

<task type="auto">
  <name>Task 5 (C1+C2+C5): Closed-set type tightenings + reason dedup</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, extensions/pi-claude-marketplace/orchestrators/types.ts</files>
  <action>
C1 -- `shared/notify.ts:863` `composeReasons` types its first param
`readonly string[] | undefined`; tighten to `readonly Reason[] | undefined` and
type the internal `composed` accumulator as `Reason[]` (line ~869). The soft-dep
markers it appends (`SOFT_DEP_MARKER_AGENTS`/`SOFT_DEP_MARKER_MCP`) are already
`REASONS` members. All call sites already pass `Reason[]` so this is a no-op at
call sites. Update the JSDoc (lines ~855-861) to describe the closed-set
contract; drop the "cross-variant ergonomics" string-widening rationale.

C2 -- `orchestrators/plugin/install.ts` `narrowResolverReasons` (~1284-1331)
pushes a `Reason` per matching note with no dedup, so a multi-note resolver
failure can emit duplicate reason tokens. Dedup before returning: collect into the
`out` array as today, then return de-duplicated members (e.g. build via a `Set`
and spread, preserving first-seen order) -- keep the empty-array `unsupported
source` fallback intact.

C5(i) -- `shared/notify.ts:484` type `PluginFailedMessage.rollbackPartial[].phase`
as the closed `UpdatePhaseBridge` union (`"skills" | "commands" | "agents" |
"mcp"`) instead of bare `string`, matching producers `orchestrators/types.ts:108`
and `shared/errors.ts:277`. Import/reference the existing union; do not
re-declare it.

C5(ii) -- `orchestrators/types.ts:48-49`: the reinstall `declaresAgents` /
`declaresMcp` flags sit only on the reinstalled arm while a comment claims the
`failed` arm sets them false. Reconcile: either move the flags to a shared
reinstall base (mirroring `PluginUpdateBase`) OR correct the comment to match the
actual type. Prefer the comment correction if moving the flags would ripple into
producers; keep it minimal and type-accurate.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && grep -q "readonly Reason\[\] | undefined" extensions/pi-claude-marketplace/shared/notify.ts && npm run check 2>&1 | tail -3</automated>
  </verify>
  <done>`composeReasons` param/accumulator are `Reason`-typed; `narrowResolverReasons` dedups; `rollbackPartial[].phase` uses the closed union; the types.ts flag/comment mismatch is reconciled; `npm run check` exit 0.</done>
</task>

<task type="auto">
  <name>Task 6 (C3+C4): Narrow migrate.ts ESLint override + add MAX_DEPTH=5 tests</name>
  <files>eslint.config.js, tests/shared/errors.test.ts</files>
  <action>
C3 -- `eslint.config.js:139-151` disables BOTH `no-restricted-syntax` and
`no-console` for `persistence/migrate.ts`. IL-3 sanctions exactly one
`console.warn` at the load-time migration save-failure callsite. Narrow the
override so it only disables the specific rule the single callsite needs: read
the callsite to determine whether it trips `no-console` (the `console.warn`),
`no-restricted-syntax` (a banned selector), or both. Keep the override scoped to
`persistence/migrate.ts` and to ONLY the rule(s) that one line requires; if only
`no-console` is needed, remove the `no-restricted-syntax: off`. Update the
override comment to cite IL-3 (no GSD narration).

C4 -- Add unit tests in `tests/shared/errors.test.ts` for the `MAX_DEPTH = 5`
bound in `causeChainTrailer` (`shared/errors.ts:46-90`):
  (a) a 6-deep cause chain renders 5 links followed by ` (truncated)`;
  (b) an exactly-5-deep chain renders 5 links with NO truncation marker;
  (c) a self-referential cycle (an Error whose `.cause` points back to itself, or
      a 2-node cycle) terminates at the bound without infinite-looping;
  (d) non-Error input returns `""`.
Build the chains with `new Error(msg, { cause })`. Assert on the exact link count
and the presence/absence of `(truncated)`.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/shared/errors.test.ts 2>&1 | grep -E "# (pass|fail)" && npx eslint extensions/pi-claude-marketplace/persistence/migrate.ts 2>&1 | tail -3</automated>
  </verify>
  <done>The migrate.ts override disables only the rule(s) IL-3 needs; four MAX_DEPTH tests added and passing; `npx eslint` clean on migrate.ts.</done>
</task>

<task type="auto">
  <name>Task 7 (D, part 1): Comment-rot sweep -- error/transaction/notify core</name>
  <files>extensions/pi-claude-marketplace/shared/errors.ts, extensions/pi-claude-marketplace/transaction/phase-ledger.ts, extensions/pi-claude-marketplace/transaction/rollback.ts, extensions/pi-claude-marketplace/shared/constants/marketplace-label-probe.ts, extensions/pi-claude-marketplace/platform/pi-api.ts, extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/shared/markers.ts</files>
  <action>
Apply the comment policy: comments describe the CODE now; KEEP requirement IDs;
REMOVE decision/task/plan/phase/wave refs, V1-vs-V2 history narration,
provenance/tombstones, and dead refs to deleted modules/symbols. Verify line
numbers before editing (they drift).

- `shared/errors.ts:38-39,43-44,298,307` -- references to removed `notifyError` /
  `renderManualRecovery`; rewrite to describe the actual `notify()` cause-chain
  consumer. Strip `canonicalised here in Phase 21 from the retired
  presentation/cause-chain.ts` (~100-101).
- `transaction/phase-ledger.ts:47,94-96,119` and `transaction/rollback.ts:21-23`
  -- references to deleted `presentation/rollback-partial.ts` and non-existent
  helpers (`renderRollbackPartial`, `composeRollbackPartialChildren`). Rewrite to
  describe the `PluginFailedMessage.rollbackPartial` -> `notify()` flow.
- `shared/constants/marketplace-label-probe.ts` header (~6-35) -- describes the
  pre-refactor world (`presentation/`, `shared/grammar/`, `SoftDepProbe`).
  Rewrite against current reality: `platform/pi-api.ts::SoftDepStatus` and the
  closed-set tokens in `shared/notify.ts`.
- `platform/pi-api.ts:12-14` -- names retired symbols (`PluginInlineRow`,
  `PluginCascadeRow`, `SoftDepProbe`, `renderRow`); describe the current
  `dependencies: Dependency[]` + `softDepStatus(pi)` mechanism.
- `shared/notify.ts:182` -- says "10-variant" union; it is now 11. Drop the count
  or fix to 11. ALSO strip the D-15-* / `canonicalised here in Phase 21` /
  `Phase 15`/`Phase 16`/`Phase 17`/`Phase 21` narration in the type-model header
  block (~178-198) and the `manual recovery` / cause-chain JSDoc blocks
  (D-16-08, D-16-11, D-16-12, D-22-03, D-17.1-* etc.) throughout the file --
  rewrite each to describe behavior; keep `SNM-*`/`MSG-*`/`AS-*` citations.
- `shared/markers.ts:21-22` and `shared/notify.ts:1311` -- style-guide `§N`
  section refs that no longer exist; reference the guide generally or by named
  section.

Run a grep for `D-16-12` across `extensions/` and fix every straggler comment in
THESE files (other files are covered in Task 8). Do not change code behavior --
comment text only.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && ! grep -nE "presentation/|renderRollbackPartial|composeRollbackPartialChildren|notifyError|SoftDepProbe|10-variant|Phase (15|16|17|21)|D-1[567]-|D-22-" extensions/pi-claude-marketplace/shared/errors.ts extensions/pi-claude-marketplace/transaction/phase-ledger.ts extensions/pi-claude-marketplace/transaction/rollback.ts extensions/pi-claude-marketplace/shared/constants/marketplace-label-probe.ts extensions/pi-claude-marketplace/platform/pi-api.ts extensions/pi-claude-marketplace/shared/notify.ts extensions/pi-claude-marketplace/shared/markers.ts && npm run check 2>&1 | tail -3</automated>
  </verify>
  <done>No dead-symbol/process/phase/decision narration remains in the seven listed files; requirement IDs retained; `npm run check` exit 0 (comment-only changes).</done>
</task>

<task type="auto">
  <name>Task 8 (D, part 2): Comment-rot sweep -- contradictory comments + tests + install ref</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, tests/architecture/scope-order-drift.test.ts, tests/architecture/catalog-uat.test.ts, tests/shared/notify-v2.test.ts, tests/orchestrators/plugin/list.test.ts</files>
  <action>
Highest priority -- CONTRADICTORY comments that mislead about user-contract
behavior: `orchestrators/marketplace/update.ts:17-19,652-655,668-670` and
`orchestrators/marketplace/autoupdate.ts:13,16` claim the `/reload` trailer fires
on `mp.status === "updated"`. The actual `shouldEmitReloadHint`
(`shared/notify.ts:1166-1181`, read during planning) fires ONLY when a PLUGIN row
has status `installed`/`updated`/`reinstalled`/`uninstalled` -- NEVER on a
marketplace status alone. Rewrite these comments to describe the actual
plugin-row-only behavior; remove the `D-16-12` claim. Grep `D-16-12` across
`extensions/` and fix any straggler in these two files (Task 7 handled the others).

Remaining comment-rot sites:
- `orchestrators/plugin/install.ts:45` -- stale `execute.ts:890` line ref (the
  file is ~810 lines). Point at the SYMBOL, not a brittle line number.
- `tests/architecture/scope-order-drift.test.ts:18` -- points at deleted
  `presentation/sort.ts`; the comparator lives in `shared/notify.ts` (the test's
  own allowlist already says so). Fix the comment to reference `shared/notify.ts`.
- `tests/architecture/catalog-uat.test.ts:17` and `tests/shared/notify-v2.test.ts:131`
  -- cite the deleted `tests/shared/notify.test.ts`. Reference the current test
  surface or drop the dead ref.
- `tests/orchestrators/plugin/list.test.ts:5-8` -- references deleted
  `tests/presentation/plugin-list.test.ts` and retired `RowSpec` /
  `PluginListPayload`. Rewrite against the current list-orchestrator surface.

Comment-only changes; no behavior changes. Verify line numbers before editing.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && grep -rn "D-16-12" extensions/ | wc -l | grep -qx 0 && ! grep -nE "presentation/|RowSpec|PluginListPayload|notify\.test\.ts|execute\.ts:890|sort\.ts" extensions/pi-claude-marketplace/orchestrators/plugin/install.ts tests/architecture/scope-order-drift.test.ts tests/architecture/catalog-uat.test.ts tests/shared/notify-v2.test.ts tests/orchestrators/plugin/list.test.ts && npm run check 2>&1 | tail -3</automated>
  </verify>
  <done>`shouldEmitReloadHint` comments describe plugin-row-only behavior; zero `D-16-12` refs remain in `extensions/`; the listed dead refs are gone; `npm run check` exit 0.</done>
</task>

<task type="auto">
  <name>Task 9 (D, part 3): Comment-rot residual sweep across remaining changed files</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/orchestrators/types.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts</files>
  <action>
Sweep the files this plan already touched (Tasks 2, 3, 5) for residual GSD-process
narration that those tasks did not explicitly cover, applying the comment policy.
Targets to scan and rewrite to code-descriptive prose (keep requirement IDs):
- `orchestrators/plugin/reinstall.ts` -- `Plan 19-*` / `D-19-02` / `CONTEXT line
  110` / `Plan 13-02a-02` narration around the synthetic-failure and
  empty-targets blocks (~340-375, ~395-418).
- `orchestrators/plugin/update.ts` -- `CR-01` is a requirement-ish tracking tag;
  KEEP requirement IDs but strip `Plan 19-05` / `D-19-02` / `Phase`-narration in
  the synthetic-failure JSDoc (~167-223, ~348-386).
- `orchestrators/types.ts` -- strip `Plan`/`D-`/`Phase` narration near the
  reinstall arm flags (the comment reconciled in Task 5).
- `edge/handlers/plugin/bootstrap.ts` -- confirm Task 2 left no `D-`/`Plan` refs.

This is a focused residual pass on plan-touched files only; the broader repo-wide
sweep beyond the review-flagged sites is out of scope for this quick. Comment-only;
no behavior change. Verify line numbers before editing.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && ! grep -nE "Plan [0-9]|D-1[0-9]-|D-2[0-9]-|CONTEXT line|Phase 1[0-9]|Phase 2[0-9]" extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts extensions/pi-claude-marketplace/orchestrators/plugin/update.ts extensions/pi-claude-marketplace/orchestrators/types.ts extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts && npm run check 2>&1 | tail -3</automated>
  </verify>
  <done>No `Plan NN`/`D-NN-NN`/`Phase NN`/`CONTEXT line` narration in the four plan-touched files; `npm run check` exit 0.</done>
</task>

<task type="auto">
  <name>Task 10 (E): Correct NFR-4 Node floor to >= 20.19.0</name>
  <files>docs/prd/pi-claude-marketplace-prd.md, CLAUDE.md, .planning/PROJECT.md</files>
  <action>
`package.json` `engines.node` is `>=20.19.0` and is CORRECT. NFR-4 currently
states "Node >= 22" -- the wrong requirement. Update NFR-4 to state Node >= 20.19.0
in all authoritative + propagated locations:
- `docs/prd/pi-claude-marketplace-prd.md:995` -- the NFR-4 row "The extension MUST
  work with Node >= 22." -> "... Node >= 20.19.0."
- `CLAUDE.md` -- the Constraints line "**Runtime:** Node >= 22 (NFR-4)" ->
  "Node >= 20.19.0 (NFR-4)". ALSO the Technology-Stack table rows asserting
  "Node.js >=22" and "NFR-4 mandates Node >=22" -- update the floor and the
  "NFR-4 mandates" prose to >= 20.19.0. Do NOT touch the auto-generated
  "Developer Profile" section.
- `.planning/PROJECT.md:142` ("Tooling baseline on `main`: Node >=22 ...") and
  `:147` ("**Runtime:** Node >= 22 (NFR-4)") -> >= 20.19.0.
Also grep `.planning/REQUIREMENTS.md` and any milestone REQUIREMENTS docs for
NFR-4 / "Node" and update any floor statement there too. Keep it factual about the
requirement; do not narrate the change history.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && ! grep -rnE "Node ≥ ?22|Node >=? ?22|mandates Node ≥22|Node\.js[^|]*>=22" docs/prd/pi-claude-marketplace-prd.md CLAUDE.md .planning/PROJECT.md && grep -q "20.19.0" docs/prd/pi-claude-marketplace-prd.md</automated>
  </verify>
  <done>NFR-4 states Node >= 20.19.0 in PRD, CLAUDE.md (Constraints + Tech-Stack table, NOT Developer Profile), and PROJECT.md; no `Node >= 22` NFR-4 assertions remain in those files.</done>
</task>

<task type="auto">
  <name>Task 11 (F): Amend PRD PU-4/AS-6 to record intentional non-surfacing of post-commit cleanup leaks</name>
  <files>docs/prd/pi-claude-marketplace-prd.md</files>
  <action>
The code deliberately swallows post-commit data-dir/cache cleanup failures
(`orchestrators/plugin/uninstall.ts:262-287`,
`orchestrators/marketplace/remove.ts:99,255`,
`orchestrators/marketplace/update.ts:635-644`); tests assert the drop. PRD PU-4
(line 276) and AS-6 (line 623) still say such leaks MUST surface at `warning`
severity. AMEND the PRD ONLY -- do NOT change code or tests.

Update PU-4 and AS-6 to record that post-commit cleanup-leak warnings are
intentionally NOT surfaced: the cleanup runs AFTER the durable atomic state save,
so a leak cannot corrupt state; surfacing was dropped to reduce notification
noise. Phrase as the requirement as it now stands (factual rationale is fine; no
GSD decision-ID narration in PRD prose). Keep AS-7 (line 624) intact -- it governs
the manual-recovery agent-index path fixed in Task 4, not post-commit cleanup.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && grep -nE "PU-4|AS-6" docs/prd/pi-claude-marketplace-prd.md | head && ! grep -nE "D-[0-9]+-[0-9]+" docs/prd/pi-claude-marketplace-prd.md</automated>
  </verify>
  <done>PU-4 and AS-6 record that post-commit cleanup leaks are intentionally not surfaced (with state-durability rationale); no code/test changes; no decision-ID narration in PRD prose.</done>
</task>

<task type="auto">
  <name>Task 12: Final green-gate verification across all CI jobs</name>
  <files>tests/integration/fold-adoption.test.ts</files>
  <action>
Run the full CI-equivalent gate suite to confirm every job the final gate
`needs:` is GREEN after Tasks 1-11. This task makes no edits unless a regression
surfaces; if a gate is red, fix the minimal cause within the established
constraints (assertion reconciliation to V2 output, never a renderer change
unless output contradicts `docs/output-catalog.md`).

Run, in order:
  1. `npm run check` (typecheck + ESLint + Prettier + unit/architecture tests)
  2. `npm run test:integration`
  3. `PI_CM_E2E_REF=pinned npm run test:e2e`

Confirm all three exit 0. The catalog-uat byte-equality gate inside `npm run
check` must stay GREEN -- if it goes red, a renderer was wrongly modified; revert
that change.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run check && npm run test:integration && PI_CM_E2E_REF=pinned npm run test:e2e</automated>
  </verify>
  <done>`npm run check` exit 0; `npm run test:integration` GREEN; `PI_CM_E2E_REF=pinned npm run test:e2e` GREEN; catalog-uat byte-equality gate GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub clone (bootstrap / marketplace add) | network fetch can fail; failure must reach the user via the V2 notify path (IL-2), not as an uncaught stack trace |
| Error cause chains -> rendered output | untrusted error `.message` text flows into `notify()` trailers; depth-bounded (MAX_DEPTH=5) to avoid runaway/cyclic chains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7pk-01 | Information Disclosure | bootstrap clone failure | mitigate | Task 2 routes the thrown failure through `notify()` as a failed marketplace row; no raw stack trace leaks to the channel (IL-2) |
| T-7pk-02 | Denial of Service | `causeChainTrailer` cyclic cause | mitigate | Task 6(c) test proves the MAX_DEPTH=5 bound terminates a self-referential cycle |
| T-7pk-03 | Repudiation | dropped error causes (update/autoupdate) | mitigate | Tasks 3 surface `cause: err` so failures are diagnosable, not silently bare `(failed)` |
| T-7pk-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; dependency versions explicitly frozen (out of scope) |
</threat_model>

<verification>
- All four originally-red CI tests GREEN: `tests/integration/fold-adoption.test.ts`
  install step + three `tests/e2e/import-command.test.ts` tests (lines 131/181/198).
- `grep -rn "D-16-12" extensions/` returns 0; flagged dead-symbol refs removed.
- NFR-4 reads Node >= 20.19.0 in PRD/CLAUDE.md/PROJECT.md, matching
  `package.json` engines.
- PRD PU-4/AS-6 record the intentional non-surfacing; no code/test changes there.
- Final gate: `npm run check` && `npm run test:integration` &&
  `PI_CM_E2E_REF=pinned npm run test:e2e` all exit 0.
</verification>

<success_criteria>
- `npm run check` exit 0 (typecheck + ESLint + Prettier + tests, incl. catalog-uat).
- `npm run test:integration` GREEN.
- `PI_CM_E2E_REF=pinned npm run test:e2e` GREEN.
- Bootstrap/update/autoupdate failures surface through the V2 notify path with cause.
- Manual-recovery rows name leaked paths (AS-7); covered by a notify-v2 test.
- Comments describe code, not GSD history; requirement IDs retained.
- Each task is a single Conventional-Commits commit; `pre-commit` clean; branch
  stays `gsd/v1.3-replan-catalog`.
</success_criteria>

<output>
Create `.planning/quick/260530-7pk-fix-pr-22-review-findings-failing-tests-/260530-7pk-SUMMARY.md` when done.
</output>
