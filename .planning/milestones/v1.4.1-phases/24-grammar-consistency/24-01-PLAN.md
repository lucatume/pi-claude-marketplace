---
phase: 24-grammar-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - docs/output-catalog.md
  - docs/messaging-style-guide.md
  - tests/architecture/catalog-uat.test.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/shared/notify-v2.test.ts
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/v1.4-MILESTONE-UAT.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - SNM-36

must_haves:
  truths:
    - "A plugin manifest declaring unsupported lspServers renders its reason brace as {lsp} (or {hooks, lsp} with hooks), never {lspServers} (D-24-01, SC#1/#2)"
    - "The REASONS closed-set tuple in shared/notify.ts contains \"lsp\" and no longer contains \"lspServers\" (D-24-02, SC#3)"
    - "The resolver-note detection substring \"lspServers\" (note.includes / MANIFEST_FIELD_REASONS set membership) still matches, so detection is not silently broken (D-24-04)"
    - "The manifest-side JSON key lspServers in the typebox schema (plugin.ts:31) and resolver (resolver.ts:142,160) is UNCHANGED (D-24-09, SC#4)"
    - "Catalog UAT byte-equality is GREEN: docs/output-catalog.md byte forms and catalog-uat fixtures both read {hooks, lsp} (D-24-07)"
    - "KEEP-bucket fixtures (errors.test.ts composed message, resolver kind:\"lspServers\" fixtures, install.test.ts detection INPUTs) stay camelCase and stay GREEN (D-24-06 false-GREEN guard)"
    - "All 6 stale shared/grammar/reasons.ts pointers re-point to shared/notify.ts::REASONS (D-24-08)"
    - "ROADMAP SC#1/#3, REQUIREMENTS SNM-36, and the UAT G-MIL-04 truth statement spell the rendered token \"lsp\", not \"lsp servers\" (D-24-03)"
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "REASONS tuple with \"lsp\" member (was \"lspServers\")"
      contains: "\"lsp\""
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      provides: "manifestFieldTokenFromNote maps detected camelCase token to emitted Reason"
      contains: "lsp"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/list.ts"
      provides: "narrowResolverNotes detects lspServers, emits lsp"
      contains: "lsp"
    - path: "docs/output-catalog.md"
      provides: "Catalog byte forms {hooks, lsp}"
      contains: "{hooks, lsp}"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts manifestFieldTokenFromNote"
      to: "shared/notify.ts type Reason"
      via: "MANIFEST_FIELD_TO_REASON map returns a typed Reason (no `as Reason` cast)"
      pattern: "lspServers.*lsp"
    - from: "tests/architecture/catalog-uat.test.ts"
      to: "docs/output-catalog.md"
      via: "self-checking byte-equality against fenced <!-- catalog-state --> blocks"
      pattern: "hooks.*lsp"
---

<objective>
Eliminate the lone camelCase token leak in the user-rendered `REASONS` closed
set. Rename the closed-set member `"lspServers"` (`shared/notify.ts:79`) to
`"lsp"` (D-24-01, D-24-02 option (a)), propagate through the type-driven compile
cascade and the two detection-vs-emission seams (D-24-04), update the catalog /
fixture / doc byte forms in lockstep (D-24-07), fold the 6 stale
`shared/grammar/reasons.ts` pointers (D-24-08), and amend the spec/record docs
that still say `"lsp servers"` to `"lsp"` in lockstep (D-24-03). Closes SNM-36 /
G-MIL-04.

Purpose: A plugin whose manifest declares unsupported `lspServers` must render
its reason brace as `{lsp}` (parallel to the single-word `{hooks}` carve-out),
never `{lspServers}` -- while the real `.claude-plugin/plugin.json` JSON key
`lspServers` (schema + resolver) stays untouched so manifest parsing is unbroken
(D-24-09 / SC#4).

Output: One atomic commit covering the closed-set rename, both seams, the
grammar-pointer hygiene, the catalog/fixture/doc byte-form lockstep, and the
spec/record amendments. `npm run check` GREEN; catalog-UAT byte-equality GREEN.

This is a single atomic plan (one wave) because D-24-07 mandates that the EMIT
rename and the catalog/fixture byte-form updates land in the SAME commit -- they
cannot be split across tasks or waves that could land separately. The three
tasks below are sequential within this one plan.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-grammar-consistency/24-CONTEXT.md
@.planning/phases/24-grammar-consistency/24-RESEARCH.md
@.planning/phases/24-grammar-consistency/24-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the closed-set member and rewire both detection-vs-emission seams</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/orchestrators/plugin/list.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (REASONS tuple :69-98, member "lspServers" at :79; type Reason at :100 derives from it; composeReasons at :863 is the sole render point and needs NO change -- purity means the renamed value flows through verbatim; :192 history comment STAYS, it is accurate Phase-21 history)
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts (ListReason union :169-176 incl. | "lspServers" at :171; narrowResolverNotes :270-296 -- detection substring note.includes("lspServers") at :283 plus seen bookkeeping; pushed value at :284; return/accumulator types at :272-273; JSDoc/comments at :164, :265, :310, :393)
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (MANIFEST_FIELD_REASONS set :1221; manifestFieldTokenFromNote :1231-1244 incl. return token as Reason at :1240; comments at :1141, :1208, :1219, :1238-1239, :1254, :1277)
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts (stale grammar pointer at :99)
    - extensions/pi-claude-marketplace/domain/resolver.ts (READ ONLY -- confirm UNSUPPORTED_COMPONENT_KINDS at :142 and conventions at :160 stay lspServers; this drives the "contains lspServers" note the seams detect; DO NOT EDIT per D-24-09/SC#4)
    - extensions/pi-claude-marketplace/domain/components/plugin.ts (READ ONLY -- confirm typebox field at :31 and JSDoc example at :46 stay lspServers; DO NOT EDIT per D-24-09/SC#4)
    - .planning/phases/24-grammar-consistency/24-RESEARCH.md (§Architecture Patterns "data flow through the seam"; §Code Examples -- the two recommended seam rewrites; §Common Pitfalls 1 and 4; §Stale shared/grammar Hygiene table)
  </read_first>
  <action>
    Do NOT blanket find/replace lspServers to lsp. The token wears two hats: a
    DETECTION key (matches the resolver's "contains lspServers" note, derived from
    the real JSON manifest field) which STAYS camelCase, and an EMITTED closed-set
    Reason value which becomes "lsp" (D-24-04, the load-bearing rule).

    1. shared/notify.ts: in the REASONS tuple, change the single member "lspServers"
    (:79) to "lsp". This is the one source-of-truth edit; type Reason re-derives via
    indexed access. Leave composeReasons (:863) untouched (D-24-02 rejected a
    renderer-side swap; purity means the renamed value renders verbatim). Leave the
    :192 history comment untouched (D-24-08 carve-out -- it accurately records the
    Phase-21 grammar retirement).

    2. install.ts seam (manifestFieldTokenFromNote): the DETECTION set
    MANIFEST_FIELD_REASONS = new Set(["hooks", "lspServers"]) (:1221) STAYS camelCase
    (it matches the bare token sliced from "contains lspServers"). Per D-24-05
    (Claude's discretion, RESEARCH-recommended mechanism), add a sibling emission map
    MANIFEST_FIELD_TO_REASON typed Readonly<Record<string, Reason>> with entries
    hooks to "hooks" and lspServers to "lsp", and rewrite the function body to
    return MANIFEST_FIELD_TO_REASON[token] instead of return token as Reason (:1240).
    This is robust regardless of whether tsc flags the old as Reason cast (RESEARCH
    Open Question 1 / A1 -- do NOT rely on the compiler catching the cast; the map
    rewrite removes it entirely). The function must still gate on the camelCase token
    (only return for members the map knows; undefined for others), preserving current
    behavior for non-carve-out notes.

    3. list.ts seam (narrowResolverNotes): keep the detection substring
    note.includes("lspServers") (:283) camelCase. Change the emitted union types
    ListReason (:171, | "lspServers" to | "lsp") and the narrowResolverNotes return
    type plus accumulator (:272, :273) lspServers slot to "lsp". Change the pushed
    value (:284) out.push("lspServers") to out.push("lsp"). Per Pitfall 4, flip the
    seen dedup key (:283/:285) to "lsp" for parallelism with the pushed value (the
    note.includes(...) substring on :283 STAYS camelCase; only the
    seen.has(...)/seen.add(...) key flips). Do not let the dedup key and the pushed
    value diverge.

    4. Comment/JSDoc partitioning (per RESEARCH §Partition Validation -- partition each
    by detect-describing vs emit-describing):
    - list.ts :164 JSDoc ("produces hooks / lspServers" -- describes EMITTED reasons)
      RENAME the lspServers token to lsp.
    - list.ts :265 ("passes hooks / lspServers verbatim" -- describes emission)
      RENAME to lsp.
    - list.ts :310 and :393 (describe what narrowResolverNotes RECOGNISES -- the
      detection token) KEEP camelCase lspServers.
    - install.ts :1141, :1208, :1254, :1277 (all narrate the detection-input token /
      "contains lspServers" note shape) KEEP camelCase lspServers.

    5. Stale shared/grammar/reasons.ts pointer hygiene (D-24-08 -- re-point to
    shared/notify.ts::REASONS; scope STRICTLY to these pointers -- do NOT touch
    status-tokens.ts references, the generic-directory references, or notify.ts:192,
    which are out of scope per RESEARCH):
    - install.ts :1219 (carve-out comment "MUST also be added to
      shared/grammar/reasons.ts") re-point to shared/notify.ts::REASONS.
    - install.ts :1238-1239 (cast-safety comment "in shared/grammar/reasons.ts")
      re-point to shared/notify.ts::REASONS; since the cast is being removed (step 2),
      reword the comment so it describes the map-based emission rather than a cast.
    - uninstall.ts :99 ("Reasons live in shared/grammar/reasons.ts") re-point to
      shared/notify.ts::REASONS.

    After edits, run npm run typecheck. The genuine type breaks will be in list.ts
    (the union/push mismatch); fix them per steps above until clean. DO NOT touch
    domain/resolver.ts or domain/components/plugin.ts (SC#4 / D-24-09).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: grep -n '"lspServers"' extensions/pi-claude-marketplace/shared/notify.ts returns NOTHING; grep -n '"lsp"' extensions/pi-claude-marketplace/shared/notify.ts shows the renamed REASONS member.
    - SOURCE (DETECT stays): grep -n 'note.includes("lspServers")' extensions/pi-claude-marketplace/orchestrators/plugin/list.ts STILL returns a match (detection substring unchanged).
    - SOURCE (DETECT stays): grep -n 'new Set(\["hooks", "lspServers"\])' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts STILL returns a match (MANIFEST_FIELD_REASONS unchanged).
    - SOURCE (EMIT renamed): install.ts contains a MANIFEST_FIELD_TO_REASON map with a lspServers key whose value is "lsp", and manifestFieldTokenFromNote no longer contains return token as Reason.
    - SOURCE (EMIT renamed): list.ts narrowResolverNotes pushes "lsp" and its seen key is "lsp"; the ListReason union and the function's return/accumulator types contain "lsp" and no longer contain "lspServers".
    - SOURCE (SC#4 untouched): grep -n lspServers extensions/pi-claude-marketplace/domain/components/plugin.ts extensions/pi-claude-marketplace/domain/resolver.ts STILL shows the schema field (:31) and resolver entries (:142, :160).
    - SOURCE (hygiene): grep -rn 'shared/grammar/reasons.ts' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts returns NOTHING (all 3 in-orchestrator pointers re-pointed).
    - BEHAVIOR: npm run typecheck exits 0 (TypeScript strict, the rename's type cascade resolves cleanly).
  </acceptance_criteria>
  <done>
    The REASONS member is "lsp", both seams detect camelCase and emit "lsp", the SC#4
    manifest surface is untouched, the 3 in-orchestrator grammar pointers are
    re-pointed, and npm run typecheck is GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update catalog / fixture / doc byte forms in lockstep; preserve the KEEP bucket</name>
  <files>docs/output-catalog.md, docs/messaging-style-guide.md, tests/architecture/catalog-uat.test.ts, tests/orchestrators/plugin/install.test.ts, tests/shared/notify-v2.test.ts</files>
  <read_first>
    - docs/output-catalog.md (byte forms {hooks, lspServers} at :158 and :300; carve-out prose mentioning {lspServers} at :58 and :118; stale grammar pointer at :58)
    - docs/messaging-style-guide.md (stale grammar pointers at :54 and :146)
    - tests/architecture/catalog-uat.test.ts (RENAME fixtures: structured reasons: ["hooks", "lspServers"] fed to notify() at :246 and :490; READ the test mechanism near :5-100 to confirm it reads the doc's fenced <!-- catalog-state --> blocks at runtime -- doc + fixture must change together)
    - tests/orchestrators/plugin/install.test.ts (RENAME the EMIT outputs only: narrowed row.reasons expected ["hooks","lspServers"] at :1589; expected output of __test_narrowResolverReasons ["lspServers"] at :1698 and :1712 -- KEEP the ["contains lspServers"] INPUT on those same lines AND the ["contains hooks","contains lspServers"] INPUT at :1579)
    - tests/shared/errors.test.ts (READ ONLY -- KEEP bucket: raw reasons array at :202, composed message "…is not installable: hooks; lspServers" at :204, :208 -- these are PRE-narrowing manifest-field names; DO NOT EDIT per D-24-06 / Pitfall 2 and 3)
    - tests/domain/resolver-loose.test.ts and tests/domain/resolver-strict.test.ts (READ ONLY -- KEEP bucket: kind: "lspServers" manifest-component fixtures at :194 and :163 respectively; DO NOT EDIT)
    - tests/shared/notify-v2.test.ts (review for any lsp reason byte fixtures that need lockstep adjustment, D-24-07; the SNM-35 hash-display work touched this file in Phase 23 -- confirm whether any reason-brace fixture references the renamed token)
    - .planning/phases/24-grammar-consistency/24-RESEARCH.md (§Partition Validation RENAME and KEEP tables with CORRECTED line numbers; §Validation Architecture; §Common Pitfalls 2 and 3)
    - .planning/phases/24-grammar-consistency/24-VALIDATION.md (the (a)/(b)/(c)/(d) regression-detection dimensions)
  </read_first>
  <action>
    Update ONLY the RENAME-bucket byte forms / EMIT-side expectations; leave every
    KEEP-bucket fixture byte-identical (D-24-06). A mistaken rename in the KEEP bucket
    is a silent false-GREEN regression (Pitfall 2).

    1. docs/output-catalog.md:
    - :158 and :300 fenced byte forms "⊘ … (unavailable) {hooks, lspServers}" to
      "{hooks, lsp}" (these are the catalog-state blocks the UAT reads).
    - :58 carve-out prose {lspServers} example (reason-describing) to {lsp}; in the
      SAME line re-point the stale grammar pointer
      extensions/pi-claude-marketplace/shared/grammar/reasons.ts::REASONS to
      extensions/pi-claude-marketplace/shared/notify.ts::REASONS (D-24-08).
    - :118 carve-out prose {lspServers} example (reason-describing) to {lsp}.

    2. tests/architecture/catalog-uat.test.ts: change the structured fixtures
    reasons: ["hooks", "lspServers"] at :246 and :490 to ["hooks", "lsp"]. These feed
    real notify() and must byte-match the doc blocks edited in step 1 -- they move in
    lockstep or the UAT goes RED.

    3. tests/orchestrators/plugin/install.test.ts (EMIT side only):
    - :1589 expected narrowed output ["hooks","lspServers"] to ["hooks","lsp"]
      (post-classifyEntityShapeError emitted Reason).
    - :1698 and :1712 expected output of __test_narrowResolverReasons ["lspServers"]
      to ["lsp"]. CRITICAL: the ["contains lspServers"] INPUT on those same lines
      STAYS camelCase, and the :1579 INPUT ["contains hooks","contains lspServers"]
      STAYS camelCase -- they prove the detect-camelCase / emit-lsp path end to end
      (dimension (b)).

    4. docs/messaging-style-guide.md: re-point the stale grammar pointers at :54 and
    :146 from shared/grammar/reasons.ts to shared/notify.ts::REASONS (D-24-08). Do NOT
    touch any status-tokens.ts / generic-directory references (out of scope per
    RESEARCH).

    5. tests/shared/notify-v2.test.ts: if any reason-brace byte fixture references the
    renamed token, adjust it to lsp in lockstep (D-24-07). If none reference it, leave
    the file unchanged and note that in the SUMMARY.

    Do NOT edit tests/shared/errors.test.ts, tests/domain/resolver-loose.test.ts, or
    tests/domain/resolver-strict.test.ts -- they are the KEEP-bucket false-GREEN guard
    and MUST stay GREEN with camelCase.

    Then run the quick validation command (catalog-UAT + install + errors + resolver)
    to confirm EMIT byte-equality is GREEN and the KEEP bucket still passes.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test "tests/architecture/catalog-uat.test.ts" "tests/orchestrators/plugin/install.test.ts" "tests/shared/errors.test.ts" "tests/domain/resolver-loose.test.ts" "tests/domain/resolver-strict.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE (EMIT, dimension a): grep -n 'lspServers' docs/output-catalog.md returns NOTHING; grep -n '{hooks, lsp}' docs/output-catalog.md shows the two updated byte forms (formerly :158, :300).
    - SOURCE (EMIT, dimension a): grep -c 'lspServers' tests/architecture/catalog-uat.test.ts returns 0 (both fixtures flipped to "lsp").
    - SOURCE (EMIT, dimension a): tests/orchestrators/plugin/install.test.ts expected outputs at the former :1589/:1698/:1712 now read "lsp"; the ["contains lspServers"] INPUTs on those lines and :1579 STILL read camelCase (grep 'contains lspServers' tests/orchestrators/plugin/install.test.ts STILL returns matches).
    - SOURCE (KEEP, dimension c): tests/shared/errors.test.ts, tests/domain/resolver-loose.test.ts, tests/domain/resolver-strict.test.ts are byte-identical to their pre-task state (git diff shows no changes to these three files).
    - SOURCE (hygiene): grep -rn 'shared/grammar/reasons.ts' docs/output-catalog.md docs/messaging-style-guide.md returns NOTHING (all 3 doc pointers re-pointed).
    - BEHAVIOR (dimension a): catalog-uat.test.ts passes -- the self-checking byte-equality against the {hooks, lsp} doc blocks is GREEN (proves EMIT renders correctly and doc+fixture are in sync).
    - BEHAVIOR (dimension b): the install.test.ts narrow-helper test passes -- INPUT ["contains lspServers"] still resolves to expected ["lsp"], proving detect-camelCase / emit-lsp end to end.
    - BEHAVIOR (dimension c, false-GREEN guard): errors.test.ts and the two resolver tests pass with camelCase intact -- a RED here would mean a KEEP site was wrongly renamed.
  </acceptance_criteria>
  <done>
    Catalog doc + catalog-uat fixtures + install.test EMIT expectations all read lsp
    and are GREEN; the three KEEP-bucket test files are byte-unchanged and GREEN; the
    3 doc grammar pointers are re-pointed; the quick validation command passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Amend spec/record docs ("lsp servers" → "lsp"), reframe the call-site wording, and run the full gate</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/v1.4-MILESTONE-UAT.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 24 ledger entry :95 "lspServers"/"lsp servers"/"13 consumer call-sites"; Phase 24 goal :447; SC#1 :455; SC#2 :456; SC#3 :457 -- note SC#4 :458's lspServers is the manifest-side field name and STAYS; Phase 25 SC#1 :472 "no lspServers leak" is a manifest-key reference and STAYS; Phase 26 SC#2 :490 says "lsp servers" rename for SNM-36 -- amend to "lsp")
    - .planning/REQUIREMENTS.md (SNM-36 at :24 -- option (a) wording "lsp servers" and "13 consumer call-sites"; the manifest-side references to plugin.ts:31 + resolver.ts:142,160 STAY as the JSON field name)
    - .planning/v1.4-MILESTONE-UAT.md (G-MIL-04 truth statement at :497 -- the acceptance oracle; title :496 and triage rows :744/:754 are the historical reporter quote)
    - .planning/PROJECT.md (G-MIL-04 gap record at :30 -- "either rename to lsp servers or insert a renderer-side translation"; the manifest-side references and the unsupported-component-kinds list at :96 STAY camelCase)
    - .planning/phases/24-grammar-consistency/24-RESEARCH.md (§Common Pitfall 5; §Open Questions item 2; §State of the Art -- the lockstep amendment row; the precedent that Phase 23 amended SNM-34 in lockstep)
    - .planning/phases/23-version-display-bundle/23-CONTEXT.md (the D-23-01/03 requirement-text-correction-in-lockstep precedent this mirrors)
  </read_first>
  <action>
    Amend the spec and acceptance-oracle records so they spell the rendered token
    "lsp" (D-24-01/D-24-03), in lockstep with the code shipped in Tasks 1-2. This is
    a documentation correction (NOT code) -- distinguish the user-rendered Reason
    ("lsp servers" to "lsp") from the manifest JSON field name (lspServers -- STAYS
    everywhere it appears as the JSON key).

    1. .planning/ROADMAP.md:
    - :95 ledger entry: change "to \"lsp servers\"" to "to \"lsp\"". Also reframe the
      imprecise "propagate to 13 consumer call-sites in list.ts (8) and install.ts
      (4)" wording per D-24-04: the real shape is a single closed-set tuple edit plus
      a type-driven compile cascade through two detection-vs-emission seams
      (list.ts::narrowResolverNotes and install.ts::manifestFieldTokenFromNote) plus
      the catalog/fixture byte-form lockstep -- detection substrings stay camelCase,
      only the emitted Reason becomes "lsp". The manifest-side lspServers reference in
      this entry STAYS (it is the JSON key).
    - :447 goal: "the rendered REASON reads lsp servers in space-separated lowercase"
      to "the rendered REASON reads lsp"; the manifest JSON key lspServers reference
      in the goal STAYS.
    - :455 SC#1: "{lsp servers}" to "{lsp}" (both occurrences in the sentence).
    - :456 SC#2: "{lsp servers}" to "{lsp}".
    - :457 SC#3: "the renamed discriminator \"lsp servers\"" to "\"lsp\""; reframe the
      "13 consumer call-sites in list.ts (8) and install.ts (4)" clause the same way
      as the :95 reframe (two detection-vs-emission seams + type cascade + fixtures,
      not 13 flat call-sites).
    - :458 SC#4: LEAVE -- lspServers there is the manifest field name (the SC that
      mandates it stays).
    - :490 Phase 26 SC#2: "the \"lsp servers\" rename for SNM-36" to "the \"lsp\"
      rename for SNM-36".
    - :472 Phase 25 SC#1: LEAVE -- "no lspServers leak" refers to the manifest-key
      leak that the smoke list must not show; it is a manifest-key reference.

    2. .planning/REQUIREMENTS.md SNM-36 (:24): change option (a)'s
    "to \"lsp servers\"" to "to \"lsp\"" and the option (b) "lspServers to lsp
    servers" translation example to "lspServers to lsp". Reframe "update all 13
    consumer call-sites in list.ts (8) and install.ts (4)" per D-24-04 (two
    detection-vs-emission seams + type cascade + catalog/fixture lockstep; detection
    substrings stay camelCase). The manifest-side references (plugin.ts:31,
    resolver.ts:142,160) and the "actual .claude-plugin/plugin.json field name"
    clause STAY camelCase.

    3. .planning/v1.4-MILESTONE-UAT.md :497 truth statement (the acceptance oracle for
    G-MIL-04, RESEARCH Open Question 2 recommendation = amend): change "should render
    as lsp servers" to "should render as lsp". The first occurrence of lspServers in
    that sentence (the token that leaks) and any later manifest-key references STAY
    camelCase. LEAVE the historical reporter quote at :499 ("the token lspServers
    appears … where it should read lsp servers") and the title :496 and triage rows
    :744/:754 -- those record the 2026-05-28 finding-as-reported (amending the
    reporter's quote would falsify the historical record). The :522-525 remediation
    options (which mention "lsp servers") are historical proposal prose -- LEAVE them.

    4. .planning/PROJECT.md G-MIL-04 (:30): change "rename the discriminator string to
    \"lsp servers\"" to "\"lsp\"". This keeps the milestone gap record's stated target
    truthful. The manifest-side field-name reference in that line and the
    unsupported-component-kinds list at :96 STAY camelCase.

    Then run the FULL gate npm run check (typecheck + ESLint + Prettier + tests) and
    confirm it is GREEN, plus a final SC#4 grep proving the manifest surface is
    untouched.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run check && grep -n lspServers extensions/pi-claude-marketplace/domain/components/plugin.ts extensions/pi-claude-marketplace/domain/resolver.ts</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: grep -n 'lsp servers' .planning/ROADMAP.md .planning/REQUIREMENTS.md returns NOTHING (every spec occurrence of the two-word token amended to "lsp").
    - SOURCE: .planning/ROADMAP.md SC#1/SC#2 now contain "{lsp}" and no longer contain "{lsp servers}".
    - SOURCE: .planning/v1.4-MILESTONE-UAT.md :497 truth statement contains "render as lsp" (not "lsp servers"); the :499 reporter quote and :496 title still read "lsp servers" (historical record preserved).
    - SOURCE: .planning/PROJECT.md :30 contains the rename target "lsp" (not "lsp servers").
    - SOURCE (call-site reframe): the "13 consumer call-sites" framing in ROADMAP :95/:457 and REQUIREMENTS :24 is reframed to the detection-vs-emission-seam + type-cascade description (grep for "detection" or "seam" near the SNM-36 wording returns a match).
    - SOURCE (SC#4 untouched, dimension d): grep -n lspServers extensions/pi-claude-marketplace/domain/components/plugin.ts extensions/pi-claude-marketplace/domain/resolver.ts STILL shows :31, :142, :160.
    - BEHAVIOR: npm run check exits 0 -- typecheck + ESLint + Prettier + full test suite all GREEN (NFR-6).
  </acceptance_criteria>
  <done>
    ROADMAP SC#1/#3 + :95 ledger + :490, REQUIREMENTS SNM-36, the UAT :497 truth
    oracle, and PROJECT.md :30 all spell the rendered token "lsp"; the "13 call-sites"
    framing is reframed to the seam/cascade reality; historical reporter quotes are
    preserved; npm run check is GREEN; the SC#4 manifest surface grep confirms
    lspServers untouched at :31/:142/:160.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced or altered) | This phase renames a user-facing display token and corrects doc/comment prose. No untrusted input crosses any boundary as a result of this change. The only external-input surface in the vicinity -- manifest JSON parsing -- is owned by the typebox schema at `domain/components/plugin.ts:31`, which D-24-09 / SC#4 explicitly forbids touching. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-NA | N/A | user-rendered Reason token (`shared/notify.ts` REASONS) | accept | No applicable ASVS L1 category. RESEARCH §Security Domain confirms: this is a display-token rename plus doc/comment hygiene touching no authentication, session, access-control, input-validation, or cryptography surface. Manifest input-validation (V5) is owned by the typebox schema, which is OUT OF SCOPE and unchanged (D-24-09). The detection substrings that match manifest-derived notes are explicitly preserved (D-24-04), so no parsing/validation behavior changes. No high-severity threat is introduced or altered; the gate clears. |

No `npm install` / package-manager installs occur in this phase (RESEARCH §Standard Stack: "No external packages"), so no `T-24-SC` supply-chain checkpoint or Package Legitimacy Audit is required.
</threat_model>

<verification>
Phase-level checks (map to VALIDATION.md regression dimensions):

- **(a) EMIT renders `{lsp}` / `{hooks, lsp}`:** `node --test "tests/architecture/catalog-uat.test.ts"` GREEN -- the self-checking byte-equality test reads the `{hooks, lsp}` doc blocks and asserts `notify()` output equals them; plus `tests/orchestrators/plugin/install.test.ts` EMIT expectations (former :1589/:1698/:1712) read `lsp`.
- **(b) DETECT still matches camelCase:** the `install.test.ts` narrow-helper test passes with INPUT `["contains lspServers"]` resolving to `["lsp"]` (detect-camelCase / emit-lsp end to end).
- **(c) KEEP-bucket false-GREEN guard:** `tests/shared/errors.test.ts`, `tests/domain/resolver-loose.test.ts`, `tests/domain/resolver-strict.test.ts` GREEN with camelCase intact and byte-unchanged (git diff clean for these three).
- **(d) SC#4 manifest untouched:** `grep -n lspServers extensions/pi-claude-marketplace/domain/components/plugin.ts extensions/pi-claude-marketplace/domain/resolver.ts` still shows `:31`, `:142`, `:160`.
- **Full gate (NFR-6):** `npm run check` exits 0 (typecheck + ESLint + Prettier + full test suite).
- **Spec lockstep (D-24-03):** `grep -n 'lsp servers' .planning/ROADMAP.md .planning/REQUIREMENTS.md` returns nothing; UAT :497 oracle reads `lsp`.
</verification>

<success_criteria>
SNM-36 / G-MIL-04 closed when ALL hold:

1. The `REASONS` tuple member at `shared/notify.ts:79` is `"lsp"`; no `"lspServers"` remains in the closed set (SC#3).
2. A manifest declaring unsupported `lspServers` renders `{lsp}` (or `{hooks, lsp}`) -- proven by the catalog-UAT byte-equality test (SC#1/#2, dimension a).
3. Detection still matches camelCase `lspServers` -- proven by the install.test narrow-helper INPUT staying camelCase and still producing `["lsp"]` (D-24-04, dimension b).
4. The manifest-side `lspServers` (typebox schema :31, resolver :142,:160) is unchanged (SC#4 / D-24-09, dimension d).
5. KEEP-bucket fixtures (errors + resolver) are byte-unchanged and GREEN (D-24-06, dimension c -- false-GREEN guard).
6. All 6 stale `shared/grammar/reasons.ts` pointers re-point to `shared/notify.ts::REASONS` (D-24-08); the out-of-scope `status-tokens.ts`/generic-directory references and `notify.ts:192` are untouched.
7. ROADMAP SC#1/#3 + ledger + Phase 26 SC#2, REQUIREMENTS SNM-36, UAT :497 truth oracle, and PROJECT.md :30 spell the rendered token `"lsp"`; the "13 call-sites" framing is reframed to the detection-vs-emission-seam reality (D-24-03).
8. `npm run check` is GREEN (NFR-6).
</success_criteria>

<output>
Create `.planning/phases/24-grammar-consistency/24-01-SUMMARY.md` when done.
</output>
