---
phase: 26-green-gate-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/26-green-gate-close/VERIFICATION.md
  - CHANGELOG.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SNM-40]
must_haves:
  truths:
    - "git status --porcelain is empty before the GREEN-gate run (clean checkout, SC#1)"
    - "npm run check exits 0 across typecheck + ESLint + Prettier + npm test on the clean tree (SC#1, SNM-40)"
    - "The observed npm test count is recorded in the closure narrative (mirrors 21-04-SUMMARY exit-0 + count format, D-26-03)"
    - "VERIFICATION.md locates each SNM-33/34/35/36 regression test as file:case and confirms it is GREEN (SC#2, D-26-06)"
    - "package.json version stays 0.2.0 -- no version bump, no chore(release), no tag (D-26-01)"
    - "CHANGELOG has exactly one unreleased [0.2.0] section and zero [0.3.0] section; no v1.3/v1.4 substance lost (D-26-02)"
    - "The four-doc closure narrative lands as a single docs(26): commit over CHANGELOG + STATE.md + PROJECT.md + REQUIREMENTS.md (D-26-04)"
    - "REQUIREMENTS.md SNM-23 traceability row reconciled from Pending and SNM-40 marked complete in the same pass (D-26-05)"
    - "Milestone is marked ready for /gsd-complete-milestone; archival is NOT run in this phase (D-26-04)"
  artifacts:
    - path: ".planning/phases/26-green-gate-close/VERIFICATION.md"
      provides: "GREEN-gate evidence (exit 0 + recorded count) + SNM-33..36 to test:case inventory table"
      contains: "SNM-33"
    - path: "CHANGELOG.md"
      provides: "Single consolidated unreleased [0.2.0] section spanning v1.3 + v1.4 + v1.4.1"
      contains: "[0.2.0]"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SNM-23 row reconciled + SNM-40 marked complete in traceability table"
      contains: "SNM-40"
  key_links:
    - from: "VERIFICATION.md SNM inventory"
      to: "tests/shared/notify-v2.test.ts + tests/orchestrators/plugin/install.test.ts + tests/architecture/catalog-uat.test.ts"
      via: "file:case citation per SNM row, confirmed GREEN by the gate run"
      pattern: "SNM-3[3-6]"
    - from: "docs(26): closure commit"
      to: "CHANGELOG.md + .planning/STATE.md + .planning/PROJECT.md + .planning/REQUIREMENTS.md"
      via: "single commit touching exactly the four docs"
      pattern: "docs\\(26\\)"
---

<objective>
Close the v1.4.1 (Post-ship UAT Patches) milestone (SNM-40) by proving `npm run
check` is GREEN end-to-end on a clean tree, locating and re-confirming the
SNM-33/34/35/36 regression tests, and recording the closure narrative across the
four planning docs + CHANGELOG -- mirroring the v1.4-close pattern (commit
`e465ef9`). This is a verification + documentation phase: NO source/code changes,
NO version bump (`package.json` stays `0.2.0`), NO archival.

Purpose: SNM-40 is the milestone-close GREEN gate. After Phases 22-25 landed all
v1.4.1 UAT patches, this phase confirms the gate is green on a clean checkout,
proves the named regression tests are present and passing, reconciles the
CHANGELOG down to a single unreleased `[0.2.0]`, sweeps the dangling SNM-23
traceability row, and leaves the milestone ready for `/gsd-complete-milestone`
(operator-initiated archival is a separate step).

Output: `VERIFICATION.md` (GREEN evidence + SNM->test inventory) and a single
`docs(26):` commit updating CHANGELOG + STATE.md + PROJECT.md + REQUIREMENTS.md.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-green-gate-close/26-CONTEXT.md
@.planning/phases/21-final-teardown-green-gate/21-04-SUMMARY.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean the working tree, run the GREEN gate, capture exit code + observed npm test count</name>
  <read_first>
    - .planning/phases/26-green-gate-close/26-CONTEXT.md (D-26-01 no version bump; D-26-03 clean-tree-first method)
    - .planning/phases/21-final-teardown-green-gate/21-04-SUMMARY.md (the exit-0 + "1120 baseline + 2 new = 1122" count-record format to mirror)
    - package.json (the `check` script chain: typecheck -> lint -> format:check -> test; confirm `npm test` glob is the unit/architecture track, NOT `test:integration`)
  </read_first>
  <action>
    Make the working tree clean FIRST, then gate, then record (D-26-03 order).
    (1) Resolve the two uncommitted noise files (`git status --porcelain` currently
    shows `M .claude/settings.json` and `M package-lock.json`) so GREEN is not an
    artifact of local state. Your discretion: either stash them, or commit them as
    a SEPARATE `chore:` commit (e.g. `chore: clean working tree before v1.4.1 close
    gate`) -- if you commit, it MUST NOT be folded into the Task 3 `docs(26):`
    closure commit. Confirm `git status --porcelain` prints nothing before gating.
    (2) Run `npm run check` (typecheck + ESLint + Prettier + `npm test`) on the
    clean tree; capture its exit code. It must exit 0. Do NOT run `npm run
    test:integration` -- the `tests/integration/fold-adoption.test.ts` phase-1
    failure is out of scope (SC#3) and lives on a separate track; do not conflate it
    with `npm test`. (3) Capture the integer `npm test` pass count from the run
    output (the `# pass N` / `# tests N` summary line). Hold the exit code and the
    observed count for use in Task 2 (VERIFICATION.md) and Task 3 (closure
    narrative). Make NO source, test, or config edits -- this is a pure run + record;
    do NOT touch `package.json` version (stays `0.2.0`, D-26-01).
  </action>
  <verify>
    <automated>git status --porcelain | wc -l  # must be 0 before the gate run</automated>
    <automated>npm run check; echo "check exit: $?"  # must print "check exit: 0"</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` prints nothing (empty) immediately before `npm run check` is invoked.
    - `npm run check` exits 0; the typecheck, ESLint, Prettier, and `npm test` stages all pass.
    - The integer `npm test` pass count is captured from the run summary line and is non-zero.
    - `grep '"version"' package.json` still shows `0.2.0` (version field untouched).
    - No files under `extensions/`, `tests/`, `eslint.config.js`, `tsconfig*.json`, or `package.json` were modified by this task (gate is run-only).
    - If the noise was committed rather than stashed, it is its own `chore:` commit (its `git log -1 --format=%s` is NOT `docs(26): ...`).
  </acceptance_criteria>
  <done>
    Working tree clean before the gate; `npm run check` confirmed exit 0 on the
    clean tree; the observed `npm test` count recorded for downstream tasks; no
    source/version changes made.
  </done>
</task>

<task type="auto">
  <name>Task 2: Locate the SNM-33/34/35/36 regression tests, confirm GREEN, and write VERIFICATION.md (gate evidence + SNM->test inventory)</name>
  <read_first>
    - tests/shared/notify-v2.test.ts (SNM-33 empty-add/remove/no-op negative no-trailer cases around lines 478-520 and 793-819; SNM-35 `v#<7hex>` cases around lines 1668-1759)
    - tests/orchestrators/plugin/install.test.ts (SNM-34 tier-1 plugin.json-version test, `test("SNM-34: plugin.json version present, entry.version absent ...")` around line 627)
    - tests/architecture/catalog-uat.test.ts (SNM-35 `hash-version-list` ~line 421 + `hash-version-arrow` ~line 946; SNM-36 `lsp` byte form in `reasons: ["hooks", "lsp"]` ~lines 246, 490)
    - .planning/REQUIREMENTS.md (SC#2 wording -- §SNM-33..36 at lines 14, 18, 20, 24)
    - .planning/phases/26-green-gate-close/26-CONTEXT.md (D-26-06 inventory shape; D-26-03 "standard VERIFICATION.md is the evidence")
  </read_first>
  <action>
    Grep/locate the exact regression-test cases for each SNM ID at execution time
    (the canonical surfaces are named in `read_first`; resolve the precise
    `file:case` -- test title or line -- for each). The tests already exist (added
    in Phases 22-24); this task LOCATES and re-confirms them GREEN, it does NOT
    write new tests. Then write `.planning/phases/26-green-gate-close/VERIFICATION.md`
    as the standard gsd evidence report containing: (a) the GREEN-gate result from
    Task 1 -- `npm run check` exit 0 with the observed `npm test` count, in the
    21-04-SUMMARY exit-0 + count format; (b) an explicit SC#3 note that
    `tests/integration/fold-adoption.test.ts` is out of scope and on the separate
    `npm run test:integration` track (not part of this gate); and (c) a compact
    markdown SNM->test inventory table with one row each for SNM-33, SNM-34,
    SNM-35, SNM-36, each citing the resolved `file:case` and a GREEN marker. SNM-33
    -> the three no-trailer byte-equality cases in `tests/shared/notify-v2.test.ts`;
    SNM-34 -> the tier-1 plugin.json-version test in
    `tests/orchestrators/plugin/install.test.ts`; SNM-35 -> the `v#<7hex>` cases in
    `tests/shared/notify-v2.test.ts` + the `hash-version-list`/`hash-version-arrow`
    fixtures in `tests/architecture/catalog-uat.test.ts`; SNM-36 -> the `lsp`
    byte-form fixtures in `tests/architecture/catalog-uat.test.ts` plus the impacted
    unit tests. Re-run the three named test files directly (via `node --test` on
    those paths) to confirm each cited case is GREEN before recording it.
  </action>
  <verify>
    <automated>node --test "tests/shared/notify-v2.test.ts" "tests/orchestrators/plugin/install.test.ts" "tests/architecture/catalog-uat.test.ts"; echo "named-tests exit: $?"  # must print "named-tests exit: 0"</automated>
    <automated>test -f .planning/phases/26-green-gate-close/VERIFICATION.md && grep -c -E 'SNM-3[3-6]' .planning/phases/26-green-gate-close/VERIFICATION.md  # must be >= 4</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/26-green-gate-close/VERIFICATION.md` exists.
    - It records `npm run check` exit 0 and the observed integer `npm test` count from Task 1.
    - It contains a markdown table with one row each for SNM-33, SNM-34, SNM-35, SNM-36 (`grep -E 'SNM-33' / 'SNM-34' / 'SNM-35' / 'SNM-36'` each returns >=1 match).
    - Each SNM row cites a concrete `file:case` (test file path plus test title or line) and a GREEN marker.
    - It contains an explicit SC#3 out-of-scope note naming `tests/integration/fold-adoption.test.ts` and the `npm run test:integration` track.
    - `node --test` over the three named test files exits 0 (every cited case is GREEN).
  </acceptance_criteria>
  <done>
    VERIFICATION.md written with the GREEN-gate evidence, the SC#3 out-of-scope
    note, and a 4-row SNM->test inventory where each row cites a re-confirmed-GREEN
    `file:case`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the four-doc closure narrative (CHANGELOG fold + STATE milestone-ready + PROJECT evolution + REQUIREMENTS SNM-23/SNM-40) as one docs(26): commit</name>
  <read_first>
    - CHANGELOG.md (current `[0.3.0]`=v1.4 + `[0.2.0]`=v1.3; D-26-02 folds both into one unreleased `[0.2.0]`)
    - .planning/STATE.md (milestone header, Current Position, Deferrals table, Blockers/Concerns -- mark milestone-ready per D-26-04)
    - .planning/PROJECT.md (`## Current Milestone` block ~line 13, `### Validated` ~line 52, `## Key Decisions` ~line 136, `## Evolution` ~line 166 -- evolve with the v1.4.1 close)
    - .planning/REQUIREMENTS.md (traceability table; SNM-23 row at line 146 `| SNM-23 | Phase 20 | Pending |`; SNM-40 row at line 163 `| SNM-40 | Phase 26 | Pending |`; the Coverage block at lines 165-176)
    - .planning/phases/21-final-teardown-green-gate/21-04-SUMMARY.md (the recorded-count narrative format)
    - .planning/phases/26-green-gate-close/26-CONTEXT.md (D-26-01..06 -- all locked)
    - git show e465ef9 (the four-doc single-commit close pattern to mirror; note it wrote `[0.3.0]` without bumping package.json -- the inconsistency D-26-02 reconciles)
  </read_first>
  <action>
    Edit the four docs and land them as ONE `docs(26):` commit (mirror `e465ef9`),
    using the observed `npm test` count from Task 1.
    (a) CHANGELOG.md (D-26-02): fold the `## [0.3.0] - 2026-05-27 -- v1.4 ...`
    entry and the new v1.4.1 closure narrative INTO the existing `## [0.2.0]` v1.3
    section, producing exactly ONE consolidated unreleased `[0.2.0]` section
    covering all messaging work since `v0.1.7` (v1.3 + v1.4 + v1.4.1). Remove the
    `[0.3.0]` heading. Mark the section unreleased -- drop the `2026-05-2x` ship
    dates that imply a release (e.g. retitle to `## [0.2.0] -- unreleased`). Lose NO
    v1.3/v1.4 substance: preserve the user-visible + Internals bullets. Add the
    v1.4.1 bullets (SNM-33 reload-hint discipline / SNM-34 plugin.json tier-1
    version / SNM-35 `v#<7hex>` display / SNM-36 `lsp` grammar) and the recorded
    test count. Exact interleave prose + whether to keep per-milestone sub-headings
    inside the one block is your discretion. Do NOT touch the `[0.1.7]` and earlier
    released entries. Do NOT add a new version number for v1.4.1 (D-26-01).
    (b) .planning/STATE.md (D-26-04): mark the v1.4.1 milestone ready for
    `/gsd-complete-milestone` -- update status to verified/complete, mark Phase 26
    complete, and reflect the close in Current Position / Session Continuity. Leave
    the Deferrals archival row intact (archival stays operator-initiated). Phrasing
    is your discretion; do NOT run or imply archival here.
    (c) .planning/PROJECT.md: evolve per the post-phase pattern -- record the v1.4.1
    close in the milestone narrative (`## What This Is` / `## Current Milestone` /
    `### Validated` / `## Evolution`), mirroring how the v1.4 close was recorded.
    Exact text is your discretion.
    (d) .planning/REQUIREMENTS.md: in the traceability table, flip the SNM-40 row
    from `Pending` to the complete status, AND reconcile the SNM-23 row (line 146)
    from `Pending` to its complete status (D-26-05; SNM-23's work shipped in Phase
    20 -- only the row is stale). Update the Coverage block counts (Pending/Complete)
    to match. This is the ONLY v1.4-era sweep -- reopen no other v1.4 scope.
    Then commit all four files as a single `docs(26): close v1.4.1 milestone +
    CHANGELOG + STATE + PROJECT + REQUIREMENTS` commit. Run `pre-commit run --files
    <the four changed files>` (or `--all-files`) and fix any failures before
    committing; never use `--no-verify`. Do NOT fold any Task-1 `chore:` tree-cleaning
    commit into this commit. Do NOT edit `package.json` (D-26-01).
  </action>
  <verify>
    <automated>grep -c '^## \[0.2.0\]' CHANGELOG.md  # must be exactly 1</automated>
    <automated>grep -c '^## \[0.3.0\]' CHANGELOG.md  # must be exactly 0</automated>
    <automated>grep '"version"' package.json | head -1  # must still show 0.2.0</automated>
    <automated>grep -nE '^\| SNM-23 ' .planning/REQUIREMENTS.md | grep -v 'Pending'  # SNM-23 row must no longer be Pending</automated>
    <automated>grep -nE '^\| SNM-40 ' .planning/REQUIREMENTS.md | grep -v 'Pending'  # SNM-40 row must no longer be Pending</automated>
    <automated>git show --stat HEAD | grep -E 'CHANGELOG.md|STATE.md|PROJECT.md|REQUIREMENTS.md'  # the docs(26) commit touches exactly these four</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` has exactly one `## [0.2.0]` heading and zero `## [0.3.0]` heading; the section is marked unreleased (no `2026-05-2x` ship date implying a release).
    - No v1.3 or v1.4 substance is lost: the prior v1.3 user-visible/Internals bullets and the v1.4 Internals bullets are still present, regrouped under the single `[0.2.0]`.
    - The recorded `npm test` count from Task 1 appears in the closure narrative (CHANGELOG and/or STATE).
    - `grep '"version"' package.json` still shows `0.2.0` -- version field unchanged; no `chore(release)` commit, no tag created.
    - REQUIREMENTS.md traceability row for SNM-23 is no longer `Pending` (reconciled to complete); the SNM-40 row is no longer `Pending` (marked complete); the Coverage Pending/Complete counts are updated to match.
    - STATE.md marks the v1.4.1 milestone ready for `/gsd-complete-milestone`; the Deferrals archival row is left intact and archival is NOT run.
    - The `docs(26):` commit (`git show --stat HEAD`) touches exactly `CHANGELOG.md`, `.planning/STATE.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md` (the plan SUMMARY and any `chore:` tree-clean commit are separate).
  </acceptance_criteria>
  <done>
    CHANGELOG folded to a single unreleased `[0.2.0]`; STATE/PROJECT evolved to
    milestone-ready; REQUIREMENTS SNM-23 + SNM-40 rows reconciled; all four docs
    committed as one `docs(26):` commit; `package.json` version unchanged; archival
    NOT run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase introduces NO new attack surface. It makes no source-code changes, adds
no command surface, opens no network calls, and accepts no new external input. The
only writes are local Markdown docs (`VERIFICATION.md` + the four planning docs +
CHANGELOG) inside the repository working tree. The NFR-10 containment constraint
(no writes outside `<scopeRoot>/pi-claude-marketplace/`, `<scopeRoot>/agents/`,
`<scopeRoot>/mcp.json`) is not exercised because no `scopeRoot` runtime writes
occur -- all writes are repo-local docs under version control.

| Boundary | Description |
|----------|-------------|
| developer -> repo working tree | Local doc writes only; no untrusted input crosses any runtime boundary in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information disclosure | Committed closure docs (CHANGELOG / STATE / PROJECT / REQUIREMENTS / VERIFICATION.md) | mitigate | Do not paste secrets, tokens, or credentials into any committed doc. The trufflehog pre-commit hook runs on the commit (per CLAUDE.md) and scans for leaked secrets; the docs contain only test counts, requirement IDs, and narrative prose. |
| T-26-02 | Tampering | npm/pip/cargo installs | accept | No package installs occur in this phase (no `npm install`, no dependency changes). No package-legitimacy surface is touched; the existing locked dependency set is unchanged. |
| T-26-SC | Tampering | Supply-chain / install integrity | accept | No new packages, no `package.json`/`package-lock.json` dependency edits beyond the pre-existing uncommitted `package-lock.json` noise (which is committed/stashed as-is in Task 1, not regenerated). Nothing is fetched or installed. |
</threat_model>

<verification>
Phase-level checks (all must hold at phase close):

- `git status --porcelain` was empty before the GREEN-gate run (clean checkout, SC#1).
- `npm run check` exits 0 on the clean tree across typecheck + ESLint + Prettier + `npm test` (SC#1, SNM-40).
- The observed `npm test` count is recorded in VERIFICATION.md and the closure narrative (D-26-03).
- VERIFICATION.md contains a 4-row SNM->test inventory (SNM-33/34/35/36), each citing a GREEN `file:case` (SC#2, D-26-06).
- `tests/integration/fold-adoption.test.ts` is explicitly noted out of scope and NOT part of the gate (SC#3).
- CHANGELOG has exactly one unreleased `## [0.2.0]` section and zero `## [0.3.0]` section; no v1.3/v1.4 substance lost (D-26-02).
- `package.json` version is unchanged at `0.2.0`; no `chore(release)` commit, no tag (D-26-01).
- REQUIREMENTS.md SNM-23 and SNM-40 rows reconciled out of `Pending`; Coverage counts updated (D-26-05).
- The four-doc closure landed as one `docs(26):` commit (D-26-04).
- STATE.md marks the milestone ready for `/gsd-complete-milestone`; archival NOT run (D-26-04).
</verification>

<success_criteria>
- SC#1: `npm run check` exits 0 on a clean checkout of the post-Phase-25 tree (typecheck + ESLint + Prettier + full `npm test`).
- SC#2: the SNM-33/34/35/36 regression tests are located, present, and GREEN, recorded as a `file:case` inventory in VERIFICATION.md.
- SC#3: `tests/integration/fold-adoption.test.ts` phase-1 failure remains out of scope (separate `npm run test:integration` track); the close gate is not blocked by it.
- SC#4: the closure narrative is recorded across CHANGELOG + STATE.md + PROJECT.md + REQUIREMENTS.md per the v1.4-close pattern; the milestone is ready for `/gsd-complete-milestone`.
- D-26-01..06 all honored: no version bump; single unreleased `[0.2.0]`; clean-tree GREEN with recorded count; SNM->test inventory; four-doc single `docs(26):` commit; SNM-23 row reconciled; milestone ready-not-archived.
</success_criteria>

<output>
Create `.planning/phases/26-green-gate-close/26-01-SUMMARY.md` when done.
</output>
