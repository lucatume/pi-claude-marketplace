---
phase: 25-runtime-publish-verification
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - tests/shared/snm38-indent-ladder.test.ts
  - docs/output-catalog.md
autonomous: true
requirements: [SNM-38]
must_haves:
  truths:
    - "The leading-whitespace byte counts of a representative /claude:plugin list, captured at ctx.ui.notify (pre-tui), are recorded per line."
    - "Those byte counts are compared against the catalog ladder (marketplace header column 0, plugin row 2-space, cause 4-space, phase-cause 6-space) per D-16-08."
    - "A verdict is recorded: REFUTE (not-a-bug + catalog wording clarification noting the display-layer caveat) if the renderer emits the catalog ladder, OR a renderer fix + byte-equality regression if it truly emits a non-catalog ladder."
  artifacts:
    - path: "tests/shared/snm38-indent-ladder.test.ts"
      provides: "Explicit per-line leading-whitespace assertion locking the 0/2(/4/6) ladder"
      contains: "trimStart"
    - path: "docs/output-catalog.md"
      provides: "Catalog wording clarification noting the markdown/tui display-layer caveat for the indent ladder"
      contains: "display"
  key_links:
    - from: "tests/shared/snm38-indent-ladder.test.ts"
      to: "notify()"
      via: "pre-tui byte capture at ctx.ui.notify"
      pattern: "ui.*notify"
---

<objective>
SNM-38 (G-MIL-03 indent ladder): byte-evidence-first reproduce-or-refute (D-25-09). Capture what the renderer ACTUALLY emits at the `ctx.ui.notify` boundary (pre-tui) for a representative `/claude:plugin list`, count leading whitespace per line, and compare against the catalog ladder. The RESEARCH verdict is REFUTE (HIGH confidence): the renderer emits the catalog-conformant ladder (header at column 0, plugin rows at 2 spaces, cause chains at 4, rollback-phase causes at 6), already locked by `tests/architecture/catalog-uat.test.ts` inside `npm run check`. The user's observed "1-space header / 3-space plugin" is a markdown/tui display artifact. BUT the phase contract is reproduce-OR-refute: actually run the byte capture and KEEP the conditional fork -- do not hard-code the conclusion.

Purpose: Settle G-MIL-03 with binding byte evidence at the correct layer (pre-tui), record the verdict, and (Claude's-discretion default: yes) leave a cheap explicit readability test as drift insurance feeding Phase 26's GREEN gate.

Output: an explicit per-line leading-whitespace assertion in `tests/shared/snm38-indent-ladder.test.ts`, plus the recorded verdict as a catalog wording clarification in `docs/output-catalog.md`.

WARNING (RESEARCH Pitfall 1): the UAT G-MIL-03 `truth:` line misquotes the contract as "header 2-space / row 4-space"; the catalog actually says "header at column 0; plugin rows two spaces beneath" (0/2). Anchor on `docs/output-catalog.md` + the `notify.ts` constants, NOT the UAT restatement. A "fix" that pushes the header to 2 spaces would BREAK the existing catalog-uat byte-equality gate -- that breakage is the signal you followed the wrong "truth".
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-runtime-publish-verification/25-CONTEXT.md
@.planning/phases/25-runtime-publish-verification/25-RESEARCH.md
@.planning/phases/25-runtime-publish-verification/25-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Byte-capture the indent ladder at ctx.ui.notify and decide REFUTE-vs-real-bug</name>
  <files>tests/architecture/catalog-uat.test.ts (read-only verify; no change unless real-bug fork)</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts -- the two render chokepoints (VERIFIED line numbers from RESEARCH drift table): `composeMarketplaceBlock` ~:1254 (header has NO prefix → column 0); `composePluginLines` ~:1230 (plugin row prefixed with `"  "` → 2 spaces, D-16-04); `renderIndentedCauseChain` ~:1191 (cause trailer `"    "` → 4 spaces); `composeRollbackPartialLines` ~:1207 (phase cause `"      "` → 6 spaces); `renderPluginRow` is at ~:921 (the `:887` citation in CONTEXT is a doc comment); `renderMpHeader` ~:648
    - docs/output-catalog.md -- canonical bytes: "header at column 0; plugin rows two spaces beneath" (~L15/L51/L137); `single-mp-mixed` (~L153-160), `project-orphan-folded` (~L186-190), `hash-version-list` (~L257). These are the authoritative byte source.
    - tests/architecture/catalog-uat.test.ts -- the existing byte-equality gate (the `mock.fn()` ctx at ~:149-151; the driver walking each `<!-- catalog-state: STATE -->` block); confirm `single-mp-mixed` / `project-orphan-folded` already encode the 0/2 ladder
    - .planning/v1.4-MILESTONE-UAT.md (G-MIL-03 ~:476-494 -- note the user's visual sample AND that the `truth:` line misquotes the contract as 2/4)
    - .planning/phases/25-runtime-publish-verification/25-RESEARCH.md (Pitfall 1; the G-MIL-03 chokepoint code excerpt; D-25-09 boundary)
  </read_first>
  <action>
    This byte-capture is FULLY automated (D-25-07: automate as much as possible first; the G-MIL-03 evidence is captured at the pre-tui `ctx.ui.notify` boundary, independent of the live tui). Drive a representative `/claude:plugin list` `NotificationMessage` through `notify(ctx, pi, message)` with the `mock.fn()` notify-capture ctx (mirroring `catalog-uat.test.ts:149`) and read the body from `ctx.ui.notify.mock.calls[0].arguments[0]` -- the pre-tui bytes. Use a fixture exercising an installed/available mix and the `{...}` reason brace (reuse the `single-mp-mixed` shape). Compute leading whitespace per line as `body.split("\n").map(l => l.length - l.trimStart().length)` and record the per-line counts in the SUMMARY. DECIDE via the D-25-09 fork: if the marketplace-header lines are at 0 and plugin rows are at 2 (catalog-conformant ladder) → verdict is REFUTE (the observed 1/3 is a markdown/tui display artifact). If -- and only if -- the renderer truly emits a non-catalog ladder (e.g. header at 1, row at 3) → this is a real D-16-08 violation; STOP and flag for a renderer fix at `composeMarketplaceBlock`/`composePluginLines` shipped WITH `docs/output-catalog.md` + the catalog-uat fixtures updated in the same commit (so `catalog-uat.test.ts` stays byte-equal). Do NOT change the header to 2 spaces "to match the UAT truth line" -- that would break the catalog-uat gate and is the wrong-truth signal. Confirm the existing gate is green: `node --test "tests/architecture/catalog-uat.test.ts"`.
  </action>
  <verify>
    <automated>node --test "tests/architecture/catalog-uat.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - The per-line leading-whitespace byte counts of the representative list output are captured at `ctx.ui.notify` (pre-tui) and recorded in the SUMMARY.
    - The byte counts are compared against the catalog ladder (header 0 / row 2 / cause 4 / phase-cause 6), anchored on `docs/output-catalog.md` + `notify.ts` constants -- NOT the UAT 2/4 misquote.
    - A verdict is reached via the D-25-09 fork (REFUTE on catalog-conformant emit; real-bug only on a true non-catalog emit).
    - `node --test "tests/architecture/catalog-uat.test.ts"` passes (existing ladder lock unbroken).
  </acceptance_criteria>
  <done>The indent ladder's actual pre-tui bytes are captured and a REFUTE-or-real-bug verdict is reached against the catalog ladder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Explicit leading-whitespace readability test + record the verdict as a catalog clarification</name>
  <files>tests/shared/snm38-indent-ladder.test.ts, docs/output-catalog.md</files>
  <read_first>
    - tests/architecture/catalog-uat.test.ts (the drive-`notify()`-and-assert shape + the `mock.fn()` ctx at ~:149-151 -- mirror it)
    - extensions/pi-claude-marketplace/shared/notify.ts (the verified constants: header prefix `""`, plugin row prefix `"  "`, cause `"    "`, phase-cause `"      "` -- assert against THESE, not the UAT 2/4)
    - docs/output-catalog.md (find the indent-ladder / D-16-08 prose around L15/L51/L137 where the "header at column 0; plugin rows two spaces beneath" contract is stated -- this is where the clarification note attaches)
    - .planning/phases/25-runtime-publish-verification/25-PATTERNS.md (the "snm38-indent-ladder" optional-test assignment + the explicit-leading-whitespace excerpt)
    - the SUMMARY/verdict from Task 1 (REFUTE vs real-bug) -- this task records that verdict
  </read_first>
  <behavior>
    - Test 1 (header at column 0): for the representative list body captured at `ctx.ui.notify`, every marketplace-header line (the `●`/status-glyph marketplace rows) has 0 leading spaces.
    - Test 2 (plugin row at 2 spaces): every plugin row line has exactly 2 leading spaces (D-16-04 / D-16-08).
    - (If the fixture includes a failed/cause row) Test 3: the cause trailer line has 4 leading spaces.
  </behavior>
  <action>
    Create `tests/shared/snm38-indent-ladder.test.ts` (Claude's-discretion default: YES -- cheap drift insurance feeding Phase 26's GREEN gate). Mirror the `catalog-uat.test.ts` drive-and-capture shape: build a representative list `NotificationMessage`, call `notify()`, capture `body` at the mocked `ctx.ui.notify`, compute `indents = body.split("\n").map(l => l.length - l.trimStart().length)`, and assert header lines → 0 and plugin rows → 2 (and cause → 4 if present). Anchor the expected values on the `notify.ts` constants, NOT the UAT 2/4 misquote. Then record the verdict in `docs/output-catalog.md` as a catalog wording clarification near the D-16-08 indent prose: state that the renderer emits the 0/2(/4/6) ladder at `ctx.ui.notify` and that the markdown/tui display layer may add a single leading space, so a "1/3" visual appearance is a display-layer artifact, not a renderer deviation (the byte-exact contract is captured pre-tui). If Task 1 reached a real-bug verdict instead, this task's catalog edit becomes the corrected canonical byte block shipped alongside the renderer fix + a byte-equality regression -- keep the conditional fork.
  </action>
  <verify>
    <automated>node --test "tests/shared/snm38-indent-ladder.test.ts" && node --test "tests/architecture/catalog-uat.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/shared/snm38-indent-ladder.test.ts` exists, lives under `tests/shared/` (runs inside `npm run check`), and passes; it asserts header→0 / plugin-row→2 leading spaces against the captured pre-tui body.
    - `docs/output-catalog.md` contains a clarification note (string `display`) explaining the markdown/tui display-layer caveat for the indent ladder (REFUTE path) OR the corrected canonical byte block (real-bug path).
    - `node --test "tests/architecture/catalog-uat.test.ts"` still passes (existing gate byte-equal).
    - `npm test 2>&1 | grep -Eq '# fail 0'`.
  </acceptance_criteria>
  <done>An explicit ladder-locking readability test is in the in-check suite and the G-MIL-03 verdict is recorded as a catalog clarification (or corrected canonical bytes on the real-bug path).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → notify boundary | `notify()` produces byte strings captured pre-tui via the mocked `ctx.ui.notify`; no Pi process, no disk writes |
| renderer → markdown/tui display | the display layer (pi-tui) may mutate leading whitespace; the byte-exact contract is asserted BEFORE this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Repudiation (wrong verdict recorded) | asserting on post-markdown bytes instead of pre-tui | mitigate | Capture exclusively at `ctx.ui.notify` (D-25-09); the test computes leading whitespace on the pre-tui body -- the markdown layer is explicitly out of the assertion. |
| T-25-05 | Tampering | a naive "fix" pushing header to 2 spaces silently breaks the catalog-uat gate | mitigate | Task 1 anchors on `docs/output-catalog.md` + `notify.ts` constants and keeps `catalog-uat.test.ts` green as the wrong-truth tripwire (RESEARCH Pitfall 1). |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed (RESEARCH Package Legitimacy Audit: not applicable); `node:test` built in. |

No `high`-severity threats: pure read-only rendering, no network (NFR-5), no new file writes beyond one test file + a catalog doc clarification. Stated explicitly per the phase security directive.
</threat_model>

<verification>
- `node --test "tests/shared/snm38-indent-ladder.test.ts"` passes (header→0 / row→2 at the pre-tui boundary).
- `node --test "tests/architecture/catalog-uat.test.ts"` passes (existing ladder lock byte-equal).
- `npm run check` exits 0.
- `docs/output-catalog.md` carries the recorded G-MIL-03 verdict (display-layer clarification on REFUTE, or corrected canonical bytes on real-bug).
</verification>

<success_criteria>
- G-MIL-03 is conclusively reproduced-or-refuted with binding pre-tui byte evidence compared against the catalog ladder (D-16-08).
- The verdict is RECORDED (catalog clarification noting the display-layer caveat, or a renderer fix + byte-equality regression on the real-bug path).
- An explicit ladder-locking readability test runs inside `npm run check`; the existing catalog-uat gate stays green.
</success_criteria>

<output>
Create `.planning/phases/25-runtime-publish-verification/25-02-SUMMARY.md` when done. Record: the per-line leading-whitespace byte counts captured, the catalog ladder they were compared against, the verdict (REFUTE + display-layer caveat, or real-bug fix), and the catalog clarification location.
</output>
