---
phase: 25-runtime-publish-verification
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - tests/edge/completions/provider.test.ts
  - .planning/v1.4-MILESTONE-UAT.md
  - .planning/STATE.md
autonomous: false
requirements: [SNM-39]
must_haves:
  truths:
    - "The G-MIL-07 root cause is isolated among (a) provider code-path divergence, (b) Pi-tui consumption of the AutocompleteItem[] payload, (c) getInstalledPluginToMarketplacesMap empty due to scope-root mismatch."
    - "A live scripts/pi.sh interactive `/claude:plugin update @<TAB>` trigger is run with the user to confirm the runtime behavior (the one non-automatable success criterion)."
    - "A verdict is recorded per finding: fix + regression if the cause is our code; defer-with-finding (with pi-tui line refs) if the cause is genuinely Pi-tui-external."
  artifacts:
    - path: "tests/edge/completions/provider.test.ts"
      provides: "Regression coverage / finding comment pinning our provider's correct @<mp> payload"
      contains: "update @"
    - path: ".planning/v1.4-MILESTONE-UAT.md"
      provides: "Recorded verdicts for G-MIL-07 (and G-MIL-03 cross-reference) in root_cause/artifacts fields"
      contains: "root_cause"
  key_links:
    - from: "tests/edge/completions/provider.test.ts"
      to: "getArgumentCompletions"
      via: "update @ → [@mp-a, @mp-b]"
      pattern: "getArgumentCompletions"
---

<objective>
SNM-39 (G-MIL-07 tab completion): root-cause-first reproduce-or-refute (D-25-10). `/claude:plugin update @<TAB>` returns nothing in the runtime despite the passing unit test at `tests/edge/completions/provider.test.ts:806`. Trace to the actual cause among: (a) provider code-path divergence (eliminated once v0.2.0 source loads), (b) Pi-tui consumption of the `AutocompleteItem[]` payload, (c) `getInstalledPluginToMarketplacesMap` empty due to a scope-root mismatch. The RESEARCH verdict is DEFER-WITH-FINDING (HIGH confidence): cause (b) -- pi-tui's `CombinedAutocompleteProvider.getSuggestions` checks `extractAtPrefix` BEFORE the slash-command branch, so any `@`-leading token is routed to file-mention completion and our (correct) `getArgumentCompletions` is never invoked for bare `update @`. That is `@earendil-works/pi-tui`-external -- defer, do NOT contort our code to fight the host. BUT the phase contract is reproduce-OR-refute: run the live trigger and keep the conditional fork.

Purpose: Confirm the root cause with a LIVE keystroke artifact (D-25-08 -- go straight to a live `scripts/pi.sh` session, do NOT build a keystroke harness), record the verdict, and pin our provider's correct payload as the honest regression. The live trigger is an interactive escalation to the user → this plan is `autonomous: false`.

Output: a regression-coverage / finding comment in `tests/edge/completions/provider.test.ts`, and the recorded verdicts (G-MIL-07 defer-with-finding incl. pi-tui line refs; plus a cross-reference of the G-MIL-03 verdict from plan 02) in `.planning/v1.4-MILESTONE-UAT.md` + `.planning/STATE.md`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-runtime-publish-verification/25-CONTEXT.md
@.planning/phases/25-runtime-publish-verification/25-RESEARCH.md
@.planning/phases/25-runtime-publish-verification/25-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root-cause the completion gap (a/b/c) + pin our provider's correct payload</name>
  <files>tests/edge/completions/provider.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/completions/provider.ts -- `parseUpdateMode` sets `allowMarketplaceOnly: true` for `update` (~:196); `getArgumentCompletions` (~:213) returns `AutocompleteItem[] | null`
    - extensions/pi-claude-marketplace/edge/completions/data.ts -- `getMarketplaceOnlyCompletions` (~:383), `getInstalledPluginToMarketplacesMap` (~:313, installed-plugins-only -- the G-MIL-07 precondition), `getPluginToMarketplacesMap` (~:346)
    - extensions/pi-claude-marketplace/edge/register.ts -- the DUAL completion wiring: command-registered `getArgumentCompletions(prefix, makeLocationsResolver(process.cwd()))` (~:98-99, captures cwd at ~:99) vs the `session_start` `addAutocompleteProvider` wrapper (~:107-122 -- confirm it delegates `getSuggestions` verbatim and only normalizes `applyCompletion` whitespace, so it cannot override the `@`-precedence)
    - tests/edge/completions/provider.test.ts (FULL header + the TC-6 test at ~:793-814 -- `update @` → `["@mp-a","@mp-b"]`, GREEN; the `makeFixture` builder ~:36-87; `__resetCacheForTests` usage)
    - @earendil-works/pi-tui dist/autocomplete.js -- `CombinedAutocompleteProvider.getSuggestions` (checks `extractAtPrefix` ~:191 BEFORE the slash-command branch ~:205) and `extractAtPrefix` (`PATH_DELIMITERS` has no `@`). VERIFY the GLOBAL pi-tui version at `~/.npm-global/...` (0.76.0 expected) -- `scripts/pi.sh` execs the global pi, not local `.bin` (RESEARCH Pitfall 4 + A1)
    - .planning/phases/25-runtime-publish-verification/25-RESEARCH.md (the G-MIL-07 root-cause excerpts; Pitfall 2; D-25-10 boundary; A1 version re-verify)
    - .planning/v1.4-MILESTONE-UAT.md (G-MIL-07 ~:686-721 -- recon's 4 hypotheses, `user_observation`)
  </read_first>
  <action>
    Trace the three candidate causes statically against the loaded v0.2.0 source + the GLOBAL pi-tui: (a) provider divergence is eliminated because the live runtime now loads v0.2.0 (the version the unit test covers) -- record it ruled out; (b) read the global pi-tui `getSuggestions` and `extractAtPrefix` to confirm the `@`-precedence interception (any token starting `@` after the last `PATH_DELIMITERS` boundary is routed to `getFuzzyFileSuggestions` BEFORE the slash branch, so `getArgumentCompletions("update @")` is never reached); (c) spot-check the scope-root seam (`makeLocationsResolver(process.cwd())` closing over cwd at `register.ts:99`; user scope via `getAgentDir()` reading `PI_CODING_AGENT_DIR`) to confirm whether `getInstalledPluginToMarketplacesMap` could be empty in the runtime view. Re-verify the global pi-tui version (RESEARCH A1) and note it in the SUMMARY. Confirm our side stays correct by running the existing TC-6 lock: `node --test "tests/edge/completions/provider.test.ts"` (call `__resetCacheForTests()` patterns are already in-file). Add a finding COMMENT immediately above the TC-6 test (`:793`) pointing at the pi-tui `getSuggestions` / `extractAtPrefix` line numbers and stating this test proves OUR provider is correct, the gap is host-side `@`-precedence (cause b). Do NOT add product code or contort the provider to dodge the host's `@`-precedence (D-25-10 anti-pattern -- it would degrade the documented bare-`@<mp>` UX without fixing the interception).
  </action>
  <verify>
    <automated>node --test "tests/edge/completions/provider.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - The root cause is isolated among (a)/(b)/(c) with the global pi-tui `getSuggestions`/`extractAtPrefix` line references cited; cause (a) is recorded as eliminated by the v0.2.0 source-load.
    - The global pi-tui version is re-verified at execution time (RESEARCH A1) and recorded.
    - `node --test "tests/edge/completions/provider.test.ts"` passes (TC-6 `update @` → `["@mp-a","@mp-b"]` -- our provider is correct).
    - A finding comment above TC-6 (~:793) cites the pi-tui interception lines; NO product code is added to the provider.
  </acceptance_criteria>
  <done>The completion gap is root-caused (expected: cause b, pi-tui-external) with line-level evidence; our provider's correct payload is pinned by the existing TC-6 lock + finding comment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: LIVE scripts/pi.sh interactive `update @<TAB>` trigger (interactive escalation, D-25-08)</name>
  <files>(no file written -- live keystroke artifact captured into the SUMMARY)</files>
  <read_first>
    - scripts/pi.sh (the `--home <PATH>` sandbox + `--cd <PATH>` cwd flags; bare `exec pi` → GLOBAL pi 0.76.0)
    - .planning/phases/25-runtime-publish-verification/25-CONTEXT.md (D-25-08 live-trigger; D-25-05 real-`~/.pi` false-refute guard for G-MIL-07 ONLY; D-25-02 sandbox-primary)
    - .planning/phases/25-runtime-publish-verification/25-RESEARCH.md (Pattern 3 live trigger; A2 -- a cwd with no `@`-matching files shows NOTHING (matches the original report), a cwd with files shows file paths; both confirm the same interception)
    - the Task 1 root-cause SUMMARY (so the user knows what behavior to expect)
  </read_first>
  <action>
    AUTOMATE FIRST, THEN ESCALATE. Before the checkpoint, the executor prepares a `--home <tmp>` sandbox with ≥1 installed plugin per marketplace (the G-MIL-07 precondition) via the install handler / `installTargetWithMockPi` against a fixture marketplace (mirror the `tmp/pihome` shape), and prepares a `<fixture-project>` cwd with NO `@`-matching files so the interception is unambiguous (RESEARCH A2). The executor does NOT build a programmable keystroke harness (D-25-08 forbids it). Then escalate to the user to run the live `scripts/pi.sh` interactive trigger per `<how-to-verify>` and report the keystroke result; the executor records that observation as the live artifact. Keep the conditional fork: nothing / file paths → confirms cause (b) interception → defer; `@<mp>` candidates in BOTH sandbox and real-home → refuted.
  </action>
  <what-built>
    A sandbox fixture with ≥1 installed plugin per marketplace (the G-MIL-07 precondition), built via the install handler / `installTargetWithMockPi` against a fixture marketplace under a `--home <tmp>` sandbox (mirror the `tmp/pihome` shape). The static root-cause (Task 1) is complete; this task records the LIVE keystroke artifact that confirms the host-side interception in the actual runtime.
  </what-built>
  <how-to-verify>
    1. From the project root, launch the interactive runtime against the sandbox fixture:
       `scripts/pi.sh --home <tmp-sandbox> --cd <fixture-project-with-no-@-matching-files>`
       (the executor prepares `<tmp-sandbox>` with ≥1 installed plugin per marketplace beforehand).
    2. At the Pi prompt, type exactly: `/claude:plugin update @`
    3. Press Tab to trigger completion.
    4. Observe the result and report it: did NO completion items appear (matching the original G-MIL-07 report), or did FILE PATHS appear, or did `@<mp>` marketplace candidates appear?
    5. FALSE-REFUTE GUARD (D-25-05, G-MIL-07 ONLY): if -- improbably -- `@<mp>` marketplace candidates DID appear in the sandbox, re-run a read-only spot-check against the real `~/.pi` home (no `--home` override, do not mutate) and report whether the candidates appear there too, before concluding "refuted".
    Expected per RESEARCH: nothing appears (or file paths appear) -- either outcome confirms the pi-tui `@`-precedence interception (cause b → defer-with-finding). If instead `@<mp>` candidates appear in BOTH sandbox and real-home, the finding is REFUTED → report so the verdict fork flips.
  </how-to-verify>
  <verify>
    <human-check>User runs `scripts/pi.sh --home <tmp> --cd <fixture>`, types `/claude:plugin update @`, presses Tab, and reports the result (nothing / file paths / `@<mp>` candidates); the D-25-05 real-`~/.pi` read-only spot-check is reported if `@<mp>` candidates appeared in the sandbox.</human-check>
  </verify>
  <resume-signal>Report what you saw after pressing Tab (nothing / file paths / `@<mp>` candidates), and the real-`~/.pi` spot-check result if it was needed. Then type "approved" to record the verdict.</resume-signal>
  <acceptance_criteria>
    - The executor prepared a `--home <tmp>` sandbox with ≥1 installed plugin per marketplace and a `<fixture-project>` cwd with no `@`-matching files (automate-first), without building a keystroke harness.
    - The user ran the live `/claude:plugin update @<TAB>` trigger and reported the observed result.
    - The observation (nothing / file paths / `@<mp>` candidates) is captured into the SUMMARY as the live artifact; the D-25-05 real-home spot-check result is recorded if it was triggered.
  </acceptance_criteria>
  <done>The live runtime keystroke artifact for `update @<TAB>` is captured and confirms (or refutes) the cause-(b) interception; the verdict fork is resolved by observed behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Record the verdicts (G-MIL-07 defer-with-finding + G-MIL-03 cross-ref) in UAT + STATE</name>
  <files>.planning/v1.4-MILESTONE-UAT.md, .planning/STATE.md</files>
  <read_first>
    - .planning/v1.4-MILESTONE-UAT.md (G-MIL-07 block ~:686-721 -- fill its `root_cause:` / `artifacts:` / `missing:` fields; the Triage table row ~:747; G-MIL-03 block ~:476-494 -- cross-reference plan 02's verdict here so the UAT reflects both outcomes)
    - .planning/STATE.md (Blockers/Concerns ~:142 operator-gated SNM-37 hand-off -- mark resolved; Decisions section -- add the phase verdicts; Deferred Items table -- add the pi-tui upstream finding if deferred)
    - the Task 1 root-cause SUMMARY + the Task 2 live-trigger report (the binding artifacts being recorded)
    - .planning/phases/25-runtime-publish-verification/25-RESEARCH.md (Open Question 2 -- "upstream note" = record in-repo with exact pi-tui line refs; opening an upstream issue is the user's call, out of strict phase scope)
    - .planning/phases/24-grammar-consistency/24-CONTEXT.md (D-24's finding-recording precedent to mirror)
  </read_first>
  <action>
    Record the verdicts (a verdict that isn't recorded is not done). In `.planning/v1.4-MILESTONE-UAT.md`: fill the G-MIL-07 finding's `root_cause:` with the isolated cause (expected: cause b -- pi-tui `CombinedAutocompleteProvider.getSuggestions` `@`-precedence, with the exact `getSuggestions`/`extractAtPrefix` line refs and the global pi-tui version), `artifacts:` with the live-trigger observation from Task 2 + the passing TC-6 unit test, `missing:` with the defer rationale ("`@earendil-works/pi-tui`-external; fixing in our code would degrade the bare-`@<mp>` UX without fixing the interception -- defer per SNM-39's fix-or-defer clause, D-25-10"); update the Triage row (~:747) status accordingly. Cross-reference plan 02's G-MIL-03 verdict (REFUTE + display-layer caveat, or real-bug fix) in the G-MIL-03 block so the UAT reflects both Phase-25 outcomes. In `.planning/STATE.md`: mark the operator-gated SNM-37 hand-off blocker (~:142) RESOLVED (gate satisfied via `scripts/pi.sh` source-load, plan 01); add a Decisions entry summarizing the SNM-38 REFUTE + SNM-39 defer-with-finding verdicts; add the pi-tui `@`-precedence upstream finding to the Deferred Items table (recorded in-repo with line refs; opening an upstream issue is the user's call per RESEARCH Open Q2). If the live trigger REFUTED the finding instead (improbable), record the refutation + the byte/keystroke evidence and skip the defer entry. Do NOT introduce any product/provider code change in this task.
  </action>
  <verify>
    <automated>grep -q 'CombinedAutocompleteProvider\|extractAtPrefix\|pi-tui' .planning/v1.4-MILESTONE-UAT.md && grep -vq 'Phase 25 SNM-37 publish step requires an operator action' <(grep 'operator action' .planning/STATE.md)</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v1.4-MILESTONE-UAT.md` G-MIL-07 `root_cause:` / `artifacts:` / `missing:` fields are filled with the isolated cause (incl. pi-tui line refs + version) and the live-trigger observation; the Triage row is updated.
    - The G-MIL-03 verdict (from plan 02) is cross-referenced in the UAT G-MIL-03 block so both Phase-25 outcomes are reflected.
    - `.planning/STATE.md` marks the SNM-37 operator-gated hand-off blocker RESOLVED, adds the SNM-38/SNM-39 verdict decisions, and records the pi-tui upstream finding in Deferred Items (defer path).
    - No product/provider code change is introduced.
  </acceptance_criteria>
  <done>Both Phase-25 findings have recorded verdicts (G-MIL-07 defer-with-finding incl. pi-tui line refs; G-MIL-03 cross-ref) in UAT + STATE, and the operator-gated SNM-37 blocker is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keystroke → pi-tui completion host | user input crosses into pi-tui's `CombinedAutocompleteProvider`; our provider is downstream of the host's `@`-precedence decision |
| sandbox home → real `~/.pi` | the live trigger runs under `--home <tmp>`; the D-25-05 false-refute spot-check against real `~/.pi` is READ-ONLY |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06 | Tampering | live-trigger sandbox escape to real `~/.pi` | mitigate | The live trigger runs under `--home <tmp>` (D-25-02); the only real-home access is the D-25-05 spot-check, which is read-only (no `--home` mutation). |
| T-25-07 | Repudiation (false refute) | stale completion cache hides a real result | mitigate | `__resetCacheForTests()` in the unit path + a fresh `--home` sandbox for the live trigger avoid stale 10-min plugin-index reads (RESEARCH Security row). |
| T-25-08 | Spoofing | global pi-tui version drift changes the `@`-precedence | mitigate | Task 1 re-verifies the global pi-tui version (RESEARCH A1); the verdict cites the exact version exercised. |
| T-25-09 | Tampering | contorting our provider to dodge the host `@`-precedence | mitigate | D-25-10 forbids it; no product code lands -- only a finding comment + recorded verdict. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed (RESEARCH Package Legitimacy Audit: not applicable); `node:test` built in. `ensure_global_package` only installs already-present companions if absent. |

No `high`-severity threats: read-only completion + sandboxed live trigger, no network (NFR-5), no new product code, no file writes outside the sandbox / a test comment / planning docs. Stated explicitly per the phase security directive.
</threat_model>

<verification>
- `node --test "tests/edge/completions/provider.test.ts"` passes (TC-6 proves our provider returns `["@mp-a","@mp-b"]`).
- The live `scripts/pi.sh` `update @<TAB>` trigger artifact is captured and reported (nothing / file paths / `@<mp>` candidates) with the D-25-05 spot-check if needed.
- `.planning/v1.4-MILESTONE-UAT.md` G-MIL-07 verdict fields are filled with pi-tui line refs; `.planning/STATE.md` blocker resolved + verdicts recorded.
- `npm run check` exits 0 (no product code changed; only a test comment + planning docs).
</verification>

<success_criteria>
- G-MIL-07 is conclusively reproduced-or-refuted against the v1.4 runtime via a LIVE keystroke trigger (D-25-08), with the root cause isolated among (a)/(b)/(c).
- The verdict is RECORDED per finding: defer-with-finding (pi-tui `@`-precedence, with line refs + version) if Pi-tui-external; fix + regression if our code.
- Our provider's correct `@<mp>` payload is pinned (existing TC-6 + finding comment); the operator-gated SNM-37 hand-off blocker is closed; the G-MIL-03 verdict is cross-referenced so both Phase-25 outcomes are recorded.
</success_criteria>

<output>
Create `.planning/phases/25-runtime-publish-verification/25-03-SUMMARY.md` when done. Record: the isolated root cause + global pi-tui version, the live-trigger observation, the recorded verdict per finding, and the resolution of the SNM-37 operator-gated blocker.
</output>
