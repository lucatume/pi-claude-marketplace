---
phase: 25-runtime-publish-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/shared/snm37-behavioral-smoke.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SNM-37]
must_haves:
  truths:
    - "The v0.2.0 extension source loads into Pi via scripts/pi.sh (sandbox home) and the existing e2e runtime smoke confirms load-only success."
    - "A /claude:plugin list driven through the notify-capture harness emits v1.4 catalog-conformant byte forms: no /reload trailer on the read-only list, a v#<7hex> hash display, and {lsp} not {lspServers}."
    - "SNM-37 requirement text and ROADMAP SC#1 are amended in lockstep to the scripts/pi.sh source-load + behavioral-smoke methodology."
  artifacts:
    - path: "tests/shared/snm37-behavioral-smoke.test.ts"
      provides: "Behavioral byte-form smoke that runs inside npm run check"
      contains: "claudeMarketplaceExtension"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SNM-37 amended methodology text"
      contains: "scripts/pi.sh"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 25 SC#1 amended to behavioral smoke"
      contains: "behavioral"
  key_links:
    - from: "tests/shared/snm37-behavioral-smoke.test.ts"
      to: "ctx.ui.notify"
      via: "makeMockPi/makeCtx notify-capture seam"
      pattern: "notifications"
---

<objective>
SNM-37 (the gate for SNM-38 + SNM-39): deliver the v0.2.0 (v1.4) extension source into a Pi runtime via the EXISTING `scripts/pi.sh` (sandbox home), and prove v1.4 identity with a BEHAVIORAL byte-form smoke -- not `pi --version` (moot under `-e` source-load per D-25-04). Amend the SNM-37 requirement text and ROADMAP SC#1 in the SAME commit as the work (lockstep, mirroring the Phase 23 D-23-01 / Phase 24 D-24-03 precedent).

Purpose: This is the operational gate. SNM-38 and SNM-39 cannot reproduce-or-refute against the v1.4 runtime until the v0.2.0 source is loadable and confirmed to execute the new code paths. Real `npm publish` / `npm link` is explicitly NOT used (D-25-01) and packaged-artifact validation is explicitly DEFERRED (D-25-06) -- record the deferral in the smoke test's header comment, do not silently skip it.

Output: a new gating test `tests/shared/snm37-behavioral-smoke.test.ts` (placed in `tests/shared/` so it runs inside `npm run check` -- `tests/e2e/**` is EXCLUDED from `npm test`), plus the lockstep SNM-37 / SC#1 amendments.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-runtime-publish-verification/25-CONTEXT.md
@.planning/phases/25-runtime-publish-verification/25-RESEARCH.md
@.planning/phases/25-runtime-publish-verification/25-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source-load delivery via scripts/pi.sh under a sandbox home (record outcome + deferral)</name>
  <files>tests/e2e/pi-runtime-smoke.test.ts (read-only verify; no source change in this task)</files>
  <read_first>
    - scripts/pi.sh (FULL -- verify the `-e extensions/pi-claude-marketplace/index.ts` source-load, the `ensure_global_package` companion bootstrap for `pi-mcp-adapter` + `pi-subagents`, and the `--home <PATH>` → `PI_CODING_AGENT_DIR` / `PI_CODING_AGENT_SESSION_DIR` mapping at ~:97-101, then the bare `exec pi`)
    - tests/e2e/pi-runtime-smoke.test.ts (the existing load-only e2e smoke that asserts the extension loads via the `pi` bin under isolated HOME/cwd)
    - tests/e2e/_helpers.ts (`runPiRuntimeSmoke`)
    - .planning/phases/25-runtime-publish-verification/25-RESEARCH.md (Pitfall 4: `scripts/pi.sh` execs the GLOBAL pi 0.76.0, not local `.bin`; Pitfall 5: interactive `pi` needs a TTY, the smoke does not; A1: re-verify the global `pi`/`pi-tui` version at execution time)
  </read_first>
  <action>
    Confirm the source-load delivery path works as the SNM-37 delivery mechanism (D-25-01/D-25-02) WITHOUT any `npm publish` / `npm link`. Run the existing load-only e2e smoke (`npm run test:e2e` scoped to `tests/e2e/pi-runtime-smoke.test.ts`) to confirm the v0.2.0 extension loads via the `pi` bin under an isolated HOME. Then re-verify the runtime versions per RESEARCH A1: capture `pi --version` (expect global 0.76.0) and `npm ls -g @earendil-works/pi-tui pi-mcp-adapter pi-subagents` so the SUMMARY records the exact runtime exercised. Do NOT launch the interactive `scripts/pi.sh` TTY session here (Pitfall 5) -- the binding v1.4-identity proof is the in-process behavioral smoke in Task 2 (D-25-04), and the live interactive session is reserved for the SNM-39 G-MIL-07 keystroke (plan 03, D-25-08). Record in the SUMMARY: (a) the delivery command shape `scripts/pi.sh --home <tmp> --cd <fixture-project>`; (b) that real `npm publish` / packaged-artifact (release-tarball) validation is DEFERRED per D-25-06 (the `files:` tarball + real npm install are NOT exercised; SNM-37 is reproduction-enablement, not a release gate).
  </action>
  <verify>
    <automated>npm run test:e2e 2>&1 | grep -Eq 'pass|ok' && pi --version</automated>
  </verify>
  <acceptance_criteria>
    - The existing `tests/e2e/pi-runtime-smoke.test.ts` passes (source loads via the `pi` bin under isolated HOME/cwd).
    - `pi --version` and the global `npm ls -g` versions are captured in the SUMMARY (RESEARCH A1 re-verification).
    - The SUMMARY records the `scripts/pi.sh --home <tmp> --cd <fixture-project>` delivery command shape AND the explicit D-25-06 deferral of real-publish / packaged-artifact validation (recorded, not silently skipped).
    - No `npm publish` or `npm link` is invoked anywhere in this task.
  </acceptance_criteria>
  <done>The v0.2.0 source-load delivery is confirmed via the existing load-only e2e smoke and the runtime versions are re-verified; the deferral of real-publish validation is recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Behavioral byte-form smoke test (the D-25-04 v1.4-identity proof) in tests/shared/</name>
  <files>tests/shared/snm37-behavioral-smoke.test.ts</files>
  <read_first>
    - tests/e2e/_helpers.ts (`makeMockPi` :55-81, `makeCtx` :83-98 -- the notify-capture seam pushing each `(message, severity?)` into `notifications[]`; `installTargetWithMockPi` :141-168 for the optional real-install fixture path; note `import claudeMarketplaceExtension from "../../extensions/pi-claude-marketplace/index.ts"` at :7 -- the new test lives in `tests/shared/` so its relative import path differs)
    - tests/architecture/catalog-uat.test.ts (FIXTURES for `/claude:plugin list`: `hash-version-list` ~:421-439 gives the `v#2ea95f8` form for the `v#<7hex>` assertion; `single-mp-mixed` ~:226-253 gives an installed/available mix + `{hooks, lsp}` reason brace; mirror its inline `mock.fn()` ctx OR reuse `makeCtx`)
    - package.json (lines 71, 76 -- confirm `npm test` globs `tests/{architecture,...,shared,...}/**/*.test.ts` and that `tests/e2e/**` is EXCLUDED; this is WHY the test must live in `tests/shared/`)
    - extensions/pi-claude-marketplace/shared/notify.ts (`shouldEmitReloadHint` -- present/list status excluded → no `/reload` trailer; `formatHashVersionForDisplay` → `v#<7hex>`; `composeReasons` → `lsp` member; confirm the byte forms the assertions target)
    - .planning/phases/25-runtime-publish-verification/25-PATTERNS.md (the "snm37-behavioral-smoke" assignment + fixture-shape minimums)
  </read_first>
  <behavior>
    - Test 1 (no reload trailer): a read-only `/claude:plugin list` notify body does NOT contain the substring `/reload to pick up changes` (SNM-33 reload-hint discipline; list/present status is excluded from `shouldEmitReloadHint`).
    - Test 2 (hash display): the same body matches `/v#[0-9a-f]{7}\b/` (SNM-35 `v#<7hex>` git-style short SHA) for a hash-versioned installed plugin in the fixture.
    - Test 3 (grammar): the body contains `{lsp}` and does NOT contain `lspServers` anywhere (SNM-36 emitted-Reason rename; detection substrings may stay camelCase but the emitted brace must read `lsp`).
    - Fixture minimum (Claude's discretion within locked minimums): ≥1 installed plugin per marketplace, at least one row exercising the `{...}` reason brace, an installed/available mix, and at least one `hash-<12hex>` plugin so the `v#<7hex>` assertion has a target. Reuse the `hash-version-list` + `single-mp-mixed` fixture shapes from `catalog-uat.test.ts` (pure `NotificationMessage` data path is sufficient and fastest -- no fs install needed for byte-form assertions).
  </behavior>
  <action>
    Create `tests/shared/snm37-behavioral-smoke.test.ts`. Drive the real `list` rendering through the notify-capture seam: either (Option 1, preferred -- fastest, no fs) build the `NotificationMessage` inline from the `catalog-uat` fixture shapes and call `notify(ctx, pi, message)` directly, capturing the pre-tui body from the mocked `ctx.ui.notify`; or (Option 2) call `claudeMarketplaceExtension(mock.pi)` then `command.handler("list", ctx)` and read `notifications.at(-1).message`. Capture the body at the `ctx.ui.notify` boundary (pre-tui, per D-25-09 -- never assert on post-markdown bytes, the markdown layer is exactly what introduces the false 1/3 appearance). Assert all three v1.4 byte forms named in the `<behavior>` block using `assert.doesNotMatch` / `assert.match`. Add a header comment stating this is the D-25-04 behavioral-smoke proof of v1.4 identity (stronger than `pi --version`, which is moot under `-e` source-load) and that real-publish validation is deferred (D-25-06). Adjust the relative import of `claudeMarketplaceExtension` / `_helpers` for the `tests/shared/` location. Do NOT place this test under `tests/e2e/` (it would be invisible to `npm run check` and to Phase 26's GREEN gate -- RESEARCH Pitfall 3).
  </action>
  <verify>
    <automated>node --test "tests/shared/snm37-behavioral-smoke.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/shared/snm37-behavioral-smoke.test.ts` exists and `node --test "tests/shared/snm37-behavioral-smoke.test.ts"` passes.
    - The test captures the body at `ctx.ui.notify` (pre-tui) via `makeMockPi`/`makeCtx` or the catalog-uat `mock.fn()` ctx -- NOT post-markdown bytes.
    - The body assertions are exactly: NO `/reload to pick up changes` substring; matches `/v#[0-9a-f]{7}\b/`; contains `{lsp}` and does NOT contain `lspServers`.
    - The fixture has ≥1 installed plugin per marketplace, a `{...}` reason brace row, an installed/available mix, and ≥1 hash-versioned plugin.
    - The file lives under `tests/shared/` (runs inside `npm run check`), not `tests/e2e/`.
    - `npm test 2>&1 | grep -Eq '# fail 0'` (no new failures in the in-check suite).
  </acceptance_criteria>
  <done>The behavioral byte-form smoke proves v1.4 identity at the pre-tui notify boundary and runs inside `npm run check`.</done>
</task>

<task type="auto">
  <name>Task 3: Lockstep amendment of SNM-37 text + ROADMAP SC#1 (mirrors D-23-01 / D-24-03)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (SNM-37 at :28 -- current text says "published to npm or npm-linked … verify Pi loads the new code via `pi --version` and a smoke `/claude:plugin list`")
    - .planning/ROADMAP.md (Phase 25 SC#1 at ~:476 -- "verified via `pi --version` or equivalent + a smoke `/claude:plugin list`"; also the Phase-25 ROADMAP bullet at ~:96 whose "(a) Publish/npm-link v0.2.0 … verify … via `pi --version`" half is amended)
    - .planning/phases/23-version-display-bundle/23-CONTEXT.md (D-23-01 amendment precedent) and .planning/phases/24-grammar-consistency/24-CONTEXT.md (D-24-03 amendment precedent) -- match the in-lockstep wording-correction style
    - .planning/phases/25-runtime-publish-verification/25-CONTEXT.md (D-25-03 exact replacement wording: "loaded from source via `scripts/pi.sh` (sandbox home)"; D-25-04 behavioral-smoke wording)
  </read_first>
  <action>
    Amend SNM-37's methodology text in `.planning/REQUIREMENTS.md:28` per D-25-03: replace "published to npm or npm-linked into the user's Pi runtime" (and the "publish to npm OR `npm link` from the source tree into `~/.npm-global`" methodology sentence) with "loaded from source via `scripts/pi.sh` (sandbox home)". Per D-25-04, replace the "verify Pi loads the new code via `pi --version`" half with the behavioral byte-form smoke (no `/reload` trailer on read-only list, `v#<7hex>` hash form, `{lsp}` not `{lspServers}`), noting `pi --version` is moot under `-e` source-load. Keep the requirement's PURPOSE unchanged (reproduction-enablement gating SNM-38/39). Apply the matching edit to ROADMAP SC#1 (~:476) and the Phase-25 ROADMAP bullet (~:96): the `pi --version` half becomes "+ a behavioral byte-form smoke". Note the D-25-06 deferral of real-publish validation in the amended SNM-37 text. These doc edits must land in the SAME commit as Tasks 1-2 (lockstep) -- the executor commits all three tasks together.
  </action>
  <verify>
    <automated>grep -q 'scripts/pi.sh' .planning/REQUIREMENTS.md && grep -vq 'pi --version' <(grep -A2 'SNM-37' .planning/REQUIREMENTS.md | head -3) && grep -q 'behavioral' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` SNM-37 text references `scripts/pi.sh` (sandbox home) and the behavioral byte-form smoke; the old "published to npm or npm-linked" + "`pi --version`" methodology is removed.
    - `.planning/ROADMAP.md` SC#1 (~:476) and the Phase-25 bullet (~:96) `pi --version` half is replaced with the behavioral smoke wording.
    - The SNM-37 requirement's PURPOSE (gating SNM-38/39) is unchanged; the D-25-06 real-publish deferral is noted.
    - The amendment commit also contains the Task 1-2 work (single lockstep commit, mirroring D-23-01 / D-24-03).
  </acceptance_criteria>
  <done>SNM-37 text + ROADMAP SC#1 are corrected to the actual source-load + behavioral-smoke methodology, committed in lockstep with the phase work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree → sandboxed Pi runtime | `scripts/pi.sh --home <tmp>` loads `-e` source into a Pi process; the only crossing is read-only source + sandbox env vars |
| test harness → notify boundary | `makeMockPi`/`makeCtx` captures `ctx.ui.notify` strings in-process; no real Pi process, no disk writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | `scripts/pi.sh --home` escape to real `~/.pi` | mitigate | Always pass `--home <tmp>` (D-25-02); this plan does not launch the interactive session at all -- the behavioral proof is fully in-process via the harness (no Pi process, no sandbox writes). |
| T-25-02 | Tampering/Elevation | `ensure_global_package` installs an unexpected package | accept | Both companions (`pi-mcp-adapter@2.6.1`, `pi-subagents@0.24.3`) are already globally present (verified); the function installs only if absent and introduces no new package names. Low-value, local-only. |
| T-25-03 | Spoofing | global `pi`/`pi-tui` version drift between research and execution | mitigate | Task 1 re-verifies `pi --version` + `npm ls -g` at execution time (RESEARCH A1); the SUMMARY records the exact runtime exercised. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | This phase installs NO external packages (RESEARCH Package Legitimacy Audit: not applicable); `node:test` is built in, all components in-repo. No `[ASSUMED]`/`[SUS]` packages. |

No `high`-severity threats in scope: no network (NFR-5), no new commands, no new input parsing, no new file writes outside the locked sandbox / new test file. Stated explicitly per the phase security directive.
</threat_model>

<verification>
- `npm run test:e2e` (scoped to `pi-runtime-smoke.test.ts`) passes -- v0.2.0 source loads via the `pi` bin.
- `node --test "tests/shared/snm37-behavioral-smoke.test.ts"` passes -- v1.4 byte forms present at the pre-tui notify boundary.
- `npm run check` exits 0 (the new `tests/shared/` test is inside the gate; no regressions).
- `.planning/REQUIREMENTS.md` SNM-37 + `.planning/ROADMAP.md` SC#1 reference `scripts/pi.sh` + behavioral smoke; no residual `pi --version` / "npm-linked" methodology.
</verification>

<success_criteria>
- SNM-37 gate satisfied: v0.2.0 source is loadable via `scripts/pi.sh` and the behavioral byte-form smoke proves v1.4 identity (no `/reload` trailer on read-only list, `v#<7hex>`, `{lsp}` not `{lspServers}`).
- The new smoke test runs inside `npm run check` (placed in `tests/shared/`, not `tests/e2e/`).
- SNM-37 text + ROADMAP SC#1 amended in lockstep, in the same commit; real-publish validation recorded as deferred (D-25-06).
</success_criteria>

<output>
Create `.planning/phases/25-runtime-publish-verification/25-01-SUMMARY.md` when done. Record: the delivery command shape, the re-verified runtime versions, the deferral of real-publish validation (D-25-06), and that this plan is the gate for SNM-38 (plan 02) + SNM-39 (plan 03).
</output>
