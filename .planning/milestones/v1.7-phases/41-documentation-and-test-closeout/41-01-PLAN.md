---
phase: 41-documentation-and-test-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/bridges/agents/stage.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
  - tests/bridges/agents/stage.test.ts
  - tests/orchestrators/plugin/list.test.ts
autonomous: true
requirements:
  - TR-07
  - TR-08
must_haves:
  truths:
    - "commitPreparedAgents step-1 parallel rm loop (stage.ts:328-348) carries an inline comment explaining the ENOENT-tolerant idempotency contract in plain WHY terms (pre-rm old targets; ENOENT = already gone; retry-safe)."
    - "A behavior-asserting TR-07 regression test in tests/bridges/agents/stage.test.ts drives prepare -> commit -> partial-commit-drift injection (rm target file) -> re-prepare -> commit and asserts clean final disk state, agents-index reflects truth, and the second staging dir is cleaned up."
    - "The TR-07 test asserts ONLY final disk/index state; it does NOT spy on rm calls, count function invocations, or otherwise assert intermediate implementation details."
    - "orchestrators/plugin/list.ts availableRowMessage probe-failure catch at lines 382-403 (NOT a phantom cache-drop site) carries an inline comment that leads with the D-19-01 rationale: probe failures during list are diagnostic noise, not actionable errors; the V1 PROBE_FAILURES capture-buffer + summary notifyWarning was retired in Plan 19-03; the existing narrowResolverNotes historical comparison is retained below the rationale."
    - "A TR-08 source-grep architecture test appended to the source-grep block in tests/orchestrators/plugin/list.test.ts asserts list.ts contains no PROBE_FAILURES identifier and no top-level mutable module state matching the pattern (let or var followed by an identifier at start of line)."
    - "npm run check exits 0 with no regression from the Phase 40 baseline (1366 tests -> 1368 tests after the two new tests land)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/bridges/agents/stage.ts"
      provides: "TR-07 inline WHY comment at the step-1 parallel rm loop"
      contains: "ENOENT = already gone"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/list.ts"
      provides: "TR-08 inline WHY comment at the availableRowMessage probe-failure catch (NOT a cache-drop site)"
      contains: "D-19-01"
    - path: "tests/bridges/agents/stage.test.ts"
      provides: "TR-07 behavior-asserting regression test"
      contains: "TR-07 commitPreparedAgents step-1 ENOENT-tolerance"
    - path: "tests/orchestrators/plugin/list.test.ts"
      provides: "TR-08 source-grep architecture test in the source-grep block"
      contains: "PROBE_FAILURES"
  key_links:
    - from: "extensions/pi-claude-marketplace/bridges/agents/stage.ts:328"
      to: "tests/bridges/agents/stage.test.ts (TR-07 test)"
      via: "behavior assertion on final disk + index state after partial-commit-drift retry"
      pattern: "TR-07 commitPreparedAgents step-1 ENOENT-tolerance"
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/list.ts:382-403"
      to: "tests/orchestrators/plugin/list.test.ts (TR-08 source-grep test)"
      via: "readFile + stripComments + assert.equal(code.includes('PROBE_FAILURES'), false)"
      pattern: "TR-08 / D-19-01: list.ts has no module-level PROBE_FAILURES"
---

<objective>
Close out v1.7 with the two LOW-priority docs+tests fixes (TR-07, TR-08) per
the Phase 41 ROADMAP and RESEARCH. Both target sites are already correct in
code (Phase 38 landed TR-01/TR-06; Plan 19-03 retired the V1 PROBE_FAILURES
buffer). What is missing: (a) inline WHY comments pinning each contract so
future "tidy-up" refactors cannot accidentally revert it, and (b) two
behavior-asserting regression tests that lock the contract from the outside.

Purpose: Documentation closure on two intentional self-heal / no-op-swallow
patterns, plus regression locks so the patterns survive future refactors.
The phase is purely ADDITIVE: zero control-flow changes, zero new files,
zero new dependencies.

CRITICAL clarification (per RESEARCH "Summary" + "User Constraints" #3):
the ROADMAP/CONTEXT phrase "post-state-commit cache-drop swallow in list.ts"
is a misnomer. list.ts has no cache-drop call (read-only orchestrator by
NFR-5 / PL-3 design). The actual D-19-01 swallow is the availableRowMessage
probe-failure catch at lines 382-403. TR-08 lands there.

Output:
- 2 inline comment additions/augmentations (stage.ts step-1; list.ts
  availableRowMessage catch)
- 2 new tests (TR-07 behavior-asserting in stage.test.ts; TR-08 source-grep
  in list.test.ts)
- npm run check GREEN, 1366 -> 1368 tests
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-documentation-and-test-closeout/41-CONTEXT.md
@.planning/phases/41-documentation-and-test-closeout/41-RESEARCH.md
@.planning/phases/41-documentation-and-test-closeout/41-VALIDATION.md

# Target source files (modify)
@extensions/pi-claude-marketplace/bridges/agents/stage.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/list.ts

# Target test files (append)
@tests/bridges/agents/stage.test.ts
@tests/orchestrators/plugin/list.test.ts

# Pattern donors (TR-06 test shape for TR-07; existing source-grep block for TR-08)
# Already covered by the test file imports above; reference lines:
# - tests/bridges/agents/stage.test.ts:1330-1395 (TR-06 prepare/commit/seed-state/re-prepare/commit shape)
# - tests/orchestrators/plugin/list.test.ts:855-895 (existing source-grep block + stripComments helper)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (TR-07): Inline WHY comment at step-1 rm loop + behavior-asserting regression test</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/stage.ts, tests/bridges/agents/stage.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/agents/stage.ts lines 309-348 (function JSDoc + step-1 parallel rm loop; the existing 1-liner inline at line 328 is being expanded, NOT moved).
    - tests/bridges/agents/stage.test.ts lines 1330-1395 (TR-06 pattern donor: prepare -> commit -> seed-state -> re-prepare -> replace; TR-07 mirrors this prepare -> commit -> rm target -> re-prepare -> commit shape; reuse withTmpScope, FIXTURES, makeResolved helpers already imported at the top of the file).
    - .planning/phases/41-documentation-and-test-closeout/41-RESEARCH.md "Pattern 1" + "Required new comment: TR-07 inline at step 1" sections (the exact comment wording + test sketch).
    - .planning/research/PITFALLS.md Pitfall 13 (implementation-vs-behavior testing anti-pattern -- TR-07 explicitly forbids intermediate function-call assertions).
  </read_first>
  <behavior>
    - Test 1: "TR-07 commitPreparedAgents step-1 ENOENT-tolerance enables retry-safe self-heal"
      - Setup: withTmpScope; pluginRoot = path.join(FIXTURES, "test-plugin"); resolved = makeResolved("acme", pluginRoot).
      - Cycle 1: call prepareStagePluginAgents (assert kind === "staged") then commitPreparedAgents.
      - Inject partial-commit drift: assert the bot target file exists at locations.agentsDir/pi-claude-marketplace-acme-bot.md; rm it (simulating a prior partial commit where the file was removed but the index still points at the OLD targetPath).
      - Cycle 2: call prepareStagePluginAgents again (assert kind === "staged") then commitPreparedAgents. Step 1's parallel rm loop attempts rm on the already-gone target; ENOENT swallow lets step 2 proceed.
      - Behavior assertions (final state ONLY, NO intermediate spies, NO call counts):
        * pathExists(targetPath) === true after retry ("bot.md must exist at target after retry").
        * agents-index.json has exactly one row whose generatedName === "pi-claude-marketplace-acme-bot".
        * pathExists(prepared2.stagingDir) === false ("staging dir cleaned up after retry").
    - Test MUST NOT spy on rm, MUST NOT assert how many times step 1 ran, MUST NOT count Promise.all iterations.
  </behavior>
  <action>
    Per TR-07. CONTEXT.md marks implementation choices as Claude's discretion, so no D-NN IDs apply at the file level; this task implements TR-07 verbatim.

    Sub-step 1 -- stage.ts inline comment (purely additive; no control-flow change):
    - At extensions/pi-claude-marketplace/bridges/agents/stage.ts, REPLACE the current 3-line inline comment at lines 328-330 (which only says "rm ONLY safe-to-overwrite previous target files; _foreignPreservedEntries is INTENTIONALLY excluded ...") with the expanded TR-07 WHY block defined in RESEARCH "Required new comment: TR-07 inline at step 1".
    - The new comment MUST:
      * Open with "TR-07 / Phase 41:" so the requirement ID is grep-discoverable from source.
      * State the ENOENT-tolerant idempotency contract in plain terms: pre-rm OLD plugin-owned targets; ENOENT means "already gone" -- the only way ENOENT fires is if a prior partial commit already removed the target, in which case the second pass is a no-op and step 2 proceeds with the new rename. This is retry-safe by construction.
      * Cross-reference the function JSDoc at lines 309-320 (the function-level self-heal property).
      * Explain WHY the loop stays parallel: step 1 has no source to roll back to (those files were never backed up -- commitPreparedAgents is the commit path, not the replacePreparedAgents backup path); rollback would have nothing to restore.
      * Retain the existing _foreignPreservedEntries clarification ("INTENTIONALLY excluded -- those targets stay untouched on disk and their rows stay in the index").
    - The comment is placed at the same scroll position as the code it describes (immediately above try { await Promise.all(...) at line 331) so a future reader sees the rationale next to the ENOENT-swallow code. Do NOT add a duplicate rationale at the function JSDoc; the JSDoc layer (function-level) and the inline layer (rm-loop level) are intentionally separated per the RESEARCH anti-pattern "Adding a third 'ENOENT swallow rationale' comment elsewhere ... dilutes the signal".
    - The code body (lines 331-348) is byte-unchanged.

    Sub-step 2 -- TR-07 regression test (purely additive; appended to existing file):
    - At tests/bridges/agents/stage.test.ts, APPEND a new test(...) block after the existing TR-06 test (which ends at line 1395). Add a one-line section separator comment above it ("// TR-07 step-1 ENOENT-tolerance regression test ----------------").
    - Test name: "TR-07 commitPreparedAgents step-1 ENOENT-tolerance enables retry-safe self-heal" (matches the TR-01 / TR-06 naming precedent in the same file -- requirement ID + function + behavior).
    - Body MUST follow the RESEARCH "Example test shape" sketch verbatim in intent (test mechanics may use the locally-idiomatic helpers already imported in the file). Specifically: withTmpScope -> prepareStagePluginAgents (cycle 1) -> commitPreparedAgents -> assert bot target exists -> rm target (the partial-commit drift injection) -> prepareStagePluginAgents (cycle 2) -> commitPreparedAgents -> assert (a) bot target exists at the final path, (b) agents-index.json contains exactly one row whose generatedName === "pi-claude-marketplace-acme-bot", (c) prepared2.stagingDir does NOT exist (cleanup happened).
    - Use only behavior-level assertions (pathExists / readFile / index row count). No mock.method, no spy on rm, no Promise.all-call-count assertions. If a future refactor swaps Promise.all for Promise.allSettled (still ENOENT-tolerant), this test MUST stay GREEN. Pitfall 13 is the explicit hazard -- do not regress into implementation-asserting shapes.
    - Reuse already-imported helpers: withTmpScope, FIXTURES, makeResolved, prepareStagePluginAgents, commitPreparedAgents, pathExists, rm (from node:fs/promises), readFile, path, assert. Do NOT add new top-level imports unless an already-needed helper is genuinely missing.

    The whole change is ONE atomic commit per task. Conventional Commits: "docs(bridges): TR-07 inline WHY comment + behavior-asserting test for commitPreparedAgents step-1 ENOENT-tolerance". Run pre-commit run --files extensions/pi-claude-marketplace/bridges/agents/stage.ts tests/bridges/agents/stage.test.ts BEFORE git commit; fix Prettier/ESLint failures and re-stage. NEVER use --no-verify. No SKIP=trufflehog (not a worktree).
  </action>
  <verify>
    <automated>npm test -- tests/bridges/agents/stage.test.ts 2>&1 | tail -20 && grep -c "TR-07 commitPreparedAgents step-1 ENOENT-tolerance" tests/bridges/agents/stage.test.ts && grep -c "TR-07 / Phase 41" extensions/pi-claude-marketplace/bridges/agents/stage.ts</automated>
  </verify>
  <done>
    - extensions/pi-claude-marketplace/bridges/agents/stage.ts has the multi-line TR-07 WHY comment immediately above the step-1 try { await Promise.all(...) block; the comment names the requirement ID ("TR-07 / Phase 41"), states the ENOENT-tolerant idempotency contract, references the function JSDoc, explains why the loop stays parallel, and retains the _foreignPreservedEntries clarification. Lines 331-348 are byte-unchanged.
    - tests/bridges/agents/stage.test.ts has a new test("TR-07 commitPreparedAgents step-1 ENOENT-tolerance enables retry-safe self-heal", ...) appended after the TR-06 test (line 1395). The test follows the prepare -> commit -> rm target -> re-prepare -> commit -> assert-final-state shape and asserts ONLY (a) bot target exists at the final path, (b) the index has exactly one row for the bot agent, (c) the second staging dir is cleaned up. No spies, no call counts.
    - npm test -- tests/bridges/agents/stage.test.ts exits 0; the new TR-07 test name appears in the output and PASSES; all pre-existing tests in the file still pass.
    - The grep gates above both return at least 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (TR-08): Augment WHY comment at availableRowMessage probe-failure catch + source-grep architecture test</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/list.ts, tests/orchestrators/plugin/list.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts lines 370-403 (the availableRowMessage catch -- the ACTUAL D-19-01 swallow site; the ROADMAP/CONTEXT phrasing "post-state-commit cache-drop swallow" is a confirmed misnomer per RESEARCH "Summary" + "User Constraints (from CONTEXT.md) - Locked Decisions #3". list.ts has NO cache-drop call -- it is a read-only orchestrator by NFR-5 / PL-3.)
    - tests/orchestrators/plugin/list.test.ts lines 855-895 (the existing source-grep block: section header comment, stripComments helper, and three precedent source-grep tests -- TR-08 appends inside this block after line 895, BEFORE the line-897 "Uncovered-path gap tests" section).
    - .planning/phases/41-documentation-and-test-closeout/41-RESEARCH.md "Pattern 2" + "Required new comment: TR-08 augmented inline at probe-failure catch" sections (the exact comment + test sketch).
    - RESEARCH "Common Pitfalls" Pitfall 2 (TR-08 regex MUST be limited to let|var at start of line, NOT including const, to avoid false-positive on the legitimate const SYNTHETIC_LIST_FAILURE_MARKETPLACE_NAME = "(list)" at list.ts:915).
  </read_first>
  <behavior>
    - Test 1: "TR-08 / D-19-01: list.ts has no module-level PROBE_FAILURES-style accumulator"
      - readFile("extensions/pi-claude-marketplace/orchestrators/plugin/list.ts", "utf8") -> stripComments(src).
      - Assertion A (direct identifier match): assert.equal(code.includes("PROBE_FAILURES"), false, "list.ts must not contain a PROBE_FAILURES identifier").
      - Assertion B (generic top-level mutable-state heuristic): match the source for top-of-line let/var declarations (regex anchored to start-of-line, matching "let" or "var" followed by whitespace and an identifier, multiline mode); the match array MUST be empty. Assertion failure message includes the offending matches for debuggability.
      - Both assertions execute every run; the test is a single test(...) block (matches the precedent in the same source-grep block -- one test per architectural invariant).
  </behavior>
  <action>
    Per TR-08. The inline comment cites D-19-01 by name (the decision that retired the V1 PROBE_FAILURES capture-buffer + drain notifyWarning in Plan 19-03).

    Sub-step 1 -- list.ts inline comment augmentation (purely additive; no control-flow change):
    - At extensions/pi-claude-marketplace/orchestrators/plugin/list.ts, REPLACE the current ~13-line block-comment inside the catch (probeErr) block at lines 383-395 with the augmented TR-08 WHY block per RESEARCH "Required new comment: TR-08 augmented inline at probe-failure catch".
    - The new comment MUST lead with the RATIONALE for reader-first ordering -- "TR-08 / D-19-01: per-row probe-failure narrowing. Probe failures during list are diagnostic noise, NOT actionable user errors -- the user sees the cause class on the (unavailable) row's reasons[] and decides whether to act. The V1 PROBE_FAILURES module-level capture-buffer + summary notifyWarning was retired by Plan 19-03 (the buffer had no other consumer; the summary was redundant with per-row signal)."
    - Then retain the existing historical comparison ("The previous implementation routed EVERY throw through narrowResolverNotes, which only recognises hooks and lspServers and silently degraded everything else to {unsupported source}. That hid EACCES, JSON parse failures, and programming bugs behind a misleading reason. ..."). The historical comparison is augment-in-place per RESEARCH Q3 ADOPTED.
    - Close with a forward reference to the new TR-08 architecture test: "TR-08 architecture test at tests/orchestrators/plugin/list.test.ts asserts no module-level PROBE_FAILURES-style state may reappear."
    - The catch-body code (the const reason = narrowProbeError(probeErr); return { status: "unavailable", ... }; lines after the comment block) is byte-unchanged.
    - CRITICAL: this lands at the availableRowMessage probe-failure catch (lines 382-403), NOT at any phantom cache-drop site. list.ts has no cache-drop call. If you cannot find a cache-drop site, that confirms the RESEARCH clarification -- proceed at the availableRowMessage catch.

    Sub-step 2 -- TR-08 source-grep architecture test (purely additive; appended inside existing block):
    - At tests/orchestrators/plugin/list.test.ts, APPEND one new test(...) block immediately after the existing "D-04 corollary: list.ts does not use withStateGuard (read-only)" test (which ends at line 895) and BEFORE the section separator at line 897 ("// Uncovered-path gap tests"). Stay inside the existing "Source-grep self-tests" block so future contributors see all source-grep invariants together (locality per RESEARCH Q1 ADOPTED).
    - Test name: "TR-08 / D-19-01: list.ts has no module-level PROBE_FAILURES-style accumulator".
    - Body uses the existing readFile import + the file-local stripComments helper at line 863 (do NOT extract a new helper -- single-test addition does not warrant abstraction per "Don't Hand-Roll" in RESEARCH).
    - Implement BOTH assertions per behavior block above:
      (1) Direct identifier match against "PROBE_FAILURES" in the comment-stripped source.
      (2) Generic top-level mutable-state heuristic: a multiline regex anchored to start-of-line matching let or var followed by whitespace and an identifier (specifically: ^(let|var)\\s+\\w+ with the g and m flags); the match array MUST be empty.
    - The regex MUST be restricted to let|var (the const keyword is INTENTIONALLY omitted) -- per the Pitfall 2 rationale: const SYNTHETIC_LIST_FAILURE_MARKETPLACE_NAME = "(list)" at list.ts:915 is a legitimate module-level constant (deliberate, immutable, non-accumulating); only let/var are the anti-pattern. The defense-in-depth pair (named-identifier + structural heuristic) catches reintroduction by name OR by shape.
    - Reuse already-imported helpers: readFile (from node:fs/promises), assert (from node:assert/strict), test (from node:test), and the file-local stripComments. Do NOT add new top-level imports.

    The whole change is ONE atomic commit per task. Conventional Commits: "docs(orchestrators): TR-08 augment availableRowMessage WHY comment + source-grep test for D-19-01 PROBE_FAILURES retirement". Run pre-commit run --files extensions/pi-claude-marketplace/orchestrators/plugin/list.ts tests/orchestrators/plugin/list.test.ts BEFORE git commit; fix any Prettier/ESLint failures and re-stage. NEVER use --no-verify. No SKIP=trufflehog (not a worktree).
  </action>
  <verify>
    <automated>npm test -- tests/orchestrators/plugin/list.test.ts 2>&1 | tail -20 && grep -c "TR-08 / D-19-01: list.ts has no module-level PROBE_FAILURES" tests/orchestrators/plugin/list.test.ts && grep -c "TR-08 / D-19-01" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts</automated>
  </verify>
  <done>
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts catch (probeErr) block at lines 382-403 has the augmented multi-line comment leading with "TR-08 / D-19-01: per-row probe-failure narrowing. Probe failures during list are diagnostic noise ...", followed by the retained narrowResolverNotes historical comparison, and closing with the forward reference to the TR-08 architecture test. The catch-body executable code (narrowProbeError + return PluginUnavailableMessage) is byte-unchanged.
    - tests/orchestrators/plugin/list.test.ts has a new test("TR-08 / D-19-01: list.ts has no module-level PROBE_FAILURES-style accumulator", ...) appended inside the source-grep block, AFTER the "D-04 corollary" test and BEFORE the "Uncovered-path gap tests" separator. The test runs both assertions (direct identifier match + top-level let/var heuristic).
    - npm test -- tests/orchestrators/plugin/list.test.ts exits 0; the new TR-08 test PASSES; the pre-existing source-grep tests + Uncovered-path gap tests all still pass.
    - The grep gates above both return at least 1.
  </done>
</task>

<task type="auto">
  <name>Task 3 (Phase Gate): npm run check GREEN; verify 1366 -> 1368 test count</name>
  <files>(no source edits)</files>
  <read_first>
    - .planning/phases/41-documentation-and-test-closeout/41-VALIDATION.md (Per-Task Verification Map row 41-01-03: npm run check is the phase gate).
    - .planning/phases/41-documentation-and-test-closeout/41-RESEARCH.md "Validation Architecture" + "Phase Requirements -> Test Map" sections (baseline 1366 tests; expected delta +2).
    - tests/bridges/agents/stage.test.ts and tests/orchestrators/plugin/list.test.ts (verify the two new tests landed in Tasks 1+2).
  </read_first>
  <action>
    Phase-gate verification only. No source or test edits.

    1) Run pre-commit run --all-files one more time to verify Prettier/ESLint/typecheck hygiene across the full repo (the Task 1+2 commits already verified their own scope, but the all-files sweep catches cross-file drift). Fix and re-stage anything that fails; commit any fixes as a separate "chore" commit if needed (do NOT amend Task 1 or Task 2 commits).
    2) Run npm run check. Expected: exits 0; typecheck + ESLint + Prettier + tests all GREEN. Test count: 1366 (Phase 40 baseline) + 2 (new TR-07 + TR-08 tests) = 1368. If the count is different, the diff MUST be only the +2 new tests; any additional change is a regression to investigate.
    3) Capture the final test count from the node:test output (the summary line shows tests/pass/fail counters); record it in the phase SUMMARY for traceability.
    4) Confirm no regression from Phase 40: ALL pre-existing tests must remain green; no test renamed, removed, or skipped.

    Do NOT modify any source file or test file in this task. If npm run check FAILS, the failure is a regression introduced by Task 1 or Task 2 -- diagnose the failure, return to the offending task, and apply a fix as a NEW commit (NEVER use git commit --amend per CLAUDE.md). Re-run pre-commit and npm run check after every fix.
  </action>
  <verify>
    <automated>npm run check 2>&1 | tail -30</automated>
  </verify>
  <done>
    - npm run check exits 0 (typecheck + ESLint + Prettier + tests all GREEN).
    - Test count is 1368 (1366 Phase 40 baseline + 2 new tests).
    - No pre-existing tests are skipped, renamed, or removed.
    - The phase SUMMARY records the final test count.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none -- comments + tests only) | Phase 41 is purely additive documentation and regression-test work. It introduces no new user input parsing, no new file I/O paths, no new subprocess invocation, no new network surface, no new auth or crypto code. The existing trust boundaries of stage.ts (rename atomicity at the bridges layer) and list.ts (read-only orchestrator by NFR-5 / PL-3) are byte-unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering (information integrity) | TR-07 inline comment at bridges/agents/stage.ts:328 | mitigate | The TR-07 behavior-asserting test in tests/bridges/agents/stage.test.ts exercises exactly the behavior the comment describes (prepare -> commit -> rm-drift -> re-prepare -> commit -> clean final state). If a future refactor changes step 1 behavior (e.g., expands the target set, drops ENOENT-tolerance, or stops being retry-safe), the test fails, forcing the contributor to either revert the behavior change or update the comment. The test is the comment's honesty guarantor (Pitfall 3 in RESEARCH). |
| T-41-02 | Tampering (information integrity) | TR-08 inline comment at orchestrators/plugin/list.ts:386 | mitigate | The TR-08 source-grep architecture test in tests/orchestrators/plugin/list.test.ts asserts the structural invariant the comment describes (no module-level PROBE_FAILURES identifier; no top-level let/var mutable state). A future refactor that reintroduces the V1 capture-buffer pattern -- by name or by shape -- fails the test. Defense-in-depth pair (named-identifier match + structural heuristic) catches both regression vectors. |
| T-41-03 | Tampering | npm/pip/cargo installs | accept | RESEARCH "Package Legitimacy Audit" section: "Not applicable -- Phase 41 installs no new packages. All work uses already-installed Node built-ins (fs/promises, path, test, assert/strict) and existing project modules." No package legitimacy gate is required (no new dependencies introduced; v1.7 milestone-wide verdict in .planning/research/SUMMARY.md). |
</threat_model>

<verification>
Per-task automated commands (see each task's <verify> block):

- Task 1: `npm test -- tests/bridges/agents/stage.test.ts` + grep gates on the test name and source comment marker.
- Task 2: `npm test -- tests/orchestrators/plugin/list.test.ts` + grep gates on the test name and source comment marker.
- Task 3: `npm run check` (full repo: typecheck + ESLint + Prettier + tests).

Phase-gate verification matches VALIDATION.md row 41-01-03: `npm run check` GREEN with 1368 tests (1366 Phase 40 baseline + 2 new tests).
</verification>

<success_criteria>
1. bridges/agents/stage.ts has a multi-line TR-07 WHY comment immediately above the step-1 try { await Promise.all(...) block at line 331. The comment opens with "TR-07 / Phase 41:", states the ENOENT-tolerant idempotency contract, references the function JSDoc, and retains the _foreignPreservedEntries clarification. Lines 331-348 of stage.ts are byte-unchanged.
2. orchestrators/plugin/list.ts catch (probeErr) block at lines 382-403 has an augmented multi-line comment leading with "TR-08 / D-19-01: per-row probe-failure narrowing. Probe failures during list are diagnostic noise ...", retaining the existing narrowResolverNotes historical comparison, and closing with a forward reference to the TR-08 architecture test. The catch-body executable code is byte-unchanged. NOTE: the comment lands at availableRowMessage probe-failure catch, NOT a phantom cache-drop site (which does not exist in list.ts -- read-only by NFR-5 / PL-3).
3. tests/bridges/agents/stage.test.ts has a new behavior-asserting test "TR-07 commitPreparedAgents step-1 ENOENT-tolerance enables retry-safe self-heal" appended after the TR-06 test (line 1395). The test drives prepare -> commit -> rm-target -> re-prepare -> commit and asserts ONLY (a) bot target exists at the final path, (b) the agents-index has exactly one row for the bot, (c) the second staging dir is cleaned up. NO spies, NO call counts, NO Promise.all-iteration assertions.
4. tests/orchestrators/plugin/list.test.ts has a new source-grep test "TR-08 / D-19-01: list.ts has no module-level PROBE_FAILURES-style accumulator" appended inside the source-grep block (after the "D-04 corollary" test at line 895, before the "Uncovered-path gap tests" separator at line 897). The test asserts (a) no PROBE_FAILURES identifier in the comment-stripped source, AND (b) no top-of-line let/var declarations matching the heuristic.
5. npm run check exits 0; total test count is 1368 (1366 Phase 40 baseline + 2 new tests); no pre-existing test is skipped, renamed, or removed.
</success_criteria>

<output>
Create `.planning/phases/41-documentation-and-test-closeout/41-01-SUMMARY.md` when done. Include:
- Task-by-task close-out narrative (TR-07 comment + test; TR-08 comment + test; phase gate).
- Confirmation that TR-08 landed at availableRowMessage (lines 382-403), NOT at a phantom cache-drop site.
- Final test count (expected 1368) captured from the npm run check output.
- Closure of TR-07 and TR-08 in REQUIREMENTS.md (mark Pending -> Complete; or note that the executor flips the traceability row).
</output>
</content>
