---
phase: 40-update-state-before-commit-reorder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - tests/orchestrators/plugin/update.test.ts
autonomous: true
requirements: [TR-04]
requirements_addressed: [TR-04]

must_haves:
  truths:
    - "[SC#1] `markUpdateInProgress(args, preflight)` runs INSIDE a `withStateGuard` BEFORE phase-3a; it is the ONLY state write that touches disk before commits begin. It sets `sRecord.compatibility = { installable: false, notes: [UPDATE_IN_PROGRESS_NOTE], supported: [...sRecord.compatibility.supported], unsupported: [...sRecord.compatibility.unsupported] }`. ST-9 stale-version check (`sRecord.version !== fromVersion`) lives here (moved from the retired swapStateRecord). Version + resources + resolvedSource + updatedAt are NOT mutated in this window."
    - "[SC#2 -- per-bridge] `finalizeUpdateRecord(args, preflight, handles, phase3aFailures)` runs INSIDE a second `withStateGuard` AFTER phase-3a; for each bridge in {skills, commands, agents, mcp}, when `!failedPhases.has(bridge)`, it writes `sRecord.resources.<schemaField>` from `handles.<bridge>.result.recorded.map(r => r.generatedName)` -- INDEPENDENT of other bridges' outcomes. The bridge -> schema-field mapping is locked: skills -> resources.skills, commands -> resources.prompts, agents -> resources.agents, mcp -> resources.mcpServers (commands -> prompts asymmetry inherited from Phase 39 / TR-03)."
    - "[SC#2 -- all-or-nothing] In `finalizeUpdateRecord`, version bump (`sRecord.version = toVersion`), `sRecord.compatibility = { installable: true, notes: [...installable.notes], supported: [...installable.supported], unsupported: [...installable.unsupported] }`, and `sRecord.resolvedSource = installable.pluginRoot` happen ONLY when `phase3aFailures.length === 0`. On any failure, version stays at `fromVersion`, `compatibility` stays at the intent-mark values (installable:false + notes:[UPDATE_IN_PROGRESS_NOTE]), resolvedSource stays at the pre-update path. `sRecord.updatedAt = new Date().toISOString()` is set on BOTH branches."
    - "[SC#3 -- D-03 preserved] The four `try { await commitPrepared*(handles.X) } catch { phase3aFailures.push({ phase: X, msg, cause }) }` blocks at update.ts:885-923 are byte-identical post-fix: all four bridges attempt regardless of individual failures; `phase3aFailures: Phase3Failure[]` accumulates them; the existing recovery-hint emission (lines 927-963, `RECOVERY_PLUGIN_REINSTALL_PREFIX` + `notifyDirectFailure({ reasonOverride: 'rollback partial', rollbackPartial: phase3aFailures })` + `partition='failed'` outcome with `phaseFailures`) fires on `phase3aFailures.length > 0`."
    - "[Lock discipline / D-06] TWO separate `withStateGuard` calls (intent-mark + finalize) bracket phase-3a; phase-3a renames run WITHOUT the per-scope `.state-lock` held -- the intent-mark's `installable: false` + `notes: [UPDATE_IN_PROGRESS_NOTE]` IS the cross-process coordination signal. NO `withLockedStateTransaction` is introduced; NO ST-9 re-check inside finalize (rejected per Open Q1 RESOLVED)."
    - "[Finalize-failure routing] If `finalizeUpdateRecord` throws, a synthetic `Phase3Failure` entry { phase: 'mcp', msg: `state finalize failed: ${errorMessage(err)}`, cause: err } is pushed into `phase3aFailures`, routing through the existing recovery-hint pipeline. NO `shared/errors.ts` schema change (no `phase: 'finalize'` member). An inline comment at the catch site references Pitfall 4 + Assumption A3."
    - "[swapStateRecord retired] The current `swapStateRecord(args, preflight, handles)` function at update.ts:781-823 is DELETED; its only caller at line 868 is replaced by the `markUpdateInProgress` + finalize sequence."
    - "[PUP-6 trigger preserved] The PUP-6 test at update.test.ts:744 uses `writeFile(path.join(locations.skillsTargetDir, 'hello-tool'), 'obstacle')`; Phase 38's `removeOrphanIfPresent` is kind-strict (tree-mode skips file kinds) AND only rms state.json-membership orphans (the literal 'obstacle' is NOT in any plugin's resources list), so the obstacle stays intact and `commitPreparedSkills` still throws ENOTDIR. Phase 40 does NOT modify any bridge commit path or any `replacePrepared*` helper. `notifications.length === 1` invariant (CR-01 single-emission) is preserved byte-identically."
    - "[Existing tests augmented, NOT replaced] PUP-6 (update.test.ts:744), phase3a-commands-fail (1584), phase3a-agents-fail (1641), and WR-04 (817) all keep their existing seeds, obstacles, and notification-match assertions. New post-state assertions are APPENDED via `const after = await loadState(locations.extensionRoot); const rec = after.marketplaces['mp']?.plugins['hello']; assert.ok(rec !== undefined);` plus per-axis equality checks."
    - "[PUP-3 untouched] PUP-3 at update.test.ts:265 stays byte-identical (no new assertions). Preflight short-circuits at update.ts:830-833 BEFORE any withStateGuard call, so the existing `before === after` state assertion remains accurate."
    - "[SC#4 -- 4-bridge x 2-outcome matrix] Four NEW unit tests are appended to update.test.ts, each forcing exactly ONE bridge to fail while the other three succeed. Per-bridge orthogonality of the finalize logic makes the remaining 12 combinatorial cases compose deterministically; a comment block at the top of the matrix block documents this coverage attestation and cross-links to .planning/phases/40-update-state-before-commit-reorder/40-RESEARCH.md (Assumption A5)."
    - "[SC#5 -- retry] One NEW two-call retry test is appended to update.test.ts. Call 1 = PUP-6 shape (skills obstacle file at `<skillsTargetDir>/hello-tool`); between calls the obstacle is rm'd and a pre-call-2 state assertion verifies `version === '1.0.0'`, `compatibility.installable === false`, `compatibility.notes.includes('update-in-progress')`. Call 2 runs against the same target + manifest and reaches `version === '1.0.1'`, `installable === true`, all four resources reflect new generated names, AND `compatibility.notes` does NOT include 'update-in-progress' (no-leak assertion)."
    - "[SC#6] `npm run check` exits 0 (typecheck + lint + format:check + test). Test count moves up from the 1362 post-Phase-39 baseline by approximately 5 net new (4 matrix + 1 retry) -- existing tests are AUGMENTED in place, not multiplied."
    - "[Naming + module constants] `UPDATE_IN_PROGRESS_NOTE = 'update-in-progress'` and `PHASE3_FAILURE_PHASES = ['skills', 'commands', 'agents', 'mcp'] as const` plus `type Phase3Phase = (typeof PHASE3_FAILURE_PHASES)[number]` are declared at module scope in update.ts. Helper names are exactly `markUpdateInProgress` and `finalizeUpdateRecord` per CONTEXT.md SC#1/#2."
    - "[A1 -- notify.ts spot-check] `shared/notify.ts` does NOT consume `compatibility.notes` (verified by grep: zero hits for `compatibility.notes` outside the persistence schema). The intent-mark marker is internal-only; no `tests/shared/notify*.test.ts` regression risk."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/update.ts"
      provides: "Module-level constants UPDATE_IN_PROGRESS_NOTE + PHASE3_FAILURE_PHASES + Phase3Phase type; markUpdateInProgress + finalizeUpdateRecord helpers; rewired runThreePhaseUpdate (intent-mark BEFORE phase-3a, finalize AFTER phase-3a with try/catch routing through phase3aFailures synthetic 'mcp' entry); deleted swapStateRecord function."
      contains: "markUpdateInProgress"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/update.ts"
      provides: "finalizeUpdateRecord helper -- per-bridge `if (!failedPhases.has('skills'))` gating + all-or-nothing `if (phase3aFailures.length === 0)` version bump"
      contains: "finalizeUpdateRecord"
    - path: "tests/orchestrators/plugin/update.test.ts"
      provides: "PUP-6 / phase3a-commands-fail / phase3a-agents-fail / WR-04 augmented with post-state assertions; PUP-3 untouched; 4 new matrix tests (skills-only / commands-only / agents-only / mcp-only fail); 1 new retry test"
      contains: "TR-04 matrix"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/update.ts::runThreePhaseUpdate"
      to: "markUpdateInProgress (BEFORE phase-3a) + finalizeUpdateRecord (AFTER phase-3a)"
      via: "two sequential withStateGuard calls bracketing the four bridge commits"
      pattern: "markUpdateInProgress[\\s\\S]*?commitPreparedSkills[\\s\\S]*?finalizeUpdateRecord"
    - from: "finalizeUpdateRecord per-bridge filter"
      to: "handles.commands -> sRecord.resources.prompts (NOT resources.commands -- mirrors Phase 39 TR-03 asymmetry)"
      via: "explicit per-bridge if-blocks with inline comment naming the mapping"
      pattern: "sRecord\\.resources\\.prompts.*handles\\.commands"
    - from: "finalizeUpdateRecord version bump"
      to: "phase3aFailures.length === 0 gate"
      via: "single all-or-nothing branch wrapping version + compatibility + resolvedSource writes"
      pattern: "phase3aFailures\\.length === 0"
    - from: "runThreePhaseUpdate finalize catch"
      to: "phase3aFailures.push({ phase: 'mcp', msg: 'state finalize failed: ...' })"
      via: "synthetic Phase3Failure entry routed through the existing recovery-hint pipeline"
      pattern: "state finalize failed"
    - from: "PUP-6 / phase3a-* / WR-04 post-state assertions"
      to: "loadState(locations.extensionRoot) -> marketplaces.mp.plugins.hello.{version, compatibility, resources}"
      via: "appended assertion block after the existing notification-match block"
      pattern: "after\\.marketplaces\\[.mp.\\]\\?\\.plugins\\[.hello.\\]"
---

<objective>
Close TR-04 (state-write-before-commit reorder). Replace `swapStateRecord` in
`orchestrators/plugin/update.ts` with two helpers: `markUpdateInProgress`
(pre-commit intent-mark setting `compatibility.installable = false` + `notes:
[UPDATE_IN_PROGRESS_NOTE]`, plus the ST-9 stale-version check) and
`finalizeUpdateRecord` (post-commit per-bridge resource update independent
across bridges; all-or-nothing version bump gated on
`phase3aFailures.length === 0`). Rewire `runThreePhaseUpdate` (lines 867-989) so
the intent-mark window runs BEFORE phase-3a and the finalize window runs AFTER
phase-3a but BEFORE the existing phase-3b recovery-hint emission. Route a
finalize-throw as a synthetic `phase: "mcp"` `Phase3Failure` entry so the
existing `notifyDirectFailure` + `partition='failed'` pipeline fires unchanged.
D-03 continue-on-failure semantics (all four bridges attempt regardless of
individual failures) are preserved byte-identically.

Purpose: Eliminate the F4 / Pitfall 4 / Pitfall 12 state-write-before-commit
divergence -- post-Phase-40, state.json reflects truthful on-disk content for
every phase-3a outcome (succeeded bridges' resources committed to state, failed
bridges' resources unchanged), version bumps only on the all-success path, and
the intent-mark marker (installable:false + notes:[update-in-progress])
survives a process crash mid-commit so the next `/reload` + retry sees the
truthful prior version and the `RECOVERY_PLUGIN_REINSTALL_PREFIX` recovery hint
is structurally mirrored on disk. NFR-3 retry-safety is enforced end-to-end via
the SC#5 retry test.

Output: One source-file edit (`update.ts`) + one test-file edit
(`update.test.ts`) with 4 in-place augmentations (PUP-6, phase3a-commands-fail,
phase3a-agents-fail, WR-04) + 5 new tests (4 matrix + 1 retry) + a regression
gate via `npm run check` that confirms the full suite reaches ~1367 GREEN tests
(post-Phase-39 baseline 1362 + 5 net new).
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@.planning/phases/40-update-state-before-commit-reorder/40-CONTEXT.md
@.planning/phases/40-update-state-before-commit-reorder/40-RESEARCH.md
@.planning/phases/40-update-state-before-commit-reorder/40-VALIDATION.md
@.planning/phases/39-cascade-ghost-record/39-RESEARCH.md
@.planning/phases/38-sequential-commit-loops-orphan-tolerance/38-RESEARCH.md
@extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
@extensions/pi-claude-marketplace/transaction/with-state-guard.ts
@extensions/pi-claude-marketplace/persistence/state-io.ts
@extensions/pi-claude-marketplace/shared/errors.ts
@tests/orchestrators/plugin/update.test.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add markUpdateInProgress + finalizeUpdateRecord helpers + module constants (NO wiring change yet)</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  </files>

  <read_first>
    extensions/pi-claude-marketplace/orchestrators/plugin/update.ts in full (1653 lines; specifically: imports at lines 53-118, ThreePhaseArgs at 504-544, PrepHandles at 546-551, PluginPreflight at 553-560, current swapStateRecord at 781-823, current runThreePhaseUpdate at 825-1010 with phase-3a try/catch blocks at 885-923 and recovery-hint emission at 927-963),
    extensions/pi-claude-marketplace/transaction/with-state-guard.ts (66-76 -- ST-7 contract "save only on no-throw"),
    extensions/pi-claude-marketplace/persistence/state-io.ts (lines 38-55 -- PLUGIN_INSTALL_RECORD_SCHEMA confirming compatibility = { installable, notes, supported, unsupported } and resources = { skills, prompts, agents, mcpServers }),
    extensions/pi-claude-marketplace/shared/errors.ts (lines 269-278 -- Phase3Failure interface with `phase: string`, `msg: string`, `cause?: unknown`),
    extensions/pi-claude-marketplace/shared/notify.ts (grep for `compatibility.notes` to confirm A1 -- zero hits expected; the intent-mark marker is internal-only),
    .planning/phases/40-update-state-before-commit-reorder/40-RESEARCH.md "Pattern 1" (lines 298-360 -- markUpdateInProgress shape), "Pattern 2" (lines 384-503 -- finalizeUpdateRecord shape including bridge -> schema-field mapping comment), "Required new pattern: TR-04 module-level constant + helpers" (lines 1196-1208), "Pitfall 7" (compatibility.supported / .unsupported carry-forward on intent-mark)
  </read_first>

  <action>
    Append two new functions to `extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` immediately AFTER the existing `swapStateRecord` declaration (currently lines 781-823). Leave `swapStateRecord` in place untouched -- Task 2 deletes it after rewiring the caller. Declare two module-level constants at file scope near the top-of-file alongside the existing `SYNTHETIC_UPDATE_PLACEHOLDER_NAME` constant (or just above the new helpers if locality is clearer): `const UPDATE_IN_PROGRESS_NOTE = "update-in-progress";` and `const PHASE3_FAILURE_PHASES = ["skills", "commands", "agents", "mcp"] as const;` + `type Phase3Phase = (typeof PHASE3_FAILURE_PHASES)[number];`.

    Define `async function markUpdateInProgress(args: ThreePhaseArgs, preflight: PluginPreflight): Promise<void>` per RESEARCH Pattern 1. Inside a single `withStateGuard(locations, (s) => {...})`: re-read `sMp = s.marketplaces[marketplace]` (throw with the same `disappeared from state during update of "${plugin}"` wording as today's swapStateRecord if undefined); re-read `sRecord = sMp.plugins[plugin]` (throw `Plugin "${plugin}" was concurrently uninstalled.` if undefined); perform ST-9: `if (sRecord.version !== fromVersion)` throw the same `concurrently updated; expected "${fromVersion}", found "${sRecord.version}"` message currently in swapStateRecord lines 801-805. Then mutate ONLY `sRecord.compatibility = { installable: false, notes: [UPDATE_IN_PROGRESS_NOTE], supported: [...sRecord.compatibility.supported], unsupported: [...sRecord.compatibility.unsupported] }`. Do NOT touch `sRecord.version`, `sRecord.resources`, `sRecord.resolvedSource`, or `sRecord.updatedAt` in this window (Pitfall 7 -- supported/unsupported carry forward from the EXISTING sRecord values, NOT from `preflight.installable`). Add a JSDoc block stating the cross-process contract: "A second process observing `installable: false` + `notes: [UPDATE_IN_PROGRESS_NOTE]` MUST treat this plugin as in-flight; the next `update` call from any process is the recovery path. ST-9 lives here; finalize does NOT re-check ST-9 (Open Q1 RESOLVED)."

    Define `async function finalizeUpdateRecord(args: ThreePhaseArgs, preflight: PluginPreflight, handles: PrepHandles, phase3aFailures: readonly Phase3Failure[]): Promise<void>` per RESEARCH Pattern 2. Inside a single `withStateGuard(locations, (s) => {...})`: re-read `sMp`/`sRecord` with the same disappeared/concurrently-uninstalled guards (use "during finalize of" wording to disambiguate from the intent-mark site). Compute `const failedPhases = new Set<Phase3Phase>(phase3aFailures.map((f) => f.phase as Phase3Phase));`. Per-bridge write block -- FOUR independent `if (!failedPhases.has("skills")) { sRecord.resources.skills = handles.skills.result.recorded.map((r) => r.generatedName); }` blocks, one per bridge, using the locked schema-field mapping: `skills -> resources.skills`, `commands -> resources.prompts`, `agents -> resources.agents`, `mcp -> resources.mcpServers`. Add an inline comment above the four blocks naming the `commands -> prompts` asymmetry and cross-linking to Phase 39 TR-03. All-or-nothing branch -- `if (phase3aFailures.length === 0) { sRecord.version = preflight.toVersion; sRecord.compatibility = { installable: true, notes: [...preflight.installable.notes], supported: [...preflight.installable.supported], unsupported: [...preflight.installable.unsupported] }; sRecord.resolvedSource = preflight.installable.pluginRoot; }`. On the failure branch leave `compatibility` at the intent-mark values (installable:false + notes:[UPDATE_IN_PROGRESS_NOTE] from the prior `markUpdateInProgress` call); leave `version` at `fromVersion`; leave `resolvedSource` unchanged. ALWAYS set `sRecord.updatedAt = new Date().toISOString()` on BOTH branches (truthful "we touched the record" stamp). Add an inline comment above the all-or-nothing branch citing CONTEXT.md SC#2 + Pitfall 1 ("the trap SC#2 prevents -- do NOT gate per-bridge writes on phase3aFailures.length === 0; only the version bump uses that gate").

    Do NOT call either new helper from anywhere yet -- Task 2 wires them into `runThreePhaseUpdate`. After this task: `swapStateRecord` is still the only caller of state-write logic; both new helpers exist as dead code; `npm run check` MUST stay GREEN (typecheck confirms the helper bodies compile against the existing types).

    A1 spot-check (record finding in Task 1 commit body): grep `shared/notify.ts` for `compatibility.notes`. If the grep returns zero hits (expected), the intent-mark marker is internal-only and no notify-rendering test needs updating; if any hit references a closed-set Reason gate, surface it as a BLOCKER before proceeding to Task 2 (recourse: add `update-in-progress` to that closed set or move the marker into a NEW state field, per A1 mitigation guidance in RESEARCH.md).

    Pre-commit: `pre-commit run --files extensions/pi-claude-marketplace/orchestrators/plugin/update.ts`; fix and restage on failure. Commit: `feat(orchestrators): add markUpdateInProgress + finalizeUpdateRecord helpers (TR-04)`. Body cites SC#1 + SC#2 + RESEARCH.md Patterns 1+2 + A1 spot-check result.
  </action>

  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit 2>&1 | tail -20 && npm run test -- tests/orchestrators/plugin/update.test.ts 2>&1 | tail -30</automated>
  </verify>

  <done>
    Module-level constants `UPDATE_IN_PROGRESS_NOTE`, `PHASE3_FAILURE_PHASES`, and type `Phase3Phase` exist at update.ts file scope. Functions `markUpdateInProgress` and `finalizeUpdateRecord` are exported as file-private declarations with the shapes specified above. `swapStateRecord` still exists and is still the only state-write callsite. Typecheck GREEN. `update.test.ts` runs GREEN against the unchanged `runThreePhaseUpdate` (no behavior change yet). A1 spot-check recorded in the commit body confirming `shared/notify.ts` does not depend on `compatibility.notes` contents.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewire runThreePhaseUpdate to call markUpdateInProgress BEFORE phase-3a and finalizeUpdateRecord AFTER; delete swapStateRecord</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  </files>

  <read_first>
    extensions/pi-claude-marketplace/orchestrators/plugin/update.ts runThreePhaseUpdate at lines 825-1010 (specifically: preflight + early-return at 830-833, generatedNames + cross-plugin guard + prepareUpdateHandles at 850-853, swapStateRecord call wrapped in try/catch with abortHandles at 867-872, phase-3a aggregation block at 885-923, recovery-hint emission + partition='failed' return at 927-989, success path with dropPluginCompletionCache + partition='updated' return at 991-1010),
    .planning/phases/40-update-state-before-commit-reorder/40-RESEARCH.md "Pattern 3" (lines 504-672 -- post-fix runThreePhaseUpdate skeleton including the finalize-failure try/catch with synthetic 'mcp' Phase3Failure entry), "Pitfall 4" (lines 880-921 -- finalize-failure routing rationale + Assumption A3 reference), "Pitfall 5" (PUP-6 obstacle survives unchanged), "Anti-Patterns" (lines 674-723 -- single-withStateGuard / withLockedStateTransaction / finalize ST-9 re-check / phase: 'finalize' schema change all FORBIDDEN)
  </read_first>

  <action>
    Edit `runThreePhaseUpdate` in `extensions/pi-claude-marketplace/orchestrators/plugin/update.ts`. After the existing `prepareUpdateHandles` call (currently line 853), REPLACE the existing intent-mark stub `try { await swapStateRecord(args, preflight, handles); } catch (err) { throw appendLeaks(err, await abortHandles(handles)); }` (lines 867-872) with `try { await markUpdateInProgress(args, preflight); } catch (err) { throw appendLeaks(err, await abortHandles(handles)); }`. The phase-3a block (lines 885-923 -- four `try { await commitPrepared*(handles.X) } catch { phase3aFailures.push(...) }` blocks) MUST stay byte-identical -- do NOT touch the try/catch shapes, do NOT change phase-3a ordering (skills -> commands -> agents -> mcp), do NOT alter the leak-handling for skills + agents.

    Immediately AFTER the four phase-3a try/catch blocks complete (so AFTER line 923 -- AFTER the `mcp` push but BEFORE the `if (phase3aFailures.length > 0)` recovery-hint branch at line 927) insert a new try/catch wrapping the finalize call: `try { await finalizeUpdateRecord(args, preflight, handles, phase3aFailures); } catch (finalizeErr) { phase3aFailures.push({ phase: "mcp", msg: \`state finalize failed: ${errorMessage(finalizeErr)}\`, cause: finalizeErr }); }`. Add an inline comment above the catch citing Pitfall 4 + Assumption A3: "Finalize failure routes through phase3aFailures as a synthetic 'mcp' entry so the existing recovery-hint pipeline fires; the `msg` field carries the explicit `state finalize failed:` text so operator diagnostics see the truthful cause. A dedicated `phase: 'finalize'` Phase3Failure member is deferred to v1.8 (RESEARCH Open Q3 RESOLVED)." Do NOT introduce a `phase: "finalize"` literal -- the synthetic 'mcp' routing is the v1.7 contract.

    Do NOT change the `if (phase3aFailures.length > 0)` recovery-hint branch (lines 927-989) -- the `RECOVERY_PLUGIN_REINSTALL_PREFIX` text, the `PluginUpdatePhase3Error` aggregate, the `notifyDirectFailure({ reasonOverride: "rollback partial" as const, rollbackPartial: phase3aFailures })` call, and the `return { partition: "failed", ..., phaseFailures: phase3aFailures.map((f) => ({ phase: f.phase, msg: f.msg })), declaresAgents: false, declaresMcp: false }` outcome are ALL byte-identical post-fix. Do NOT change the success path (lines 991-1010 with `dropPluginCompletionCache` + `partition: "updated"` outcome).

    DELETE the entire `swapStateRecord` function declaration (currently lines 781-823) -- no callers remain after the rewire above. Verify by grepping the file post-edit: `grep -n swapStateRecord extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` MUST return zero results.

    Order discipline: the finalize call MUST run BEFORE the phase-3b recovery-hint branch. If finalize ran AFTER the recovery hint emission on a success path that flips to finalize-failure, the user would see a success notification then a stale state -- worst of both worlds. The synthetic 'mcp' push inside the finalize catch trips the phase-3b branch so the recovery hint fires; this is the correct routing per Pitfall 4.

    Expected test fallout (do NOT fix in this task): `npm run test -- tests/orchestrators/plugin/update.test.ts` will RED on the post-state assertions that PUP-6 / phase3a-commands-fail / phase3a-agents-fail / WR-04 currently lack (Task 3 adds them) AND on any test that incidentally asserts state.json contents reflecting the OLD `swapStateRecord` behavior (version=NEW + resources=NEW even on phase-3a failure). PUP-3 stays GREEN (short-circuit unchanged). Notification-assertion tests (PUP-6 `notifications.length === 1`, phase3a-commands-fail `assert.match(... recovery-hint)`, etc.) MUST stay GREEN; if any RED on a notification axis it indicates a regression in the recovery-hint pipeline and is a BLOCKER.

    Pre-commit: `pre-commit run --files extensions/pi-claude-marketplace/orchestrators/plugin/update.ts`. Commit: `refactor(orchestrators): rewire runThreePhaseUpdate to write state after commits (TR-04)`. Body cites SC#1 + SC#2 + SC#3 + Pitfall 4 finalize-failure routing + the deletion of swapStateRecord. Note the expected test RED count + which tests will turn GREEN in Task 3 (the four augmentations).
  </action>

  <verify>
    <automated>grep -c "swapStateRecord\|markUpdateInProgress\|finalizeUpdateRecord" extensions/pi-claude-marketplace/orchestrators/plugin/update.ts && npx tsc -p tsconfig.json --noEmit 2>&1 | tail -10</automated>
  </verify>

  <done>
    `runThreePhaseUpdate` calls `markUpdateInProgress(args, preflight)` BEFORE the phase-3a four-bridge try/catch block, and calls `finalizeUpdateRecord(args, preflight, handles, phase3aFailures)` inside a try/catch AFTER phase-3a + BEFORE the `if (phase3aFailures.length > 0)` recovery-hint branch. The finalize catch pushes a synthetic `{ phase: "mcp", msg: "state finalize failed: ...", cause }` entry into phase3aFailures. `swapStateRecord` function is DELETED (grep -c returns 0 for "swapStateRecord"). Typecheck GREEN. PUP-3 + notification-axis assertions in update.test.ts stay GREEN; post-state regression failures are expected and explicitly deferred to Task 3.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Augment EXISTING update.test.ts tests (PUP-6, phase3a-commands-fail, phase3a-agents-fail, WR-04) with post-state assertions; PUP-3 untouched</name>
  <files>
    tests/orchestrators/plugin/update.test.ts
  </files>

  <read_first>
    tests/orchestrators/plugin/update.test.ts (~1695 lines; specifically: imports at 1-25 -- `loadState` already imported at 19, `GENERATED_AGENT_PREFIX` at 8; makePluginRecord helper at 94-111 noting the seeded `compatibility = { installable: true, notes: [], supported: [], unsupported: [] }` + empty resources arrays; seedPathMarketplace helper at 123-221; PUP-3 unchanged test at 265-316 -- LEAVE UNTOUCHED; PUP-6 happy at 406-478 -- HAS post-state assertions already on line 437-444 from a prior pass, augment with intent-mark-absent + installable=true checks; PUP-6 phase-3 failure at 744-813 -- needs full post-state block; WR-04 at 817-851 -- direct uses `updateSinglePlugin` against `locations.extensionRoot` via `process.chdir(cwd)`; phase3a-commands-fail at 1584-1637; phase3a-agents-fail at 1641-1695),
    .planning/phases/40-update-state-before-commit-reorder/40-RESEARCH.md "Validation Architecture" -> "Recommended exact test cases" (lines 1478-1542 -- exact assertion shapes for each augmentation; matrix coverage table at 1543-1567)
  </read_first>

  <action>
    Edit `tests/orchestrators/plugin/update.test.ts` augmenting four existing tests in place. Do NOT alter any test's seed (seedPathMarketplace call, manifestPlugins, installedVersions), the obstacle file/dir setup (`writeFile(..., "obstacle")` in PUP-6 phase-3 / phase3a-commands-fail / phase3a-agents-fail), or the existing notification-match assertions (`notifications.length === 1`, `assert.match(allText, /plugin-uninstall \+ plugin-install for "hello"\./)`, etc.). The augmentations are APPENDED -- they add new assertions; they do NOT modify or delete existing ones.

    **PUP-3 (line 265) -- UNTOUCHED.** Preflight short-circuits at update.ts:830-833 BEFORE any withStateGuard call; the existing `before === after` assertion remains accurate. Do NOT touch this test.

    **PUP-6 happy (line 406) -- ADD all-success post-state expansion.** This test already loads state at line 437 (`const after = await loadState(locations.extensionRoot)`) and asserts version + resources arrays. AFTER the existing per-resource assertions (currently lines 440-444), APPEND a block asserting the all-success finalize contract: `assert.equal(record.compatibility.installable, true);` and `assert.ok(!record.compatibility.notes.includes("update-in-progress"), "intent-mark must NOT leak into success state");`. This is the "no-intent-mark-leak" assertion from Pitfall 6.

    **PUP-6 phase-3 failure (line 744) -- ADD full failure-path post-state block.** After the existing `assert.match(allText, /plugin-uninstall \+ plugin-install for "hello"\./, ...)` assertion (currently at line 804-808) and BEFORE the closing `} finally {` block, APPEND: `const after = await loadState(locations.extensionRoot); const rec = after.marketplaces["mp"]?.plugins["hello"]; assert.ok(rec !== undefined, "plugin record must survive partial failure");` then the five intent-mark-survives assertions: `assert.equal(rec.version, "1.0.0", "version must NOT bump on failure");` `assert.equal(rec.compatibility.installable, false, "installable must be false during/after failure");` `assert.ok(rec.compatibility.notes.includes("update-in-progress"), "intent-mark marker must survive failure");` `assert.deepEqual([...rec.resources.skills], [], "failed bridge resources stay at pre-update value");`. PUP-6 fails ONLY skills (no other obstacle), so commands + agents + mcp succeeded: `assert.deepEqual([...rec.resources.prompts], [], "commands had no manifest entry in this seed -> empty array");` `assert.deepEqual([...rec.resources.agents], [], "agents had no manifest entry -> empty array");` `assert.deepEqual([...rec.resources.mcpServers], [], "mcp had no manifest entry -> empty array");`. (The PUP-6 seed at line 763 sets `{ hello: { version: "1.0.1", hasSkill: true } }` -- only skills are declared, so successful bridges produce empty `recorded[]` arrays mapped to empty `resources.*` arrays. The orthogonal logic is still exercised: skills FAILED so `resources.skills` was NOT touched; the other three SUCCEEDED so `resources.prompts/.agents/.mcpServers` were written to `[]` -- byte-identical to the pre-update value but exercising the !failedPhases.has(bridge) gate.)

    **phase3a-commands-fail (line 1584) -- ADD post-state block.** This test seeds `{ hello: { version: "1.0.1", hasSkill: true, hasCommand: true } }` and obstacles BOTH skills (file at skillsTargetDir/hello-tool) AND commands (directory at promptsTargetDir/hello:deploy.md). So skills + commands FAILED; agents + mcp SUCCEEDED. After the existing recovery-hint match (currently lines 1628-1632) and BEFORE the closing `} finally {`, APPEND: `const after = await loadState(locations.extensionRoot); const rec = after.marketplaces["mp"]?.plugins["hello"]; assert.ok(rec !== undefined);` then `assert.equal(rec.version, "1.0.0");` `assert.equal(rec.compatibility.installable, false);` `assert.ok(rec.compatibility.notes.includes("update-in-progress"));` `assert.deepEqual([...rec.resources.skills], []);` `assert.deepEqual([...rec.resources.prompts], []);` `assert.deepEqual([...rec.resources.agents], []);` `assert.deepEqual([...rec.resources.mcpServers], []);`. (Pre-update record was empty per `makePluginRecord("1.0.0")` defaults; agents + mcp succeed with no declared resources so write empty arrays; skills + commands failed so resources.{skills,prompts} were NOT touched -- both remain empty. The per-bridge orthogonality contract is still exercised because the two SUCCEEDING bridges hit the `!failedPhases.has(bridge)` write path.)

    **phase3a-agents-fail (line 1641) -- ADD post-state block.** Seeds `{ hello: { version: "1.0.1", hasSkill: true, hasAgent: true } }` and obstacles skills (file) + agents (dir at agentsDir/<prefix>hello-bot.md). So skills + agents FAILED; commands + mcp SUCCEEDED. Same post-state block shape as phase3a-commands-fail, also expecting all four resources arrays empty (because the original `makePluginRecord("1.0.0")` defaulted to empty).

    **WR-04 (line 817) -- ADD success post-state block.** This test calls `updateSinglePlugin("hello", "mp", "project")` after `process.chdir(cwd)`; the locations are derived via `locationsFor("project", cwd)`. After the existing outcome-shape assertions (currently lines 835-841) and BEFORE the `process.chdir(prevCwd)` finally, APPEND: `const locations = locationsFor("project", cwd); const after = await loadState(locations.extensionRoot); const rec = after.marketplaces["mp"]?.plugins["hello"]; assert.ok(rec !== undefined);` then `assert.equal(rec.version, "1.0.1");` `assert.equal(rec.compatibility.installable, true);` `assert.ok(!rec.compatibility.notes.includes("update-in-progress"), "intent-mark marker must NOT leak into the all-success state");` `assert.deepEqual([...rec.resources.skills], ["hello-tool"]);` `assert.deepEqual([...rec.resources.agents], [\`${GENERATED_AGENT_PREFIX}hello-bot\`]);` `assert.deepEqual([...rec.resources.mcpServers], ["server1"]);`. (No `prompts` array assertion -- WR-04 seed has no `hasCommand: true`, so the commands bridge stages nothing.)

    Run `npm run test -- tests/orchestrators/plugin/update.test.ts` -- ALL EXISTING TESTS MUST BE GREEN against the post-fix `runThreePhaseUpdate` (intent-mark + finalize + 5-arm matrix all wired). If any test fails on a notification axis, this indicates a Task 2 regression in the recovery-hint pipeline -- BLOCKER. If any test fails on a post-state axis, re-read the failure carefully against the per-bridge orthogonality contract before mutating either the test or the implementation.

    Pre-commit: `pre-commit run --files tests/orchestrators/plugin/update.test.ts`. Commit: `test(orchestrators): add post-state assertions to existing update tests (TR-04)`. Body lists the four augmented tests + their pre/post-state axes + the SC#1/SC#2/SC#4 mapping.
  </action>

  <verify>
    <automated>npm run test -- tests/orchestrators/plugin/update.test.ts 2>&1 | tail -40</automated>
  </verify>

  <done>
    PUP-6 happy (line 406), PUP-6 phase-3 failure (line 744), phase3a-commands-fail (line 1584), phase3a-agents-fail (line 1641), and WR-04 (line 817) each carry an appended post-state assertion block locking the intent-mark survives-on-failure / does-not-leak-on-success contract per the per-bridge orthogonality model. PUP-3 (line 265) is UNTOUCHED. `update.test.ts` runs GREEN. Notification-axis assertions remain byte-identical (`notifications.length === 1` etc.).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Append 4 NEW per-bridge matrix tests + 1 NEW retry test to update.test.ts</name>
  <files>
    tests/orchestrators/plugin/update.test.ts
  </files>

  <read_first>
    tests/orchestrators/plugin/update.test.ts (the existing seedPathMarketplace at 123-221 for shape + obstacle-pattern reference; the three existing obstacle setups at 776 / 1607-1609 / 1664-1671 for the per-bridge forcing functions; PUP-6 happy structure at 406-478 as the all-success template; phase3a-commands-fail at 1584-1637 as the directory-obstacle template; locations.mcpJsonPath via `locationsFor`),
    extensions/pi-claude-marketplace/persistence/locations.ts (locate skillsTargetDir / promptsTargetDir / agentsDir / mcpJsonPath property names),
    .planning/phases/40-update-state-before-commit-reorder/40-RESEARCH.md "Validation Architecture" -> "Recommended exact test cases" (the 4 matrix + 1 retry test specs at lines 1508-1542), "4-bridge x 2-outcome matrix coverage analysis" (lines 1543-1567 including the per-bridge orthogonality attestation comment block)
  </read_first>

  <action>
    APPEND 5 new tests to the END of `tests/orchestrators/plugin/update.test.ts`. Above the first new test, add a comment block: `// ─── TR-04 matrix coverage (Phase 40, SC#4) ──────────────────────────────` / `// Per-bridge orthogonality: finalizeUpdateRecord gates each bridge's resource` / `// write on !failedPhases.has(bridge), independent of other bridges' outcomes.` / `// 4 dedicated "exactly one bridge fails" tests below + the all-success WR-04` / `// + existing PUP-6 (skills-only-fail) + phase3a-commands-fail (skills+commands` / `// fail) + phase3a-agents-fail (skills+agents fail) cover every per-bridge` / `// "succeed" vs "fail" outcome in both directions for all four bridges; the` / `// remaining 9 multi-failure cases compose deterministically. See .planning/` / `// phases/40-update-state-before-commit-reorder/40-RESEARCH.md "4-bridge x 2-` / `// outcome matrix coverage analysis" for the full 16-case table.`.

    Matrix shape: each test seeds a fresh hermetic cwd via `withHermeticHome` + `mkdtemp`; uses `seedPathMarketplace` with `manifestPlugins: { hello: { version: "1.0.1", hasSkill: true, hasCommand: true, hasAgent: true, hasMcp: true } }` and `installedVersions: { hello: "1.0.0" }` (all four bridges declared so all four are exercised); pre-creates exactly ONE obstacle to force exactly ONE bridge to fail; calls `updatePlugins({ ctx, pi, scope: "project", cwd, target: { kind: "plugin", plugin: "hello", marketplace: "mp" } })`; asserts (a) `notifications.length === 1` with recovery-hint match (`assert.match(allText, /plugin-uninstall \+ plugin-install for "hello"\./)`), (b) post-state via `const after = await loadState(locations.extensionRoot); const rec = after.marketplaces["mp"]?.plugins["hello"];` with the per-axis expectations below.

    **Matrix #1 -- skills-fails-others-succeed.** Obstacle: `await mkdir(locations.skillsTargetDir, { recursive: true }); await writeFile(path.join(locations.skillsTargetDir, "hello-tool"), "obstacle");`. Post-state: `assert.equal(rec.version, "1.0.0");` (no bump) `assert.equal(rec.compatibility.installable, false);` `assert.ok(rec.compatibility.notes.includes("update-in-progress"));` `assert.deepEqual([...rec.resources.skills], []);` (failed -- pre-update value) `assert.deepEqual([...rec.resources.prompts], ["hello:deploy"]);` (commands succeeded -- new generated name) `assert.deepEqual([...rec.resources.agents], [\`${GENERATED_AGENT_PREFIX}hello-bot\`]);` (agents succeeded) `assert.deepEqual([...rec.resources.mcpServers], ["server1"]);` (mcp succeeded).

    **Matrix #2 -- commands-fails-others-succeed.** Obstacle: `await mkdir(locations.promptsTargetDir, { recursive: true }); await mkdir(path.join(locations.promptsTargetDir, "hello:deploy.md"), { recursive: true });` (directory at the target file path forces EISDIR on rename). Post-state: version=1.0.0, installable=false, notes includes update-in-progress; `skills === ["hello-tool"]`; `prompts === []` (failed -- pre-update); `agents === [GENERATED_AGENT_PREFIX + "hello-bot"]`; `mcpServers === ["server1"]`.

    **Matrix #3 -- agents-fails-others-succeed.** Obstacle: `await mkdir(locations.agentsDir, { recursive: true }); await mkdir(path.join(locations.agentsDir, \`${GENERATED_AGENT_PREFIX}hello-bot.md\`), { recursive: true });` (directory at agent file path -> EISDIR). Post-state: version=1.0.0, installable=false, notes includes update-in-progress; `skills === ["hello-tool"]`; `prompts === ["hello:deploy"]`; `agents === []` (failed); `mcpServers === ["server1"]`.

    **Matrix #4 -- mcp-fails-others-succeed.** Obstacle: pre-create a DIRECTORY at the mcp.json path: `await mkdir(path.dirname(locations.mcpJsonPath), { recursive: true }); await mkdir(locations.mcpJsonPath, { recursive: true });`. Mcp commit's atomic file write (write-file-atomic) fails because the target is a directory. (If `locations.mcpJsonPath` is unavailable as a property, substitute `path.join(cwd, ".pi", "mcp.json")` for project scope -- verify by reading `persistence/locations.ts` during execution; if the path resolves differently the obstacle MUST land at whatever path `commitPreparedMcp` actually writes.) Post-state: version=1.0.0, installable=false, notes includes update-in-progress; `skills === ["hello-tool"]`; `prompts === ["hello:deploy"]`; `agents === [GENERATED_AGENT_PREFIX + "hello-bot"]`; `mcpServers === []` (failed).

    **Retry test (SC#5).** Two `updatePlugins` calls in the same hermetic cwd. Seed shape mirrors PUP-6 phase-3 failure: `manifestPlugins: { hello: { version: "1.0.1", hasSkill: true } }`, `installedVersions: { hello: "1.0.0" }`. Pre-create the skills obstacle file (PUP-6 shape) BEFORE call 1. Call 1: `await updatePlugins({...})`; assert `notifications.length === 1` with recovery-hint; assert intermediate post-state: `version === "1.0.0"`, `installable === false`, `notes.includes("update-in-progress")`, `resources.skills === []`. Between calls: `await rm(path.join(locations.skillsTargetDir, "hello-tool"), { force: true });`. (Optionally rm the skillsTargetDir entirely with `{ recursive: true, force: true }` to clear any partial state.) Make a SECOND `makeCtx()` invocation to get a fresh `notifications` array (`const { ctx: ctx2, pi: pi2, notifications: notifications2 } = makeCtx();`). Call 2: `await updatePlugins({ ctx: ctx2, pi: pi2, scope: "project", cwd, target: { kind: "plugin", plugin: "hello", marketplace: "mp" } });`. Post-call-2 assertions: NO unexpected error notification -- `assert.equal(notifications2.filter((n) => n.severity === "error").length, 0, "second run must NOT emit errors");`; final state: `const final = await loadState(locations.extensionRoot); const rec2 = final.marketplaces["mp"]?.plugins["hello"]; assert.equal(rec2.version, "1.0.1");` `assert.equal(rec2.compatibility.installable, true);` `assert.ok(!rec2.compatibility.notes.includes("update-in-progress"), "intent-mark must NOT survive a successful retry");` `assert.deepEqual([...rec2.resources.skills], ["hello-tool"]);`.

    Each new test follows the existing file's wrap-pattern: `await withHermeticHome(async () => { const cwd = await mkdtemp(path.join(tmpdir(), "update-tr04-<slug>-")); try { ... } finally { await rm(cwd, { recursive: true, force: true }); } });`. Test names use the convention `TR-04 matrix: <bridge>-fails-others-succeed` and `TR-04 retry: partial-success-state-converges-to-new-version`.

    Verification: `npm run test -- tests/orchestrators/plugin/update.test.ts` MUST be GREEN with the 5 new tests landing alongside the augmented existing tests. Expected file-level test count: existing ~50 file tests + 5 new = ~55. If Matrix #4 RED on `mcpJsonPath` resolution, fix the path before declaring the obstacle inadequate -- the mcp commit DOES write atomically through write-file-atomic and IS forceable by a directory-at-path obstacle.

    Pre-commit: `pre-commit run --files tests/orchestrators/plugin/update.test.ts`. Commit: `test(orchestrators): add 4-bridge matrix + retry tests (TR-04)`. Body cites SC#4 (matrix) + SC#5 (retry) + the 16-case coverage attestation + the per-bridge orthogonality model.
  </action>

  <verify>
    <automated>npm run test -- tests/orchestrators/plugin/update.test.ts 2>&1 | tail -50</automated>
  </verify>

  <done>
    Five new tests appended to `update.test.ts`: 4 matrix tests (one per bridge fails in isolation against an otherwise all-success manifest) + 1 retry test (partial-success seed converges to version=NEW on second run with no error notifications + no intent-mark leak). The per-bridge orthogonality coverage attestation comment block sits above the matrix tests. `update.test.ts` runs GREEN file-wide. Existing test count moves up by 5 net new.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: npm run check GREEN gate; record test count delta in SUMMARY</name>
  <files>
    .planning/phases/40-update-state-before-commit-reorder/40-01-SUMMARY.md
  </files>

  <read_first>
    .planning/templates/summary.md (if present) for SUMMARY frontmatter conventions; .planning/phases/39-cascade-ghost-record/39-01-SUMMARY.md for a recent precedent on shape + test-count delta wording; the actual `npm run check` output post-Task-4 to populate the test-count line truthfully
  </read_first>

  <action>
    Run `npm run check` from the repo root. Expected: typecheck + lint + format:check + node:test all GREEN. Baseline (post-Phase-39) was 1362 tests; Task 4 adds 5 new tests; Tasks 3 augmented existing tests in place (no count change). Expected post-Phase-40 count: ~1367 GREEN. If lint or format:check RED, fix the offending file (likely `update.ts` or `update.test.ts`) and re-run before declaring GREEN.

    Write `.planning/phases/40-update-state-before-commit-reorder/40-01-SUMMARY.md` with the following sections:

    Frontmatter: `phase: 40-update-state-before-commit-reorder`, `plan: 01`, `requirements_addressed: [TR-04]`, `status: complete`, `nyquist_compliant: true`, `tests_baseline: 1362`, `tests_after: <actual count from npm run check>`, `commits: [<sha1>, <sha2>, <sha3>, <sha4>]` (the four commits from Tasks 1-4).

    Body sections:
    - **Outcome:** F4 / TR-04 state-write-before-commit divergence closed. `runThreePhaseUpdate` now writes an intent-mark BEFORE phase-3a (installable:false + notes:[update-in-progress]) and a per-bridge / all-or-nothing finalize AFTER phase-3a. swapStateRecord retired. D-03 continue-on-failure preserved byte-identically. Finalize-failure routes through phase3aFailures as a synthetic 'mcp' entry.
    - **SC closure:** Map SC#1 -> markUpdateInProgress; SC#2 -> finalizeUpdateRecord (per-bridge + all-or-nothing); SC#3 -> phase-3a aggregation unchanged; SC#4 -> 4 new matrix tests + augmented PUP-6/phase3a-*; SC#5 -> retry test; SC#6 -> npm run check GREEN, test count <baseline+5>.
    - **Pitfall closure:** Pitfall 1 (per-bridge gating with !failedPhases.has, NOT length === 0); Pitfall 2 (existing tests augmented, not replaced); Pitfall 3 (two-withStateGuard model + intent-mark as cross-process signal); Pitfall 4 (finalize-failure synthetic 'mcp' routing); Pitfall 5 (PUP-6 obstacle preserved); Pitfall 6 (WR-04 post-state expansion); Pitfall 7 (supported/unsupported carry-forward on intent-mark); Pitfall 8 (reassignment, never in-place mutation).
    - **Hand-offs:** Phase 41 (TR-07 + TR-08 docs/test closeout) can proceed; no other v1.7 phase blocked. v1.8 follow-ups: dedicated `phase: "finalize"` Phase3Failure member (Open Q3 deferred); finalize-failure dedicated unit test (deferred per Pitfall 4 step 4 -- complex withStateGuard mocking).
    - **Files touched:** `extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` (modified -- swapStateRecord removed, two helpers added, runThreePhaseUpdate rewired); `tests/orchestrators/plugin/update.test.ts` (modified -- 4 augmented + 5 new).
    - **Commit chain:** list the four commits with subjects.

    Pre-commit: `pre-commit run --files .planning/phases/40-update-state-before-commit-reorder/40-01-SUMMARY.md`. Commit: `docs(40): SUMMARY for Phase 40 update-state-before-commit reorder (TR-04)`. Body cites `npm run check GREEN` + final test count.
  </action>

  <verify>
    <automated>npm run check 2>&1 | tail -10</automated>
  </verify>

  <done>
    `npm run check` exits 0: typecheck + lint + format:check + node:test all GREEN. Final test count recorded in 40-01-SUMMARY.md frontmatter as `tests_after: <count>`; delta from 1362 baseline is +5 (4 matrix + 1 retry). 40-01-SUMMARY.md exists with all sections above. nyquist_compliant flipped true in the SUMMARY frontmatter.
  </done>
</task>

</tasks>

<verification>
After all five tasks complete, the following must hold (verifiable by grep / loadState / npm run check):

- `grep -c "swapStateRecord" extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` returns `0`.
- `grep -c "markUpdateInProgress\|finalizeUpdateRecord" extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` returns `>= 4` (one definition + two calls per helper).
- `grep -n "UPDATE_IN_PROGRESS_NOTE\b" extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` returns a single module-level constant declaration + at least one usage inside markUpdateInProgress.
- `grep -c "withStateGuard" extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` returns exactly `2` (intent-mark + finalize). NOT one (the old single-guard model) and NOT three (no introduction of a third state-write path).
- `grep -n "PHASE3_FAILURE_PHASES\b" extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` returns at least one module-level tuple declaration + at least one usage in `Set<Phase3Phase>` construction inside finalize.
- `grep -c "TR-04 matrix:" tests/orchestrators/plugin/update.test.ts` returns `4` (one per bridge).
- `grep -c "TR-04 retry:" tests/orchestrators/plugin/update.test.ts` returns `1`.
- `npm run check` exits 0 with the test count recorded in 40-01-SUMMARY.md frontmatter.
- All four augmented existing tests (PUP-6 happy, PUP-6 phase-3 failure, phase3a-commands-fail, phase3a-agents-fail, WR-04) carry a `const after = await loadState(...)` block followed by version + compatibility.installable + compatibility.notes + per-bridge resources assertions.
- PUP-3 (line 265) is UNTOUCHED -- diff against pre-phase-40 baseline shows zero lines changed inside the PUP-3 test body.
</verification>

<success_criteria>
- SC#1: `markUpdateInProgress` runs INSIDE a `withStateGuard` BEFORE phase-3a; the ONLY state write before commits begin. Sets `compatibility = { installable: false, notes: [UPDATE_IN_PROGRESS_NOTE], supported: [...sRecord.compatibility.supported], unsupported: [...sRecord.compatibility.unsupported] }`. ST-9 stale-version check lives here. Verified by grep + PUP-6 phase-3 failure post-state assertion.
- SC#2: `finalizeUpdateRecord` runs INSIDE a SECOND `withStateGuard` AFTER phase-3a. Per-bridge `if (!failedPhases.has(bridge))` gating applies `sRecord.resources.X = handles.X.result.recorded.map(r => r.generatedName)` for each of skills / commands / agents / mcp INDEPENDENTLY (commands -> prompts asymmetry). Version bump + installable=true happen ONLY when `phase3aFailures.length === 0`. Verified by the 4 matrix tests + WR-04 augmentation.
- SC#3: D-03 continue-on-failure preserved. The four phase-3a try/catch blocks at update.ts:885-923 are byte-identical post-fix. All four bridges attempt regardless of individual failures. `phase3aFailures[]` accumulates them. Recovery-hint emission (RECOVERY_PLUGIN_REINSTALL_PREFIX + notifyDirectFailure + partition='failed' outcome) fires on any failure. Verified by phase3a-commands-fail + phase3a-agents-fail GREEN post-augmentation.
- SC#4: 4-bridge x 2-outcome matrix covered by 4 NEW per-bridge tests (Matrix #1..#4 -- skills / commands / agents / mcp each fails in isolation). Per-bridge orthogonality of finalize logic means the remaining 12 cases compose deterministically; coverage attestation comment block documents this + cross-links to 40-RESEARCH.md.
- SC#5: Retry test seeds partial-success state via call-1 PUP-6 failure; between calls rms the obstacle; call-2 reaches `version === "1.0.1"`, `installable === true`, no `update-in-progress` note, no error notifications. NFR-3 retry-safety enforced end-to-end.
- SC#6: `npm run check` GREEN with the test count delta recorded in 40-01-SUMMARY.md frontmatter (`tests_after: <count>`, +5 from 1362 baseline).
</success_criteria>

<output>
Create `.planning/phases/40-update-state-before-commit-reorder/40-01-SUMMARY.md` in Task 5.
</output>
