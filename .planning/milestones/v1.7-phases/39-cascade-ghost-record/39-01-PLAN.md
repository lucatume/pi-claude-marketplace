---
phase: 39-cascade-ghost-record
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts
  - tests/orchestrators/plugin/uninstall.test.ts
  - tests/orchestrators/marketplace/remove.test.ts
autonomous: true
requirements: [TR-03]
requirements_addressed: [TR-03]

must_haves:
  truths:
    - "On non-AG-5 cascade partial failure in uninstall.ts, state.json (after reload) shows sRecord.resources.skills/.prompts/.agents/.mcpServers shrunken by outcome.dropped.skills/.commands/.agents/.mcpServers; the plugin row is preserved (not deleted)."
    - "On non-AG-5 cascade partial failure in remove.ts per-plugin loop, the same filter is applied to record.plugins[pluginName].resources.* in place; the plugin row stays, mp record is retained (failedPlugins.length > 0)."
    - "When outcome.cause instanceof AgentsUnstageFailureError (AG-5 foreign content), the state row is preserved INTACT in both call sites -- no filter is applied; resources.* arrays are byte-identical to the pre-cascade snapshot."
    - "The cascade primitive cascadeUnstagePlugin (shared.ts:317-395) makes ZERO state mutation; outcome.dropped is Object.freeze'd at lines 374-393 and the filter at the orchestrator boundary does NOT mutate it (uses .filter which returns a new array)."
    - "The field-name asymmetry is honored at both filter sites: outcome.dropped.commands maps to resources.prompts (NOT resources.commands which does not exist on the state schema); inline comments + grep-checkable line documents the mapping."
    - "Existing PU-3+PU-7 invariant test at tests/orchestrators/plugin/uninstall.test.ts:344 stays GREEN verbatim (AG-5 path throws inside guard -> ST-7 aborts save -> row preserved -> single error notification)."
    - "Existing MR-4 cascade-failure test at tests/orchestrators/marketplace/remove.test.ts:338 stays GREEN verbatim (cascade fails for plugin-a, succeeds for plugin-b, mp record retained)."
    - "npm run check exits 0; full suite reaches 1362+ tests (1358 baseline + 4 new) all GREEN."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts"
      provides: "Hybrid AG-5-throw + non-AG-5-sentinel-filter inside withStateGuard; new cascadeFailure sentinel + post-guard PluginFailedMessage emission BEFORE post-state cleanup block; uses existing narrowCascadeFailure for Reason mapping."
      contains: "instanceof AgentsUnstageFailureError"
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts"
      provides: "Per-plugin loop else arm extended with non-AG-5 in-place filter on plugin.resources.{skills,prompts,agents,mcpServers}; AG-5 case skips filter; failedPlugins.push happens in BOTH branches; trailing saveState commits shrunken record."
      contains: "instanceof AgentsUnstageFailureError"
    - path: "tests/orchestrators/plugin/uninstall.test.ts"
      provides: "Two new TR-03 unit tests appended: (a) non-AG-5 partial with dropped.skills=['s1'] asserts resources.skills===['s2'] post-reload + error notification; (b) AG-5 cause asserts resources.skills unchanged === ['s1','s2'] + error notification."
    - path: "tests/orchestrators/marketplace/remove.test.ts"
      provides: "Two new TR-03 unit tests appended: (c) non-AG-5 partial with dropped.commands=['c1'] asserts resources.prompts===['c2'] (locking dropped.commands -> resources.prompts asymmetry) + mp retained; (d) AG-5 cause asserts resources.prompts unchanged === ['c1','c2'] + mp retained."
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts (AgentsUnstageFailureError export at line 55)"
      via: "import { cascadeUnstagePlugin, AgentsUnstageFailureError } -- already present at uninstall.ts:49"
      pattern: "cause instanceof AgentsUnstageFailureError"
    - from: "extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts"
      via: "import { AgentsUnstageFailureError } -- already present at remove.ts:49"
      pattern: "cause instanceof AgentsUnstageFailureError"
    - from: "uninstall.ts filter block"
      to: "outcome.dropped.commands -> sRecord.resources.prompts (NOT resources.commands)"
      via: "explicit four-line filter with mapping comment"
      pattern: "sRecord\\.resources\\.prompts.*outcome.*dropped\\.commands"
    - from: "remove.ts filter block"
      to: "outcome.dropped.commands -> plugin.resources.prompts"
      via: "explicit four-line filter with mapping comment"
      pattern: "plugin\\.resources\\.prompts.*outcome\\.dropped\\.commands"
---

<objective>
Close TR-03 (cascade ghost record). Both `orchestrators/plugin/uninstall.ts` and
`orchestrators/marketplace/remove.ts` materialize `outcome.dropped.*` into a partial
`sRecord.resources.*` filter when `outcome.ok === false` AND the cause is NOT
`AgentsUnstageFailureError`. When the cause IS `AgentsUnstageFailureError` (AG-5
foreign content), the full state row is preserved intact -- foreign content
owned by another process must not cause data loss. The cascade primitive itself
(`cascadeUnstagePlugin` at `marketplace/shared.ts:317-395`) stays read-only on
state; all filtering happens at the orchestrator boundary INSIDE `withStateGuard`.

Purpose: Eliminate the F3 ghost-record defect documented in
`.planning/research/PITFALLS.md` Pitfall 3 -- post-Phase-39, state.json reflects
what is actually on disk after partial cascade failure, enabling clean retry
semantics without losing the AG-5 manual-recovery anchor.

Output: Two source-file edits + two test-file appends + a regression gate that
asserts 1362 GREEN tests (1358 baseline + 4 new) via `npm run check`.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@.planning/phases/39-cascade-ghost-record/39-CONTEXT.md
@.planning/phases/39-cascade-ghost-record/39-RESEARCH.md
@.planning/phases/39-cascade-ghost-record/39-VALIDATION.md
@extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
@extensions/pi-claude-marketplace/transaction/with-state-guard.ts
@extensions/pi-claude-marketplace/persistence/state-io.ts
@tests/orchestrators/plugin/uninstall.test.ts
@tests/orchestrators/marketplace/remove.test.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: uninstall.ts hybrid AG-5-throw / non-AG-5-sentinel-filter + 2 regression tests</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts,
    tests/orchestrators/plugin/uninstall.test.ts
  </files>

  <read_first>
    extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts (full file, ~336 lines -- closure at lines 162-211, catch at 212-240, cleanup at 250-279, narrowCascadeFailure at 93-114, imports incl. AgentsUnstageFailureError at line 49),
    extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts (lines 55-62 for AgentsUnstageFailureError; lines 290-302 for UnstageOutcome shape; lines 317-395 for cascadeUnstagePlugin showing dropped.commands populated from installedPlugin.resources.prompts at line 339; lines 374-393 for Object.freeze on outcome.dropped),
    extensions/pi-claude-marketplace/transaction/with-state-guard.ts (lines 50-76 -- ST-7 contract: save only on no-throw; throw inside mutate aborts save),
    extensions/pi-claude-marketplace/persistence/state-io.ts (lines 38-55 PLUGIN_INSTALL_RECORD_SCHEMA confirming resources.{skills,prompts,agents,mcpServers}),
    tests/orchestrators/plugin/uninstall.test.ts (existing PU-3+PU-7 test at line 344; makePluginRecord helper at lines 84-100; the existing stubCascade injection seam used by every cascade-dependent test),
    .planning/phases/39-cascade-ghost-record/39-RESEARCH.md (Patterns 1+3; Pitfalls 1+2+3+4+7; Open Question 1 RESOLVED REJECT TR-D03 extraction; Open Question 2 RESOLVED ADOPT full-row AG-5 preservation; Code Examples block at lines 887-925; Validation Architecture lines 1095-1178)
  </read_first>

  <behavior>
    - Test 1 (PU-TR03-A: non-AG-5 partial filter): seed state via makePluginRecord with plugin `hello` carrying resources `{ skills: ["s1","s2"], prompts: ["p1"], agents: [], mcpServers: [] }`. Stub cascade to return `{ ok: false, dropped: { skills: ["s1"], commands: [], agents: [], mcpServers: [] }, cause: new Error("forced EACCES") }` (frozen via Object.freeze to mirror the production primitive). Call `uninstallPlugin`. Reload state via `loadState`. Assert: `state.marketplaces[mp].plugins.hello !== undefined` (row preserved); `state.marketplaces[mp].plugins.hello.resources.skills` deepEquals `["s2"]` (s1 filtered); `state.marketplaces[mp].plugins.hello.resources.prompts` deepEquals `["p1"]` (unchanged because dropped.commands is empty); notifications.length === 1; notifications[0] severity is `error`; message body contains `(failed)` substring confirming PluginFailedMessage byte form.
    - Test 2 (PU-TR03-B: AG-5 full-row preservation): same seed. Stub cascade to return `{ ok: false, dropped: { skills: ["s1"], commands: [], agents: [], mcpServers: [] }, cause: new AgentsUnstageFailureError("agents leak", []) }`. Call `uninstallPlugin`. Reload state. Assert: `state.marketplaces[mp].plugins.hello !== undefined`; `state.marketplaces[mp].plugins.hello.resources.skills` deepEquals `["s1","s2"]` (UNCHANGED despite dropped.skills); notifications.length === 1; severity error. This locks SC#3 (AG-5 carve-out) AND the PU-3+PU-7 invariant that the existing test already exercises -- the new test makes the AG-5 vs non-AG-5 distinction explicit alongside it.
    - Behavior of production code under test: pre-cascade snapshot captured in `outcome`; sentinel `cascadeFailure: Error | undefined` declared in outer scope; INSIDE the guard, after `outcome = await cascade(...)`, branch on `!outcome.ok`: (i) if `cause instanceof AgentsUnstageFailureError` re-throw `cause` (this keeps ST-7 abort-save semantics -- the existing outer catch at lines 212-240 still emits the PluginFailedMessage and returns); (ii) else mutate `sRecord.resources.{skills,prompts,agents,mcpServers}` in place using `.filter(n => !outcome.dropped.{skills,commands,agents,mcpServers}.includes(n))` with the mapping `dropped.commands -> resources.prompts`, set `cascadeFailure = cause`, then `return` from the closure (no throw -- guard saves the shrunken row). Happy path unchanged: `delete mp.plugins[plugin]`. After the guard, NEW branch: `if (cascadeFailure !== undefined)` emits the existing PluginFailedMessage shape (status: "failed", name: plugin, reasons: [narrowCascadeFailure(cascadeFailure)], optional version: removedVersion, cause: cascadeFailure) via the existing `notify(ctx, pi, { marketplaces: [{ name: marketplace, scope, plugins: [failedRow] }] })` shape -- byte-identical to the existing catch block emission -- and `return` BEFORE the post-state cleanup block (dropMarketplaceCache + rm(dataDir) + PluginUninstalledMessage). The existing outer try/catch stays in place as the AG-5 surface (PU-3+PU-7 path unchanged).
  </behavior>

  <action>
    Implement TR-03 in `extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` per RESEARCH Pattern 1 ("Cascade-failure sentinel + post-guard branch"). Concrete steps (NO fenced code blocks; specifics by identifier and line range):

    1. Declare a new outer-scope sentinel variable `cascadeFailure: Error | undefined` alongside the existing `alreadyGone` / `outcome` / `removedVersion` declarations at uninstall.ts:158-160. The declaration MUST live OUTSIDE the `withStateGuard` closure so the post-guard branch can read it after `withStateGuard` resolves.

    2. Inside the `withStateGuard` closure at uninstall.ts:163-211, replace the existing single-line `if (!outcome.ok) { throw outcome.cause ?? new Error(...) }` block (lines 198-205) with a two-arm branch:
       - Compute `const cause = outcome.cause ?? new Error("Cascade unstage failed for plugin \"" + plugin + "\".")` (preserving the existing fallback message wording for log diagnosability).
       - Arm A (AG-5 carve-out, SC#3): `if (cause instanceof AgentsUnstageFailureError) throw cause;` -- preserves the existing PU-3+PU-7 path verbatim (ST-7 aborts save; existing outer catch at 212-240 emits PluginFailedMessage; existing PU-3+PU-7 test stays GREEN unmodified).
       - Arm B (non-AG-5 partial filter, SC#1): alias `const sRecord = mp.plugins[plugin]!;` (the non-null assertion mirrors the existing `installed` capture at line 182 and is required because TS flow analysis cannot prove `mp.plugins[plugin]` is defined after the await). Mutate the four `resources.*` arrays in place via `sRecord.resources.skills = sRecord.resources.skills.filter((n) => !outcome.dropped.skills.includes(n));` (and analogous lines). CRITICAL field-name mapping (Pitfall 1): the SECOND filter line MUST read `sRecord.resources.prompts = sRecord.resources.prompts.filter((n) => !outcome.dropped.commands.includes(n));` -- the asymmetry `dropped.commands -> resources.prompts` is load-bearing and MUST be inline-commented with the four-line mapping table from RESEARCH (`dropped.skills -> resources.skills`, `dropped.commands -> resources.prompts (note asymmetry)`, `dropped.agents -> resources.agents`, `dropped.mcpServers -> resources.mcpServers`). After the four filter assignments, set `cascadeFailure = cause;` and `return;` from the closure (NO throw -- the guard's saveState commits the shrunken row).

    3. Inside the closure, the line referencing `outcome` after the `!outcome.ok` guard may require a `!` non-null assertion in the filter callbacks because `outcome` is typed `UnstageOutcome | undefined` in the outer scope. Use `outcome!.dropped.skills.includes(n)` inside each `.filter` callback (the existing post-line-205 code at lines 209-210 also assumes outcome is defined; the new code follows the same pattern). NOTE: the closure's existing `outcome = await cascade(...)` already narrows `outcome` to `UnstageOutcome` for the rest of the closure, but TS may still complain inside the filter callbacks due to closure-capture timing -- if so, use the non-null assertion (the runtime invariant holds because we only enter the filter branch after `if (!outcome.ok)`).

    4. AFTER the `withStateGuard(...)` call returns (uninstall.ts:211 closing `})`), and BEFORE the existing outer `} catch (err) {` at line 212, NO new code. The existing outer try/catch at lines 212-240 STAYS as the AG-5 surface (catches the Arm A throw, emits PluginFailedMessage, returns). DO NOT remove or restructure the existing catch -- it is load-bearing for PU-3+PU-7.

    5. AFTER the outer try/catch (after line 240), and BEFORE the existing `if (alreadyGone)` early-return at line 246, ADD a new branch:
       - `if (cascadeFailure !== undefined) { ... }` body: build `const failedRow: PluginFailedMessage = { status: "failed", name: plugin, reasons: [narrowCascadeFailure(cascadeFailure)], ...(removedVersion !== undefined && { version: removedVersion }), cause: cascadeFailure };` (byte-identical to the existing catch emission at lines 221-229 -- copy/paste the shape, just use `cascadeFailure` instead of `cause`). Then call `notify(ctx, pi, { marketplaces: [{ name: marketplace, scope, plugins: [failedRow] }] });` (mirroring lines 230-238). Then `return;` BEFORE the post-state cleanup (per Pitfall 4: cleanup is skipped on cascade failure of any kind).
       - Inline comment block above the branch: "// TR-03 (SC#1): non-AG-5 partial cascade failure surfaces via the sentinel set inside the guard. The guard already saved the shrunken sRecord.resources.*; here we emit the existing PluginFailedMessage shape. Same severity (error) and notification structure as the AG-5 catch-block path at lines 212-240; only the surface mechanism differs (sentinel vs throw)."

    6. The post-state cleanup block at lines 250-279 (`dropMarketplaceCache`, `rm(dataDir)`, `PluginUninstalledMessage` notify) STAYS unchanged. The new `if (cascadeFailure !== undefined) return;` at step 5 ensures it is SKIPPED on the non-AG-5 partial path (Pitfall 4 compliance).

    7. Append the two new tests (PU-TR03-A and PU-TR03-B per <behavior>) to `tests/orchestrators/plugin/uninstall.test.ts` USING the existing stubCascade injection seam pattern that PU-3+PU-7 (line 344) already uses. The tests MUST call `loadState(locations.extensionRoot)` AFTER `uninstallPlugin` returns to assert the ON-DISK shrunken state (Pitfall 3 prevention -- in-memory mutation only would silently pass without the reload). Use `assert.deepEqual` for array assertions; `assert.equal(notifications.length, 1)` and `assert.equal(notifications[0].severity, "error")` for the notification surface. Test naming convention: `test("TR-03 PU-TR03-A: non-AG-5 partial cascade filters resources.* by dropped.* and preserves shrunken row", async () => { ... })` and analogous for PU-TR03-B. Place the new tests AFTER the existing PU-3+PU-7 block in the file (preserve the PU-* taxonomy ordering).

    8. Run `npm run test -- tests/orchestrators/plugin/uninstall.test.ts` and confirm GREEN; existing PU-3+PU-7 test MUST stay GREEN verbatim (no modification).

    9. Run `pre-commit run --files extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts tests/orchestrators/plugin/uninstall.test.ts`; fix any failures, restage, re-run until clean. Then commit per CLAUDE.md (Conventional Commits, no --no-verify): commit title `fix(orchestrators): uninstall.ts filters partial cascade outcome (TR-03)` (under 72 chars); body lines wrapped at 80; reference TR-03 and the field-name asymmetry (`dropped.commands -> resources.prompts`) in one body sentence.

    Do NOT (anti-patterns from RESEARCH):
    - Do NOT extract `applyPartialUnstageToRecord` to `marketplace/shared.ts` (TR-D03 DEFERRED; inline locality per Open Question 1 RESOLVED).
    - Do NOT mutate `outcome.dropped` or call `.sort()` on it (frozen; Pitfall 6).
    - Do NOT replace the AG-5 throw with a unified sentinel (Assumption A3 -- migrating to `withLockedStateTransaction` is OUT OF SCOPE for Phase 39).
    - Do NOT touch `narrowCascadeFailure`, `cascadeUnstagePlugin`, or the cascade primitive's signature (SC#1 + Anti-Patterns).
    - Do NOT introduce substring matching for AG-5 detection -- use `instanceof AgentsUnstageFailureError` (Pattern 3; honors quick-task 260525-aub typed-dispatch discipline).
    - Do NOT use a fenced code block of generated production code in a downstream artifact (catalog/comments) -- only inline mapping comments at the filter site.
  </action>

  <verify>
    <automated>npm run test -- tests/orchestrators/plugin/uninstall.test.ts</automated>
    <automated>grep -nE "outcome\.dropped\.commands" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts | grep -v "^#" | grep -c "prompts"</automated>
    <automated>grep -nE "instanceof AgentsUnstageFailureError" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npm run test -- tests/orchestrators/plugin/uninstall.test.ts` exits 0; new tests PU-TR03-A and PU-TR03-B PASS; existing PU-3+PU-7 (line 344) and all other PU-* tests PASS verbatim.
    - `grep -nE "outcome\.dropped\.commands" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns at least one line; that line also contains `resources.prompts` (proves the asymmetry mapping is wired correctly; "commands -> prompts" not "commands -> commands").
    - `grep -nE "instanceof AgentsUnstageFailureError" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns at least one line in the closure body (NOT only in imports) -- proves the AG-5 carve-out is wired by type discrimination, not substring matching.
    - `grep -c "cascadeFailure" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns >= 3 (declaration + set-in-closure + post-guard branch).
    - `pre-commit run --files extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts tests/orchestrators/plugin/uninstall.test.ts` exits 0.
    - One Conventional Commit landed with title `fix(orchestrators): uninstall.ts filters partial cascade outcome (TR-03)` (verify via `git log -1 --pretty=%s`).
  </acceptance_criteria>

  <done>
    uninstall.ts catch restructured: AG-5 cause throws inside guard (ST-7 abort-save preserves row); non-AG-5 cause mutates sRecord.resources.* in place using the dropped.commands -> resources.prompts asymmetry mapping, sets cascadeFailure sentinel, returns from closure so guard saves shrunken row; post-guard sentinel branch emits the existing PluginFailedMessage shape and returns BEFORE post-state cleanup. Two new PU-TR03-A/B tests appended to uninstall.test.ts, both GREEN, both asserting ON-DISK state after loadState reload. PU-3+PU-7 invariant test stays GREEN verbatim. Commit landed. Pre-commit GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: remove.ts per-plugin loop else-arm filter + 2 regression tests</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts,
    tests/orchestrators/marketplace/remove.test.ts
  </files>

  <read_first>
    extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts (full file ~333 lines -- per-plugin loop at lines 202-220, post-loop branches at 246-253 and 269+, narrowCascadeFailure mirror at lines 102-148, imports incl. AgentsUnstageFailureError at line 49),
    extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts (lines 55-62 AgentsUnstageFailureError; lines 290-302 UnstageOutcome; lines 317-395 cascadeUnstagePlugin including the dropped.commands <- installedPlugin.resources.prompts mapping at line 339 that locks the asymmetry),
    extensions/pi-claude-marketplace/transaction/with-state-guard.ts (lines 50-76 ST-7 contract -- relevant because the loop does NOT throw so guard saves the partial state at end of loop),
    extensions/pi-claude-marketplace/persistence/state-io.ts (lines 38-55 confirming resources.{skills,prompts,agents,mcpServers}),
    tests/orchestrators/marketplace/remove.test.ts (existing MR-4 cascade-failure test at line 338; the stubCascade injection seam pattern used by every cascade-dependent test; the multi-plugin marketplace seed pattern in MR-4),
    .planning/phases/39-cascade-ghost-record/39-RESEARCH.md (Pattern 2; Pitfalls 1+2+5; Code Examples block at lines 928-967; Validation Architecture Wave-0 test specs c+d)
  </read_first>

  <behavior>
    - Test 1 (MR-TR03-C: non-AG-5 partial filter): seed a state record with marketplace `acme-mp` containing `plugin-a` whose resources are `{ skills: [], prompts: ["c1","c2"], agents: [], mcpServers: [] }`. Stub cascade for plugin-a to return `{ ok: false, dropped: { skills: [], commands: ["c1"], agents: [], mcpServers: [] }, cause: new Error("forced EACCES") }`. Call `removeMarketplace({ name: "acme-mp", ... })`. Reload state. Assert: `state.marketplaces["acme-mp"] !== undefined` (mp retained because failedPlugins.length > 0; this is existing MR-4 invariant); `state.marketplaces["acme-mp"].plugins["plugin-a"] !== undefined` (plugin row preserved -- NOT deleted); `state.marketplaces["acme-mp"].plugins["plugin-a"].resources.prompts` deepEquals `["c2"]` (c1 filtered; the dropped.commands -> resources.prompts asymmetry is the load-bearing assertion); notifications.length === 1; severity error; message contains `(failed)`.
    - Test 2 (MR-TR03-D: AG-5 full-row preservation): same seed. Stub cascade for plugin-a to return `{ ok: false, dropped: { skills: [], commands: ["c1"], agents: [], mcpServers: [] }, cause: new AgentsUnstageFailureError("agents leak", []) }`. Call `removeMarketplace`. Reload state. Assert: `state.marketplaces["acme-mp"].plugins["plugin-a"] !== undefined`; `state.marketplaces["acme-mp"].plugins["plugin-a"].resources.prompts` deepEquals `["c1","c2"]` (UNCHANGED -- full row preserved per SC#3); notifications.length === 1; severity error. Locks the AG-5 carve-out at the remove.ts boundary.
    - Behavior of production code under test: per-plugin loop at lines 202-214 unchanged on the ok=true arm (delete record.plugins[pluginName] + successfullyUnstaged.push). On the ok=false arm at lines 209-213, BEFORE the existing `failedPlugins.push({ name: pluginName, cause })`, insert a discrimination branch: if `!(cause instanceof AgentsUnstageFailureError)` mutate `plugin.resources.{skills,prompts,agents,mcpServers}` in place via `.filter` with the `dropped.commands -> resources.prompts` mapping; else (AG-5) skip the filter. In BOTH branches, end with `failedPlugins.push({ name: pluginName, cause })`. The trailing `saveState` (inside the guard's no-throw return path) commits the shrunken record. The post-guard branches at lines 246+ remain unchanged.
  </behavior>

  <action>
    Implement TR-03 in `extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` per RESEARCH Pattern 2 ("Per-plugin in-loop filter"). Concrete steps (NO fenced code blocks):

    1. Locate the per-plugin loop at remove.ts:202-214 (the `for (const [pluginName, plugin] of Object.entries(record.plugins))` block inside the existing `withStateGuard` closure starting at line 181). The ok=true arm at lines 204-208 (successfullyUnstaged.push + delete) STAYS unchanged. The ok=false else arm at lines 209-213 (cause derivation + failedPlugins.push) is restructured.

    2. Inside the else arm (lines 209-213), AFTER `const cause = outcome.cause ?? new Error("unknown cascade failure for " + pluginName);` (line 211) and BEFORE `failedPlugins.push({ name: pluginName, cause });` (line 212), INSERT a discrimination branch:
       - `if (!(cause instanceof AgentsUnstageFailureError)) { ... }` body: in-place mutate `plugin.resources.{skills,prompts,agents,mcpServers}` via `.filter`. Concretely four lines: `plugin.resources.skills = plugin.resources.skills.filter((n) => !outcome.dropped.skills.includes(n));`, `plugin.resources.prompts = plugin.resources.prompts.filter((n) => !outcome.dropped.commands.includes(n));`, `plugin.resources.agents = plugin.resources.agents.filter((n) => !outcome.dropped.agents.includes(n));`, `plugin.resources.mcpServers = plugin.resources.mcpServers.filter((n) => !outcome.dropped.mcpServers.includes(n));`.
       - CRITICAL: the second line MUST read `plugin.resources.prompts` paired with `outcome.dropped.commands` -- the field-name asymmetry (Pitfall 1) is load-bearing. Inline-comment the mapping above the four-line block with the same four-row table from Task 1 (`dropped.skills -> resources.skills`, `dropped.commands -> resources.prompts (note asymmetry)`, `dropped.agents -> resources.agents`, `dropped.mcpServers -> resources.mcpServers`).
       - The AG-5 case (else / fall-through) intentionally skips the filter. Inline-comment: "// SC#3: AG-5 foreign-content cause -- preserve the full row (foreign content owned by another process must not cause data loss). The row stays intact; failedPlugins.push still fires for the failure notification."

    3. After the discrimination branch, the existing `failedPlugins.push({ name: pluginName, cause });` (line 212) STAYS as the final statement of the else arm. The push fires for BOTH non-AG-5 (after filter) and AG-5 (no filter) so the failure notification surface is unchanged.

    4. Pitfall 5 check: the `delete record.plugins[pluginName]` MUST stay INSIDE the `if (outcome.ok)` arm (line 208). The else arm NEVER deletes -- it shrinks (non-AG-5) or preserves (AG-5). The trailing `if (failedPlugins.length === 0) delete state.marketplaces[opts.name];` at lines 216-219 stays unchanged: when ANY plugin failed, the mp record is retained.

    5. The post-guard branches at remove.ts:222+ (cache cleanup at 230-236, post-state cleanup at 242-253, notify branches at 269+) STAY unchanged. The guard's `saveState` commits the shrunken record at the closure-return boundary; downstream code reads the partially-mutated `record` reference.

    6. Append the two new tests (MR-TR03-C and MR-TR03-D per <behavior>) to `tests/orchestrators/marketplace/remove.test.ts` USING the existing stubCascade injection seam pattern that MR-4 (line 338) already uses. The tests MUST call `loadState` AFTER `removeMarketplace` returns to assert the ON-DISK shrunken state. Use `assert.deepEqual` for the `resources.prompts` array assertion (the load-bearing asymmetry lock); `assert.equal(notifications.length, 1)` and severity assertion. Test naming: `test("TR-03 MR-TR03-C: non-AG-5 partial cascade filters resources.prompts by dropped.commands and preserves shrunken row", async () => { ... })` and analogous for MR-TR03-D. Place the new tests after the existing MR-4 block.

    7. Run `npm run test -- tests/orchestrators/marketplace/remove.test.ts` and confirm GREEN; existing MR-4 test MUST stay GREEN verbatim.

    8. Run `pre-commit run --files extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts tests/orchestrators/marketplace/remove.test.ts`; fix any failures, restage, re-run. Then commit per CLAUDE.md: commit title `fix(orchestrators): remove.ts filters partial cascade outcome (TR-03)` (under 72 chars); body lines wrapped at 80; reference TR-03 and the asymmetry mapping.

    Do NOT (anti-patterns):
    - Do NOT extract a helper to `shared.ts` (TR-D03 DEFERRED).
    - Do NOT add a throw inside the loop -- the existing loop is fail-soft by design and MR-4 depends on it.
    - Do NOT delete `record.plugins[pluginName]` in the else arm (Pitfall 5).
    - Do NOT mutate `outcome.dropped` (frozen; Pitfall 6).
    - Do NOT touch `narrowCascadeFailure`, `cascadeUnstagePlugin`, or the cascade primitive's signature.
    - Do NOT introduce substring matching for AG-5 detection.
  </action>

  <verify>
    <automated>npm run test -- tests/orchestrators/marketplace/remove.test.ts</automated>
    <automated>grep -nE "outcome\.dropped\.commands" extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts | grep -v "^#" | grep -c "prompts"</automated>
    <automated>grep -nE "instanceof AgentsUnstageFailureError" extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npm run test -- tests/orchestrators/marketplace/remove.test.ts` exits 0; new tests MR-TR03-C and MR-TR03-D PASS; existing MR-4 (line 338) and all other MR-* tests PASS verbatim.
    - `grep -nE "outcome\.dropped\.commands" extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns at least one line that also contains `resources.prompts` (asymmetry mapping wired correctly).
    - `grep -nE "instanceof AgentsUnstageFailureError" extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns at least one line in the per-plugin loop body (NOT only in imports).
    - Per-plugin loop's `delete record.plugins[pluginName]` is INSIDE the `if (outcome.ok)` arm only -- verified via `grep -nE "delete record\.plugins" extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returning the line in the ok-true arm (line ~208 region).
    - `pre-commit run --files extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts tests/orchestrators/marketplace/remove.test.ts` exits 0.
    - One Conventional Commit landed with title `fix(orchestrators): remove.ts filters partial cascade outcome (TR-03)`.
  </acceptance_criteria>

  <done>
    remove.ts per-plugin loop else arm extended: non-AG-5 cause filters plugin.resources.* in place using the dropped.commands -> resources.prompts asymmetry; AG-5 cause skips the filter (full row preserved); failedPlugins.push fires for both. Two new MR-TR03-C/D tests appended, both GREEN, both asserting ON-DISK state. MR-4 stays GREEN verbatim. Commit landed. Pre-commit GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regression gate -- cascade primitive read-only proof + full npm run check</name>
  <files>
    (verification only -- no source modifications expected; if a regression surfaces in shared.test.ts, fix is in scope and uses the established cascade test patterns)
  </files>

  <read_first>
    tests/orchestrators/marketplace/shared.test.ts (the cascade-primitive test suite; confirm it includes assertions that outcome.dropped is frozen and that cascadeUnstagePlugin does NOT mutate state -- if absent, the existing Object.freeze at shared.ts:374-393 + the read-only test gates in the new uninstall/remove tests provide indirect coverage),
    .planning/phases/39-cascade-ghost-record/39-RESEARCH.md (SC#5 wording; Validation Architecture per-task verify map; Sampling Rate "Phase gate" section),
    .planning/phases/39-cascade-ghost-record/39-VALIDATION.md (Per-Task Verification Map rows 39-01-03 + 39-01-04 + 39-01-05),
    extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts (lines 374-393 confirming Object.freeze on outcome and outcome.dropped)
  </read_first>

  <action>
    Phase-gate verification. NO new source-code changes are expected in this task; it is a verification gate that proves Tasks 1+2 closed TR-03 without regression.

    1. Run `npm run test -- tests/orchestrators/marketplace/shared.test.ts` and confirm GREEN. If the suite asserts that `cascadeUnstagePlugin` does not mutate state and that `outcome.dropped` is frozen, both gates remain GREEN (the primitive shape was NOT touched in Tasks 1+2). If a test fails (unexpected -- Tasks 1+2 do not modify shared.ts), STOP and investigate before proceeding.

    2. Run the full phase-gate command: `npm run check`. Confirm exit code 0 and a final test count of at least 1362 tests (1358 baseline from Phase 38 + 4 new from Tasks 1+2 = 1362 minimum). If `npm run check` fails on typecheck, lint, format, OR test, STOP and fix at the source. Do not work around failures.

    3. Capture the test-count delta and existing-test-preservation evidence:
       - Run `npm run test 2>&1 | tail -20` and capture the "tests N passed" line into the task's verification record.
       - Confirm specifically: PU-3+PU-7 test at tests/orchestrators/plugin/uninstall.test.ts:344 is GREEN (run `npm run test -- tests/orchestrators/plugin/uninstall.test.ts 2>&1 | grep -E "(PU-3|PU-7|TR-03)"` and verify all four matches PASS).
       - Confirm MR-4 test at tests/orchestrators/marketplace/remove.test.ts:338 is GREEN (run `npm run test -- tests/orchestrators/marketplace/remove.test.ts 2>&1 | grep -E "(MR-4|TR-03)"` and verify all PASS).

    4. Cross-file mapping consistency check (field-name asymmetry lock): `grep -nE "outcome\.dropped\.commands.*prompts|prompts.*outcome\.dropped\.commands" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` -- both files MUST return at least one match. This is the grep-checkable proof of Pitfall 1 mitigation.

    5. No commit is created in this task (verification-only). The SUMMARY.md (generated by the execute-plan template) records the regression-gate evidence: test count, existing-test-preservation results, and the grep-checked asymmetry mapping.

    If any verification fails, do NOT proceed to SUMMARY emission. Surface the failure in-band and treat it as a blocker for `/gsd-verify-work`.
  </action>

  <verify>
    <automated>npm run test -- tests/orchestrators/marketplace/shared.test.ts</automated>
    <automated>npm run check</automated>
    <automated>grep -nE "outcome\.dropped\.commands.*prompts|prompts.*outcome\.dropped\.commands" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npm run test -- tests/orchestrators/marketplace/shared.test.ts` exits 0; cascade primitive read-only contract preserved (no mutation of state by cascadeUnstagePlugin).
    - `npm run check` exits 0; final test pass count >= 1362 (1358 Phase 38 baseline + 4 new TR-03 tests from Tasks 1+2).
    - PU-3+PU-7 test at uninstall.test.ts:344 reports PASS in the suite output.
    - MR-4 test at remove.test.ts:338 reports PASS in the suite output.
    - `grep -nE "outcome\.dropped\.commands.*prompts|prompts.*outcome\.dropped\.commands" ...` returns matches in BOTH uninstall.ts AND remove.ts (asymmetry mapping wired in both fix sites).
    - No new git commit (verification-only task).
  </acceptance_criteria>

  <done>
    npm run check GREEN at 1362+ tests; cascade primitive read-only contract verified GREEN via shared.test.ts; PU-3+PU-7 and MR-4 existing invariants confirmed GREEN; field-name asymmetry mapping (dropped.commands -> resources.prompts) is grep-verifiable in both uninstall.ts and remove.ts; phase gate satisfied for `/gsd-verify-work`.
  </done>
</task>

</tasks>

<verification>
End-of-phase verification (executed via Task 3):

1. **Cascade primitive read-only:** `npm run test -- tests/orchestrators/marketplace/shared.test.ts` GREEN; cascadeUnstagePlugin makes no state mutation; outcome.dropped frozen (Object.freeze at shared.ts:374-393).
2. **Filter correctness in both fix sites:** uninstall.ts AND remove.ts both contain a four-line filter with the `dropped.commands -> resources.prompts` asymmetry locked by inline comment AND by the four new TR-03 regression tests.
3. **AG-5 carve-out preserved at both fix sites:** `instanceof AgentsUnstageFailureError` discrimination is wired BEFORE the filter at both sites; the AG-5 path preserves the full row.
4. **Existing invariants preserved:** PU-3+PU-7 test (uninstall.test.ts:344) + MR-4 test (remove.test.ts:338) stay GREEN verbatim.
5. **Full phase gate:** `npm run check` GREEN at 1362+ tests (1358 Phase 38 baseline + 4 new).

Maps to VALIDATION.md rows: 39-01-01 (uninstall filter -- Task 1), 39-01-02 (remove filter -- Task 2), 39-01-03 (cascade primitive read-only -- Task 3), 39-01-04 (field-name asymmetry grep gate -- Tasks 1+2 verify blocks), 39-01-05 (npm run check -- Task 3).
</verification>

<success_criteria>
Phase 39 closes TR-03 when ALL of the following are TRUE:

1. **SC#1 (uninstall.ts non-AG-5 filter):** When `outcome.ok === false` AND cause is NOT AgentsUnstageFailureError, sRecord.resources.{skills,prompts,agents,mcpServers} are filtered to remove names present in outcome.dropped.{skills,commands,agents,mcpServers}; the shrunken row persists via the guard's saveState. The cascade primitive itself makes no state mutation. (Task 1; SC verified by PU-TR03-A test.)

2. **SC#2 (remove.ts per-plugin loop):** Same filter applied per-plugin inside the loop; mp record retained when any plugin failed; per-plugin row preserved (not deleted) when filter applied. (Task 2; SC verified by MR-TR03-C test.)

3. **SC#3 (AG-5 carve-out at BOTH sites):** When cause instanceof AgentsUnstageFailureError, the state row is preserved INTACT -- no filter applied -- so foreign content owned by another process does not cause data loss. (Tasks 1+2; SC verified by PU-TR03-B and MR-TR03-D tests.)

4. **SC#4 (regression coverage):** 4 new regression tests cover both sites x both cause types (non-AG-5 partial + AG-5 full-row). All assert ON-DISK state via loadState reload, locking Pitfall 3 (ST-7 throw-aborts-save trap). (Tasks 1+2.)

5. **SC#5 (no regression):** npm run check GREEN; total test count >= 1362 (1358 Phase 38 baseline + 4 new). Existing PU-3+PU-7 (uninstall.test.ts:344) and MR-4 (remove.test.ts:338) tests GREEN verbatim. (Task 3.)
</success_criteria>

<output>
On completion, generate `.planning/phases/39-cascade-ghost-record/39-01-SUMMARY.md` per the standard template, capturing:
- TR-03 requirement closure with evidence (test names, line numbers, commit SHAs)
- Field-name asymmetry mitigation (dropped.commands -> resources.prompts) with the grep-checked proof in both files
- AG-5 carve-out evidence (instanceof AgentsUnstageFailureError discrimination at both sites)
- PU-3+PU-7 and MR-4 invariant preservation
- Test count delta (1358 -> 1362)
- Two Conventional Commits (one per task 1+2; task 3 is verification-only)
</output>
