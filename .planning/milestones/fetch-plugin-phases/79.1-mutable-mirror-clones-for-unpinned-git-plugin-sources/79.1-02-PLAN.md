---
phase: 79.1-mutable-mirror-clones-for-unpinned-git-plugin-sources
plan: 02
type: execute
wave: 2
depends_on: ["79.1-01"]
autonomous: true
requirements: [MIRR-05]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts
  - tests/orchestrators/plugin/git-source-probe.test.ts
  - tests/orchestrators/plugin/mirror-head-read.test.ts

must_haves:
  truths:
    - "makePresenceProbe derives an unpinned source's fetched-state from mirror-dir presence (plugin-clones/<urlhash12>/) fs-only — no prefix scan, no network"
    - "An unpinned source with a warm mirror returns { kind: materialized, pluginRoot: <mirrorDir>, resolvedSha } where resolvedSha is read fs-only from the mirror's checked-out HEAD"
    - "An unpinned source with no mirror dir returns { kind: not-cached } (the Phase-80 (remote) arm)"
    - "The fs-only HEAD read resolves BOTH loose-ref and packed-ref layouts as materialized by isomorphic-git, verified against a real isomorphic-git clone fixture"
    - "git-source-probe.ts still imports zero git surface (no gitOps / DEFAULT_GIT_OPS / platform/git / refreshGitHubClone)"
  artifacts:
    - "orchestrators/plugin/git-source-probe.ts::readMirrorHeadSha(mirrorDir) fs-only HEAD reader (loose + packed refs)"
    - "makePresenceProbe unpinned arm rewritten to mirror presence"
    - "tests/orchestrators/plugin/mirror-head-read.test.ts — real isomorphic-git fixture verification"
  key_links:
    - "makePresenceProbe → pluginMirrorKey(cloneUrl) → locations.pluginCloneDir(mirrorKey) → pathExists (SC-7 chokepoint)"
    - "readMirrorHeadSha uses plain node:fs readFile only — never spawns git; keeps the module git-import-free so list/completion importers stay network-free"
---

<objective>
Invert the `makePresenceProbe` unpinned arm from the old `not-cached` degrade to mirror-dir presence, and add the fs-only HEAD-sha reader the `materialized` result requires. This is the read-surface half of the mirror substrate (MIRR-05) — consumed downstream by list, completion, and Phase 80's `info`/`(remote)` classifier. It is the phase's ONLY unverified mechanism: the fs-only sha read must handle both loose and packed refs as isomorphic-git writes them, so it is verified against a REAL isomorphic-git clone fixture.

Purpose: Let read surfaces resolve an unpinned plugin's components fs-only from the warm mirror, with zero network and zero git surface, so the no-orchestrator-network gate stays green for list/info importers (MIRR-05 / NFR-5).
Output: `readMirrorHeadSha` (fs-only loose+packed ref reader), the rewritten unpinned arm of `makePresenceProbe`, and a real-fixture test proving the read against an isomorphic-git-produced clone.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/url-source/phases/79.1-mutable-mirror-clones-for-unpinned-git-plugin-sources/79.1-CONTEXT.md
@.planning/workstreams/url-source/phases/79.1-mutable-mirror-clones-for-unpinned-git-plugin-sources/79.1-RESEARCH.md
@.planning/workstreams/url-source/phases/79.1-mutable-mirror-clones-for-unpinned-git-plugin-sources/79.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add readMirrorHeadSha fs-only HEAD reader, verified against a real isomorphic-git clone</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts, tests/orchestrators/plugin/mirror-head-read.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts (fs-only module; imports pathExists from shared/fs-utils.ts, pluginCloneKey from domain/clone-key.ts; header at lines 11-16 declares "carries no clone-materializing git seam and never spawns git")
    - tests/platform/git-auth-callbacks.test.ts (lines 223-260: the real isomorphic-git repo-builder pattern — `import * as git from "isomorphic-git"`, `import * as fs from "node:fs"`, `git.init({ fs, dir, defaultBranch: "main" })`, `writeFile`, `git.commit({...})`, then `resolveRef({ dir, ref: "HEAD" })` returns the sha). Copy this to build the fixture.
    - extensions/pi-claude-marketplace/platform/git.ts (how isomorphic-git resolveRef reads HEAD — reference for what loose vs packed layout the fs read must match)
    - .claude/rules/typescript-comments.md (MIRR-05 / A1 are allowed anchors; no phase/plan/wave tokens)
  </read_first>
  <behavior>
    - readMirrorHeadSha(dir) reads <dir>/.git/HEAD; when HEAD is a symbolic ref ("ref: refs/heads/<b>"), it resolves the sha from the loose ref file <dir>/.git/refs/heads/<b> when present, else from <dir>/.git/packed-refs
    - When HEAD is a detached 40-hex sha (no "ref:" prefix), readMirrorHeadSha returns that sha directly
    - Given a clone produced by real isomorphic-git (git.init + git.commit, loose-ref layout), readMirrorHeadSha returns exactly the sha that git.resolveRef({ dir, ref: "HEAD" }) returns
    - Given a repo whose ref lives ONLY in packed-refs (loose ref file absent), readMirrorHeadSha still returns the correct sha
    - readMirrorHeadSha uses only node:fs/promises readFile — it never imports or calls isomorphic-git / gitOps
  </behavior>
  <action>
    Add an exported async helper `readMirrorHeadSha(mirrorDir: string): Promise<string>` to git-source-probe.ts using ONLY `node:fs/promises` readFile (add the import; the module already imports `pathExists` from shared/fs-utils.ts which you may reuse for existence checks). Algorithm: read `path.join(mirrorDir, ".git", "HEAD")` trimmed. If it starts with `"ref: "`, take the ref path after that prefix (e.g. `refs/heads/main`); try reading the loose ref file `path.join(mirrorDir, ".git", <refPath>)` and return its trimmed 40-hex content; if that file is absent (ENOENT), parse `path.join(mirrorDir, ".git", "packed-refs")` line-by-line for a line whose second whitespace-delimited field equals the ref path and return the first field (the sha), skipping comment lines that start with `#` and peeled-tag lines that start with `^`. If HEAD does NOT start with `"ref: "`, it is a detached sha — return it trimmed. Add `import path from "node:path"` if not present. Cite MIRR-05 and A1 (the isomorphic-git on-disk-layout assumption this resolves) as anchors. Do NOT import isomorphic-git, gitOps, DEFAULT_GIT_OPS, or refreshGitHubClone — this module must stay git-surface-free.

    Create tests/orchestrators/plugin/mirror-head-read.test.ts. Using the real-repo pattern from tests/platform/git-auth-callbacks.test.ts: (a) build a real clone with `git.init({ fs, dir, defaultBranch: "main" })` + a file write + `git.commit(...)`, then assert `readMirrorHeadSha(dir)` equals `await git.resolveRef({ fs, dir, ref: "HEAD" })` (loose-ref layout); (b) run `git.pack`/`git.packObjects` is unnecessary — instead simulate the packed-refs layout by moving the loose ref content into a `.git/packed-refs` file and deleting the loose ref file, then assert readMirrorHeadSha still returns the same sha; (c) a detached-HEAD case: overwrite `.git/HEAD` with a bare 40-hex sha and assert readMirrorHeadSha returns it. Use mkdtemp under os.tmpdir() and clean up.
  </action>
  <verify>
    <automated>node --test "tests/orchestrators/plugin/mirror-head-read.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function readMirrorHeadSha" extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts` returns a match
    - A test builds a real isomorphic-git clone and asserts `readMirrorHeadSha(dir)` deep-equals `git.resolveRef({ fs, dir, ref: "HEAD" })`
    - A packed-refs-only test (loose ref file deleted) passes
    - A detached-HEAD (bare sha in .git/HEAD) test passes
    - `grep -v '^\s*//' extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts | grep -Ec 'isomorphic-git|\bgitOps\b|DEFAULT_GIT_OPS|refreshGitHubClone|platform/git'` returns 0 (module stays git-surface-free)
    - `node --test "tests/orchestrators/plugin/mirror-head-read.test.ts"` passes
  </acceptance_criteria>
  <done>readMirrorHeadSha reads the mirror HEAD sha fs-only across loose-ref, packed-refs, and detached layouts, is proven against a real isomorphic-git clone, and adds no git surface to the module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite makePresenceProbe unpinned arm to mirror-dir presence (MIRR-05)</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts, tests/orchestrators/plugin/git-source-probe.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts (makePresenceProbe lines 58-81; the current unpinned arm at lines 71-73 returns not-cached; cloneUrl reconstruction lines 66-67; pinned arm lines 75-79)
    - extensions/pi-claude-marketplace/domain/clone-key.ts (pluginMirrorKey — from Plan 01)
    - extensions/pi-claude-marketplace/domain/resolver.ts (GitPluginRootResult union lines 257-259: the `materialized` arm REQUIRES a non-optional `resolvedSha: string`, so the unpinned materialized result MUST supply a sha)
    - tests/orchestrators/plugin/git-source-probe.test.ts (existing unpinned test at line 88 "makePresenceProbe: an unpinned source (no sha) returns not-cached without touching the network" — this assertion INVERTS under MIRR-05; the pinned tests at lines 101/125 stay)
  </read_first>
  <behavior>
    - makePresenceProbe with an unpinned source (source.sha === undefined) and NO mirror dir on disk → returns { kind: "not-cached" }
    - makePresenceProbe with an unpinned source and a PRESENT mirror dir (plugin-clones/<pluginMirrorKey(cloneUrl)>/) → returns { kind: "materialized", pluginRoot: <mirrorDir>, resolvedSha: <readMirrorHeadSha(mirrorDir)> }, touching no network
    - The pinned arm (source.sha set) is UNCHANGED: pluginCloneKey(cloneUrl, source.sha) → pluginCloneDir → pathExists → materialized/not-cached
    - No git surface is touched on any arm (fs-only)
  </behavior>
  <action>
    In makePresenceProbe, replace the current unpinned early-return (lines 71-73, `if (source.sha === undefined) { return { kind: "not-cached" }; }`) with the mirror-presence arm: when `source.sha === undefined`, compute `const mirrorDir = await locations.pluginCloneDir(pluginMirrorKey(cloneUrl))`; if `!(await pathExists(mirrorDir))` return `{ kind: "not-cached" }` (the arm Phase 80 renders as `(remote)`); otherwise read `const sha = await readMirrorHeadSha(mirrorDir)` (Task 1) and return `{ kind: "materialized", pluginRoot: mirrorDir, resolvedSha: sha }`. Import `pluginMirrorKey` from ../../domain/clone-key.ts (extend the existing line-18 import that already brings pluginCloneKey). Reuse the existing `cloneUrl` reconstruction (lines 66-67) and the existing `pathExists` import — do NOT reconstruct the URL a second time. Leave the pinned arm (lines 75-79) untouched. Update the module docstring's unpinned-source note (currently "returns not-cached") to describe the mirror-presence derivation, citing MIRR-05 (no phase/plan/wave tokens per the comment policy). Do NOT add any gitOps / network call — this is the fs-only read surface: read surfaces read the warm mirror fs-only and NEVER refresh (D-79.1-02, network policy unchanged).

    In tests/orchestrators/plugin/git-source-probe.test.ts: rewrite the existing line-88 test so an unpinned source with NO mirror dir returns not-cached (assertion kept) AND add a companion test where the mirror dir is pre-created on disk (mkdir plugin-clones/<pluginMirrorKey(cloneUrl)>/ with a minimal `.git/HEAD` + loose ref, or reuse a small real-git fixture) so the probe returns materialized with the fs-read sha and records no network. Frame the change as the intentional MIRR-05 behavior inversion, not a regression.
  </action>
  <verify>
    <automated>node --test "tests/orchestrators/plugin/git-source-probe.test.ts" "tests/architecture/no-orchestrator-network.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - The unpinned arm no longer unconditionally returns not-cached: `grep -n "pluginMirrorKey" extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts` returns a match inside makePresenceProbe
    - A test asserts an unpinned source with a present mirror dir returns `{ kind: "materialized" }` with a non-empty resolvedSha and no recorded network call
    - A test asserts an unpinned source with no mirror dir still returns `{ kind: "not-cached" }`
    - The pinned-source tests (present dir → materialized, absent dir → not-cached) still pass unchanged
    - `node --test "tests/architecture/no-orchestrator-network.test.ts"` stays green (git-source-probe stays git-free; list.ts importer unaffected)
  </acceptance_criteria>
  <done>makePresenceProbe derives unpinned fetched-state from mirror-dir presence fs-only, supplies the resolver-required resolvedSha via readMirrorHeadSha, leaves the pinned arm intact, and keeps the module and its importers network-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk mirror .git dir → read surface | The read surface trusts the local mirror tree; it must not reach the network to derive state |
| plugin manifest source URL → mirror-key lookup | The canonical URL is hashed into the presence-probe path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79.1-04 | Elevation of privilege | read surface accidentally spawning git/network | high | mitigate | git-source-probe.ts stays git-surface-free (verified by grep gate + no-orchestrator-network gate on its list.ts importer); the fs-only readMirrorHeadSha uses plain readFile |
| T-79.1-05 | Tampering | presence-probe path traversal via mirror key | high | mitigate | Route through locations.pluginCloneDir (SC-7: assertSafeName + assertPathInside); bare 12-hex key cannot escape |
| T-79.1-06 | Repudiation | stale/empty sha from a packed-refs-only mirror | medium | mitigate | readMirrorHeadSha handles both loose and packed refs, verified against a real isomorphic-git clone fixture (A1) |
</threat_model>

<verification>
- `node --test "tests/orchestrators/plugin/mirror-head-read.test.ts"` passes (real-fixture loose + packed + detached)
- `node --test "tests/orchestrators/plugin/git-source-probe.test.ts"` passes (mirror-presence inversion + pinned arm intact)
- `node --test "tests/architecture/no-orchestrator-network.test.ts"` green
- `npm run check` green before the plan is done
</verification>

<success_criteria>
- readMirrorHeadSha reads the mirror HEAD sha fs-only across loose/packed/detached layouts, proven against real isomorphic-git output
- makePresenceProbe returns materialized-from-mirror for a warm unpinned source and not-cached for a cold one, with zero network
- git-source-probe.ts imports no git surface; the no-orchestrator-network gate stays green
</success_criteria>

<output>
Create `.planning/workstreams/url-source/phases/79.1-mutable-mirror-clones-for-unpinned-git-plugin-sources/79.1-02-SUMMARY.md` when done
</output>
