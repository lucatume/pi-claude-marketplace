---
phase: 79.1-mutable-mirror-clones-for-unpinned-git-plugin-sources
verified: 2026-07-14T06:45:11Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 79.1: Mutable mirror clones for unpinned git plugin sources Verification Report

**Phase Goal:** An unpinned git-source plugin (url / git-subdir / github with no manifest `sha`) is backed by exactly ONE marketplace-style mutable mirror clone per canonical URL at `plugin-clones/<urlhash12>/` — refreshed in place like a marketplace clone — so fetched-state derives from a single well-known directory and multi-clone ambiguity is impossible by construction; pinned sources keep the per-sha immutable content-addressed cache unchanged.

**Verified:** 2026-07-14T06:45:11Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Installing an unpinned git-source plugin materializes/refreshes the single mirror clone at `plugin-clones/<urlhash12>/` (no sha suffix) and installs from its working tree; recorded version = resolved sha (PURL-09/PI-7) | VERIFIED | `domain/clone-key.ts::pluginMirrorKey` (bare 12-hex, provably the left half of `pluginCloneKey`); `orchestrators/plugin/clone-cache.ts::materializeOrRefreshPluginMirror` (materialize-on-cold via staging+atomic-rename, no fixed-pin checkout); `install.ts::makeInstallCloneProbe` forks on `gitSource.sha === undefined` to the mirror seam (lines 572-592), returns `resolvedSha` from mirror HEAD. `install.test.ts` (92 pass) asserts bare-key `resolvedSource` + `sha-<12hex>` recorded version for unpinned url/git-subdir installs. |
| 2 | Pinned sources continue to use `plugin-clones/<urlhash12>-<sha12>/` byte-identically to today | VERIFIED | `materializePluginClone` (pinned per-sha path) is untouched by this phase (git history shows no modification to its body); `install.test.ts`/`update.test.ts` include explicit pinned-regression cases asserting the `<12hex>-<12hex>` key shape unchanged. |
| 3 | Mirror refresh is in-place at parity with marketplace clone update (fetch + force-update ref + checkout), idempotent or fail-clean; a concurrent same-URL operation never corrupts the mirror | VERIFIED | `materializeOrRefreshPluginMirror` calls `refreshGitHubClone` verbatim (the same function `orchestrators/marketplace/update.ts` uses) on every call, including a just-materialized mirror (refresh-on-warm, D-79.1-02). EEXIST/ENOTEMPTY rename race is treated as concurrent-win (clean own staging, fall through to refresh) rather than a throw. `clone-cache.test.ts` test `MIRR-03: a concurrent create losing the rename race treats the winner's dir as the warm mirror (no throw)` passes; idempotency (two successive calls both succeed) covered. |
| 4 | The fs-only presence probe and classifier derive unpinned fetched-state from mirror-dir existence through the SC-7 chokepoint — no prefix scan, no network — and components resolve fs-only from the mirror working tree | VERIFIED | `git-source-probe.ts::makePresenceProbe` unpinned arm computes `pluginMirrorKey(cloneUrl)` → `locations.pluginCloneDir` (SC-7) → `pathExists`; warm mirror returns `materialized` with `resolvedSha` from `readMirrorHeadSha` (fs-only, no `isomorphic-git`/`gitOps`/`DEFAULT_GIT_OPS`/`platform/git` import — grep confirms 0 matches). `readMirrorHeadSha` verified against a REAL isomorphic-git clone fixture across loose-ref, packed-refs-only, and detached-HEAD layouts (`mirror-head-read.test.ts`, 11 pass). |
| 5 | GC treats the mirror as referenced while any installed plugin records that canonical URL unpinned; unreferenced mirrors and orphaned per-sha clones of unpinned sources are swept by `garbageCollectPluginClones` — no persisted migration state | VERIFIED | `clone-gc.ts::deriveLiveCloneKeys` is byte-unchanged (confirmed via `git diff` against the pre-phase commit) — its pre-existing first-path-segment derivation (`path.relative(pluginClonesDir, resolvedSource).split(path.sep)[0]`) already yields the bare `<urlhash12>` for both a mirror root and a mirror+subdir record. New tests in `clone-gc.test.ts` (MIRR-04/MIRR-06) assert a mirror-anchored record's bare-key dir survives GC while an unreferenced per-sha orphan of the same URL is swept, a git-subdir mirror record still protects the bare root, and an old-design per-sha record still protects its own dir (coexistence). No new persisted-state file was added anywhere in the diff. |
| 6 | Reinstall repairs an unpinned mirror-anchored plugin fs-only, without re-anchoring or materializing a cold mirror (D-79.1-04 lazy re-anchor); pinned/old-design behavior preserved | VERIFIED | `reinstall.ts::makeReinstallCloneProbe` unpinned arm (lines 1335-1362) checks mirror presence via `pluginMirrorKey`+`pathExists`, reads `readMirrorHeadSha` fs-only when warm, and falls through to the EXISTING recorded-sha `materializePluginClone` path when the mirror is cold — no re-anchor, no cold materialize, no network. `grep` confirms reinstall.ts imports no git surface (0 matches for `gitOps`/`DEFAULT_GIT_OPS`/`refreshGitHubClone`/`platform/git`). `reinstall.test.ts` (74 pass) covers warm-mirror repair, per-sha coexistence, and cold-degrade-without-network-clone. |

**Score:** 6/6 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `domain/clone-key.ts::pluginMirrorKey` | bare 12-hex mirror key, no sha suffix | VERIFIED | Present, exported, reuses existing `KEY_TRUNC`/`createHash` (no duplicate declarations); provably equals `pluginCloneKey`'s left half (test asserted). |
| `orchestrators/plugin/clone-cache.ts::materializeOrRefreshPluginMirror` | git-legal mirror materialize+refresh seam | VERIFIED | Present, exported; routes through `pluginCloneDir(pluginMirrorKey(...))` (SC-7); materialize-on-cold (staging→atomic-rename, no fixed-pin checkout) + refresh-on-warm via `refreshGitHubClone`; returns `{ pluginRoot, resolvedSha }`. |
| `orchestrators/plugin/git-source-probe.ts::readMirrorHeadSha` | fs-only HEAD reader (loose+packed+detached) | VERIFIED | Present, exported; uses only `node:fs/promises`; verified against a real isomorphic-git fixture across all three ref layouts. |
| `makePresenceProbe` unpinned arm | mirror-presence derivation, fs-only | VERIFIED | Rewritten from unconditional `not-cached` to mirror-dir presence check; pinned arm untouched; module still imports zero git surface. |
| `InstallCloneCacheSeam`/`UpdateCloneCacheSeam` extended with `materializeOrRefreshPluginMirror` | seam wiring for install/update | VERIFIED | Both interfaces extended (required member), default injections updated, probe forks present at 4 sites each (import, interface, fork, injection) per grep in both SUMMARYs and independently re-confirmed. |
| `makeReinstallCloneProbe` unpinned mirror arm | warm-mirror fs-only repair | VERIFIED | Present at top of probe; falls through to existing per-sha path when mirror absent; reinstall.ts remains a clean `FORBIDDEN_TARGET` (0 git-surface matches). |
| `clone-gc.ts` coexistence tests | mirror protected, per-sha orphan swept | VERIFIED | `clone-gc.ts` itself is byte-unchanged since before the phase (git diff empty); new tests added and passing (11/11 in file, including the 3 new MIRR-04/06 cases). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `materializeOrRefreshPluginMirror` | `locations.pluginCloneDir(pluginMirrorKey(cloneUrl))` | SC-7 chokepoint | WIRED | Confirmed by direct source read (clone-cache.ts:162). |
| `materializeOrRefreshPluginMirror` refresh path | `refreshGitHubClone` (marketplace/shared.ts) | verbatim reuse | WIRED | Confirmed by direct source read (clone-cache.ts:205) — same function marketplace update calls. |
| `makeInstallCloneProbe`/`makeUpdateCloneProbe` unpinned fork | `seam.materializeOrRefreshPluginMirror(...)` | injected seam member | WIRED | Confirmed in both install.ts (line 577) and update.ts (line 799); orchestrator bodies never name gitOps directly (install.ts 0 matches; update.ts is the sanctioned S-9 exempt orchestrator). |
| `makePresenceProbe` unpinned arm | `pluginMirrorKey` → `pluginCloneDir` → `pathExists` → `readMirrorHeadSha` | fs-only chain | WIRED | Confirmed in git-source-probe.ts lines 122-132; no git import in the module. |
| `makeReinstallCloneProbe` unpinned arm | `pluginMirrorKey` → `pluginCloneDir` → `pathExists` → `readMirrorHeadSha` | fs-only chain, no re-anchor | WIRED | Confirmed in reinstall.ts lines 1343-1362. |
| `garbageCollectPluginClones` | `deriveLiveCloneKeys` first-path-segment | resolvedSource → live key set | WIRED | Confirmed unchanged in clone-gc.ts:36-52; new tests prove it protects the bare mirror key and mirror+subdir shapes. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|--------------|--------|----------|
| MIRR-01 | 79.1-01, 79.1-03 | Exactly ONE mutable mirror clone per canonical URL at `plugin-clones/<urlhash12>/`; pinned unchanged | SATISFIED | `pluginMirrorKey` + `materializeOrRefreshPluginMirror` + install/update probe forks; tests pass. |
| MIRR-02 | 79.1-01 | Mirror refresh in-place at marketplace parity; idempotent/fail-clean; concurrent same-URL safe | SATISFIED | `refreshGitHubClone` reuse verbatim; EEXIST/ENOTEMPTY concurrent-win handling; `clone-cache.test.ts` 32/32 pass. |
| MIRR-03 | 79.1-01, 79.1-03 | Install/update/reinstall consume the mirror working tree; recorded version = resolved sha | SATISFIED | install.ts/update.ts/reinstall.ts probes all return `resolvedSha` from mirror HEAD; `sha-<12hex>` version derivation unchanged downstream. |
| MIRR-04 | 79.1-04 | GC treats mirror as referenced; unreferenced mirrors swept | SATISFIED | `deriveLiveCloneKeys` unchanged (verified via diff); new clone-gc.test.ts cases pass (bare-key protection + orphan sweep + git-subdir). |
| MIRR-05 | 79.1-02 | fs-only presence probe derives unpinned fetched-state from mirror-dir existence via SC-7; no prefix scan, no network | SATISFIED | `makePresenceProbe` rewritten arm + `readMirrorHeadSha`; git-source-probe.ts confirmed git-surface-free; real isomorphic-git fixture test passes. |
| MIRR-06 | 79.1-04 | Pre-existing per-sha clones coexist; lazy re-anchor; orphan sweep; no persisted migration state | SATISFIED | `makeReinstallCloneProbe` falls through to per-sha path when mirror cold (no re-anchor on reinstall, D-79.1-04); update/install re-anchor naturally; no new persisted-state file in the diff. |

**Note:** `.planning/workstreams/url-source/REQUIREMENTS.md` still shows MIRR-01..06 as unchecked `- [ ]` checkboxes and the traceability table as "Pending" — this is a documentation-currency gap (the checklist was not updated after phase completion), not a code gap. All six requirements have concrete, tested, wired implementations in the codebase as detailed above.

### Anti-Patterns Found

None. Grep for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` across all six phase-modified source files (`clone-key.ts`, `clone-cache.ts`, `git-source-probe.ts`, `install.ts`, `update.ts`, `reinstall.ts`) returns zero hits except a pre-existing, unrelated `SYNTHETIC_UPDATE_PLACEHOLDER_NAME` constant in update.ts (predates this phase, not a stub marker).

### Architecture Gate

| Gate | Command | Result | Status |
|------|---------|--------|--------|
| no-orchestrator-network | `node --test "tests/architecture/no-orchestrator-network.test.ts"` | 1/1 pass | GREEN — install.ts and reinstall.ts confirmed git-surface-free (0 grep matches); update.ts remains the sole sanctioned gitOps-exempt orchestrator (7 matches, expected/unchanged pattern). |

### Behavioral Spot-Checks / Test Execution

| Suite | Command | Result | Status |
|-------|---------|--------|--------|
| clone-key + clone-cache | `node --test tests/domain/clone-key.test.ts tests/orchestrators/plugin/clone-cache.test.ts` | 32 pass | PASS |
| mirror-head-read + git-source-probe | `node --test tests/orchestrators/plugin/mirror-head-read.test.ts tests/orchestrators/plugin/git-source-probe.test.ts` | 11 + 11 pass | PASS |
| install | `node --test tests/orchestrators/plugin/install.test.ts` | 92 pass | PASS |
| update | `node --test tests/orchestrators/plugin/update.test.ts` | 66 pass | PASS |
| reinstall | `node --test tests/orchestrators/plugin/reinstall.test.ts` | 74 pass | PASS |
| clone-gc | `node --test tests/orchestrators/plugin/clone-gc.test.ts` | 11 pass | PASS |
| list + info (regression, Pitfall 2 concern) | `node --test tests/orchestrators/plugin/list.test.ts tests/orchestrators/plugin/info.test.ts` | 119 pass | PASS |
| Full suite (single run) | `npm test` | 2761 pass / 0 fail / 1 pre-existing skip | PASS |
| typecheck | `npm run typecheck` | clean | PASS |
| lint | `npm run lint` | clean (no output) | PASS |

### Human Verification Required

None. All truths are verified via direct source inspection, real behavioral test fixtures (including the phase's one genuine unknown — the fs-only HEAD read against real isomorphic-git on-disk layouts, Pitfall 3/A1 — proven via a real clone fixture rather than left as an assumption), and passing automated gates.

### Gaps Summary

No gaps. All 6 roadmap success criteria and all 6 MIRR-01..06 requirements are satisfied with concrete, wired, tested code. The only non-code finding is that `.planning/workstreams/url-source/REQUIREMENTS.md`'s checkbox/traceability table has not been updated to reflect completion — flagged above as a documentation-currency note, not a blocker, since the actual codebase evidence is unambiguous.

---

_Verified: 2026-07-14T06:45:11Z_
_Verifier: Claude (gsd-verifier)_
