---
phase: 79.1-mutable-mirror-clones-for-unpinned-git-plugin-sources
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [MIRR-01, MIRR-02, MIRR-03]
files_modified:
  - extensions/pi-claude-marketplace/domain/clone-key.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/clone-cache.ts
  - tests/domain/clone-key.test.ts
  - tests/orchestrators/plugin/clone-cache.test.ts

must_haves:
  truths:
    - "An unpinned git-source (source.sha === undefined) materializes exactly ONE mirror clone at plugin-clones/<urlhash12>/ (bare 12-hex key, no -<sha12> suffix)"
    - "A warm mirror is refreshed in place on a mutating verb (fetch + force-update ref + checkout) via refreshGitHubClone, not short-circuited like the immutable per-sha cache"
    - "The mirror seam returns the checked-out HEAD sha so install/update record the resolved sha (PURL-09 / PI-7 preserved)"
    - "Pinned sources still key plugin-clones/<urlhash12>-<sha12>/ byte-identically to today"
  artifacts:
    - "domain/clone-key.ts::pluginMirrorKey(canonicalUrl) exported"
    - "orchestrators/plugin/clone-cache.ts::materializeOrRefreshPluginMirror(...) exported"
  key_links:
    - "materializeOrRefreshPluginMirror routes its path through locations.pluginCloneDir(pluginMirrorKey(cloneUrl)) (SC-7 chokepoint)"
    - "refresh path reuses refreshGitHubClone from marketplace/shared.ts verbatim; the git surface stays inside clone-cache.ts"
---

<objective>
Add the bare URL-keyed mirror key (`pluginMirrorKey`) and the mirror materialize+refresh seam (`materializeOrRefreshPluginMirror`) to the git-legal clone-cache tier. This is the substrate every other plan in the phase consumes: the orchestrator clone probes (Plan 03) call the seam by name, and the presence probe (Plan 02) derives the same bare key for fs-only reads.

Purpose: Give unpinned git-source plugins exactly ONE mutable mirror clone per canonical URL, refreshed in place at marketplace-clone parity, so multi-clone ambiguity is impossible by construction (MIRR-01/02/03).
Output: `pluginMirrorKey` (pure), `materializeOrRefreshPluginMirror` (git-legal seam), plus unit coverage for the bare-key shape and the mirror materialize/refresh/idempotency behavior.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/url-source/phases/79.1-mutable-mirror-clones-for-unpinned-git-plugin-sources/79.1-CONTEXT.md
@.planning/workstreams/url-source/phases/79.1-mutable-mirror-clones-for-unpinned-git-plugin-sources/79.1-RESEARCH.md
@.planning/workstreams/url-source/phases/79.1-mutable-mirror-clones-for-unpinned-git-plugin-sources/79.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pluginMirrorKey (bare 12-hex URL key) to domain/clone-key.ts</name>
  <files>extensions/pi-claude-marketplace/domain/clone-key.ts, tests/domain/clone-key.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/domain/clone-key.ts (existing pluginCloneKey at lines 31-34, KEY_TRUNC=12, createHash import at line 14 — the analog to copy)
    - tests/domain/clone-key.test.ts (existing test shape, 1.8K)
    - .claude/rules/typescript-comments.md (comment policy — D-79.1-01 and MIRR-01 are allowed traceability anchors; no "Phase 79.1"/"Plan"/"Wave" tokens)
  </read_first>
  <behavior>
    - pluginMirrorKey("https://example.com/repo") returns a bare 12-hex string with NO "-" separator (matches /^[0-9a-f]{12}$/)
    - pluginMirrorKey(url) equals the LEFT half of pluginCloneKey(url, anySha) — i.e. pluginCloneKey(url, sha).startsWith(pluginMirrorKey(url) + "-") holds for any sha
    - Two different URLs produce different keys; the same URL always produces the same key (deterministic)
  </behavior>
  <action>
    Add an exported function `pluginMirrorKey(canonicalUrl: string): string` beside `pluginCloneKey` in domain/clone-key.ts. It returns exactly the existing `left` expression: `createHash("sha256").update(canonicalUrl).digest("hex").slice(0, KEY_TRUNC)` — the bare 12-hex left half with no `-<sha>` suffix. Reuse the existing `KEY_TRUNC` constant (value 12) and the existing `createHash` import at line 14 — do NOT re-import or redefine. Do NOT derive the key by string-splitting `pluginCloneKey`'s two-part return (the `<hash>-<sha>` shape confusion the discretion note warns against). Document it as the URL-only mirror key per MIRR-01. Keep the function pure — no network, no gitOps, no fs.

    In tests/domain/clone-key.test.ts add cases asserting: (a) the bare 12-hex shape via a `/^[0-9a-f]{12}$/` match; (b) equivalence with the left half — `pluginCloneKey(url, sha).startsWith(pluginMirrorKey(url) + "-")` is true; (c) determinism and URL-sensitivity.
  </action>
  <verify>
    <automated>node --test "tests/domain/clone-key.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function pluginMirrorKey" extensions/pi-claude-marketplace/domain/clone-key.ts` returns a match
    - The test file asserts `pluginCloneKey(url, sha).startsWith(pluginMirrorKey(url) + "-")` for at least one url/sha pair
    - `node --test "tests/domain/clone-key.test.ts"` passes with the new cases
    - No second `createHash` import and no new `KEY_TRUNC` declaration were added (`grep -c "const KEY_TRUNC" extensions/pi-claude-marketplace/domain/clone-key.ts` returns 1)
  </acceptance_criteria>
  <done>pluginMirrorKey is exported, returns the bare 12-hex left half, provably equals pluginCloneKey's left half, and is covered by passing unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add materializeOrRefreshPluginMirror seam to clone-cache.ts</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/clone-cache.ts, tests/orchestrators/plugin/clone-cache.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/clone-cache.ts (materializePluginClone lines 57-113 — the staging→atomic-rename create path to copy; note the `gitOps.checkout({ dir, ref: pin })` at line 85 which the mirror MUST NOT do; imports at lines 22-33)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts (refreshGitHubClone signature lines 191-197: `refreshGitHubClone(cloneDir, storedRef, gitOps, onFetchSucceeded?, auth?)`; the D-14 three-form sequence lines 176-259; GitOps.resolveRef lines 122; DEFAULT_GIT_OPS line 146; GitAuthBundle type)
    - extensions/pi-claude-marketplace/domain/clone-key.ts (pluginMirrorKey — added in Task 1)
    - tests/orchestrators/plugin/clone-cache.test.ts (freshLocations helper lines 24-29; makeMockGitOps usage; existing materialize test shape)
    - tests/helpers/git-mock.ts (makeMockGitOps: clone/fetch/forceUpdateRef/checkout/resolveRef call logs; resolveRef("HEAD") returns state.head; a warm second call hits pathExists short-circuit)
  </read_first>
  <behavior>
    - Mirror ABSENT: materializeOrRefreshPluginMirror clones into a staging dir (with the ref hint + singleBranch when source.ref is set), then atomic-renames staging → plugin-clones/<urlhash12>/. It does NOT checkout a fixed pin (the working tree tracks HEAD/ref). It returns { pluginRoot, resolvedSha } where resolvedSha comes from gitOps.resolveRef({ dir: mirrorRoot, ref: "HEAD" })
    - Mirror PRESENT (warm): it calls refreshGitHubClone(mirrorRoot, ref, gitOps, onFetchSucceeded, auth) in place — fetch + force-update ref + checkout — then reads HEAD via resolveRef. It does NOT short-circuit like the immutable per-sha cache (MIRR-02, refresh-on-warm)
    - The returned pluginRoot is exactly locations.pluginCloneDir(pluginMirrorKey(cloneUrl)) — the bare-key mirror root
    - A concurrent create that loses the rename race (EEXIST/ENOTEMPTY) cleans its staging and treats the winner's dir as the warm mirror, then refreshes+reads HEAD from it (no throw)
    - ref === undefined threads through as default-branch HEAD tracking (refreshGitHubClone's first form); ref set threads as the branch/tag
  </behavior>
  <action>
    Add an exported async function `materializeOrRefreshPluginMirror(args: { locations: ScopedLocations; cloneUrl: string; ref?: string; gitOps?: GitOps; auth?: GitAuthBundle }): Promise<{ pluginRoot: string; resolvedSha: string }>` to clone-cache.ts. Default `gitOps = args.gitOps ?? DEFAULT_GIT_OPS` exactly as materializePluginClone line 65. Import `pluginMirrorKey` from ../../domain/clone-key.ts (extend the existing line-26 import) and `refreshGitHubClone` from ../marketplace/shared.ts (extend the existing line-30 import that already pulls DEFAULT_GIT_OPS/GitAuthBundle/GitOps).

    Flow: compute `mirrorRoot = await args.locations.pluginCloneDir(pluginMirrorKey(args.cloneUrl))`. If `await pathExists(mirrorRoot)` is FALSE → run the create path copied from materializePluginClone lines 74-113 EXCEPT omit the `gitOps.checkout({ dir, ref: pin })` line (the mirror tracks a moving ref, not a fixed pin) and key the target with the bare mirror key. Preserve the staging clone (ref-hint singleBranch when args.ref is set, auth threaded), the append-leak cleanup on clone failure, the mkdir(dirname) + rename, and the EEXIST/ENOTEMPTY-as-concurrent-win branch (clean staging, fall through to the present/refresh path rather than returning early — the winner's tree still needs a HEAD read).

    After the dir exists (freshly materialized OR already present), refresh it in place: declare `let cloneAdvanced = false;` and call `await refreshGitHubClone(mirrorRoot, args.ref, gitOps, () => { cloneAdvanced = true; }, args.auth)`. Then read the pin: `const resolvedSha = await gitOps.resolveRef({ dir: mirrorRoot, ref: "HEAD" })`. Return `{ pluginRoot: mirrorRoot, resolvedSha }`. NOTE: a just-materialized clone is also refreshed — that is intended parity with marketplace update (MIRR-02, refresh-on-warm resolution of OQ2). The EEXIST/ENOTEMPTY concurrent-win handling is the same accepted multi-process exposure marketplace clones ship with today, with fail-clean single-process behavior via the append-leak cleanup (D-79.1-03). Do NOT add a persisted migration stamp or refcount (derive-not-persist).

    Keep ALL git surface (refreshGitHubClone, gitOps, resolveRef, DEFAULT_GIT_OPS) inside clone-cache.ts only — never surfaced to install/reinstall/list/info. Cite D-79.1-01 / D-79.1-02 / MIRR-01 / MIRR-02 / MIRR-03 in the docstring as the traceability anchors (no phase/plan/wave tokens per the comment policy).

    In tests/orchestrators/plugin/clone-cache.test.ts add cases using makeMockGitOps + freshLocations: (a) mirror absent → clone into staging then rename to plugin-clones/<bare-key>/ (assert cloneCalls fired once, no checkout of a fixed 40-hex pin, the returned pluginRoot ends with the bare pluginMirrorKey and has NO "-" in its last segment); (b) mirror present → refreshGitHubClone fires (assert fetchCalls non-empty) and resolveRef("HEAD") supplies resolvedSha; (c) idempotency — two successive calls both succeed and the second refreshes rather than throwing; (d) resolvedSha equals the mock's configured HEAD sha.
  </action>
  <verify>
    <automated>node --test "tests/orchestrators/plugin/clone-cache.test.ts" "tests/architecture/no-orchestrator-network.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function materializeOrRefreshPluginMirror" extensions/pi-claude-marketplace/orchestrators/plugin/clone-cache.ts` returns a match
    - The mirror create path contains NO `checkout` call on the mirror staging dir (the mirror tracks HEAD; assert via test that no fixed-pin checkout is recorded on the mirror path)
    - A test asserts the returned pluginRoot's last path segment matches `/^[0-9a-f]{12}$/` (bare mirror key, no sha suffix)
    - A test asserts refreshGitHubClone runs on the warm-mirror path (fetchCalls non-empty on the second call)
    - `node --test "tests/orchestrators/plugin/clone-cache.test.ts"` passes
    - `node --test "tests/architecture/no-orchestrator-network.test.ts"` stays green (clone-cache.ts is NOT in FORBIDDEN_TARGETS; the git surface is legal here)
  </acceptance_criteria>
  <done>materializeOrRefreshPluginMirror materializes-on-cold and refreshes-on-warm, returns the bare-key mirror root plus the HEAD sha, keeps all git surface inside clone-cache.ts, and is covered by passing unit tests without regressing the network gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin manifest source URL → clone path | An untrusted canonical URL is hashed into the on-disk clone key |
| remote git host → local working tree | Fetched refs and tree contents cross into local fs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79.1-01 | Tampering | mirror clone path derivation (pluginMirrorKey → pluginCloneDir) | high | mitigate | Route every mirror path through locations.pluginCloneDir (SC-7 chokepoint: assertSafeName + assertPathInside). The bare 12-hex key is a fixed hash — no user string enters the path segment; NFR-10 containment holds verbatim |
| T-79.1-02 | Tampering | partial write during first materialization | medium | mitigate | Staging clone → same-FS atomic rename (NFR-1); sources-staging/ and plugin-clones/ are siblings under extensionRoot; EEXIST/ENOTEMPTY treated as concurrent-win, not corruption |
| T-79.1-03 | Information disclosure | auth bundle in clone/refresh | medium | mitigate | Reuse buildAuthForHost/GitAuthBundle threading verbatim (no mirror-specific auth path); auth stays in-memory, never written to the mirror tree or state |
</threat_model>

<verification>
- `node --test "tests/domain/clone-key.test.ts"` passes (pluginMirrorKey bare-key + left-half equivalence)
- `node --test "tests/orchestrators/plugin/clone-cache.test.ts"` passes (materialize-on-cold, refresh-on-warm, idempotency, resolvedSha=HEAD)
- `node --test "tests/architecture/no-orchestrator-network.test.ts"` green (git surface confined to clone-cache.ts)
- `npm run check` green before the plan is considered done (typecheck + lint + format + tests)
</verification>

<success_criteria>
- pluginMirrorKey returns a bare 12-hex key equal to pluginCloneKey's left half
- materializeOrRefreshPluginMirror clones-then-renames on a cold mirror and refreshGitHubClone-refreshes on a warm mirror, always returning the mirror root + HEAD sha
- Pinned per-sha materialization (materializePluginClone) is unchanged
- The no-orchestrator-network gate stays green
</success_criteria>

<output>
Create `.planning/workstreams/url-source/phases/79.1-mutable-mirror-clones-for-unpinned-git-plugin-sources/79.1-01-SUMMARY.md` when done
</output>
