---
phase: 80-remote-status-glyph-reassignment-warm-cache-resolution
plan: 02
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/plugin-state-classifier.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts
  - extensions/pi-claude-marketplace/shared/completion-cache.ts
  - extensions/pi-claude-marketplace/edge/completions/data.ts
  - tests/orchestrators/plugin/git-source-probe.test.ts
autonomous: true
requirements: [RSTA-01, RSTA-03, RSTA-05, RSTA-06]
must_haves:
  truths:
    - "A not-installed git-source plugin (url / git-subdir / github) with NO materialized clone classifies `remote` (not `available`)"
    - "A not-installed git-source plugin with a WARM clone/mirror classifies via the three-way resolver: `available` / `partially-available` / `unavailable`"
    - "The plugin-index completion cache schemaVersion is 6; pre-fix caches drop+rebuild"
    - "Install completion still offers `remote` plugins (install performs the fetch)"
  artifacts:
    - "orchestrators/plugin/plugin-state-classifier.ts (ManifestEntryClassification += `remote`)"
    - "orchestrators/plugin/git-source-probe.ts (probeManifestEntry presence-derived rewrite; `_locations` becomes used)"
    - "shared/completion-cache.ts (`remote` in schema status union + PluginIndexRow.status; schemaVersion 5→6 in all three sites)"
    - "edge/completions/data.ts (INSTALL_STATUSES += `remote`)"
  key_links:
    - "probeManifestEntry consumes makePresenceProbe (79.1) + resolveStrict; not-cached → remote, materialized → classifyManifestEntry"
    - "PluginIndexRow.status must include `remote` before edge-deps (Plan 03) and completion offer set can carry it without a type error"
    - "The composition imports ONLY makePresenceProbe + resolveStrict — never platform/git (NFR-5 no-orchestrator-network gate)"
---

<objective>
Rewrite the shared git-source classification substrate so a not-installed git-source plugin derives `remote` (cold clone) vs the three-way resolver verdict (warm clone) via the Phase 79.1 presence probe. Add the `remote` member to `ManifestEntryClassification`, add `remote` to the completion-cache status union + `PluginIndexRow`, bump the cache schemaVersion 5→6 (all three literal sites), and grow the install-completion offer set to include `remote`.

Purpose: This is the canonical classification decision every read surface reads (RSTA-01/03/05/06). Consolidating `remote` derivation into the shared `probeManifestEntry` makes list/completion parity structural, not drift-guard-enforced. The completion-cache type + schema change is the substrate Plan 03's `edge-deps.ts` parity verification and the install-completion offer both depend on.

Output: `probeManifestEntry` presence-derived; `ManifestEntryClassification` + `PluginIndexRow.status` carry `remote`; cache schemaVersion at 6; `INSTALL_STATUSES` offers `remote`; the three cold-cache `(available)` unit tests inverted to `remote` plus warm-tree three-way cases added.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/url-source/phases/80-remote-status-glyph-reassignment-warm-cache-resolution/80-CONTEXT.md
@.planning/workstreams/url-source/phases/80-remote-status-glyph-reassignment-warm-cache-resolution/80-RESEARCH.md
@.planning/workstreams/url-source/phases/80-remote-status-glyph-reassignment-warm-cache-resolution/80-PATTERNS.md
@.planning/workstreams/url-source/phases/79.1-mutable-mirror-clones-for-unpinned-git-plugin-sources/79.1-02-SUMMARY.md
@.claude/rules/typescript-comments.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates/changes (Plan 03 consumes the classifier + cache types):

| Symbol | File | Kind | Change |
|--------|------|------|--------|
| `ManifestEntryClassification` | orchestrators/plugin/plugin-state-classifier.ts | type union | `+ "remote"` → `"available" \| "partially-available" \| "unavailable" \| "remote"` |
| `probeManifestEntry` | orchestrators/plugin/git-source-probe.ts | function | body rewritten: presence-probe-first; `_locations` param renamed `locations` (now used) |
| `PluginIndexRow.status` | shared/completion-cache.ts | type union | `+ "remote"` |
| schema status `Type.Union` | shared/completion-cache.ts | typebox | `+ Type.Literal("remote")` |
| `schemaVersion` | shared/completion-cache.ts | literal | `5` → `6` at all three sites (~86, ~338, ~353) |
| `INSTALL_STATUSES` | edge/completions/data.ts | Set | `new Set(["available", "remote"])` |

Depends on Plan 01 artifacts: the `remote` PLUGIN_STATUSES member (so `PluginIndexRow.status` `"remote"` is a valid PluginStatus if referenced).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite probeManifestEntry to presence-derived remote/warm classification + widen ManifestEntryClassification</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/plugin-state-classifier.ts, extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts, tests/orchestrators/plugin/git-source-probe.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts (probeManifestEntry lines 159-178 — the short-circuit to replace; makePresenceProbe 110-141; probeUpgradeCandidate 191-205 — the composition template to mirror)
    - extensions/pi-claude-marketplace/orchestrators/plugin/plugin-state-classifier.ts (ManifestEntryClassification line 53; classifyManifestEntry 167-178 — the assertNever switch that MUST stay 3-way, NFR-7)
    - extensions/pi-claude-marketplace/domain/resolver.ts (ResolveContext ~267-278; GitPluginRootResult; the not-cached → unavailable{not installed} mapping ~666-681)
    - tests/orchestrators/plugin/git-source-probe.test.ts (the three cold-cache `(available)` assertions ~51-84 to invert; the warm-mirror staging recipe ~110-144)
    - .planning/workstreams/url-source/phases/79.1-mutable-mirror-clones-for-unpinned-git-plugin-sources/79.1-02-SUMMARY.md (makePresenceProbe mirror arm + readMirrorHeadSha contract)
    - .claude/rules/typescript-comments.md
  </read_first>
  <behavior>
    - probeManifestEntry(entry, marketplaceRoot, locations) for a git source (url/git-subdir/github) with a COLD clone/mirror → returns `"remote"`
    - Same, with a WARM clone/mirror → returns `classifyManifestEntry(resolveStrict(...))` = `"available"` (installable), `"partially-available"`, or `"unavailable"` per the real on-disk three-way resolution
    - Non-git source (path/npm/unknown) → unchanged: `classifyManifestEntry(resolveStrict(entry, { marketplaceRoot }))`, fold to `"unavailable"` on throw
    - The three previously-passing cold-cache `(available)` unit tests now assert `"remote"` (intentional inversion — NOT a regression)
    - New warm-mirror test: stage a mirror dir (git.init + commit), assert probeManifestEntry classifies `available` (or the degraded arm for a partial fixture)
  </behavior>
  <action>
Widen `ManifestEntryClassification` (plugin-state-classifier.ts line 53) to `"available" | "partially-available" | "unavailable" | "remote"`, with a comment citing `RSTA-01 / D-80-06`: `remote` is the not-installed git-source-with-no-materialized-clone bucket, derived at the classification layer (NOT a resolver arm — NFR-7). Leave `classifyManifestEntry`'s `switch` + `assertNever` (167-178) UNCHANGED — it maps the resolver's three-way `ResolvedPlugin.state` and `remote` derives OUTSIDE that switch.

Fetched-state derivation (D-80-02 / RSTA-06): unpinned sources derive fetched-state from mirror-dir presence via `makePresenceProbe`'s mirror arm; pinned sources use the exact per-sha key. Manifest pin wins for pinned entries (exact key or `remote`), even if stale clones of the same URL exist — this is `makePresenceProbe`'s behavior (shipped 79.1), so `probeManifestEntry` inherits it by consuming the probe, no extra logic needed. Multi-clone ambiguity is structurally impossible (no prefix scan).

Rewrite `probeManifestEntry` (git-source-probe.ts 159-178): rename the `_locations` param to `locations` (it becomes used). Parse the source. For a git source (`parsedSource.kind === "url" | "git-subdir" | "github"`) use the probe-first pattern (RESEARCH Pattern 1a): call `makePresenceProbe(locations)(parsedSource)` directly; on `{ kind: "not-cached" }` return `"remote"`; on `{ kind: "materialized" }` build a `ResolveContext` with `resolveGitPluginRoot: makePresenceProbe(locations)` and return `classifyManifestEntry(await resolveStrict(entry, ctx))`, folding a `resolveStrict` throw to `"unavailable"`. Non-git sources keep the existing `classifyManifestEntry(await resolveStrict(entry, { marketplaceRoot }))` path with the catch-to-`unavailable`. Update the function docstring: replace the "classifies `available` BEFORE any resolveStrict" over-claim narrative with the presence-derived `remote` vs warm-tree three-way description; cite `RSTA-01 / RSTA-05 / RSTA-06 / NFR-5`. Import ONLY `makePresenceProbe` (already in-file) + `resolveStrict` + `ResolveContext` — never `platform/git` (NFR-5 gate greps this file).

Note: the `parsedSource` narrowing — `makePresenceProbe`'s callable is typed for `UrlSource | GitSubdirSource | GitHubSource`, so the git-source branch already narrows `parsedSource` to that union; pass it directly.

git-source-probe.test.ts — INVERT the three cold-cache tests at ~51-84 (url / git-subdir / github, no materialized clone) from asserting `"available"` to asserting `"remote"`; reword the test titles to describe the not-fetched → remote behavior (no phase/plan refs). ADD warm-tree tests using the staging recipe at ~110-144: stage a mirror (unpinned) and a per-sha clone (pinned) dir, then assert `probeManifestEntry` returns `"available"` for a clean staged plugin (and, if a partial/unavailable fixture is convenient, the corresponding three-way arm). These new tests cover RSTA-05/06.

Comment discipline: `RSTA-NN` / `D-80-NN` / `NFR-N` anchors only.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/git-source-probe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ManifestEntryClassification` includes `"remote"`: `rg -a "ManifestEntryClassification =" extensions/pi-claude-marketplace/orchestrators/plugin/plugin-state-classifier.ts` output contains `"remote"`
    - `classifyManifestEntry` still has exactly three `case` arms + `assertNever` (unchanged): `rg -a -c "case \"(installable|partially-available|unavailable)\"" extensions/pi-claude-marketplace/orchestrators/plugin/plugin-state-classifier.ts` equals 3
    - `probeManifestEntry` no longer contains the unconditional `return "available"` git short-circuit: `rg -a -A3 "kind === \"url\"" extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts` does not show a bare `return "available"`
    - git-source-probe.ts imports no git surface: `rg -av '^\s*//|^\s*\*' extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts | rg -c 'platform/git|gitOps|isomorphic-git'` equals 0
    - The three cold-cache tests assert `"remote"` (behavior asserted by the inverted unit tests); warm-tree tests assert `available` — `node --test tests/orchestrators/plugin/git-source-probe.test.ts` exits 0
  </acceptance_criteria>
  <done>probeManifestEntry derives `remote` on a cold cache and the three-way resolver verdict on a warm cache; the classifier union carries `remote`; the resolver switch stays 3-way; the git-source-probe suite (inverted + new warm cases) is green.</done>
</task>

<task type="auto">
  <name>Task 2: Bump completion-cache schemaVersion 5→6, add `remote` to the cache status union, offer `remote` in install completion</name>
  <files>extensions/pi-claude-marketplace/shared/completion-cache.ts, extensions/pi-claude-marketplace/edge/completions/data.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/completion-cache.ts (PLUGIN_INDEX_CACHE_SCHEMA schemaVersion Type.Literal(5) line 86; status Type.Union 92-101; PluginIndexRow.status 113-125; poison writer `5 as const` line 338; main writer `5 as const` line 353)
    - extensions/pi-claude-marketplace/edge/completions/data.ts (INSTALL_STATUSES line 63; PARTIAL_INSTALL_STATUSES 70-73)
    - .claude/rules/typescript-comments.md
  </read_first>
  <action>
completion-cache.ts — add `Type.Literal("remote")` to the schema status `Type.Union` (append after `Type.Literal("unavailable")`, ~100) and add `| "remote"` to the `PluginIndexRow.status` union (~124). Bump the schemaVersion literal `5` → `6` at ALL THREE sites: the schema `Type.Literal(5)` (line 86), the poison-writer `5 as const` (line 338), and the main-writer `5 as const` (line 353) — bumping fewer than all three makes the just-written cache fail its own validator on next read. Update the schemaVersion comment (~80-84) to cite `RSTA-03`: the 5→6 bump drops caches carrying the old not-installed git-source `(available)` classification so pre-fix caches drop+rebuild via the existing drop-on-mismatch path (no manual migration — the cache is an ephemeral optimization).

edge/completions/data.ts — grow `INSTALL_STATUSES` from `new Set(["available"])` to `new Set(["available", "remote"])` (line 63) with a comment citing `RSTA-01 / D-80-05`: install still offers `(remote)` plugins because install performs the fetch, so a remote plugin is a valid install target. Leave `PARTIAL_INSTALL_STATUSES` (70-73) unchanged unless a type error forces otherwise (it should not — `remote` is not a partial-install candidate).

Comment discipline: `RSTA-NN` / `D-80-NN` anchors only.
  </action>
  <verify>
    <automated>npm run typecheck && node --test tests/orchestrators/edge-deps.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - schemaVersion is `6` at all three sites and `5` at none: `rg -a -c "schemaVersion.*6|6 as const" extensions/pi-claude-marketplace/shared/completion-cache.ts` is at least 3 AND `rg -a -c "Type.Literal\(5\)|5 as const" extensions/pi-claude-marketplace/shared/completion-cache.ts` equals 0
    - The cache status schema and `PluginIndexRow.status` both include `remote`: `rg -a 'Type.Literal\("remote"\)' extensions/pi-claude-marketplace/shared/completion-cache.ts` matches once AND `rg -a '"remote"' extensions/pi-claude-marketplace/shared/completion-cache.ts` shows the PluginIndexRow union member
    - `INSTALL_STATUSES` includes `remote`: `rg -a 'INSTALL_STATUSES.*new Set\(\["available", "remote"\]\)' extensions/pi-claude-marketplace/edge/completions/data.ts` matches
    - `npm run typecheck` exits 0 (the `PluginIndexRow.status` widen resolves the `edge-deps.ts` `classifyNotInstalledPluginRow` return-type compatibility with `remote`)
    - `node --test tests/orchestrators/edge-deps.test.ts` exits 0
  </acceptance_criteria>
  <done>The cache schema and row type carry `remote`; schemaVersion is 6 everywhere; install completion offers `remote`; typecheck and the edge-deps suite are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest entry → classification | untrusted manifest source string parsed by parsePluginSource; presence derived fs-only from plugin-clones/ contents |
| on-disk completion cache → read surface | a stale cache carrying the old `(available)` classification would serve wrong rows until invalidated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-03 | Info disclosure / policy | git-source-probe.ts composition | high | mitigate | Import only makePresenceProbe + resolveStrict; never platform/git — no-orchestrator-network.test.ts greps this file for gitOps/platform/git and fails on a hit (NFR-5) |
| T-80-04 | Tampering (indirect) | stale plugin-index cache | medium | mitigate | schemaVersion 5→6 drop+rebuild on mismatch + the existing 10-min TTL invalidate pre-fix caches |
| T-80-05 | Tampering | typebox cache validator | low | accept | The `remote` literal joins the existing Compile(PLUGIN_INDEX_CACHE_SCHEMA) gate; V5 input validation preserved by the schema growth |
| T-80-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs (in-repo TS edits only) per RESEARCH.md |
</threat_model>

<verification>
- `node --test tests/orchestrators/plugin/git-source-probe.test.ts` — green (inverted + warm-tree cases)
- `npm run typecheck` — green (classifier + cache union widen compiles)
- `node --test tests/orchestrators/edge-deps.test.ts` — green (parity substrate; full parity assertion completed in Plan 03)
- `no-orchestrator-network.test.ts` — green (git-source-probe.ts stays git-free)
- `npm run check` — green before Wave 3
</verification>

<success_criteria>
- `probeManifestEntry` returns `remote` (cold) / three-way (warm) for git sources; unchanged for non-git
- `ManifestEntryClassification` and `PluginIndexRow.status` carry `remote`; classifier switch stays 3-way
- schemaVersion is 6 at all three sites; install completion offers `remote`
- git-source-probe.ts imports no git surface; typecheck + touched suites green
</success_criteria>

<output>
Create `.planning/workstreams/url-source/phases/80-remote-status-glyph-reassignment-warm-cache-resolution/80-02-SUMMARY.md` when done
</output>
