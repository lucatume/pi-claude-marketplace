---
phase: 80-remote-status-glyph-reassignment-warm-cache-resolution
plan: 03
type: execute
wave: 3
depends_on: ["80-01", "80-02"]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts
  - extensions/pi-claude-marketplace/orchestrators/edge-deps.ts
  - tests/orchestrators/plugin/list.test.ts
  - tests/orchestrators/edge-deps.test.ts
autonomous: true
requirements: [RSTA-01, RSTA-03, RSTA-07]
must_haves:
  truths:
    - "`list` renders `◌ <name> (remote)` (bare) for a not-installed git-source plugin with no materialized clone; a warm one renders its three-way verdict"
    - "`list --remote` selects exactly the `remote` bucket; `--available` no longer includes unfetched git-source plugins; `--available --remote` restores the pre-milestone set"
    - "The completion bucketizer emits `remote` at parity with list (output-parity drift-guard green)"
    - "installed git plugins never render `(remote)` (D-78-04 degrade preserved)"
  artifacts:
    - "orchestrators/plugin/list.ts (availableRowMessage consolidated onto the shared presence-derived classification; FilterBucket += remote; shouldShow/filtersPassive/scopeOf remote arms; ListPluginsOptions.remote)"
    - "edge/handlers/plugin/list.ts (--remote in BOOLEAN_FLAGS + spread; USAGE string)"
    - "orchestrators/edge-deps.ts (verify-only — inherits remote via probeManifestEntry)"
  key_links:
    - "availableRowMessage must receive locations (locationsFor(pluginScope, opts.cwd)) to run the presence probe; the block builder enumerateMarketplacePlugins already holds pluginScope + opts.cwd"
    - "PluginRemoteMessage (Plan 01) is the emitted row on not-cached; ManifestEntryClassification remote (Plan 02) is the bucket"
    - "list.ts imports only makePresenceProbe + resolveStrict from git-source-probe — never platform/git (NFR-5)"
---

<objective>
Consolidate the `list.ts` inline git-source short-circuit onto the shared presence-derived classification so `remote` derives once (parity-by-construction), emit `PluginRemoteMessage` on a cold clone, add the `remote` member to `FilterBucket` + the `--remote` filter, wire the `--remote` flag at the edge, and verify the completion bucketizer inherits `remote` at parity with list.

Purpose: RSTA-07 adds `list --remote` to the PL-1 filter union (intended behavior change: `--available` stops including unfetched git-source plugins). RSTA-03 requires list/completion parity to be structural — consolidating the second inline short-circuit removes the drift risk the output-parity guard exists to catch. This plan runs parallel with Plan 04 (info surface) — disjoint file sets.

Output: `list.ts` renders `(remote)` for cold git sources and the three-way verdict for warm ones; `list --remote` filters the bucket; edge-deps parity verified.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/url-source/phases/80-remote-status-glyph-reassignment-warm-cache-resolution/80-CONTEXT.md
@.planning/workstreams/url-source/phases/80-remote-status-glyph-reassignment-warm-cache-resolution/80-RESEARCH.md
@.planning/workstreams/url-source/phases/80-remote-status-glyph-reassignment-warm-cache-resolution/80-PATTERNS.md
@.claude/rules/typescript-comments.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates/changes:

| Symbol | File | Kind | Change |
|--------|------|------|--------|
| `FilterBucket` | orchestrators/plugin/list.ts | type union | `+ "remote"` |
| `ListPluginsOptions.remote` | orchestrators/plugin/list.ts | interface field | `remote?: boolean` |
| `availableRowMessage` return union | orchestrators/plugin/list.ts | type | `+ PluginRemoteMessage` |
| `availableRowMessage` signature | orchestrators/plugin/list.ts | function | gains a `locations: ScopedLocations` param |
| `filtersPassive` / `shouldShow` / `scopeOf` | orchestrators/plugin/list.ts | functions | `remote` arms added |
| `--remote` | edge/handlers/plugin/list.ts | flag | added to BOOLEAN_FLAGS + spread + USAGE |

Consumes Plan 01 (`PluginRemoteMessage`) + Plan 02 (`ManifestEntryClassification` remote, `probeManifestEntry`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Consolidate availableRowMessage onto presence-derived classification + add the remote FilterBucket/scopeOf arms</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/list.ts, tests/orchestrators/plugin/list.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts (FilterBucket line 135; ListPluginsOptions 146-169; filtersPassive 175-182; shouldShow 191-234; availableRowMessage 513-620 — the inline git short-circuit at 534-552; enumerateMarketplacePlugins 679-739 — the caller at 729-732 that holds pluginScope + opts.cwd; the probeUpgradeCandidate injection precedent at 383-399 using locationsFor(pluginScope, cwd) + makePresenceProbe; scopeOf switch 1054-1096; PluginRenderStatus 107-119)
    - extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts (makePresenceProbe, classifyManifestEntry consumption; the probe callable's typed source union)
    - extensions/pi-claude-marketplace/shared/notify.ts (PluginRemoteMessage from Plan 01)
    - tests/orchestrators/plugin/list.test.ts (existing available/partially-available/unavailable list-surface cases as fixture templates; the __test_availableRowMessage export usage)
    - .claude/rules/typescript-comments.md
  </read_first>
  <behavior>
    - availableRowMessage for a git source with a COLD clone → `{ message: PluginRemoteMessage (status "remote", bare), bucket: "remote" }`
    - Same with a WARM clone → the existing three-way switch on resolved.state (available / partially-available / unavailable) via the presence-injected resolveStrict
    - Non-git source → unchanged three-way path
    - A `(remote)` list row renders `◌ <name> (remote)` with no scope bracket and no reason brace (byte-asserted)
    - `--remote` selects only remote rows; `--available` alone excludes cold git sources; `--available --remote` includes both
  </behavior>
  <action>
Replace the inline git short-circuit in `availableRowMessage` (list.ts 534-552) with the shared presence-derived classification. Change the function signature to accept a `locations: ScopedLocations` param (thread it from the caller). Widen the return `message` union type (517) to add `PluginRemoteMessage` and widen the `bucket` to the extended `FilterBucket`. In the body: parse the source; for a git source, call `makePresenceProbe(locations)(parsedSource)` — on `{ kind: "not-cached" }` return `{ message: { status: "remote", name: manifestEntry.name, ...(version) , ...descriptionField }, bucket: "remote" }`; on `{ kind: "materialized" }` fall through to a `resolveStrict(manifestEntry, ctx)` (ctx with `resolveGitPluginRoot: makePresenceProbe(locations)`) feeding the EXISTING `switch (resolved.state)` three-way block at 577-620 (available/partially-available/unavailable). Non-git sources keep the existing `resolveStrict(manifestEntry, { marketplaceRoot })` + switch. Keep the `catch (probeErr)` narrowing arm. Update the docstring to cite `RSTA-01 / RSTA-05 / RSTA-06 / NFR-5` and drop the "renders (available)" over-claim.

Thread `locations` at the caller: in `enumerateMarketplacePlugins`, the `availableRowMessage(manifestEntry, mpRecord.marketplaceRoot)` call at 729-732 becomes `availableRowMessage(manifestEntry, mpRecord.marketplaceRoot, locationsFor(pluginScope, opts.cwd))` — `locationsFor` is already imported (line 59) and `pluginScope` + `opts.cwd` are in scope (same pattern as the probeUpgradeCandidate injection at 394).

FilterBucket (line 135) — add `"remote"`: `"installed-inventory" | "available" | "partially-available" | "unavailable" | "remote"`.

ListPluginsOptions (146-169) — add `readonly remote?: boolean;` with a doc comment citing `RSTA-07 / D-80-07 / PL-1`: union filter for the `(remote)` bucket that joins the PL-1 filter family (`--installed` / `--available` / `--unavailable` / `--partial`).

filtersPassive (175-182) — add `opts.remote !== true` to the conjunction so an active `--remote` makes filters non-passive.

shouldShow (191-234) — add an arm modeled on the `available` arm (215): `if (opts.remote === true && bucket === "remote") return true;` with a comment citing `RSTA-07`. The `--available` arm stays keyed on `status === "available"` — a cold git source now carries `bucket "remote"` and render status `"remote"`, so it no longer passes `--available` (the INTENDED behavior change). Note: `PluginRenderStatus` (107-119) must also gain `| "remote"` since `shouldShow`'s `status` param is `PluginRenderStatus` and the remote row's status is `"remote"`.

scopeOf switch (1054-1096) — add `case "remote":` alongside the `available | unavailable | partially-available` arm (1067-1072) returning `marketplaceScope` (SNM-11: no scope field). This keeps the `assertNever(p)` exhaustive.

list.test.ts — add cases: (a) cold git source → `(remote)` bare row byte-equal `◌ <name> (remote)`; (b) `--remote` selects the remote bucket only; (c) `--available` alone EXCLUDES the cold git source (regression-guard for the intended change); (d) `--available --remote` includes both available and remote; (e) a warm git source classifies its three-way verdict (stage a mirror via the git-source-probe warm recipe). Reuse the existing list-surface fixture shapes.

Comment discipline: `RSTA-NN` / `D-80-NN` / `SNM-NN` / `NFR-N` anchors only.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/list.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `FilterBucket` includes `"remote"`: `rg -a "type FilterBucket" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` output contains `"remote"`
    - `availableRowMessage` no longer contains an unconditional `status: "available"` return inside the git-source branch: `rg -a -A6 "kind === \"url\"" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` does not show a bare `status: "available"` short-circuit block
    - A cold git source renders `◌ <name> (remote)` bare (behavior asserted by list.test.ts byte-equality case)
    - `--available` alone excludes cold git sources; `--available --remote` includes them (asserted by list.test.ts filter cases)
    - list.ts imports no git surface: `rg -av '^\s*//|^\s*\*' extensions/pi-claude-marketplace/orchestrators/plugin/list.ts | rg -c 'platform/git|gitOps|isomorphic-git'` equals 0
    - `node --test tests/orchestrators/plugin/list.test.ts` exits 0
  </acceptance_criteria>
  <done>list.ts derives `remote` once via the shared presence probe, renders the bare `(remote)` row, filters the bucket, and stays git-surface-free; the list suite covers the intended `--available` behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the `--remote` edge flag and verify completion-bucketizer parity</name>
  <files>extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts, extensions/pi-claude-marketplace/orchestrators/edge-deps.ts, tests/orchestrators/edge-deps.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts (USAGE string line 21-22; BOOLEAN_FLAGS line 24; the listPlugins spread 68-78)
    - extensions/pi-claude-marketplace/orchestrators/edge-deps.ts (classifyNotInstalledPluginRow 116-132 — consumes probeManifestEntry, already threads locations; no logic change expected)
    - tests/orchestrators/edge-deps.test.ts (the output-parity drift-guard that feeds a manifest through __test_availableRowMessage + the bucketizer; add a git-source cold-cache parity case)
    - .claude/rules/typescript-comments.md
  </read_first>
  <action>
edge/handlers/plugin/list.ts — add `"--remote"` to `BOOLEAN_FLAGS` (line 24). Add one spread line to the `listPlugins({...})` call (after line 77): `...(filterFlags.has("--remote") && { remote: true }),`. Append `[--remote]` to the `USAGE` string (line 21-22) after `[--partial]`. Comment the flag addition with `RSTA-07`.

edge-deps.ts — `classifyNotInstalledPluginRow` (116-132) already calls `probeManifestEntry(entry, marketplaceRoot, locations)` and returns its status directly; once Plan 02's probe returns `"remote"`, this row inherits it automatically. Verify no local change is needed for the return type (the `PluginIndexRow.status` widen in Plan 02 already admits `"remote"`). If a comment references the old git-source `(available)` short-circuit as living "in the shared probe", update it to describe the presence-derived `remote`/warm classification (cite `RSTA-03`). Do NOT re-implement classification here — parity is by construction.

edge-deps.test.ts — add a parity case: feed a manifest with a not-installed git-source entry (cold cache) through both `__test_availableRowMessage` (list) and the bucketizer, asserting BOTH classify `remote` — this is the RSTA-03 output-parity drift-guard extended to the new bucket.

Comment discipline: `RSTA-NN` / `D-80-NN` anchors only.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/edge-deps.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `--remote` is a recognized flag: `rg -a '"--remote"' extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts` matches in BOOLEAN_FLAGS AND the spread
    - USAGE string includes `[--remote]`: `rg -a "\[--remote\]" extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts` matches
    - The drift-guard asserts list + completion both classify a cold git source `remote` (behavior asserted by the new edge-deps.test.ts parity case)
    - edge-deps.ts imports no git surface: `rg -av '^\s*//|^\s*\*' extensions/pi-claude-marketplace/orchestrators/edge-deps.ts | rg -c 'platform/git|gitOps'` equals 0
    - `node --test tests/orchestrators/edge-deps.test.ts` exits 0 and `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>`list --remote` parses and filters the remote bucket; the completion bucketizer inherits `remote` at parity with list (drift-guard green); no git surface leaks into edge-deps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → list filter | `--remote` flag parsed at the edge; a union-filter change alters which plugins are shown |
| list orchestrator → completion bucketizer | the two surfaces must classify identically or the drift-guard fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-06 | Info disclosure / policy | list.ts presence composition | high | mitigate | list.ts imports only makePresenceProbe + resolveStrict; no-orchestrator-network.test.ts greps list.ts for gitOps/platform/git (NFR-5 — list incl. --remote stays network-free) |
| T-80-07 | Tampering | list/completion parity | medium | mitigate | Consolidation onto the shared probe makes parity structural; the output-parity drift-guard asserts both surfaces classify `remote` identically |
| T-80-08 | Spoofing | installed plugin rendering remote | medium | mitigate | `remote` derives ONLY on the not-installed availableRowMessage path; the installed path keeps the probeUpgradeCandidate CR-01 degrade (D-78-04) — a list.test.ts case asserts an installed git plugin with a missing clone stays `(upgradable)` |
| T-80-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs (in-repo TS edits only) per RESEARCH.md |
</threat_model>

<verification>
- `node --test tests/orchestrators/plugin/list.test.ts` — green (remote render + filter cases, intended --available change, installed-never-remote guard)
- `node --test tests/orchestrators/edge-deps.test.ts` — green (parity drift-guard extended to remote)
- `no-orchestrator-network.test.ts` — green (list.ts + edge-deps stay git-free)
- `npm run check` — green before merge
</verification>

<success_criteria>
- `list` renders bare `(remote)` for cold git sources and the three-way verdict for warm ones
- `list --remote` filters the remote bucket; `--available` excludes cold git sources; `--available --remote` restores the old set
- Completion bucketizer inherits `remote` at parity (drift-guard green)
- Installed git plugins never render `(remote)`; list.ts + edge-deps stay network-free
</success_criteria>

<output>
Create `.planning/workstreams/url-source/phases/80-remote-status-glyph-reassignment-warm-cache-resolution/80-03-SUMMARY.md` when done
</output>
