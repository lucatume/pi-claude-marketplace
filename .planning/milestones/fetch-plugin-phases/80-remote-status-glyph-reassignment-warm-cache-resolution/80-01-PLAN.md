---
phase: 80-remote-status-glyph-reassignment-warm-cache-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - docs/output-catalog.md
  - docs/messaging-style-guide.md
  - tests/architecture/notify-closed-set-locks.test.ts
  - tests/architecture/notify-grammar-invariant.test.ts
  - tests/architecture/catalog-uat.test.ts
autonomous: true
requirements: [RSTA-01, RSTA-02]
must_haves:
  truths:
    - "STATUS_TOKENS and PLUGIN_STATUSES each carry a new final member `remote`"
    - "The `(remote)` plugin row renders with glyph `◌` (U+25CC); `(disabled)` and `(will disable)` rows render with glyph `◍` (U+25CD)"
    - "`npm run check` is green after the amendment commit (one atomic commit)"
  artifacts:
    - "extensions/pi-claude-marketplace/shared/notify.ts (ICON_REMOTE, PluginRemoteMessage, remote tuple members, renderer arm)"
    - "docs/output-catalog.md (remote legend + status row + fixtures; disabled glyph flipped to ◍)"
    - "docs/messaging-style-guide.md (PluginRemoteMessage variant line; disabled ◍ note)"
  key_links:
    - "PluginStatus derives from PLUGIN_STATUSES via indexed access; PluginInfoRowBase.status Extract list must include `remote` or the info surface cannot render it"
    - "catalog-uat.test.ts FIXTURES entries pair bidirectionally with docs/output-catalog.md catalog-state blocks (byte-equality both directions)"
---

<objective>
Land the closed-set token/glyph amendment for the `(remote)` plugin status in ONE atomic green commit (RSTA-01 vocabulary + RSTA-02 lockstep). Add the `remote` member to both closed-set tuples, add `ICON_REMOTE = "◌"`, reassign `ICON_DISABLED` to `◍`, add the `PluginRemoteMessage` variant + its renderer arms, and update every tripwire test, catalog byte form, catalog-UAT fixture, and the style guide in lockstep so the amendment cannot land partially.

Purpose: The closed-set lockstep discipline (RSTA-02, D-80-01) requires the token, glyph reassignment, catalog byte forms, catalog-UAT fixtures, and style-guide entry to land atomically — a partial amendment leaves the closed-set authority (notify.ts) out of sync with the byte-normative catalog and fails the tripwire tests. This plan is the foundation every downstream classification seam (Plans 02/03/04) builds on: they emit `"remote"` / `PluginRemoteMessage`, which only exist after this commit.

Output: `notify.ts` with the `remote` vocabulary; updated tripwire tests (tuple lengths 23→24 / 18→19, glyph regexes); catalog + catalog-UAT fixtures with `(remote)` rows and flipped `◍` disabled bytes; style guide updated.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/url-source/phases/80-remote-status-glyph-reassignment-warm-cache-resolution/80-CONTEXT.md
@.planning/workstreams/url-source/phases/80-remote-status-glyph-reassignment-warm-cache-resolution/80-RESEARCH.md
@.planning/workstreams/url-source/phases/80-remote-status-glyph-reassignment-warm-cache-resolution/80-PATTERNS.md
@.claude/rules/typescript-comments.md
</context>

<artifacts_this_phase_produces>
The symbols this plan creates (downstream plans 02/03/04 consume them):

| Symbol | File | Kind | Value / signature |
|--------|------|------|-------------------|
| `remote` (STATUS_TOKENS member) | shared/notify.ts | tuple member | appended LAST after `partially-available` |
| `remote` (PLUGIN_STATUSES member) | shared/notify.ts | tuple member | appended LAST after `partially-available` |
| `ICON_REMOTE` | shared/notify.ts | `export const` | `"◌"` (U+25CC) |
| `ICON_DISABLED` (reassigned) | shared/notify.ts | `export const` | `"◍"` (U+25CD) — value change only |
| `PluginRemoteMessage` | shared/notify.ts | `export interface` | `status: "remote"; name: string; version?: string; description?: string` — NO scope, NO reasons |
| `PluginNotificationMessage` (union member) | shared/notify.ts | union arm | `\| PluginRemoteMessage` |
| `PluginInfoRowBase.status` (Extract widen) | shared/notify.ts | type | `\| "remote"` added to the Extract list |

NO new resolver arm is introduced (NFR-7). `remote` is a plugin-STATUS token only; `ManifestEntryClassification` grows in Plan 02, not here.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land the `remote` closed-set token + glyph reassignment amendment atomically</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, docs/output-catalog.md, docs/messaging-style-guide.md, tests/architecture/notify-closed-set-locks.test.ts, tests/architecture/notify-grammar-invariant.test.ts, tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (STATUS_TOKENS ~211-248, PLUGIN_STATUSES ~400-430, PluginAvailableMessage ~681-686, PluginPartiallyAvailableMessage ~723-734, PluginDisabledMessage ~666-672, PluginNotificationMessage union ~928-946, PluginInfoRowBase ~1196-1216, ICON_DISABLED ~1452, ICON constants ~1439-1475, plugin-row renderer switch ~2120-2239, pluginInfoStatusGlyph ~3042-3064)
    - tests/architecture/notify-closed-set-locks.test.ts (STATUS_TOKENS length assertion line 41, PLUGIN_STATUSES length assertion line 49)
    - tests/architecture/notify-grammar-invariant.test.ts (WILL_TOKEN_RE line 238, DISABLED_TOKEN_RE line 247, ROW_ICONS line 73)
    - tests/architecture/catalog-uat.test.ts (FIXTURES map ~280, disabled-inventory fixture ~655, disabled-inventory-with-description ~679, forward walk ~3728, inverse walk ~3970)
    - docs/output-catalog.md (glyph legend lines 8-13, plugin-row `<icon>` enumeration line 38, status-token table lines 130-147, disabled fixtures)
    - docs/messaging-style-guide.md (variant list lines 38-53, "16 plugin status discriminators" line 58)
    - .claude/rules/typescript-comments.md (only D-NN / RSTA-NN / SNM-NN traceability anchors in comments; no phase/plan/wave refs)
  </read_first>
  <action>
Perform the closed-set amendment in a SINGLE commit. All of the following land together so the tripwires never see a partial state:

notify.ts tuples — append `"remote"` as the LAST member of BOTH `STATUS_TOKENS` and `PLUGIN_STATUSES` (after `"partially-available"`), each with a block comment citing `RSTA-01 / D-80-06`: inventory row, `info` severity, `needsReload: false`, appended last below the reload-hint trigger window (same discipline as `partially-available`). In the `PLUGIN_STATUSES` comment note the `Extract<PluginStatus, ...>` coupling that requires the member here (mirroring the `partially-available` comment at ~423-429).

notify.ts glyph constants — reassign `ICON_DISABLED` from `"◌"` to `"◍"` (U+25CD, per D-80-01), keeping its export. Add a new sibling `export const ICON_REMOTE = "◌";` (U+25CC) near the other `ICON_` constants (~1439-1475) with a docstring citing `RSTA-02 / D-80-01`: the `(remote)` dotted-circle glyph for a not-installed git-source plugin with no materialized clone. Update the `ICON_DISABLED` docstring to state it now uses `◍` and add a note that `◌` is the reassigned `ICON_REMOTE` glyph. Do NOT change the `will disable` / `disabled` renderer-arm code — they consume `ICON_DISABLED` through the constant, so the glyph flows through automatically.

notify.ts variant — add `export interface PluginRemoteMessage extends MessageBase` modeled on `PluginAvailableMessage` (~681-686): `status: "remote"`, `name: string`, optional `version?: string`, optional `description?: string`. NO `scope` field (SNM-11 carve-out family: `available | partially-available | unavailable` — `remote` joins it), NO `reasons`, NO `partialHint`. Docstring cites `RSTA-01 / D-80-03`. Add `| PluginRemoteMessage` to the `PluginNotificationMessage` union (~928-946).

notify.ts PluginInfoRowBase — add `| "remote"` to the `Extract<PluginStatus, ...>` list at ~1202-1210 so the info surface can render `(remote)` (RSTA-01 replaces `(available)` in info too).

notify.ts plugin-row renderer switch (~2120-2239) — add a `case "remote":` arm cloned from the `available` arm (~2120-2129): use `ICON_REMOTE`, `p.name`, `renderScopeBracket(undefined, mpScope)`, `renderVersion(p.version)`, `"(remote)"`, and DROP the `composeReasons(...)` line (bare row per D-80-03 — remote carries no reasons). Comment cites `RSTA-01 / D-80-03 / SNM-11`. The existing `default: assertNever(p)` will compile only once this arm exists.

notify.ts pluginInfoStatusGlyph switch (~3042-3064) — add `case "remote": return ICON_REMOTE;` before the `default: assertNever(status)`. Comment cites `RSTA-01`.

Tripwire test notify-closed-set-locks.test.ts — bump the STATUS_TOKENS length assertion `23` → `24` (line 41) and the PLUGIN_STATUSES length assertion `18` → `19` (line 49); extend the existing block comments with a `RSTA-01` line for the +1.

Tripwire test notify-grammar-invariant.test.ts — in `WILL_TOKEN_RE` (line 238) change the leading char class `[●○⊘◌]` → `[●○⊘◍]` (◌ reassigned away from `will disable`); in `DISABLED_TOKEN_RE` (line 247) change the anchor `^◌ ` → `^◍ `. Leave `ROW_ICONS` (line 73) unchanged — `remote` is info-severity and does not appear in the error/warning fixtures this constant drives (A3).

catalog-uat.test.ts FIXTURES — flip the two `disabled` fixtures' expected glyph bytes `◌` → `◍` (the `disabled-inventory` ~655 and `disabled-inventory-with-description` ~679 entries render through the flipped `ICON_DISABLED`, so their byte-equality expectations must match the new render). Add new `remote-inventory` (and, if the catalog carries a described variant, `remote-inventory-with-description`) FIXTURES entries under the plugin-list section keyed to match the new catalog blocks, each carrying a `PluginRemoteMessage`-bearing NotificationMessage (`status: "remote"`, severity `info`, `needsReload: false`). Each new FIXTURES entry MUST have a paired `<!-- catalog-state: remote-... -->` block in output-catalog.md (bidirectional runner: forward walk ~3728 + inverse orphan walk ~3970).

docs/output-catalog.md — glyph legend (lines 8-13): split the `◌` entry so `◌` now describes `(remote)`, and add a new `◍` legend entry describing `(disabled)` / `(will disable)`. Plugin-row `<icon>` enumeration (line 38): add `◍` to the `● / ○ / ⊘ / ⊖ / ◌` list. Status-token table (lines 130-147): add a `(remote)` row with icon `◌` describing the list/info/install-completion not-installed git-source surface; change the `(will disable)` and `(disabled)` rows' icon column `◌` → `◍`. Add paired `<!-- catalog-state: remote-... -->` fixture block(s) in the plugin-list section rendering `◌ <name> (remote)` (bare, no scope, no reason) and, for the info section if it carries one, the `(remote)` + `components: not resolved` form. Flip any existing `◌ <name> ... (disabled)` byte-fixture rows to `◍`.

docs/messaging-style-guide.md — variant list (lines 38-53): add a `PluginRemoteMessage // status: "remote"; NO scope (SNM-11); NO reasons; ◌ (RSTA-01)` line; annotate the `PluginDisabledMessage` line to note it now renders `◍`. Line 58: the "16 plugin status discriminators" prose count is stale; either fix it to `19` or drop the numeric count per the guide's own "do not re-enumerate in prose" rule (Claude's discretion — dropping the count is preferred).

Comment discipline: every new/changed comment uses only `RSTA-NN` / `D-80-NN` / `SNM-NN` / `NFR-N` traceability anchors — NO phase/plan/wave references (.claude/rules/typescript-comments.md).
  </action>
  <verify>
    <automated>node --test tests/architecture/notify-closed-set-locks.test.ts tests/architecture/notify-grammar-invariant.test.ts tests/architecture/catalog-uat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `STATUS_TOKENS` has exactly 24 members with `"remote"` last; `PLUGIN_STATUSES` has exactly 19 members with `"remote"` last (behavior asserted by notify-closed-set-locks.test.ts equal(24) / equal(19))
    - `ICON_REMOTE` exports `"◌"` (U+25CC) and `ICON_DISABLED` exports `"◍"` (U+25CD): `rg -a "ICON_REMOTE = \"◌\"" extensions/pi-claude-marketplace/shared/notify.ts` and `rg -a "ICON_DISABLED = \"◍\"" extensions/pi-claude-marketplace/shared/notify.ts` each match once
    - `PluginRemoteMessage` interface exists with `status: "remote"`, no `scope` field, no `reasons` field: `rg -a "interface PluginRemoteMessage" extensions/pi-claude-marketplace/shared/notify.ts` matches; the interface body contains no `scope` and no `reasons` line
    - Plugin-row renderer emits `◌ <name> (remote)` with no reason brace for a `PluginRemoteMessage` (asserted by a new catalog-uat FIXTURES `remote-inventory` byte-equality entry)
    - `(disabled)` / `(will disable)` rows render `◍`: notify-grammar-invariant.test.ts `DISABLED_TOKEN_RE` anchored `^◍ ` and `WILL_TOKEN_RE` char class `[●○⊘◍]` pass
    - catalog-uat forward walk AND inverse-walk both green (no orphan fixture, no unpaired catalog block)
    - `node --test tests/architecture/notify-closed-set-locks.test.ts tests/architecture/notify-grammar-invariant.test.ts tests/architecture/catalog-uat.test.ts` exits 0
    - No comment in the touched files contains a `Phase`/`Plan`/`Wave` planning reference: `rg -an "Phase [0-9]|Plan [0-9]|Wave [0-9]" extensions/pi-claude-marketplace/shared/notify.ts` returns only pre-existing domain-language matches (two-phase commit narration), no new ones
  </acceptance_criteria>
  <done>The `remote` token + `◌`/`◍` glyph reassignment lands in one commit; all three architecture tripwire suites are green; the catalog byte forms and style guide match the new render.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render-time closed-set authority → user terminal | notify.ts is the single source of truth for status tokens/glyphs; a drift between it and the byte-normative catalog produces wrong output the user reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-01 | Tampering | closed-set tuples / catalog byte forms | medium | mitigate | Lockstep discipline: tuple-length tripwires + bidirectional catalog-UAT byte-equality gate fail the commit if notify.ts and docs/output-catalog.md diverge |
| T-80-02 | Information disclosure | glyph legend accuracy | low | accept | The `◍`/`◌` reassignment was operator-verified in-terminal 2026-07-13 (D-80-01); no runtime re-verification, gate cleared |
| T-80-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan (in-repo TS/doc edits only); RESEARCH.md confirms no external deps introduced |
</threat_model>

<verification>
- `node --test tests/architecture/notify-closed-set-locks.test.ts tests/architecture/notify-grammar-invariant.test.ts tests/architecture/catalog-uat.test.ts` — all green
- `npm run check` — green (typecheck + ESLint + Prettier + full suite) before handing off to Wave 2
</verification>

<success_criteria>
- `remote` is a member of both closed-set tuples (lengths 24 / 19), appended last
- `ICON_REMOTE = "◌"`, `ICON_DISABLED = "◍"`; renderer + info-glyph switches have `remote` arms
- `PluginRemoteMessage` variant exists (no scope, no reasons) and is a `PluginNotificationMessage` union member
- Catalog + catalog-UAT + style guide all reflect the amendment; bidirectional byte-equality green
- Single atomic commit; `npm run check` green
</success_criteria>

<output>
Create `.planning/workstreams/url-source/phases/80-remote-status-glyph-reassignment-warm-cache-resolution/80-01-SUMMARY.md` when done
</output>
