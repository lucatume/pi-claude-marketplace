---
phase: 81-fetch-verb-info-fetch
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts
  - tests/orchestrators/plugin/info.test.ts
autonomous: true
requirements: [FTCH-03, FTCH-04, FTCH-06]
must_haves:
  truths:
    - "info --fetch <plugin>@<marketplace> fetches the clone/mirror then resolves and lists components in one step"
    - "A fetch failure inside info --fetch degrades to the existing components: not resolved arm with an existing closed-set reason (network unreachable / authentication required) and NEVER fails the info command"
    - "Bare info (no --fetch) stays network-free — the fetch hook only runs when --fetch is passed"
    - "info.ts reaches git only through clone-cache.ts entrypoints and auth-host.ts re-exports; it names zero git surface (it is already a FORBIDDEN_TARGET)"
  artifacts:
    - "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts (GetPluginInfoOptions gains fetch?: boolean; a fetch hook runs before the fs-only resolve)"
    - "extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts (recognizes the --fetch boolean flag, keeps rejecting all other flags)"
    - "tests/orchestrators/plugin/info.test.ts (info --fetch success + fetch-failure-degrade cases)"
  key_links:
    - "the edge info handler threads ...(fetch && { fetch: true }) into getPluginInfo"
    - "getPluginInfo wraps the fetch hook in try/catch and falls through to the existing componentsResolved: false arm on failure"
---

<objective>
Add `info --fetch` (FTCH-03): a boolean flag on the `info` verb that fetches the git-source clone/mirror, then resolves and lists components in one step. On any fetch failure the command degrades to the existing `components: not resolved` arm with an existing closed-set reason and never fails info. Bare `info` stays network-free.

Purpose: `info --fetch` is the "fetch then resolve in one step" convenience locked by FTCH-03/D-81-04. It reuses the same install-style clone-cache seam delegation as the fetch orchestrator, but lives inside `info.ts` (already a FORBIDDEN_TARGET), so it must name zero git surface. This plan is independent of the `fetch.ts` orchestrator (it reaches the seam directly), so it runs in Wave 1 in parallel.

Output: edited `orchestrators/plugin/info.ts` and `edge/handlers/plugin/info.ts`, plus extended `tests/orchestrators/plugin/info.test.ts`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/url-source/ROADMAP.md
@.planning/workstreams/url-source/STATE.md
@.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-CONTEXT.md
@.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-RESEARCH.md
@.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-PATTERNS.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend info.test.ts with info --fetch success + failure-degrade cases (Wave 0)</name>
  <files>tests/orchestrators/plugin/info.test.ts</files>
  <read_first>
    - tests/orchestrators/plugin/info.test.ts (the existing info test structure: how getPluginInfo is driven, how the mock ctx + injected seam/ops are set up, how the components:not-resolved arm is currently asserted for external sources)
    - tests/helpers/git-mock.ts, tests/helpers/credential-mock.ts, tests/helpers/device-flow-mock.ts (injectable mocks — confirm exported factory names)
    - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts (the current getPluginInfo signature + the existing componentsResolved: false / components: not resolved arm ~756-761 the degrade must reuse — use `grep -a` / `rg --text`, info.ts has a pre-existing NUL byte near line 332; do NOT fix it)
  </read_first>
  <behavior>
    - info --fetch on a cold (remote) git plugin: the mock git seam is called, then components are resolved and listed (the info block shows resolved components, not "remote").
    - info --fetch when the fetch THROWS (network/auth error): the command still SUCCEEDS, rendering the normal info row with components: not resolved + an existing reason (network unreachable OR authentication required). No throw escapes getPluginInfo.
    - bare info (no --fetch) on the same git plugin: the mock git seam is NOT called (network-free); components resolve fs-only only if already warm, else the existing behavior is unchanged.
  </behavior>
  <action>
    Add three test cases to tests/orchestrators/plugin/info.test.ts driving getPluginInfo with `fetch: true` (and one control with fetch omitted). Inject the mock clone-cache seam so no real git runs. For the failure case, configure the mock seam to throw a network/auth-typed error and assert getPluginInfo RESOLVES (does not reject) and that the emitted info block carries components: not resolved + an existing closed-set reason. For the bare-info control, assert the mock git seam call count is zero. These tests fail RED until Task 2 threads the fetch hook. Do not weaken assertions to pass early.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/orchestrators/plugin/info.test.ts 2>&1 | tail -15; echo "RED_EXPECTED"</automated>
  </verify>
  <acceptance_criteria>
    - tests/orchestrators/plugin/info.test.ts gains three cases: info --fetch success, info --fetch failure-degrade, bare-info network-free control.
    - The failure-degrade test asserts getPluginInfo does NOT reject and the info block shows components: not resolved + an existing reason.
    - Cases fail RED pending Task 2 (fetch option not yet threaded).
  </acceptance_criteria>
  <done>info.test.ts covers the three behaviors and fails RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the --fetch hook through info.ts + edge info handler</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts (GetPluginInfoOptions ~98-112; the fs-only resolve path; the existing componentsResolved: false arm ~756-761 — use `grep -a`/`rg --text`, do NOT touch the pre-existing NUL byte)
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (makeInstallCloneProbe ~552-623 — the pinned/unpinned fork + buildProbeAuth + seam injection shape to reuse for the info fetch hook)
    - extensions/pi-claude-marketplace/orchestrators/plugin/clone-cache.ts (materializePluginClone / materializeOrRefreshPluginMirror / resolvePluginPin / canonicalCloneUrl — the only git entrypoints info.ts may name)
    - extensions/pi-claude-marketplace/orchestrators/auth-host.ts (buildAuthForHost + AuthAttemptResult — gate-clean auth; do NOT import platform/git)
    - extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts (lines ~30-79: positional parse + the current flag-rejection loop ~44-52 that emits "Unknown flag" for any --flag; splitPluginMarketplaceRef; delegate to getPluginInfo)
    - extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts (parsePositionalsWithFlags allow-list arm ~50-71 — the idiom for accepting one boolean flag while rejecting others)
    - tests/architecture/no-orchestrator-network.test.ts (confirm info.ts is already a FORBIDDEN_TARGET — the fetch hook must stay git-surface-free)
  </read_first>
  <action>
    In orchestrators/plugin/info.ts: add `fetch?: boolean` to GetPluginInfoOptions. Before the fs-only component resolve, add a fetch hook (network on cache miss only, auth at install parity — D-81-05) that runs ONLY when `opts.fetch === true` and the plugin is a git source: reuse the install-style pinned/unpinned fork (resolvePluginPin + materializePluginClone for pinned; canonicalCloneUrl + materializeOrRefreshPluginMirror for unpinned) via the clone-cache seam, with buildProbeAuth for host auth. CRITICAL (D-81-04): wrap the entire fetch hook in try/catch INSIDE getPluginInfo; on failure, fall through to the EXISTING componentsResolved: false arm with an existing reason (network unreachable / authentication required via the same typed-error narrowing update/install use) — NEVER let the throw reject getPluginInfo. After a successful fetch, proceed into the normal fs-only resolve against the now-warm tree (fresh, not a stale probe). Name ZERO git surface (info.ts is a FORBIDDEN_TARGET): only clone-cache.ts entrypoints + auth-host.ts re-exports; no gitOps / platform/git even type-only. Do not add persisted state.
    In edge/handlers/plugin/info.ts: change the flag-rejection loop to recognize the single boolean `--fetch` (set a local `fetch = true`) while still rejecting every other `--flag` via the existing usage-error path. Thread `...(fetch && { fetch: true })` into the getPluginInfo call. Keep bare info (no --fetch) unchanged and network-free.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/orchestrators/plugin/info.test.ts 2>&1 | tail -15 && echo "---GATE (expect 0)---" && grep -aEc '\bgitOps\b|DEFAULT_GIT_OPS|platform/git|refreshGitHubClone' extensions/pi-claude-marketplace/orchestrators/plugin/info.ts</automated>
  </verify>
  <acceptance_criteria>
    - The three info.test.ts cases pass GREEN.
    - `grep -aEc '\bgitOps\b|DEFAULT_GIT_OPS|platform/git|refreshGitHubClone' extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` returns 0 (info.ts stays a clean FORBIDDEN_TARGET).
    - GetPluginInfoOptions has `fetch?: boolean`; the edge handler accepts `--fetch` and rejects other flags.
    - The fetch hook is wrapped in try/catch; a thrown fetch does NOT reject getPluginInfo (verify by the failure-degrade test).
    - Bare info makes zero git-seam calls (network-free control test passes).
    - No new REASONS member introduced (`grep -aE 'REASONS =' info.ts` shows no new definition; the degrade reuses network unreachable / authentication required).
  </acceptance_criteria>
  <done>info --fetch fetches-then-resolves, degrades safely on failure, keeps bare info network-free, and info.ts names zero git surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user command → info --fetch | untrusted `<plugin>@<marketplace>` ref + `--fetch` flag cross here |
| info --fetch → git remote | network egress on cache miss only; host auth crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-07 | Denial of service | fetch throw failing the whole info command | medium | mitigate | Fetch hook wrapped in try/catch inside getPluginInfo; failure falls through to the existing componentsResolved: false arm (D-81-04) — info always succeeds. |
| T-81-08 | Information disclosure | credential leak into the info failure reason text | high | mitigate | Degrade reason comes from the closed REASONS set via typed-error narrowing, never raw auth-bundle interpolation; auth-host.ts never interpolates credentials (AUTH-09). |
| T-81-09 | Tampering | info.ts (FORBIDDEN_TARGET) gaining a git surface | high | mitigate | Fetch hook reaches git only through clone-cache.ts entrypoints + auth-host.ts re-exports; gate grep returns 0; the no-orchestrator-network gate keeps info.ts locked. |
| T-81-SC | Tampering | npm/pip/cargo installs | high | accept | No package install in this phase (100% internal composition). No install task; no [ASSUMED]/[SUS] package. |
</threat_model>

<verification>
- `node --test tests/orchestrators/plugin/info.test.ts` green.
- git-surface gate grep returns 0 for info.ts.
- Bare info network-free (control test).
</verification>

<success_criteria>
info --fetch fetches then resolves, degrades to not-resolved + existing reason on failure without failing info, bare info stays network-free, and info.ts names zero git surface.
</success_criteria>

<output>
Create `.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-03-SUMMARY.md` when done.
</output>
