---
phase: 81-fetch-verb-info-fetch
plan: 02
type: execute
wave: 2
depends_on: ["81-01"]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts
  - tests/orchestrators/plugin/fetch.test.ts
autonomous: true
requirements: [FTCH-01, FTCH-02, FTCH-04, FTCH-06, FTCH-07]
must_haves:
  truths:
    - "A single fetch <plugin>@<marketplace> materializes the plugin's clone/mirror into the shared cache without installing, then renders the post-fetch derived status row (available/partially-available/unavailable)"
    - "A path source and a pinned-warm clone render (skipped) + up-to-date at info severity and touch no network (no-op BEFORE any git seam call)"
    - "An unpinned-warm source always refreshes its mirror (the refresh IS the consented fetch) and renders its fresh post-refresh row"
    - "@<marketplace> and bare fetch enumerate the fetchable set from the MARKETPLACE MANIFEST (not installed state); a per-plugin failure never aborts the sweep"
    - "A single authMemo Map spans the whole sweep so a bulk sweep of N private-host plugins triggers each host's device flow at most once"
    - "fetch.ts names zero git surface: it reaches git only through clone-cache.ts entrypoints and auth-host.ts re-exports"
  artifacts:
    - "extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts (exports fetchPlugins + FetchTarget union + a test-only cloneCacheSeam override)"
    - "tests/orchestrators/plugin/fetch.test.ts"
  key_links:
    - "fetchPlugins imports FETCH_CONTEXT from fetch.messaging.ts and dispatches the cascade via notifyWithContext / notify-context helpers"
    - "the fetch-one core drives materializePluginClone (pinned) / materializeOrRefreshPluginMirror (unpinned) via the injected seam, with buildProbeAuth threading the sweep-wide authMemo"
    - "the post-fetch row is derived FRESH from probeManifestEntry / availableRowMessage against the now-warm tree, never a pre-materialize probe"
---

<objective>
Create `orchestrators/plugin/fetch.ts` — the fetch orchestrator implementing all three shapes (`fetch <plugin>@<marketplace>`, `fetch @<marketplace>`, bare `fetch`) on the install-style seam-injection model. Enumerate the fetchable set from the marketplace manifest, run each target through a fs-only no-op gate then the clone-cache git seam, derive the fresh post-fetch status row, and render the bulk cascade at update-verb grammar parity. Ship the Wave 0 test file scaffold covering the single/bulk shapes, no-op rows, network-free path/pinned-warm behavior, once-per-sweep auth, and failure-tolerant sweep.

Purpose: This is the core new capability of the phase (FTCH-01/02/04/06/07). It reuses shipped seams for every hard problem (git materialize, auth, classification, cascade) — the real work is the manifest enumeration glue, the no-op gate, and the failure-tolerant sweep.

Output: `fetch.ts` (exporting `fetchPlugins`, the `FetchTarget` union, and a test-only `cloneCacheSeam` override) and `tests/orchestrators/plugin/fetch.test.ts`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/url-source/ROADMAP.md
@.planning/workstreams/url-source/STATE.md
@.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-CONTEXT.md
@.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-RESEARCH.md
@.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-PATTERNS.md
@.claude/rules/typescript-comments.md
@extensions/pi-claude-marketplace/orchestrators/plugin/fetch.messaging.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold fetch.test.ts (Wave 0 — failing tests for the fetch orchestrator)</name>
  <files>tests/orchestrators/plugin/fetch.test.ts</files>
  <read_first>
    - tests/orchestrators/plugin/update.test.ts (the exact bulk-test analog: how it constructs targets, injects makeMockGitOps / makeMockCredentialOps / makeMockDeviceFlowHttp, asserts per-plugin rows, no-op rows, tally, and failure-tolerant sweep)
    - tests/helpers/git-mock.ts, tests/helpers/credential-mock.ts, tests/helpers/device-flow-mock.ts (the injectable mocks — confirm exact exported factory names before importing)
    - tests/helpers (locate fixtureMarketplaceDir / fixture builders update.test.ts uses to stand up a manifest with git-source entries)
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts (the fetchPlugins signature shape to mirror: opts object with target, scope, locations, ctx, credentialOps, optional deviceFlowHttp, optional cloneCacheSeam)
  </read_first>
  <behavior>
    - Single shape: fetch <plugin>@<mp> on a cold (remote) git plugin calls the mock git seam once, then emits a post-fetch status row (available/partially-available/unavailable) — NOT an install.
    - No-op (path): fetch on a path-source plugin renders (skipped) + up-to-date at info severity and makes ZERO mock git calls.
    - No-op (pinned-warm): fetch on a pinned source whose clone is already materialized renders (skipped) + up-to-date and makes ZERO mock git calls (no network).
    - Unpinned-warm refresh: fetch on an unpinned source with a warm mirror DOES call the mirror-refresh seam (refresh is the consented fetch) and renders the fresh row.
    - Bulk (@<mp>): enumerates the manifest's fetchable entries; a per-plugin fetch that throws is captured as a failed row and the sweep continues to the remaining plugins; output carries a summary line + tally.
    - Auth once-per-host: a bulk sweep of two plugins on the same private host triggers the mock device flow at most once (authMemo spans the sweep).
  </behavior>
  <action>
    Create tests/orchestrators/plugin/fetch.test.ts. Import the injectable mocks (makeMockGitOps, makeMockCredentialOps, makeMockDeviceFlowHttp) and the fixture-marketplace builder from tests/helpers, mirroring update.test.ts's imports. Write the six failing test cases described in behavior, driving `fetchPlugins` with an injected `cloneCacheSeam` (so no real git runs) and a mock ctx. Assert on the notify output rows (subject-first grammar, `(skipped) {up-to-date}` for no-ops), on the mock git call counts (zero for path/pinned-warm, one for cold-single, refresh-called for unpinned-warm), on sweep continuation after a thrown per-plugin fetch, and on the device-flow invocation count (<= 1 per host across the sweep). These tests are expected to FAIL now (fetchPlugins does not yet exist) and pass after Task 2. Do NOT weaken any assertion to pass early.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/orchestrators/plugin/fetch.test.ts 2>&1 | tail -15; echo "RED_EXPECTED"</automated>
  </verify>
  <acceptance_criteria>
    - tests/orchestrators/plugin/fetch.test.ts exists with six named test cases matching the behavior list.
    - Running the file fails (RED) because fetchPlugins is not yet implemented (import/reference error or assertion failures) — this is the expected pre-implementation state.
    - Tests import the real injectable mocks from tests/helpers (no ad-hoc git); the suite performs no real network or git.
  </acceptance_criteria>
  <done>fetch.test.ts scaffolds all six behaviors and fails RED pending the orchestrator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fetch.ts orchestrator (enumerate + fetch-one + failure-tolerant cascade)</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (lines ~505-623: makeInstallCloneProbe — the pinned/unpinned fork, buildProbeAuth, InstallCloneCacheSeam injection shape; lift this git-surface delegation verbatim in structure)
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts (bulk sweep skeleton ~267-441: empty-targets sentinel, outcome/target pairing, cardinality single-vs-plural, per-target try/catch; updateSinglePlugin NEVER-throws contract ~547-597; reasonsFromTypedError ~605-645 and classifyGitProbeFailure ~876 for failure-reason narrowing; how opts.authMemo threads through the batch loop ~387)
    - extensions/pi-claude-marketplace/orchestrators/edge-deps.ts (loadMarketplaceManifest + probeManifestEntry enumeration pattern ~195-224 — the fetchable-set source; mirror THIS, not update's state-record enumeration)
    - extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts (makePresenceProbe for the fs-only no-op gate ~110-139; probeManifestEntry for post-fetch classification ~167+)
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts (availableRowMessage / __test_availableRowMessage ~547-676 — the fresh post-fetch success-row builder)
    - extensions/pi-claude-marketplace/orchestrators/plugin/clone-cache.ts (materializePluginClone, materializeOrRefreshPluginMirror, resolvePluginPin, canonicalCloneUrl — the ONLY git entrypoints fetch.ts may name)
    - extensions/pi-claude-marketplace/orchestrators/auth-host.ts (buildAuthForHost, hostFromCloneUrl, DEFAULT_CREDENTIAL_OPS, AuthAttemptResult — the gate-clean auth re-exports; do NOT import platform/git)
    - extensions/pi-claude-marketplace/shared/notify-context.ts (notifyWithContext / notifyUpdateWithContext / notifyUpdateNoOpWithContext dispatch — how a CommandContext + marketplaces[] + cardinality + tally renders the cascade)
    - extensions/pi-claude-marketplace/orchestrators/plugin/fetch.messaging.ts (FETCH_CONTEXT — the render vocabulary produced by Plan 01)
  </read_first>
  <action>
    Implement `fetchPlugins(opts)` in fetch.ts on the install-style seam-injection model (NOT the update-style gitOps exemption). Per D-81-01, define a `FetchTarget` discriminated union covering the three shapes: `{ kind: "plugin", plugin, marketplace }` (single), `{ kind: "marketplace", marketplace }`, `{ kind: "all" }`. Network on cache miss only, auth at install parity (D-81-05). Structure:
    (1) Enumerate the fetchable set from the marketplace MANIFEST: for the plugin shape resolve the single manifest entry; for marketplace/all shapes call `loadMarketplaceManifest` per marketplace and classify each entry via `probeManifestEntry` (fs-only). Fetchable = `status === "remote"` OR (git source AND `source.sha === undefined` i.e. unpinned). Pinned-warm (status not remote AND sha set) and path/non-git are no-op targets (still enumerated, rendered as skipped).
    (2) Create `const authMemo = new Map<string, AuthAttemptResult>()` ONCE at the top of the sweep and thread it into every `buildProbeAuth` call (once-per-host contract, FTCH-06 — a per-plugin memo would re-trigger device flow N times).
    (3) For each target, inside try/catch (NEVER-throws per-plugin, mirroring updateSinglePlugin): run `makePresenceProbe(locations)(source)` FIRST; if the target is a path source, or a pinned source (`sha` set) whose presence is `materialized`, it is a no-op → emit `(skipped)` + `up-to-date` via FETCH_CONTEXT, skip the network. Otherwise (remote-cold OR any unpinned) run the fetch-one core: pinned arm → `resolvePluginPin` + `buildProbeAuth` + `materializePluginClone`; unpinned arm → `canonicalCloneUrl` + `buildProbeAuth` + `materializeOrRefreshPluginMirror` (always refreshes). After the seam returns, derive the success row FRESH by re-running `probeManifestEntry` / `availableRowMessage` against the now-warm tree (never a pre-materialize probe). Capture a thrown per-plugin fetch into a `partition: "failed"` outcome via `reasonsFromTypedError` / `classifyGitProbeFailure` (all reasons already in REASONS: `network unreachable`, `authentication required`, `source missing`, `permission denied`) — never abort the sweep.
    (4) Aggregate outcomes into per-(scope, marketplace) blocks and dispatch via `notifyWithContext` with `FETCH_CONTEXT`. Cardinality: plugin shape = `single` (no tally); marketplace/all = `plural` (tally). Empty fetchable set → the update-style never-silent no-op headline analog (a "nothing to fetch" constant, mirroring update's `Plugin update: nothing to update`).
    Expose a test-only optional `cloneCacheSeam` override (mirror `InstallCloneCacheSeam`) so tests inject mocks. Name ZERO git surface: no `gitOps`, `DEFAULT_GIT_OPS`, `refreshGitHubClone`, or `platform/git` import (even type-only) — the gate greps `\bgitOps\b` on comment-stripped source. Do NOT persist any fetch state (derive-not-persist). Do NOT add a second `softDepStatus(pi)` probe — pass `pi` through; notify owns the single probe.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/orchestrators/plugin/fetch.test.ts 2>&1 | tail -15 && echo "---GATE (expect 0)---" && grep -aEc '\bgitOps\b|DEFAULT_GIT_OPS|platform/git|refreshGitHubClone' extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts</automated>
  </verify>
  <acceptance_criteria>
    - All six tests in tests/orchestrators/plugin/fetch.test.ts pass (GREEN).
    - `grep -aEc '\bgitOps\b|DEFAULT_GIT_OPS|platform/git|refreshGitHubClone' extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts` returns 0 (zero git surface named — the gate in Plan 04 will lock this permanently).
    - fetch.ts exports fetchPlugins, the FetchTarget union, and the test-only cloneCacheSeam override.
    - fetch.ts imports FETCH_CONTEXT from fetch.messaging.ts and the git seam only via clone-cache.ts entrypoints and auth-host.ts re-exports.
    - Path source and pinned-warm targets produce ZERO mock git calls in the tests (network-free no-op, FTCH-02/04); unpinned-warm produces a refresh call.
    - A single authMemo is constructed once at sweep top and threaded into every buildProbeAuth call (verify by reading: exactly one `new Map` for the memo, passed to each fetch-one; the device-flow-count test is <= 1 per host).
    - `grep -aEc 'writeFile|state.json|fetchRegistry|refcount' extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts` shows no persisted fetch state (derive-not-persist; any fs write present must be only the clone seam's, which lives in clone-cache.ts, not here).
  </acceptance_criteria>
  <done>fetchPlugins implements all three shapes, is network-free for no-ops, once-per-host for auth, failure-tolerant for bulk, git-surface-free, and all six tests pass GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user command → fetch orchestrator | untrusted `<plugin>@<marketplace>` ref + `--scope` cross here |
| fetch orchestrator → git remote | network egress on cache miss only; private/self-hosted host auth crosses here |
| manifest file → enumeration | marketplace manifest content drives which entries are fetched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-03 | Information disclosure | credential leak into notify/error text on a failed fetch | high | mitigate | Failure narrowing routes through reasonsFromTypedError / classifyGitProbeFailure (closed REASONS) — never raw interpolation of the auth bundle; auth-host.ts never interpolates credentials (AUTH-09, enforced by no-credential-leak.test.ts). |
| T-81-04 | Elevation of privilege / Info disclosure | cross-host credential reuse in a bulk sweep | high | mitigate | authMemo is host-keyed; buildAuthForHost returns undefined for no-provider hosts (T-79-04 guard), so a per-host bundle cannot leak to another host even within one sweep. |
| T-81-05 | Tampering | path escape via git-subdir during materialize | medium | mitigate | Containment lives in the unchanged clone-cache.ts seam (resolveGitSubdirRoot clone-root-anchored, SC-7 chokepoint, NFR-10); fetch.ts never writes outside plugin-clones/. |
| T-81-06 | Tampering | malicious manifest triggering unexpected network | medium | mitigate | Network only on cache miss for git sources the user explicitly targeted; path/npm/unknown never fetch; the no-op gate runs BEFORE any git seam call. |
| T-81-SC | Tampering | npm/pip/cargo installs | high | accept | No package install in this phase (100% internal composition — RESEARCH.md Package Legitimacy Audit: zero external packages). No install task, no [ASSUMED]/[SUS] package. |
</threat_model>

<verification>
- `node --test tests/orchestrators/plugin/fetch.test.ts` all green.
- git-surface gate grep returns 0 for fetch.ts (locked permanently by Plan 04's FORBIDDEN_TARGETS addition).
- No persisted fetch state written by fetch.ts (derive-not-persist).
</verification>

<success_criteria>
fetchPlugins ships all three shapes with correct no-op / fetch / failure-tolerant behavior, once-per-host auth, and zero git surface. Ready for Plan 04 to wire the edge and lock the gate.
</success_criteria>

<output>
Create `.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-02-SUMMARY.md` when done.
</output>
