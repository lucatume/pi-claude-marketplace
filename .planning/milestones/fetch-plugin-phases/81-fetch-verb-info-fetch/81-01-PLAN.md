---
phase: 81-fetch-verb-info-fetch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/fetch.messaging.ts
autonomous: true
requirements: [FTCH-02]
must_haves:
  truths:
    - "FETCH_CONTEXT exposes a total render map keyed by FETCH_STATUSES; a missing arm is a TypeScript compile error"
    - "The (skipped) no-op arm renders (skipped) + up-to-date via the shared notify row helper, at info severity"
    - "No new ICON, STATUS_TOKEN, PLUGIN_STATUS, or REASONS member is introduced"
  artifacts:
    - "extensions/pi-claude-marketplace/orchestrators/plugin/fetch.messaging.ts"
  key_links:
    - "FETCH_CONTEXT.render arm bodies CALL shared notify.ts row helpers verbatim (no duplicated row-building code)"
    - "FetchMsg is a subset of the central plugin message shapes exported by shared/notify.ts"
---

<objective>
Create the command-local messaging vocabulary for the new `fetch` verb: `fetch.messaging.ts`, exporting `FETCH_STATUSES`, the `FetchMsg` union, a total render map, and the `FETCH_CONTEXT` command context. This is a near-clone of `update.messaging.ts` with fetch's private status set.

Purpose: The `fetch` orchestrator (Plan 02) and its cascade renderer need a `CommandContext` that maps each fetch outcome status to a shared `notify.ts` row helper. Isolating the render vocabulary in its own file keeps the D-10 open/closed discipline (a missing arm is a compile error) and lets the orchestrator import a ready `FETCH_CONTEXT`.

Output: `extensions/pi-claude-marketplace/orchestrators/plugin/fetch.messaging.ts`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/url-source/ROADMAP.md
@.planning/workstreams/url-source/STATE.md
@.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-CONTEXT.md
@.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-RESEARCH.md
@.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-PATTERNS.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fetch.messaging.ts (FETCH_CONTEXT render vocabulary)</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/fetch.messaging.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.messaging.ts (the exact analog — clone its shape: private status tuple, message union, total render map, CommandContext export)
    - extensions/pi-claude-marketplace/shared/notify.ts (the shared row helpers the render arms must CALL, the central plugin message shapes PluginAvailableMessage / PluginPartiallyAvailableMessage / PluginUnavailableMessage / PluginRemoteMessage / PluginSkippedMessage / PluginFailedMessage, and the closed sets STATUS_TOKENS / PLUGIN_STATUSES / REASONS / ICON constants — confirm every member fetch needs already exists)
    - extensions/pi-claude-marketplace/shared/notify-context.ts (the CommandContext type + notifyWithContext dispatch contract fetch.messaging.ts must satisfy)
    - .claude/rules/typescript-comments.md (comment policy: no phase/plan/wave numbers; keep decision IDs and requirement IDs as traceability)
  </read_first>
  <action>
    Create fetch.messaging.ts modeled byte-for-structure on update.messaging.ts. Export a `FETCH_STATUSES` const tuple with members exactly: `available`, `partially-available`, `unavailable`, `remote`, `skipped`, `failed`. Export `type FetchStatus = (typeof FETCH_STATUSES)[number]`. Export a `FetchMsg` union of the central message shapes: `PluginAvailableMessage`, `PluginPartiallyAvailableMessage`, `PluginUnavailableMessage`, `PluginRemoteMessage`, `PluginSkippedMessage`, `PluginFailedMessage` (import from shared/notify.ts). Declare a total render map `const FETCH_RENDER: { [K in FetchStatus]: RenderFn<Extract<FetchMsg, { status: K }>> }` whose arm bodies CALL the shared notify.ts row helpers verbatim (do not re-implement row building). The `skipped` arm mirrors update.messaging.ts's skipped arm — render the row through `pluginRow` with `ICON_UNINSTALLABLE`, the `(skipped)` token, and the `up-to-date` reason (per D-81-02: existing closed-set reason, info severity). Export `const FETCH_CONTEXT = { Messaging: { label: "Plugin fetch" }, render: FETCH_RENDER } as const satisfies CommandContext<FetchStatus, FetchMsg>`. Introduce NO new ICON, STATUS_TOKEN, PLUGIN_STATUS, or REASONS member anywhere (closed sets do not grow this phase — the Phase 80 amendment was the only closed-set change). Match the existing file's import style and the ESM `.ts` extension import convention used across the orchestrators directory.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && npx tsc --noEmit -p extensions/pi-claude-marketplace/tsconfig.json 2>&1 | grep -v '^$' | head -20; echo "TYPECHECK_DONE"</automated>
  </verify>
  <acceptance_criteria>
    - File extensions/pi-claude-marketplace/orchestrators/plugin/fetch.messaging.ts exists and exports FETCH_STATUSES, FetchStatus, FetchMsg, FETCH_CONTEXT.
    - FETCH_STATUSES tuple contains exactly: available, partially-available, unavailable, remote, skipped, failed (verify: `grep -c` the six literals in the tuple).
    - Typecheck passes with zero errors (FETCH_RENDER is total: a deleted arm would raise TS2741 — confirm by transiently removing one arm locally then restoring).
    - `grep -aE 'ICON_[A-Z]+ =|STATUS_TOKENS|PLUGIN_STATUSES|REASONS =' extensions/pi-claude-marketplace/orchestrators/plugin/fetch.messaging.ts` returns no NEW closed-set DEFINITIONS (the file only IMPORTS/CONSUMES existing members, never defines a new one).
    - No render arm re-implements a row-building helper; each arm calls a shared notify.ts helper (verify by reading the arm bodies).
  </acceptance_criteria>
  <done>fetch.messaging.ts compiles, exports a total FETCH_CONTEXT render map, and grows no closed set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render vocabulary → user terminal | Message text this file selects is rendered to the user; wrong token/reason would misreport plugin state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-01 | Tampering | fetch.messaging.ts closed-set members | low | mitigate | Render arms CONSUME existing STATUS_TOKENS/REASONS/ICON members only; completeness proofs + catalog-UAT (later plans) fail on any new member. No npm install in this plan. |
| T-81-02 | Information disclosure | (skipped)/failed row text | low | mitigate | Row helpers are the audited shared notify.ts builders; no credential or path interpolation happens in this file. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the extension (FETCH_RENDER totality enforced by the compiler).
- No new closed-set member defined (grep gate above).
</verification>

<success_criteria>
fetch.messaging.ts exists, typechecks, exports a total FETCH_CONTEXT, and introduces no new closed-set member. Ready for Plan 02 to import FETCH_CONTEXT.
</success_criteria>

<output>
Create `.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-01-SUMMARY.md` when done.
</output>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Phase 81, all plans)

Every symbol/file created or edited across the five plans, so the executor and downstream verification know the full surface.

### New source files
- `orchestrators/plugin/fetch.messaging.ts` (Plan 01) — exports `FETCH_STATUSES`, `type FetchStatus`, `type FetchMsg`, `FETCH_CONTEXT` (+ internal `FETCH_RENDER`).
- `orchestrators/plugin/fetch.ts` (Plan 02) — exports `fetchPlugins`, the `FetchTarget` union, and a test-only `cloneCacheSeam` override type. Internal: enumerate-fetchable-set, fetch-one core (pinned/unpinned arms), failure-tolerant sweep, `authMemo`.
- `edge/handlers/plugin/fetch.ts` (Plan 04) — exports `makeFetchHandler`; parses the three positional shapes → `FetchTarget`.

### Edited source files
- `orchestrators/plugin/info.ts` (Plan 03) — `GetPluginInfoOptions` gains `fetch?: boolean`; new try/catch fetch hook before the fs-only resolve (degrade-safe).
- `edge/handlers/plugin/info.ts` (Plan 03) — recognizes the `--fetch` boolean flag.
- `edge/router.ts` (Plan 04) — `SubcommandHandlers.fetch`, `"fetch"` in `TOP_LEVEL_SUBCOMMANDS`, fetch line in `TOP_LEVEL_USAGE`, `case "fetch"` in the switch.
- `edge/register.ts` (Plan 04) — `fetch: makeFetchHandler(pi)` in the handlers record.
- `edge/completions/provider.ts` (Plan 04) — `"fetch"` in `PluginRefMode`; `case "fetch"` branch (`allowMarketplaceOnly: true`).
- `edge/completions/data.ts` (Plan 04) — `FETCH_STATUSES` filter set `{remote, available, partially-available, unavailable}`.

### Docs
- `docs/output-catalog.md` (Plan 05) — `## /claude:plugin fetch` H2 section + byte-normative rows.
- `docs/messaging-style-guide.md` (Plan 05) — fetch referenced in the grammar summary.
- `README.md` (Plan 05) — fetch documented as a pi-only extension.

### Tests
- `tests/orchestrators/plugin/fetch.test.ts` NEW (Plan 02) — FTCH-01/02/04/06/07.
- `tests/orchestrators/plugin/info.test.ts` EDIT (Plan 03) — info --fetch success + failure-degrade + network-free control (FTCH-03).
- `tests/edge/handlers/plugin/fetch.test.ts` NEW (Plan 04) — 3-shape parse.
- `tests/edge/completions/provider.test.ts` EDIT (Plan 04) — fetch completion buckets.
- `tests/architecture/no-orchestrator-network.test.ts` EDIT (Plan 04) — `fetch.ts` added to `FORBIDDEN_TARGETS`.
- `tests/architecture/catalog-uat.test.ts` EDIT (Plan 05) — fetch catalog fixtures.
- `tests/orchestrators/plugin/clone-gc.test.ts` EDIT (Plan 05) — FTCH-05 self-heal regression.

### Flag / registration additions (closed-set discipline: NO new tokens, icons, or REASONS)
- Verb: `fetch` (three shapes) + `info --fetch` boolean flag.
- Reused reasons only: `up-to-date` (no-op), `network unreachable` / `authentication required` (failure degrade). No REASONS growth.
- No completion-cache schema change (stays schemaVersion 6).
</artifacts_this_phase_produces>
