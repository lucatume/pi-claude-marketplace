---
phase: 81-fetch-verb-info-fetch
plan: 04
type: execute
wave: 3
depends_on: ["81-02"]
files_modified:
  - extensions/pi-claude-marketplace/edge/router.ts
  - extensions/pi-claude-marketplace/edge/register.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/fetch.ts
  - extensions/pi-claude-marketplace/edge/completions/provider.ts
  - extensions/pi-claude-marketplace/edge/completions/data.ts
  - tests/edge/handlers/plugin/fetch.test.ts
  - tests/edge/completions/provider.test.ts
  - tests/architecture/no-orchestrator-network.test.ts
autonomous: true
requirements: [FTCH-01, FTCH-07]
must_haves:
  truths:
    - "/claude:plugin fetch is a registered subcommand: the router dispatches all three shapes to the fetch handler and TOP_LEVEL_USAGE lists fetch byte-parallel to update"
    - "The fetch edge handler parses the three positional shapes (<plugin>@<mp>, @<mp>, bare) into a FetchTarget union and rejects unknown flags"
    - "fetch <tab> offers {remote, available, partially-available, unavailable} git-source plugins; fetch @<tab> offers marketplace names"
    - "fetch.ts is added to FORBIDDEN_TARGETS in the no-orchestrator-network gate (install-style; exempt set stays exactly {update.ts})"
  artifacts:
    - "extensions/pi-claude-marketplace/edge/handlers/plugin/fetch.ts (thin-shim handler → FetchTarget → fetchPlugins)"
    - "edge/router.ts (SubcommandHandlers.fetch + TOP_LEVEL_SUBCOMMANDS + TOP_LEVEL_USAGE + switch case)"
    - "edge/register.ts (makeFetchHandler wired into the handlers record)"
    - "edge/completions/provider.ts + data.ts (fetch PluginRefMode + fetchable status filter)"
    - "tests/edge/handlers/plugin/fetch.test.ts + tests/edge/completions/provider.test.ts + updated no-orchestrator-network.test.ts"
  key_links:
    - "router 'fetch' case → handlers.fetch → makeFetchHandler → fetchPlugins (Plan 02)"
    - "completion provider 'fetch' branch has allowMarketplaceOnly: true (mirrors update) and data.ts FETCH_STATUSES filter offers the fetchable buckets"
    - "FORBIDDEN_TARGETS now includes orchestrators/plugin/fetch.ts, permanently locking its zero-git-surface"
---

<objective>
Wire the `fetch` verb into the edge: register it in the router (4 sites), wire the handler in register.ts, create the thin-shim handler that parses the three positional shapes into a `FetchTarget` and delegates to `fetchPlugins`, add the completion branch + fetchable status filter, and add `fetch.ts` to `FORBIDDEN_TARGETS` to permanently lock its zero-git-surface (install-style; exempt set stays exactly `{update.ts}`).

Purpose: This makes `/claude:plugin fetch` a real user-facing verb (FTCH-01) with all three shapes reachable and tab-completion (FTCH-07 surface). It also closes the architecture-gate decision: `fetch.ts` joins the guarded set so the gate stays honest.

Output: edited `router.ts`, `register.ts`, `completions/{provider,data}.ts`; new `edge/handlers/plugin/fetch.ts`; new `tests/edge/handlers/plugin/fetch.test.ts`; updated `tests/edge/completions/provider.test.ts` and `tests/architecture/no-orchestrator-network.test.ts`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/url-source/ROADMAP.md
@.planning/workstreams/url-source/STATE.md
@.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-CONTEXT.md
@.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-RESEARCH.md
@.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-PATTERNS.md
@.claude/rules/typescript-comments.md
@extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register fetch in the router + wire the handler + add FORBIDDEN_TARGETS gate</name>
  <files>extensions/pi-claude-marketplace/edge/router.ts, extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/fetch.ts, tests/architecture/no-orchestrator-network.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/router.ts (the update registration path — 4 sites: SubcommandHandlers interface ~line 30; TOP_LEVEL_SUBCOMMANDS tuple ~55-69; TOP_LEVEL_USAGE string ~87-100; routeClaudePlugin switch ~143-172)
    - extensions/pi-claude-marketplace/edge/register.ts (makeUpdateHandler import + handlers-record entry — mirror for makeFetchHandler)
    - extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts (positional parse ~30-79 + splitPluginMarketplaceRef + delegate — the shim shape for the new fetch handler)
    - extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts (parsePositionalsWithFlags allow-list arm ~50-71 — flag-scan idiom for --scope-only)
    - extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts (fetchPlugins signature + FetchTarget union produced by Plan 02 — the delegation target)
    - tests/architecture/no-orchestrator-network.test.ts (FORBIDDEN_TARGETS list ~56-61 and the exempt-set comment ~38/116 — add fetch.ts to FORBIDDEN_TARGETS, keep update.ts the only gitOps-exempt file)
  </read_first>
  <action>
    Router (edge/router.ts) — add fetch in the four update-parallel sites: (1) SubcommandHandlers interface gains `fetch: (args, ctx) => Promise<void>`; (2) TOP_LEVEL_SUBCOMMANDS tuple gains `"fetch"`; (3) TOP_LEVEL_USAGE gains a line byte-parallel to update's — `  fetch [<plugin>@<marketplace> | @<marketplace>] [--scope user|project]` (the resolved USAGE prose: byte-parallel to update; bare fetch = the no-positional case); (4) routeClaudePlugin switch gains `case "fetch": return handlers.fetch(rest, ctx)`.
    register.ts — import makeFetchHandler and add `fetch: makeFetchHandler(pi)` to the handlers record.
    Create edge/handlers/plugin/fetch.ts (thin shim): parse the three positional shapes (D-81-01) into a FetchTarget — `<plugin>@<marketplace>` (split via splitPluginMarketplaceRef → kind "plugin"), `@<marketplace>` (leading `@`, no plugin → kind "marketplace"), and no positional (bare → kind "all"). Follow info.ts's inline flag-rejection loop: accept only `--scope` (user|project), reject any other `--flag` via the usage-error path. Delegate to fetchPlugins with the parsed target + scope + ctx + credentialOps/deviceFlowHttp wiring exactly as the update handler does.
    Gate (tests/architecture/no-orchestrator-network.test.ts) — add `"extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts"` to FORBIDDEN_TARGETS. Do NOT add it to any exempt list; the exempt (gitOps-allowed) set stays exactly {update.ts}. Update the explanatory comment only if it enumerates the guarded set (keep it truthful; strip no requirement/decision IDs).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/architecture/no-orchestrator-network.test.ts 2>&1 | tail -8 && node --test tests/edge/handlers/plugin/fetch.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"fetch"' extensions/pi-claude-marketplace/edge/router.ts` shows fetch present in the subcommand tuple and switch; TOP_LEVEL_USAGE contains the fetch line byte-parallel to update.
    - register.ts handlers record contains `fetch: makeFetchHandler(pi)`.
    - edge/handlers/plugin/fetch.ts exists and maps all three positional shapes to the FetchTarget union, rejecting non-scope flags.
    - no-orchestrator-network.test.ts passes AND FORBIDDEN_TARGETS contains orchestrators/plugin/fetch.ts; the gitOps-exempt set is still exactly {update.ts} (verify: fetch.ts is NOT on any exempt list).
    - The fetch handler test (Task 2 file) drives the three shapes; the delegation to fetchPlugins is asserted.
  </acceptance_criteria>
  <done>fetch is a routed verb with a working handler and the gate locks fetch.ts's zero-git-surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fetch handler test (3 shapes) + completion branch + fetchable status filter</name>
  <files>tests/edge/handlers/plugin/fetch.test.ts, extensions/pi-claude-marketplace/edge/completions/provider.ts, extensions/pi-claude-marketplace/edge/completions/data.ts, tests/edge/completions/provider.test.ts</files>
  <read_first>
    - tests/edge/handlers/plugin/info.test.ts (or sibling handler tests) — the handler-test harness: how a handler is invoked with args + mock ctx and how the delegated orchestrator call is asserted (spy/stub)
    - extensions/pi-claude-marketplace/edge/completions/provider.ts (PluginRefMode ~181-188 + the update branch of pluginRefBranchConfig ~221-227 — the analog for the fetch branch with allowMarketplaceOnly: true)
    - extensions/pi-claude-marketplace/edge/completions/data.ts (INSTALL_STATUSES ~64 / PARTIAL_UPDATE_STATUSES ~87 filter sets — the analog for FETCH_STATUSES)
    - extensions/pi-claude-marketplace/shared/completion-cache.ts (PluginIndexRow.status union incl. remote, schemaVersion 6 — confirm the status literals; NO schema change this phase)
    - tests/edge/completions/provider.test.ts (existing completion-bucket assertions to mirror for fetch)
  </read_first>
  <behavior>
    - fetch <plugin>@<mp> handler → fetchPlugins called with kind "plugin"; @<mp> → kind "marketplace"; bare → kind "all".
    - fetch <tab> completion offers git-source plugins whose status is in {remote, available, partially-available, unavailable} (per D-81-03 option (a): pinned-warm is accepted-if-typed and simply no-ops, so it need not be excluded from the offer set).
    - fetch @<tab> completion offers marketplace names (allowMarketplaceOnly: true).
  </behavior>
  <action>
    Create tests/edge/handlers/plugin/fetch.test.ts covering the three-shape positional parse and the delegation to fetchPlugins (stub the orchestrator, assert the FetchTarget kind for each shape, assert non-scope flags are rejected).
    completions/provider.ts — add `"fetch"` to PluginRefMode and a `case "fetch":` in pluginRefBranchConfig returning `{ mode: "fetch", allowMarketplaceOnly: true, ...(explicitScope !== undefined && { targetScope: explicitScope }) }` (mirrors the update branch).
    completions/data.ts — add a `FETCH_STATUSES` filter set = `new Set(["remote", "available", "partially-available", "unavailable"])` typed as `ReadonlySet<PluginIndexRow["status"]>`, and wire the fetch mode to use it (parallel to INSTALL_STATUSES / PARTIAL_UPDATE_STATUSES). Per RESEARCH Open Question 1 / D-81-03: the completion cache status cannot distinguish pinned-warm from unpinned-warm, so option (a) offers warm buckets and pinned-warm no-ops if typed — document this in a short comment (concept only; keep decision ID D-81-03 as traceability, no phase/plan numbers). Do NOT change completion-cache schemaVersion (stays 6).
    Extend tests/edge/completions/provider.test.ts with the fetch <tab> (fetchable buckets) and fetch @<tab> (marketplace-only) assertions.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/edge/handlers/plugin/fetch.test.ts tests/edge/completions/provider.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/edge/handlers/plugin/fetch.test.ts passes: each of the three shapes maps to the correct FetchTarget kind and non-scope flags are rejected.
    - provider.ts PluginRefMode includes "fetch"; the fetch branch has allowMarketplaceOnly: true.
    - data.ts FETCH_STATUSES = {remote, available, partially-available, unavailable}; no other status leaks into the fetch offer.
    - `grep -c 'schemaVersion' extensions/pi-claude-marketplace/shared/completion-cache.ts` unchanged and the value is still 6 (no schema bump).
    - provider.test.ts fetch-completion cases pass (fetchable buckets for <tab>; marketplace names for @<tab>).
  </acceptance_criteria>
  <done>The fetch handler + completion buckets work across all three shapes with no completion-cache schema change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI args → edge fetch handler | untrusted positional ref + flags cross here |
| completion cache → offered plugin names | cache contents drive what tab-completion offers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-10 | Tampering | malformed positional/flag input to the fetch handler | medium | mitigate | Handler parses the closed set of three shapes + --scope only; every other flag routes to the existing usage-error path (no injection surface); ref validated via splitPluginMarketplaceRef. |
| T-81-11 | Elevation of privilege | fetch.ts acquiring a git surface later | high | mitigate | fetch.ts is now in FORBIDDEN_TARGETS; the no-orchestrator-network gate fails CI on any future gitOps import; the exempt set stays exactly {update.ts}. |
| T-81-SC | Tampering | npm/pip/cargo installs | high | accept | No package install in this phase (100% internal composition). No install task; no [ASSUMED]/[SUS] package. |
</threat_model>

<verification>
- `node --test tests/architecture/no-orchestrator-network.test.ts` green with fetch.ts locked in FORBIDDEN_TARGETS.
- fetch handler + completion tests green.
- completion-cache schemaVersion unchanged (still 6).
</verification>

<success_criteria>
/claude:plugin fetch is a fully wired, routed, tab-completed verb across all three shapes; fetch.ts's zero-git-surface is permanently gate-locked.
</success_criteria>

<output>
Create `.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-04-SUMMARY.md` when done.
</output>
