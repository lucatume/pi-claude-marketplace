---
phase: 81-fetch-verb-info-fetch
plan: 05
type: execute
wave: 3
depends_on: ["81-02"]
files_modified:
  - docs/output-catalog.md
  - docs/messaging-style-guide.md
  - README.md
  - tests/architecture/catalog-uat.test.ts
  - tests/orchestrators/plugin/clone-gc.test.ts
autonomous: true
requirements: [FTCH-01, FTCH-05]
must_haves:
  truths:
    - "docs/output-catalog.md has a new ## /claude:plugin fetch H2 section with byte-normative catalog-state rows for the fetch verb (success rows, (skipped) no-op row, bulk headers/summary)"
    - "The catalog-UAT test locks the new fetch rows byte-for-byte (one fixture per (fetch section, catalog-state) tuple)"
    - "fetch is documented as a pi-only extension (upstream /plugin has no fetch verb) in the repo's command-surface docs (README + catalog/style-guide prose)"
    - "A fetched-but-uninstalled clone is swept by garbageCollectPluginClones and the plugin's next probe returns (remote) — self-heal, no persisted state, no GC code change"
  artifacts:
    - "docs/output-catalog.md (## /claude:plugin fetch section)"
    - "docs/messaging-style-guide.md (fetch referenced in the grammar summary)"
    - "README.md (fetch documented as a pi-only extension)"
    - "tests/architecture/catalog-uat.test.ts (fetch fixtures)"
    - "tests/orchestrators/plugin/clone-gc.test.ts (FTCH-05 fetched-uninstalled sweep regression)"
  key_links:
    - "the catalog byte forms match exactly what fetch.messaging.ts / notify.ts render (drift-guarded by catalog-uat.test.ts)"
    - "the GC regression proves derive-not-persist self-heal: fetch → no install → GC → next probeManifestEntry returns remote"
---

<objective>
Land the fetch verb's documentation lockstep and prove the FTCH-05 self-heal property. Add the `## /claude:plugin fetch` catalog section with byte-normative rows, lock them with catalog-UAT fixtures, reference fetch in the messaging style guide, document fetch as a pi-only extension in the README, and add the fetched-uninstalled GC sweep regression test (verify-don't-build — GC already handles it).

Purpose: A new verb requires its own catalog rows and byte-locking (closed-set lockstep discipline); FTCH-01 requires documenting fetch as a pi-only extension (upstream `/plugin` has no fetch verb, verified 2026-07-13); FTCH-05 is a verify-don't-build property that needs a regression test, not code.

Output: edited `docs/output-catalog.md`, `docs/messaging-style-guide.md`, `README.md`; extended `tests/architecture/catalog-uat.test.ts` and `tests/orchestrators/plugin/clone-gc.test.ts`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/url-source/ROADMAP.md
@.planning/workstreams/url-source/STATE.md
@.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-CONTEXT.md
@.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-RESEARCH.md
@.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-PATTERNS.md
@.claude/rules/typescript-comments.md
@extensions/pi-claude-marketplace/orchestrators/plugin/fetch.messaging.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: FTCH-05 GC self-heal regression test (verify-don't-build)</name>
  <files>tests/orchestrators/plugin/clone-gc.test.ts</files>
  <read_first>
    - tests/orchestrators/plugin/clone-gc.test.ts (existing sweep regression structure — how it stands up a clone, runs garbageCollectPluginClones, and asserts the dir is/ isn't swept)
    - extensions/pi-claude-marketplace/orchestrators/plugin/clone-gc.ts (garbageCollectPluginClones / deriveLiveCloneKeys — live keys derive from installed records only; a fetched-uninstalled clone has no record → not live → swept. CONFIRM no code change is needed)
    - extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts (probeManifestEntry — asserts the post-sweep status returns remote)
    - tests/helpers (git/fixture mocks used by the existing gc test)
  </read_first>
  <action>
    Add a regression test to tests/orchestrators/plugin/clone-gc.test.ts proving the FTCH-05 self-heal: materialize a git-source plugin clone/mirror WITHOUT installing it (no state record), run garbageCollectPluginClones, assert the clone directory is gone, and assert the next probeManifestEntry for that entry returns `remote`. This is verify-don't-build — do NOT change clone-gc.ts. If the test reveals GC does NOT sweep a fetched-uninstalled clone, STOP and surface it (that would be a real defect, not a plan task). Keep the test network-free (inject mocks, materialize the fixture clone dir directly if the existing test helpers support it).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/orchestrators/plugin/clone-gc.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - tests/orchestrators/plugin/clone-gc.test.ts gains a fetched-uninstalled-sweep case that passes.
    - The test asserts: (a) the fetched clone dir exists before GC, (b) is gone after garbageCollectPluginClones, (c) the next probeManifestEntry returns `remote` (self-heal).
    - No change to extensions/pi-claude-marketplace/orchestrators/plugin/clone-gc.ts (verify: `git diff --stat` shows clone-gc.ts unchanged).
  </acceptance_criteria>
  <done>FTCH-05 self-heal is proven by a passing regression test with no GC code change.</done>
</task>

<task type="auto">
  <name>Task 2: fetch catalog section + catalog-UAT fixtures + style-guide + README pi-only note</name>
  <files>docs/output-catalog.md, docs/messaging-style-guide.md, README.md, tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - docs/output-catalog.md (the `## /claude:plugin update` H2 section ~790-861 as the structural template + the remote rows ~345-368 for the row grammar; match catalog prose style)
    - docs/messaging-style-guide.md (the verb grammar summary structure — where to reference fetch; note v2.0 retired the closed-set frontmatter, so add prose only, no new frontmatter)
    - tests/architecture/catalog-uat.test.ts (the fixture-keying scheme — how each (verb section, catalog-state) tuple maps to a byte-locked expected row; add one fixture per fetch catalog-state)
    - extensions/pi-claude-marketplace/orchestrators/plugin/fetch.messaging.ts (FETCH_CONTEXT / the exact status set + skipped/up-to-date row form the catalog rows must match byte-for-byte)
    - README.md (the command-surface section that lists /claude:plugin verbs — where to document fetch as a pi-only extension)
    - .planning/workstreams/url-source/REQUIREMENTS.md (FTCH-01: pi-only extension wording; "Upstream /plugin fetch parity claim — out of scope")
  </read_first>
  <action>
    docs/output-catalog.md — add a new `## /claude:plugin fetch` H2 section modeled on the update section: byte-normative catalog-state rows for the fetch verb — the post-fetch success rows (available / partially-available / unavailable derived rows), the `(skipped) {up-to-date}` no-op row, and the bulk grammar (marketplace headers, per-plugin rows, summary line prepended to error/warning cascades, tally). The rows MUST match exactly what fetch.messaging.ts / notify.ts render. Include a one-line note that fetch is a pi-only extension (upstream `/plugin` has no fetch verb).
    tests/architecture/catalog-uat.test.ts — add one fixture per (fetch section, catalog-state) tuple, byte-locking the new rows against the catalog (mirror how update's fixtures are keyed).
    docs/messaging-style-guide.md — reference fetch in the verb grammar summary (prose only, no new closed-set frontmatter — v2.0 retired it).
    README.md — document fetch under the /claude:plugin command surface as a pi-only extension (upstream /plugin has no fetch verb, verified 2026-07-13) alongside install/update/uninstall (FTCH-01).
    Keep decision/requirement IDs where used for traceability; strip no phase/plan/wave numbers into docs. Introduce NO new status token, icon, or REASONS member in any catalog row.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/architecture/catalog-uat.test.ts 2>&1 | tail -12 && echo "---PIONLY (expect >=1 each)---" && grep -ic 'pi-only\|pi only' docs/output-catalog.md README.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/output-catalog.md has a `## /claude:plugin fetch` H2 with byte-normative rows: post-fetch success rows, `(skipped) {up-to-date}` no-op, and bulk headers/summary/tally.
    - tests/architecture/catalog-uat.test.ts passes with one fixture per (fetch section, catalog-state) tuple byte-locking the new rows.
    - README.md and docs/output-catalog.md each document fetch as a pi-only extension (grep for "pi-only"/"pi only" returns >= 1 in each).
    - docs/messaging-style-guide.md references fetch in the grammar summary (prose only; `git diff` shows no new frontmatter closed-set list).
    - No new status token / icon / REASONS member appears in any catalog row (verify: the fetch rows use only existing tokens — `(available)`/`(partially-available)`/`(unavailable)`/`(skipped)` and the `up-to-date` reason).
  </acceptance_criteria>
  <done>The fetch catalog section, its byte-locked fixtures, the style-guide reference, and the README pi-only note all land in one green commit with no closed-set growth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog byte forms → user-visible output contract | catalog rows are the normative spec the renderer must match; drift misreports state |
| GC live-key derivation → clone reclamation | which clones are swept is derived from installed records, not persisted fetch state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-12 | Tampering | catalog rows drifting from the renderer | low | mitigate | catalog-UAT fixtures byte-lock the fetch rows against the actual render; the drift-guard fails CI on any divergence. |
| T-81-13 | Repudiation | fetched-uninstalled clone leaking as persisted state | low | mitigate | FTCH-05 regression proves derive-not-persist: GC sweeps the unreferenced clone and the status self-heals to remote; no fetch registry exists. |
| T-81-SC | Tampering | npm/pip/cargo installs | high | accept | No package install in this phase (100% internal composition). No install task; no [ASSUMED]/[SUS] package. |
</threat_model>

<verification>
- `node --test tests/architecture/catalog-uat.test.ts` green with fetch fixtures.
- `node --test tests/orchestrators/plugin/clone-gc.test.ts` green with the FTCH-05 self-heal regression.
- fetch documented pi-only in README + catalog; no closed-set growth in any catalog row.
</verification>

<success_criteria>
The fetch verb is fully documented (catalog + style-guide + README pi-only note) with byte-locked catalog-UAT fixtures, and FTCH-05 self-heal is proven by a passing regression — all with no closed-set growth and no GC code change.
</success_criteria>

<output>
Create `.planning/workstreams/url-source/phases/81-fetch-verb-info-fetch/81-05-SUMMARY.md` when done.
</output>
