---
phase: 79-provider-auth-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/domain/auth-registry.ts
  - extensions/pi-claude-marketplace/domain/github-auth.ts
  - tests/domain/auth-registry.test.ts
  - tests/architecture/no-credential-leak.test.ts
autonomous: true
requirements:
  - PROV-01
  - PROV-05
must_haves:
  truths:
    - "The three existing auth tests (tests/domain/github-auth.test.ts, tests/shared/device-flow-prompt.test.ts, tests/integration/auth-e2e.test.ts) pass UNCHANGED — byte-identical github.com behavior (success criterion 1)."
    - "findProviderForHost('github.com') returns the GitHub descriptor; findProviderForHost('example.com') returns undefined."
    - "initiateDeviceFlow with no provider field behaves identically to today (defaults to GITHUB_PROVIDER); with a mock provider it drives the engine off the descriptor's endpoints/clientId/scope/credentialFrom."
    - "The no-credential-leak gate scans domain/auth-registry.ts and catches a token interpolation there."
  artifacts:
    - "extensions/pi-claude-marketplace/domain/auth-registry.ts (GitAuthProvider interface, GITHUB_PROVIDER const, findProviderForHost)"
    - "tests/domain/auth-registry.test.ts (PROV-01 lookup + descriptor shape)"
  key_links:
    - "github-auth.ts engine reads endpoints/clientId/scope/credentialFrom from opts.provider (defaulting to GITHUB_PROVIDER) instead of module-level constants."
    - "no-credential-leak.test.ts PROVIDER_FILES array includes auth-registry.ts and github-auth.ts; the errorOrNotifyWithToken regex applies to each."
---

<objective>
Create the `GitAuthProvider` registry (D-79-04) and parameterize the existing
RFC-8628 Device Flow engine by a provider descriptor, WITHOUT changing github.com
behavior. The GitHub descriptor supplies today's exact literals; `provider`
defaults to `GITHUB_PROVIDER` so the three existing auth tests compile and pass
UNCHANGED (byte-identity proof, success criterion 1). Extend the no-credential-leak
architecture gate to cover every provider file (PROV-05).

Purpose: this is the domain foundation. Plans 02 and 03 look up providers by host
and bind the parameterized engine into clone auth bundles.

Output: `domain/auth-registry.ts`, a parameterized `domain/github-auth.ts`, a new
`tests/domain/auth-registry.test.ts`, and an extended
`tests/architecture/no-credential-leak.test.ts`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/url-source/phases/79-provider-auth-registry/79-CONTEXT.md
@.planning/workstreams/url-source/phases/79-provider-auth-registry/79-RESEARCH.md
@.planning/workstreams/url-source/phases/79-provider-auth-registry/79-PATTERNS.md
@.claude/rules/typescript-comments.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates or changes (consumed by Plans 02 and 03):

- `domain/auth-registry.ts`:
  - `interface GitAuthProvider` — `{ id: string; hostMatch(host: string): boolean; deviceCodeUrl: string; tokenUrl: string; clientId: string; scope: string; credentialFrom(accessToken: string): GitCredentials }`
  - `const GITHUB_PROVIDER: GitAuthProvider` — today's exact literals
  - `function findProviderForHost(host: string): GitAuthProvider | undefined`
- `domain/github-auth.ts`:
  - `InitiateDeviceFlowOpts` gains an OPTIONAL `provider?: GitAuthProvider` field (defaults to `GITHUB_PROVIDER`)
  - `DeviceFlowHttp.requestCode` / `.pollToken` continue to accept `clientId` / `scope` params sourced from the descriptor (mock signature unchanged)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the GitAuthProvider registry (auth-registry.ts) with a failing-first test</name>
  <files>extensions/pi-claude-marketplace/domain/auth-registry.ts, tests/domain/auth-registry.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/domain/github-auth.ts (lines 136-147: the 4 github-specific constants GITHUB_DEVICE_CODE_URL, GITHUB_ACCESS_TOKEN_URL, GITHUB_OAUTH_CLIENT_ID, REQUESTED_SCOPE; line 315: the hardcoded credential mapping)
    - extensions/pi-claude-marketplace/domain/source.ts (const-table + pure-lookup-function analog; head to see the module shape)
    - extensions/pi-claude-marketplace/platform/git.ts (lines 305-311: GitCredentials type — imported as `type` by github-auth.ts:47)
    - extensions/pi-claude-marketplace/domain/README.md (domain tier import discipline: shared/ only, no fs/network/ctx)
    - .claude/rules/typescript-comments.md (comment policy: decision/requirement IDs only, no phase/plan/wave refs)
  </read_first>
  <behavior>
    - Test 1: findProviderForHost("github.com") returns an object whose id === "github".
    - Test 2: findProviderForHost("example.com") returns undefined.
    - Test 3: findProviderForHost("gitlab.com") returns undefined (no GitLab descriptor in v1; PROV-06 deferred).
    - Test 4: GITHUB_PROVIDER.credentialFrom("tok123") deep-equals { username: "x-access-token", password: "tok123" }.
    - Test 5: GITHUB_PROVIDER carries deviceCodeUrl === "https://github.com/login/device/code", tokenUrl === "https://github.com/login/oauth/access_token", clientId === "Ov23liNcyK08uGdU0mMl", scope === "repo".
  </behavior>
  <action>
    Create domain/auth-registry.ts. Declare `interface GitAuthProvider` with the exact fields listed in artifacts_this_phase_produces per D-79-04 (id, hostMatch predicate, deviceCodeUrl, tokenUrl, clientId, scope, credentialFrom). Import `GitCredentials` as a type from ../platform/git.ts (mirror github-auth.ts:47 — type-only, honors the domain->platform type-import ledge). Declare `const GITHUB_PROVIDER: GitAuthProvider` supplying the EXACT literals currently in github-auth.ts: deviceCodeUrl and tokenUrl from lines 136-137, clientId from line 144, scope from line 147 ("repo"), hostMatch as `(host) => host === "github.com"`, and credentialFrom as `(accessToken) => ({ username: "x-access-token", password: accessToken })` (the mapping currently hardcoded at github-auth.ts:315). Declare a module-private `const PROVIDERS: readonly GitAuthProvider[] = [GITHUB_PROVIDER]` and export `function findProviderForHost(host: string): GitAuthProvider | undefined` returning `PROVIDERS.find((p) => p.hostMatch(host))`. Anchor comments to D-79-04 / PROV-01 / D-32-03 (public client_id) only — no phase/plan/wave references. Do NOT interpolate any credential field into an Error/notify (AUTH-09; the gate in Task 3 will scan this file). Create tests/domain/auth-registry.test.ts FIRST asserting the five behaviors above; run it and confirm it FAILS before auth-registry.ts exists, then make it pass.
  </action>
  <verify>
    <automated>node --test "tests/domain/auth-registry.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test "tests/domain/auth-registry.test.ts"` passes (5 tests green).
    - `grep -c "export interface GitAuthProvider" extensions/pi-claude-marketplace/domain/auth-registry.ts` returns 1.
    - `grep -c "export function findProviderForHost" extensions/pi-claude-marketplace/domain/auth-registry.ts` returns 1.
    - `grep -c "Ov23liNcyK08uGdU0mMl" extensions/pi-claude-marketplace/domain/auth-registry.ts` returns 1 (byte-identical client_id).
    - auth-registry.ts contains no import from orchestrators/ or persistence/ (domain tier discipline): `grep -E "from \"\.\./(orchestrators|persistence)" extensions/pi-claude-marketplace/domain/auth-registry.ts` returns nothing.
  </acceptance_criteria>
  <done>The registry exists with the GitHub descriptor carrying today's exact literals; lookup returns github for github.com and undefined otherwise.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parameterize the github-auth.ts engine by an optional provider descriptor (byte-identical default)</name>
  <files>extensions/pi-claude-marketplace/domain/github-auth.ts, tests/domain/auth-registry.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/domain/github-auth.ts (WHOLE FILE — the engine; note lines 114-125 InitiateDeviceFlowOpts, 149-178 requestCodeImpl, 180-254 pollTokenImpl, 256-259 DEFAULT_DEVICE_FLOW_HTTP, 277-290 safePollToken, 292-358 runPollLoop:315 credential mapping, 360-379 initiateDeviceFlow:365)
    - extensions/pi-claude-marketplace/domain/auth-registry.ts (the descriptor created in Task 1 — the source of provider.deviceCodeUrl/tokenUrl/clientId/scope/credentialFrom)
    - tests/domain/github-auth.test.ts (23 initiateDeviceFlow call shapes — NONE pass a `provider` key; these MUST stay unchanged)
    - tests/shared/device-flow-prompt.test.ts (initiateDeviceFlow call shape — no provider key)
    - tests/integration/auth-e2e.test.ts (initiateDeviceFlow call shape — no provider key)
    - tests/helpers/device-flow-mock.ts (requestCode(clientId, scope) / pollToken(clientId, deviceCode, intervalSec) mock signature — asserts the passed-through clientId/scope; must NOT change)
  </read_first>
  <behavior>
    - Test 1 (byte-identity, existing suite): all of tests/domain/github-auth.test.ts passes with ZERO edits after the refactor.
    - Test 2 (byte-identity, existing): tests/shared/device-flow-prompt.test.ts passes unchanged.
    - Test 3 (byte-identity, existing): tests/integration/auth-e2e.test.ts passes unchanged.
    - Test 4 (parity, NEW in tests/domain/auth-registry.test.ts): initiateDeviceFlow called WITH `provider: GITHUB_PROVIDER` explicitly and WITHOUT provider produce identical requestCodeCalls[0].clientId ("Ov23liNcyK08uGdU0mMl") and .scope ("repo") on the mock http.
    - Test 5 (descriptor drive, NEW): initiateDeviceFlow with a synthetic mock provider (clientId "mock-id", scope "read", credentialFrom returning username "oauth2") drives the mock http with clientId "mock-id"/scope "read" and, on success, approves a credential with username "oauth2".
  </behavior>
  <action>
    Add an OPTIONAL `provider?: GitAuthProvider` field to `InitiateDeviceFlowOpts` (import GitAuthProvider as a type from ./auth-registry.ts). At the TOP of `initiateDeviceFlow`, resolve `const provider = opts.provider ?? GITHUB_PROVIDER` (import GITHUB_PROVIDER from ./auth-registry.ts). CRITICAL: `provider` is optional and defaults to GITHUB_PROVIDER so the 23 existing test calls (which pass no provider key) compile and behave byte-identically — this is the byte-identity mechanism, do not make provider required. Thread `provider.clientId` and `provider.scope` into the requestCode call (github-auth.ts:365 currently passes GITHUB_OAUTH_CLIENT_ID + REQUESTED_SCOPE) and into safePollToken (line 283 currently passes GITHUB_OAUTH_CLIENT_ID). Parameterize the two fetch URLs: requestCodeImpl (line 151 uses GITHUB_DEVICE_CODE_URL) and pollTokenImpl (line 193 uses GITHUB_ACCESS_TOKEN_URL) must receive their URL from the descriptor — pass the URL as a new leading arg through the DeviceFlowHttp seam OR bind it via the provider inside initiateDeviceFlow/runPollLoop; keep the DeviceFlowHttp.requestCode/pollToken PUBLIC signature (clientId, scope) / (clientId, deviceCode, intervalSec) UNCHANGED so tests/helpers/device-flow-mock.ts needs no edit — pass the descriptor URL into the DEFAULT_DEVICE_FLOW_HTTP impls via a closure or a private module binding set from the provider, not by widening the mock's public method signature. In runPollLoop's success arm (line 315) replace the hardcoded `{ username: "x-access-token", password: r.accessToken }` with `provider.credentialFrom(r.accessToken)` (thread `provider` into runPollLoop via opts or an explicit param). The GITHUB_DEVICE_CODE_URL/GITHUB_ACCESS_TOKEN_URL/GITHUB_OAUTH_CLIENT_ID/REQUESTED_SCOPE module constants MAY remain as the GITHUB_PROVIDER field values' source, or be removed if the descriptor fully replaces their reads — either is acceptable so long as the default path stays byte-identical. Preserve EVERY notify string, deadline math, slow_down cumulative +5, pending no-mutate, and access_denied/expired_token message VERBATIM (Pitfall: byte-identity regression). Do NOT edit the three existing byte-identity test files. Add the two NEW parity/descriptor-drive tests to tests/domain/auth-registry.test.ts. Keep AUTH-09 discipline: never interpolate access_token/cred.*/r.accessToken into any Error/notifyFn.
  </action>
  <verify>
    <automated>node --test "tests/domain/github-auth.test.ts" "tests/shared/device-flow-prompt.test.ts" "tests/integration/auth-e2e.test.ts" "tests/domain/auth-registry.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - All four test files pass green.
    - The three existing byte-identity test files are UNMODIFIED: `git diff --name-only tests/domain/github-auth.test.ts tests/shared/device-flow-prompt.test.ts tests/integration/auth-e2e.test.ts` returns nothing.
    - `provider` is optional on the opts interface: `grep -c "provider?: GitAuthProvider" extensions/pi-claude-marketplace/domain/github-auth.ts` returns 1.
    - The hardcoded credential mapping is gone from runPollLoop: after stripping comments, `x-access-token` appears in github-auth.ts only inside the credentialFrom path if at all — verify the success arm uses the descriptor: `grep -c "provider.credentialFrom" extensions/pi-claude-marketplace/domain/github-auth.ts` returns at least 1.
    - `npm run typecheck` passes (exactOptionalPropertyTypes-clean).
  </acceptance_criteria>
  <done>The engine reads endpoints/clientId/scope/credentialFrom from the descriptor (defaulting to GITHUB_PROVIDER); github.com output is byte-identical and the three existing auth tests pass without edits.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the no-credential-leak architecture gate to cover every provider file (PROV-05)</name>
  <files>tests/architecture/no-credential-leak.test.ts</files>
  <read_first>
    - tests/architecture/no-credential-leak.test.ts (WHOLE FILE — file-constant arrays at lines 45-52, stripComments at 54-56, the GITHUB_AUTH_FILE gate test at 104-135 with the errorOrNotifyWithToken regex at 128-129)
    - extensions/pi-claude-marketplace/domain/auth-registry.ts (the new provider file that must join the scanned list)
    - .claude/rules/typescript-comments.md (test-title policy: no phase/plan/wave in describe/test titles)
  </read_first>
  <action>
    Add domain/auth-registry.ts to the no-credential-leak gate's scanned file set. Follow the existing GITHUB_AUTH_FILE test shape (lines 104-135): introduce a PROVIDER_FILES readonly array containing the auth-registry.ts path (and, if github-auth.ts was renamed in Task 2, update GITHUB_AUTH_FILE — it was NOT renamed per this plan, so leave it), and add a test that loops PROVIDER_FILES, reads each file, stripComments, and asserts the same errorOrNotifyWithToken regex (line 128-129: matches new Error(...) or notifyFn(...) interpolating access_token/accessToken/cred.<field>/r.accessToken) returns false. Keep the vacuous-pass-when-file-absent guard (access().then) so a not-yet-authored file does not fail the gate. Title the test with the PROV-05 requirement ID (e.g. "PROV-05: every provider file is scanned for token interpolation") — no phase/plan/wave references. Add an INTENTIONAL-FAILURE self-check is NOT required; instead prove coverage by grep in acceptance.
  </action>
  <verify>
    <automated>node --test "tests/architecture/no-credential-leak.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test "tests/architecture/no-credential-leak.test.ts"` passes.
    - auth-registry.ts is in the gate's scanned set: `grep -c "domain/auth-registry.ts" tests/architecture/no-credential-leak.test.ts` returns at least 1.
    - The new test cites PROV-05: `grep -c "PROV-05" tests/architecture/no-credential-leak.test.ts` returns at least 1.
    - The gate still passes for github-auth.ts (existing GITHUB_AUTH_FILE test unbroken): the file still references GITHUB_AUTH_FILE — `grep -c "GITHUB_AUTH_FILE" tests/architecture/no-credential-leak.test.ts` returns at least 2 (declaration + use).
  </acceptance_criteria>
  <done>The no-credential-leak gate scans domain/auth-registry.ts (and github-auth.ts) for token interpolation; PROV-05 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device Flow HTTP (fetch → provider endpoints) | Untrusted OAuth token-endpoint responses cross into the engine; validated by requestCodeImpl field checks before use. |
| In-code descriptor → engine | Provider descriptors are COMPILE-TIME constants (D-79-04); no user-editable/injection surface in v1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-01 | Information Disclosure | domain/auth-registry.ts, domain/github-auth.ts | high | mitigate | AUTH-09: never interpolate access_token/cred.*/r.accessToken into Error/notify; enforced by the extended no-credential-leak gate (Task 3, PROV-05). |
| T-79-02 | Spoofing | provider descriptor endpoints | low | accept | Descriptors are compile-time constants; no runtime provider config in v1 (D-79-04). PROV-07 (per-source declarations) deferred to v2. |
| T-79-03 | Tampering | github.com behavior drift during refactor | high | mitigate | The GITHUB_PROVIDER descriptor supplies today's exact literals; the three existing auth tests must pass UNCHANGED (byte-identity gate, Task 2). |
| T-79-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs NO external packages (RESEARCH: Package Legitimacy Audit not applicable); no [ASSUMED]/[SUS] packages. |
</threat_model>

<verification>
- `node --test "tests/domain/auth-registry.test.ts" "tests/domain/github-auth.test.ts" "tests/shared/device-flow-prompt.test.ts" "tests/integration/auth-e2e.test.ts" "tests/architecture/no-credential-leak.test.ts"` all green.
- The three existing byte-identity test files are byte-for-byte unmodified.
- `npm run typecheck` passes.
</verification>

<success_criteria>
- GitAuthProvider registry exists with findProviderForHost (PROV-01).
- github.com Device Flow is byte-identical — three existing auth tests pass unchanged (success criterion 1).
- no-credential-leak gate covers auth-registry.ts (PROV-05).
</success_criteria>

<output>
Create `.planning/workstreams/url-source/phases/79-provider-auth-registry/79-01-SUMMARY.md` when done.
</output>
