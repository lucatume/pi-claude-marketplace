---
phase: 78-plugin-git-source-lifecycle
plan: 04
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - tests/orchestrators/plugin/uninstall.test.ts
autonomous: true
requirements: [PURL-05]
must_haves:
  truths:
    - "Uninstalling the LAST plugin referencing a url+sha clone deletes that plugin-clones/<key> dir"
    - "Uninstalling ONE of two plugins that share a url+sha clone leaves the clone on disk (still referenced)"
    - "GC runs POST-state-commit (after the uninstall transaction saves) so a GC failure cannot strand state in installed=true"
    - "A GC leak (rm failure) does NOT fail the user-visible uninstall — the uninstall still reports success"
  artifacts:
    - "A garbageCollectPluginClones(locations) call in uninstall.ts beside the existing post-commit rm(pluginDataDir) cleanup"
  key_links:
    - "uninstall.ts imports garbageCollectPluginClones from ./clone-gc (fs-only) — uninstall.ts stays git-clean (no gitOps token added)"
    - "GC is invoked AFTER withLockedStateTransaction commits, inside the D-19-01 swallow discipline"
---

<objective>
Wire the Plan-01 GC helper into `uninstall` so removing a git-source plugin garbage-collects its
cached clone once no surviving record references it (PURL-05). GC runs at the exact
POST-state-commit spot where uninstall already drops the per-plugin data dir (uninstall.ts:596-610),
inside the same D-19-01 silent-swallow discipline.

Purpose: PURL-05 — uninstall must reclaim a clone when its last referencer is removed, but must
leave a shared clone intact while another installed plugin still references it. The derive-live-keys
logic (D-78-01) lives entirely in garbageCollectPluginClones (Plan 01); this plan only calls it at
the right place with the right ordering.

Output: one GC call in uninstall.ts + extended uninstall.test.ts.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/url-source/phases/78-plugin-git-source-lifecycle/78-CONTEXT.md
@.planning/workstreams/url-source/phases/78-plugin-git-source-lifecycle/78-RESEARCH.md
@.planning/workstreams/url-source/phases/78-plugin-git-source-lifecycle/78-PATTERNS.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend uninstall.test.ts with last-referencer-GC and shared-clone-survives cases</name>
  <files>tests/orchestrators/plugin/uninstall.test.ts</files>
  <read_first>
    - tests/orchestrators/plugin/uninstall.test.ts (how an installed git-source record + its on-disk plugin-clones/<key> dir is set up; how uninstall is invoked and the success message asserted)
    - tests/orchestrators/plugin/clone-gc.test.ts (Plan 01 — the fixture pattern for creating plugin-clones/<key> dirs and multi-record state)
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts lines 588-611 (the post-commit cleanup region where GC will be added)
  </read_first>
  <action>
    Extend tests/orchestrators/plugin/uninstall.test.ts:
    (1) last-referencer-GC: a single installed git-source plugin (record has resolvedSha; its clone dir
        exists on disk) -> after uninstall, the plugin-clones/<key> dir is GONE.
    (2) shared-clone-survives: two installed git-source plugins whose records resolve to the SAME clone key
        -> uninstall ONE -> the shared plugin-clones/<key> dir still EXISTS; uninstall the SECOND -> the dir
        is GONE.
    (3) GC-leak-non-fatal: simulate a GC rm failure (e.g. make the clone dir un-removable, or stub the helper
        boundary as the test harness allows) -> uninstall still reports success (the leak is swallowed).
    (4) POST-commit ordering: assert the state record is deleted (state.json no longer has the plugin) even
        in the leak case — GC never rolls back the committed uninstall.
    Titles use plain behavioral prose. Cases (1)-(2) MUST fail before the uninstall.ts edit (RED) because no
    GC is wired yet.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test "tests/orchestrators/plugin/uninstall.test.ts" 2>&1 | grep -qE "not ok|# fail" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - uninstall.test.ts gains last-referencer-GC, shared-clone-survives, GC-leak-non-fatal, and post-commit-ordering cases
    - Cases (1)-(2) fail (RED) before the uninstall.ts edit
    - No test title contains Phase/Plan/Wave/Pitfall-N tokens
  </acceptance_criteria>
  <done>uninstall.test.ts carries the four GC cases and the referencer cases are RED.</done>
</task>

<task type="auto">
  <name>Task 2: Call garbageCollectPluginClones after the uninstall state commit</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts lines 596-611 (post-state-commit rm(pluginDataDir) + D-19-01 swallow — the insertion point; confirm `locations` is in scope here)
    - extensions/pi-claude-marketplace/orchestrators/plugin/clone-gc.ts (Plan 01 — garbageCollectPluginClones signature)
    - tests/architecture/no-orchestrator-network.test.ts (uninstall.ts is NOT in FORBIDDEN_TARGETS but must stay git-clean; the GC helper is fs-only so this holds)
  </read_first>
  <action>
    In uninstall.ts, import garbageCollectPluginClones from "./clone-gc". After the withLockedStateTransaction
    commit — beside the existing post-commit rm(pluginDataDir) cleanup (uninstall.ts:605-610) — call
    `await garbageCollectPluginClones(locations)` inside a try/catch that swallows any thrown error (D-19-01:
    hygienic cleanup never becomes the primary user-facing path; the helper already returns leak strings rather
    than throwing, but the try/catch is belt-and-braces). Place the call so it runs ONLY after the state save
    succeeded (a still-installed record must keep its clone). Do NOT reorder or gate the existing pluginDataDir
    cleanup. Tag with PURL-05 / D-78-01 / NFR-3 anchors; NO Phase/Plan/Wave tokens. Add no gitOps token —
    garbageCollectPluginClones is fs-only, keeping uninstall.ts git-clean.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test "tests/orchestrators/plugin/uninstall.test.ts" "tests/architecture/no-orchestrator-network.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - Uninstalling the last referencer deletes its clone dir; uninstalling one of two sharers leaves the shared dir (uninstall.test.ts green)
    - The GC call runs AFTER the state transaction commits (post-commit ordering test green)
    - A GC leak does not fail the uninstall (leak-non-fatal test green)
    - uninstall.ts imports only the fs-only garbageCollectPluginClones from ./clone-gc — no new git import in executable code (clone-gc.ts's fs-only-ness is gated in Plan 01). Do NOT gate on a raw source grep of uninstall.ts (a legal comment would false-positive); no-orchestrator-network.test.ts staying green covers the imported surface
  </acceptance_criteria>
  <done>uninstall calls garbageCollectPluginClones post-commit; all uninstall + architecture tests pass; uninstall stays git-clean.</done>
</task>

</tasks>

<verification>
- `node --test "tests/orchestrators/plugin/uninstall.test.ts" "tests/architecture/no-orchestrator-network.test.ts"` fully green.
- uninstall.ts adds only the fs-only garbageCollectPluginClones import; no new git import in executable code (clone-gc.ts's fs-only-ness is gated in Plan 01). A raw comment-inclusive grep is not the gate.
- `cd extensions/pi-claude-marketplace && npm run typecheck` clean.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uninstall state commit → GC | GC must run only after the committed state truthfully reflects the removed record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-08 | Availability | GC-before-commit strands a live clone | high | mitigate | GC is invoked strictly AFTER withLockedStateTransaction commits (D-78-01); a crash before GC leaves an orphan the next pass removes (idempotent) |
| T-78-09 | Availability | GC failure fails the user-visible uninstall | medium | mitigate | GC runs inside the D-19-01 swallow; the helper returns leak strings and the caller try/catch absorbs any throw so uninstall success is never blocked |
</threat_model>

<success_criteria>
- Uninstall reclaims a clone when its last referencer is removed and preserves a shared clone otherwise, without ever failing the user-visible operation or touching the git surface.
</success_criteria>

<output>
Create `.planning/workstreams/url-source/phases/78-plugin-git-source-lifecycle/78-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 04)

- Post-commit `garbageCollectPluginClones(locations)` invocation in `orchestrators/plugin/uninstall.ts` (wiring only; the helper is from Plan 01).
