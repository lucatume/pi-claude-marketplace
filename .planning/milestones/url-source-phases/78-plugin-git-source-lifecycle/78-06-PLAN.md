---
phase: 78-plugin-git-source-lifecycle
plan: 06
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - tests/orchestrators/plugin/update.test.ts
  - tests/architecture/catalog-uat.test.ts
autonomous: true
requirements: [PURL-06]
must_haves:
  truths:
    - "A pinned git-source plugin whose manifest sha differs from the recorded resolvedSha swaps to the new sha; equal shas render (unchanged) (D-78-05)"
    - "An unpinned git-source plugin re-resolves remote HEAD at update time and swaps when the resolved sha differs from the recorded one (D-78-05)"
    - "The new clone materializes into the cache BEFORE the 3-phase swap; the old clone is GC'd AFTER the swap commits iff no surviving record references it (D-78-05 / D-78-01)"
    - "update-finalize WRITES resolvedSha for a git-source swap (update.ts:1168 currently omits it)"
    - "A vanished-repo unpinned update fails clean on the recorded sha via the existing network-unreachable/authentication-required REASONS (no new token, NFR-3)"
    - "The version arrow renders v#<7hex> → v#<7hex> for a sha-<12hex> pair with no new render grammar (D-78-06)"
  artifacts:
    - "A clone-materializing resolveGitPluginRoot probe with D-78-05 pin logic injected at update's candidate resolveStrict (update.ts:758)"
    - "resolvedSha written in finalizeUpdateRecord (update.ts:1168) + garbageCollectPluginClones called after the finalize commit"
  key_links:
    - "update.ts is gitOps-exempt (already imports GitOps for marketplace sync) so the probe may resolveRemoteRef for unpinned entries and materializePluginClone directly"
    - "The swap-or-not decision falls out of the existing toVersion === fromVersion short-circuit (update.ts:818) with toVersion = shaVersion(pin) for git sources"
---

<objective>
Give `update` a git-source refresh arm (PURL-06). update.ts is gitOps-exempt (it already imports
GitOps for marketplace sync), so unlike install/reinstall it may resolveRemoteRef and materialize
inline. Four additive changes to the existing 3-phase swap flow:

1. Candidate resolve (update.ts:758): inject a clone-materializing resolveGitPluginRoot probe with
   D-78-05 pin logic — pinned: source.sha; unpinned: resolveRemoteRef(HEAD) at update time. The probe
   materializes the new clone (network only on cache-miss) and returns the clone-anchored pluginRoot.
   The swap-or-not decision falls out of the existing toVersion === fromVersion short-circuit
   (update.ts:818): for a git source toVersion must be shaVersion(pin) so equal shas render (unchanged)
   and differing shas swap (mirror install's deriveInstallVersion git-source branch).
2. materialize-before-swap: prepareUpdateHandles (update.ts:837) reads installable.pluginRoot — the NEW
   clone the probe materialized — so the new tree is on disk before the swap begins (D-78-05). Unchanged
   in structure.
3. finalize (update.ts:1168): on the all-success arm, additionally set sRecord.resolvedSha = <captured pin>
   (currently only version/compatibility/resolvedSource are written).
4. GC-after-swap: call garbageCollectPluginClones(locations) after finalizeUpdateRecord's withStateGuard
   returns on the all-success arm — the old clone is reclaimed iff no surviving record references it (D-78-01).

Plus verify D-78-06: the sha-version arrow already renders v#<7hex> → v#<7hex> via composeVersionArrow;
add a catalog fixture, write ZERO render code.

Output: git-source arm in update.ts + extended update.test.ts + a catalog-uat arrow fixture.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/url-source/phases/78-plugin-git-source-lifecycle/78-CONTEXT.md
@.planning/workstreams/url-source/phases/78-plugin-git-source-lifecycle/78-RESEARCH.md
@.planning/workstreams/url-source/phases/78-plugin-git-source-lifecycle/78-PATTERNS.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend update.test.ts with the sha-change swap, unchanged, GC, and vanished-repo cases</name>
  <files>tests/orchestrators/plugin/update.test.ts</files>
  <read_first>
    - tests/orchestrators/plugin/update.test.ts (how a git-source installed record + marketplace manifest are set up; how update is invoked; how the mock gitOps + cloneCacheSeam are injected; how the update outcome partition/version-arrow is asserted)
    - tests/orchestrators/plugin/install.test.ts (the cloneCacheSeam injection + resolvedSha capture pattern update mirrors)
    - tests/helpers/git-mock.ts (resolveRemoteRef return values + resolveRemoteRefThrows + cloneCalls for the pinned/unpinned/vanished cases)
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts lines 755-830 (candidate resolve + toVersion===fromVersion short-circuit) and 1160-1180 (finalize all-success arm)
  </read_first>
  <action>
    Extend tests/orchestrators/plugin/update.test.ts:
    (1) pinned-sha-change: an installed git-source plugin (recorded resolvedSha = shaOld) whose manifest now
        carries source.sha = shaNew -> update swaps; the finalize record has resolvedSha = shaNew and version =
        shaVersion(shaNew); the OLD clone dir is GC'd (gone) and the NEW clone dir exists.
    (2) pinned-unchanged: manifest sha equals recorded resolvedSha -> outcome partition "unchanged", no swap,
        clone dirs untouched.
    (3) unpinned-head-change: an unpinned entry (source has ref or nothing) -> the probe calls
        resolveRemoteRef(HEAD) returning shaNew != recorded -> swap; assert resolveRemoteRef WAS called
        (unpinned re-resolve, D-78-05) and the new resolvedSha recorded.
    (4) shared-clone-not-GCd: two git plugins share the old url+sha; updating one swaps it but the old clone
        SURVIVES because the second plugin still references it.
    (5) vanished-repo: an unpinned update whose resolveRemoteRef THROWS a network/HttpError -> update fails
        clean for that plugin (skipped/failed partition with the existing network-unreachable /
        authentication-required reason), the plugin STAYS on its recorded sha, no new token.
    Titles use plain behavioral prose. Cases MUST fail before the update.ts edits (RED).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test "tests/orchestrators/plugin/update.test.ts" 2>&1 | grep -qE "not ok|# fail" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - update.test.ts gains pinned-sha-change, pinned-unchanged, unpinned-head-change, shared-clone-not-GCd, and vanished-repo cases
    - The vanished-repo case asserts the existing REASONS token (no new token) and the plugin staying on its recorded sha
    - Cases fail (RED) before the update.ts edits
    - No test title contains Phase/Plan/Wave/Pitfall-N tokens
  </acceptance_criteria>
  <done>update.test.ts carries the five git-source cases and is RED.</done>
</task>

<task type="auto">
  <name>Task 2: Inject the clone-materializing probe + write resolvedSha in finalize + GC after swap</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/update.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts lines 128 (resolvePluginVersion import), 177/219/238-262 (gitOps availability + syncClone), 755-830 (candidate resolve + resolvePluginVersion at 817 + toVersion===fromVersion short-circuit at 818), 837-886 (prepareUpdateHandles reads installable.pluginRoot), 1160-1180 (finalize all-success arm), 331-347 (phase-2 error arm)
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts lines 457-493 (makeInstallCloneProbe — the clone-materializing probe with resolvedSha side-channel capture) and 544-561 (deriveInstallVersion — the shaVersion(resolvedSha) git-source branch update must mirror for toVersion)
    - extensions/pi-claude-marketplace/orchestrators/plugin/clone-cache.ts (resolvePluginPin — pinned/unpinned pin resolution; materializePluginClone; canonicalCloneUrl from Plan 02)
    - extensions/pi-claude-marketplace/orchestrators/plugin/clone-gc.ts (Plan 01 — garbageCollectPluginClones)
    - extensions/pi-claude-marketplace/domain/version.ts (shaVersion) and domain/source.ts (parsePluginSource)
  </read_first>
  <action>
    In update.ts (which already imports GitOps — it is gitOps-exempt):
    - Build a clone-materializing resolveGitPluginRoot probe (like makeInstallCloneProbe): use resolvePluginPin
      (D-78-05: pinned source.sha short-circuits; unpinned resolves HEAD via resolveRemoteRef at update time),
      then materializePluginClone into the cache; capture the resolved pin via a side-channel getter. Inject it
      at the candidate resolveStrict (update.ts:758): resolveStrict(entry, { marketplaceRoot: mp.marketplaceRoot,
      resolveGitPluginRoot: probe }). Keep path/github-name plugins on the existing no-git-callback behavior.
    - Fix toVersion for git sources: at update.ts:817, when the source is git (url/git-subdir/github) and the
      probe captured a resolvedSha, toVersion must be shaVersion(pin) (mirror deriveInstallVersion) so the
      existing toVersion === fromVersion short-circuit (update.ts:818) correctly renders (unchanged) on equal
      shas and swaps on differing shas. Path/github-name plugins keep resolvePluginVersion.
    - finalize (update.ts:1168, all-success arm): add `if (capturedSha !== undefined) sRecord.resolvedSha = capturedSha;`
      alongside version/compatibility/resolvedSource. resolvedSource already becomes installable.pluginRoot (the
      new clone root) — leave that.
    - GC-after-swap: after finalizeUpdateRecord's withStateGuard commits on the all-success arm, call
      `await garbageCollectPluginClones(locations)` inside a try/catch swallow (D-19-01). Confirm locations is in
      scope at that hook; if not, thread it in. GC runs POST-commit so the new resolvedSha is already persisted
      and the old clone is unreferenced iff no other record maps to it.
    - Vanished-repo: leave the existing phase-2 error arm (update.ts:331-347) to catch the probe's
      resolveRemoteRef/materialize throw and surface the existing network-unreachable/authentication-required
      REASONS; the plugin stays on its recorded sha (NFR-3). No new token.
    Tag with PURL-06 / D-78-05 / D-78-01 / NFR-1 / NFR-3 anchors; NO Phase/Plan/Wave tokens. update.ts MAY name
    gitOps (it is exempt) but reuse the clone-cache seam by import for the materialize/pin primitives.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test "tests/orchestrators/plugin/update.test.ts" "tests/architecture/no-orchestrator-network.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - Pinned sha-change swaps and records the new resolvedSha + shaVersion; equal shas render (unchanged) (update.test.ts green)
    - Unpinned update re-resolves HEAD (resolveRemoteRef called) and swaps on a differing sha
    - The old clone is GC'd after the swap unless another record references it (shared-clone-not-GCd green)
    - finalizeUpdateRecord writes sRecord.resolvedSha for git-source swaps
    - Vanished-repo update fails clean on the recorded sha with the existing REASONS token
    - no-orchestrator-network.test.ts stays green (update.ts is exempt; the change adds no forbidden token to any gated file)
  </acceptance_criteria>
  <done>update swaps git sources on sha change, records resolvedSha, GCs the old clone post-commit, and fails clean on a vanished repo; update.test.ts green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the sha-version arrow renders v#<7hex> → v#<7hex> via a catalog-uat fixture (no render code)</name>
  <files>tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts lines 1779-1849 (formatShaVersionForDisplay, renderVersion, composeVersionArrow — already handle sha-<12hex> pairs)
    - tests/architecture/catalog-uat.test.ts (how a version-arrow byte form is asserted for an update row; the existing arrow fixtures to mirror)
  </read_first>
  <action>
    Add a catalog-uat fixture asserting that a git-source update from sha-<12hex>OLD to sha-<12hex>NEW renders
    the version arrow as `v#<7hex> → v#<7hex>` (the compact 7-hex display forms) through composeVersionArrow.
    This is a VERIFY-ONLY task (D-78-06): do NOT modify notify.ts or any render code — composeVersionArrow ->
    renderVersion -> formatShaVersionForDisplay already produces this form. If the fixture passes with no source
    change, D-78-06 is confirmed. If it fails, STOP and surface the discrepancy (do not add render code without
    re-confirming the decision). Titles use plain behavioral prose.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test "tests/architecture/catalog-uat.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - catalog-uat.test.ts gains a git-source update-arrow fixture asserting v#<7hex> → v#<7hex>
    - The fixture passes with NO change to notify.ts or any render code (D-78-06)
    - No test title contains Phase/Plan/Wave/Pitfall-N tokens
  </acceptance_criteria>
  <done>The sha-version arrow byte form is pinned by a catalog fixture; zero render code was written.</done>
</task>

</tasks>

<verification>
- `node --test "tests/orchestrators/plugin/update.test.ts" "tests/architecture/catalog-uat.test.ts" "tests/architecture/no-orchestrator-network.test.ts"` fully green.
- `cd extensions/pi-claude-marketplace && npm run typecheck` clean.
- notify.ts is byte-unchanged (D-78-06 needs no render code).
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest sha / remote HEAD → materialize pin | The pin drives the new clone identity; captured ONCE at candidate resolve |
| update swap commit → GC | GC must run only after the new resolvedSha is persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-13 | Tampering | update-time TOCTOU (manifest sha changes between resolve and swap) | high | mitigate | The pin is captured ONCE at the candidate resolve and the clone materialized at that pin BEFORE the swap; prepareUpdateHandles reads installable.pluginRoot (the materialized tree), not a re-read manifest |
| T-78-14 | Availability | finalize forgets resolvedSha -> GC deletes the new clone / next update miscompares | high | mitigate | The all-success finalize arm writes sRecord.resolvedSha = capturedSha; a pinned-unchanged and pinned-sha-change test both assert the recorded sha, and GC runs post-commit against the persisted record |
| T-78-15 | Availability | vanished-repo update crashes mid-swap | medium | mitigate | The probe throw is caught by the existing phase-2 error arm; the plugin stays on its recorded sha (no swap), fail-clean per NFR-3, existing REASONS token, no new token |
| T-78-16 | Information disclosure | credential leak in an update-time clone error | high | accept | Public-only (D-77-06 carried forward) — no credentials handled; authentication required carries only the HTTP status; no new file interpolates a token so the no-credential-leak gate needs no new entry |
</threat_model>

<success_criteria>
- update detects sha changes (pinned + unpinned), materializes the new clone before the swap, records the new resolvedSha, GCs the old clone when unreferenced, and fails clean on a vanished repo — with the version arrow already rendering v#<7hex> → v#<7hex>.
</success_criteria>

<output>
Create `.planning/workstreams/url-source/phases/78-plugin-git-source-lifecycle/78-06-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 06)

- A clone-materializing `resolveGitPluginRoot` probe with D-78-05 pin logic in `orchestrators/plugin/update.ts` (candidate-resolve injection).
- `sRecord.resolvedSha` write in `finalizeUpdateRecord`; git-source `toVersion = shaVersion(pin)` branch.
- Post-commit `garbageCollectPluginClones(locations)` call in update (wiring; helper from Plan 01).
- catalog-uat fixture pinning the `v#<7hex> → v#<7hex>` sha-version arrow (D-78-06, verify-only).
