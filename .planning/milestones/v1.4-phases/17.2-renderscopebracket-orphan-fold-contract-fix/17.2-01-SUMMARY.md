---
phase: 17.2-renderscopebracket-orphan-fold-contract-fix
plan: 01
subsystem: messaging
tags: [notify, renderer, orphan-fold, scope-bracket, exhaustiveness, version-arrow, docstring-hygiene]

# Dependency graph
requires:
  - phase: 16-renderer-public-api-alongside-v1
    provides: V2 renderer (`notify()` + `renderPluginRow` + `composePluginLines` + `composeMarketplaceBlock`) with the file-private `renderScopeBracket` helper that this plan corrects.
  - phase: 17.1-v2-grammar-amendment-autoupdate-surface
    provides: CR-01 finding + WR-01/WR-03/WR-06 warnings from the 17.1 code review (`.planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-REVIEW.md`).
provides:
  - 2-arg `renderScopeBracket(pluginScope, mpScope)` honoring the documented orphan-fold contract (D-17.2-01..D-17.2-03).
  - `mpScope: Scope` threaded from `composeMarketplaceBlock` through `composePluginLines` into `renderPluginRow`.
  - 4 new byte-equality unit tests (D-17.2-07a..d) locking the corrected contract independently of the catalog UAT.
  - Refreshed top-of-file + notifyUsageError docstrings (WR-01) describing V1 and V2 surfaces honestly.
  - Tightened `composeVersionArrow(from: string, to: string)` with the single live branch (WR-03).
  - Hardened `default: { assertNever(x); return ""; }` shape on both exhaustiveness gates (WR-06).
affects:
  - 17.2-02 (catalog + fixture sync) -- builds on the corrected renderer.
  - 17.2-03/17.2-04 (verification + close-out) -- assume Plan 01 is GREEN.
  - Phase 18+ orchestrator migration -- naturally constructs `PluginNotificationMessage` with `p.scope` from domain records; the corrected renderer suppresses the bracket on same-scope rows automatically.

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Orphan-fold 2-arg renderer helper: caller passes both per-row and parent scope; helper short-circuits on undefined and equality."
    - "Hardened exhaustiveness gate: `default: { assertNever(x); return \"\"; }` relies on runtime throw, not the value-returning `never` shape."

key-files:
  created: []
  modified:
    - "extensions/pi-claude-marketplace/shared/notify.ts (lines 16-65 docstring; 95-118 notifyUsageError preamble; 643-646 + 929-932 hardened defaults; 695-720 renderScopeBracket; 730-736 composeVersionArrow; 815-819 + 925-927 renderPluginRow signature + 10 call sites; 1097-1101 composePluginLines; 1128 composeMarketplaceBlock)"
    - "tests/shared/notify-v2.test.ts (lines 989-1180 added: 4 new byte-equality tests 21b-21e for D-17.2-07a..d)"

key-decisions:
  - "D-17.2-01..D-17.2-03 applied verbatim: 2-arg orphan-fold signature, non-optional mpScope, mechanical threading."
  - "Test (d) reason switched from the planner-drafted `bridge` (not in the closed Reason set) to `unsupported source` (closed-set member) -- Rule 1 auto-fix during Task 3 typecheck."

patterns-established:
  - "Pattern: file-private 2-arg renderer helpers receive both per-row and parent-marketplace scope; orphan-fold contract enforced at the helper, not at the caller."
  - "Pattern: hardened `assertNever` exhaustiveness gate uses block form + explicit `return \"\"` so the compile-time barrier holds without relying on `assertNever`'s `never` return type for value flow."

requirements-completed: []  # Plan has no SNM-* requirements per CONTEXT D-17.2-04 (pure tech-debt fix).

# Metrics
duration: 27min
completed: 2026-05-26
---

# Phase 17.2 Plan 01: CR-01 renderScopeBracket orphan-fold fix + notify.ts hygiene sweep Summary

**`renderScopeBracket` now takes both the plugin scope and the parent marketplace scope and emits the inline `[<scope>]` bracket only in the documented orphan-fold case, with `mp.scope` threaded through the renderer chain and three sibling 17.1-review warnings (WR-01/WR-03/WR-06) folded into the same notify.ts hygiene sweep.**

## Performance

- **Duration:** ~27 min
- **Started:** 2026-05-26T18:27Z
- **Completed:** 2026-05-26T18:38Z
- **Tasks:** 3 (plus 1 auto-fix commit between Task 2 and Task 3)
- **Files modified:** 2 (`extensions/pi-claude-marketplace/shared/notify.ts`, `tests/shared/notify-v2.test.ts`)

## Accomplishments

- Corrected `renderScopeBracket` to honor the binding contract at `docs/messaging-style-guide.md:73` ("plugin row emits `[<scope>]` ONLY when its scope differs from the parent marketplace's scope") -- the renderer now matches the documented orphan-fold rule end-to-end.
- Threaded `mp.scope` from `composeMarketplaceBlock` through `composePluginLines` into `renderPluginRow` as a non-optional `mpScope: Scope` parameter; updated all 10 per-arm call sites inside the switch.
- Added 4 new byte-equality unit tests (D-17.2-07a..d) covering same-scope `installed`, orphan-fold `installed`, same-scope `updated`, and orphan-fold `failed` -- the contract is now locked at the renderer level independently of the catalog UAT.
- Refreshed the top-of-file + `notifyUsageError` docstrings (WR-01) so both V1 severity-named wrappers and V2 structured entry points are described honestly with the Phase 21 deletion timeline; removed the "Phase 6 will" / "Phase 1 ships the primitive only" / "Phase 12 affirmation" historical-fiction clauses.
- Simplified `composeVersionArrow` (WR-03) to the single live branch with a tightened `(from: string, to: string): string` signature; removed the 3-branch defensive shape that was unreachable under Phase 15 D-15-04's REQUIRED `from`/`to` precondition.
- Hardened both exhaustiveness gates (WR-06) -- `renderMpHeader`'s default arm on `mp.status` and `renderPluginRow`'s default arm on `p` -- to the block form `default: { assertNever(x); return ""; }` so the typecheck relies on the runtime throw rather than the value-returning `never` shape.

## Task Commits

Each task committed atomically:

1. **Task 1: 2-arg renderScopeBracket + mpScope threading + 10 call-site updates** -- `5f69010` (refactor)
2. **Task 2: 4 byte-equality tests for D-17.2-07a..d** -- `df444db` (test)
3. **Auto-fix: D-17.2-07d test reason corrected to closed-set value** -- `dde5dbd` (fix; Rule 1 deviation, see below)
4. **Task 3: WR-01 docstrings + WR-03 composeVersionArrow + WR-06 default-arm hardening** -- `b5c5d5a` (refactor)

## Files Created/Modified

- `extensions/pi-claude-marketplace/shared/notify.ts` -- 4 distinct edit sites:
  - Lines 16-65: top-of-file block comment refreshed (WR-01) to describe V1 + V2 surfaces side-by-side.
  - Lines 93-118: `notifyUsageError` preamble refreshed (WR-01) to describe both overloads honestly.
  - Lines 643-646 / 929-932: both exhaustiveness gate defaults hardened to the block form (WR-06).
  - Lines 672-720: `renderScopeBracket` refactored to the 2-arg orphan-fold contract (CR-01).
  - Lines 723-736: `composeVersionArrow` simplified to the single live branch (WR-03).
  - Lines 815-927: `renderPluginRow` signature gains `mpScope: Scope`; all 10 per-arm call sites updated (8 `(p.scope, mpScope)` + 2 `(undefined, mpScope)`).
  - Lines 1097-1101: `composePluginLines` signature gains `mpScope: Scope`; forwards to `renderPluginRow`.
  - Line 1128: `composeMarketplaceBlock` passes `mp.scope` as the third argument when calling `composePluginLines`.
- `tests/shared/notify-v2.test.ts` -- appended 4 new tests after test 21a (line 974):
  - Line 989: `D-17.2-07a` -- same-scope `installed`, no bracket on plugin row.
  - Line 1037: `D-17.2-07b` -- orphan-fold `installed`, `[project]` bracket inline.
  - Line 1080: `D-17.2-07c` -- same-scope `updated`, no bracket; version-arrow `<from> → v<to>`.
  - Line 1133: `D-17.2-07d` -- orphan-fold `failed`, `[project]` bracket inline + `error` severity + reload-hint trailer.

## Post-fix call-site counts (verification)

```
$ grep -cE "renderScopeBracket\(p\.scope, mpScope\)" extensions/pi-claude-marketplace/shared/notify.ts
8
$ grep -cE "renderScopeBracket\(undefined, mpScope\)" extensions/pi-claude-marketplace/shared/notify.ts
2
$ grep -cE "renderScopeBracket\(scope\)" extensions/pi-claude-marketplace/shared/notify.ts
0
$ grep -cE "function composeVersionArrow\(from: string, to: string\)" extensions/pi-claude-marketplace/shared/notify.ts
1
$ grep -cE "Phase [16] will" extensions/pi-claude-marketplace/shared/notify.ts
0
$ grep -cE "return assertNever\(" extensions/pi-claude-marketplace/shared/notify.ts
0
```

## Test results

```
$ node --test tests/shared/notify-v2.test.ts
# tests 41
# suites 0
# pass 41
# fail 0
```

41/41 tests pass -- 37 pre-existing + 4 new orphan-fold contract locks.

## Decisions Made

- D-17.2-01..D-17.2-03 (renderer-level contract) and D-17.2-04 (folding WR-01/WR-03/WR-06 in the same plan) applied verbatim per the phase plan; no judgment calls beyond the planner-discretion items already enumerated in the CONTEXT.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] D-17.2-07d test reason `"bridge"` is not in the closed Reason set**
- **Found during:** Task 3 (initial `npx tsc --noEmit` after WR-01/WR-03/WR-06 edits)
- **Issue:** The planner-drafted byte form for the D-17.2-07d test asserted `reasons: ["bridge"]` and a literal `{bridge}` brace, but `shared/grammar/reasons.ts` exports a 28-entry closed `Reason` union (drift-locked against `docs/messaging-style-guide.md` frontmatter) and `"bridge"` is not a member. Typecheck failed with `Type '"bridge"' is not assignable to type ...` listing the closed set.
- **Fix:** Replaced `"bridge"` with `"unsupported source"` (a real closed-set Reason that is semantically plausible for an orphan-fold `failed` plugin and is already used across failure-class catalog rows). Updated both the input `reasons:` array and the expected byte form's `{...}` brace accordingly.
- **Files modified:** `tests/shared/notify-v2.test.ts` (Task 2's commit was complete; the fix landed as a separate `fix:` commit per task_commit_protocol -- amending the previous commit would have violated CLAUDE.md's "create new commits rather than amending" rule).
- **Verification:** Typecheck clean after the fix; 41/41 notify-v2 tests pass; the D-17.2-07d test still exercises the orphan-fold contract for an error-class arm (the substantive coverage is unchanged).
- **Committed in:** `dde5dbd` (fix commit between Task 2 and Task 3).

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Cosmetic fix to a test fixture's closed-set value. Substantive coverage of the orphan-fold contract for an error-class arm is preserved.

## Issues Encountered

- None beyond the Rule 1 deviation above. The phase plan was unusually precise and all acceptance criteria were satisfied on first attempt for Tasks 1 and 3.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries were introduced. The renderer is pure-string composition consuming TypeScript-validated `NotificationMessage` payloads. The plan's `<threat_model>` (T-17.2-01 Tampering, T-17.2-02 Information disclosure, T-17.2-03 Denial of service) all hold post-fix: T-17.2-01's mitigation (TypeScript strict mode + `npx tsc --noEmit`) is exercised by the 2-arg signature change (compile-time barrier verified across all 10 call sites + 3 threading sites).

## Known Stubs

None. The fix is a complete one-file refactor with no placeholders or partial wiring.

## Expected Downstream Failure (intentional)

Per the plan's `<verification>` block, `tests/architecture/catalog-uat.test.ts` will FAIL after this plan lands because the `project-orphan-folded` fixture still produces the pre-fix byte form. This is EXPECTED and INTENTIONAL: Plan 02 (catalog + fixture sync) re-aligns the catalog byte form + fixture in Wave 2 so byte-equality matches the corrected renderer. Plan 01's success criteria require only typecheck + `notify-v2.test.ts` GREEN, both of which are satisfied. The phase-level `npm run check` gate (Plan 04) is where GREEN UAT becomes mandatory.

## Self-Check: PASSED

- File: `extensions/pi-claude-marketplace/shared/notify.ts` -- FOUND (modified).
- File: `tests/shared/notify-v2.test.ts` -- FOUND (modified).
- Commit `5f69010` (Task 1) -- FOUND in `git log --oneline`.
- Commit `df444db` (Task 2) -- FOUND in `git log --oneline`.
- Commit `dde5dbd` (Rule 1 auto-fix) -- FOUND in `git log --oneline`.
- Commit `b5c5d5a` (Task 3) -- FOUND in `git log --oneline`.
- Acceptance criteria for Tasks 1, 2, 3: all greps return the expected counts.
- Tests: 41/41 pass on `node --test tests/shared/notify-v2.test.ts`.

## Next Phase Readiness

- The corrected renderer is ready for Plan 02 (catalog `project-orphan-folded` byte form + `tests/architecture/catalog-uat.test.ts` `project-orphan-folded` fixture cleanup + WR-02 narrative tightening).
- No blockers. The expected catalog UAT failure is documented above and lands GREEN in Plan 04 once Plan 02 completes.

---
*Phase: 17.2-renderscopebracket-orphan-fold-contract-fix*
*Completed: 2026-05-26*
