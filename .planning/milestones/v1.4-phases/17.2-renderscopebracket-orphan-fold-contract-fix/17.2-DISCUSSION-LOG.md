# Phase 17.2: renderScopeBracket orphan-fold contract fix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md -- this log preserves the alternatives considered.

**Date:** 2026-05-26
**Phase:** 17.2-renderscopebracket-orphan-fold-contract-fix
**Areas discussed:** Direction (CR-01 disposition), Cleanup scope, Test coverage, Phase 18 prep, WR-04/05/06 inclusion, Recurrence guard

---

## Direction (CR-01 disposition)

| Option | Description | Selected |
|--------|-------------|----------|
| Fix renderer to honor docs | Thread `mp.scope` through `composePluginLines`/`renderPluginRow` into a 2-arg `renderScopeBracket(pluginScope, mpScope)` that emits `""` when scopes match. Update catalog byte form + fixture to reflect the now-correct rule. REVIEW's recommendation; preserves the originally-documented user-contract. | ✓ |
| Rewrite docs to match impl | Keep `renderScopeBracket` as-is; rewrite `messaging-style-guide.md:73` + `output-catalog.md:39/46/196` so the bracket is documented as "emitted whenever `p.scope` is defined". Simpler code, but loosens the user-contract and means orchestrators must omit `p.scope` on same-scope rows. | |
| Hybrid: fix renderer but keep both inputs valid | Make `renderScopeBracket` compare scopes (correct same-scope behavior) AND keep the existing single-arg call sites working. Either input shape (orchestrator omits `p.scope` OR sets it to `mp.scope`) produces the no-bracket output. Most forgiving for Phase 18+ orchestrators. | |

**User's choice:** Fix renderer to honor docs
**Notes:** Aligns with the REVIEW.md CR-01 recommendation. Preserves the user-contract that has been documented across `messaging-style-guide.md`, `output-catalog.md`, and the Phase 16 D-16-17 design decision since the original v2 grammar landed.

---

## Cleanup scope

| Option | Description | Selected |
|--------|-------------|----------|
| Minimum: just the broken fixture + byte form | Remove the workaround `scope: "user"` from `project-orphan-folded` fixture (`catalog-uat.test.ts:303-309`); update catalog byte form line 191 to drop `[user]` from the same-scope row. Other scope-bearing fixtures unchanged (they're naturally orphan-fold or `available`/`unavailable` carve-outs). | |
| Audit all scope-bearing fixtures + WR-02 narrative | Sweep every fixture and catalog state that touches scope to confirm same-scope rows don't carry `[scope]` post-fix. Also rewrite the `output-catalog.md:182/196` narratives that referenced the buggy state. More work but closes WR-02 in the same phase. | |
| Full hygiene: fold in WR-01 + WR-03 too | Address all three 17.1 review warnings here -- stale top-of-file docstrings (WR-01) and `composeVersionArrow`'s defensive dead branches (WR-03). Single tech-debt sweep instead of two. Increases blast radius beyond the named phase goal. | ✓ |

**User's choice:** Full hygiene: fold in WR-01 + WR-03 too
**Notes:** Initial selection. Expanded further in follow-up question (see "WR-04/05/06 inclusion" below).

---

## Test coverage

| Option | Description | Selected |
|--------|-------------|----------|
| Add focused unit tests in `notify-v2.test.ts` | Add 3-4 new byte-equality cases in `tests/shared/notify-v2.test.ts` that explicitly construct same-scope rows (no bracket) and orphan-fold rows (bracket present) for representative variants (`installed`, `updated`, `failed`). Locks the contract at the renderer level independent of the catalog UAT. | ✓ |
| Add a compile-time/type-level proof in `notify-types.test.ts` | Lock the `renderScopeBracket` signature change (2-arg form taking `mpScope`) via a `tests/architecture/notify-types.test.ts` shape proof. Prevents accidental regression to the 1-arg signature. Pair with byte-equality tests below or use alone. | |
| Rely on updated catalog UAT only | The catalog UAT byte-equality with the fixed `project-orphan-folded` fixture is sufficient to lock the rule. No new unit tests. Minimum surface area; counts on existing coverage to catch any drift. | |
| Both: unit tests AND type-level proof | Belt-and-suspenders. Type proof locks the signature contract; byte-equality tests lock the runtime behavior for representative cases. Catalog UAT continues to lock the rendered surface. Recommended if Phase 18+ orchestrators are likely to flex scope edge cases. | |

**User's choice:** Add focused unit tests in `notify-v2.test.ts`
**Notes:** Byte-equality at the renderer level is sufficient; no compile-time signature proof needed.

---

## Phase 18 prep

| Option | Description | Selected |
|--------|-------------|----------|
| Just fix the renderer -- Phase 18 builds normally | Phase 18 orchestrators construct `PluginNotificationMessage` payloads naturally (`p.scope` from domain records); the now-correct renderer suppresses brackets when scopes match. No special prep needed. Cleanest hand-off. | ✓ |
| Add a "natural construction" contract test | Add a test that builds a `PluginNotificationMessage` as Phase 18+ orchestrators will (passing `p.scope` straight from domain records, same as `mp.scope`) and asserts no bracket appears. Documents the expected orchestrator pattern as a regression test for future call sites. | |
| Document the call-site pattern in a renderer JSDoc block | Update the `renderScopeBracket` / `renderPluginRow` JSDoc to explicitly state: "Orchestrators may pass `p.scope` straight from domain records; the renderer suppresses the bracket when `p.scope === mp.scope`." Lightweight, no test surface added. | |

**User's choice:** Just fix the renderer -- Phase 18 builds normally
**Notes:** Phase 18 builds on top of the corrected behavior; no extra scaffolding.

---

## WR-04/05/06 inclusion

| Option | Description | Selected |
|--------|-------------|----------|
| Fold all six warnings in (WR-01..WR-06) | Single sweep closes every 17.1 review finding. Phase scope grows but eliminates a long-tail of small tech debt items. WR-04 adds new tests; WR-05 deletes a dead helper; WR-06 hardens an exhaustiveness gate. | ✓ |
| Only WR-01 + WR-02 + WR-03 (notify.ts code-quality) | Keep scope narrow to `notify.ts` changes: stale docstrings (WR-01), catalog narratives (WR-02), `composeVersionArrow` dead branches (WR-03). Leaves WR-04/05/06 for a separate hygiene phase or deferred backlog. | |
| WR-01..WR-03 now; defer WR-04/05/06 to backlog with explicit notes | Same as above but explicitly record WR-04/05/06 as deferred ideas in CONTEXT.md so they aren't lost. Phase 17.2 stays focused on the CR-01 critical + adjacent notify.ts hygiene. | |

**User's choice:** Fold all six warnings in (WR-01..WR-06)
**Notes:** Single hygiene sweep across notify.ts + pi-api.ts + catalog-uat fixtures; avoids a follow-on tech-debt phase.

---

## Recurrence guard

| Option | Description | Selected |
|--------|-------------|----------|
| No -- byte-equality tests + catalog UAT are enough | The new unit tests + catalog UAT lock the rule by construction. Adding more guards is over-engineering for a one-off fix. Trust the existing structural enforcement (`no-legacy-markers` test, type proofs). | ✓ |
| Add a renderer-vs-docs invariant test | Add a `tests/architecture/*` test that asserts the orphan-fold rule's natural-input shape (`p.scope` set === `mp.scope`) produces no bracket and (`p.scope` set !== `mp.scope`) produces the bracket. Sits next to `no-legacy-markers` as a structural enforcement of the contract. | |
| Update `messaging-style-guide.md` frontmatter to lock the rule | If the style guide has frontmatter that's machine-readable (like the 34-rule ESLint plugin used to read), encode the orphan-fold rule there so a future renderer drift would be flagged at lint time. Heavier; only do this if the existing frontmatter loader is still load-bearing post-v1.4. | |

**User's choice:** No -- byte-equality tests + catalog UAT are enough
**Notes:** Trust the existing test infrastructure; avoid one-off structural locks for a one-shot fix.

---

## Claude's Discretion

- File-internal helper organization for threading `mp.scope` (positional args vs. a private context object) -- planner picks based on readability.
- Exact JSDoc rewording for WR-01 and WR-03 -- planner drafts; existing style in the file is the template.
- Whether the new orphan-fold byte-equality tests live as a new `describe` block or are interleaved into the existing per-variant suites in `notify-v2.test.ts` -- planner picks based on file convention.
- Exact handling of the WR-04 `try/catch` test (how to mock `pi.getAllTools()` to throw) -- planner picks based on `tests/platform/pi-api.test.ts` style if present, else the `tests/shared/notify-v2.test.ts` convention.
- WR-06 exhaustiveness gate fix shape (the REVIEW.md lists multiple acceptable patterns) -- planner picks the cleanest.

## Deferred Ideas

- Structural renderer-vs-docs invariant test (Phase 22+ candidate if a similar divergence recurs).
- JSDoc "call-site pattern" guidance for Phase 18+ orchestrators (revisit only if Phase 18 surfaces ergonomic pain).
- Cross-file consistency sweep beyond `notify.ts` / `pi-api.ts` / `catalog-uat.test.ts`.
- Hybrid forgiving 2-arg `renderScopeBracket` signature that accepts both `p.scope` set and omitted on same-scope rows (rejected in D-17.2-01).
- Reviving the `messaging-style-guide.md` frontmatter loader (retired in v1.4) to encode the orphan-fold rule structurally.
