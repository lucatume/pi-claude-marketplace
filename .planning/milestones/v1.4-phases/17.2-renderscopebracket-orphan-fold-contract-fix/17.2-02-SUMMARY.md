---
phase: 17.2-renderscopebracket-orphan-fold-contract-fix
plan: 02
subsystem: messaging
tags: [catalog, orphan-fold, scope-bracket, byte-equality, fixture-hygiene, wr-05, wr-02]

# Dependency graph
requires:
  - phase: 17.2-renderscopebracket-orphan-fold-contract-fix
    plan: 01
    provides: 2-arg `renderScopeBracket(pluginScope, mpScope)` honoring the orphan-fold contract; `mp.scope` threaded through the renderer chain. The corrected renderer emits no bracket on same-scope rows, which is the precondition for this plan's catalog + fixture sync.
provides:
  - Catalog `project-orphan-folded` byte form at `docs/output-catalog.md:191` synced to the corrected renderer (`● alpha v1.0.0 (installed)`; no `[user]` token).
  - Tightened narrative below the fenced block (line 196) describing the symmetry between the orphan-fold row (carries `[project]`) and the same-scope row (omits the bracket) per D-16-17.
  - Cleaned `project-orphan-folded` fixture in `tests/architecture/catalog-uat.test.ts` (workaround comment rewritten; explicit `scope: "user"` dropped to mirror the cleaner sibling pattern).
  - WR-05 dead-helper hack deleted (`void piWithSubagentsLoaded;` + 4-line rationalisation comment + the helper definition itself, since it had no remaining callers).
affects:
  - 17.2-03 onward (verification + close-out plans) -- catalog UAT byte-equality is now GREEN against the post-Plan-01 renderer.
  - Phase 18+ orchestrator migration -- the same-scope-no-bracket invariant is now end-to-end provable via byte equality, not just by unit-level lock.

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Catalog-state fixture symmetry: same-scope rows can either omit `p.scope` entirely (cleaner) or set it equal to the parent's `mp.scope` (harmless). Both shapes produce the same byte form because the renderer's orphan-fold rule short-circuits on either arm."
    - "Test-helper hygiene rule: a `void <helperName>;` reference statement is a smell -- if the helper has no real callers, delete it. The `void` reference defeats unused-import linting without serving any purpose."

key-files:
  created: []
  modified:
    - "docs/output-catalog.md (lines 188-194 fenced byte form; line 196 narrative tightening; line 182 narrative left unchanged because the existing claim is already correct)"
    - "tests/architecture/catalog-uat.test.ts (lines 282-313 `project-orphan-folded` fixture cleanup; lines 1328-1332 WR-05 hack deletion; lines 169-174 unused `piWithSubagentsLoaded` helper deleted)"

key-decisions:
  - "D-17.2-05 applied verbatim: dropped the explicit `scope: \"user\"` line in the `project-orphan-folded` fixture's same-scope row (the planner-recommended cleaner option); replaced the 4-line workaround comment with a short comment explaining the symmetry of the orphan-fold short-circuit."
  - "D-17.2-06 applied: line-196 narrative rewritten to describe the symmetry. Line-182 narrative left untouched because the existing claim (`Plugin rows omit the scope bracket because p.scope === mp.scope.`) is already accurate and concise -- per the plan, no tightening is needed."
  - "WR-05: the `piWithSubagentsLoaded` helper was deleted along with the `void` reference because no real caller existed. The three remaining factory helpers (`piWithBothLoaded`, `piWithMcpLoaded`, `piWithNothingLoaded`) cover every catalog fixture today."
  - "Cross-file audit confirmed no other same-scope row in either `tests/shared/notify-v2.test.ts` or `tests/architecture/catalog-uat.test.ts` asserted a wrong `[scope]` bracket on a plugin row -- the only buggy assertion was the catalog itself, fixed in Task 1."

patterns-established:
  - "Pattern: a catalog state's byte form is the single source of truth for the UAT runner; both the fixture (input) and the catalog (expected output) must be updated together when the renderer changes. Updating only one half causes the byte-equality runner to fail until both halves match."

requirements-completed: []  # Plan 17.2 carries no SNM-* requirements per CONTEXT D-17.2-04 (pure tech-debt fix).

# Metrics
duration: 6min (plus ~5min recovery from a cwd-drift incident; see Deviations below)
completed: 2026-05-26
---

# Phase 17.2 Plan 02: Catalog + fixture sync to corrected renderer Summary

**The `project-orphan-folded` catalog state's byte form on `docs/output-catalog.md:191` now drops the `[user]` token to match the post-Plan-01 renderer, the matching `tests/architecture/catalog-uat.test.ts` fixture is cleaned of its workaround comment, and the WR-05 dead-helper hack is gone -- catalog UAT byte-equality is GREEN.**

## Performance

- **Duration:** ~6 min substantive work (plus ~5 min for recovery from a cwd-drift incident, see Deviations)
- **Started:** 2026-05-26T22:41:58Z
- **Completed:** 2026-05-26T22:47:58Z
- **Tasks:** 2 (each committed atomically)
- **Files modified:** 2 (`docs/output-catalog.md`, `tests/architecture/catalog-uat.test.ts`)

## Accomplishments

- Synced the `project-orphan-folded` byte form (`docs/output-catalog.md:191`) to match the corrected renderer landed in Plan 01: same-scope `alpha v1.0.0` row no longer carries the `[user]` token (D-17.2-05).
- Rewrote the narrative below the fenced block (line 196) so it now describes the orphan-fold rule symmetry: the project-scoped row carries `[project]` because `plugin.scope !== marketplace.scope`, and the user-scoped row omits the bracket because `plugin.scope === marketplace.scope` (D-16-17 + D-17.2-06).
- Cleaned the `project-orphan-folded` fixture in `tests/architecture/catalog-uat.test.ts` (lines 282-313): dropped the explicit `scope: "user"` line on the same-scope row (the planner-recommended cleaner option) so the fixture mirrors the sibling `same-plugin-both-scopes` pattern; replaced the 4-line workaround comment with a short comment explaining the symmetry of the orphan-fold short-circuit.
- Deleted the WR-05 dead-code hack at `tests/architecture/catalog-uat.test.ts:1328-1332` (the `void piWithSubagentsLoaded;` statement plus its 4-line rationalisation comment). Because the helper had no other callers, also deleted the `piWithSubagentsLoaded` factory definition at lines 169-174.
- Audited `tests/shared/notify-v2.test.ts` and the rest of `tests/architecture/catalog-uat.test.ts` for other same-scope rows asserting a wrong `[scope]` bracket on a plugin row -- none found. Every existing `[user]` token on a plugin row in those files was already on an orphan-fold case (parent marketplace `[project]`); no additional fixture corrections were needed.

## Task Commits

Each task committed atomically:

1. **Task 1: catalog byte form + narrative tightening** -- `951e12b` (docs)
2. **Task 2: `project-orphan-folded` fixture cleanup + WR-05 dead-helper deletion** -- `73f1511` (test)

## Files Created/Modified

- `docs/output-catalog.md` -- 2 edit sites inside the `project-orphan-folded` section:
  - Line 191 byte form: `● alpha [user] v1.0.0 (installed)` → `● alpha v1.0.0 (installed)`.
  - Line 196 narrative: rewritten to describe the symmetry (orphan-fold row carries `[project]`; same-scope row omits the bracket) per D-16-17.
  - Line 182 narrative for `same-plugin-both-scopes`: **unchanged** (existing claim is already accurate and concise).
  - Lines 39 and 46 (Conventions rule statements): **unchanged** (correctly state the orphan-fold rule).
- `tests/architecture/catalog-uat.test.ts` -- 3 distinct edits:
  - Lines 282-313: dropped `scope: "user"` on the same-scope row of the `project-orphan-folded` fixture; replaced the 4-line workaround comment with a 7-line comment explaining the orphan-fold short-circuit symmetry.
  - Lines 169-174: deleted the unused `piWithSubagentsLoaded` factory definition (was only referenced via the WR-05 `void` hack, which is also deleted below).
  - Lines 1328-1332: deleted the WR-05 dead-code hack (the 4-line rationalisation comment + the `void piWithSubagentsLoaded;` statement).

## Acceptance criteria evidence

```
$ grep -cE "● alpha \[user\] v1\.0\.0 \(installed\)" docs/output-catalog.md
0
$ grep -cE "● alpha \[project\] v0\.9\.0 \(installed\)" docs/output-catalog.md
1
$ grep -c "omits the bracket" docs/output-catalog.md
1
$ awk 'NR==39 || NR==46' docs/output-catalog.md  # rule statements unchanged
- `[<scope>]` -- emitted ONLY in the orphan-fold case ...
The plugin-row `[<scope>]` bracket is emitted ONLY ...
$ grep -cE "void piWithSubagentsLoaded" tests/architecture/catalog-uat.test.ts
0
$ grep -cE "Same-scope row also carries explicit" tests/architecture/catalog-uat.test.ts
0
$ grep -cE "function piWithSubagentsLoaded" tests/architecture/catalog-uat.test.ts
0
$ grep -cE "piWithSubagentsLoaded" tests/architecture/catalog-uat.test.ts
0
```

## Test results

```
$ node --test tests/architecture/catalog-uat.test.ts
# tests 3
# suites 0
# pass 3
# fail 0

$ node --test tests/shared/notify-v2.test.ts
# tests 41
# suites 0
# pass 41
# fail 0

$ npx tsc --noEmit
(no output -- typecheck clean)
```

3/3 catalog UAT pass (byte-equality runner walks every `<!-- catalog-state: STATE -->` discriminator and every state matches); 41/41 notify-v2 tests continue to pass (no regression from the test-file edits).

## Decisions Made

- **D-17.2-05 disposition for the same-scope row:** dropped the explicit `scope: "user"` line (planner-recommended cleaner option). The byte-form expectation is unambiguous either way -- the renderer short-circuits on the `undefined` arm of `renderScopeBracket(pluginScope, mpScope)` when `pluginScope === undefined`, and on the equality arm when `pluginScope === mpScope`. Mirrors the sibling `same-plugin-both-scopes` fixture.
- **D-17.2-06 disposition for line-182 narrative:** **unchanged**. The plan permits "leave the paragraph as-is and note this in the SUMMARY" if no tightening is needed; the existing claim ("Plugin rows omit the scope bracket because `p.scope === mp.scope`.") is already accurate and concise.
- **WR-05 disposition for the helper:** **deleted** because no callers remained after removing the `void` hack. The remaining three factories (`piWithBothLoaded`, `piWithMcpLoaded`, `piWithNothingLoaded`) cover every catalog fixture in the file today.
- **Cross-file audit disposition:** no additional fixture corrections required. The grep `[user].*v[0-9].*(installed)` against `tests/shared/notify-v2.test.ts` matched only marketplace-header forms (`● demo [user] (added)`) and one orphan-fold plugin row (parent marketplace `[project]`, plugin `[user]` -- correctly bracketed). The grep against `tests/architecture/catalog-uat.test.ts` produced no plugin-row matches at all.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking issue] cwd drifted out of the worktree during Task 1 edit**
- **Found during:** Task 1 (initial commit)
- **Issue:** The first `Bash(cd /home/acolomba/pi-claude-marketplace && ...)` call (verifying the Wave 1 fix is present) implicitly changed cwd into the main repo. The subsequent `Edit` tool call used the absolute path `/home/acolomba/pi-claude-marketplace/docs/output-catalog.md` -- which resolves to the **main repo's** file, not the worktree's. The commit therefore landed on the main-repo branch `gsd/v1.3-replan-catalog` (commit `e34ade1`) instead of on the worktree branch `worktree-agent-a49633c2fed9fab62`.
- **Fix:** Recovered atomically:
  1. `git update-ref refs/heads/gsd/v1.3-replan-catalog fbc37412... e34ade1` (CAS reset of the main-repo branch back to its pre-incident HEAD `fbc3741`, dropping the misplaced commit). The third-arg `oldvalue` makes the operation refuse to run if another commit had landed in between -- safe.
  2. `git restore --staged docs/output-catalog.md && git restore docs/output-catalog.md` in the main repo to revert the working tree to match the restored HEAD.
  3. Re-applied the change inside the worktree using the worktree-rooted absolute path `/home/acolomba/pi-claude-marketplace/.claude/worktrees/agent-a49633c2fed9fab62/docs/output-catalog.md` and committed there normally (`951e12b`).
- **Why it's not a destructive-git violation:** the `git update-ref` target `gsd/v1.3-replan-catalog` is NOT in the protected-ref deny-list (`main|master|develop|trunk|release/*`); the operation used the atomic-CAS form so it would have refused to run if another commit had landed; only my own misplaced commit was rewound. No concurrent work was destroyed.
- **Files modified:** main repo's `gsd/v1.3-replan-catalog` branch HEAD restored to `fbc3741`; worktree branch `worktree-agent-a49633c2fed9fab62` correctly carries the Task 1 commit.
- **Verification:** `git log --oneline -3` on the worktree shows the correct ancestry; the main-repo branch's `git log -1` shows the pre-incident `fbc3741`; no stale artifacts left in either tree.
- **Future-prevention:** every subsequent Edit/Write call in this plan used the worktree-rooted absolute path explicitly. The cwd-drift assertion at task_commit_protocol step 0a would catch this kind of drift on the next commit attempt; in retrospect, running the assertion *before* the first edit (not just before the first commit) would catch it earlier.

---

**Total deviations:** 1 auto-fixed (Rule 3 -- blocking issue resolved atomically without losing concurrent work)
**Impact on plan:** ~5 min of recovery time; substantive plan execution unchanged.

## Issues Encountered

- The trufflehog pre-commit hook fails inside the worktree because `.git` is a file (a gitlink) rather than a directory, and the trufflehog scanner cannot find `.git/index` as a regular file. This is documented in `./CLAUDE.md`'s git guidelines; the documented workaround is `SKIP=trufflehog` on `git commit` from inside a worktree, which is what I used for both task commits. Running `pre-commit run trufflehog --all-files` standalone also fails with the same error inside this worktree (the CLAUDE.md guidance that the standalone scan "still passes" assumes a different failure mode than the one observed here).
- No other issues. Both tasks completed on first attempt after the cwd-drift recovery.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries. The catalog file (`docs/output-catalog.md`) is the user-contract source-of-truth and was modified only to drop the now-incorrect `[user]` token from one byte form and to tighten the surrounding prose. The test file (`tests/architecture/catalog-uat.test.ts`) had its `project-orphan-folded` fixture cleaned and an unused helper deleted -- no new test logic or data introduced. The plan's `<threat_model>` (T-17.2-04 Tampering, T-17.2-05 Information disclosure) holds post-change: T-17.2-04's mitigation (catalog UAT byte-equality runner) now passes GREEN.

## Known Stubs

None. The fix is a complete one-shot byte-form + fixture sync with no placeholders or partial wiring.

## TDD Gate Compliance

Both tasks were marked `tdd="true"` in the plan. The RED gate was satisfied by the pre-existing catalog UAT byte-mismatch failure on `project-orphan-folded` (verified before any edits). The GREEN gate (catalog UAT pass) was reached jointly by Tasks 1 and 2 because the two halves must be updated together to satisfy byte equality:

1. Task 1 (`951e12b`, `docs(...)`) updated the catalog's expected byte form.
2. Task 2 (`73f1511`, `test(...)`) updated the fixture so the input matches.

Per the conventional-commits classification of these test-suite-affecting changes, the gate sequence here is `(docs + test)` rather than the canonical `test → feat`. No `feat(...)` or `refactor(...)` commit was required because Plan 02 is a doc/test-only sync; the production renderer was changed in Plan 01.

## Self-Check: PASSED

- File: `docs/output-catalog.md` -- FOUND (modified at lines 191 and 196).
- File: `tests/architecture/catalog-uat.test.ts` -- FOUND (modified at lines 169-174, 282-313, 1319-1320 deletion site).
- Commit `951e12b` (Task 1) -- FOUND in `git log --oneline` on `worktree-agent-a49633c2fed9fab62`.
- Commit `73f1511` (Task 2) -- FOUND in `git log --oneline` on `worktree-agent-a49633c2fed9fab62`.
- Acceptance criteria for Tasks 1, 2: all greps return the expected counts (see "Acceptance criteria evidence" above).
- Tests: 3/3 catalog UAT pass; 41/41 notify-v2 tests pass; `npx tsc --noEmit` clean.

## Next Phase Readiness

- Plan 02's catalog + fixture sync is complete; catalog UAT byte-equality is GREEN against the post-Plan-01 renderer.
- The phase-level `npm run check` gate (Plan 04) is now achievable; downstream verification plans build cleanly on top.
- No blockers.

---
*Phase: 17.2-renderscopebracket-orphan-fold-contract-fix*
*Completed: 2026-05-26*
