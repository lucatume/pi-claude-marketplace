---
phase: 17.2-renderscopebracket-orphan-fold-contract-fix
plan: 2
type: execute
wave: 2
depends_on:
  - 17.2-01
files_modified:
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-17.2-05: Catalog `project-orphan-folded` byte form at docs/output-catalog.md:191 drops the now-incorrect `[user]` bracket on the same-scope `alpha` row; the fixture workaround at tests/architecture/catalog-uat.test.ts:303-309 is removed and the misleading comment is rewritten or deleted."
    - "D-17.2-06: WR-02 narrative claims at docs/output-catalog.md:182 and :196 are rewritten to match the corrected rule (the line-196 prose is already aspirationally correct; light tightening only)."
    - "WR-05: The `void piWithSubagentsLoaded;` dead-code hack at tests/architecture/catalog-uat.test.ts:1328-1332 plus the 4-line rationalisation comment are deleted. If `piWithSubagentsLoaded` itself becomes unused at the helper-definition site, delete the helper too."
    - "Other scope-bearing fixtures across both test files (catalog-uat.test.ts and notify-v2.test.ts) have been audited; any same-scope row whose byte-form expectation still includes a bracket has been corrected. Same-scope rows now produce no bracket post-fix."
  artifacts:
    - path: "docs/output-catalog.md"
      provides: "Corrected `project-orphan-folded` byte form + tightened WR-02 narratives at lines 182 and 196"
      contains: "● alpha v1.0.0 (installed)"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "Cleaned `project-orphan-folded` fixture without the `scope: \"user\"` workaround comment; WR-05 dead-helper deletion"
      contains: "project-orphan-folded"
  key_links:
    - from: "docs/output-catalog.md:189-194 (`project-orphan-folded` byte form)"
      to: "tests/architecture/catalog-uat.test.ts (`project-orphan-folded` fixture)"
      via: "catalog UAT runner reads the byte form between <!-- catalog-state: project-orphan-folded --> and the next discriminator; the fixture supplies the matching NotificationMessage"
      pattern: "project-orphan-folded"
---

<objective>
Sync the catalog byte form, the catalog narrative prose, and the catalog UAT fixture to match the
corrected `renderScopeBracket` orphan-fold contract landed by Plan 01. The `project-orphan-folded`
state at `docs/output-catalog.md:184-196` is the single catalog state whose byte form changes; the
fixture at `tests/architecture/catalog-uat.test.ts:282-313` is the matching driver. Fold in WR-05
(deletion of the `void piWithSubagentsLoaded;` dead-code hack at lines 1328-1332) as part of the
same `catalog-uat.test.ts` edit so the file is touched once.

Purpose: After Plan 01 lands, the catalog UAT byte-equality runner WILL FAIL because the
`project-orphan-folded` byte form on line 191 still shows `● alpha [user] v1.0.0 (installed)`, but
the corrected renderer now emits `● alpha v1.0.0 (installed)` (`p.scope === mp.scope === "user"` →
no bracket). This plan restores GREEN catalog UAT by updating the catalog AND the fixture together.
The WR-02 narratives at `:182` and `:196` are already aspirationally correct prose (the bug was in
the byte form, not the prose); a light tightening pass is sufficient per D-17.2-06.

Output: Two modified files. Catalog UAT byte-equality is GREEN against the corrected renderer.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-CONTEXT.md
@.planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-PATTERNS.md
@.planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-01-SUMMARY.md
@docs/output-catalog.md
@tests/architecture/catalog-uat.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update `project-orphan-folded` byte form in docs/output-catalog.md + tighten narrative prose at lines 182 and 196 (WR-02)</name>
  <files>docs/output-catalog.md</files>
  <read_first>
    docs/output-catalog.md (read lines 30-50 for the orphan-fold rule statement + lines 168-200 for both the `same-plugin-both-scopes` state and the `project-orphan-folded` state)
    .planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-CONTEXT.md (items 2 + 3 of `<domain>`; D-17.2-05, D-17.2-06)
    .planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-PATTERNS.md (section 6)
    docs/messaging-style-guide.md (line 73 -- binding contract)
  </read_first>
  <behavior>
    - The `project-orphan-folded` byte form (lines 188-194) shows two plugin rows: the project-scoped `alpha v0.9.0` row retains the `[project]` bracket (orphan-fold case, `p.scope !== mp.scope`); the user-scoped `alpha v1.0.0` row drops the now-incorrect `[user]` bracket (same-scope case, `p.scope === mp.scope === "user"`).
    - The narrative immediately above (line 182, for `same-plugin-both-scopes`) is tightened for clarity but its claim ("Plugin rows omit the scope bracket because `p.scope === mp.scope`.") is already correct and remains.
    - The narrative immediately below (line 196, for `project-orphan-folded`) is tightened so it accurately describes the corrected byte form: "`official [project]` does not exist. The project-scoped `alpha` is folded under the user-scope marketplace header; its row carries the explicit `[project]` bracket because `plugin.scope !== marketplace.scope` (Phase 16 D-16-17). The user-scoped `alpha` omits the bracket because `plugin.scope === marketplace.scope`." (paraphrase as needed for prose quality; keep the rule citation D-16-17).
    - Catalog UAT byte-equality (Task 2 fixture) now matches the corrected byte form.
  </behavior>
  <action>
    Edit `docs/output-catalog.md`:

    1. **Byte form update (line 191):** Inside the fenced text block starting at line 188 and ending at line 194, change the literal line `  ● alpha [user] v1.0.0 (installed)` to `  ● alpha v1.0.0 (installed)` (drop the `[user]` token; preserve the 2-space leading indent). The other line `  ● alpha [project] v0.9.0 (installed)` is unchanged -- that row IS the orphan-fold case (`p.scope === "project"`, `mp.scope === "user"`) and correctly carries `[project]`.

    2. **Narrative tightening at line 196:** Rewrite the existing paragraph to: "`official [project]` does not exist; the project-scoped `alpha` is folded under the user-scope marketplace header. Its row carries the explicit `[project]` bracket because `plugin.scope !== marketplace.scope` (Phase 16 D-16-17). The user-scoped `alpha` row omits the bracket because `plugin.scope === marketplace.scope` -- the orphan-fold rule applies symmetrically." Keep the paragraph short and the D-16-17 reference; final wording is at the executor's discretion subject to byte-form accuracy.

    3. **Narrative tightening at line 182:** The existing claim ("Two marketplace blocks; one per scope. Joined by one blank line (D-16-07). Plugin rows omit the scope bracket because `p.scope === mp.scope`.") is already correct. Light edit only -- clarify ambiguous prose if found, but do NOT change the substantive claim. If no tightening is needed, leave the paragraph as-is and note this in the SUMMARY.

    4. **DO NOT** modify the rule statements at lines 39 and 46 (Conventions §"Marketplace and plugin row syntax") -- those state the orphan-fold rule correctly and are referenced by the new prose at line 196.

    5. **DO NOT** add new catalog states or modify other catalog states.
  </action>
  <verify>
    <automated>grep -nE "^\s*●\s+alpha\s+(\[user\]\s+)?v1\.0\.0\s+\(installed\)$" docs/output-catalog.md | head -10</automated>
    <automated>awk 'NR&gt;=185 &amp;&amp; NR&lt;=200' docs/output-catalog.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/output-catalog.md` line ~191 (inside the `project-orphan-folded` fenced block) contains the literal string `● alpha v1.0.0 (installed)` and does NOT contain `● alpha [user] v1.0.0`.
    - `docs/output-catalog.md` line ~190 (inside the `project-orphan-folded` fenced block) still contains `● alpha [project] v0.9.0 (installed)` (unchanged -- that row is the orphan-fold case).
    - `docs/output-catalog.md` line ~196 (the paragraph below the fenced block) contains the phrase `omits the bracket` and references `D-16-17`.
    - `docs/output-catalog.md` lines 39 and 46 are UNCHANGED.
    - `grep -cE "● alpha \\[user\\] v1\\.0\\.0 \\(installed\\)" docs/output-catalog.md` returns `0` (the buggy byte form is gone).
    - `grep -cE "● alpha \\[project\\] v0\\.9\\.0 \\(installed\\)" docs/output-catalog.md` returns `1` (the orphan-fold row is preserved).
  </acceptance_criteria>
  <done>The `project-orphan-folded` catalog state's byte form on line ~191 matches the corrected renderer's output. The line-196 narrative accurately describes the corrected rule. The line-182 narrative is tightened (or unchanged if no improvement is needed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clean up `project-orphan-folded` fixture in catalog-uat.test.ts (drop workaround comment) + delete WR-05 dead-helper hack + audit other scope-bearing fixtures</name>
  <files>tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    tests/architecture/catalog-uat.test.ts (read lines 1-50 for imports, lines 160-180 for `piWithSubagentsLoaded` definition, lines 250-315 for the two scope-related fixtures, lines 1300-1340 for the dead-helper hack)
    .planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-CONTEXT.md (items 2 + 8 of `<domain>`; D-17.2-05)
    .planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-PATTERNS.md (sections 7 + 8)
  </read_first>
  <behavior>
    - The `project-orphan-folded` fixture (lines 282-313) is updated so the same-scope row at lines 298-309 either:
      (a) drops the explicit `scope: "user"` line so the fixture relies on the natural same-scope absence (planner-preferred cleaner option per D-17.2-05), OR
      (b) retains `scope: "user"` (harmless after the renderer fix) but the 4-line "Same-scope row also carries explicit `scope` because the orchestrator disambiguates..." comment is deleted because that rationalisation is now false.
    - The 4-line misleading comment at lines 303-307 is removed regardless of the chosen option.
    - The `void piWithSubagentsLoaded;` statement at line 1332 AND the 4-line rationalisation comment at lines 1328-1331 are deleted.
    - If `piWithSubagentsLoaded` itself is now unused (the only reference was the `void` statement), the helper definition at lines 170-180 is also deleted to satisfy the unused-export lint rule. Verify via grep before deletion.
    - Other scope-bearing fixtures in `catalog-uat.test.ts` and `tests/shared/notify-v2.test.ts` are scanned: any same-scope row whose byte form (in the catalog or test assertion) still expects a bracket is corrected. Specifically: the `same-plugin-both-scopes` fixture at lines 262-280 is already correct (no explicit `p.scope`); no other same-scope fixture in catalog-uat.test.ts carries the buggy shape.
    - Catalog UAT byte-equality (`tests/architecture/catalog-uat.test.ts`) runs GREEN against the corrected catalog and renderer.
  </behavior>
  <action>
    Edit `tests/architecture/catalog-uat.test.ts`:

    1. **`project-orphan-folded` fixture cleanup (lines 282-313):** Delete the 4-line comment at lines
       303-307 (`// Same-scope row also carries explicit \`scope\` because the orchestrator disambiguates ...`).
       Planner's discretion per D-17.2-05: either drop the `scope: "user",` line entirely (cleaner, mirrors
       the sibling `same-plugin-both-scopes` fixture at lines 262-280) OR retain it with no comment
       (harmless after the renderer fix). Recommended: drop it for cleanliness -- the fixture then mirrors
       the sibling pattern and the byte-form expectation is unambiguous.

    2. **WR-05 dead-helper deletion (lines 1328-1332):** Delete the 4-line block-comment at lines
       1328-1331 (`// Reference \`piWithSubagentsLoaded\` so the helper does not become dead code ...`)
       AND the `void piWithSubagentsLoaded;` statement at line 1332.

    3. **Conditional helper deletion (lines ~170-180):** Run
       `grep -nE "piWithSubagentsLoaded" tests/architecture/catalog-uat.test.ts` AFTER making the
       deletions in step 2. If the only remaining match is the `function piWithSubagentsLoaded()`
       definition itself (and possibly its docstring), DELETE the helper definition too. If any
       fixture still references it (none do today per the CONTEXT analysis), leave the helper in
       place.

    4. **Cross-file audit:**
       Grep `tests/shared/notify-v2.test.ts` and `tests/architecture/catalog-uat.test.ts` for any
       same-scope row with an explicit byte-form expectation containing `[scope]` where the plugin
       scope matches the parent marketplace scope. Specifically:

       - `grep -nE "\\[user\\].*v[0-9].*\\(installed\\)" tests/shared/notify-v2.test.ts` to find
         any test asserting a `[user]` bracket on a plugin row.
       - For each match, verify the parent marketplace `scope` in the same test block. If
         `mp.scope === "user"` AND `p.scope === "user"` (same-scope case), the byte-form expectation
         is now wrong and must be corrected to drop the `[user]` token from the plugin-row line.
       - The CONTEXT analysis indicates no such case exists in `notify-v2.test.ts` other than the new
         tests added by Plan 01 (which are already correct). If grep surfaces unexpected matches,
         report them and update each.

    5. **Catalog UAT runner is unchanged.** Only the fixture map mutates.
  </action>
  <verify>
    <automated>grep -nE "void piWithSubagentsLoaded" tests/architecture/catalog-uat.test.ts</automated>
    <automated>grep -nE "Same-scope row also carries explicit" tests/architecture/catalog-uat.test.ts</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; node --test tests/architecture/catalog-uat.test.ts 2>&amp;1 | tail -40</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; node --test tests/shared/notify-v2.test.ts 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "void piWithSubagentsLoaded" tests/architecture/catalog-uat.test.ts` returns `0`.
    - `grep -cE "Same-scope row also carries explicit" tests/architecture/catalog-uat.test.ts` returns `0` (the misleading comment is gone).
    - `grep -cE "project-orphan-folded" tests/architecture/catalog-uat.test.ts` returns `>= 1` (the fixture entry itself is preserved).
    - If `piWithSubagentsLoaded` was deleted: `grep -cE "function piWithSubagentsLoaded" tests/architecture/catalog-uat.test.ts` returns `0`. If it was kept (because some fixture references it after all): grep returns `1`. Either is acceptable; the executor records the choice in the SUMMARY.
    - `node --test tests/architecture/catalog-uat.test.ts` exits 0; the `project-orphan-folded` byte-equality assertion is GREEN against the post-Plan-01 renderer.
    - `node --test tests/shared/notify-v2.test.ts` continues to exit 0.
  </acceptance_criteria>
  <done>The `project-orphan-folded` fixture is cleaned (misleading comment removed; explicit same-scope `scope: "user"` either dropped or retained without rationalisation). The WR-05 dead-code hack is deleted. `piWithSubagentsLoaded` helper is deleted iff unused. Catalog UAT and notify-v2 tests both pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase modifies documentation (`docs/output-catalog.md`) and a unit-test file
(`tests/architecture/catalog-uat.test.ts`). No production code is touched; no I/O, network, or auth
surface is involved. The catalog file IS the user-contract source-of-truth -- drift between catalog
and renderer is the only failure mode, and that mode is verified end-to-end by `npm run check`.

| Boundary | Description |
|----------|-------------|
| `docs/output-catalog.md` ↔ catalog UAT runner | The runner parses `<!-- catalog-state: STATE -->` discriminators and the fenced text blocks below them; byte-equality against `notify()` output is the contract |
| Test file ↔ production source | The fixture map drives the catalog UAT via real `notify()` calls; no production surface depends on the test file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.2-04 | Tampering | Catalog ↔ renderer drift | mitigate | The catalog UAT byte-equality runner (existing, unchanged) is the structural enforcement; this plan re-syncs catalog + fixture to match the corrected renderer, restoring GREEN UAT |
| T-17.2-05 | Information disclosure | Doc/test content | accept | All content is the user-contract surface -- already public; no secrets exposed |
</threat_model>

<verification>
- `node --test tests/architecture/catalog-uat.test.ts` exits 0; the byte-equality runner walks every
  `<!-- catalog-state: STATE -->` discriminator (>= 30 examples per the runner's assertion at line
  ~1346) and every fixture matches.
- `node --test tests/shared/notify-v2.test.ts` continues to exit 0 (no test byte form regressed).
- `grep -cE "void piWithSubagentsLoaded" tests/architecture/catalog-uat.test.ts` returns `0`.
- `grep -cE "Same-scope row also carries explicit" tests/architecture/catalog-uat.test.ts` returns `0`.
- `grep -cE "● alpha \\[user\\] v1\\.0\\.0 \\(installed\\)" docs/output-catalog.md` returns `0`.
- `grep -cE "● alpha v1\\.0\\.0 \\(installed\\)" docs/output-catalog.md` returns `>= 1`.
- The phase-level `npm run check` (Plan 04) will run all checks together; this plan ensures the
  catalog UAT portion is GREEN.
</verification>

<success_criteria>
1. The `project-orphan-folded` catalog state's byte form at `docs/output-catalog.md:~191` drops the
   buggy `[user]` token (per D-17.2-05).
2. The accompanying narrative at line ~196 accurately describes the corrected orphan-fold rule (per
   D-17.2-06).
3. The line-182 narrative is unchanged or lightly tightened (already aspirationally correct).
4. The `project-orphan-folded` fixture in `tests/architecture/catalog-uat.test.ts` has the misleading
   4-line comment removed; the explicit `scope: "user"` is either dropped (cleaner) or retained
   (harmless).
5. The `void piWithSubagentsLoaded;` dead-code hack at lines 1328-1332 is deleted along with its
   4-line rationalisation comment (WR-05).
6. The `piWithSubagentsLoaded` helper function is deleted iff it has no remaining callers.
7. Catalog UAT byte-equality is GREEN against the post-Plan-01 renderer.
</success_criteria>

<output>
Create `.planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-02-SUMMARY.md`. Include:
the byte-form diff at `docs/output-catalog.md:191`, the comment+block deletions in
`tests/architecture/catalog-uat.test.ts` with line ranges, the chosen disposition for the
`scope: "user"` line in the fixture (dropped or retained), the chosen disposition for the
`piWithSubagentsLoaded` helper (deleted or retained), and confirmation that catalog UAT exits 0.
</output>
