---
phase: 17.2-renderscopebracket-orphan-fold-contract-fix
verified: 2026-05-26T23:30:00Z
status: passed
score: 11/11 must-haves verified
overrides_applied: 0
---

# Phase 17.2: renderScopeBracket Orphan-Fold Contract Fix -- Verification Report

**Phase Goal:** Resolve the divergence between the documented "orphan-fold only" plugin-row
scope-bracket contract (D-16-17 + `docs/messaging-style-guide.md:73` +
`docs/output-catalog.md:39/46/196`) and the renderer at
`extensions/pi-claude-marketplace/shared/notify.ts:683-685`, then fold in the remaining six
17.1 code-review warnings (WR-01..WR-06) into a single notify.ts hygiene sweep before Phase
18 starts migrating orchestrators onto the v2 grammar.

**Verified:** 2026-05-26T23:30:00Z
**Status:** PASSED
**Re-verification:** No -- initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-17.2-01/02: `renderScopeBracket` has the 2-arg signature `(pluginScope: Scope \| undefined, mpScope: Scope): string` and returns `""` when `pluginScope === undefined \|\| pluginScope === mpScope` | ✓ VERIFIED | Signature confirmed at line 719; body condition at line 720; grep count = 1 |
| 2 | D-17.2-03: `composePluginLines` and `renderPluginRow` take a non-optional `mpScope: Scope` parameter threaded from `composeMarketplaceBlock` | ✓ VERIFIED | Both signatures confirmed at lines 817-821 and 1101-1105; `composeMarketplaceBlock` passes `mp.scope` at line 1132 |
| 3 | D-17.2-01 call sites: exactly 8 scope-bearing arms call `renderScopeBracket(p.scope, mpScope)`; exactly 2 carve-out arms call `renderScopeBracket(undefined, mpScope)`; 0 legacy 1-arg calls remain | ✓ VERIFIED | `grep -cE "renderScopeBracket\(p\.scope, mpScope\)"` = 8; `grep -cE "renderScopeBracket\(undefined, mpScope\)"` = 2; `grep -cE "renderScopeBracket\(scope\)"` = 0 |
| 4 | D-17.2-05/06: `docs/output-catalog.md` `project-orphan-folded` byte form at line ~191 is `● alpha v1.0.0 (installed)` (no `[user]` token); narrative at line 196 describes the corrected rule and references D-16-17 | ✓ VERIFIED | `grep -cE "● alpha \[user\] v1\.0\.0 \(installed\)"` = 0; `grep -cE "● alpha \[project\] v0\.9\.0 \(installed\)"` = 1; `grep -c "omits the bracket"` = 1; D-16-17 reference confirmed in narrative at line 196 |
| 5 | D-17.2-07: Four new byte-equality unit tests in `tests/shared/notify-v2.test.ts` cover same-scope `installed`, orphan-fold `installed`, same-scope `updated`, orphan-fold `failed` (D-17.2-07a..d) | ✓ VERIFIED | `grep -cE "D-17\.2-07" tests/shared/notify-v2.test.ts` = 15 (test names + assertion messages); all 4 new tests present at lines 989, 1037, 1080, 1133; 41/41 pass |
| 6 | WR-01: Top-of-file block comment and `notifyUsageError` preamble describe both V1 and V2 surfaces honestly, state Phase 21 deletion timeline, contain no `Phase 6 will`/`Phase 1 ships the primitive only` historical-fiction clauses | ✓ VERIFIED | `grep -cE "Phase [16] will"` = 0; `grep -cE "Phase 1 ships the primitive only"` = 0; `grep -n "Phase 21 deletes"` confirms multiple occurrences (lines 34, 53, 110, 126, 188) |
| 7 | WR-03: `composeVersionArrow` has signature `(from: string, to: string): string` with a single live branch `return \`${from} → v${to}\`` | ✓ VERIFIED | `grep -cE "function composeVersionArrow\(from: string, to: string\)"` = 1; function body confirmed at lines 734-736 |
| 8 | WR-05: `void piWithSubagentsLoaded;` dead-code hack, its 4-line rationalisation comment, and the helper definition itself are all deleted from `tests/architecture/catalog-uat.test.ts` | ✓ VERIFIED | `grep -cE "void piWithSubagentsLoaded"` = 0; `grep -cE "Same-scope row also carries explicit"` = 0; `grep -cE "function piWithSubagentsLoaded"` = 0 |
| 9 | WR-04: `tests/platform/pi-api.test.ts` has 6 tests (3 new) locking the three WR-04 branches: (a) source-substring boundary, (b) try/catch fallback + in-callback throw, (c) `tool.name === undefined` boundary; `ToolStub` interface widened to `name?: string` | ✓ VERIFIED | `grep -cE "^test\("` = 6; `grep -cE "WR-04\|D-17\.2-08"` = 5; `grep -cE "name\?: string"` = 1; all 6/6 pass |
| 10 | WR-06: Both default arms in `renderMpHeader` (line 643) and `renderPluginRow` (line 932) use the hardened `default: { assertNever(x); return ""; }` block form; `return assertNever(...)` pattern is gone | ✓ VERIFIED | `grep -nE "default: \{"` returns lines 643 and 932; `grep -cE "return assertNever\("` = 0 |
| 11 | D-17.2-09/10: No Phase-18 orchestrator prep added; no new `tests/architecture/*` structural enforcement test introduced | ✓ VERIFIED | No orchestrator/bridge files modified per production-source delta (single file `shared/notify.ts`); `tests/architecture/` contains 6 files, none new for Phase 17.2 (only `catalog-uat.test.ts` modified) |

**Score:** 11/11 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `extensions/pi-claude-marketplace/shared/notify.ts` | Corrected `renderScopeBracket` + threaded `mpScope` + WR-01/WR-03/WR-06 hygiene | ✓ VERIFIED | 2-arg signature at line 719; 10 call sites updated; both default arms hardened; docstrings refreshed; `composeVersionArrow` simplified |
| `tests/shared/notify-v2.test.ts` | 4 new orphan-fold byte-equality tests (D-17.2-07a..d) | ✓ VERIFIED | Tests at lines 989, 1037, 1080, 1133; 41/41 pass |
| `docs/output-catalog.md` | Corrected `project-orphan-folded` byte form + tightened WR-02 narratives | ✓ VERIFIED | `[user]` token gone from line 191; narrative at line 196 references D-16-17 and uses "omits the bracket" |
| `tests/architecture/catalog-uat.test.ts` | Cleaned `project-orphan-folded` fixture + WR-05 dead-helper deletion | ✓ VERIFIED | Workaround comment deleted; `scope: "user"` dropped; `piWithSubagentsLoaded` helper and `void` hack fully removed; 3/3 catalog UAT pass |
| `tests/platform/pi-api.test.ts` | 3 new WR-04 branch-coverage tests | ✓ VERIFIED | 6 total tests (3 new); all pass; `ToolStub` widened |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `renderPluginRow` (8 scope-bearing arms) | `renderScopeBracket` | `renderScopeBracket(p.scope, mpScope)` | ✓ WIRED | grep count = 8; verified at lines 827, 841, 855, 869, 898, 907, 916, 925 |
| `renderPluginRow` (2 carve-out arms) | `renderScopeBracket` | `renderScopeBracket(undefined, mpScope)` | ✓ WIRED | grep count = 2; verified at lines 879, 889 (`available` and `unavailable` arms) |
| `composeMarketplaceBlock` | `composePluginLines` | `composePluginLines(p, probe, mp.scope)` | ✓ WIRED | Confirmed at line 1132 |
| `composePluginLines` | `renderPluginRow` | `renderPluginRow(p, probe, mpScope)` | ✓ WIRED | Confirmed at line 1106 |
| `tests/architecture/catalog-uat.test.ts` | `docs/output-catalog.md` byte form | catalog-state discriminator byte-equality runner | ✓ WIRED | 3/3 catalog UAT pass; 52 catalog-state markers walked |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All tests GREEN (typecheck + ESLint + Prettier + 1362 tests) | `npm run check` | exit 0; 1360 pass, 0 fail, 2 todo | ✓ PASS |
| notify-v2 orphan-fold contract locks | `node --test tests/shared/notify-v2.test.ts` | 41/41 pass | ✓ PASS |
| Catalog UAT byte-equality | `node --test tests/architecture/catalog-uat.test.ts` | 3/3 pass | ✓ PASS |
| Platform probe boundary cases | `node --test tests/platform/pi-api.test.ts` | 6/6 pass | ✓ PASS |
| No legacy 1-arg `renderScopeBracket` call sites | `grep -cE "renderScopeBracket\(scope\)"` | 0 | ✓ PASS |
| No `return assertNever(...)` pattern | `grep -cE "return assertNever\("` | 0 | ✓ PASS |
| No historical-fiction futurism clauses | `grep -cE "Phase [16] will"` | 0 | ✓ PASS |
| Production-source delta confined to `notify.ts` | `git diff --name-only c9f8b38..HEAD -- extensions/` | Single file `shared/notify.ts` | ✓ PASS |

---

### Anti-Patterns Found

No blockers or warnings detected:

- `grep -cE "TBD|FIXME|XXX"` on modified files: 0 unreferenced debt markers.
- `grep -cE "TODO|HACK|PLACEHOLDER"` on modified files: 0 unacknowledged markers.
- No stub patterns in the corrected `renderScopeBracket` -- the function body is the complete production implementation.
- No `return null`, `return {}`, or empty handler stubs introduced.
- The `composeVersionArrow` simplification from 4-branch to 1-branch is a genuine reduction, not a stub.

---

### Requirements Coverage

Phase 17.2 carries no SNM-* requirements per D-17.2-04 (pure tech-debt fix closing the 17.1 review
findings CR-01 + WR-01..WR-06). All eleven D-17.2-NN decisions referenced in the plan
`must_haves.truths` are verified above. REQUIREMENTS.md has no additional IDs mapped to this phase.

---

### Human Verification Required

None. All contracts are verifiable by grep and automated test execution. The phase delivers
implementation changes (renderer fix, docstring refresh, test additions) with no user-visible
behavior requiring manual inspection -- the orphan-fold contract is locked by byte-equality tests
against the renderer itself.

---

## Gaps Summary

No gaps. All 11 must-have truths verified against the actual codebase with direct evidence.

---

*Verified: 2026-05-26T23:30:00Z*
*Verifier: Claude (gsd-verifier)*
