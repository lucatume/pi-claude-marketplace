---
phase: 17.2-renderscopebracket-orphan-fold-contract-fix
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/platform/pi-api.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-17.2-08: WR-04 soft-dep probe coverage tests live in tests/platform/pi-api.test.ts (the file exists per CONTEXT analysis; D-17.2-08 directs the planner to use it when it exists)."
    - "WR-04: The three previously-uncovered or thinly-covered branches of `hasLoadedPiSubagents` / `hasLoadedPiMcpAdapter` are locked by explicit tests: (a) `pi-mcp-adapter` `sourceInfo.source` substring path, (b) `try/catch` fallback when `getAllTools()` throws, (c) `tool.name === undefined` boundary case."
    - "After Plan 03, `tool.name === undefined` produces `false` from both probes (no crash, no spurious `true`); the existing `ToolStub` interface is widened (or replaced with an inline one-off type) so a `name: undefined` mock can be constructed without TypeScript complaint."
  artifacts:
    - path: "tests/platform/pi-api.test.ts"
      provides: "WR-04 branch coverage for the three previously-thin/uncovered probe paths"
      contains: "tool.name === undefined"
  key_links:
    - from: "tests/platform/pi-api.test.ts"
      to: "extensions/pi-claude-marketplace/platform/pi-api.ts"
      via: "imports `hasLoadedPiSubagents`, `hasLoadedPiMcpAdapter`, `softDepStatus`"
      pattern: "import \\{[^}]*hasLoadedPiSubagents"
---

<objective>
Add WR-04 soft-dep probe coverage tests to `tests/platform/pi-api.test.ts` so the three currently
thinly-or-uncovered branches of `hasLoadedPiSubagents` / `hasLoadedPiMcpAdapter` at
`extensions/pi-claude-marketplace/platform/pi-api.ts:51-78` are locked at the unit-test level.

Purpose: Per WR-04 in the 17.1 REVIEW, the probes degrade silently to `false` when Pi's tool shape
drifts; without unit tests covering the boundary cases, that silent-degrade is not detected by CI.
This plan adds tests for: (a) the `pi-mcp-adapter` `sourceInfo.source` substring path (currently
covered by line 47 -- verify byte-faithfulness), (b) the `try/catch` fallback when `getAllTools()`
throws (currently covered at lines 41 and 53 -- verify), and (c) the `tool.name === undefined`
boundary case (NOT currently covered -- this is the real coverage gap).

Plan 03 is in Wave 1 and parallel-safe with Plan 01 (different file: `tests/platform/pi-api.test.ts`
vs `extensions/pi-claude-marketplace/shared/notify.ts` + `tests/shared/notify-v2.test.ts`). It does
NOT depend on Plan 01's renderer changes; it only tests existing platform-layer behavior.

Output: One modified test file with new tests covering the three WR-04 branches. The pi-api.ts
source code is NOT modified -- the probes are already correct; only the test coverage was thin.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-CONTEXT.md
@.planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-PATTERNS.md
@extensions/pi-claude-marketplace/platform/pi-api.ts
@tests/platform/pi-api.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add three new tests in pi-api.test.ts locking the WR-04 branches</name>
  <files>tests/platform/pi-api.test.ts</files>
  <read_first>
    tests/platform/pi-api.test.ts (entire file -- read all 63 lines for imports, helpers, existing 3 tests + style conventions)
    extensions/pi-claude-marketplace/platform/pi-api.ts (entire file -- read lines 1-86 for both probe implementations, the `ToolStub`-equivalent shape used internally, the `try/catch` fallback structure)
    .planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-CONTEXT.md (item 7 of `<domain>`; D-17.2-08)
    .planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-PATTERNS.md (section 10)
  </read_first>
  <behavior>
    - **Branch (a) -- `pi-mcp-adapter` `sourceInfo.source` substring path:** Verify-and-strengthen the existing test at line 47 ("platform pi-api detects mcp adapter by name or source"). The existing assertion uses `sourceInfo: { source: "@scope/pi-mcp-adapter@1.0.0" }`. Per CONTEXT this branch is already covered; the new test (or existing-test strengthening) should explicitly assert the boundary: `source` containing the exact substring `pi-mcp-adapter` (e.g. `"some-other-pkg/pi-mcp-adapter"`, `"pi-mcp-adapter-clone"`, `"prefix-pi-mcp-adapter-suffix"`) all return `true`; `source: "pi-mcp"` (no `-adapter` suffix) returns `false`; `source` set to a non-string (`undefined`, a number) does NOT match and does NOT crash.
    - **Branch (b) -- `try/catch` fallback:** Verify-and-strengthen the existing `makeThrowingPi()` coverage. The new test asserts: both `hasLoadedPiSubagents(makeThrowingPi())` and `hasLoadedPiMcpAdapter(makeThrowingPi())` return `false`; `softDepStatus(makeThrowingPi())` returns `{ piSubagentsLoaded: false, piMcpAdapterLoaded: false }` (composes both probe failures into a clean negative). Throwing inside the `.some()` callback (rather than from `getAllTools()` itself) also degrades to `false` -- construct a `Pi` mock whose `getAllTools()` returns an array containing a Proxy or accessor that throws on `name` lookup; assert the probe returns `false`.
    - **Branch (c) -- `tool.name === undefined` boundary case (the real coverage gap):** Construct a mock `Pi` whose `getAllTools()` returns tools with `name: undefined` (e.g. `[{}]` cast, or via a relaxed inline type). Assert: `hasLoadedPiSubagents` returns `false` (the `tool.name === "subagent"` check correctly treats `undefined` as non-match); `hasLoadedPiMcpAdapter` returns `false` when both `name === undefined` AND `sourceInfo?.source` is also undefined; `hasLoadedPiMcpAdapter` returns `true` when `name === undefined` AND `sourceInfo: { source: "pi-mcp-adapter" }` is set (the source-substring fallback path still fires).
    - The existing `ToolStub` interface at line 21 declares `name: string` as required, which prevents constructing the `name: undefined` case without a cast. Plan 03 either widens the interface to `name?: string` to match the same shape as `notify-v2.test.ts:144-147` (`interface MockTool { name?: string; sourceInfo?: { source?: string } }`) OR adds a one-off inline cast at the test call site. Planner picks based on readability -- widening the shared interface is cleaner and matches the codebase pattern, but a single-test inline cast is also acceptable.
  </behavior>
  <action>
    Edit `tests/platform/pi-api.test.ts`:

    1. **Widen `ToolStub` interface (lines 21-24) to relax `name` to optional:**

       Change `name: string;` to `name?: string;` so the new tests can construct tools without a
       `name` field. This matches the shape used in `tests/shared/notify-v2.test.ts:144-147` and
       aligns with the actual probe implementation, which guards `candidate.name === "mcp"` against
       undefined via the equality check itself.

    2. **Add Test 4 -- `pi-mcp-adapter` source-substring boundary (branch a):**

       Name: `"platform pi-api: hasLoadedPiMcpAdapter source-substring boundary matches"` (or
       similar). Assertions:

       - `hasLoadedPiMcpAdapter(makePi([{ name: "other", sourceInfo: { source: "pi-mcp-adapter" } }]))` returns `true` (exact substring match).
       - `hasLoadedPiMcpAdapter(makePi([{ name: "other", sourceInfo: { source: "wrapper/pi-mcp-adapter-clone" } }]))` returns `true` (substring within a larger string).
       - `hasLoadedPiMcpAdapter(makePi([{ name: "other", sourceInfo: { source: "pi-mcp" } }]))` returns `false` (substring `pi-mcp` does NOT match `pi-mcp-adapter`).
       - `hasLoadedPiMcpAdapter(makePi([{ name: "other", sourceInfo: { source: "" } }]))` returns `false`.
       - `hasLoadedPiMcpAdapter(makePi([{ name: "other", sourceInfo: {} }]))` returns `false` (no `source` field at all).

    3. **Add Test 5 -- `try/catch` fallback hardening (branch b):**

       Name: `"platform pi-api: probes return false when getAllTools() throws or any tool accessor throws"`.
       Assertions:

       - `hasLoadedPiSubagents(makeThrowingPi())` returns `false` (already covered by Test 1 line 41; add as redundancy).
       - `hasLoadedPiMcpAdapter(makeThrowingPi())` returns `false` (already covered by Test 2 line 53; add as redundancy).
       - `softDepStatus(makeThrowingPi())` returns `{ piSubagentsLoaded: false, piMcpAdapterLoaded: false }` (composes the two probe failures).

       If the probe's `.some(...)` callback can be made to throw (e.g. by passing a Proxy or an
       object with a getter that throws on `name` access), assert this also degrades to `false`. If
       this is structurally awkward (since `name` is a plain property in the existing helpers),
       skip the in-callback-throw assertion and rely on the existing `makeThrowingPi()` coverage of
       `getAllTools()` itself throwing.

       Implementation note: to test the in-callback-throw case cleanly, construct a tool via:

       ```
       function makePiWithThrowingTool(): ExtensionAPI {
         return {
           getAllTools: () => [
             new Proxy({}, {
               get(_t, prop) {
                 if (prop === "name") throw new Error("accessor failed");
                 return undefined;
               },
             }),
           ],
         } as unknown as ExtensionAPI;
       }
       ```

       Assert `hasLoadedPiSubagents(makePiWithThrowingTool())` returns `false`. Note: a getter that
       throws on EVERY property access could also break `sourceInfo` lookup in
       `hasLoadedPiMcpAdapter`. Scope the Proxy to throw ONLY on `name` so the test isolates the
       in-callback-throw to the subagent probe; or accept either behavior since both end in `false`.
       Planner picks the cleanest setup.

    4. **Add Test 6 -- `tool.name === undefined` boundary case (branch c, the real gap):**

       Name: `"platform pi-api: probes do not crash on tool.name === undefined; fall through to source-substring or false"`.
       Assertions:

       - `hasLoadedPiSubagents(makePi([{}]))` returns `false` (no crash; equality check `undefined === "subagent"` is `false`).
       - `hasLoadedPiSubagents(makePi([{ sourceInfo: { source: "pi-subagents" } }]))` returns `false` (the subagent probe does NOT have a source-substring fallback -- see `hasLoadedPiSubagents` body at lines 51-57; only the `name === "subagent"` check exists).
       - `hasLoadedPiMcpAdapter(makePi([{}]))` returns `false` (no `name`, no `sourceInfo`).
       - `hasLoadedPiMcpAdapter(makePi([{ sourceInfo: { source: "pi-mcp-adapter" } }]))` returns `true` (the source-substring fallback fires even when `name` is undefined -- the probe's `tool.name === "mcp"` check returns `false` for undefined-name, then falls through to the `sourceInfo.source` check).

    5. **DO NOT modify `extensions/pi-claude-marketplace/platform/pi-api.ts`.** The probes are already
       correct per CONTEXT analysis; only test coverage is added.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; node --test tests/platform/pi-api.test.ts 2>&amp;1 | tail -30</automated>
    <automated>grep -cE "name\?: string" tests/platform/pi-api.test.ts</automated>
    <automated>grep -cE "WR-04|D-17\.2-08" tests/platform/pi-api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/platform/pi-api.test.ts` contains the literal `interface ToolStub` declaration with `name?:` (relaxed to optional) -- verified by `grep -cE "name\\?: string" tests/platform/pi-api.test.ts` returning `>= 1`.
    - `tests/platform/pi-api.test.ts` contains at least 3 NEW `test(...)` blocks beyond the original 3, totaling 6 `test()` calls. Verify via `grep -cE "^test\\(" tests/platform/pi-api.test.ts` returning `>= 6`.
    - The file references either `WR-04` or `D-17.2-08` in a comment so an executor can identify the new tests (`grep -cE "WR-04|D-17\\.2-08" tests/platform/pi-api.test.ts` returns `>= 1`).
    - `node --test tests/platform/pi-api.test.ts` exits 0; all 6 tests pass.
    - `extensions/pi-claude-marketplace/platform/pi-api.ts` is UNMODIFIED (no source edits) -- verify by `git diff --name-only extensions/pi-claude-marketplace/platform/pi-api.ts` returning empty.
  </acceptance_criteria>
  <done>Three new tests in `tests/platform/pi-api.test.ts` lock the WR-04 branches: source-substring boundary, try/catch fallback hardening (including in-callback-throw if cleanly testable), and `tool.name === undefined` boundary. `ToolStub` interface is widened to `name?: string`. Pi-api.ts source is untouched. `node --test` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies only a unit-test file (`tests/platform/pi-api.test.ts`). No production code, no
I/O, no network surface. The platform `pi-api.ts` boundary it tests is the existing soft-dep probe
surface (already in production).

| Boundary | Description |
|----------|-------------|
| Test file ↔ platform-API surface | Tests call the exported `hasLoadedPiSubagents` / `hasLoadedPiMcpAdapter` / `softDepStatus` with mocked `Pi` objects; no real Pi API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.2-06 | Tampering | Probe drift detection | mitigate | The new tests lock the contract for the three boundary branches so future drift in Pi's tool shape (e.g. `tool.name` becoming optional in the upstream Pi API) is caught by CI rather than silently degrading the renderer's soft-dep marker output |
| T-17.2-07 | Information disclosure | Test content | accept | Test content is the user-contract surface; no secrets |
</threat_model>

<verification>
- `node --test tests/platform/pi-api.test.ts` exits 0 with all 6 tests passing.
- `git diff --name-only extensions/pi-claude-marketplace/platform/pi-api.ts` returns empty (source
  is unchanged).
- `grep -cE "^test\\(" tests/platform/pi-api.test.ts` returns `>= 6`.
- `grep -cE "name\\?: string" tests/platform/pi-api.test.ts` returns `>= 1` (interface widened).
</verification>

<success_criteria>
1. Three new tests in `tests/platform/pi-api.test.ts` cover the WR-04 branches per D-17.2-08.
2. The `ToolStub` interface is widened to allow `name?: string` so the `name === undefined`
   boundary case can be constructed cleanly.
3. The platform `pi-api.ts` source is unmodified -- only test coverage is added.
4. All tests pass.
</success_criteria>

<output>
Create `.planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-03-SUMMARY.md`.
Include: list of new test names with their target branch (a/b/c) and approximate line numbers,
confirmation that the `ToolStub` interface was widened (or that an inline cast was used in one
test), confirmation that `pi-api.ts` source was NOT touched, and the test count diff (3 → 6).
</output>
