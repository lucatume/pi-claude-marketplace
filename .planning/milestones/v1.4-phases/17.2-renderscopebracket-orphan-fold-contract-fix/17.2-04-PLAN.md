---
phase: 17.2-renderscopebracket-orphan-fold-contract-fix
plan: 4
type: execute
wave: 3
depends_on:
  - 17.2-01
  - 17.2-02
  - 17.2-03
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-17.2-04 (rollup): All six 17.1 review warnings (WR-01..WR-06) are closed: WR-01 (docstring refresh in Plan 01), WR-02 (catalog narrative tightening in Plan 02), WR-03 (composeVersionArrow simplification in Plan 01), WR-04 (soft-dep probe coverage tests in Plan 03), WR-05 (dead-helper deletion in Plan 02), WR-06 (exhaustiveness gate hardening in Plan 01)."
    - "D-17.2-09: No Phase-18-orchestrator-migration prep beyond fixing the renderer is in this phase; Phase 18 builds normally on top of the corrected renderScopeBracket."
    - "D-17.2-10: No new structural enforcement test (e.g. renderer-vs-docs invariant) was added; byte-equality unit tests + catalog UAT are the locks."
    - "D-17.2-11: Sequencing verified -- Wave 1 (Plans 01 + 03) landed before Wave 2 (Plan 02), and Plan 04 (Wave 3) is the final gate after all upstream plans complete."
    - "`npm run check` exits 0: typecheck + ESLint + Prettier + all tests pass cleanly."
    - "Catalog UAT byte-equality is GREEN against the corrected renderer."
  artifacts: []
  key_links:
    - from: "phase 17.2 closure"
      to: "phase 18 (Migration Wave 1) unblocked"
      via: "verified GREEN `npm run check` against the corrected renderer + updated catalog"
      pattern: "npm run check"
---

<objective>
Final GREEN-gate verification for Phase 17.2. Run `npm run check` (the project-wide
typecheck + ESLint + Prettier + tests command) and confirm exit code 0. This plan adds NO new code;
it is the rollup gate that confirms Plans 01 / 02 / 03 (Waves 1 + 2) cohere into a clean phase
closure. Per D-17.2-04 it is also the place where the executor explicitly records that all six
WR-01..WR-06 warnings are closed.

Purpose: Phase 17.2 is a tech-debt fix with no SNM-* requirement to close; the only "delivery"
proof is `npm run check` GREEN against the corrected renderer + updated catalog + new tests. Per
D-17.2-09 / D-17.2-10 / D-17.2-11, no additional structural test or Phase-18 prep is added; the
existing byte-equality tests (notify-v2 + catalog UAT) are the sufficient locks.

Output: A SUMMARY.md confirming GREEN + the WR-01..WR-06 closure mapping table. No source/test edits.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-CONTEXT.md
@.planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-01-SUMMARY.md
@.planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-02-SUMMARY.md
@.planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run `npm run check` and confirm GREEN; record WR-01..WR-06 closure mapping</name>
  <files></files>
  <read_first>
    .planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-01-SUMMARY.md
    .planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-02-SUMMARY.md
    .planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-03-SUMMARY.md
    .planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-CONTEXT.md (D-17.2-04, D-17.2-09, D-17.2-10, D-17.2-11)
  </read_first>
  <behavior>
    - `npm run check` exits 0. Typecheck has zero errors. ESLint reports zero errors and zero new warnings beyond the pre-Phase-17.2 baseline. Prettier reports zero formatting drift. All `node --test` tests pass.
    - Catalog UAT byte-equality (the `node --test tests/architecture/catalog-uat.test.ts` portion) is GREEN against the corrected renderer + updated catalog.
    - The `notify-v2.test.ts` test count includes the 4 new orphan-fold byte-equality tests from Plan 01 (D-17.2-07).
    - The `pi-api.test.ts` test count includes the 3 new WR-04 branch-coverage tests from Plan 03 (D-17.2-08).
    - No production source file other than `extensions/pi-claude-marketplace/shared/notify.ts` has been modified across the phase. Verify via `git diff --stat`.
    - The WR-01..WR-06 closure mapping is recorded in the SUMMARY (which plan closed which warning + verification evidence).
  </behavior>
  <action>
    Run the project's full quality gate and verify each component:

    1. **Typecheck:** `cd /home/acolomba/pi-claude-marketplace && npx tsc --noEmit -p tsconfig.json`.
       Capture the exit code. Must be 0.

    2. **Tests:** `cd /home/acolomba/pi-claude-marketplace && npm test` (or `npm run test` depending
       on package.json -- the executor inspects `package.json` "scripts" to identify the right
       command; `npm run check` is the umbrella but may be slow, in which case running each
       sub-command for clarity is acceptable). Must exit 0.

    3. **ESLint:** `cd /home/acolomba/pi-claude-marketplace && npx eslint .` (or whatever `npm run lint`
       resolves to per `package.json`). Must exit 0 with zero new warnings vs the pre-Phase-17.2
       baseline. If ESLint warnings appear that did not exist before Phase 17.2 (e.g. unused-export
       for `piWithSubagentsLoaded` if Plan 02 retained the helper), classify them: either fix in a
       follow-up commit in Plan 02 (if attributable to a Plan 02 omission) or surface to the
       developer for triage.

    4. **Prettier:** `cd /home/acolomba/pi-claude-marketplace && npx prettier --check .` (or
       `npm run format:check` if defined). Must exit 0.

    5. **The umbrella `npm run check`:** Run `cd /home/acolomba/pi-claude-marketplace && npm run check`
       and capture exit code + tail of output. This is the binding gate per CONTEXT item 10. Must
       exit 0.

    6. **Catalog UAT focused re-run** (defensive): `cd /home/acolomba/pi-claude-marketplace && node --test tests/architecture/catalog-uat.test.ts 2>&1 | tail -40`.
       Verify the byte-equality runner walks >= 30 examples and the `project-orphan-folded` state
       does NOT appear in the failures list.

    7. **Cross-phase audit -- production source delta:** Run
       `cd /home/acolomba/pi-claude-marketplace && git diff --stat $(git merge-base HEAD main 2>/dev/null || git rev-list --max-parents=0 HEAD) -- extensions/`
       (or equivalent -- the executor picks the right base ref based on the branch model). Verify
       that the only production source file modified across the phase is
       `extensions/pi-claude-marketplace/shared/notify.ts`. The platform `pi-api.ts` was NOT
       modified (per Plan 03 contract). If any other extension file shows up, surface as an
       unexpected change.

    8. **WR-01..WR-06 closure mapping table** -- record in the SUMMARY:

       | Warning | Closed by | Verification evidence |
       |---------|-----------|----------------------|
       | WR-01 (docstring refresh) | Plan 01 Task 3 | `grep -cE "Phase [16] will" notify.ts` returns 0 |
       | WR-02 (catalog narrative) | Plan 02 Task 1 | `output-catalog.md:196` references D-16-17 + corrected rule |
       | WR-03 (composeVersionArrow simplification) | Plan 01 Task 3 | signature `(from: string, to: string)` present; single live branch |
       | WR-04 (soft-dep probe coverage) | Plan 03 Task 1 | 3 new tests in `pi-api.test.ts` |
       | WR-05 (dead-helper deletion) | Plan 02 Task 2 | `grep -c "void piWithSubagentsLoaded"` returns 0 |
       | WR-06 (exhaustiveness gate hardening) | Plan 01 Task 3 | both `default: { assertNever(...); return ""; }` blocks present |

       Also record the CR-01 closure:

       | Finding | Closed by | Verification evidence |
       |---------|-----------|----------------------|
       | CR-01 (renderScopeBracket orphan-fold contract fix) | Plan 01 Task 1 + Task 2; Plan 02 Task 1 + Task 2 | 2-arg signature; 10 call sites threaded; 4 new byte-equality tests; catalog UAT GREEN |
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; npm run check 2>&amp;1 | tail -30</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; echo "exit=$?"</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; node --test tests/architecture/catalog-uat.test.ts 2>&amp;1 | tail -10</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; node --test tests/shared/notify-v2.test.ts 2>&amp;1 | tail -10</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; node --test tests/platform/pi-api.test.ts 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0.
    - `node --test tests/architecture/catalog-uat.test.ts` exits 0; the runner walks ≥ 30 catalog examples; `project-orphan-folded` is NOT in any failure list.
    - `node --test tests/shared/notify-v2.test.ts` exits 0.
    - `node --test tests/platform/pi-api.test.ts` exits 0; the file has ≥ 6 `test()` blocks.
    - `git diff --stat` shows production source delta confined to `extensions/pi-claude-marketplace/shared/notify.ts`; doc delta confined to `docs/output-catalog.md`; test delta confined to `tests/shared/notify-v2.test.ts`, `tests/architecture/catalog-uat.test.ts`, `tests/platform/pi-api.test.ts`. No unexpected files modified.
    - The SUMMARY records the WR-01..WR-06 closure mapping table and the CR-01 closure row.
    - If `npm run check` exits non-zero, the executor surfaces the failure context (which sub-command, what the error was) and does NOT write a "complete" SUMMARY -- instead, surface the failure to the developer for triage.
  </acceptance_criteria>
  <done>`npm run check` exits 0. Catalog UAT, notify-v2, and pi-api tests all GREEN. The WR-01..WR-06 + CR-01 closure mapping is recorded in the SUMMARY. The phase is ready for verification + close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan runs verification commands; it modifies no source. No I/O risk surface beyond reading
files already in the repository.

| Boundary | Description |
|----------|-------------|
| local CLI ↔ repository | `npm run check` reads source files and runs typecheck/ESLint/Prettier/tests locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.2-08 | Tampering | False-positive GREEN | mitigate | Run each sub-command in addition to the umbrella `npm run check`; require zero new ESLint warnings vs baseline; require ≥ 30 catalog UAT examples walked |
| T-17.2-09 | Repudiation | Closure claim | mitigate | The WR-01..WR-06 + CR-01 closure mapping table is recorded in the SUMMARY with grep-able verification evidence for each row |
</threat_model>

<verification>
- `npm run check` exits 0.
- The umbrella command output ends with no error markers (no `error TS`, no `error` from ESLint, no
  `code style issues found` from Prettier, no failed tests).
- All three primary test files (`notify-v2.test.ts`, `catalog-uat.test.ts`, `pi-api.test.ts`) exit 0
  when run individually.
- Production source delta is confined to `extensions/pi-claude-marketplace/shared/notify.ts`.
- The SUMMARY contains the WR-01..WR-06 + CR-01 closure mapping table.
</verification>

<success_criteria>
1. `npm run check` exits 0 -- typecheck + ESLint + Prettier + tests all GREEN.
2. Catalog UAT byte-equality is GREEN against the corrected renderer.
3. All four `must_haves.truths` are evidenced in the SUMMARY: WR-01..WR-06 closure mapping, no
   Phase-18 prep added (D-17.2-09), no structural enforcement test added (D-17.2-10), Wave sequencing
   verified (D-17.2-11).
4. The phase is ready for `/gsd-verify-phase 17.2` and `/gsd-close-phase 17.2`.
</success_criteria>

<output>
Create `.planning/phases/17.2-renderscopebracket-orphan-fold-contract-fix/17.2-04-SUMMARY.md` containing:

1. `npm run check` exit code (must be 0) + tail of output.
2. Per-test-file exit codes for `notify-v2.test.ts`, `catalog-uat.test.ts`, `pi-api.test.ts`.
3. The WR-01..WR-06 closure mapping table (6 rows).
4. The CR-01 closure row.
5. Confirmation that no production source file other than
   `extensions/pi-claude-marketplace/shared/notify.ts` was modified across the phase.
6. Explicit confirmation that D-17.2-09 / D-17.2-10 / D-17.2-11 were honored (no Phase-18 prep, no
   structural enforcement test, sequencing followed).
</output>
