---
phase: 15-type-model-adr-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
autonomous: true
requirements:
  - SNM-01
  - SNM-02
  - SNM-03
  - SNM-04
  - SNM-05
  - SNM-06
  - SNM-07
  - SNM-08
  - SNM-09
  - SNM-10
  - SNM-11

must_haves:
  truths:
    - "shared/notify.ts exports the full v1.4 structured type model (NotificationMessage, MarketplaceNotificationMessage, PluginNotificationMessage as a 10-variant discriminated union, PluginStatus, MarketplaceStatus, Dependency, MarketplaceDetails, UsageErrorMessage) per SNM-01..SNM-11 and D-15-01..D-15-09."
    - "shared/notify.ts exports the runtime const tuples PLUGIN_STATUSES (10 entries), MARKETPLACE_STATUSES (4 entries), DEPENDENCIES (2 entries) with derived literal-union types per D-15-11; PluginStatus round-trips with PluginNotificationMessage['status'] (SNM-04)."
    - "The V1 wrappers notifySuccess / notifyWarning / notifyError / notifyUsageError remain BYTE-IDENTICAL to the current file (lines 47-97 untouched); the new exports are appended below line 97."
    - "npm run check stays GREEN; success criterion #4 holds (zero call-site references to the new types outside shared/notify.ts and tests/architecture/notify-types.test.ts -- the latter ships in Plan 02)."
    - "Reason is imported unchanged from shared/grammar/reasons.ts (per D-15-03); the file does NOT redeclare Reason, does NOT prune the 3 structurally-absorbed entries (rollback partial / requires pi-subagents / requires pi-mcp), and re-exports Reason from shared/notify.ts as a single-import convenience surface for downstream Phase 16-20 call-site authors."
    - "Empty plugins: readonly PluginNotificationMessage[] IS the structural representation of the '(no plugins)' rendering (per D-15-08) and empty marketplaces: readonly MarketplaceNotificationMessage[] IS the structural representation of '(no marketplaces)' (per D-15-09); no separate noPlugins or noMarketplaces discriminator field is introduced -- the empty-array case is reachable in every variant of MarketplaceNotificationMessage and NotificationMessage by construction."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "v1.4 structured notification type model + const tuples (additive; V1 wrappers intact)"
      contains: "export const PLUGIN_STATUSES; export const MARKETPLACE_STATUSES; export const DEPENDENCIES; export type PluginStatus; export type MarketplaceStatus; export type Dependency; export interface MarketplaceDetails; export interface UsageErrorMessage; export type PluginNotificationMessage; export interface MarketplaceNotificationMessage; export interface NotificationMessage"
  key_links:
    - from: "extensions/pi-claude-marketplace/shared/notify.ts"
      to: "extensions/pi-claude-marketplace/shared/types.ts"
      via: "import type { Scope }"
      pattern: "import type \\{ Scope \\}"
    - from: "extensions/pi-claude-marketplace/shared/notify.ts"
      to: "extensions/pi-claude-marketplace/shared/grammar/reasons.ts"
      via: "import type { Reason }"
      pattern: "import type \\{ Reason \\}"
---

<objective>
Append the v1.4 structured notification type model to `extensions/pi-claude-marketplace/shared/notify.ts` per SNM-01..SNM-11 and locked decisions D-15-01..D-15-09, D-15-11. Ship runtime `as const` tuples (`PLUGIN_STATUSES`, `MARKETPLACE_STATUSES`, `DEPENDENCIES`) alongside the types so the derived literal unions and downstream fixture iteration share one source.

Purpose: Lay the type-level foundation that Phase 16 (`notify(ctx, NotificationMessage)` renderer + public API), Phase 17 (catalog UAT migration to structured fixtures), and Phases 18-20 (call-site migration waves) consume. Zero runtime impact in Phase 15: the V1 wrappers stay intact, no call site references the new types yet.

Output: A `shared/notify.ts` whose existing 97-line V1-wrapper body is BYTE-IDENTICAL and whose new content is appended below it. `npm run check` GREEN.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-type-model-adr-refresh/15-CONTEXT.md
@.planning/phases/15-type-model-adr-refresh/15-RESEARCH.md
@.planning/phases/15-type-model-adr-refresh/15-VALIDATION.md
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/shared/types.ts
@extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts
@extensions/pi-claude-marketplace/shared/grammar/reasons.ts
@extensions/pi-claude-marketplace/presentation/compact-line.ts
@extensions/pi-claude-marketplace/persistence/state-io.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append v1.4 structured type model + const tuples to shared/notify.ts</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (current 97-line V1 wrapper file; lines 47-97 must remain BYTE-IDENTICAL)
    - .planning/phases/15-type-model-adr-refresh/15-CONTEXT.md (locked decisions D-15-01..D-15-11; per-variant field discipline)
    - .planning/phases/15-type-model-adr-refresh/15-RESEARCH.md (pattern templates §"Pattern 1" `as const`+indexed-access, §"Pattern 2" discriminated union of named interfaces, §"Code Examples" Examples 1-4 showing the exact interface shapes)
    - .planning/REQUIREMENTS.md (SNM-01..SNM-11 type-shape requirements)
    - extensions/pi-claude-marketplace/shared/types.ts (Scope = "user" | "project" source; import only as `import type { Scope }`)
    - extensions/pi-claude-marketplace/shared/grammar/reasons.ts (Reason closed enum source; import only as `import type { Reason }`)
    - extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts (PATTERN REFERENCE: `as const` tuple + `(typeof X)[number]` literal-union template -- the new arrays mirror this shape exactly)
    - extensions/pi-claude-marketplace/presentation/compact-line.ts (PATTERN REFERENCE lines 96-259: 9-variant `RowSpec` discriminated union of named per-variant interfaces joined in one `export type` alias; `readonly` discipline on every field; the new `PluginNotificationMessage` follows the same shape but discriminates on `status` instead of `kind`)
    - extensions/pi-claude-marketplace/persistence/state-io.ts (line 70 `lastUpdatedAt: Type.Optional(Type.String())` -- `MarketplaceDetails.lastUpdatedAt?: string` mirrors this shape so list-surface orchestrators can pass the record's value through unchanged per D-15-05)
  </read_first>
  <action>
    Append the new type model below line 97 of `extensions/pi-claude-marketplace/shared/notify.ts`. Lines 1-97 (the existing V1 wrapper body: `notifySuccess`, `notifyWarning`, `notifyError`, `notifyUsageError` plus the file header doc-comment) MUST remain BYTE-IDENTICAL -- do not touch them. Add the new content as one logical block after a section header comment that names the SNM-* requirements and points at D-15-01..D-15-11.

    Imports to add at the top of the file (use `import type` for type-only imports per `compact-line.ts:58-59` precedent; respect ESLint import-x ordering): `import type { Scope } from "./types.ts";` and `import type { Reason } from "./grammar/reasons.ts";`. Per CONTEXT D-15-discretion, ALSO add `export type { Reason } from "./grammar/reasons.ts";` so downstream callers in Phase 16/18-20 have a single-import surface (recommendation from RESEARCH §"Open Questions" Q1).

    Add three `as const` runtime tuples per D-15-11 (mirror the `STATUS_TOKENS` pattern at `shared/grammar/status-tokens.ts:34-52`):
    - `export const PLUGIN_STATUSES = ['installed', 'updated', 'reinstalled', 'uninstalled', 'available', 'unavailable', 'upgradable', 'failed', 'skipped', 'manual recovery'] as const;` (10 entries; `"manual recovery"` MUST be the literal string with a space per status-tokens.ts:47 precedent; do NOT use kebab/camel forms).
    - `export const MARKETPLACE_STATUSES = ['added', 'removed', 'updated', 'failed'] as const;` (4 entries; no `"skipped"` per D-15-07).
    - `export const DEPENDENCIES = ['agents', 'mcp'] as const;` (2 entries).

    Derive the three literal-union types via indexed access:
    - `export type PluginStatus = (typeof PLUGIN_STATUSES)[number];`
    - `export type MarketplaceStatus = (typeof MARKETPLACE_STATUSES)[number];`
    - `export type Dependency = (typeof DEPENDENCIES)[number];`

    Add `MarketplaceDetails` interface per SNM-07 / D-15-05: `export interface MarketplaceDetails { readonly autoupdate: boolean; readonly lastUpdatedAt?: string; }`. `autoupdate` REQUIRED boolean; `lastUpdatedAt?: string` optional ISO timestamp matching `persistence/state-io.ts:70` shape; no `source`, no `version`, no other entries.

    Add `UsageErrorMessage` interface per SNM-08: `export interface UsageErrorMessage { readonly message: string; readonly usage: string; }`. No `cause`, no `severity`.

    Add the 10 named per-variant interfaces for `PluginNotificationMessage` (follow `compact-line.ts:96-259` `RowSpec` precedent: named exported interfaces joined in a `union`). Every field `readonly`. Per-variant required/optional fields are governed by D-15-01 (reasons), D-15-02 (dependencies), D-15-04 (version vs from/to), D-15-11 + SNM-11 (scope absent on `available`/`unavailable`), SNM-09 (rollbackPartial only on `failed`), SNM-10 (cause only on `failed`/`manual recovery`):

    - `PluginInstalledMessage`: status `"installed"`; required `name: string`, `dependencies: readonly Dependency[]`; optional `version?: string`, `scope?: Scope`. NO `reasons` (D-15-01), NO `cause`, NO `rollbackPartial`, NO `from`/`to`.
    - `PluginUpdatedMessage`: status `"updated"`; required `name: string`, `from: string`, `to: string` (D-15-04), `dependencies: readonly Dependency[]`; optional `scope?: Scope`. NO `version` (use `from`/`to` instead), NO `reasons`, NO `cause`, NO `rollbackPartial`.
    - `PluginReinstalledMessage`: status `"reinstalled"`; required `name: string`, `dependencies: readonly Dependency[]`; optional `version?: string`, `scope?: Scope`. NO `reasons`, NO `cause`, NO `rollbackPartial`, NO `from`/`to`.
    - `PluginUninstalledMessage`: status `"uninstalled"`; required `name: string`; optional `version?: string`, `scope?: Scope`. NO `reasons`, NO `dependencies` (D-15-02), NO `cause`, NO `rollbackPartial`, NO `from`/`to`.
    - `PluginAvailableMessage`: status `"available"`; required `name: string`; optional `version?: string`. NO `scope` (SNM-11 carve-out), NO `reasons`, NO `dependencies`, NO `cause`, NO `rollbackPartial`, NO `from`/`to`.
    - `PluginUnavailableMessage`: status `"unavailable"`; required `name: string`, `reasons: readonly Reason[]` (D-15-01); optional `version?: string`. NO `scope` (SNM-11 carve-out), NO `dependencies`, NO `cause`, NO `rollbackPartial`, NO `from`/`to`.
    - `PluginUpgradableMessage`: status `"upgradable"`; required `name: string`, `reasons: readonly Reason[]` (D-15-01); optional `version?: string`, `scope?: Scope`. NO `dependencies`, NO `cause`, NO `rollbackPartial`, NO `from`/`to`.
    - `PluginFailedMessage`: status `"failed"`; required `name: string`, `reasons: readonly Reason[]` (D-15-01); optional `version?: string`, `scope?: Scope`, `cause?: Error` (SNM-10), `rollbackPartial?: readonly { readonly phase: string; readonly cause?: Error }[]` (SNM-09). NO `dependencies`, NO `from`/`to`.
    - `PluginSkippedMessage`: status `"skipped"`; required `name: string`, `reasons: readonly Reason[]` (D-15-01); optional `version?: string`, `scope?: Scope`. NO `dependencies`, NO `cause`, NO `rollbackPartial`, NO `from`/`to`.
    - `PluginManualRecoveryMessage`: status `"manual recovery"` (literal string with space per status-tokens.ts:47); required `name: string`, `reasons: readonly Reason[]` (D-15-01); optional `version?: string`, `scope?: Scope`, `cause?: Error` (SNM-10). NO `dependencies`, NO `rollbackPartial`, NO `from`/`to`.

    Add the union: `export type PluginNotificationMessage = PluginInstalledMessage | PluginUpdatedMessage | PluginReinstalledMessage | PluginUninstalledMessage | PluginAvailableMessage | PluginUnavailableMessage | PluginUpgradableMessage | PluginFailedMessage | PluginSkippedMessage | PluginManualRecoveryMessage;` (SNM-03).

    Add `MarketplaceNotificationMessage` interface per SNM-02 / D-15-06: `export interface MarketplaceNotificationMessage { readonly name: string; readonly scope: Scope; readonly status?: MarketplaceStatus; readonly details?: MarketplaceDetails; readonly plugins: readonly PluginNotificationMessage[]; }`. `status?` and `details?` are independent optionals per D-15-06.

    Add `NotificationMessage` interface per SNM-01: `export interface NotificationMessage { readonly marketplaces: readonly MarketplaceNotificationMessage[]; }`. NO `severity`, NO `trailer` (both computed by Phase 16's `notify()` switch).

    Use JSDoc comments above each new export to (a) cite the SNM-* requirement(s) it satisfies, (b) cite the D-15-XX decision(s) that govern its shape, (c) cross-reference the pattern source file (e.g. `// Pattern: shared/grammar/status-tokens.ts:34-52`). Do NOT touch lines 1-97; do NOT introduce any `ctx.ui.notify` call (the V1 wrappers are the only call sites in this file per D-07; per-file ESLint override remains valid).

    Run `npm run typecheck`, then `npm run check`, fix any prettier/eslint violations until both exit 0. Do NOT add `// eslint-disable-*` directives.
  </action>
  <acceptance_criteria>
    - Source: `git diff -- extensions/pi-claude-marketplace/shared/notify.ts` shows ONLY appended content; lines 1-97 of the prior file content are unchanged byte-for-byte. Verify via `git diff -U0 -- extensions/pi-claude-marketplace/shared/notify.ts | grep -E "^-[^-]"` returns empty (no lines removed).
    - Source: `grep -E "^export const PLUGIN_STATUSES = \[" extensions/pi-claude-marketplace/shared/notify.ts` returns exactly 1 match.
    - Source: `grep -E "^export const MARKETPLACE_STATUSES = \[" extensions/pi-claude-marketplace/shared/notify.ts` returns exactly 1 match.
    - Source: `grep -E "^export const DEPENDENCIES = \[" extensions/pi-claude-marketplace/shared/notify.ts` returns exactly 1 match.
    - Source: `grep -E "^export type PluginStatus = " extensions/pi-claude-marketplace/shared/notify.ts` returns exactly 1 match.
    - Source: `grep -E "^export type MarketplaceStatus = " extensions/pi-claude-marketplace/shared/notify.ts` returns exactly 1 match.
    - Source: `grep -E "^export type Dependency = " extensions/pi-claude-marketplace/shared/notify.ts` returns exactly 1 match.
    - Source: `grep -E "^export type PluginNotificationMessage = " extensions/pi-claude-marketplace/shared/notify.ts` returns exactly 1 match.
    - Source: `grep -E "^export interface MarketplaceNotificationMessage " extensions/pi-claude-marketplace/shared/notify.ts` returns exactly 1 match.
    - Source: `grep -E "^export interface NotificationMessage " extensions/pi-claude-marketplace/shared/notify.ts` returns exactly 1 match.
    - Source: `grep -E "^export interface MarketplaceDetails " extensions/pi-claude-marketplace/shared/notify.ts` returns exactly 1 match.
    - Source: `grep -E "^export interface UsageErrorMessage " extensions/pi-claude-marketplace/shared/notify.ts` returns exactly 1 match.
    - Source: 10 named per-variant interfaces present: `grep -cE "^export interface Plugin(Installed|Updated|Reinstalled|Uninstalled|Available|Unavailable|Upgradable|Failed|Skipped|ManualRecovery)Message " extensions/pi-claude-marketplace/shared/notify.ts` returns 10.
    - Source: `"manual recovery"` literal (with space) present exactly once as a status discriminator: `grep -cE "status: \"manual recovery\"" extensions/pi-claude-marketplace/shared/notify.ts` returns 1; `grep -E "manual-recovery|manualRecovery" extensions/pi-claude-marketplace/shared/notify.ts` returns empty.
    - Source: V1 wrappers untouched -- `grep -cE "^export function notify(Success|Warning|Error|UsageError)" extensions/pi-claude-marketplace/shared/notify.ts` returns 4 (all four wrappers still present).
    - Source: no `severity` field on `NotificationMessage` / `MarketplaceNotificationMessage` (SNM-01): `awk '/^export interface NotificationMessage /,/^}/' extensions/pi-claude-marketplace/shared/notify.ts | grep -c "severity"` returns 0; `awk '/^export interface MarketplaceNotificationMessage /,/^}/' extensions/pi-claude-marketplace/shared/notify.ts | grep -c "severity"` returns 0.
    - Source: no `trailer` field on `NotificationMessage` (SNM-01): `awk '/^export interface NotificationMessage /,/^}/' extensions/pi-claude-marketplace/shared/notify.ts | grep -c "trailer"` returns 0.
    - Behavior (Success Criterion #4): no call-site references the new types yet -- `git grep -nE "PluginNotificationMessage|MarketplaceNotificationMessage|UsageErrorMessage|NotificationMessage|PluginStatus|MarketplaceStatus|MarketplaceDetails" -- 'extensions/' ':!extensions/pi-claude-marketplace/shared/notify.ts'` returns empty (the compile-check file ships in Plan 02; this assertion runs at the end of Plan 01 against `extensions/` only).
    - Command: `npm run typecheck` exits 0.
    - Command: `npm run check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run typecheck && npm run check</automated>
  </verify>
  <done>
    `extensions/pi-claude-marketplace/shared/notify.ts` carries the full v1.4 structured type model + const tuples appended below the untouched V1 wrappers; all SNM-01..SNM-11 shape requirements are encoded in the file's text; no call site in `extensions/` references any of the new symbols; `npm run check` GREEN.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit Plan 01 changes</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts</files>
  <read_first>
    - CLAUDE.md (git policy: never commit to main; Conventional Commits; pre-commit run --files before git commit; SKIP=trufflehog inside worktrees; never --no-verify; never --amend after hook failure)
    - .planning/phases/15-type-model-adr-refresh/15-RESEARCH.md §"Plan Granularity Recommendation" (commit message skeleton)
    - .planning/phases/15-type-model-adr-refresh/15-RESEARCH.md §"Pitfall 5" (pre-commit hook + worktree handling)
  </read_first>
  <action>
    Confirm we are NOT on `main` via `git rev-parse --abbrev-ref HEAD`. If on `main`, stop and surface the violation. Confirm the current branch (expected `gsd/v1.3-replan-catalog` per planning context; verify the v1.4 work continues on the same GSD branch -- per CLAUDE.md the planner does NOT create a new feature branch unilaterally).

    Stage only `extensions/pi-claude-marketplace/shared/notify.ts` (no `git add -A`). Run `pre-commit run --files extensions/pi-claude-marketplace/shared/notify.ts`. If hooks modify staged files (formatter), re-stage and re-run until clean. If hooks fail with a real violation, fix and re-run. If running inside a worktree (`git rev-parse --git-common-dir` differs from `git rev-parse --git-dir`), additionally run `pre-commit run trufflehog --all-files` standalone and confirm it exits clean BEFORE committing.

    Commit with the Conventional Commits message below. If inside a worktree, prefix with `SKIP=trufflehog` per CLAUDE.md (do NOT extend `SKIP=` to other hooks).

    Title (<=72 chars): `feat(notify): add v1.4 structured type model (SNM-01..SNM-11)`

    Body (lines <=80 chars):
    - "Append PluginNotificationMessage (10-variant discriminated union on"
    - "status), MarketplaceNotificationMessage, NotificationMessage,"
    - "PluginStatus / MarketplaceStatus / Dependency literal unions,"
    - "MarketplaceDetails, UsageErrorMessage to shared/notify.ts below the"
    - "untouched V1 wrappers."
    - "" (blank)
    - "Ships runtime PLUGIN_STATUSES / MARKETPLACE_STATUSES / DEPENDENCIES"
    - "const tuples per D-15-11 so the derived literal unions and downstream"
    - "fixture iteration share one source. Closed-set membership and"
    - "per-variant invariants are locked by Plan 02's notify-types arch test."
    - "" (blank)
    - "Per-variant field discipline per D-15-01 (reasons only on the 5 status-"
    - "with-{reason} variants), D-15-02 (dependencies only on installed /"
    - "updated / reinstalled), D-15-04 (version vs from/to placement),"
    - "D-15-05 (MarketplaceDetails shape mirrors state-io.ts:70),"
    - "D-15-06 (status? / details? independent optionals)."
    - "" (blank)
    - "SNM-09 (rollbackPartial only on failed), SNM-10 (cause only on"
    - "failed / manual recovery), SNM-11 (scope absent on available /"
    - "unavailable)."
    - "" (blank)
    - "Zero runtime impact: no call site references the new types yet"
    - "(success criterion #4); V1 wrappers stay byte-identical at lines"
    - "47-97. npm run check GREEN."
    - "" (blank)
    - "Refs: SNM-01, SNM-02, SNM-03, SNM-04, SNM-05, SNM-06, SNM-07, SNM-08,"
    - "      SNM-09, SNM-10, SNM-11"
    - "Phase: 15-type-model-adr-refresh / Plan 01"

    After commit, run `git status` to confirm clean tree. Do NOT push.
  </action>
  <acceptance_criteria>
    - Command: `git rev-parse --abbrev-ref HEAD` does NOT print `main`.
    - Behavior: `git log -1 --pretty=%s` matches `^feat\(notify\): add v1.4 structured type model \(SNM-01\.\.SNM-11\)$` (title 5-72 chars, Conventional Commits format).
    - Behavior: `git log -1 --pretty=%B | awk 'length > 80'` returns empty (no body line >80 chars).
    - Behavior: `git log -1 --name-only --pretty=format:"" | grep -v '^$'` lists exactly `extensions/pi-claude-marketplace/shared/notify.ts` (no other files committed).
    - Behavior: `git status --porcelain` returns empty (clean tree post-commit).
    - Command: `npm run check` exits 0 against HEAD.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && git log -1 --pretty=%B && git status --porcelain && npm run check</automated>
  </verify>
  <done>
    A single Conventional-Commits commit on the active GSD branch (not main) carries only the `shared/notify.ts` change; pre-commit hooks passed without `--no-verify`; `npm run check` GREEN against HEAD.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 15 introduces zero runtime code paths, no user input, no network, no disk mutation outside the planner's commit. Threat surface is N/A. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | shared/notify.ts (existing V1 wrappers) | mitigate | Acceptance criterion asserts lines 1-97 unchanged byte-for-byte via `git diff -U0 ... | grep -E "^-[^-]"` returning empty. V1 wrappers remain the only `ctx.ui.notify` call site (D-07) -- per-file ESLint override remains valid. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Phase 15 installs ZERO new packages (RESEARCH §"Standard Stack" / §"Package Legitimacy Audit"). No supply-chain surface added; no `[ASSUMED]`/`[SUS]`/`[SLOP]` entries to gate. |
</threat_model>

<verification>
**Plan-level checks (in addition to per-task `<verify>`):**

1. `cd /home/acolomba/pi-claude-marketplace && npm run check` exits 0.
2. `git grep -nE "PluginNotificationMessage|MarketplaceNotificationMessage|UsageErrorMessage|NotificationMessage|PluginStatus|MarketplaceStatus|MarketplaceDetails" -- 'extensions/' ':!extensions/pi-claude-marketplace/shared/notify.ts'` returns empty (Success Criterion #4 -- zero call-site references in `extensions/` at the end of Plan 01).
3. `git log -1 --name-only --pretty=format:"" | grep -v '^$'` lists exactly one file: `extensions/pi-claude-marketplace/shared/notify.ts`.
4. The V1 wrappers section of `shared/notify.ts` is BYTE-IDENTICAL to the prior commit: `git diff HEAD~1 -- extensions/pi-claude-marketplace/shared/notify.ts | grep -E "^-(export function|  ctx\.ui\.notify)"` returns empty (no removed wrapper lines).
</verification>

<success_criteria>
- `shared/notify.ts` exports the full v1.4 type model: `NotificationMessage` (SNM-01), `MarketplaceNotificationMessage` (SNM-02), 10-variant `PluginNotificationMessage` union (SNM-03), `PluginStatus` derived (SNM-04), `MarketplaceStatus` (SNM-05), `Dependency` + required `dependencies` on 3 variants (SNM-06), `MarketplaceDetails` (SNM-07), `UsageErrorMessage` (SNM-08), `rollbackPartial?` on `failed` (SNM-09), `cause?` on `failed`/`manual recovery` (SNM-10), `scope?` absent on `available`/`unavailable` (SNM-11).
- Runtime `as const` tuples (`PLUGIN_STATUSES` 10 entries, `MARKETPLACE_STATUSES` 4 entries, `DEPENDENCIES` 2 entries) ship per D-15-11.
- V1 wrappers (`notifySuccess`, `notifyWarning`, `notifyError`, `notifyUsageError`) remain byte-identical at lines 47-97.
- `npm run check` GREEN.
- Exactly one Conventional-Commits commit; branch is NOT `main`; pre-commit hooks ran clean (no `--no-verify`).
- Zero call-site references to the new symbols in `extensions/` (Success Criterion #4 holds for Plan 01; Plan 02 will add the only sanctioned reference at `tests/architecture/notify-types.test.ts`).
</success_criteria>

<output>
Create `.planning/phases/15-type-model-adr-refresh/15-01-SUMMARY.md` when done. The summary records:

1. Net LoC added to `shared/notify.ts` (expect ~+120 LoC: 10 variant interfaces × ~8 lines + 3 supporting interfaces + 3 const arrays + 3 derived types).
2. The exact 10 status literals shipped in `PLUGIN_STATUSES` (proves D-15-11 + SNM-04).
3. The exact 4 status literals shipped in `MARKETPLACE_STATUSES` (proves D-15-07).
4. The exact 2 dependency literals shipped in `DEPENDENCIES` (proves SNM-06).
5. Confirmation that Success Criterion #4 holds at end of Plan 01 (grep returns empty for the listed type names outside `shared/notify.ts`).
6. Confirmation that V1 wrappers are byte-identical (git diff check passed).
7. Commit SHA + branch name.
</output>
