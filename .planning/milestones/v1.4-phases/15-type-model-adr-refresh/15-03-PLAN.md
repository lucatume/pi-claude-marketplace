---
phase: 15-type-model-adr-refresh
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified:
  - docs/adr/v2-001-structured-notify.md
autonomous: true
requirements:
  - SNM-21

must_haves:
  truths:
    - "docs/adr/v2-001-structured-notify.md Status line flips from 'Proposed (v2 target)' to 'Accepted (Phase 15, 2026-05-25)' per D-15-13; the title line and Context section (lines 7-16 of the pre-refresh ADR) remain BYTE-IDENTICAL per the carve-out in D-15-13 and RESEARCH §'Pitfall 6'."
    - "Decision section is rewritten end-to-end to describe the single notify(ctx, NotificationMessage) entrypoint plus discriminated-union shape per D-15-13: status renames (PluginStatus / MarketplaceStatus named enums), *NotificationMessage type names, Dependency closed set, per-plugin causes, dropped top-level trailer, computed severity, always-marketplace-header spec change. Consequences section reflects the new SNM-* shape."
    - "Alternative 2 (single notify, no typed wrappers) flips from Rejected to ACCEPTED with explanatory note per D-15-16. Alternatives 1, 3, 4, 5, 6 remain Rejected with original reasoning."
    - "Migration section cites concrete phase numbers (Phase 16 renderer; Phase 17 spec + catalog UAT; Phases 18-20 call-site migration waves; Phase 21 V1 wrapper deletion + GREEN gate) per D-15-15."
    - "Open Questions section is DELETED entirely per D-15-14 (Q1 resolved by single PluginNotificationMessage union + always-marketplace-header spec; Q2 resolved by compile-enforced discriminated union + assertNever)."
    - "npm run check stays GREEN (markdown-only change; no code paths touched)."
  artifacts:
    - path: "docs/adr/v2-001-structured-notify.md"
      provides: "Refreshed source-of-truth ADR for v1.4 single-notify structured-payload design"
      contains: "Status: Accepted (Phase 15, 2026-05-25); Decision: single notify(ctx, NotificationMessage); Consequences: computed severity + computed reload-hint + per-plugin causes + always-marketplace-header; Migration: Phase 16-21 references; no Open Questions section"
  key_links:
    - from: "docs/adr/v2-001-structured-notify.md"
      to: ".planning/REQUIREMENTS.md SNM-01..SNM-11"
      via: "Decision section enumerates the locked type shapes"
      pattern: "PluginNotificationMessage|MarketplaceNotificationMessage|UsageErrorMessage"
    - from: "docs/adr/v2-001-structured-notify.md"
      to: ".planning/ROADMAP.md Phases 16-21"
      via: "Migration section forward references"
      pattern: "Phase 1[6-9]|Phase 2[01]"
---

<objective>
Refresh `docs/adr/v2-001-structured-notify.md` end-to-end to reflect the v1.4 locked design (single `notify(ctx, NotificationMessage)` entrypoint with discriminated union on `status`) and flip the Status from "Proposed" to "Accepted" with a forward reference to Phase 15. The current ADR's per-outcome wrapper design is OBSOLETE -- the design pivot landed between ADR draft and CONTEXT.md decisions, and this refresh documents the pivot honestly (D-15-16: Alternative 2 flipped Rejected -> ACCEPTED).

Purpose: Lock the source-of-truth artifact for the remaining v1.4 phases. Phase 16 (renderer + public API), Phase 17 (spec + catalog UAT), Phases 18-20 (call-site migration), and Phase 21 (V1 wrapper deletion + GREEN gate) all consume this ADR as the binding contract. Without the refresh, downstream phases would be reading a contradictory spec.

Output: A refreshed `v2-001-structured-notify.md` where the title + Context are preserved (Context still describes the v1.3 drift problem accurately per RESEARCH §"Pitfall 6"), Status / Decision / Consequences / Alternatives Alt-2 / Migration are rewritten, and Open Questions is deleted. SNM-21 closes here.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-type-model-adr-refresh/15-CONTEXT.md
@.planning/phases/15-type-model-adr-refresh/15-RESEARCH.md
@.planning/phases/15-type-model-adr-refresh/15-VALIDATION.md
@.planning/phases/15-type-model-adr-refresh/15-01-PLAN.md
@.planning/phases/15-type-model-adr-refresh/15-02-PLAN.md
@docs/adr/v2-001-structured-notify.md
@extensions/pi-claude-marketplace/shared/notify.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refresh ADR v2-001 end-to-end (Status + Decision + Consequences + Alt-2 + Migration; delete Open Questions)</name>
  <files>docs/adr/v2-001-structured-notify.md</files>
  <read_first>
    - docs/adr/v2-001-structured-notify.md (current 170-line file; the Title + Context lines 1, 7-16 must remain BYTE-IDENTICAL -- verify line numbers with `git log -p docs/adr/v2-001-structured-notify.md` to confirm baseline matches CONTEXT.md description per RESEARCH §"Assumptions Log" A3)
    - .planning/phases/15-type-model-adr-refresh/15-CONTEXT.md (locked decisions D-15-13 full Decision/Consequences rewrite + Status flip + Context preserved; D-15-14 delete Open Questions inline-resolving Q1 and Q2; D-15-15 Migration cites Phase 16-21; D-15-16 Alternative 2 flipped Rejected -> ACCEPTED; Alternatives 1/3/4/5/6 stay rejected)
    - .planning/phases/15-type-model-adr-refresh/15-RESEARCH.md §"Pitfall 6" (preserve Title line, preserve Context section lines 7-16, rewrite Decision/Consequences/Alt-2/Migration, delete Open Questions; "Refresh" does NOT mean "rewrite the whole document")
    - .planning/REQUIREMENTS.md (SNM-21 closure; SNM-01..SNM-11 type-shape facts the Decision section must restate accurately; SNM-22..SNM-32 phase-mapping that the Migration section cites)
    - .planning/ROADMAP.md §"Phase 16" through §"Phase 21" (the concrete phase numbers + goal statements the Migration section forward-references -- Phase 16 renderer & public API alongside V1; Phase 17 spec rewrite + catalog UAT migration; Phase 18 marketplace orchestrator family migration; Phase 19 plugin orchestrator family migration; Phase 20 edge handlers + UsageError migration; Phase 21 final teardown + GREEN gate)
    - extensions/pi-claude-marketplace/shared/notify.ts (the just-shipped Plan 01 type model; the Decision section's code snippet must match the actual exported names: NotificationMessage, MarketplaceNotificationMessage, PluginNotificationMessage, PluginStatus, MarketplaceStatus, Dependency, MarketplaceDetails, UsageErrorMessage)
  </read_first>
  <action>
    Edit `docs/adr/v2-001-structured-notify.md` in place. The file's section-by-section disposition follows D-15-13..D-15-16.

    Title line (current line 1) -- PRESERVE: `# ADR-v2-001: Structured \`notify\` payload with typed wrappers` remains the line-1 text. The "with typed wrappers" phrasing is now historically inaccurate (the design pivot dropped per-outcome wrappers in favor of a single `notify`), but D-15-13 LOCKS the title as preserved. The Decision section's body re-frames the design.

    Status line (current line 3) -- REWRITE: Change `- **Status:** Proposed (v2 target)` to `- **Status:** Accepted (Phase 15, 2026-05-25)`. Use that exact phrasing so the VALIDATION.md grep `grep -q "Status:.*Accepted.*Phase 15" docs/adr/v2-001-structured-notify.md` matches.

    Date line (current line 4) -- UPDATE: ensure `- **Date:** 2026-05-25`.

    Supersedes line (current line 5) -- PRESERVE OR UPDATE: keep `- **Supersedes:** D-CMC-11 (no structured-payload arg)`; add (do not remove) a more accurate v1.3 reference if appropriate.

    Context section (current lines 7-16) -- PRESERVE BYTE-IDENTICAL: D-15-13 LOCKS this. The Context section describes the v1.3 drift problem (34 lint rules, stringly-typed API, lint plugin as parallel codebase). It is still accurate post-pivot. Do NOT touch any character in this range. The acceptance check below grep-verifies a key phrase.

    Decision section (current lines 18-91) -- REWRITE END-TO-END per D-15-13. Cover:

    1. Framing paragraph: v1.4 introduces a single structured `notify(ctx, NotificationMessage)` entrypoint and a single `notifyUsageError(ctx, UsageErrorMessage)` entrypoint. The discriminator on `status:` literal (per-variant interfaces joined in a discriminated union) recovers per-outcome-wrapper autocomplete ergonomics without the per-wrapper file maintenance cost; `assertNever` in the switch retains the compile-error gate (D-15-16).

    2. Subsection "Public surface" -- list the two exported entrypoints and the user-facing TYPES exported from `shared/notify.ts`: `notify(ctx: ExtensionContext, message: NotificationMessage): void`; `notifyUsageError(ctx: ExtensionContext, message: UsageErrorMessage): void`; public TYPES `NotificationMessage`, `MarketplaceNotificationMessage`, `PluginNotificationMessage`, `PluginStatus`, `MarketplaceStatus`, `Dependency`, `MarketplaceDetails`, `UsageErrorMessage`. State that `presentation/` composers become module-internal helpers of `notify()` and are NOT re-exported from the barrel (SNM-18).

    3. Subsection "Implementation seam" -- describe the internal switch over `PluginStatus` / `MarketplaceStatus` as the SOLE site that knows the user-output grammar (SNM-17); `assertNever` exhaustiveness; computed severity (SNM-14: any `failed` -> error; `skipped` / `manual recovery` -> warning; otherwise success); computed reload-hint trailer (SNM-15: emitted iff any plugin status in `{installed, updated, reinstalled, uninstalled}` or any marketplace status set); render-time dependency probe (SNM-16: `agents` -> pi-subagents; `mcp` -> pi-mcp-adapter; emits `{requires pi-subagents}` / `{requires pi-mcp}` per absent probe on the corresponding plugin row). Include ONE code snippet showing the seam shape -- a roughly 12-line `switch` skeleton with two arms and a default `assertNever` arm. The snippet may reference the actual exported names from `shared/notify.ts`.

    4. Subsection "NotificationMessage shape" -- describe the type model in prose backed by ONE code block showing the discriminated-union shape. The code block MUST reference the actual exported types from Plan 01 (do NOT invent type names that don't exist). Enumerate: `NotificationMessage = { marketplaces: readonly MarketplaceNotificationMessage[] }` -- no `severity`, no `trailer` (SNM-01). `MarketplaceNotificationMessage = { name; scope; status?; details?; plugins }` -- `status?` and `details?` independent optionals (SNM-02 + D-15-06). `PluginNotificationMessage` = 10-variant discriminated union on `status` (SNM-03); list the 10 status literals with their per-variant field carve-outs (D-15-01..D-15-04, D-15-12). `PluginStatus = "installed" | "updated" | "reinstalled" | "uninstalled" | "available" | "unavailable" | "upgradable" | "failed" | "skipped" | "manual recovery"` -- derived via indexed access (SNM-04). `MarketplaceStatus = "added" | "removed" | "updated" | "failed"` (SNM-05 + D-15-07). `Dependency = "agents" | "mcp"`; required `dependencies: readonly Dependency[]` on installed / updated / reinstalled only (SNM-06 + D-15-02). `MarketplaceDetails = { autoupdate: boolean; lastUpdatedAt?: string }` (SNM-07 + D-15-05). `UsageErrorMessage = { message; usage }` (SNM-08). `failed` carries optional `rollbackPartial?: readonly { phase; cause? }[]` (SNM-09; no separate "rollback failed" status -- rollback partial is structurally a sub-state of `failed`). `failed` and `manual recovery` carry optional `cause?: Error` (SNM-10; per-plugin, not top-level; the v1.3 top-level cause-chain trailer is RETIRED). `scope?` on all plugin variants except `available` / `unavailable` (SNM-11; MSG-PL-6 carve-out preserved structurally).

       Explicitly call out the always-marketplace-header spec change: every output renders a marketplace header at column 0 with plugin rows indented two spaces. The spec rewrite lives in Phase 17.

    Consequences section (current lines 92-142) -- REWRITE END-TO-END per D-15-13. Cover six subsections:

    A. "Removed at compile time" -- severity, icon, status-token literal, grammar slot order, scope brackets, closed sets, soft-dep markers, reload-hint trailer, per-plugin cause-chain trailers all derive structurally inside `notify()` rather than per caller. Closed sets are encoded as `as const` tuples in `shared/notify.ts` (D-15-11) so the runtime arrays power downstream fixture iteration AND the literal-union types power compile-time narrowing.

    B. "Custom ESLint plugin deleted entirely" -- `tests/lint-rules/` (~4096 lines: 34 MSG-* rules + 34 RuleTester suites + registry + helpers) deleted; `tests/architecture/msg-rule-registry.test.ts` deleted; `eslint.config.js` swaps MSG-plugin wiring for stock `no-restricted-syntax` blocking `ctx.ui.notify` outside `shared/notify.ts` and `no-console` with per-file override for `persistence/migrate.ts` (IL-3 sanctioned legacy-migration warn). Deletion lands in Phase 21 after the call-site migration waves complete.

    C. "Coverage moves to per-variant unit tests + catalog UAT" -- each `PluginStatus` and `MarketplaceStatus` value gets a small unit test that calls `notify()` with a mock `ctx` and asserts on the exact string passed to `ctx.ui.notify` (SNM-30); catalog UAT runner keeps its byte-equality role unchanged but is now fed by structured `NotificationMessage` fixtures (SNM-31), not pre-assembled strings. `docs/output-catalog.md` is rewritten in Phase 17 to reflect the always-marketplace-header spec (SNM-20). `docs/messaging-style-guide.md` v2.0 describes the structured type model as the binding contract (SNM-19).

    D. "Other consequences" -- `presentation/` composers become module-internal helpers (SNM-18); per-plugin causes survive every cascade (SNM-10); the top-level cause-chain trailer is retired (no top-level `severity`, no top-level `trailer` on `NotificationMessage` per SNM-01); discoverability is recovered by discriminated-union literal narrowing on `status:`; greppability preserved (per-status `case` arms in the switch are distinct symbols).

    E. "Costs" -- migrating ~33 call sites total (~20 orchestrator `notifySuccess` / `Warning` / `Error` sites + ~13 edge `notifyUsageError` sites); ~120 LoC of types in `shared/notify.ts` (added by Phase 15); ~80 LoC of compile-check arch test (added by Phase 15); the Phase 16 `notify()` renderer body. One-time deletion of `tests/lint-rules/` + `tests/architecture/msg-rule-registry.test.ts` lands in Phase 21.

    F. "Net code delta" -- keep the milestone-level estimate: ~+400 LoC types/switch/wrappers minus ~4700 LoC deleted (lint plugin + RuleTester suites + parity test + absorbed presentation composers) = **~4300 LoC net removed** at milestone close. Phase 15 in isolation ADDS ~+200 LoC; the deletion happens in Phase 21.

    Alternatives section (current lines 144-156) -- PATCH per D-15-16. Find the bullet for Alternative 2 ("Single `notify(ctx, payload)` only, no typed wrappers. Loses per-call-site autocomplete and adds `kind:` literal ceremony at every site. Rejected for ergonomics.") and replace WITH a "Single `notify(ctx, payload)` only, no typed wrappers. **ACCEPTED (v1.4 design pivot).**" paragraph explaining that discriminated-union literal narrowing on `status:` recovers per-outcome-wrapper autocomplete ergonomics without the per-wrapper file maintenance cost; `assertNever` in the `notify()` switch retains the compile-error gate that motivated the original wrappers; the pivot trades 15-20 per-outcome wrapper symbols for one symbol whose discriminated `status` field is structurally narrower than wrapper-argument types could have been; per-status unit tests (SNM-30) preserve the per-outcome coverage affordance.

    Alternatives 1, 3, 4, 5, 6 -- PRESERVE their rejection text byte-identical. They remain rejected with their original reasoning per D-15-16.

    Migration section (current lines 158-165) -- REWRITE per D-15-15 with concrete phase numbers:

    - Phase 15 (this ADR): land the type model in `shared/notify.ts` + the compile-check arch test at `tests/architecture/notify-types.test.ts`. No call sites change; V1 wrappers stay intact alongside the new types. `npm run check` GREEN.
    - Phase 16: introduce `notify(ctx, NotificationMessage)` and `notifyUsageError(ctx, UsageErrorMessage)` in `shared/notify.ts` alongside the V1 severity-named wrappers (V1 NOT yet deleted). The internal switch with `assertNever` is the sole grammar site (SNM-17). Per-status unit tests (SNM-30) exercise every variant via a mock `ctx`. Catalog UAT stays GREEN against the V1 callsites unchanged.
    - Phase 17: rewrite `docs/messaging-style-guide.md` to v2.0 (SNM-19) and `docs/output-catalog.md` to the always-marketplace-header spec (SNM-20). Migrate `tests/architecture/catalog-uat.test.ts` to feed structured `NotificationMessage` fixtures through `notify()` via mock ctx (SNM-31). Byte-equality assertion remains the user-contract gate.
    - Phases 18-20: migrate call sites by family -- Phase 18 marketplace orchestrators, Phase 19 plugin orchestrators, Phase 20 edge handlers + `notifyUsageError` (SNM-23). Each phase narrows the MSG-* `files:` globs in `eslint.config.js`. Catalog UAT byte-equality is GREEN for the family at the end of each migration phase.
    - Phase 21: delete the V1 severity-named wrappers (`notifySuccess` / `notifyWarning` / `notifyError`) and the V1 three-argument `notifyUsageError(ctx, msg, usage)` signature from `shared/notify.ts` (SNM-22); delete `tests/lint-rules/` (SNM-24) and `tests/architecture/msg-rule-registry.test.ts` (SNM-25); rewrite or delete `tests/architecture/grammar-frontmatter.test.ts` (SNM-26); swap `eslint.config.js` to stock `no-restricted-syntax` + `no-console` with the `persistence/migrate.ts` per-file override (SNM-27); review `tests/architecture/no-legacy-markers.test.ts` for v2 vocabulary (SNM-28); resolve `shared/grammar/` retain-or-delete (SNM-29). `npm run check` GREEN against the new minimal surface (SNM-32).

    Drift mitigation paragraph: "If the phase numbers shift between this ADR's acceptance and the final teardown, the canonical traceability is `.planning/REQUIREMENTS.md`'s phase-mapping table (Phase column per SNM-* row); this ADR's phase numbers are informative, not binding."

    Open Questions section (current lines 167-170) -- DELETE ENTIRELY per D-15-14. Remove the `## Open questions` heading and both bullet points. Q1 (cascade-section abstraction) is resolved by the single `PluginNotificationMessage` discriminated union plus the always-marketplace-header spec. Q2 (runtime validation of Scope / Reason / StatusToken) is resolved by the compile-enforced discriminated union (Plan 01's type model) + `assertNever` in the Phase 16 `notify()` switch.

    After editing: run `npm run format:check` to verify prettier acceptance of the markdown file; if prettier flags formatting, run `npx prettier --write docs/adr/v2-001-structured-notify.md`, re-verify. Then run `npm run check`. Sanity-grep before commit:

    - Status line: `grep -E "^- \\*\\*Status:\\*\\* Accepted \\(Phase 15, 2026-05-25\\)$" docs/adr/v2-001-structured-notify.md` returns exactly 1.
    - Open Questions deleted: `grep -E "^## Open questions" docs/adr/v2-001-structured-notify.md` returns empty.
    - Migration phase references: `grep -cE "Phase 1[6-9]|Phase 2[01]" docs/adr/v2-001-structured-notify.md` returns at least 6.
    - Alt-2 flip: `grep -c "ACCEPTED (v1.4 design pivot)" docs/adr/v2-001-structured-notify.md` returns 1.
    - Context preserved: `grep -c "V1 ships a stringly-typed user-output surface" docs/adr/v2-001-structured-notify.md` returns 1.
  </action>
  <acceptance_criteria>
    - Source: Status line flipped -- `grep -c "^- \\*\\*Status:\\*\\* Accepted (Phase 15, 2026-05-25)$" docs/adr/v2-001-structured-notify.md` returns exactly 1.
    - Source: `grep -c "^- \\*\\*Status:\\*\\* Proposed" docs/adr/v2-001-structured-notify.md` returns 0 (no leftover Proposed).
    - Source: Title preserved -- `head -1 docs/adr/v2-001-structured-notify.md` is exactly `# ADR-v2-001: Structured \`notify\` payload with typed wrappers`.
    - Source: Context section preserved -- `grep -c "V1 ships a stringly-typed user-output surface" docs/adr/v2-001-structured-notify.md` returns 1.
    - Source: Alternative 2 flipped -- `grep -c "ACCEPTED (v1.4 design pivot)" docs/adr/v2-001-structured-notify.md` returns exactly 1.
    - Source: Alternatives 1, 3, 4, 5, 6 still rejected -- spot-check via `grep -c "Rejected" docs/adr/v2-001-structured-notify.md` returns at least 5.
    - Source: Migration section cites Phase 16-21 -- `grep -cE "Phase 1[6-9]|Phase 2[01]" docs/adr/v2-001-structured-notify.md` returns at least 6.
    - Source: Open Questions section deleted -- `grep -cE "^## Open questions" docs/adr/v2-001-structured-notify.md` returns 0.
    - Source: Decision section names the v1.4 types -- `grep -cE "NotificationMessage|PluginNotificationMessage|MarketplaceNotificationMessage|UsageErrorMessage|PluginStatus|MarketplaceStatus|Dependency|MarketplaceDetails" docs/adr/v2-001-structured-notify.md` returns at least 8.
    - Source: 10-status closed-set referenced -- `grep -cE "installed|updated|reinstalled|uninstalled|available|unavailable|upgradable|failed|skipped|manual recovery" docs/adr/v2-001-structured-notify.md` returns at least 10.
    - Source: always-marketplace-header spec mentioned -- `grep -cE "always-marketplace-header|marketplace header at column 0" docs/adr/v2-001-structured-notify.md` returns at least 1.
    - Source: dropped top-level trailer cited -- `grep -cE "top-level cause-chain trailer is RETIRED|top-level trailer|dropped top-level trailer" docs/adr/v2-001-structured-notify.md` returns at least 1.
    - Source: per-plugin causes cited -- `grep -cE "per-plugin cause|per-plugin causes" docs/adr/v2-001-structured-notify.md` returns at least 1.
    - Behavior: only `docs/adr/v2-001-structured-notify.md` changed -- `git status --porcelain` shows exactly one modified file at that path.
    - Command: `npm run check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && grep -q "^- \*\*Status:\*\* Accepted (Phase 15, 2026-05-25)$" docs/adr/v2-001-structured-notify.md && grep -q "ACCEPTED (v1.4 design pivot)" docs/adr/v2-001-structured-notify.md && ! grep -qE "^## Open questions" docs/adr/v2-001-structured-notify.md && npm run check</automated>
  </verify>
  <done>
    `docs/adr/v2-001-structured-notify.md` carries the refreshed Status (Accepted, Phase 15, 2026-05-25), rewritten Decision + Consequences + Migration sections, flipped Alternative 2, and no Open Questions section. Context section preserved byte-identical. SNM-21 closed; `npm run check` GREEN.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit Plan 03 changes</name>
  <files>docs/adr/v2-001-structured-notify.md</files>
  <read_first>
    - CLAUDE.md (git policy: never commit to main; Conventional Commits; pre-commit run --files before git commit; SKIP=trufflehog inside worktrees; never --no-verify)
    - .planning/phases/15-type-model-adr-refresh/15-RESEARCH.md §"Plan Granularity Recommendation" (Plan 03 commit message skeleton)
    - .planning/phases/15-type-model-adr-refresh/15-RESEARCH.md §"Pitfall 5" (pre-commit hook + worktree handling)
  </read_first>
  <action>
    Confirm we are NOT on `main` via `git rev-parse --abbrev-ref HEAD`. Stage only `docs/adr/v2-001-structured-notify.md` (no `git add -A`). Run `pre-commit run --files docs/adr/v2-001-structured-notify.md`. Re-stage any formatter changes; re-run until clean. If inside a worktree, additionally run `pre-commit run trufflehog --all-files` standalone and confirm clean BEFORE committing.

    Commit with Conventional Commits format. If inside a worktree, prefix with `SKIP=trufflehog`.

    Title (<=72 chars): `docs(adr): refresh v2-001 -- single-notify locked design (SNM-21)`

    Body (lines <=80 chars):
    - "Flip Status from Proposed to Accepted (Phase 15, 2026-05-25). Rewrite"
    - "Decision and Consequences end-to-end to describe the single"
    - "notify(ctx, NotificationMessage) entrypoint plus discriminated-union"
    - "shape on status. Patch Alternative 2 from Rejected to ACCEPTED (v1.4"
    - "design pivot). Rewrite Migration section to cite concrete phase"
    - "numbers (Phase 16-21). Delete the Open Questions section -- Q1 and"
    - "Q2 are resolved by the single union + assertNever exhaustiveness."
    - "" (blank)
    - "Preserve the title line and the Context section byte-identical: the"
    - "v1.3 drift problem description remains accurate post-pivot."
    - "" (blank)
    - "Type names cited (locked in shared/notify.ts by Plan 01):"
    - "NotificationMessage, MarketplaceNotificationMessage,"
    - "PluginNotificationMessage (10-variant union on status), PluginStatus,"
    - "MarketplaceStatus, Dependency, MarketplaceDetails, UsageErrorMessage."
    - "" (blank)
    - "Always-marketplace-header spec change documented (spec rewrite lands"
    - "in Phase 17). Computed severity (SNM-14), computed reload-hint"
    - "(SNM-15), render-time dependency probe (SNM-16), assertNever"
    - "exhaustiveness (SNM-17) all referenced in the Decision section."
    - "" (blank)
    - "Refs: SNM-21"
    - "Phase: 15-type-model-adr-refresh / Plan 03"

    After commit, `git status` clean. Do NOT push.
  </action>
  <acceptance_criteria>
    - Command: `git rev-parse --abbrev-ref HEAD` does NOT print `main`.
    - Behavior: `git log -1 --pretty=%s` length is within 5-72 chars and matches `^docs\(adr\): refresh v2-001 -- single-notify locked design \(SNM-21\)$`.
    - Behavior: `git log -1 --pretty=%B | awk 'length > 80'` returns empty.
    - Behavior: `git log -1 --name-only --pretty=format:"" | grep -v '^$'` lists exactly `docs/adr/v2-001-structured-notify.md`.
    - Behavior: `git status --porcelain` returns empty post-commit.
    - Command: `npm run check` exits 0 against HEAD.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && git log -1 --pretty=%B && git status --porcelain && npm run check</automated>
  </verify>
  <done>
    Exactly one Conventional-Commits commit on the active GSD branch (not main) lands the ADR refresh; pre-commit hooks passed without `--no-verify`; `npm run check` GREEN against HEAD; SNM-21 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 15 Plan 03 edits a single markdown ADR. No code paths, no user input, no network, no disk mutation outside the planner's commit. Threat surface is N/A. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | docs/adr/v2-001-structured-notify.md Context section | mitigate | D-15-13 LOCKS the Context section as preserved byte-identical. Acceptance criterion grep verifies the key phrase "V1 ships a stringly-typed user-output surface" survives. RESEARCH §"Pitfall 6" calls out this carve-out explicitly. |
| T-15-05 | Repudiation | ADR Status change | accept | Conventional Commits commit message records the transition Proposed -> Accepted (Phase 15, 2026-05-25) with the commit SHA. Acceptable per RESEARCH §"Security Domain". |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Phase 15 Plan 03 installs ZERO new packages. No supply-chain surface added. |
</threat_model>

<verification>
**Plan-level checks (in addition to per-task `<verify>`):**

1. `cd /home/acolomba/pi-claude-marketplace && npm run check` exits 0 against HEAD.
2. Status flip verified: `grep -E "^- \\*\\*Status:\\*\\* Accepted \\(Phase 15, 2026-05-25\\)$" docs/adr/v2-001-structured-notify.md` returns 1; `grep -E "^- \\*\\*Status:\\*\\* Proposed" docs/adr/v2-001-structured-notify.md` returns 0.
3. Open Questions section deleted: `grep -E "^## Open questions" docs/adr/v2-001-structured-notify.md` returns 0.
4. Phase 16-21 references present: `grep -cE "Phase 1[6-9]|Phase 2[01]" docs/adr/v2-001-structured-notify.md` returns at least 6.
5. Alternative 2 flipped: `grep -c "ACCEPTED (v1.4 design pivot)" docs/adr/v2-001-structured-notify.md` returns 1.
6. Context preserved: `grep -c "V1 ships a stringly-typed user-output surface" docs/adr/v2-001-structured-notify.md` returns 1.
7. Phase-level Success Criterion #4 STILL holds at end of Plan 03: `git grep -nE "PluginNotificationMessage|MarketplaceNotificationMessage|UsageErrorMessage|NotificationMessage|PluginStatus|MarketplaceStatus|MarketplaceDetails" -- 'extensions/' ':!extensions/pi-claude-marketplace/shared/notify.ts'` returns empty (no `extensions/` files reference the new types; the only references are in `tests/architecture/notify-types.test.ts` per Plan 02 and `docs/adr/v2-001-structured-notify.md` per Plan 03).
8. `git log -1 --name-only --pretty=format:"" | grep -v '^$'` lists exactly one file: `docs/adr/v2-001-structured-notify.md`.
</verification>

<success_criteria>
- Status line is `- **Status:** Accepted (Phase 15, 2026-05-25)`; no leftover `Proposed` status anywhere in the file.
- Title and Context section preserved byte-identical (D-15-13 carve-out).
- Decision and Consequences sections rewritten to reflect single-`notify` design + computed severity + computed reload-hint + render-time dependency probe + per-plugin causes + always-marketplace-header spec change.
- Alternative 2 flipped to ACCEPTED (v1.4 design pivot) with rationale; Alternatives 1, 3, 4, 5, 6 still rejected.
- Migration section cites Phase 16-21 concretely; drift mitigation paragraph points to `.planning/REQUIREMENTS.md`'s traceability table.
- Open Questions section deleted entirely.
- `npm run check` GREEN.
- Exactly one Conventional-Commits commit; branch is NOT `main`; pre-commit hooks ran clean.
- SNM-21 closed.
</success_criteria>

<output>
Create `.planning/phases/15-type-model-adr-refresh/15-03-SUMMARY.md` when done. The summary records:

1. The exact new Status line text (proves SNM-21 closure).
2. The line range of the preserved Context section (proves D-15-13 carve-out honored).
3. The 6+ Phase 16-21 references in the Migration section (proves D-15-15).
4. Confirmation that Alternative 2 carries the "ACCEPTED (v1.4 design pivot)" marker (proves D-15-16).
5. Confirmation that the Open Questions section is absent (proves D-15-14).
6. Confirmation Success Criterion #4 still holds (grep returns empty).
7. Commit SHA + branch name.
8. Plan-level wrap-up: all three Phase 15 plans complete; phase moves to verification.
</output>
