---
phase: 15-type-model-adr-refresh
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - tests/architecture/notify-types.test.ts
autonomous: true
requirements:
  - SNM-01
  - SNM-02
  - SNM-03
  - SNM-04
  - SNM-05
  - SNM-06
  - SNM-07
  - SNM-08
  - SNM-09
  - SNM-10
  - SNM-11

must_haves:
  truths:
    - "tests/architecture/notify-types.test.ts ships compile-time `type _Assert_*` proofs covering closed-set membership (D-15-11 tuple lengths), bidirectional PluginStatus round-trip (D-15-11 + SNM-04 forward + backward), and per-variant structural invariants for cause? / rollbackPartial? / scope? / dependencies / reasons / from-to per D-15-12."
    - "Negative-presence assertions (cause absent on 8 variants, rollbackPartial absent on 9 variants, scope absent on `available` / `unavailable`) use `@ts-expect-error` directives so a future commit that mistakenly adds the absent field causes typecheck to flag the unused directive."
    - "The file contains a trivial `test(...)` node:test block with `assert.equal(1, 1)` body so the runner counts the file in `npm run check` output (D-15-10; matches `grammar-frontmatter.test.ts` precedent)."
    - "npm run check stays GREEN; if any `_Assert_*` type fails to resolve to `true`, `npm run typecheck` exits non-zero and the failure surfaces in CI."
  artifacts:
    - path: "tests/architecture/notify-types.test.ts"
      provides: "Closed-system compile-time proof of SNM-01..SNM-11 + per-variant invariants from D-15-12"
      contains: "type _Assert_PluginStatusForward; type _Assert_PluginStatusBackward; type _Assert_PluginStatusesLen; type _Assert_MarketplaceStatusesLen; type _Assert_DependenciesLen; type _Assert_CausePresent; type _Assert_RollbackOnFailed; type _Assert_DepsRequired; type _Assert_ReasonsOnly5; type _Assert_FromToUpdated; @ts-expect-error directives for negative-presence; test(...) block with assert.equal(1, 1)"
  key_links:
    - from: "tests/architecture/notify-types.test.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts"
      via: "import { PLUGIN_STATUSES, MARKETPLACE_STATUSES, DEPENDENCIES, type PluginStatus, type MarketplaceStatus, type Dependency, type PluginNotificationMessage, type MarketplaceNotificationMessage, type NotificationMessage, type MarketplaceDetails, type UsageErrorMessage }"
      pattern: "from \"\\.\\./\\.\\./extensions/pi-claude-marketplace/shared/notify\\.ts\""
---

<objective>
Add `tests/architecture/notify-types.test.ts` to lock the v1.4 type model's closed-set membership and per-variant structural invariants at compile time. This is the SOLE consumer of the new types in Phase 15 and the only file in the codebase that imports them outside `shared/notify.ts`.

Purpose: Without this file nothing in the type set is detectable as broken. SNM-04's "PluginStatus derived via indexed access" round-trip cannot drift silently if the tuple and the variant `status` literals fall out of sync (RESEARCH §"Pitfall 1"). Per-variant invariants from D-15-12 (cause? only on `failed`/`manual recovery`; rollbackPartial? only on `failed`; scope? absent on `available`/`unavailable`; dependencies required on 3 variants; reasons required on 5 variants; from/to only on `updated`) become typecheck-enforced.

Output: A single new file at `tests/architecture/notify-types.test.ts` that compiles when the type model is correct and FAILS `npm run typecheck` if any invariant breaks. `npm run check` GREEN at end of plan.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-type-model-adr-refresh/15-CONTEXT.md
@.planning/phases/15-type-model-adr-refresh/15-RESEARCH.md
@.planning/phases/15-type-model-adr-refresh/15-VALIDATION.md
@.planning/phases/15-type-model-adr-refresh/15-01-PLAN.md
@extensions/pi-claude-marketplace/shared/notify.ts
@tests/architecture/grammar-frontmatter.test.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/architecture/notify-types.test.ts with closed-set + per-variant invariant proofs</name>
  <files>tests/architecture/notify-types.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (current file shipped by Plan 01; the import surface this test consumes -- read to capture the exact exported symbol names and per-variant field shapes)
    - .planning/phases/15-type-model-adr-refresh/15-CONTEXT.md (locked decisions D-15-10 separate file at `tests/architecture/notify-types.test.ts`, D-15-11 const tuples + derived types, D-15-12 per-variant invariants to lock)
    - .planning/phases/15-type-model-adr-refresh/15-RESEARCH.md §"Pattern 3" (architecture-test file shape -- node:test import + `type _Assert_*` blocks + trivial `assert.equal(1, 1)` body), §"Code Examples" Example 5 (the exact compile-check file template), §"Pitfall 1" (SNM-04 round-trip forward+backward), §"Pitfall 2" (`"manual recovery"` literal with space), §"Pitfall 3" (negative-presence requires `@ts-expect-error`)
    - .planning/REQUIREMENTS.md (SNM-01..SNM-11 -- every requirement must have at least one `_Assert_*` block proving its shape)
    - tests/architecture/grammar-frontmatter.test.ts (PATTERN REFERENCE -- established architecture-test file shape: node:test import, `import assert from "node:assert/strict"`, real `test("…", () => assert.deepEqual(…))` body; for Phase 15 the `_Assert_*` types do the work and the runtime body is a trivial `assert.equal(1, 1)` so node:test counts the file per D-15-10)
    - tsconfig.json (verify `"strict": true` and `"noUncheckedIndexedAccess": true` are enabled -- research A1 confirms both; the `@ts-expect-error` negative-presence idiom requires strict to be effective)
  </read_first>
  <action>
    Create a NEW file `tests/architecture/notify-types.test.ts`. The file's structure: import block, top-of-file JSDoc header citing SNM-01..SNM-11 + D-15-10..D-15-12 + the pattern reference at `grammar-frontmatter.test.ts`, then a series of named `type _Assert_*` blocks each followed by a `const _<short>: _Assert_* = true;` assignment (the assignment fails to typecheck if the conditional type resolves to `never`), then a single `test(...)` block at the bottom with body `assert.equal(1, 1);` so node:test counts the file (D-15-10).

    Import block (use the path style of `tests/architecture/grammar-frontmatter.test.ts:1-14` -- relative `../../extensions/pi-claude-marketplace/shared/notify.ts` with explicit `.ts` extension; mix runtime + `type` imports in one statement per `compact-line.ts` precedent OR split into one runtime + one `import type` statement, whichever satisfies ESLint import-x rules):
    - `import assert from "node:assert/strict";`
    - `import test from "node:test";`
    - From `../../extensions/pi-claude-marketplace/shared/notify.ts`: runtime values `DEPENDENCIES`, `MARKETPLACE_STATUSES`, `PLUGIN_STATUSES`; types `Dependency`, `MarketplaceDetails`, `MarketplaceNotificationMessage`, `MarketplaceStatus`, `NotificationMessage`, `PluginNotificationMessage`, `PluginStatus`, `UsageErrorMessage`.

    Top-of-file JSDoc comment summarizing: "Phase 15 / SNM-01..SNM-11 / D-15-10..D-15-12 -- closed-system compile-time proof. Type-level `_Assert_*` types resolve to `true` when invariants hold and `never` when they break; the `const _<short>: _Assert_* = true;` assignment is the load-bearing typecheck. Negative-presence assertions use `@ts-expect-error` per RESEARCH §"Pitfall 3". Trivial `assert.equal(1, 1)` body anchors the file to `node:test` so failures surface in `npm run check`."

    Closed-set membership assertions:
    - `_Assert_PluginStatusForward = PluginStatus extends PluginNotificationMessage["status"] ? true : never;` + `const _pf: _Assert_PluginStatusForward = true;`
    - `_Assert_PluginStatusBackward = PluginNotificationMessage["status"] extends PluginStatus ? true : never;` + `const _pb: _Assert_PluginStatusBackward = true;` (BOTH directions per RESEARCH §"Pitfall 1" -- drop either and SNM-04 silently breaks).
    - `_Assert_PluginStatusesLen = (typeof PLUGIN_STATUSES)["length"] extends 10 ? true : never;` + `const _l1: _Assert_PluginStatusesLen = true;`.
    - `_Assert_MarketplaceStatusesLen = (typeof MARKETPLACE_STATUSES)["length"] extends 4 ? true : never;` + `const _l2: ...`.
    - `_Assert_DependenciesLen = (typeof DEPENDENCIES)["length"] extends 2 ? true : never;` + `const _l3: ...`.
    - Membership proofs for the literal values: `_Assert_PluginStatusValues = "installed" | "updated" | "reinstalled" | "uninstalled" | "available" | "unavailable" | "upgradable" | "failed" | "skipped" | "manual recovery" extends PluginStatus ? PluginStatus extends "installed" | "updated" | "reinstalled" | "uninstalled" | "available" | "unavailable" | "upgradable" | "failed" | "skipped" | "manual recovery" ? true : never : never;` (proves SNM-03 exact 10-variant set + D-15-11). Mirror for `MarketplaceStatus` (added/removed/updated/failed -- proves SNM-05 + D-15-07) and `Dependency` (agents/mcp -- proves SNM-06).

    Top-level shape assertions:
    - `_Assert_NotificationMessageShape = NotificationMessage extends { readonly marketplaces: readonly MarketplaceNotificationMessage[] } ? { readonly marketplaces: readonly MarketplaceNotificationMessage[] } extends NotificationMessage ? true : never : never;` (proves SNM-01 -- exactly `marketplaces` field, no `severity`, no `trailer`).
    - Negative for `severity` on `NotificationMessage`: `// @ts-expect-error -- SNM-01: NotificationMessage has NO severity field` then `type _NoSeverityOnNotificationMessage = NotificationMessage["severity"];`.
    - Negative for `trailer` on `NotificationMessage`: `// @ts-expect-error -- SNM-01: NotificationMessage has NO trailer field` then `type _NoTrailerOnNotificationMessage = NotificationMessage["trailer"];`.
    - `_Assert_MarketplaceMessageShape = MarketplaceNotificationMessage extends { readonly name: string; readonly scope: import("../../extensions/pi-claude-marketplace/shared/types.ts").Scope; readonly status?: MarketplaceStatus; readonly details?: MarketplaceDetails; readonly plugins: readonly PluginNotificationMessage[]; } ? true : never;` (proves SNM-02 + D-15-06).
    - `_Assert_MarketplaceDetailsShape = MarketplaceDetails extends { readonly autoupdate: boolean; readonly lastUpdatedAt?: string } ? { readonly autoupdate: boolean; readonly lastUpdatedAt?: string } extends MarketplaceDetails ? true : never : never;` (proves SNM-07 + D-15-05 -- exactly two fields, no `source`, no `version`).
    - `_Assert_UsageErrorMessageShape = UsageErrorMessage extends { readonly message: string; readonly usage: string } ? { readonly message: string; readonly usage: string } extends UsageErrorMessage ? true : never : never;` (proves SNM-08 -- no `cause`, no `severity`).
    - Negative for `cause` on `UsageErrorMessage`: `// @ts-expect-error -- SNM-08: UsageErrorMessage has NO cause field` then `type _NoCauseOnUsageError = UsageErrorMessage["cause"];`.
    - Negative for `severity` on `UsageErrorMessage`: `// @ts-expect-error -- SNM-08: UsageErrorMessage has NO severity field` then `type _NoSeverityOnUsageError = UsageErrorMessage["severity"];`.

    Per-variant invariants (D-15-12) -- one assertion per rule:
    - cause? present on `failed` and `manual recovery` (SNM-10): `_Assert_CauseOnFailed = Extract<PluginNotificationMessage, { status: "failed" }>["cause"] extends Error | undefined ? true : never;` + assign; same for `"manual recovery"`.
    - cause? absent on the other 8 variants (SNM-10 + D-15-12) -- one `@ts-expect-error` per variant: `installed`, `updated`, `reinstalled`, `uninstalled`, `available`, `unavailable`, `upgradable`, `skipped`. Each follows the pattern `// @ts-expect-error -- SNM-10: <variant> has NO cause field` then `type _NoCauseOn<Variant> = Extract<PluginNotificationMessage, { status: "<status>" }>["cause"];`.
    - rollbackPartial? present on `failed` (SNM-09): `_Assert_RollbackOnFailed = Extract<PluginNotificationMessage, { status: "failed" }> extends { rollbackPartial?: readonly { phase: string; cause?: Error }[] } ? true : never;` + assign.
    - rollbackPartial? absent on the other 9 variants -- one `@ts-expect-error` per variant.
    - dependencies REQUIRED (not optional) on `installed` / `updated` / `reinstalled` (SNM-06 + D-15-02): `_Assert_DepsRequiredInstalled = Extract<PluginNotificationMessage, { status: "installed" }>["dependencies"] extends readonly Dependency[] ? true : never;` + assign; same for `updated` and `reinstalled`. To prove REQUIRED (not optional): `_Assert_DepsNotOptionalInstalled = undefined extends Extract<PluginNotificationMessage, { status: "installed" }>["dependencies"] ? never : true;` + assign; same for `updated` and `reinstalled`.
    - dependencies absent on the other 7 variants -- one `@ts-expect-error` each.
    - reasons REQUIRED on the 5 status-with-{reason} variants per D-15-01 (`unavailable`, `upgradable`, `skipped`, `failed`, `manual recovery`): `_Assert_ReasonsRequiredUnavailable = Extract<PluginNotificationMessage, { status: "unavailable" }>["reasons"] extends readonly import("../../extensions/pi-claude-marketplace/shared/grammar/reasons.ts").Reason[] ? true : never;` + assign; repeat for the other 4 variants. To prove REQUIRED: `_Assert_ReasonsNotOptionalUnavailable = undefined extends Extract<PluginNotificationMessage, { status: "unavailable" }>["reasons"] ? never : true;` + assign; repeat for the other 4.
    - reasons absent on the other 5 variants -- one `@ts-expect-error` each: `installed`, `updated`, `reinstalled`, `uninstalled`, `available`.
    - scope? absent on `available` and `unavailable` per SNM-11 -- two `@ts-expect-error` blocks: `type _NoScopeOnAvailable = Extract<PluginNotificationMessage, { status: "available" }>["scope"];` and `type _NoScopeOnUnavailable = Extract<PluginNotificationMessage, { status: "unavailable" }>["scope"];`.
    - scope? present on the other 8 variants (SNM-11 + D-15-12): `_Assert_ScopeOnInstalled = Extract<PluginNotificationMessage, { status: "installed" }>["scope"] extends import("../../extensions/pi-claude-marketplace/shared/types.ts").Scope | undefined ? true : never;` + assign; repeat for `updated`, `reinstalled`, `uninstalled`, `upgradable`, `failed`, `skipped`, `manual recovery`.
    - from / to REQUIRED on `updated` (D-15-04): `_Assert_FromOnUpdated = Extract<PluginNotificationMessage, { status: "updated" }>["from"] extends string ? true : never;` + assign; same for `to`. Negative: `from` / `to` absent on the other 9 variants -- one `@ts-expect-error` each for `from`, plus one each for `to` (18 directives total; OR consolidate into a single negative-presence helper if it reads cleanly).
    - version OPTIONAL on the 9 listed variants per D-15-04: `_Assert_VersionOnInstalled = Extract<PluginNotificationMessage, { status: "installed" }>["version"] extends string | undefined ? true : never;` + assign; repeat for `uninstalled`, `reinstalled`, `available`, `unavailable`, `upgradable`, `failed`, `skipped`, `manual recovery`. The `updated` variant uses `from`/`to` not `version` -- covered above.

    Trailing runtime block: `test("Phase 15 / SNM-01..SNM-11 / D-15-12: notify type model invariants hold at compile time", () => { assert.equal(1, 1); });` with a comment above noting "type-level `_Assert_*` assignments above carry the proof; this body exists so node:test counts the file in `npm run check` output (D-15-10; mirrors `tests/architecture/grammar-frontmatter.test.ts` precedent)".

    Run `npm run typecheck` after writing. If any `const _xx: _Assert_xx = true;` errors with "Type 'never' is not assignable to type 'true'", the type model in `shared/notify.ts` is broken -- surface the failure with the failing assertion's name so the executor can trace back to the variant. If any `// @ts-expect-error` directive reports as "unused", the corresponding field is mistakenly present on a variant where SNM/D-15 says it must be absent -- surface and fix in `shared/notify.ts` (this is the load-bearing failure mode the file is designed to catch).

    Then run `npm run check`. Fix any prettier/eslint violations. Do NOT add `// eslint-disable-*` directives (the `@ts-expect-error` directives are NOT lint disables -- they are TypeScript directives and are expected).
  </action>
  <acceptance_criteria>
    - Source: `test -f tests/architecture/notify-types.test.ts` returns 0 (file exists).
    - Source: file imports from `../../extensions/pi-claude-marketplace/shared/notify.ts` -- `grep -c "from \"\\.\\./\\.\\./extensions/pi-claude-marketplace/shared/notify\\.ts\"" tests/architecture/notify-types.test.ts` returns at least 1.
    - Source: round-trip assertions for SNM-04 present BOTH directions -- `grep -c "_Assert_PluginStatusForward" tests/architecture/notify-types.test.ts` >= 1 AND `grep -c "_Assert_PluginStatusBackward" tests/architecture/notify-types.test.ts` >= 1 AND BOTH have a `const _<short>: _Assert_PluginStatus<Forward|Backward> = true;` assignment.
    - Source: tuple-length assertions present for all 3 const arrays -- `grep -cE "_Assert_(Plugin|Marketplace)StatusesLen|_Assert_DependenciesLen" tests/architecture/notify-types.test.ts` returns 3.
    - Source: `@ts-expect-error` directives cover at minimum the 8 cause-absent variants + 9 rollbackPartial-absent variants + 7 dependencies-absent variants + 5 reasons-absent variants + scope-absent `available` + scope-absent `unavailable` + severity/trailer absent on `NotificationMessage` + cause/severity absent on `UsageErrorMessage`. `grep -c "^// @ts-expect-error" tests/architecture/notify-types.test.ts` returns at least 35.
    - Source: file contains exactly one `test(...)` block with a trivial body -- `grep -cE "^test\(" tests/architecture/notify-types.test.ts` returns 1; `grep -c "assert.equal(1, 1)" tests/architecture/notify-types.test.ts` returns 1.
    - Source: file is consumed by the `npm test` glob -- the `package.json` script line `"test": "node --test \"tests/{architecture,...}\"` includes `architecture`; `node --test tests/architecture/notify-types.test.ts` exits 0 (proves node:test parses + runs the file).
    - Source: SNM-04 round-trip drift detection works -- temporarily editing a `status: "installed"` literal in `shared/notify.ts` to `status: "instaled"` causes BOTH `_Assert_PluginStatusForward` and/or `_Assert_PluginStatusBackward` to resolve to `never` and the `const _pf: _Assert_PluginStatusForward = true;` assignment to fail typecheck. (Verify by reasoning, not by editing -- the assertion shape IS the proof.)
    - Behavior: Success Criterion #4 still holds -- `git grep -nE "PluginNotificationMessage|MarketplaceNotificationMessage|UsageErrorMessage|NotificationMessage|PluginStatus|MarketplaceStatus|MarketplaceDetails" -- 'extensions/' ':!extensions/pi-claude-marketplace/shared/notify.ts'` returns empty (the new file lives under `tests/architecture/`, NOT `extensions/`, so the grep against `extensions/` stays clean).
    - Command: `npm run typecheck` exits 0.
    - Command: `node --test tests/architecture/notify-types.test.ts` exits 0.
    - Command: `npm run check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run typecheck && node --test tests/architecture/notify-types.test.ts && npm run check</automated>
  </verify>
  <done>
    `tests/architecture/notify-types.test.ts` ships with closed-set membership proofs, bidirectional SNM-04 round-trip, and one `_Assert_*` block per D-15-12 invariant (positive + negative-presence via `@ts-expect-error`). `npm run check` GREEN; the file is the SOLE consumer of the new types in Phase 15.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit Plan 02 changes</name>
  <files>tests/architecture/notify-types.test.ts</files>
  <read_first>
    - CLAUDE.md (git policy: never commit to main; Conventional Commits; pre-commit run --files before git commit; SKIP=trufflehog inside worktrees; never --no-verify)
    - .planning/phases/15-type-model-adr-refresh/15-RESEARCH.md §"Plan Granularity Recommendation" (Plan 02 commit message skeleton)
    - .planning/phases/15-type-model-adr-refresh/15-RESEARCH.md §"Pitfall 5" (pre-commit hook + worktree handling)
    - .planning/phases/15-type-model-adr-refresh/15-01-PLAN.md (precedent for the commit shape and worktree handling -- same branch policy applies)
  </read_first>
  <action>
    Confirm we are NOT on `main` via `git rev-parse --abbrev-ref HEAD`. Stage only `tests/architecture/notify-types.test.ts` (no `git add -A`, no other files). Run `pre-commit run --files tests/architecture/notify-types.test.ts`; re-stage any formatter changes; re-run until clean. If inside a worktree, also run `pre-commit run trufflehog --all-files` standalone and confirm clean.

    Commit with Conventional Commits format. If inside a worktree, prefix with `SKIP=trufflehog`.

    Title (<=72 chars): `test(notify): lock v1.4 type invariants via compile-time _Assert_*`

    Body (lines <=80 chars):
    - "Add tests/architecture/notify-types.test.ts as the closed-system"
    - "compile-time proof of SNM-01..SNM-11 + D-15-12 invariants."
    - "" (blank)
    - "Locked assertions:"
    - "- Bidirectional SNM-04 round-trip (PluginStatus <-> union[status])"
    - "  catches drift between PLUGIN_STATUSES tuple and variant literals."
    - "- Tuple length assertions on PLUGIN_STATUSES (10), MARKETPLACE_STATUSES"
    - "  (4), DEPENDENCIES (2)."
    - "- Per-variant presence proofs for cause? (failed / manual recovery),"
    - "  rollbackPartial? (failed), dependencies (installed / updated /"
    - "  reinstalled), reasons (5 status-with-{reason} variants), from/to"
    - "  (updated), scope? (8 of 10 variants)."
    - "- Negative-presence via @ts-expect-error for cause / rollbackPartial /"
    - "  dependencies / reasons / scope / from / to / version / severity /"
    - "  trailer on the variants where they MUST be absent."
    - "" (blank)
    - "Trivial assert.equal(1, 1) anchors the file to node:test so failures"
    - "surface in npm run check (D-15-10; matches grammar-frontmatter.test.ts"
    - "precedent)."
    - "" (blank)
    - "Refs: SNM-01, SNM-02, SNM-03, SNM-04, SNM-05, SNM-06, SNM-07, SNM-08,"
    - "      SNM-09, SNM-10, SNM-11"
    - "Phase: 15-type-model-adr-refresh / Plan 02"

    After commit, `git status` clean. Do NOT push.
  </action>
  <acceptance_criteria>
    - Command: `git rev-parse --abbrev-ref HEAD` does NOT print `main`.
    - Behavior: `git log -1 --pretty=%s` length is within 5-72 chars and matches `^test\(notify\): lock v1\.4 type invariants via compile-time _Assert_\*$`.
    - Behavior: `git log -1 --pretty=%B | awk 'length > 80'` returns empty.
    - Behavior: `git log -1 --name-only --pretty=format:"" | grep -v '^$'` lists exactly `tests/architecture/notify-types.test.ts`.
    - Behavior: `git status --porcelain` returns empty post-commit.
    - Command: `npm run check` exits 0 against HEAD.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && git log -1 --pretty=%B && git status --porcelain && npm run check</automated>
  </verify>
  <done>
    Exactly one Conventional-Commits commit on the active GSD branch (not main) lands the new arch-test file; pre-commit hooks passed without `--no-verify`; `npm run check` GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 15 Plan 02 adds a single compile-time arch test consuming only types and runtime tuples from `shared/notify.ts`. No user input, no network, no disk mutation outside the planner's commit. Threat surface is N/A. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Tampering | tests/architecture/notify-types.test.ts (`@ts-expect-error` discipline) | mitigate | RESEARCH §"Pitfall 3" / A1: `tsconfig.json` `strict: true` is already enabled (verified). A future commit that mistakenly drops a `@ts-expect-error` directive while the field IS still absent fires "Unused @ts-expect-error" at typecheck. Acceptance criterion grep enforces the directive count floor (>=35). |
| T-15-03 | Information Disclosure | Negative-presence assertion failure surfaces real-cause locations | accept | The diagnostic comments on each `@ts-expect-error` cite the SNM-* requirement number; no PII, no path leakage. Acceptable per RESEARCH §"Security Domain". |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Phase 15 Plan 02 installs ZERO new packages. No supply-chain surface added. |
</threat_model>

<verification>
**Plan-level checks (in addition to per-task `<verify>`):**

1. `cd /home/acolomba/pi-claude-marketplace && npm run check` exits 0 against HEAD.
2. `node --test tests/architecture/notify-types.test.ts` exits 0 (proves node:test counts the file; the `assert.equal(1, 1)` body runs).
3. Success Criterion #4 still holds at end of Plan 02: `git grep -nE "PluginNotificationMessage|MarketplaceNotificationMessage|UsageErrorMessage|NotificationMessage|PluginStatus|MarketplaceStatus|MarketplaceDetails" -- 'extensions/' ':!extensions/pi-claude-marketplace/shared/notify.ts'` returns empty. (The arch test lives under `tests/architecture/`, NOT `extensions/`; the grep is correctly scoped.)
4. `git log -1 --name-only --pretty=format:"" | grep -v '^$'` lists exactly one file: `tests/architecture/notify-types.test.ts`.
5. Drift detection sanity check (NOT a CI test, but the planner-checker should be able to reason about it): If `shared/notify.ts` were to drop the `dependencies: readonly Dependency[]` field from `PluginInstalledMessage`, `_Assert_DepsRequiredInstalled` would resolve to `never` and `const _drI: _Assert_DepsRequiredInstalled = true;` would fail `npm run typecheck`. This is the load-bearing failure mode.
</verification>

<success_criteria>
- `tests/architecture/notify-types.test.ts` exists with all `_Assert_*` types from D-15-12 + closed-set membership + bidirectional SNM-04 round-trip + negative-presence `@ts-expect-error` discipline.
- `npm run check` GREEN.
- Success Criterion #4 holds at end of Plan 02: zero call-site references to the new types in `extensions/` (the only consumer ships under `tests/architecture/`).
- Exactly one Conventional-Commits commit; branch is NOT `main`; pre-commit hooks ran clean.
- File is picked up by `npm test` (covered by the `tests/architecture/**/*.test.ts` glob in `package.json:76`).
</success_criteria>

<output>
Create `.planning/phases/15-type-model-adr-refresh/15-02-SUMMARY.md` when done. The summary records:

1. Total `_Assert_*` block count and total `@ts-expect-error` directive count.
2. Confirmation that the file imports the 11 new symbols (PLUGIN_STATUSES, MARKETPLACE_STATUSES, DEPENDENCIES, PluginStatus, MarketplaceStatus, Dependency, PluginNotificationMessage, MarketplaceNotificationMessage, NotificationMessage, MarketplaceDetails, UsageErrorMessage) from `shared/notify.ts`.
3. Confirmation that `node --test tests/architecture/notify-types.test.ts` exits 0 and the runner output names the trivial-body test.
4. Confirmation Success Criterion #4 still holds (grep against `extensions/` returns empty).
5. Commit SHA + branch name.
</output>
