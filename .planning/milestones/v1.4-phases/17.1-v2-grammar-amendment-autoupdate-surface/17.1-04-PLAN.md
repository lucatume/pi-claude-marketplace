---
phase: 17.1-v2-grammar-amendment-autoupdate-surface
plan: 4
type: execute
wave: 3
depends_on: ["17.1-02"]
files_modified:
  - docs/adr/v2-001-structured-notify.md
autonomous: true
requirements: []
tags: [adr, documentation, autoupdate, marketplace-status, amendment]
must_haves:
  truths:
    - "ADR Decision section reflects the 7-entry MarketplaceStatus closed set in place (lines 35, 42, 88 per D-17.1-07)."
    - "ADR has a new `## Amendment: Phase 17.1 ({YYYY-MM-DD})` section appended after the existing `## Migration` section (per D-17.1-07)."
    - "The Amendment section covers (a) what changed (3 statuses + optional reasons field), (b) why (V1 distinction lost in Phase 17 + user-locked design D-18-05), (c) severity / reload-hint ladder updates (per specifics block)."
    - "The Consequences / Migration / Alternatives sections are byte-identical to before (per D-17.1-08)."
    - "Edit C's inline comment expansion lists all 7 marketplace status literals verbatim (per D-17.1-07; checker Issue #7 -- no shorter-summary or placeholder-comment fallback)."
    - "`npm run check` exits 0; no test or arch gate consumes the ADR text but Prettier formatting must pass."
  artifacts:
    - path: "docs/adr/v2-001-structured-notify.md"
      provides: "Updated Decision section + new Amendment section"
      contains: "## Amendment: Phase 17.1"
  key_links:
    - from: "ADR Decision section MarketplaceStatus references"
      to: "extensions/pi-claude-marketplace/shared/notify.ts MARKETPLACE_STATUSES tuple"
      via: "documentation-of-implementation pointer"
      pattern: "MarketplaceStatus.*7"
    - from: "ADR Amendment section"
      to: "Phase 18 CONTEXT.md D-18-05"
      via: "cross-reference to the user-locked design that motivated the amendment"
      pattern: "D-18-05"
---

<objective>
Amend `docs/adr/v2-001-structured-notify.md` to reflect the 7-entry `MarketplaceStatus` closed set and append a new `## Amendment: Phase 17.1` section explaining what changed and why. This plan is independent of Plan 17.1-03 within Wave 3 (per D-17.1-01) and can land in parallel.

Purpose: Preserve the ADR as the single source of truth for the V2 grammar design. The Decision section reflects the CURRENT state (count + literals); the new Amendment section preserves the HISTORICAL "why did this change" narrative without rewriting the accepted reasoning in Consequences / Migration / Alternatives (per D-17.1-08).

Output:
- ADR Decision section's count + literal references updated in place from 4 entries to 7 entries (lines 35, 42, 88 per the CONTEXT.md line refs).
- New `## Amendment: Phase 17.1 ({YYYY-MM-DD})` section appended at the bottom of the ADR (after `## Migration`).
- Consequences / Migration / Alternatives sections are byte-identical (per D-17.1-08).
- `npm run check` exits 0.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md
@.planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-02-SUMMARY.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-CONTEXT.md
@docs/adr/v2-001-structured-notify.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: In-place Decision section updates (lines 35, 42, 88)</name>
  <files>docs/adr/v2-001-structured-notify.md</files>
  <read_first>
    - docs/adr/v2-001-structured-notify.md (lines 18-127 -- the full Decision section; specifically lines 35, 42, 88 per CONTEXT.md; line 88 carries the `status?: MarketplaceStatus; // "added" | "removed" | "updated" | "failed"` inline comment that needs the 3 new literals appended)
    - .planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md (D-17.1-07 -- in-place + Amendment-section dual approach; D-17.1-08 -- Consequences/Migration/Alternatives NOT refreshed)
  </read_first>
  <action>
    Per D-17.1-07: three in-place edits to the Decision section. Use grep to verify the exact location of each before edit -- line numbers from CONTEXT.md are 2026-05-26 snapshots and may drift by 1-2 lines.

    Edit A (around line 35 -- the public-surface block): `export type MarketplaceStatus; // 4 literal strings, derived from MARKETPLACE_STATUSES tuple` → change `4 literal strings` to `7 literal strings`. The rest of the line is byte-identical.

    Edit B (around line 42 -- the runtime-tuples block): `export const MARKETPLACE_STATUSES;  // 4 entries` → change `// 4 entries` to `// 7 entries`. The rest of the line (including the leading whitespace) is byte-identical.

    Edit C (around line 88 -- the interface-shape block, inside the ```ts``` code fence at lines 81-104): `readonly status?: MarketplaceStatus; // "added" | "removed" | "updated" | "failed"` → change the inline comment to reflect the 7 literals: `readonly status?: MarketplaceStatus; // "added" | "removed" | "updated" | "failed" | "autoupdate enabled" | "autoupdate disabled" | "skipped"`. The field declaration `readonly status?: MarketplaceStatus;` is byte-identical; only the inline `//` comment widens.

    CRITICAL -- Edit C literal-list discipline (per checker Issue #7): D-17.1-07 mandates that the literal list stay visible in the Decision section so reviewers can cross-check against the PRD without leaving the ADR. The full 7-literal list MUST appear verbatim:

        // "added" | "removed" | "updated" | "failed" | "autoupdate enabled" | "autoupdate disabled" | "skipped"

    If the comment becomes too long for Prettier's max-line-width to honor, SPLIT the comment across multiple lines (one literal per line, or grouped) -- that is acceptable. What is NOT acceptable is collapsing the list to a shorter-summary form or a placeholder comment such as `// 7 literal strings (see MARKETPLACE_STATUSES)`. The fallback to "shorter form" that the previous draft of this plan suggested has been REMOVED per checker Issue #7. The full literal list, line-split if necessary, is mandatory. Prettier line-length adjustment is OK; literal-list collapse is NOT OK.

    Acceptable line-split examples (executor chooses based on Prettier output):

        // "added" | "removed" | "updated" | "failed"
        // | "autoupdate enabled" | "autoupdate disabled" | "skipped"

    or use a multi-line block comment:

        /*
         * "added" | "removed" | "updated" | "failed" |
         * "autoupdate enabled" | "autoupdate disabled" | "skipped"
         */

    Either form satisfies D-17.1-07 "literal list visible in the Decision section". If the executor encounters a Prettier failure that cannot be resolved by line-splitting, ESCALATE to the orchestrator -- do NOT fall back to a placeholder comment.

    Verification step: `grep -nE "4 (literal|entries)" docs/adr/v2-001-structured-notify.md` -- confirm 0 marketplace-related hits remain. Note: the file may legitimately contain other `4` references (e.g. "Phase 4", "Phase 14", a `4-space indent` reference) that MUST NOT be changed; verify context before edit.

    Optionally: also check around lines 88-89 for the adjacent block of `interface MarketplaceNotificationMessage { ... }` declaration -- if the inline comment for `details?: MarketplaceDetails` is consistent, no change needed. Do NOT add a `reasons?:` field to the ADR's example interface block; the example is illustrative, not a literal reflection of the type-source, and the v2 ADR Open-Questions retention discipline (per D-15-13 from Phase 15) treats minor example drift as acceptable as long as the prose is correct. The new Amendment section (Task 2) provides the full reasons-field narrative.

    After edits, run `npm run check` to confirm Prettier / markdownlint / any spell-check gate is green. The ADR is not consumed by any runtime test, so the only gate is the formatter.
  </action>
  <verify>
    <automated>grep -cE "7 (literal strings|entries)" docs/adr/v2-001-structured-notify.md</automated>
    <expected>≥2 matches (Edit A's `7 literal strings` + Edit B's `7 entries`; Edit C's literal-list expansion is also detectable but the count style differs).</expected>

    Confirm Edit C's 7 literals are present verbatim (per checker Issue #7 -- no placeholder fallback):
      grep -cE '"autoupdate enabled".*"autoupdate disabled".*"skipped"|"skipped".*"autoupdate disabled".*"autoupdate enabled"' docs/adr/v2-001-structured-notify.md
    EXPECTED: ≥1 match in the Edit C neighborhood (the 3 new literals appear together on the same line or contiguous lines if Prettier-split).

    Anti-fallback guard (per checker Issue #7 -- the shorter-form placeholder is forbidden):
      grep -cE '// 7 literal strings \(see MARKETPLACE_STATUSES\)' docs/adr/v2-001-structured-notify.md
    EXPECTED: 0 matches. If this returns ≥1, the executor took the prohibited shortcut; reject and re-edit with the full literal list (line-split if necessary).
  </verify>
  <acceptance_criteria>
    - The public-surface block (`export type MarketplaceStatus; // 4 literal strings...`) now says `7 literal strings`.
    - The runtime-tuples block (`export const MARKETPLACE_STATUSES;  // 4 entries`) now says `// 7 entries`.
    - The interface-shape block's inline comment for `MarketplaceStatus` reflects the FULL 7-literal list verbatim (line-split allowed if Prettier requires; placeholder fallback `// 7 literal strings (see MARKETPLACE_STATUSES)` is PROHIBITED per checker Issue #7).
    - `grep -nE "the closed set of 4|MarketplaceStatus.*4 literal" docs/adr/v2-001-structured-notify.md` returns 0 marketplace-related hits.
    - `grep -cE '// 7 literal strings \(see MARKETPLACE_STATUSES\)' docs/adr/v2-001-structured-notify.md` returns 0 (the placeholder fallback is absent).
    - Consequences section (line 127+), Alternatives section (line 173+), Migration section (line 187+) are byte-identical to before.
    - The Decision section's overall narrative (renderer-as-spec, closed-set discipline, severity computed, reload-hint computed, dependency probe at render time) is byte-identical.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>The Decision section reflects the 7-entry MarketplaceStatus closed set, with the full literal list visible in Edit C (no placeholder fallback per checker Issue #7). The historical narrative is untouched. Task 2 adds the Amendment section.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append new "## Amendment: Phase 17.1" section after Migration</name>
  <files>docs/adr/v2-001-structured-notify.md</files>
  <read_first>
    - docs/adr/v2-001-structured-notify.md (lines 187-198 -- the entire `## Migration` section; the new Amendment section appends AFTER this section's final paragraph at line 197-198, BEFORE EOF)
    - .planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md (D-17.1-07 -- Amendment section structure: heading + what + why + ladders; specifics block -- the 3-paragraph structure)
    - .planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-CONTEXT.md (D-18-05 mapping table is the design contract this amendment implements -- reference it in the "why" paragraph)
  </read_first>
  <action>
    Per D-17.1-07: append a new section at the END of the ADR (after the closing line 198 sentence "If the phase numbers shift between this ADR's acceptance and the final teardown..."). Insert ONE blank line, then the heading, then the section body.

    Section heading: `## Amendment: Phase 17.1 ({YYYY-MM-DD})` -- use today's UTC date in ISO-8601 calendar form (2026-05-26 per the CONTEXT.md gathered date, but use the actual current date when committing). The executor decides whether to use the gather date or the actual commit date -- the CONTEXT.md states `{YYYY-MM-DD}` is the placeholder.

    Section body -- three short paragraphs (per the specifics block):

    Paragraph 1 (What changed): "Phase 17.1 amends the V2 grammar on three layered surfaces: (1) `shared/notify.ts` adds three new `MarketplaceStatus` literals (`\"autoupdate enabled\"`, `\"autoupdate disabled\"`, `\"skipped\"`) extending `MARKETPLACE_STATUSES` from 4 entries to 7, and adds an optional `readonly reasons?: readonly Reason[]` field to `MarketplaceNotificationMessage` (a third independent optional alongside `status?` and `details?` per D-15-06); (2) the `renderMpHeader` switch gains three new arms producing the byte forms `● ${name} [${scope}] (autoupdate enabled)`, `● ${name} [${scope}] (autoupdate disabled)`, and `● ${name} [${scope}] (skipped) {<reasons>}`; (3) `docs/output-catalog.md` § `marketplace autoupdate` is rewritten with five per-state catalog blocks (`enable-fresh`, `disable-fresh`, `enable-idempotent`, `disable-idempotent`, `failure-not-found`)."

    Paragraph 2 (Why): "Phase 17's v2 catalog collapsed the marketplace autoupdate enable/disable surface to a single `(updated)` status, erasing the V1 distinction between fresh state changes and idempotent flips. The Phase 18 discuss-phase (D-18-05) locked a 5-state user-visible design that requires the type model + renderer to distinguish fresh enable/disable from idempotent no-ops from failures. Phase 17.1 implements that contract as a layered amendment to Phase 15 (types), Phase 16 (renderer), and Phase 17 (catalog) -- a pure amendment, no new orchestrator behavior."

    Paragraph 3 (Severity + reload-hint ladders): "The severity ladder (D-16-11) is extended: `mp.status === \"skipped\"` routes to `\"warning\"` consistent with plugin-level `\"skipped\"`. The reload-hint trigger ladder (D-16-12) is extended: `mp.status === \"autoupdate enabled\"` and `mp.status === \"autoupdate disabled\"` trigger the `/reload to pick up changes` trailer (the marketplace persistence record was mutated); `mp.status === \"skipped\"` does NOT trigger (idempotent no-op -- no state changed). The `\"failed\"` arm continues to route to `\"error\"` severity with no reload-hint (D-16-12 explicitly excludes failed)."

    Optionally -- at the bottom of the new section, add one short footer paragraph: "The Decision section above reflects the post-amendment state. The Consequences / Migration / Alternatives sections are intentionally not refreshed (per D-17.1-08): their accepted narrative still holds. This Amendment section preserves the historical 'why did this change' narrative without rewriting the locked reasoning."

    Do NOT modify any text before the appended section. Do NOT add the amendment-section block before the Migration section (the Migration section's last sentence ends with `... this ADR's phase numbers are informative, not binding.` at line 197) -- append AFTER that line so the Migration narrative reads cleanly.

    Run `npm run check` and confirm green. If markdownlint flags any rule violation (line length, heading hierarchy, list discipline), adjust the paragraphs to satisfy the rule while preserving the locked-content discipline (what + why + ladders).
  </action>
  <verify>
    <automated>grep -cE "^## Amendment: Phase 17.1 \([0-9]{4}-[0-9]{2}-[0-9]{2}\)" docs/adr/v2-001-structured-notify.md</automated>
    <expected>Exactly 1 match (the new section heading with an ISO-8601 date in parentheses).</expected>
  </verify>
  <acceptance_criteria>
    - The ADR has a new section heading `## Amendment: Phase 17.1 ({YYYY-MM-DD})` with `{YYYY-MM-DD}` replaced by a real ISO-8601 date.
    - The section appears AFTER the `## Migration` section (line 187+) and AT END OF FILE -- no other content follows.
    - Paragraph 1 mentions all three layered surfaces (types, renderer, catalog) and the new literals + reasons field.
    - Paragraph 2 cites D-18-05 (the Phase 18 discuss-phase user-locked design that motivates the amendment).
    - Paragraph 3 cites D-16-11 (severity ladder extension) and D-16-12 (reload-hint trigger ladder extension); states `skipped → warning` and `autoupdate enabled / disabled → reload-hint triggered`; explicitly notes `skipped does NOT trigger reload-hint`.
    - The Consequences / Alternatives / Migration sections are byte-identical to before.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>The ADR is the single source of truth for the V2 grammar design. The Decision section reflects current state; the Amendment section preserves historical context. Phase 17.1 is complete pending verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR ↔ implementation | The ADR documents the design contract; implementations in `shared/notify.ts` enforce it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-04-01 | Tampering | accidental refresh of accepted historical reasoning | mitigate | Task 1 + Task 2 explicitly do NOT touch Consequences / Migration / Alternatives sections per D-17.1-08; the acceptance criteria assert byte-identity of those sections. |
| T-17.1-04-02 | Information Disclosure | obsolete count references (4 vs 7) | mitigate | Task 1's grep verification step catches stale `4 literal` / `4 entries` references; Edit C explicitly widens the inline-comment literal list. |
| T-17.1-04-03 | Tampering | placeholder-fallback substitution for Edit C's literal list | mitigate | Per checker Issue #7, the shorter-form fallback (`// 7 literal strings (see MARKETPLACE_STATUSES)`) is PROHIBITED; Task 1's verify block runs an anti-fallback grep that returns 0 matches. The full literal list, line-split if Prettier requires, is mandatory. |
| T-17.1-04-04 | Tampering | Amendment section drift from actual implementation | accept | Future planners (Phase 18/19/20) reading the ADR may misunderstand the autoupdate surface if Paragraphs 1-3 drift from `shared/notify.ts`. Mitigated by referencing specific decision IDs (D-18-05, D-16-11, D-16-12) that ground the prose in non-prose sources. |
| T-17.1-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
After both tasks land:

```bash
# Full repo gate (Prettier + markdownlint + any drift check)
npm run check
# EXPECTED: exits 0

# Confirm Decision section is updated
grep -cE "7 literal strings|7 entries" docs/adr/v2-001-structured-notify.md
# EXPECTED: ≥2

# Confirm Amendment section exists
grep -cE "^## Amendment: Phase 17.1" docs/adr/v2-001-structured-notify.md
# EXPECTED: 1

# Confirm Consequences / Migration / Alternatives sections survive
grep -cE "^## (Consequences|Alternatives considered|Migration)" docs/adr/v2-001-structured-notify.md
# EXPECTED: 3 (all three section headings present, byte-identical)

# Confirm no stale marketplace-status count survives
grep -nE "the closed set of 4|MarketplaceStatus.*4 literal" docs/adr/v2-001-structured-notify.md
# EXPECTED: 0 marketplace-related matches

# Anti-fallback guard (per checker Issue #7 -- the placeholder shortcut is forbidden)
grep -cE '// 7 literal strings \(see MARKETPLACE_STATUSES\)' docs/adr/v2-001-structured-notify.md
# EXPECTED: 0 (the full literal list must remain visible in Edit C)

# Confirm decision IDs referenced
grep -cE "D-18-05|D-16-11|D-16-12" docs/adr/v2-001-structured-notify.md
# EXPECTED: ≥3 (in the new Amendment section)
```

Pre-commit hook: per CLAUDE.md, run `pre-commit run --files docs/adr/v2-001-structured-notify.md` before committing. If committing from a worktree, prefix with `SKIP=trufflehog` per CLAUDE.md. Do NOT use `--no-verify`. Conventional Commit title example: `docs(adr): amend v2-001 for autoupdate surface (D-17.1-07)`. Body cites D-17.1-07, D-17.1-08, D-18-05.

**Wave-3 parallel landing (per D-17.1-01; checker Issue #10):** This plan and Plan 17.1-03 are factually independent within Wave 3. They touch different files (`docs/adr/v2-001-structured-notify.md` vs. `docs/output-catalog.md` + `tests/architecture/catalog-uat.test.ts` + `docs/messaging-style-guide.md`) and share no artifacts; Plan 17.1-04 has no dependency on Plan 17.1-03's catalog rewrite. Plans 17.1-03 and 17.1-04 may land in either order, simultaneously, or be picked up by separate executor instances. There is NO recommended ordering between them -- Wave 3 remains parallel per D-17.1-01. (The earlier draft of this plan suggested "land 17.1-03 FIRST"; that recommendation has been REMOVED per checker Issue #10 because it contradicted D-17.1-01's parallel-Wave-3 stance.)
</verification>

<success_criteria>
- ADR Decision section reflects the 7-entry MarketplaceStatus closed set (3 in-place edits at lines 35, 42, 88).
- Edit C contains the FULL 7-literal list verbatim (no placeholder-comment fallback per checker Issue #7); line-splitting is acceptable to honor Prettier line-width.
- ADR has a new `## Amendment: Phase 17.1 ({YYYY-MM-DD})` section at the bottom with three paragraphs covering what + why + ladders.
- Consequences / Migration / Alternatives sections are byte-identical to before.
- Decision IDs D-18-05, D-16-11, D-16-12 are referenced in the Amendment section.
- `npm run check` exits 0.
- This plan and Plan 17.1-03 are independent -- either may land first or both simultaneously per D-17.1-01.
</success_criteria>

<output>
Create `.planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-04-SUMMARY.md` when done, recording:
- The exact line numbers where the 3 in-place edits landed.
- The exact form Edit C took (single-line, line-split, or block-comment) -- paste the literal list as written.
- The line number where the new Amendment section starts.
- The exact date used in the Amendment section heading.
- Confirmation that `npm run check` is green.

After this plan lands (alongside Plan 17.1-03), Phase 17.1 is verification-ready: run `/gsd-verify-phase 17.1` to confirm the must-haves from all four plans hold end-to-end. Phase 18's plan 18-02 (autoupdate.ts call-site migration) hard-blocks on Phase 17.1's VERIFICATION.md status: passed (per D-18-04 / D-18-09).
</output>
