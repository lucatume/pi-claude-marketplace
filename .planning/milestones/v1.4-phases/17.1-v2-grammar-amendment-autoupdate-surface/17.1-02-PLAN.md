---
phase: 17.1-v2-grammar-amendment-autoupdate-surface
plan: 2
type: execute
wave: 2
depends_on: ["17.1-01"]
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - tests/shared/notify-v2.test.ts
autonomous: true
requirements: []
tags: [notify, renderer, marketplace-header, autoupdate, severity-ladder, reload-hint-ladder]
must_haves:
  truths:
    - "renderMpHeader has 3 new arms producing byte-exact V2 grammar for `\"autoupdate enabled\"`, `\"autoupdate disabled\"`, `\"skipped\"` (per D-17.1-02, D-18-05 byte forms)."
    - "The `\"skipped\"` arm composes `mp.reasons` via `composeReasons(p.reasons, false, false, probe)` reusing the file-private helper at lines 683-704 (per D-17.1 specifics: mp-level skipped never emits soft-dep markers)."
    - "computeSeverity routes `mp.status === \"skipped\"` to `\"warning\"` consistent with plugin-level `\"skipped\"` (per D-16-11 ladder extension; D-17.1-05)."
    - "shouldEmitReloadHint fires on `mp.status === \"autoupdate enabled\"` and `mp.status === \"autoupdate disabled\"` (fresh state-change); does NOT fire on `mp.status === \"skipped\"` (no state change) (per D-16-12 ladder extension; D-17.1-05)."
    - "tests/shared/notify-v2.test.ts has 5 new tests covering: (a) autoupdate enabled byte form; (b) autoupdate disabled byte form; (c) skipped + reasons byte form; (d) severity ladder skipped→warning; (e) mixed-severity payload where mp.skipped coexists with a healthy plugin row, proving first-match severity routing."
    - "`npx tsc --noEmit` exits 0 (the inherited Wave 1 `assertNever` error is closed by the 3 new arms)."
    - "`node --test tests/shared/notify-v2.test.ts` exits 0; the file's test count grows from 32 to 37."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "Three new renderMpHeader arms + extended computeSeverity + extended shouldEmitReloadHint"
      contains: "case \"autoupdate enabled\":"
    - path: "tests/shared/notify-v2.test.ts"
      provides: "Five new byte-equality unit tests for the autoupdate surface"
      min_lines: 1141
  key_links:
    - from: "shared/notify.ts renderMpHeader \"skipped\" arm"
      to: "shared/notify.ts composeReasons helper"
      via: "shared file-private function call (reuses the helper that backs plugin-level skipped rows)"
      pattern: "composeReasons\\(.*reasons.*false.*false.*probe\\)"
    - from: "shared/notify.ts computeSeverity"
      to: "shared/notify.ts MarketplaceStatus closed set"
      via: "additional first-match check `mp.status === \"skipped\"` in the second-match warning pass"
      pattern: "mp\\.status === \"skipped\""
    - from: "shared/notify.ts shouldEmitReloadHint"
      to: "shared/notify.ts MarketplaceStatus closed set"
      via: "additional triggers `\"autoupdate enabled\"` / `\"autoupdate disabled\"` in the marketplace-status check"
      pattern: '"autoupdate enabled"|"autoupdate disabled"'
---

<objective>
Land the V2 grammar renderer amendment for the autoupdate surface: add 3 new arms to `renderMpHeader`, extend `computeSeverity` and `shouldEmitReloadHint` per the locked ladders, and add 5 new byte-equality unit tests to `tests/shared/notify-v2.test.ts`. This plan closes the intentional Wave-1 compile-error gate (`assertNever(mp.status)` at line 565) by exhaustively handling the 3 new statuses.

Purpose: Make the V2 type model widening from Plan 17.1-01 RENDERABLE -- without these renderer arms, the type system says "you may construct payloads with `status: \"autoupdate enabled\"`" but `notify()` will throw at runtime via the `assertNever` arm. Plan 17.1-03 (catalog UAT fixtures) and Plan 18-02 (autoupdate.ts call-site migration) both consume the byte forms this plan produces.

Output:
- `renderMpHeader` switch (lines 529-567) has 3 new arms:
  - `case "autoupdate enabled":` → `` `${ICON_INSTALLED} ${mp.name} [${mp.scope}] (autoupdate enabled)` ``
  - `case "autoupdate disabled":` → `` `${ICON_INSTALLED} ${mp.name} [${mp.scope}] (autoupdate disabled)` ``
  - `case "skipped":` → `` `${ICON_INSTALLED} ${mp.name} [${mp.scope}] (skipped)` `` with `composeReasons(mp.reasons, false, false, probe)` appended when `mp.reasons` is defined (the reasons brace `{<reason>}` is composed by the helper).
- `computeSeverity` adds an `mp.status === "skipped"` warning pass.
- `shouldEmitReloadHint` adds `"autoupdate enabled"` and `"autoupdate disabled"` to the trigger set; `"skipped"` is NOT added.
- `tests/shared/notify-v2.test.ts` gains 5 new tests with byte-exact `assert.deepEqual(...)` payload assertions.
- `npx tsc --noEmit` exits 0; `node --test tests/shared/notify-v2.test.ts` exits 0; `npm run check` exits 0.

**`<read_first>` discipline reminder (per checker Issue #8):** This plan is on the upper edge of single-context-budget (3 tasks, 2 files, ~70-100 lines of diff including a renderer-signature fanout). The executor MUST `cat` every cited line range listed in each task's `<read_first>` block BEFORE making edits. Skipping the read step risks missing the `renderMpHeader` call site at line ~1020 in `composeMarketplaceBlock` (per checker Issue #4 -- the single confirmed fanout point), or accidentally widening the signature without updating its caller. The line numbers in this plan come from 2026-05-26 snapshots; minor drift is acceptable, but the structural targets (the switch body, the call site, the helper signature) MUST be confirmed via grep before edit.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md
@.planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-01-SUMMARY.md
@.planning/phases/16-renderer-public-api-alongside-v1/16-CONTEXT.md
@extensions/pi-claude-marketplace/shared/notify.ts
@tests/shared/notify-v2.test.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add three new arms to renderMpHeader (autoupdate enabled, autoupdate disabled, skipped)</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (lines 505-567 -- the entire `renderMpHeader` JSDoc block + switch body; lines 683-704 -- the `composeReasons` helper signature, parameter discipline, and brace-emission rule; lines 736-850 -- `renderPluginRow`'s `"skipped"` arm for the byte form precedent `composeReasons(p.reasons, false, false, probe)` at lines 818-826; lines 1010-1030 -- the `composeMarketplaceBlock` call site at line ~1020 that invokes `renderMpHeader(mp)` and is the single confirmed fanout point per checker Issue #4)
    - .planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md (D-17.1-02 wave ordering; D-17.1-03 byte-form table; specifics block -- the `"skipped"` mp-status arm reuses composeReasons with `false, false` for soft-dep flags)
    - .planning/phases/16-renderer-public-api-alongside-v1/16-CONTEXT.md (D-16-04 renderer-as-spec; D-16-09 renderMpHeader file-private switch ownership; D-16-15 soft-dep markers are plugin-row-only)
    - .planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-CONTEXT.md (D-18-05 5-state mapping table -- the normative byte form contract)
  </read_first>
  <action>
    Per D-17.1-02 + D-18-05 (the locked byte-form contract): add three new `case` arms to the `renderMpHeader` switch (between the existing `case "failed":` at line 537-538 and `case undefined:` at line 539). Place them in this order so the closed-set widening matches the tuple order: `"autoupdate enabled"`, `"autoupdate disabled"`, `"skipped"`.

    Arm 1 -- `case "autoupdate enabled":` returns the template literal `` `${ICON_INSTALLED} ${mp.name} [${mp.scope}] (autoupdate enabled)` ``. No reasons composition (the fresh-flip arms do not carry mp.reasons per the D-18-05 mapping). Byte form: `● foo [user] (autoupdate enabled)`.

    Arm 2 -- `case "autoupdate disabled":` returns the template literal `` `${ICON_INSTALLED} ${mp.name} [${mp.scope}] (autoupdate disabled)` ``. Same shape as Arm 1. Byte form: `● foo [user] (autoupdate disabled)`.

    Arm 3 -- `case "skipped":` reuses `composeReasons` to emit the optional reasons brace. The arm body must compose two slots and join them with a single space, suppressing the brace slot when empty. Construct it as: `const reasonsBrace = composeReasons(mp.reasons, false, false, probe);` then `return reasonsBrace === "" ? \`${ICON_INSTALLED} ${mp.name} [${mp.scope}] (skipped)\` : \`${ICON_INSTALLED} ${mp.name} [${mp.scope}] (skipped) ${reasonsBrace}\`;`. CRITICAL: the `false, false` pair for the two soft-dep flags is per D-16-15 (soft-dep markers are plugin-row-only). The function signature of `renderMpHeader` currently takes only `(mp: MarketplaceNotificationMessage)` and does NOT receive the `probe: SoftDepStatus`. UPDATE the signature of `renderMpHeader` to accept the `probe` argument: change `function renderMpHeader(mp: MarketplaceNotificationMessage): string` to `function renderMpHeader(mp: MarketplaceNotificationMessage, probe: SoftDepStatus): string`. Then update every CALL SITE of `renderMpHeader` in `shared/notify.ts` (search `renderMpHeader(` in this file) to pass the threaded `probe` argument -- the single confirmed call site is line ~1020 inside `composeMarketplaceBlock` (per checker Issue #4 audit), but the executor MUST grep to confirm no additional fanout points exist. The probe is already threaded through `notify()` per D-16-14 single-probe discipline, so the wiring is mechanical.

    Update the JSDoc above `renderMpHeader` (lines 505-528) byte-form documentation: add three lines under "Byte forms" describing the 3 new arms. Preserve the existing 4 arms' documentation verbatim.

    Do NOT touch `case undefined:` (the list-surface arm with SUB-BRANCH A / B for `mp.details`) -- it is independent and unaffected.

    After the edit run `npx tsc --noEmit` -- the inherited Wave-1 error at the `assertNever(mp.status)` arm (line 565 → renumbered as the 3 arms are inserted above it) MUST now be gone (all 7 closed-set members are covered). If it survives, something is mistyped in an arm -- the closed-set widening is bidirectional, and adding less than 3 arms leaves residual cases that `assertNever` still rejects.
  </action>
  <verify>
    <automated>grep -nE 'case "autoupdate enabled":|case "autoupdate disabled":|case "skipped":' extensions/pi-claude-marketplace/shared/notify.ts</automated>
    <expected>Three matches inside the renderMpHeader switch body (not inside renderPluginRow's switch, which also has its own `case "skipped":` for plugin-level -- verify the matches are in the lines 530-570 neighborhood, not 818-826).</expected>

    Signature-fanout guard (per checker Issue #4 -- every call site must pass two args):
      grep -nE 'renderMpHeader\(mp\)([^,]|$)' extensions/pi-claude-marketplace/shared/notify.ts
    EXPECTED: 0 matches. (This catches `renderMpHeader(mp)` regardless of whitespace after the closing paren; the audit confirmed the single fanout point at line ~1020 in `composeMarketplaceBlock`.)

    Confirm every existing caller migrated:
      grep -nE 'renderMpHeader\(' extensions/pi-claude-marketplace/shared/notify.ts
    EXPECTED: every match is a two-argument call (e.g., `renderMpHeader(vm.mp, vm)` or `renderMpHeader(mp, probe)`).
  </verify>
  <acceptance_criteria>
    - The `renderMpHeader` switch contains exactly 3 new `case` arms in the order `"autoupdate enabled"`, `"autoupdate disabled"`, `"skipped"`.
    - The `"autoupdate enabled"` and `"autoupdate disabled"` arms return template literals matching the byte forms `● ${name} [${scope}] (autoupdate enabled)` and `● ${name} [${scope}] (autoupdate disabled)` respectively (ICON_INSTALLED is the `●` glyph constant at line 501).
    - The `"skipped"` arm calls `composeReasons(mp.reasons, false, false, probe)` and conditionally appends ` ${reasonsBrace}` when non-empty.
    - The `renderMpHeader` signature receives `(mp: MarketplaceNotificationMessage, probe: SoftDepStatus)`; every call site in `shared/notify.ts` passes the threaded probe.
    - `grep -nE 'renderMpHeader\(mp\)([^,]|$)' extensions/pi-claude-marketplace/shared/notify.ts` returns 0 matches (no single-argument call form remains anywhere in the file).
    - `npx tsc --noEmit` exits 0 (the inherited Wave-1 `assertNever` error is closed).
    - `case undefined:` (list-surface arm) is byte-identical to before; `case "added":`, `case "removed":`, `case "updated":`, `case "failed":` are byte-identical to before.
  </acceptance_criteria>
  <done>Three new arms exist; the switch is exhaustive over the 7-entry closed set; `tsc --noEmit` is green; the signature-fanout grep returns 0 single-argument matches; soft-dep markers cannot leak to mp-skipped rows per D-16-15.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend computeSeverity and shouldEmitReloadHint ladders</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (lines 902-928 -- `computeSeverity` first-match and second-match passes; lines 930-950 -- `shouldEmitReloadHint` first-match pass over both mp and plugin statuses)
    - .planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md (D-17.1-02 + D-17.1-05 -- the ladder updates locked alongside the renderer arms)
    - .planning/phases/16-renderer-public-api-alongside-v1/16-CONTEXT.md (D-16-11 severity ladder rationale; D-16-12 reload-hint trigger ladder rationale -- both being extended, not replaced)
  </read_first>
  <action>
    Per D-17.1-02 + D-17.1-05 (severity ladder + reload-hint ladder amendments):

    Edit A -- `computeSeverity` (lines 902-928). The function currently has two passes: first-match for `"failed"` → `"error"`; second-match for plugin-level `"skipped"` / `"manual recovery"` → `"warning"`. Extend the second-match pass to ALSO check `mp.status === "skipped"` and return `"warning"` in that case. The second-match `for (const mp of message.marketplaces)` loop at lines 918-924 currently iterates only over `mp.plugins` to find plugin-level skipped/manual-recovery. Add a check at the top of the outer loop body (before the inner `for (const p of mp.plugins)`): `if (mp.status === "skipped") { return "warning"; }`. The first-match pass at lines 905-915 is unchanged (mp.status `"failed"` still beats mp.status `"skipped"`). The JSDoc above `computeSeverity` (line 902) lists the ladder as "first-match: failed (plugin or marketplace) wins over skipped / manual recovery, wins over success" -- append "or marketplace skipped (per D-17.1-05; consistent with plugin-level skipped per D-16-11)" to the second-match clause description.

    Edit B -- `shouldEmitReloadHint` (lines 930-950). The function currently triggers on `mp.status === "added" | "removed" | "updated"` or on any of the 4 state-changing plugin statuses. Extend the mp-status check at line 933 to also include `mp.status === "autoupdate enabled"` and `mp.status === "autoupdate disabled"`. The new condition is: `if (mp.status === "added" || mp.status === "removed" || mp.status === "updated" || mp.status === "autoupdate enabled" || mp.status === "autoupdate disabled") { return true; }`. CRITICAL: do NOT add `mp.status === "skipped"` to the trigger set -- per D-17.1-05 / D-18-05, idempotent flips DO NOT trigger the reload hint (no state was changed). The JSDoc above `shouldEmitReloadHint` (line 930) -- append "Phase 17.1 amendment per D-17.1-02 / D-18-05: fresh-flip autoupdate enabled/disabled trigger; skipped (idempotent no-op) does NOT trigger."

    After both edits, the closed-set behavior is: `"failed"` → error+no-reload; `"skipped"` → warning+no-reload; `"autoupdate enabled"` / `"autoupdate disabled"` → info(undefined)+reload-hint; `"added"` / `"removed"` / `"updated"` → info+reload-hint (unchanged).

    `npx tsc --noEmit` continues to exit 0 (no type changes; only logic-conditional changes).
  </action>
  <verify>
    <automated>grep -cE 'mp\.status === "skipped"' extensions/pi-claude-marketplace/shared/notify.ts | head -1 && grep -cE '"autoupdate enabled"|"autoupdate disabled"' extensions/pi-claude-marketplace/shared/notify.ts</automated>
    <expected>The first grep counts at least 1 occurrence of `mp.status === "skipped"` in `computeSeverity`. The second grep counts at least 4 occurrences of the two autoupdate literals (3 from Task 1's renderer arms -- once each in case-label form `case "autoupdate enabled":` / `case "autoupdate disabled":` + the new `shouldEmitReloadHint` triggers -- plus any JSDoc mentions).</expected>
  </verify>
  <acceptance_criteria>
    - `computeSeverity` second-match pass returns `"warning"` when `mp.status === "skipped"`; first-match pass at `mp.status === "failed"` is unchanged (it still wins over skipped per D-16-11).
    - `shouldEmitReloadHint` returns `true` when `mp.status === "autoupdate enabled"` or `mp.status === "autoupdate disabled"`; returns `false` when `mp.status === "skipped"` (no state change).
    - The 4 pre-existing mp-status trigger conditions (`"added"`, `"removed"`, `"updated"`) and 4 pre-existing plugin-status trigger conditions (`"installed"`, `"updated"`, `"reinstalled"`, `"uninstalled"`) are byte-identical.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The severity ladder routes mp-level skipped to warning consistent with plugin-level; the reload-hint ladder triggers on fresh autoupdate flips but suppresses on idempotent no-ops. Both ladders honor the user-locked D-18-05 design.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add 5 new byte-equality tests to notify-v2.test.ts (3 arms + 2 ladders)</name>
  <files>tests/shared/notify-v2.test.ts</files>
  <read_first>
    - tests/shared/notify-v2.test.ts (lines 1-180 -- the file's mini-spec header and mock helpers `makeCtx()`, `piWithBothLoaded()`, `piWithNothingLoaded()`; lines 475-528 -- the existing per-mp-status tests for `"added"`, `"removed"`, `"updated"`, `"failed"` which are the pattern template; lines 529-551 -- the SUB-BRANCH B list-surface test for stylistic comparison; lines 993-1048 -- the severity-ladder pattern tests at lines 974-1048; lines 1048-1090 -- the reload-hint suppression pattern at line 1048)
    - extensions/pi-claude-marketplace/shared/notify.ts (read the freshly-landed 3 arms from Task 1 and the freshly-extended `computeSeverity` / `shouldEmitReloadHint` from Task 2 -- the byte forms emitted by the renderer ARE the assertion targets)
    - .planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md (D-17.1-03 byte-form table is the assertion source of truth; D-17.1-05.2 specifies 5 new tests covering 3 arms + 2 ladders)
  </read_first>
  <action>
    Per D-17.1-05.2: add 5 new `test(...)` blocks to `tests/shared/notify-v2.test.ts`. Placement: append them after the existing per-mp-status block (lines 475-528) and before the SUB-BRANCH B list-surface test at line 529. Match the pattern: `const ctx = makeCtx(); const pi = piWithNothingLoaded(); const msg: NotificationMessage = {...}; notify(ctx as never, pi as never, msg); assert.equal(ctx.ui.notify.mock.calls.length, 1); assert.deepEqual(ctx.ui.notify.mock.calls[0]!.arguments, [<expected-bytes>, <optional-severity>]);`.

    Use these exact assertion targets (the 5 normative byte forms from D-17.1-03):

    Test 1 -- `test("notify renders autoupdate enabled marketplace header alone (info severity + reload-hint per D-17.1-05)", () => { ... })`. Payload: `{ marketplaces: [{ name: "foo", scope: "user", status: "autoupdate enabled", plugins: [] }] }`. Expected arguments: `[\`● foo [user] (autoupdate enabled)\n\n/reload to pick up changes\`]` (no severity arg -- info routing means arguments.length === 1).

    Test 2 -- `test("notify renders autoupdate disabled marketplace header alone (info severity + reload-hint per D-17.1-05)", () => { ... })`. Payload: `{ marketplaces: [{ name: "foo", scope: "user", status: "autoupdate disabled", plugins: [] }] }`. Expected arguments: `[\`● foo [user] (autoupdate disabled)\n\n/reload to pick up changes\`]`.

    Test 3 -- `test("notify renders skipped marketplace header with reasons brace (warning severity, NO reload-hint per D-17.1-05)", () => { ... })`. Payload: `{ marketplaces: [{ name: "foo", scope: "user", status: "skipped", reasons: ["already enabled"], plugins: [] }] }`. Expected arguments: `[\`● foo [user] (skipped) {already enabled}\`, "warning"]` (NO `\n\n/reload to pick up changes` trailer; severity arg present).

    Test 4 -- `test("notify severity tier mp-skipped: skipped marketplace → arguments = [..., \"warning\"] (D-17.1-05 ladder extension)", () => { ... })`. Payload: `{ marketplaces: [{ name: "foo", scope: "user", status: "skipped", reasons: ["already disabled"], plugins: [] }] }`. Assert two things: (a) `ctx.ui.notify.mock.calls[0]!.arguments.length === 2`; (b) `ctx.ui.notify.mock.calls[0]!.arguments[1] === "warning"`. This is a structural assertion of the severity-arg presence; the byte form is covered by Test 3.

    Test 5 -- `test("notify mixed-severity payload: mp.skipped coexists with healthy plugin row → first-match severity routing fires on mp.status (D-17.1-05)", () => { ... })`. REFRAMED per checker Issue #3 (the original "reload-hint ladder" test was a strict subset of Test 3's `assert.deepEqual` and added no incremental coverage). New design: construct a SINGLE `NotificationMessage` payload that mixes mp-level `"skipped"` with a healthy plugin row in the SAME marketplace. Payload: `{ marketplaces: [{ name: "foo", scope: "user", status: "skipped", reasons: ["already enabled"], plugins: [{ name: "p1", status: "installed" /* healthy plugin-row status */ }] }] }`. Render via `notify(ctx as never, pi as never, msg)`. Assert THREE things in this one test:
      (a) `ctx.ui.notify.mock.calls[0]!.arguments[1] === "warning"` -- proves the severity ladder's first-match pass fires on `mp.status === "skipped"` REGARDLESS of the healthy plugin-row underneath (the plugin row's `"installed"` status alone would route to info; the mp-level skipped wins);
      (b) `ctx.ui.notify.mock.calls[0]!.arguments[0]` includes the byte form `● foo [user] (skipped) {already enabled}` (the mp header renders the skipped state);
      (c) `ctx.ui.notify.mock.calls[0]!.arguments[0]` does NOT include the substring `/reload to pick up changes` (mp.skipped is an idempotent no-op; the reload-hint ladder suppresses regardless of plugin rows underneath).
    This proves the first-match severity routing fires on mp.skipped even when the plugin row is healthy, covering routing semantics that Tests 3 and 4 alone do not -- Test 3 covers an empty-plugins payload, Test 4 covers severity-arg presence, but only Test 5 proves the mp-level routing dominates a non-empty healthy plugin set.

    Match the existing test file's lexical style: use `as never` casts on the ctx/pi arguments to `notify()`; use template literals with backticks; preserve the `// D-17.1-05` / `// per D-17.1-03` reference comments inside test bodies where they clarify the byte form.

    After adding the 5 tests, run `node --test tests/shared/notify-v2.test.ts` and confirm all 37 tests pass (32 pre-existing + 5 new). Run `npm run check` and confirm green.
  </action>
  <verify>
    <automated>node --test tests/shared/notify-v2.test.ts 2>&1 | grep -E "tests|pass|fail" | tail -5</automated>
    <expected>The test runner reports 37 tests (or higher if subtests are counted independently); 0 failures.</expected>
  </verify>
  <acceptance_criteria>
    - File has 5 new `test(...)` blocks in the lines-475-to-528 neighborhood (before the SUB-BRANCH B test).
    - Each test asserts `ctx.ui.notify.mock.calls[0]!.arguments` against the exact byte forms from D-17.1-03.
    - Test 1 asserts NO severity arg (arguments.length === 1) -- info routing.
    - Test 2 asserts NO severity arg -- info routing.
    - Test 3 asserts severity arg `"warning"` and the reasons-brace `{already enabled}` byte form.
    - Test 4 structurally asserts arguments.length === 2 and arguments[1] === "warning".
    - Test 5 (mixed-severity) asserts the severity-arg is `"warning"` AND the mp-skipped header is rendered AND the reload-hint is absent -- proving mp-level routing dominates a non-empty healthy plugin set (per checker Issue #3 reframing).
    - `node --test tests/shared/notify-v2.test.ts` exits 0 with all 37 tests passing.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>The renderer arms + ladders are gated by 5 byte-equality unit tests at the per-variant level (the Phase 16 belt-and-braces precedent). Test 5 now proves the mixed-severity routing case that Tests 3 and 4 alone do not cover. Plan 17.1-03's catalog UAT will provide the parallel byte-equality gate at the catalog level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer ↔ caller | Callers construct `MarketplaceNotificationMessage` payloads; the renderer enforces byte form. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-02-01 | Tampering | renderMpHeader byte form | mitigate | Per-arm byte-equality unit tests in `notify-v2.test.ts` (Tests 1-3) lock the exact emission for each of the 3 new arms. Plan 17.1-03's catalog UAT provides the second byte-equality gate. |
| T-17.1-02-02 | Tampering | severity ladder routing | mitigate | Test 4 asserts arguments.length === 2 and arguments[1] === "warning" for mp.status === "skipped". Test 5 (mixed-severity) proves the first-match severity routing fires on mp-level skipped regardless of plugin-row contents. The D-16-11 first-match-wins discipline is preserved (failed still beats skipped). |
| T-17.1-02-03 | Tampering | reload-hint ladder routing | mitigate | Test 5's third assertion confirms the reload-hint is absent for mp.skipped even when plugin rows coexist. |
| T-17.1-02-04 | Information Disclosure | soft-dep markers leaking onto mp-skipped rows | mitigate | The `"skipped"` arm passes `false, false` to composeReasons per D-16-15; the helper cannot emit `{requires pi-subagents}` / `{requires pi-mcp}` markers when both flags are false (verified by reading composeReasons lines 691-697). |
| T-17.1-02-05 | Tampering | renderMpHeader signature fanout | mitigate | Task 1 verify block runs `grep -nE 'renderMpHeader\(mp\)([^,]|$)'` and requires 0 matches; the single confirmed call site at line ~1020 in `composeMarketplaceBlock` (per checker Issue #4 audit) must migrate to the 2-arg form. |
| T-17.1-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
After Task 3 completes:

```bash
# Full repo gate
npm run check
# EXPECTED: exits 0

# Renderer / test gates
npx tsc --noEmit
# EXPECTED: exits 0 (the Wave-1 inherited error is fully closed)

node --test tests/shared/notify-v2.test.ts
# EXPECTED: all tests pass; test count grew from 32 to ≥37

# Targeted source assertions
grep -cE 'case "autoupdate enabled":' extensions/pi-claude-marketplace/shared/notify.ts
# EXPECTED: exactly 1 match (in renderMpHeader)

grep -cE 'case "autoupdate disabled":' extensions/pi-claude-marketplace/shared/notify.ts
# EXPECTED: exactly 1 match (in renderMpHeader)

grep -cE 'mp\.status === "skipped"' extensions/pi-claude-marketplace/shared/notify.ts
# EXPECTED: ≥1 match (in computeSeverity)

grep -cE '"autoupdate enabled"|"autoupdate disabled"' extensions/pi-claude-marketplace/shared/notify.ts
# EXPECTED: ≥4 matches (2 case-labels + 2 reload-hint triggers + tuple body from Plan 17.1-01)

# Signature-fanout guard (per checker Issue #4)
grep -nE 'renderMpHeader\(mp\)([^,]|$)' extensions/pi-claude-marketplace/shared/notify.ts
# EXPECTED: 0 matches (every call site passes the 2-arg form)

# Confirm soft-dep markers cannot leak to mp-skipped rows
grep -B1 -A6 'case "skipped":' extensions/pi-claude-marketplace/shared/notify.ts | grep -E 'composeReasons.*false.*false.*probe'
# EXPECTED: at least 1 match (the mp-skipped arm composes reasons with both soft-dep flags false)
```

**Commit strategy -- LOCKED to "single combined commit" (per CLAUDE.md `--no-verify` prohibition; checker Issues #1 + #2):**

This plan completes the Wave-1 + Wave-2 amendment. Create ONE commit that includes BOTH Plan 17.1-01's `notify.ts` + `notify-types.test.ts` diff AND this plan's `notify.ts` + `notify-v2.test.ts` diff. Title:

    feat(notify): amend v2 grammar for autoupdate surface (types + renderer)

Body should cite D-17.1-01 (Wave structure), D-17.1-02 (renderer + ladder amendments), D-17.1-05 (test surfaces), and explain that Wave 1 + Wave 2 land in a single commit because Wave-1-alone fails `npm run check` (the renderer's `assertNever` exhaustiveness check inherits the new 3 mp-status literals) and CLAUDE.md forbids `--no-verify`.

Pre-commit hook: per CLAUDE.md, run `pre-commit run --files extensions/pi-claude-marketplace/shared/notify.ts tests/architecture/notify-types.test.ts tests/shared/notify-v2.test.ts` before `git commit`. If committing from a worktree, prefix with `SKIP=trufflehog` per CLAUDE.md. Do NOT use `--no-verify`.

The back-to-back-commits alternative (one commit per wave) is REJECTED: Wave-1-alone fails `npm run check` because the renderer's `assertNever` exhaustiveness check inherits the new 3 mp-status literals, and CLAUDE.md forbids `--no-verify`. A single combined commit is the only hook-compliant path.
</verification>

<success_criteria>
- `renderMpHeader` has 3 new arms producing the byte forms from D-17.1-03.
- `renderMpHeader` signature accepts the threaded `probe: SoftDepStatus`; all call sites updated; `grep -nE 'renderMpHeader\(mp\)([^,]|$)'` returns 0 matches.
- `computeSeverity` routes `mp.status === "skipped"` to `"warning"`.
- `shouldEmitReloadHint` triggers on `mp.status === "autoupdate enabled"` and `mp.status === "autoupdate disabled"`; suppressed on `mp.status === "skipped"`.
- 5 new tests in `tests/shared/notify-v2.test.ts` assert byte-equality for the 3 arms and structurally assert the 2 ladders; Test 5 covers the mixed-severity case (mp.skipped + healthy plugin row).
- Soft-dep markers cannot leak to mp-skipped rows (composeReasons receives `false, false`).
- `npm run check` exits 0 when this plan's diff is combined with Plan 17.1-01's diff in a single commit.
- The combined commit follows the title convention `feat(notify): amend v2 grammar for autoupdate surface (types + renderer)`.
</success_criteria>

<output>
Create `.planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-02-SUMMARY.md` when done, recording:
- The exact line ranges where the 3 new arms landed (Plan 17.1-03 cross-references these to verify the catalog blocks match).
- Confirmation of the 5 byte forms emitted (paste the test assertion targets verbatim so Plan 17.1-03's catalog rewrite can mechanically copy them).
- Test count delta (32 → 37 or higher if the harness counts sub-assertions).
- Confirmation that `grep -nE 'renderMpHeader\(mp\)([^,]|$)' notify.ts` returns 0 matches.
- The exact commit SHA + title of the single combined Wave-1 + Wave-2 commit (must match `feat(notify): amend v2 grammar for autoupdate surface (types + renderer)`).
- Confirmation that `npm run check` is green at commit time.
</output>
