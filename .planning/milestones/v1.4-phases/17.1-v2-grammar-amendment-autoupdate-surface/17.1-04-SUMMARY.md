---
phase: 17.1-v2-grammar-amendment-autoupdate-surface
plan: 4
subsystem: docs/adr (v2-001 ADR amendment)
tags: [adr, documentation, autoupdate, marketplace-status, amendment]
dependency_graph:
  requires:
    - "Phase 17.1 Plan 1 (Wave 1: MARKETPLACE_STATUSES extended 4 -> 7; MarketplaceNotificationMessage.reasons? added)"
    - "Phase 17.1 Plan 2 (Wave 2: renderMpHeader 3 new arms + severity/reload-hint ladder extensions)"
  provides:
    - "ADR Decision section reflects the post-amendment 7-entry MarketplaceStatus closed set"
    - "ADR has a new `## Amendment: Phase 17.1 (2026-05-26)` section documenting what + why + ladder updates"
  affects:
    - "Phase 17.1 verification gate (per D-17.1-04): with all four 17.1 plans landed, /gsd-verify-phase 17.1 can run"
    - "Phase 18 plan 18-02 hard-blocks on Phase 17.1's VERIFICATION.md status: passed (D-18-04 / D-18-09)"
tech-stack:
  added: []
  patterns:
    - "ADR amendment discipline (D-17.1-07 + D-17.1-08): Decision section reflects CURRENT state; new Amendment section preserves HISTORICAL why-did-this-change narrative. Consequences / Alternatives / Migration sections are byte-identical to before -- their accepted reasoning is locked."
    - "Literal-list discipline (checker Issue #7): the full 7-literal list stays visible verbatim in the Decision section so reviewers cross-check against PRD without leaving the ADR; placeholder-summary fallback is prohibited."
key-files:
  created:
    - ".planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-04-SUMMARY.md"
  modified:
    - "docs/adr/v2-001-structured-notify.md"
decisions:
  - "D-17.1-04-EXEC-01: Edit C uses the SINGLE-LINE form rather than splitting across multiple lines. The project's `format:check` script targets only `*.{js,json,ts}` and markdownlint's MD013 (line-length) is disabled, so the 143-character line carries no gate cost. Single-line is clearest for the verbatim-list cross-check that D-17.1-07 mandates."
  - "D-17.1-04-EXEC-02: Amendment section heading dated `(2026-05-26)` -- the actual commit date matches the CONTEXT.md gather date, so the placeholder choice is moot."
metrics:
  duration: "Plan executed in single session; tracked at orchestrator level"
  completed: "2026-05-26"
  tasks_completed: 2
  files_modified: 1
---

# Phase 17.1 Plan 4: V2 ADR Amendment Summary

One-liner: Updated the `docs/adr/v2-001-structured-notify.md` Decision section in place at lines 35 / 42 / 88 so it reflects the post-Phase-17.1 7-entry MarketplaceStatus closed set (count widened from 4 to 7 in both runtime-tuple and derived-type references; inline literal list on `readonly status?:` widened from 4 to 7 literals verbatim), and appended a new `## Amendment: Phase 17.1 (2026-05-26)` section at line 199 with three paragraphs covering what changed (three layered surfaces: types, renderer, catalog), why (Phase 18 D-18-05 user-locked 5-state design), and severity / reload-hint ladder updates (D-16-11 + D-16-12 extensions). Consequences / Alternatives / Migration sections are byte-identical to the pre-edit base per D-17.1-08.

## What Changed

### Task 1: Three in-place Decision-section updates (lines 35, 42, 88)

**File:** `docs/adr/v2-001-structured-notify.md`

All three edits landed at the projected line numbers from the plan -- no drift. Verified by post-edit `grep -nE`:

| Edit | Line | Before | After |
|------|------|--------|-------|
| A (public-surface block) | 35 | `export type MarketplaceStatus;              // 4 literal strings, derived from MARKETPLACE_STATUSES tuple` | `export type MarketplaceStatus;              // 7 literal strings, derived from MARKETPLACE_STATUSES tuple` |
| B (runtime-tuples block) | 42 | `export const MARKETPLACE_STATUSES;  // 4 entries` | `export const MARKETPLACE_STATUSES;  // 7 entries` |
| C (interface-shape block, single-line form) | 88 | `  readonly status?: MarketplaceStatus; // "added" \| "removed" \| "updated" \| "failed"` | `  readonly status?: MarketplaceStatus; // "added" \| "removed" \| "updated" \| "failed" \| "autoupdate enabled" \| "autoupdate disabled" \| "skipped"` |

**Edit C single-line form rationale (D-17.1-04-EXEC-01):** The plan explicitly authorized line-splitting if Prettier required it. Inspection of project gates showed neither gate constrains this line:

- `npm run format:check` runs Prettier on `**/*.{js,json,ts}` only -- markdown is NOT in the glob.
- `.markdownlint-cli2.yaml` has `MD013: false` (no line-length rule).

So the 143-character single-line form is the cleanest choice for the verbatim-list cross-check D-17.1-07 mandates. The placeholder-fallback shortcut (`// 7 literal strings (see MARKETPLACE_STATUSES)`) was NOT used -- verified by anti-fallback grep returning 0 matches per checker Issue #7.

**Edit C literal as written (verbatim, paste-safe for downstream cross-reference):**

```
  readonly status?: MarketplaceStatus; // "added" | "removed" | "updated" | "failed" | "autoupdate enabled" | "autoupdate disabled" | "skipped"
```

**Commit:** `88aef74 docs(adr): update v2-001 Decision section for 7 mp statuses (D-17.1-07)`.

### Task 2: New `## Amendment: Phase 17.1 (2026-05-26)` section appended at line 199

**File:** `docs/adr/v2-001-structured-notify.md`

The new section was appended AFTER the existing `## Migration` section (line 187+, ending at line 197 with "...this ADR's phase numbers are informative, not binding."). Insertion: one blank line + section heading + four paragraphs. Final file length: 207 lines (was 197).

**Section heading (line 199):** `## Amendment: Phase 17.1 (2026-05-26)` -- ISO-8601 date matches both the CONTEXT.md gather date and the actual commit date.

**Paragraph 1 (What changed, line 201):** Describes the three layered surfaces -- (1) `shared/notify.ts` type-model amendments (3 new `MarketplaceStatus` literals + new optional `readonly reasons?: readonly Reason[]` on `MarketplaceNotificationMessage`); (2) `renderMpHeader` three new arms producing the byte forms; (3) `docs/output-catalog.md` § `marketplace autoupdate` rewrite with 5 per-state blocks (`enable-fresh`, `disable-fresh`, `enable-idempotent`, `disable-idempotent`, `failure-not-found`). Cites D-15-06 (independent-optionals rationale).

**Paragraph 2 (Why, line 203):** Cites D-18-05 explicitly -- the Phase 18 discuss-phase locked a 5-state user-visible design after Phase 17's v2 catalog collapsed the autoupdate enable/disable surface to a single `(updated)` status, erasing the V1 distinction between fresh state changes and idempotent flips. Notes Phase 17.1 is a pure amendment (no new orchestrator behavior).

**Paragraph 3 (Ladder extensions, line 205):** Cites D-16-11 (severity ladder) and D-16-12 (reload-hint trigger ladder). States `mp.status === "skipped" -> "warning"` (severity extension); `mp.status === "autoupdate enabled" | "autoupdate disabled"` trigger reload-hint; `mp.status === "skipped"` does NOT trigger reload-hint (idempotent no-op -- no state changed); `mp.status === "failed"` continues to route to `"error"` with no reload-hint (D-16-12 explicitly excludes failed).

**Paragraph 4 (Closing, line 207):** Cites D-17.1-08 -- the Consequences / Migration / Alternatives sections are intentionally not refreshed; their accepted narrative still holds. The Amendment section preserves the historical "why-did-this-change" narrative without rewriting the locked reasoning.

**Commit:** `539ef62 docs(adr): append v2-001 Amendment: Phase 17.1 section (D-17.1-07)`.

## Verification

### Plan-prescribed verification block (all green)

```bash
npm run check                                                           # exit 0 (1356/1358 pass; 0 fail; 2 todo)
grep -cE "7 literal strings|7 entries" docs/adr/v2-001-structured-notify.md   # 2 (>=2 expected)
grep -cE "^## Amendment: Phase 17.1" docs/adr/v2-001-structured-notify.md     # 1 (=1 expected)
grep -cE "^## (Consequences|Alternatives considered|Migration)" docs/adr/...  # 3 (=3 expected; all three preserved)
grep -nE "the closed set of 4|MarketplaceStatus.*4 literal" docs/adr/...      # 0 (no stale 4-count surfaces)
grep -cE '// 7 literal strings \(see MARKETPLACE_STATUSES\)' docs/adr/...     # 0 (anti-fallback guard; placeholder absent)
grep -oE 'D-18-05|D-16-11|D-16-12' docs/adr/... | sort -u                     # 3 distinct IDs all present in Amendment
```

### Byte-identity of Consequences / Alternatives / Migration sections vs base (D-17.1-08)

`git diff 2499df5 -- docs/adr/v2-001-structured-notify.md` shows exactly four hunks:

- `@@ -32,14 +32,14 @@` -- contains Edit A (line 35) and Edit B (line 42); both inside the Decision section's Public-surface block (lines 22-44).
- `@@ -85,7 +85,7 @@` -- contains Edit C (line 88); inside the Decision section's NotificationMessage-shape code fence (lines 80-104).
- `@@ -195,3 +195,13 @@` -- pure addition of the new Amendment section (lines 198-207); zero deletions on the surrounding context lines.

Lines 127-194 (the entirety of Consequences + Alternatives considered + Migration) have ZERO diff against base. D-17.1-08 satisfied.

### Section heading sequence (confirms Amendment lands AFTER Migration, at EOF)

```
7:## Context
18:## Decision
127:## Consequences
173:## Alternatives considered
187:## Migration
199:## Amendment: Phase 17.1 (2026-05-26)
```

EOF is at line 207 (4 paragraphs of the new section, no trailing content).

## Deviations from Plan

### Auto-fixed Issues

None. Both tasks landed exactly as the plan specified; no Rule 1/2/3 fixes were needed; no architectural decisions surfaced.

### Plan-compliance notes

- **Edit C form choice (single-line):** The plan authorized either single-line OR multi-line-split OR block-comment forms. Single-line was selected because (a) no Prettier / markdownlint gate constrains line length on `.md` prose (verified -- format:check globs `*.{js,json,ts}` only; markdownlint MD013 is disabled); (b) the full 7-literal list is most readable single-line for the verbatim-list cross-check D-17.1-07 mandates. Line-split or block-comment forms would be equivalent on the gates but slightly noisier visually. Decision documented as D-17.1-04-EXEC-01.
- **Amendment heading date:** Used `2026-05-26` (today UTC, which also matches the CONTEXT.md gather date). The plan said "executor decides whether to use the gather date or the actual commit date -- the CONTEXT.md states `{YYYY-MM-DD}` is the placeholder." Both candidates coincided, so the choice is moot. Decision documented as D-17.1-04-EXEC-02.

## Files Touched

- `docs/adr/v2-001-structured-notify.md` (Tasks 1 + 2)

## Commits

- `88aef74 docs(adr): update v2-001 Decision section for 7 mp statuses (D-17.1-07)` -- Task 1 (3 in-place edits at lines 35, 42, 88; 3 insertions / 3 deletions; no file additions or deletions)
- `539ef62 docs(adr): append v2-001 Amendment: Phase 17.1 section (D-17.1-07)` -- Task 2 (Amendment section appended at line 199; 10 insertions; no deletions)

Both commits used the `SKIP=trufflehog` prefix per CLAUDE.md's worktree-sandbox guidance; all other pre-commit hooks (trailing whitespace, smartquotes, BiDi-control, mdformat, markdownlint-cli2) ran cleanly. `--no-verify` was NOT used.

## Self-Check

- [x] `docs/adr/v2-001-structured-notify.md` exists with the 3 in-place edits at lines 35, 42, 88 -- verified via grep.
- [x] `docs/adr/v2-001-structured-notify.md` has the new `## Amendment: Phase 17.1 (2026-05-26)` section heading at line 199 -- verified.
- [x] Decision IDs D-18-05, D-16-11, D-16-12 all referenced in the Amendment section -- verified by `grep -oE | sort -u` returning 3 distinct hits.
- [x] Consequences / Alternatives / Migration sections byte-identical to base 2499df5 (zero diff hunks in lines 127-194) -- verified.
- [x] Anti-fallback guard: `// 7 literal strings (see MARKETPLACE_STATUSES)` placeholder absent (0 matches) -- verified per checker Issue #7.
- [x] `npm run check` exits 0; 1356/1358 tests pass, 0 fail, 2 todo -- verified.
- [x] Pre-commit hooks pass on both commits (smartquotes, mdformat, markdownlint-cli2 all green) -- verified.
- [x] Commits `88aef74` (Task 1) and `539ef62` (Task 2) exist in `git log` -- verified.
- [x] No file deletions in either commit -- verified by `git diff --diff-filter=D --name-only` returning empty.

## Self-Check: PASSED
