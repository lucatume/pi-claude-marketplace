---
phase: 17.1-v2-grammar-amendment-autoupdate-surface
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - tests/architecture/notify-types.test.ts
autonomous: true
requirements: []
tags: [notify, type-model, marketplace-status, autoupdate, closed-set]
must_haves:
  truths:
    - "MARKETPLACE_STATUSES tuple has 7 entries: added, removed, updated, failed, autoupdate enabled, autoupdate disabled, skipped (per D-17.1-01 supersession of D-15-07)."
    - "MarketplaceNotificationMessage carries optional readonly reasons?: readonly Reason[] field (per D-17.1-05 belt-and-braces; consistent with D-15-01 per-variant reasons discipline and D-15-06 independent-optionals discipline)."
    - "tests/architecture/notify-types.test.ts compile-time proofs lock the 7-entry closed set and the reasons? field shape (per D-17.1-05.1)."
    - "Repo-wide `tsc --noEmit` exits 0 after the type widening (the renderer's existing `assertNever` over mp.status compiles because the 3 new statuses are not yet rendered -- they will be added in Plan 17.1-02; verify by inspection that no Wave-1-scoped runtime test renders the 3 new statuses)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "MARKETPLACE_STATUSES with 7 entries + MarketplaceNotificationMessage with reasons?"
      contains: "\"autoupdate enabled\", \"autoupdate disabled\", \"skipped\""
    - path: "tests/architecture/notify-types.test.ts"
      provides: "Compile-time closed-set membership lock for the 7-entry tuple + readonly reasons? shape"
      contains: "extends 7"
  key_links:
    - from: "extensions/pi-claude-marketplace/shared/notify.ts MARKETPLACE_STATUSES tuple"
      to: "extensions/pi-claude-marketplace/shared/notify.ts MarketplaceStatus type"
      via: "indexed-access derivation `(typeof MARKETPLACE_STATUSES)[number]`"
      pattern: "type MarketplaceStatus = .*MARKETPLACE_STATUSES.*number"
    - from: "tests/architecture/notify-types.test.ts _Assert_MarketplaceStatusesLen"
      to: "extensions/pi-claude-marketplace/shared/notify.ts MARKETPLACE_STATUSES"
      via: "compile-time `extends 7` assertion against tuple length"
      pattern: "MARKETPLACE_STATUSES.*length.*extends 7"
---

<objective>
Land the V2 grammar type-model amendment for the autoupdate surface in `shared/notify.ts` and the matching compile-time arch-test locks in `tests/architecture/notify-types.test.ts`. This plan is the Wave 1 foundation that Plan 17.1-02's renderer arms and Plan 17.1-03's catalog UAT fixtures depend on.

Purpose: Restore the user-visible distinction between fresh autoupdate enable/disable, idempotent flips, and not-found failures that Phase 17's v2 catalog collapsed into a single `(updated)` status, per the user-locked design in D-18-05 (Phase 18 CONTEXT.md) which D-17.1-01 promotes to a Phase 17.1 amendment.

Output:
- `MARKETPLACE_STATUSES` extended from 4 entries to 7 entries (adds `"autoupdate enabled"`, `"autoupdate disabled"`, `"skipped"`).
- `MarketplaceNotificationMessage` gains optional `readonly reasons?: readonly Reason[]`.
- `tests/architecture/notify-types.test.ts` length assertion bumped 4 → 7; `_MarketplaceStatusExpected` expanded to 7 literals; `_MarketplaceMessageExpected` gains `readonly reasons?: readonly Reason[]`.
- `tsc --noEmit` exits 0; all pre-existing tests in scope still pass.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md
@.planning/phases/15-type-model-adr-refresh/15-CONTEXT.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-CONTEXT.md
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/shared/grammar/reasons.ts
@tests/architecture/notify-types.test.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend MARKETPLACE_STATUSES tuple from 4 to 7 entries</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (full lines 180-230 to see PLUGIN_STATUSES, MARKETPLACE_STATUSES, DEPENDENCIES tuple definitions and their derived types -- the pattern is `(typeof X)[number]` indexed access)
    - .planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md (lines 9-27 phase boundary; lines 29-74 decisions D-17.1-01 through D-17.1-08; lines 105-107 Phase 15 lineage anchors for D-15-07 supersession)
    - .planning/phases/15-type-model-adr-refresh/15-CONTEXT.md (locate D-15-07 to confirm the supersession context and D-15-11 closed-set discipline)
    - extensions/pi-claude-marketplace/shared/grammar/reasons.ts (lines 41-70 -- VERIFY ONLY that `"already enabled"` at line 65 and `"already disabled"` at line 66 are already present; no edit here)
  </read_first>
  <action>
    Per D-17.1-01 (supersedes D-15-07): replace the 4-entry `MARKETPLACE_STATUSES` tuple at line 202 with a 7-entry tuple. The new tuple literal MUST be exactly: `["added", "removed", "updated", "failed", "autoupdate enabled", "autoupdate disabled", "skipped"] as const`. Order is normative -- append the 3 new entries after the existing 4 (do NOT reorder the existing 4 -- this preserves the existing renderer arm ordering convention from Phase 16's `renderMpHeader` switch). Update the JSDoc block above the tuple (lines 194-201) to say "7 entries" and replace the obsolete prose explaining the v1.3 marketplace-skipped re-routing rationale with one short sentence that says: the 3 new statuses are added per D-17.1-01 to support the Phase 18 autoupdate-surface migration (D-18-04, D-18-05) and supersede D-15-07's original 4-entry lock. Do NOT touch `MarketplaceStatus` at line 224 (the indexed-access derivation `(typeof MARKETPLACE_STATUSES)[number]` automatically widens). Do NOT touch the `PLUGIN_STATUSES` tuple at line 181, `DEPENDENCIES` at line 211, `PluginStatus` at line 218, or `Dependency` at line 230. Do NOT touch `renderMpHeader` -- that is Plan 17.1-02's scope. After the edit, run `npx tsc --noEmit` and verify the renderer's `assertNever(mp.status)` at line 565 reports a compile error (this is EXPECTED -- the type-widening exposes that 3 cases are missing from the switch; Plan 17.1-02 adds those arms). If `tsc --noEmit` succeeds, something is wrong (the closed-set widening was not effective). Do not work around the error in this task; it is the desired Wave-1 signal that Plan 17.1-02 has real work to do.
  </action>
  <verify>
    <automated>grep -c '"autoupdate enabled", "autoupdate disabled", "skipped"' extensions/pi-claude-marketplace/shared/notify.ts</automated>
    <expected>≥1 (the new 3 entries appear in the tuple literal)</expected>
  </verify>
  <acceptance_criteria>
    - `MARKETPLACE_STATUSES` literal contains exactly 7 entries in this exact order: `"added"`, `"removed"`, `"updated"`, `"failed"`, `"autoupdate enabled"`, `"autoupdate disabled"`, `"skipped"`.
    - Source assertion: `node -e 'import("./extensions/pi-claude-marketplace/shared/notify.ts").then(m => console.log(m.MARKETPLACE_STATUSES.length))'` outputs `7` (skip if dynamic ESM import is awkward; the next task's compile-time `extends 7` assertion is the structural gate).
    - `npx tsc --noEmit` produces a NEW error at `extensions/pi-claude-marketplace/shared/notify.ts:565` of the form "Argument of type '\"autoupdate enabled\" | \"autoupdate disabled\" | \"skipped\"' is not assignable to parameter of type 'never'" or equivalent -- this proves the closed-set widening reached the switch's `assertNever` arm. Plan 17.1-02 will fix this. DO NOT proceed past this task expecting `tsc --noEmit` to be green.
    - PLUGIN_STATUSES, DEPENDENCIES, MarketplaceStatus type, Dependency type, renderMpHeader function body, computeSeverity, shouldEmitReloadHint are byte-identical to before.
  </acceptance_criteria>
  <done>The tuple has 7 entries in the locked order. The intentional compile error at the renderer's `assertNever` arm proves the type-widening took effect. No other source file is touched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add optional reasons?: readonly Reason[] to MarketplaceNotificationMessage</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (lines 450-470 -- the `MarketplaceNotificationMessage` interface; also note the `Reason` import -- search for `import type { Reason }` or similar; if `Reason` is not yet imported at the top of notify.ts, add the import from `./grammar/reasons.ts`)
    - .planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md (D-17.1-05.1 -- the test file shape constraint; specifics block mentioning the `"skipped"` arm consumes `mp.reasons`)
    - .planning/phases/15-type-model-adr-refresh/15-CONTEXT.md (D-15-01 per-variant reasons discipline; D-15-06 independent-optionals pattern -- `reasons?:` is the THIRD independent optional on this interface alongside `status?:` and `details?:`)
  </read_first>
  <action>
    Per D-17.1-05 / D-17.1-01 + D-15-06 independent-optionals discipline: add a single new field `readonly reasons?: readonly Reason[];` to `MarketplaceNotificationMessage` (line 464-470). Placement: between `details?: MarketplaceDetails;` and `plugins: readonly PluginNotificationMessage[];` -- this keeps the 3 OPTIONAL fields (`status?`, `details?`, `reasons?`) grouped together before the REQUIRED `plugins` field. Verify `Reason` is already imported at the top of `notify.ts`; if not, add the import `import type { Reason } from "./grammar/reasons.ts";` to the existing import block (search for `from "./grammar/` to find the right neighborhood). Extend the JSDoc above the interface (lines 450-463) by one sentence: state that `reasons?:` is an independent optional consumed by the `"skipped"` mp-status renderer arm (Phase 17.1 amendment per D-17.1-01); other arms ignore the field per D-15-06 (the type does not structurally constrain co-occurrence). Do NOT add `reasons?:` to any other interface; do NOT change `PluginNotificationMessage` variants (per-plugin `reasons` discipline is unchanged per D-15-01). Re-run `npx tsc --noEmit` -- the only remaining error should be the one at line 565 from Task 1. Confirm no NEW errors elsewhere (e.g., the test file we will edit in Task 3, or any consumer of `MarketplaceNotificationMessage` that uses `Object.keys` / exhaustive destructure).
  </action>
  <verify>
    <automated>grep -nE 'readonly reasons\?:\s*readonly Reason\[\]' extensions/pi-claude-marketplace/shared/notify.ts | head -3</automated>
    <expected>One match at a line between 464 and 470 inside `MarketplaceNotificationMessage`; zero matches elsewhere (the field is mp-specific, not plugin-specific).</expected>
    Guard against duplicate `Reason` import -- the existing import at line 6 must remain the only one (per checker Issue #9):
      grep -c 'import type { Reason }' extensions/pi-claude-marketplace/shared/notify.ts
    Expected: exactly 1.
  </verify>
  <acceptance_criteria>
    - `MarketplaceNotificationMessage` interface contains the exact field declaration `readonly reasons?: readonly Reason[];`.
    - The `Reason` type is in scope at the field declaration site (either imported from `./grammar/reasons.ts` or already in scope).
    - `grep -c 'import type { Reason }' extensions/pi-claude-marketplace/shared/notify.ts` returns exactly 1 -- no duplicate import introduced.
    - The 3 optional fields `status?`, `details?`, `reasons?` appear before the required `plugins` field for readability.
    - `npx tsc --noEmit` reports exactly ONE error (the inherited `assertNever` error from Task 1 at line 565) -- no new compile errors anywhere else.
    - No edit to `PluginNotificationMessage` variants, `MarketplaceDetails`, `NotificationMessage`, or `UsageErrorMessage`.
  </acceptance_criteria>
  <done>Optional `reasons?: readonly Reason[]` is present on `MarketplaceNotificationMessage` as the third independent optional. The type widens consumers that destructure or iterate fields; no consumer in scope of Wave 1 breaks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Lock 7-entry closed set + reasons? field in notify-types.test.ts</name>
  <files>tests/architecture/notify-types.test.ts</files>
  <read_first>
    - tests/architecture/notify-types.test.ts (lines 107-148 -- the `_Assert_PluginStatusesLen`, `_Assert_MarketplaceStatusesLen`, `_PluginStatusExpected`, `_MarketplaceStatusExpected` proofs); also lines 187-210 (the `_MarketplaceMessageExpected` shape proof and the surrounding `_MarketplaceDetailsExpected` proof so the new field placement matches the source-of-truth shape from Task 2)
    - .planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md (D-17.1-05.1 -- exact arch-test edits required; the closed-set membership lock pattern is the bidirectional-extends idiom shown at lines 134-148)
  </read_first>
  <action>
    Per D-17.1-05.1: three edits in `tests/architecture/notify-types.test.ts`.

    Edit A (lines 111-115): change `_Assert_MarketplaceStatusesLen` from `extends 4` to `extends 7`. The exact replacement is `type _Assert_MarketplaceStatusesLen = (typeof MARKETPLACE_STATUSES)["length"] extends 7 ? true : never;`. The comment above (line 111) currently says "D-15-07 + D-15-11: MARKETPLACE_STATUSES tuple length is exactly 4." -- replace with "D-17.1-01 (supersedes D-15-07) + D-15-11: MARKETPLACE_STATUSES tuple length is exactly 7." Do NOT rename the type alias `_Assert_MarketplaceStatusesLen` or the exported constant `_l2`.

    Edit B (lines 141-148): expand `_MarketplaceStatusExpected` from the 4-literal union to the 7-literal union. The exact replacement is `type _MarketplaceStatusExpected = "added" | "removed" | "updated" | "failed" | "autoupdate enabled" | "autoupdate disabled" | "skipped";`. The comment above (line 141) currently says "SNM-05 + D-15-07: `MarketplaceStatus` is EXACTLY the 4 expected literals." -- replace with "SNM-05 + D-17.1-01 (supersedes D-15-07): `MarketplaceStatus` is EXACTLY the 7 expected literals." Do NOT rename the alias or the exported constant `_msv`. The bidirectional `_PluginStatusExpected extends X ? X extends Y ? true : never : never` idiom in the surrounding `_Assert_MarketplaceStatusValues` assertion at lines 143-148 stays structurally identical -- only the `_MarketplaceStatusExpected` type body changes.

    Edit C (lines 187-198): extend `_MarketplaceMessageExpected` to include the new field. Inside the `interface _MarketplaceMessageExpected { ... }` block (lines 190-196), add the single field `readonly reasons?: readonly Reason[];` between `readonly details?: MarketplaceDetails;` and `readonly plugins: readonly PluginNotificationMessage[];`. The comment above (lines 187-189) -- currently citing D-15-06 independent-optionals -- append the phrase "+ Phase 17.1 D-17.1-01 reasons? amendment" so the rationale chain is grounded. Verify that `Reason` is already imported in this test file (search for `Reason` in the existing import block at the top of the file); if not, add `Reason` to the existing `import { ... } from "../../extensions/pi-claude-marketplace/shared/notify.ts"` or the appropriate `from "./grammar/reasons.ts"` neighbor -- match the import style already in use. Do NOT touch `_Assert_MarketplaceMessageShape` (the assert constant) or `_PluginStatusExpected` (the plugin-status proof is unchanged). The structural assertion at line 197 uses `extends` (uni-directional) -- keep it unchanged; the bidirectional proofs are on the literal-union types, not the interface shapes.

    After all three edits, run `npx tsc --noEmit`. The remaining intentional error at `extensions/pi-claude-marketplace/shared/notify.ts:565` (`assertNever` arm) survives -- that is Plan 17.1-02's responsibility. No NEW errors should appear; in particular all the `_Assert_MarketplaceStatus*` proofs should still type-check (they now lock the 7-entry set).
  </action>
  <verify>
    <automated>grep -nE '"autoupdate enabled" \| "autoupdate disabled" \| "skipped"' tests/architecture/notify-types.test.ts && grep -nE 'extends 7' tests/architecture/notify-types.test.ts | grep -i marketplace && grep -nE 'readonly reasons\?:\s*readonly Reason\[\]' tests/architecture/notify-types.test.ts</automated>
    <expected>All three greps return a match: Edit B's expanded literal union; Edit A's `extends 7` for MARKETPLACE_STATUSES; Edit C's reasons? field on `_MarketplaceMessageExpected`.</expected>
  </verify>
  <acceptance_criteria>
    - `_Assert_MarketplaceStatusesLen` asserts `extends 7`.
    - `_MarketplaceStatusExpected` is exactly the 7-literal union: `"added" | "removed" | "updated" | "failed" | "autoupdate enabled" | "autoupdate disabled" | "skipped"`.
    - `_MarketplaceMessageExpected` carries `readonly reasons?: readonly Reason[]`.
    - `_PluginStatusExpected` (10-literal union) is byte-identical to before.
    - `_Assert_PluginStatusesLen extends 10` is byte-identical to before.
    - `_Assert_DependenciesLen extends 2` is byte-identical to before.
    - `npx tsc --noEmit` produces exactly ONE error (the inherited `assertNever` arm at `shared/notify.ts:565`). All `_msv`, `_mms`, `_l2` constants compile.
    - `node --test tests/architecture/notify-types.test.ts` -- this file is a type-only compile-check; `node --test` may pass trivially or report 0 runtime tests depending on the harness. Either is acceptable as long as `tsc --noEmit` shows the expected single error.
  </acceptance_criteria>
  <done>The compile-time closed-set membership lock is updated to 7 entries; the structural shape lock includes the new `reasons?` field. Wave 1's deliverable is complete except for the intentional `assertNever` compile-error gate handed off to Plan 17.1-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure type-system + arch-test edits. No I/O, no untrusted input, no external services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-01-01 | Tampering | `MARKETPLACE_STATUSES` tuple ordering | accept | Order is normative (per D-17.1-01); reviewer / D-15-11 closed-set indexed-access derivation propagates the order to `MarketplaceStatus`; the arch-test bidirectional-extends proof catches any reordering that breaks the literal union. |
| T-17.1-01-02 | Tampering | `MarketplaceNotificationMessage` field set | accept | `_MarketplaceMessageExpected extends MarketplaceNotificationMessage` (uni-directional, per existing test idiom) catches the field-removal direction; the renderer's downstream consumption (Plan 17.1-02) catches missing field reads. Reasons? is an OPTIONAL field, so adding it cannot break existing payload constructors. |
| T-17.1-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; supply chain N/A. Phase 17.1 is pure type-system edits. |
</threat_model>

<verification>
After Task 3 completes:

```bash
# Type-system gate (the structural enforcement guarantee)
npx tsc --noEmit
# EXPECTED: exactly ONE error at extensions/pi-claude-marketplace/shared/notify.ts:565
# (the renderer's `assertNever(mp.status)` arm -- Plan 17.1-02 will close this)
# All `_Assert_Marketplace*` proofs at tests/architecture/notify-types.test.ts compile.

# Arch test runtime (compile-time proofs do not produce runtime tests)
node --test tests/architecture/notify-types.test.ts
# EXPECTED: passes trivially (the file is compile-checked, not runtime-asserted).

# Targeted source assertions
grep -c '"autoupdate enabled"' extensions/pi-claude-marketplace/shared/notify.ts
# EXPECTED: ≥1 (in MARKETPLACE_STATUSES tuple body + JSDoc references)

grep -nE 'readonly reasons\?:\s*readonly Reason\[\]' extensions/pi-claude-marketplace/shared/notify.ts
# EXPECTED: exactly 1 match (inside MarketplaceNotificationMessage)

# Duplicate-import guard (per checker Issue #9)
grep -c 'import type { Reason }' extensions/pi-claude-marketplace/shared/notify.ts
# EXPECTED: exactly 1 (the existing import at line 6 of notify.ts; do not introduce a second)

grep -nE 'extends 7' tests/architecture/notify-types.test.ts | grep -i marketplace
# EXPECTED: exactly 1 match (_Assert_MarketplaceStatusesLen)
```

`npm run check` will NOT be green at this plan boundary -- the `tsc --noEmit` step is expected to fail with the single inherited `assertNever` error. This is the Wave-1 design (per D-17.1-02 wave-order rationale: amendments cross-cut and the inter-plan compile-error gap is intentional and short). Plan 17.1-02 closes this gap by adding the 3 renderer arms.

**Commit strategy -- LOCKED to "single combined commit" (per CLAUDE.md `--no-verify` prohibition; checker Issues #1 + #2):**

Plans 17.1-01 (this) and 17.1-02 together form ONE pre-commit-clean transition. The Wave-1-alone state fails `npm run check` (the renderer's `assertNever` exhaustiveness check inherits the new 3 mp-status literals), and CLAUDE.md's Git section explicitly forbids `--no-verify`. The only hook-compliant path is a single combined commit covering both plans' diffs.

Therefore: DO NOT commit after this plan completes. Stage the changes (`git add extensions/pi-claude-marketplace/shared/notify.ts tests/architecture/notify-types.test.ts`) and proceed to Plan 17.1-02. Plan 17.1-02's verification section drives the single combined commit that covers both plans, with title:

    feat(notify): amend v2 grammar for autoupdate surface (types + renderer)

Body should cite D-17.1-01, D-17.1-02, D-17.1-05. Run `pre-commit run --files <changed files>` (or `pre-commit run --all-files`) before `git commit`; if committing from a worktree, prefix with `SKIP=trufflehog` per CLAUDE.md. Do NOT use `--no-verify`.

The back-to-back-commits alternative (one commit per wave) is REJECTED -- Wave-1-alone fails the pre-commit `tsc --noEmit` gate, and CLAUDE.md prohibits `--no-verify` as the workaround. A single combined commit is the only path that satisfies both the hook policy and the wave-order design.
</verification>

<success_criteria>
- `MARKETPLACE_STATUSES` literal contains exactly 7 entries in the locked order.
- `MarketplaceNotificationMessage` has `readonly reasons?: readonly Reason[]` as a third independent optional.
- `tests/architecture/notify-types.test.ts` proofs lock the 7-entry closed set and the `reasons?` shape via compile-time bidirectional-extends assertions.
- The inherited `assertNever` compile error at `shared/notify.ts:565` is the SOLE new `tsc --noEmit` error (proves the type-widening reached the switch).
- `grep -c 'import type { Reason }' notify.ts` returns exactly 1 -- no duplicate import introduced.
- No commit lands at the end of this plan; the working tree is staged for the combined Wave-1 + Wave-2 commit landed by Plan 17.1-02.
- No edits to PLUGIN_STATUSES, DEPENDENCIES, MarketplaceDetails, UsageErrorMessage, any PluginNotificationMessage variant, or any file outside the two listed in `files_modified`.
</success_criteria>

<output>
Create `.planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-01-SUMMARY.md` when done, recording:
- The exact line numbers where the 3 edits landed (in case Plan 17.1-02 needs to cross-reference).
- Confirmation that the `tsc --noEmit` error count is exactly 1 and the error message references `mp.status` and `assertNever`.
- Confirmation that `grep -c 'import type { Reason }' notify.ts` returns exactly 1.
- Confirmation that no commit was made by this plan and that the working tree is staged for the combined Wave-1 + Wave-2 commit landed by Plan 17.1-02.
- Any deviations from the planned line numbers (the CONTEXT.md line refs are 2026-05-26 snapshots; minor drift is acceptable as long as the structural intent is preserved).
</output>
