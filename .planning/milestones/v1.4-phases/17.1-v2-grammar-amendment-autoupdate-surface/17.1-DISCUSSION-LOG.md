# Phase 17.1: V2 Grammar Amendment: Autoupdate Surface - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md -- this log preserves the alternatives considered.

**Date:** 2026-05-26
**Phase:** 17.1-V2 Grammar Amendment: Autoupdate Surface
**Areas discussed:** Plan granularity & wave order, Catalog fixture migration, Verification surfaces, ADR amendment scope

**Pre-existing locked context (carried forward, not re-discussed):**

- The 5-state autoupdate mapping is locked by D-18-05 (Phase 18 CONTEXT.md): `(autoupdate enabled)` / `(autoupdate disabled)` for fresh flips; `(skipped) {already enabled|disabled}` for idempotent flips; `(failed)` for not-found.
- The 4-surface amendment scope is locked by D-18-04: type model (Phase 15 amendment) + renderer (Phase 16 amendment) + catalog (Phase 17 amendment) + ADR Decision-section update.
- Severity ladder is locked: `skipped → warning`, fresh enable/disable → info, `failed → error`.
- Reload-hint ladder is locked: fresh enable/disable triggers `/reload`, `skipped` and `failed` suppress.

---

## Plan granularity & wave order

### Q1: How should the 4 amendments be split into plans?

| Option | Description | Selected |
|--------|-------------|----------|
| 4 plans, 3 waves | 17.1-01 types (W1) → 17.1-02 renderer (W2) → 17.1-03 catalog + 17.1-04 ADR (W3, parallel). Matches Phase 16 per-arm precedent. | ✓ |
| 3 plans, 2 waves | Fold ADR into catalog plan. Smaller set, ADR drift risk if not kept in sync with catalog. | |
| 2 plans, 2 waves | Type+renderer in W1; catalog+ADR in W2. Largest commits; bisection at single-commit granularity. | |
| 1 atomic plan | All 4 layers in one plan. Smallest review surface, largest commit. | |

**User's choice:** 4 plans, 3 waves
**Notes:** Per-layer atomicity preferred over plan-count minimization. Aligns with Phase 16's per-arm split precedent.

### Q2: Wave order for catalog plan (depends on renderer arms)

| Option | Description | Selected |
|--------|-------------|----------|
| Wave 3 (after renderer) | 17.1-03 catalog+UAT moves to Wave 3 with 17.1-04 ADR (parallel within W3). Catalog UAT drives notify() through the new renderer arms, so it strictly depends on 17.1-02. | ✓ |
| Wave 2 parallel -- split into doc + UAT | 17.1-03a catalog markdown (W2) + 17.1-03b UAT fixtures (W3). Adds a plan to enable W2 doc parallel. | |
| Keep Wave 2 parallel | 17.1-03 in W2 parallel with 17.1-02; tolerate mid-wave byte-equality break. | |

**User's choice:** Wave 3 (after renderer)
**Notes:** Avoids mid-wave byte-equality break window. Final wave structure: W1 = 17.1-01; W2 = 17.1-02; W3 = 17.1-03 + 17.1-04 (parallel).

---

## Catalog fixture migration

### Q3: How should the catalog autoupdate section's fixtures change?

| Option | Description | Selected |
|--------|-------------|----------|
| Replace mixed with 5 per-state | Drop enable-mixed/disable-mixed; ship exactly the 5 D-18-05 fixtures. Single-marketplace, precise byte form per state. | ✓ |
| Replace + add one mixed | 5 per-state PLUS one mixed (e.g., enable-mixed-fresh-and-idempotent) to assert multi-mp block-join + reload-hint computation across heterogeneous statuses. | |
| Additive only (keep mixed) | Keep mixed-state fixtures (rewriting byte expectations), ADD idempotent fixtures. Preserves multi-mp narrative but mixes status semantics. | |

**User's choice:** Replace mixed with 5 per-state
**Notes:** Multi-marketplace cascade already structurally covered by other catalog sections. The autoupdate section's purpose is the per-state byte gate.

### Q4: Should the catalog autoupdate section's narrative prose be rewritten too?

| Option | Description | Selected |
|--------|-------------|----------|
| Full prose rewrite | Replace existing prose; the "idempotent-flip variant not expressible in v2" line is now factually wrong. Write fresh prose covering the 5 states + ladders. | ✓ |
| Minimal prose patch | Surgically delete the "not expressible" paragraph and update section intro. Smaller diff but leaves some pre-amendment phrasing. | |

**User's choice:** Full prose rewrite
**Notes:** The existing prose actively contradicts D-18-05's design; surgical patching would leave a confusing document.

---

## Verification surfaces

### Q5: Where should the 3 new arm byte-equality tests live?

| Option | Description | Selected |
|--------|-------------|----------|
| Extend notify-v2.test.ts + catalog-uat | Add per-arm tests to notify-v2.test.ts (current Phase 16 location); replace catalog-uat autoupdate fixture map. Belt-and-braces coverage -- notify-v2 catches construction bugs, catalog UAT catches drift bugs. | ✓ |
| Catalog UAT only | Rely solely on catalog UAT fixtures. Smaller diff, but loses per-arm isolation when something breaks. | |
| Per-arm in new file | Create tests/shared/notify-v2-autoupdate.test.ts to isolate 17.1's tests. More files, breaks Phase 16's single-file convention. | |

**User's choice:** Extend notify-v2.test.ts + catalog-uat
**Notes:** Belt-and-braces matches Phase 16's established pattern. Per-arm tests pinpoint construction bugs; catalog UAT catches contract-drift bugs.

### Q6: Should docs/messaging-style-guide.md be amended in this phase or deferred?

| Option | Description | Selected |
|--------|-------------|----------|
| Amend in this phase | Update "MarketplaceStatus; // 4 literal strings" → "// 7 literal strings"; land in plan 17.1-03 alongside catalog. | ✓ |
| Defer to Phase 21 cleanup | Leave stale; Phase 21 teardown does broader docs cleanup. Tiny doc inconsistency for ~3 phases. | |

**User's choice:** Amend in this phase
**Notes:** Pointer doc consistency matters; defer-to-21 creates a contradictory pointer between Phase 17.1 landing and Phase 21 teardown.

### Q7: Update notify-types.test.ts to add reasons?: to MarketplaceNotificationMessage shape assertion?

| Option | Description | Selected |
|--------|-------------|----------|
| Add reasons?: to shape assertion | Update _MarketplaceMessageExpected to include 'readonly reasons?: readonly Reason[];' so the structural-extends assertion catches future regressions. | ✓ |
| Type changes only, no arch lock | Add reasons?: to source type but leave _MarketplaceMessageExpected unchanged. Loses regression protection. | |

**User's choice:** Add reasons?: to shape assertion
**Notes:** Structural locks are cheap to write and valuable forever.

---

## ADR amendment scope

### Q8: How should the ADR Decision-section amendment be structured?

| Option | Description | Selected |
|--------|-------------|----------|
| Edit-in-place + Amendment note | Update 4 → 7 in Decision; ADD a "## Amendment: Phase 17.1" section at the bottom. Single source of truth for current state + history. | ✓ |
| Append-only Amendment section | Leave Decision untouched; only append Amendment. Cleanest historical record but readers reconcile two sections to know current state. | |
| Edit-in-place, no Amendment note | Update Decision in place; no Amendment section. Smallest diff but loses "why did this change" narrative. | |

**User's choice:** Edit-in-place + Amendment note
**Notes:** Dual approach preserves both surfaces: current state in Decision (where readers look first) + history in Amendment (where the why lives).

### Q9: Should ADR Consequences section also be refreshed?

| Option | Description | Selected |
|--------|-------------|----------|
| Decision section only | Leave Consequences/Migration/Alternatives untouched; their narrative still holds. Smaller diff. | ✓ |
| Refresh Consequences too | Update Consequences count-sensitive prose. More thorough; risk of accidentally rewriting accepted historical text. | |

**User's choice:** Decision section only
**Notes:** Only the literal counts in Decision were stale. Refreshing accepted historical reasoning risks accidental drift.

---

## Claude's Discretion

- Whether plan 17.1-01 lands MARKETPLACE_STATUSES vs. reasons?: changes in 1 or 2 commits within the plan.
- Exact wording of the new ADR Amendment section (must include what + why + ladder changes).
- Whether the catalog rewrite restructures sub-headings within the autoupdate section.
- Whether to verify and update the frontmatter REASONS count in docs/messaging-style-guide.md.
- Specific `describe(...)` grouping inside `notify-v2.test.ts` for the new tests.

## Deferred Ideas

- **Phase 18 plan 18-02** -- already scoped; consumes Phase 17.1's amendments to migrate `orchestrators/marketplace/autoupdate.ts`.
- **ADR Consequences / Migration / Alternatives refresh** -- explicitly out of scope per D-17.1-08.
- **Renaming the `"skipped"` MarketplaceStatus** to disambiguate from `PluginStatus.skipped` -- accepted overlap per D-18-05; rename would balloon catalog naming and break grammar symmetry.
