---
phase: 17.1-v2-grammar-amendment-autoupdate-surface
plan: 3
type: execute
wave: 3
depends_on: ["17.1-02"]
files_modified:
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
  - docs/messaging-style-guide.md
autonomous: true
requirements: []
tags: [catalog, uat, autoupdate, style-guide, byte-equality]
must_haves:
  truths:
    - "`docs/output-catalog.md` § `/claude:plugin marketplace autoupdate <enable|disable> <name>` is fully rewritten with 5 catalog-state blocks (per D-17.1-03, D-17.1-04)."
    - "The 5 catalog-state discriminators are `enable-fresh`, `disable-fresh`, `enable-idempotent`, `disable-idempotent`, `failure-not-found` -- exact strings, normative."
    - "Each catalog block contains the exact bytes from the D-17.1-03 byte-form table."
    - "`tests/architecture/catalog-uat.test.ts` autoupdate fixture map at lines 1244-1278 is REPLACED with the 5 new fixture entries -- keys match the catalog-state discriminators exactly."
    - "`docs/messaging-style-guide.md` line 27 + line 52 pointer prose says `7 literal strings` (was `4 literal strings`)."
    - "Catalog UAT byte-equality is GREEN: every fixture's `notify()` output byte-matches the corresponding catalog block."
    - "`npm run check` exits 0."
  artifacts:
    - path: "docs/output-catalog.md"
      provides: "Rewritten autoupdate section with 5 state blocks"
      contains: "<!-- catalog-state: enable-fresh -->"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "5-entry autoupdate fixture map keyed by the new catalog-state discriminators"
      contains: "enable-fresh"
    - path: "docs/messaging-style-guide.md"
      provides: "Updated MarketplaceStatus pointer (7 literal strings)"
      contains: "7 literal strings"
  key_links:
    - from: "docs/output-catalog.md catalog-state discriminators"
      to: "tests/architecture/catalog-uat.test.ts fixture map keys"
      via: "byte-equality UAT runner reads the catalog block by section title + discriminator key"
      pattern: "<!-- catalog-state: (enable-fresh|disable-fresh|enable-idempotent|disable-idempotent|failure-not-found) -->"
    - from: "tests/architecture/catalog-uat.test.ts fixture payloads"
      to: "extensions/pi-claude-marketplace/shared/notify.ts renderMpHeader"
      via: "notify() emits the byte form that must equal the catalog block"
      pattern: "notify\\(mockCtx, mockPi, message\\)"
---

<objective>
Land the V2 catalog rewrite + UAT fixture replacement + style-guide pointer refresh for the autoupdate surface. This plan PROVES that Plan 17.1-02's renderer arms produce byte-correct output by driving them through the catalog UAT runner against newly-written catalog blocks.

Purpose: Make the catalog spec match the renderer reality (renderer-as-spec discipline D-16-04). Phase 17's v2 catalog incorrectly stated `(updated)` was the universal autoupdate render and that the v1.0 idempotent-flip variant was "not expressible in v2" -- both statements are factually wrong after Plans 17.1-01/02 land the 3 new statuses + reasons field. This plan replaces that prose with the correct 5-state design from D-18-05.

Output:
- `docs/output-catalog.md` § `/claude:plugin marketplace autoupdate <enable|disable> <name>` (lines 838-882) fully rewritten with 5 catalog-state blocks: `enable-fresh`, `disable-fresh`, `enable-idempotent`, `disable-idempotent`, `failure-not-found`. Each block contains the exact bytes from D-17.1-03.
- `tests/architecture/catalog-uat.test.ts` autoupdate fixture map at lines 1244-1278 REPLACED with the 5 fixtures.
- `docs/messaging-style-guide.md` updates: line 27 (`export type MarketplaceStatus; // 4 literal strings...`) → `// 7 literal strings...`; line 52 (`MARKETPLACE_STATUSES -- the closed set of 4 marketplace status discriminators`) → `... 7 marketplace status discriminators`; any drift prose elsewhere about the autoupdate surface gets a matching refresh.
- `npm run check` exits 0.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md
@.planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-02-SUMMARY.md
@docs/output-catalog.md
@docs/messaging-style-guide.md
@tests/architecture/catalog-uat.test.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite output-catalog.md autoupdate section with 5 state blocks</name>
  <files>docs/output-catalog.md</files>
  <read_first>
    - docs/output-catalog.md (lines 760-840 -- the surrounding `marketplace remove` + `marketplace update` sections for stylistic precedent; lines 838-902 -- the existing autoupdate section being rewritten; lines 884-916 -- the next section for placement reference)
    - .planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md (D-17.1-03 byte-form table; D-17.1-04 full-prose-rewrite mandate; specifics block -- fixture names are normative; Claude's Discretion item allowing optional `### Fresh enable` / `### Fresh disable` sub-headings)
    - .planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-02-SUMMARY.md (verbatim byte forms emitted by the renderer -- these MUST match the catalog blocks exactly)
    - extensions/pi-claude-marketplace/shared/notify.ts (read the freshly-landed renderMpHeader arms to confirm the byte form your catalog will assert against)
  </read_first>
  <action>
    Per D-17.1-04: full prose rewrite of the section `## \`/claude:plugin marketplace autoupdate <enable|disable> <name>\`` (currently lines 838-882). The section heading MUST remain byte-identical: `## \`/claude:plugin marketplace autoupdate <enable|disable> <name>\`` -- the UAT runner keys on this heading. The section starts after the `______________________________________________________________________` horizontal rule at line 836 and ends before the next `______________________________________________________________________` at line 884.

    Rewrite contents:

    Intro paragraph (replaces the existing 1-line intro at line 840): one paragraph stating the marketplace autoupdate surface flips the marketplace persistence record's autoupdate flag with no plugin children. State that V2 distinguishes between 5 outcomes (fresh flip enable / fresh flip disable / idempotent flip / failure-marketplace-not-found) via the per-state catalog blocks below. Cross-reference the list-surface `<autoupdate>` marker on the existing `## /claude:plugin marketplace list` section (lines 686-716) -- the marker is reserved for the list surface and is NOT emitted on state-change arms.

    Sub-section 1 -- `### Fresh enable`. Add `<!-- catalog-state: enable-fresh -->` discriminator line. Add a ```text``` block with EXACTLY these bytes:
    ```
    ● foo [user] (autoupdate enabled)

    /reload to pick up changes
    ```
    (Note the blank line between header and reload-hint; this is the D-16-13 one-blank-line discipline.) Add a one-line prose explainer: "Fresh state change -- the marketplace record was mutated. `mp.status` = `\"autoupdate enabled\"`; severity = info (no severity arg); reload-hint emitted per D-16-12 state-change trigger ladder."

    Sub-section 2 -- `### Fresh disable`. Add `<!-- catalog-state: disable-fresh -->`. Add a ```text``` block with EXACTLY:
    ```
    ● foo [user] (autoupdate disabled)

    /reload to pick up changes
    ```
    Same explainer pattern: `mp.status` = `"autoupdate disabled"`; severity = info; reload-hint emitted.

    Sub-section 3 -- `### Idempotent enable`. Add `<!-- catalog-state: enable-idempotent -->`. Add a ```text``` block with EXACTLY:
    ```
    ● foo [user] (skipped) {already enabled}
    ```
    (No reload-hint trailer -- D-17.1-05 ladder rule.) One-line explainer: "Idempotent no-op -- the flag was already in the requested state. `mp.status` = `\"skipped\"`; `mp.reasons` = `[\"already enabled\"]`; severity = `\"warning\"` (consistent with plugin-level skipped per D-16-11); reload-hint suppressed."

    Sub-section 4 -- `### Idempotent disable`. Add `<!-- catalog-state: disable-idempotent -->`. Add a ```text``` block with EXACTLY:
    ```
    ● foo [user] (skipped) {already disabled}
    ```
    Same explainer pattern with `mp.reasons` = `["already disabled"]`.

    Sub-section 5 -- `### Failure -- marketplace not found`. Add `<!-- catalog-state: failure-not-found -->`. Add a ```text``` block with EXACTLY:
    ```
    ⊘ missing-mp [user] (failed)
    ```
    One-line explainer: "Marketplace persistence record lookup failed. `mp.status` = `\"failed\"`; severity = `\"error\"`; no reload-hint (failed state-change rolled back; nothing landed)."

    Then add a closing paragraph (replacing the v1.0-supersession prose at line 858 and the structural-shape prose at line 872): one paragraph summarizing the severity ladder (fresh → info, skipped → warning, failed → error) and the reload-hint ladder (fresh triggers, skipped/failed suppress). Cross-reference D-17.1-05.

    DELETE the entire existing content of the section between line 840 and line 882 -- including the obsolete `### Enable across multiple marketplaces`, `### Disable`, `### Failure -- marketplace not found` sub-sections and their byte blocks. The v1.0 idempotent-flip paragraph at line 858 ("not expressible in v2") is factually wrong post-amendment -- DELETE it.

    Do NOT touch any other section of the catalog: the `## /claude:plugin marketplace list` (lines 686-716), `## /claude:plugin marketplace add` (lines 718-758), `## /claude:plugin marketplace remove` (lines 760-793), `## /claude:plugin marketplace update` (lines 795-836), `## Manual recovery anchors` (lines 886+) are byte-identical to before.

    After the edit, do NOT run the catalog UAT yet (the fixture map still has the old 3-entry shape). The next task fixes that. CRITICAL: do NOT commit this task in isolation -- Task 2 must land in the same commit (see `<verification>` intra-plan atomicity note below).
  </action>
  <verify>
    <automated>grep -cE '<!-- catalog-state: (enable-fresh|disable-fresh|enable-idempotent|disable-idempotent|failure-not-found) -->' docs/output-catalog.md</automated>
    <expected>Exactly 5 matches.</expected>
  </verify>
  <acceptance_criteria>
    - The section heading `## \`/claude:plugin marketplace autoupdate <enable|disable> <name>\`` is byte-identical to before.
    - The section now contains exactly 5 `<!-- catalog-state: X -->` discriminators with these exact keys: `enable-fresh`, `disable-fresh`, `enable-idempotent`, `disable-idempotent`, `failure-not-found`.
    - Each ```text``` block contains the exact bytes from D-17.1-03 (assert with grep): `grep '● foo \[user\] (autoupdate enabled)' docs/output-catalog.md` returns ≥1; same for `(autoupdate disabled)`, `(skipped) {already enabled}`, `(skipped) {already disabled}`, `⊘ missing-mp [user] (failed)`.
    - The reload-hint trailer `/reload to pick up changes` appears under the `enable-fresh` and `disable-fresh` blocks (the blank line + trailer combo) and does NOT appear under `enable-idempotent`, `disable-idempotent`, or `failure-not-found`.
    - The obsolete `enable-mixed`, `disable-mixed`, and old `failure-not-found` (3-mp-mixed) blocks are gone.
    - The "v1.0 idempotent-flip variant ... not expressible in v2" prose paragraph is gone.
    - All other catalog sections are byte-identical.
    - No commit lands at the end of Task 1; the working tree is staged for the single atomic commit that lands Tasks 1 + 2 together (per intra-plan atomicity note in `<verification>`).
  </acceptance_criteria>
  <done>The catalog spec matches the renderer reality (renderer-as-spec D-16-04) for the autoupdate surface. The next task aligns the UAT fixture map.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace catalog-uat.test.ts autoupdate fixture map with 5 new fixtures</name>
  <files>tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - tests/architecture/catalog-uat.test.ts (lines 1085-1259 -- the marketplace-family fixture map; lines 1244-1278 -- the specific autoupdate fixture entry to replace; also read the FIXTURES map header and the test runner shape at the top of the file to confirm the (section, state) keying convention)
    - .planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md (D-17.1-03 -- the 5-fixture table with exact payload + expected bytes + severity + reload-hint)
    - docs/output-catalog.md (the freshly-written autoupdate section -- the UAT test reads catalog block bytes from this file and compares against `notify()` output)
  </read_first>
  <action>
    Per D-17.1-03: REPLACE the autoupdate fixture map entry at lines 1244-1278 with a new 5-entry map. The entry key MUST be byte-identical to before: `"/claude:plugin marketplace autoupdate <enable|disable> <name>"` (the UAT runner reads this string to find the matching catalog section heading).

    New entry shape (delete the existing `enable-mixed`, `disable-mixed`, `failure-not-found` 3-entry inner object and replace with 5 entries):

    Entry 1 -- `"enable-fresh"`: payload `{ marketplaces: [{ name: "foo", scope: "user", status: "autoupdate enabled", plugins: [] }] }`. `pi: piWithBothLoaded()`. NO `expectedSeverity` field (info routing -- undefined).

    Entry 2 -- `"disable-fresh"`: payload `{ marketplaces: [{ name: "foo", scope: "user", status: "autoupdate disabled", plugins: [] }] }`. `pi: piWithBothLoaded()`. NO `expectedSeverity`.

    Entry 3 -- `"enable-idempotent"`: payload `{ marketplaces: [{ name: "foo", scope: "user", status: "skipped", reasons: ["already enabled"], plugins: [] }] }`. `pi: piWithBothLoaded()`. `expectedSeverity: "warning"`.

    Entry 4 -- `"disable-idempotent"`: payload `{ marketplaces: [{ name: "foo", scope: "user", status: "skipped", reasons: ["already disabled"], plugins: [] }] }`. `pi: piWithBothLoaded()`. `expectedSeverity: "warning"`.

    Entry 5 -- `"failure-not-found"`: payload `{ marketplaces: [{ name: "missing-mp", scope: "user", status: "failed", plugins: [] }] }`. `pi: piWithBothLoaded()`. `expectedSeverity: "error"`.

    Match the existing inner-object lexical style at lines 1245-1277 (indentation, trailing-comma discipline, field order `pi` → `expectedSeverity` → `message`). Where `expectedSeverity` is undefined (Entries 1, 2), OMIT the field entirely rather than setting it to `undefined` -- match the existing precedent at lines 1245-1259 (the old `enable-mixed` entry has no `expectedSeverity`).

    Do NOT touch any other section in the FIXTURES map: the `marketplace add`, `marketplace remove`, `marketplace update`, `marketplace list` entries are byte-identical. The `manual-recovery-anchors` entry at line 1283 is byte-identical.

    After the edit, run the catalog UAT test: `node --test tests/architecture/catalog-uat.test.ts`. ALL fixtures (the 5 new autoupdate ones + every existing marketplace/plugin entry) MUST pass byte-equality. If any of the 5 new fixtures fail, the most likely cause is a byte-form mismatch between the catalog block (Plan 17.1-03 Task 1) and the renderer output (Plan 17.1-02 Tasks 1-2): read both and confirm character-by-character.
  </action>
  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts 2>&1 | tail -10</automated>
    <expected>The UAT runner reports all fixtures passing; 0 failures. The fixture count for the autoupdate section grew from 3 to 5.</expected>
  </verify>
  <acceptance_criteria>
    - The FIXTURES map entry at the key `"/claude:plugin marketplace autoupdate <enable|disable> <name>"` has exactly 5 inner-object entries with keys `enable-fresh`, `disable-fresh`, `enable-idempotent`, `disable-idempotent`, `failure-not-found`.
    - Each entry's `message` field exactly matches the payload shape from D-17.1-03 (name, scope, status, optional reasons, plugins: []).
    - Entries 3, 4, 5 declare `expectedSeverity: "warning"` (3, 4) and `expectedSeverity: "error"` (5).
    - Entries 1, 2 OMIT `expectedSeverity` (info routing means undefined).
    - `node --test tests/architecture/catalog-uat.test.ts` exits 0 with all fixtures passing.
    - The catalog UAT byte-equality check runs `notify(mockCtx, mockPi, message)` for each fixture and compares against the corresponding ```text``` block in `docs/output-catalog.md`; all 5 new comparisons pass.
  </acceptance_criteria>
  <done>The renderer / catalog / UAT triangle is consistent: the renderer emits bytes that match the catalog blocks that match the fixture map. Plan 18-02 can construct payloads with confidence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Capped style-guide drift sweep for messaging-style-guide.md (7 literal strings)</name>
  <files>docs/messaging-style-guide.md</files>
  <read_first>
    - docs/messaging-style-guide.md (lines 20-65 -- the Public Surface + closed-set pointer sections; lines 50-54 specifically -- the MARKETPLACE_STATUSES closed-set descriptor; check the entire file for additional `4 literal` or `4 entries` or `MARKETPLACE_STATUSES` references via grep so no stale prose survives)
    - .planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md (D-17.1-06 -- the style guide is a pointer doc; only the count + any drift prose gets updated)
  </read_first>
  <action>
    Per D-17.1-06: two KNOWN edits at lines 27 and 52, plus a discovery grep capped per checker Issue #5.

    Edit A (line 27) -- KNOWN: `export type MarketplaceStatus; // 4 literal strings, derived from MARKETPLACE_STATUSES tuple` → change `4 literal strings` to `7 literal strings` (the rest of the line is byte-identical).

    Edit B (line 52) -- KNOWN: the bullet `- \`MARKETPLACE_STATUSES\` -- the closed set of 4 marketplace status discriminators. \`MarketplaceStatus = (typeof MARKETPLACE_STATUSES)[number]\`. Same rule. v1.3's \`marketplace skipped\` rendering case re-routes through \`updated\` with an empty \`plugins: []\` per the always-marketplace-header spec.` -- change `4 marketplace status discriminators` to `7 marketplace status discriminators`. Then DELETE the obsolete v1.3-re-routing sentence (`v1.3's \`marketplace skipped\` rendering case re-routes through \`updated\`...`) which is now factually wrong -- replace it with one sentence: `The 3 autoupdate-surface statuses (\`autoupdate enabled\`, \`autoupdate disabled\`, \`skipped\`) were added in Phase 17.1 per D-17.1-01 to support the user-locked surface design in D-18-05.`

    **Discovery cap (per checker Issue #5):** Run a discovery grep across the full file for any additional drift hits beyond lines 27 and 52:

      grep -nE "4 (literal|marketplace|entries|entrant)" docs/messaging-style-guide.md

    Apply the cap rule:
      1. Lines 27 and 52 are KNOWN drift sites and MUST be fixed regardless of grep findings.
      2. ANY additional drift hit beyond those two known lines is allowed to be fixed in this plan IFF the total volume is **≤ 3 additional small line edits** (one-line edits, no structural changes -- same `4 → 7` substitution as Edits A/B).
      3. If the discovery grep returns MORE than 3 additional hits beyond lines 27/52, OR if any hit requires a multi-line edit (e.g., reformatting a paragraph, restructuring a heading, rewriting a code-block example), STOP and ESCALATE to the orchestrator: do NOT silently expand the scope of this plan. The escalation message should list the drift hits with `file:line` and a one-line description; the orchestrator will decide whether to add a follow-up plan in this phase or defer to a later phase.

    The discovery grep also covers other `4` references unrelated to marketplace status (e.g., "4-space indent" for the per-plugin cause chain, "Phase 4" or "Phase 14" anywhere). Those are unrelated and MUST NOT be changed -- read each grep hit in context before edit. Only `4`-references about marketplace-status counts or autoupdate-surface phrasing are in scope.

    Optional: scan for any prose specifically about the autoupdate surface (search `grep -inE "autoupdate" docs/messaging-style-guide.md`) and ensure no statement contradicts the new 5-state design. If a stale paragraph claims "marketplace autoupdate always renders (updated)" or similar, update it per D-17.1-06 -- but if the fix exceeds 3 lines or requires restructuring, escalate per the cap rule above.

    Do NOT touch the `PLUGIN_STATUSES` pointer (10 literal strings; unchanged) or the `DEPENDENCIES` pointer (2 literal strings; unchanged).

    After the edits, run `npm run check` and confirm full green status (the style guide may be subject to drift gates that read its YAML frontmatter; if a frontmatter check exists and breaks on a stale count, update the frontmatter count too -- that frontmatter update counts toward the ≤3 additional-edits budget).
  </action>
  <verify>
    <automated>grep -cE "7 literal strings" docs/messaging-style-guide.md && grep -cE "7 marketplace status discriminators" docs/messaging-style-guide.md</automated>
    <expected>Both greps return ≥1 match.</expected>

    Confirm the cap was respected -- discovery grep returns 0 marketplace-related drift hits:
      grep -nE "4 (literal|marketplace) (strings|status discriminators)" docs/messaging-style-guide.md
    EXPECTED: 0 matches (all known + capped-discovery drift sites are normalized to 7).
  </verify>
  <acceptance_criteria>
    - Line 27 (or wherever `export type MarketplaceStatus; // 4 literal strings` lives) now reads `7 literal strings`.
    - Line 52 (or wherever `MARKETPLACE_STATUSES -- the closed set of 4 marketplace status discriminators` lives) now reads `7 marketplace status discriminators`, and the v1.3-re-routing sentence is replaced with the D-17.1-01 / D-18-05 cross-reference.
    - `grep -nE "the closed set of 4" docs/messaging-style-guide.md` returns 0 matches for marketplace-specific phrases.
    - Additional drift hits beyond lines 27/52 (if any) are either fixed in-plan (≤ 3 small line edits) OR escalated to the orchestrator with a written list (per checker Issue #5 cap rule).
    - `npm run check` exits 0.
    - No edits to PLUGIN_STATUSES or DEPENDENCIES pointers; no edits to grammar/* files (the `reasons.ts` runtime tuple already has `"already enabled"` and `"already disabled"` per Phase 17.1 CONTEXT verification -- confirm via `grep "already enabled" extensions/pi-claude-marketplace/shared/grammar/reasons.ts` returns ≥1; no source change here).
  </acceptance_criteria>
  <done>The style guide pointer doc is consistent with the type model. `npm run check` is green across renderer + catalog + UAT + style guide. Drift cap respected per checker Issue #5. Plan 17.1-04 (ADR amendment) can land in parallel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec ↔ implementation | The catalog block IS the user-visible contract; the UAT fixture verifies the renderer matches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-03-01 | Tampering | catalog block byte form | mitigate | Catalog UAT byte-equality runner (`tests/architecture/catalog-uat.test.ts`) drives `notify()` through each fixture and asserts byte-equality against the catalog block. Discrepancy = test failure. |
| T-17.1-03-02 | Tampering | catalog-state discriminator naming | mitigate | Both the catalog block (`<!-- catalog-state: enable-fresh -->`) and the fixture map key (`"enable-fresh"`) are normative (D-17.1-03); the UAT runner keys on both. Mismatch = test failure. |
| T-17.1-03-03 | Information Disclosure | obsolete v1-supersession prose in catalog | mitigate | Task 1 explicitly deletes the "not expressible in v2" paragraph and the multi-mp `enable-mixed` / `disable-mixed` blocks; the new section design is single-marketplace per state. |
| T-17.1-03-04 | Tampering | style-guide drift (4 vs 7) | mitigate | Task 3's discovery grep + cap rule (per checker Issue #5) catches drift; future contributors are guided by the type model itself (compile-time arch test from Plan 17.1-01) more strongly than by the prose. |
| T-17.1-03-05 | Tampering | intra-plan compile-fail window | mitigate | Per checker Issue #6, Tasks 1 and 2 are committed as a single atomic commit; the catalog-UAT test would fail if Task 1 landed alone, so the verification block locks the co-commit discipline. |
| T-17.1-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
After all three tasks land:

```bash
# Full repo gate
npm run check
# EXPECTED: exits 0

# Catalog UAT specifically
node --test tests/architecture/catalog-uat.test.ts
# EXPECTED: all fixtures pass; the autoupdate section's 5 new fixtures pass byte-equality

# Per-variant unit tests still pass (regression check from Plan 17.1-02)
node --test tests/shared/notify-v2.test.ts
# EXPECTED: all 37 tests pass

# Targeted source assertions
grep -cE '<!-- catalog-state: (enable-fresh|disable-fresh|enable-idempotent|disable-idempotent|failure-not-found) -->' docs/output-catalog.md
# EXPECTED: 5

grep -c '"enable-fresh"\|"disable-fresh"\|"enable-idempotent"\|"disable-idempotent"' tests/architecture/catalog-uat.test.ts
# EXPECTED: ≥4 (one each for the 4 new keys; the existing "failure-not-found" key may already match)

grep -cE "7 literal strings" docs/messaging-style-guide.md
# EXPECTED: ≥1

# Confirm stale prose is gone
grep -cE "not expressible in v2|enable-mixed|disable-mixed" docs/output-catalog.md
# EXPECTED: 0 (or only matches in unrelated contexts -- verify visually)
```

**Intra-plan atomicity -- LOCKED (per checker Issue #6 and CLAUDE.md `--no-verify` prohibition):**

Tasks 1 and 2 form a SINGLE ATOMIC COMMIT. DO NOT commit between them -- the catalog-UAT test in `tests/architecture/catalog-uat.test.ts` parses `docs/output-catalog.md` for fenced blocks and round-trips against the fixture map; if Task 1 lands without Task 2 (or vice versa), the test fails and `npm run check` blocks the pre-commit hook. CLAUDE.md forbids `--no-verify`, so the only hook-compliant path is a single commit covering BOTH `docs/output-catalog.md` AND `tests/architecture/catalog-uat.test.ts` in the same diff.

Task 3 (style-guide drift sweep on `docs/messaging-style-guide.md`) is independent of Tasks 1 + 2's test/catalog dance -- its edits don't affect the catalog-UAT runner. The recommended discipline is to include Task 3 in the SAME single commit as Tasks 1 + 2, making one clean commit for the entire plan. Splitting Task 3 into a separate immediately-following commit is acceptable but adds friction; do not split unless there is a concrete reason.

Suggested combined commit title:
    docs(catalog): rewrite marketplace autoupdate section for 5-state design (D-17.1-03)

Commit body should cite D-17.1-03 (catalog rewrite), D-17.1-04 (full-prose-rewrite), D-17.1-06 (style guide update), and explicitly note that Tasks 1 + 2 are an atomic commit because the catalog-UAT round-trip test would fail if either landed alone (per checker Issue #6).

Pre-commit hook: per CLAUDE.md, run `pre-commit run --files docs/output-catalog.md tests/architecture/catalog-uat.test.ts docs/messaging-style-guide.md` before `git commit`. If committing from a worktree, prefix with `SKIP=trufflehog` per CLAUDE.md. Do NOT use `--no-verify`.

This plan and Plan 17.1-04 are independent within Wave 3 (per D-17.1-01) and can land in either commit order. There is NO dependency between them -- see Plan 17.1-04 `<verification>` for the parallel-landing confirmation.
</verification>

<success_criteria>
- `docs/output-catalog.md` autoupdate section has 5 catalog-state blocks with the normative discriminators and exact byte forms.
- `tests/architecture/catalog-uat.test.ts` autoupdate fixture map has 5 fixtures matching the catalog discriminators.
- `docs/messaging-style-guide.md` MarketplaceStatus pointer says `7 literal strings`; the v1.3 re-routing sentence is replaced with a D-17.1-01 / D-18-05 cross-reference.
- Catalog UAT byte-equality is GREEN for all 5 new fixtures.
- Tasks 1 and 2 land in a single atomic commit (catalog body + fixture map together); no commit occurs between them.
- Style-guide discovery cap respected per checker Issue #5 -- additional drift hits ≤ 3 small lines stay in plan, larger drift escalated.
- `npm run check` exits 0 at commit time.
- The `reasons.ts` runtime tuple already contains `"already enabled"` and `"already disabled"` (verify-only; no source change).
</success_criteria>

<output>
Create `.planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-03-SUMMARY.md` when done, recording:
- The line numbers where the catalog section ends up after the rewrite (Plan 17.1-04 may want to cross-reference).
- The fixture map line range after replacement.
- Confirmation that catalog UAT is green with all 5 new fixtures.
- The list of any additional style-guide drift hits found by the discovery grep (beyond lines 27/52) and whether each was fixed in-plan (within the ≤3 cap) or escalated to the orchestrator.
- Confirmation that Tasks 1 and 2 landed in a single atomic commit -- paste commit SHA + title.
- Confirmation that `npm run check` was green at commit time.
</output>
