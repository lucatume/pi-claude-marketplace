---
phase: 16-renderer-public-api-alongside-v1
plan: 06
type: execute
wave: 6
depends_on:
  - 16-02
  - 16-05
files_modified:
  - tests/shared/notify-v2.test.ts
autonomous: true
requirements:
  - SNM-30
must_haves:
  truths:
    - "tests/shared/notify-v2.test.ts exists as a new file with ≥ 22 passing per-status unit tests covering all 10 PluginNotificationMessage variants, all 5 marketplace-header cases (4 MarketplaceStatus members + undefined list-surface), plus the cross-cutting cases enumerated in D-16-17 PLUS the BLOCKER-coverage cases that affirmatively assert (a) no `[scope]` bracket emission on the 8 scope-bearing variants when `p.scope === undefined` (non-orphan-fold case; locks in plan-04 BLOCKER-1 fix) and (b) the `case undefined:` marketplace-header arm returns a bare header (no autoupdate/lastUpdatedAt tokens) and does NOT throw when `mp.details === undefined` (empty-list-surface case; locks in plan-03 BLOCKER-3 fix)"
    - "Each test constructs a structured NotificationMessage / UsageErrorMessage payload inline, invokes notify(ctx, pi, msg) or notifyUsageError(ctx, msg) via a mock ctx + mock pi, and asserts the EXACT string and severity argument(s) passed to ctx.ui.notify via `assert.deepEqual(ctx.ui.notify.mock.calls[0]!.arguments, [...])`"
    - "Mock pi shape is `{ getAllTools: () => [...] }` per D-16-16; mock ctx shape is `{ ui: { notify: mock.fn() } }` reusing the V1 test idiom from tests/shared/notify.test.ts:17-23"
    - "The test file's expected strings are anchored ONLY to D-16-04..D-16-15 grammar and to plan 03+04+05 byte-form decisions surfaced in 16-03-SUMMARY / 16-04-SUMMARY / 16-05-SUMMARY -- D-16-18: no import from docs/output-catalog.md (that's Phase 17)"
    - "The header comment of tests/shared/notify-v2.test.ts contains a mini-spec block enumerating the v2 grammar rules (icon dispatch, scope-bracket carve-out, scope-bracket conditional emission for the 8 scope-bearing variants -- non-orphan-fold = absent bracket, orphan-fold = present bracket, reasons-block format, soft-dep marker injection, reload-hint trigger, severity ladder, indent levels for cause/rollback, and the empty-list-surface marketplace-header bare-header sub-branch) -- D-16-04 authority resolution: this file is the de facto v2 spec until Phase 17 lifts it into docs/output-catalog.md"
    - "tests/shared/notify.test.ts (the V1 file) is NOT modified -- Phase 21 deletes it; per D-16-16 the V1 file stays intact"
  artifacts:
    - path: tests/shared/notify-v2.test.ts
      provides: per-status unit-test suite for V2 notify() + notifyUsageError() + mini-spec header + BLOCKER-coverage tests
      contains: "import { notify, notifyUsageError } from \"../../extensions/pi-claude-marketplace/shared/notify.ts\""
      min_lines: 270
  key_links:
    - from: tests/shared/notify-v2.test.ts
      to: extensions/pi-claude-marketplace/shared/notify.ts notify + notifyUsageError exports
      via: direct named import
      pattern: "from \"\\.\\./\\.\\./extensions/pi-claude-marketplace/shared/notify\\.ts\""
    - from: tests/shared/notify-v2.test.ts mock pi
      to: platform/pi-api.ts softDepStatus(pi) probe behavior (inspects pi.getAllTools())
      via: structural mock matching the probe's tool inspection
      pattern: "getAllTools: \\(\\) =>"
---

<objective>
Create `tests/shared/notify-v2.test.ts` as the per-status unit-test suite for the V2 `notify()` and `notifyUsageError()` entry points landed in plans 02-05. Per D-16-17 the suite covers ≥ 22 cases (target ≥ 29 per PATTERNS.md taxonomy + the 2 BLOCKER-coverage cases added by this revision): 10 plugin-status variants × 1 baseline test each, 5 marketplace-header cases (4 `MarketplaceStatus` members + 1 `undefined` list-surface), plus orphan-fold present + orphan-fold absent, `rollbackPartial`, multi-cause cascade, multi-marketplace, empty `plugins: []`, empty `marketplaces: []`, empty-list-surface (`mp.status === undefined` AND `mp.details === undefined`), single-plugin, multi-plugin, severity routing (3 tiers), reload-hint trigger + suppression, and `notifyUsageError`. The file's header comment carries the v2 mini-spec block per D-16-04 authority resolution; this file IS the de facto v2 spec until Phase 17 lifts the grammar into `docs/output-catalog.md`.

Purpose: SNM-30 mandates per-status unit tests on `notify()` with mock ctx asserting exact-string output. Phase 16 has no docs/output-catalog.md / catalog UAT migration (that's Phase 17), so the per-variant assertions in this file are the binding correctness gate for V2 grammar in Phase 16. Phase 18-20 callers will trust this contract; Phase 17 will lift it into the catalog UAT runner. Two additional BLOCKER-coverage tests lock in the plan-03 BLOCKER-3 fix (empty-list-surface bare-header sub-branch) and the plan-04 BLOCKER-1 fix (conditional scope-bracket emission on the 8 scope-bearing variants).

Output: A single new test file (≥ 270 lines, ≥ 22 passing cases) under `tests/shared/notify-v2.test.ts`. The existing `tests/shared/notify.test.ts` V1 file is NOT modified (D-16-16; Phase 21 deletes V1 testing alongside V1 wrappers). `npm run check` must stay GREEN with the new test file included in the `node --test` discovery.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-renderer-public-api-alongside-v1/16-CONTEXT.md
@.planning/phases/16-renderer-public-api-alongside-v1/16-PATTERNS.md
@.planning/phases/16-renderer-public-api-alongside-v1/16-02-SUMMARY.md
@.planning/phases/16-renderer-public-api-alongside-v1/16-03-SUMMARY.md
@.planning/phases/16-renderer-public-api-alongside-v1/16-04-SUMMARY.md
@.planning/phases/16-renderer-public-api-alongside-v1/16-05-SUMMARY.md
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/platform/pi-api.ts
@tests/shared/notify.test.ts
@tests/architecture/notify-types.test.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/shared/notify-v2.test.ts with mini-spec header and per-status unit suite</name>
  <files>tests/shared/notify-v2.test.ts</files>
  <read_first>
    - tests/shared/notify.test.ts (FULL FILE -- V1 reference; mirror the `MockCtx` interface, `makeCtx()` helper, `node:test` import style, `assert.deepEqual(ctx.ui.notify.mock.calls[0]!.arguments, [...])` assertion shape, and the `ctx as never` cast pattern verbatim; do NOT modify this file)
    - extensions/pi-claude-marketplace/shared/notify.ts (full file as it stands after plans 02+03+04+05 -- need exact byte forms of every helper output to write correct expected strings; cross-reference 16-03-SUMMARY, 16-04-SUMMARY, 16-05-SUMMARY for the chosen byte-form decisions on marketplace-header arms, per-plugin-status row shapes, soft-dep marker insertion, rollbackPartial child row, empty-marketplaces sentinel, reload-hint append discipline. PAY SPECIAL ATTENTION to lines 269-403 -- the 10 plugin variant interfaces -- to know which fields are required vs optional on each variant; the BLOCKER-coverage tests below depend on this distinction)
    - extensions/pi-claude-marketplace/platform/pi-api.ts lines 53, 67 (the `getAllTools()` inspection points in `hasLoadedPiSubagents` and `hasLoadedPiMcpAdapter` -- this is what the mock `pi.getAllTools()` needs to satisfy)
    - tests/architecture/notify-types.test.ts (FULL FILE -- the Phase 15 per-variant alias proofs document the required shape of every variant; the test fixtures in this plan must construct payloads satisfying those types)
    - .planning/phases/16-renderer-public-api-alongside-v1/16-CONTEXT.md sections `<decisions>` D-16-16 (mock-ctx + mock-pi shapes), D-16-17 (test taxonomy ≥ 20 cases; this plan extends to ≥ 22 with the 2 BLOCKER-coverage tests), D-16-18 (no import from docs/output-catalog.md; fixtures are the seed for Phase 17) and `<specifics>` "Mock pi shape" bullet (`piWithSubagentsLoaded` / `piWithMcpLoaded` / `piWithNothingLoaded` helper sketches) and `<canonical_refs>` lines on `MarketplaceDetails` (REQUIRED `autoupdate: boolean`, OPTIONAL `lastUpdatedAt?: string`) and on `MarketplaceNotificationMessage.details?` (OPTIONAL, INDEPENDENT of `status?`)
    - .planning/phases/16-renderer-public-api-alongside-v1/16-PATTERNS.md section `### tests/shared/notify-v2.test.ts (NEW FILE)` (full -- gives the verbatim-copy + new-idiom tables and the test-taxonomy table)
    - 16-05-SUMMARY (the BYTE-FORM decisions for empty-marketplaces sentinel, rollbackPartial child row, multi-marketplace join) are the source of truth for the expected strings in this plan; if those summaries are absent or unclear, re-read the implementation in shared/notify.ts directly
    - 16-03-SUMMARY (specifically the SUB-BRANCH A and SUB-BRANCH B byte-form decisions for the `case undefined:` marketplace-header arm -- the BLOCKER-coverage empty-list-surface test below asserts the SUB-BRANCH A byte form)
    - 16-04-SUMMARY (specifically the byte-form decision for the orphan-fold-ABSENT case -- the BLOCKER-coverage non-orphan-fold test below asserts that the row contains NO `[scope]` bracket when `p.scope === undefined`)
  </read_first>
  <action>
Create a NEW file `tests/shared/notify-v2.test.ts`. The file MUST contain, in this order:

SECTION A -- header comment (mini-spec). Place at the top of the file (before imports). The comment block MUST enumerate the v2 grammar rules so reviewers can audit Phase 16's binding contract in one place (D-16-04 authority resolution + planner mini-spec discretion per CONTEXT.md). Required content (each rule on its own bullet line):
  - Icon dispatch (which status maps to which of `●` / `○` / `⊘`).
  - Scope-bracket placement (carve-out): `available` and `unavailable` variants have NO `scope` field at all -- the bracket is unconditionally omitted on those rows per MSG-PL-6 / SNM-11.
  - Scope-bracket placement (conditional emission, 8 scope-bearing variants): for `installed` / `updated` / `reinstalled` / `uninstalled` / `upgradable` / `skipped` / `failed` / `manual recovery`, the `scope?: Scope` field is OPTIONAL per Phase 15. The `[<scope>]` bracket is emitted ONLY when `p.scope !== undefined`. The typical case (cascade rows inheriting the marketplace's scope via the header) leaves `p.scope` undefined and emits NO bracket. The orphan-fold case (caller sets `p.scope` explicitly to drive the inline scope inflection) emits the bracket inline on the row.
  - Reasons-block format: `{reason1, reason2}` brace block joined by `, `; soft-dep markers `requires pi-subagents` / `requires pi-mcp` go INSIDE the same brace block, NOT in separate braces.
  - Soft-dep injection rule: emit the marker when the row's `dependencies` includes the dep AND the probe says it's not loaded.
  - Marketplace header shape (state-change arms) and the `undefined` list-surface arm:
    - SUB-BRANCH A: when `mp.details === undefined`, return a BARE header (just `${icon} ${mp.name} [${mp.scope}]` with no trailing autoupdate/lastUpdatedAt tokens). This is the empty-list-surface case. Plan 03 BLOCKER-3 fix; plan 03 GUARDS the read explicitly so the arm cannot crash at runtime.
    - SUB-BRANCH B: when `mp.details !== undefined`, append the autoupdate marker and optional lastUpdatedAt token.
  - Body composition: marketplace header at column 0, plugin rows at 2-space indent, multi-marketplace blocks joined by one blank line.
  - Per-plugin cause-chain: 4-space indent under failed/manual-recovery rows when `cause` set.
  - rollbackPartial child rows: 4-space indent; nested cause-chain at 6-space indent.
  - Empty `marketplaces: []` sentinel: exact bytes (cite the value chosen in plan 05).
  - Reload-hint trigger ladder per D-16-12 (state-changing plugin statuses + state-changing marketplace statuses, NOT failed).
  - Reload-hint append: `${body}\n\n/reload to pick up changes`.
  - Severity ladder: first-match failed → error, else any skipped/manual-recovery → warning, else success (omit 2nd arg).
  Close the comment with a final sentence: "Authority: this file is the de facto v2 spec until Phase 17 lifts it into docs/output-catalog.md (SNM-19 / SNM-20 / SNM-31)."

SECTION B -- imports + mock helpers. Mirror the V1 file shape:

```
import assert from "node:assert/strict";
import test, { mock } from "node:test";

import {
  notify,
  notifyUsageError,
  type NotificationMessage,
  type UsageErrorMessage,
} from "../../extensions/pi-claude-marketplace/shared/notify.ts";
```

Add `MockCtx` and `makeCtx()` mirroring `tests/shared/notify.test.ts:17-23`. Add `MockPi` interface and three helpers per CONTEXT `<specifics>`:
  - `interface MockPi { getAllTools: () => { name?: string; sourceInfo?: { source?: string } }[]; }`
  - `function piWithBothLoaded(): MockPi` -- returns `[{ name: "subagent" }, { name: "mcp" }]` (both probes true).
  - `function piWithSubagentsLoaded(): MockPi` -- only subagents true; mcp false.
  - `function piWithMcpLoaded(): MockPi` -- only mcp true; subagents false.
  - `function piWithNothingLoaded(): MockPi` -- empty array; both probes false.

Use `ctx as never` and `pi as never` casts at the call sites (matches V1 file's structural-mismatch suppression).

SECTION C -- ≥ 22 unit tests grouped by category. Each test MUST follow the V1 pattern: arrange (build the structured payload + ctx + pi) → act (`notify(ctx as never, pi as never, payload)` or `notifyUsageError(ctx as never, payload)`) → assert (`assert.equal(ctx.ui.notify.mock.calls.length, 1)` then `assert.deepEqual(ctx.ui.notify.mock.calls[0]!.arguments, [expectedString])` or `[expectedString, "warning" | "error"]`).

Required test cases (one `test("...", () => { ... })` per case, total ≥ 22):

Per-plugin-status variants (10 cases, one per `PluginNotificationMessage` discriminant; each uses a single-plugin payload inside a single marketplace header so the 2-line body shape is asserted along with the per-row grammar. For these baseline tests, OMIT `p.scope` so the test exercises the typical non-orphan-fold path; the row must NOT contain a `[scope]` bracket on these baselines -- the bracket appears only in the orphan-fold cases below):
1. `installed` plugin with `dependencies: []` (empty array; field is REQUIRED on this variant) → header + indented `(installed)` row, no soft-dep marker, no `[scope]` bracket on the row (`p.scope` omitted).
2. `installed` plugin with `dependencies: ["agents"]` and probe says agents NOT loaded → header + row ending in `{requires pi-subagents}`.
3. `updated` plugin (from "1.0.0" → to "1.1.0") with `dependencies: ["mcp"]` and probe says mcp NOT loaded → row with `composeVersionArrow` arrow form and `{requires pi-mcp}` marker.
4. `reinstalled` plugin with `dependencies: ["agents", "mcp"]` and probe says BOTH loaded → row WITHOUT any soft-dep marker.
5. `uninstalled` plugin → row with `(uninstalled)` and no version, no soft-dep marker. (Variant has NO `dependencies` field; do NOT include one in the payload.)
6. `available` plugin → row WITHOUT scope bracket (carve-out per SNM-11; variant has NO `scope` field at all), icon `○`, status `(available)`.
7. `unavailable` plugin with `reasons: ["host missing"]` → row WITHOUT scope bracket (carve-out; variant has NO `scope` field), icon `⊘`, status `(unavailable)`, `reasons` rendered in `{host missing}` brace block. (Variant has REQUIRED `reasons` field.)
8. `upgradable` plugin with `version: "1.0.0"` and `reasons: ["1.2.0 available upstream"]` → row with version slot and `(upgradable)` and `{1.2.0 available upstream}` brace block.
9. `skipped` plugin with `reasons: ["already at latest"]` → row with `(skipped)` and `{already at latest}` brace block.
10. `failed` plugin with `reasons: ["network"]`, no `cause` and no `rollbackPartial` → row with `(failed)` and `{network}` brace block, no trailing indented lines. (Variant has REQUIRED `reasons` field per D-15-01.)

Marketplace-header variants (5 cases, each with empty `plugins: []` so the assertion focuses on the header byte form):
11. `mp.status === "added"` with `details: undefined` → state-change header line only (state-change arms ignore details).
12. `mp.status === "removed"` with `details: undefined` → header line only.
13. `mp.status === "updated"` with `details: undefined` → header line only.
14. `mp.status === "failed"` with `details: undefined` → header line only.
15. `mp.status === undefined` (list-surface case) with `details: { autoupdate: true, lastUpdatedAt: "<iso-string>" }` → SUB-BRANCH B: header with autoupdate marker + lastUpdatedAt tokens.

Cross-cutting cases (≥ 7 additional cases) PLUS the 2 BLOCKER-coverage cases (16a, 17a):
16. Empty `plugins: []` on an "added" marketplace → header-only block; reload-hint APPENDED at end (state-changing marketplace status triggers).
17. Empty `marketplaces: []` → exact sentinel from 16-05-SUMMARY (e.g. `"(no marketplaces)"`); no reload-hint (no state-changing marketplace statuses).
17a. **BLOCKER-COVERAGE (locks in plan-03 BLOCKER-3 fix)**: SUB-BRANCH A empty-list-surface payload -- single marketplace with `status: undefined`, `details: undefined` (BOTH absent independently per Phase 15's optional-independent typing), and `plugins: []`. Expected output: the BARE marketplace header from SUB-BRANCH A of `renderMpHeader` (`${ICON_INSTALLED} ${mp.name} [${mp.scope}]` with NO trailing autoupdate/lastUpdatedAt tokens). Critical assertion: the call MUST NOT throw -- the `case undefined:` arm of `renderMpHeader` explicitly guards `mp.details === undefined` before reading `mp.details.autoupdate` per plan 03's BLOCKER-3 fix. Reload-hint MUST be suppressed (neither plugin nor marketplace status is in the trigger set). The test name should explicitly call out the bare-header sub-branch: `test("notify renders bare marketplace header when mp.status and mp.details are both undefined (no-crash, BLOCKER-3 coverage)", ...)`. Use template-string-with-explicit-substitutions for the expected output anchored to 16-03-SUMMARY's SUB-BRANCH A byte form.
18. Single-plugin payload (re-using one of the per-status fixtures is acceptable -- can be a dedicated test that explicitly asserts the 2-line shape `header\n  row`).
19. Multi-plugin payload (3 installed plugins under one "added" marketplace) → header + 3 indented rows in caller-supplied order (verify the order is NOT re-sorted alphabetically; pass plugins in non-alphabetical order and assert it's preserved per D-16-06). For each row, OMIT `p.scope` (non-orphan-fold baseline).
20. Multi-marketplace payload (2 "added" marketplaces with 1 plugin each) → blocks separated by one blank line, reload-hint appended at end.
21. Orphan-fold PRESENT: a plugin row with `scope: "user"` explicitly set inside a marketplace header with `scope: "project"` → plugin row's `[user]` bracket reflects the plugin's scope; header's `[project]` bracket reflects the marketplace's scope. (Variant must be one of the 8 that carry `scope?` -- e.g. installed.)
21a. **BLOCKER-COVERAGE (locks in plan-04 BLOCKER-1 fix)**: Orphan-fold ABSENT -- the same `installed` plugin payload as test 21 BUT with `p.scope` OMITTED (undefined). Expected output: the plugin row contains NO `[scope]` bracket at all -- the `renderScopeBracket(p.scope)` helper yields the empty string when `p.scope === undefined`, and `joinTokens` filters the empty slot out of the final composition. The marketplace header still emits its own `[<mp.scope>]` bracket (e.g. `[project]`). Critical assertion: the row substring MUST NOT contain `[undefined]`, MUST NOT contain `[project]` (that's the header's, not the row's), and MUST NOT contain ANY `[...]` bracket between the plugin name and the version/status slots. The test name should call out the non-orphan-fold case: `test("notify omits scope bracket on plugin row when p.scope is undefined (non-orphan-fold, BLOCKER-1 coverage)", ...)`. Use a tightly-anchored `assert.equal` (or `assert.deepEqual` on the arguments array) so the test fails LOUDLY if the implementation regresses to an unconditional `[${p.scope}]` interpolation (which would produce the literal string `[undefined]`).
22. `failed` plugin with `rollbackPartial: [{ phase: "skills" }, { phase: "agents" }]` and no causes → row with `(failed)` followed by 4-space-indented child rows for each phase per 16-05-SUMMARY byte form.
23. `failed` plugin with `cause: new Error("inner", { cause: new Error("root") })` and `rollbackPartial: [{ phase: "skills", cause: new Error("EACCES") }]` → row + 4-space-indent cause-chain trailer + 4-space-indent rollback-partial child + 6-space-indent phase cause-chain trailer (mirrors the PATTERNS.md "Indent shape worked example" block).
24. Multi-cause cascade: 2 `failed` plugins each with their own `cause?: Error`, both under one marketplace → each plugin row followed by its OWN 4-space-indented cause-chain trailer (D-16-08: cause chains are inline below their row, not aggregated).
25. Severity tier info: `installed` plugin in `added` marketplace → `mock.calls[0]!.arguments` length is 1 (no severity arg).
26. Severity tier warning: single `skipped` plugin → `arguments` is `[..., "warning"]`.
27. Severity tier error: any `failed` plugin in payload → `arguments` is `[..., "error"]`. Also verify first-match: payload containing BOTH a `failed` plugin AND a `skipped` plugin → returns `"error"` (failed wins per D-16-11).
28. Reload-hint suppression: payload with only `failed` plugins under `failed` marketplaces → body has NO `/reload to pick up changes` trailer (neither plugin nor marketplace status is in the trigger set).
29. `notifyUsageError` shape: payload `{ message: "Unknown plugin", usage: "Usage: /claude:plugin install <name>" }` → `arguments` is `[\`Unknown plugin\n\nUsage: /claude:plugin install <name>\`, "error"]`.

Plan picks test names; recommended format: `test("notify <case-summary>", () => { ... })` for state-change tests, `test("notifyUsageError <case-summary>", () => { ... })` for the usage-error tests. The two BLOCKER-coverage tests (17a and 21a) MUST have descriptive names referencing the BLOCKER they cover so future readers can trace the assertion back to this revision.

Each test MUST end with: `assert.equal(ctx.ui.notify.mock.calls.length, 1);` to prove `notify()` made exactly one call. Use `assert.deepEqual` for the arguments array. Use template strings with explicit `\n` for newline-bearing expected strings (mirrors V1 file line 56). For tests involving cause chains, construct `Error` instances via `new Error("msg", { cause: ... })` mirroring V1 file lines 84-86.

Do NOT import from `docs/output-catalog.md` (D-16-18). Do NOT import any helper from `extensions/pi-claude-marketplace/presentation/*` -- the expected strings are anchored to plan 03/04/05's byte forms recorded in their summaries. Do NOT modify `tests/shared/notify.test.ts` (V1 file, untouched per D-16-16). Do NOT modify `tests/architecture/notify-types.test.ts` or `tests/architecture/catalog-uat.test.ts`.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && npx node --test tests/shared/notify-v2.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/shared/notify-v2.test.ts` exists; `wc -l tests/shared/notify-v2.test.ts` outputs at least `270` lines
    - `grep -c "^import" tests/shared/notify-v2.test.ts` outputs at least `2` (node:assert/strict + node:test, plus the named imports from shared/notify.ts)
    - `grep -c "from \"../../extensions/pi-claude-marketplace/shared/notify.ts\"" tests/shared/notify-v2.test.ts` outputs `1`
    - `grep -c "^test(" tests/shared/notify-v2.test.ts` outputs at least `22` (one `test(...)` call per case; plan target ≥ 29 per PATTERNS.md taxonomy + 2 BLOCKER-coverage tests)
    - `grep -cE 'mock\\.calls\\[0\\]!\\.arguments' tests/shared/notify-v2.test.ts` outputs at least `22` (every test ends with the V1-style assertion)
    - `grep -c "Authority: this file is the de facto v2 spec" tests/shared/notify-v2.test.ts` outputs `1` (mini-spec authority anchor present in header)
    - `grep -cE 'interface MockCtx' tests/shared/notify-v2.test.ts` outputs `1` AND `grep -cE 'interface MockPi' tests/shared/notify-v2.test.ts` outputs `1`
    - `grep -c "from \"../../extensions/pi-claude-marketplace/presentation/" tests/shared/notify-v2.test.ts` outputs `0` (no presentation/* import)
    - `grep -c "docs/output-catalog.md" tests/shared/notify-v2.test.ts` outputs `0` (D-16-18: no catalog import)
    - `npx node --test tests/shared/notify-v2.test.ts` exits 0 with at least 22 reported `ok` cases (parse the output footer for the pass count -- the run should report `pass <N>` with `N >= 22` and `fail 0`)
    - `npm run check` exits 0 (typecheck + ESLint + Prettier + ALL tests, including the new notify-v2 file; pre-existing tests/shared/notify.test.ts and tests/architecture/* still pass)
    - `git diff --stat tests/shared/notify.test.ts` shows zero changes (V1 file untouched per D-16-16)
    - `grep -cE 'test\\("notify[^"]*severity' tests/shared/notify-v2.test.ts` outputs at least `3` (severity tier tests for info, warning, error present)
    - `grep -c "rollbackPartial" tests/shared/notify-v2.test.ts` outputs at least `2` (at least the no-cause and the with-cause rollbackPartial tests are present)
    - `grep -cE 'orphan' tests/shared/notify-v2.test.ts` outputs at least `2` (orphan-fold PRESENT test 21 AND orphan-fold ABSENT test 21a both named accordingly)
    - `grep -c "/reload to pick up changes" tests/shared/notify-v2.test.ts` outputs at least `2` (positive trigger test + negative suppression test both reference the literal in their expected strings or assertions)
    - `grep -c "notifyUsageError" tests/shared/notify-v2.test.ts` outputs at least `2` (import + at least one test invocation)
    - **BLOCKER-3 coverage assertion**: `grep -cE "BLOCKER-3|no-crash" tests/shared/notify-v2.test.ts` outputs at least `1` (test 17a's name or assertion message references the BLOCKER-3 / no-crash coverage so future regressions trip the right test)
    - **BLOCKER-1 coverage assertion**: `grep -cE "BLOCKER-1|non-orphan-fold|\\[undefined\\]" tests/shared/notify-v2.test.ts` outputs at least `1` (test 21a's name or expected-string-anchor explicitly references BLOCKER-1, the non-orphan-fold case, or the `[undefined]` anti-pattern that the test guards against)
  </acceptance_criteria>
  <done>`tests/shared/notify-v2.test.ts` exists with ≥ 22 passing per-status unit tests covering the 10 plugin-status variants, the 5 marketplace-header cases (including SUB-BRANCH B), the orphan-fold PRESENT case AND the orphan-fold ABSENT case (BLOCKER-1 coverage, test 21a), the empty-list-surface SUB-BRANCH A case (BLOCKER-3 coverage, test 17a), the rollbackPartial cases, the multi-cause cascade, the multi-marketplace case, the empty-payload cases, the severity ladder (3 tiers + first-match), the reload-hint trigger + suppression, and `notifyUsageError`. Header comment carries the v2 mini-spec authority anchor and explicitly enumerates both the conditional scope-bracket emission rule (8 scope-bearing variants) and the bare-header SUB-BRANCH A semantics. `npm run check` GREEN. V1 test file untouched.</done>
</task>

</tasks>

<verification>
- `npx node --test tests/shared/notify-v2.test.ts` reports `pass >= 22` and `fail 0`
- `grep -c "^test(" tests/shared/notify-v2.test.ts` outputs at least `22`
- `grep -cE "BLOCKER-3|no-crash" tests/shared/notify-v2.test.ts` outputs at least `1`
- `grep -cE "BLOCKER-1|non-orphan-fold|\\[undefined\\]" tests/shared/notify-v2.test.ts` outputs at least `1`
- `npm run check` exits 0
- `git diff --stat tests/shared/notify.test.ts tests/architecture/catalog-uat.test.ts tests/architecture/notify-types.test.ts` shows zero changes (V1 test file and Phase 15 / V1 architecture tests untouched)
</verification>

<success_criteria>
- The new `tests/shared/notify-v2.test.ts` file is the binding correctness gate for V2 grammar in Phase 16.
- Mini-spec header documents every grammar rule used by the tests, anchored as the de facto v2 spec for the Phase 16-17 window, including the conditional scope-bracket emission rule on the 8 scope-bearing variants (BLOCKER-1 anchor) and the bare-header SUB-BRANCH A semantics for the `case undefined:` marketplace-header arm (BLOCKER-3 anchor).
- ≥ 22 passing tests cover the D-16-17 taxonomy (10 plugin variants + 5 marketplace cases + orphan-fold present + orphan-fold absent + empty-list-surface + rollbackPartial + multi-cause cascade + multi-marketplace + empty cases + severity tiers + reload-hint cases + notifyUsageError).
- BLOCKER-coverage tests 17a and 21a affirmatively lock in plan-03's BLOCKER-3 fix (empty-list-surface no-crash) and plan-04's BLOCKER-1 fix (conditional scope-bracket emission); test names reference the BLOCKER for traceability.
- Mock-ctx + mock-pi patterns reuse the V1 file's `node:test` idioms verbatim.
- D-11 layering preserved (no `presentation/*` imports).
- D-16-18 honored (no `docs/output-catalog.md` import; fixtures are the seed for Phase 17).
- V1 `tests/shared/notify.test.ts`, `tests/architecture/notify-types.test.ts`, and `tests/architecture/catalog-uat.test.ts` are unchanged.
- `npm run check` GREEN.
- Single atomic conventional-commit (`test(16): add per-status unit suite for V2 notify + notifyUsageError`).
</success_criteria>

<output>
Create `.planning/phases/16-renderer-public-api-alongside-v1/16-06-SUMMARY.md` when done. Record: (1) final test count and pass/fail breakdown from `node --test`, (2) the byte form chosen for the expected string of one representative test per D-16-17 category (one per-plugin-status case, one marketplace-header case, the orphan-fold PRESENT case 21, the orphan-fold ABSENT case 21a with the explicit assertion that the row contains NO `[scope]` bracket, the empty-list-surface SUB-BRANCH A case 17a with the explicit assertion that the call did not throw and the header is bare, the rollbackPartial+cause-chain worked-example case, the multi-marketplace case, the empty-marketplaces case, the reload-hint suppression case), (3) confirmation that all helpers from plans 03/04 and notify() from plan 05 are exercised correctly, (4) confirmation that `npm run check` stayed GREEN and the catalog UAT + Phase 15 compile-check still pass, (5) confirmation that the mini-spec header is the binding v2 contract until Phase 17 (SNM-19/20/31) ratifies it in docs/output-catalog.md, (6) close-out of Phase 16: all 8 phase REQ-IDs (SNM-12..18, SNM-30) have at least one passing test or implementation acceptance criterion covering them, (7) confirmation that BLOCKER-1 and BLOCKER-3 coverage tests (17a, 21a) are passing and that the negative-assertion anti-patterns (`[undefined]` substring on the row, runtime crash on empty-list-surface) are absent from the actual output.
</output>
</task>
</tasks>
</content>
</invoke>
