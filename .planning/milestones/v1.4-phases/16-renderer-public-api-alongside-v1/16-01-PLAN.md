---
phase: 16-renderer-public-api-alongside-v1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - docs/adr/v2-001-structured-notify.md
autonomous: true
requirements:
  - SNM-12
  - SNM-15
must_haves:
  truths:
    - "REQUIREMENTS.md SNM-12 spells out a 3-arg notify signature (ctx, pi, message) per D-16-01"
    - "REQUIREMENTS.md SNM-15 trigger language restricts the marketplace branch to state-changing statuses (added, removed, updated -- not failed) per D-16-12"
    - "docs/adr/v2-001-structured-notify.md Decision-section signature snippet matches the 3-arg form so Phase 16 and the ADR cannot drift"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: SNM-12 and SNM-15 editorial refinements
      contains: "notify(ctx: ExtensionContext, pi: ExtensionAPI, message: NotificationMessage): void"
    - path: docs/adr/v2-001-structured-notify.md
      provides: ADR Decision-section signature snippet aligned with SNM-12 (D-16-01)
      contains: "notify(ctx: ExtensionContext, pi: ExtensionAPI, message: NotificationMessage): void"
  key_links:
    - from: .planning/REQUIREMENTS.md SNM-12
      to: docs/adr/v2-001-structured-notify.md Decision section
      via: identical 3-arg notify signature
      pattern: "notify\\(ctx: ExtensionContext, pi: ExtensionAPI, message: NotificationMessage\\)"
    - from: .planning/REQUIREMENTS.md SNM-15
      to: Phase 16 reload-hint implementation (downstream plan 05)
      via: "state-changing marketplace status" wording lock
      pattern: "state-changing marketplace status \\(added, removed, updated"
---

<objective>
Land the two REQUIREMENTS.md editorial refinements identified in CONTEXT.md (D-16-01 for SNM-12; D-16-12 for SNM-15) plus the matching ADR Decision-snippet alignment, so the binding spec and the renderer implementation that follows in this phase cannot disagree on the public signature or the reload-hint trigger.

Purpose: SNM-12 currently documents `notify(ctx, message)` as 2-arg, but D-16-01 amends the signature to `notify(ctx, pi, message)` because `ExtensionContext` does not expose `getAllTools()` and SNM-16 mandates a render-time soft-dep probe via `softDepStatus(pi)`. SNM-15 currently says "any marketplace status set" but D-16-12 refines that to "any state-changing marketplace status (added, removed, updated -- not failed)" because failed marketplace ops rolled back and have nothing to reload. Without these edits Phase 16 ships an implementation that contradicts the requirements file.

Output: Updated `.planning/REQUIREMENTS.md` (two row edits) and `docs/adr/v2-001-structured-notify.md` (one Decision-section signature-snippet edit at line 27) committed as a single atomic docs-only commit. No source code or tests touched.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-renderer-public-api-alongside-v1/16-CONTEXT.md
@.planning/phases/16-renderer-public-api-alongside-v1/16-PATTERNS.md
@docs/adr/v2-001-structured-notify.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md SNM-12 to 3-arg notify signature</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file -- the table format and SNM-* row prose style must be matched verbatim; line 30 is the SNM-12 row that gets edited)
    - .planning/phases/16-renderer-public-api-alongside-v1/16-CONTEXT.md sections `<decisions>` D-16-01 and `<canonical_refs>` "Source-of-truth specs" (binding before/after wording for the edit)
    - .planning/phases/16-renderer-public-api-alongside-v1/16-PATTERNS.md section `### .planning/REQUIREMENTS.md (editorial: SNM-12 + SNM-15)` (gives the exact before/after byte shapes)
    - extensions/pi-claude-marketplace/platform/pi-api.ts lines 41-86 (proves `softDepStatus(pi)` requires `pi: ExtensionAPI`, justifying the signature change)
  </read_first>
  <action>
Replace the existing SNM-12 row at .planning/REQUIREMENTS.md line 30 so the signature is the 3-arg form `notify(ctx: ExtensionContext, pi: ExtensionAPI, message: NotificationMessage): void`. Keep the row in checkbox form with the `**SNM-12**:` label prefix and the trailing "exported from `shared/notify.ts`. The single public entrypoint for state-change notifications." sentence. Append one new sentence after that: "The `pi` argument is required for the render-time soft-dep probe (SNM-16); orchestrators already receive both `ctx` and `pi` separately." Do NOT renumber or reorder any other SNM-* row. Do NOT touch the requirement-status table at lines 89-95 (SNM-12 remains `Phase 16 | Pending`). Per D-16-01 this is an editorial refinement that ratifies the implementation Phase 16 will ship.
  </action>
  <verify>
    <automated>grep -c "SNM-12.*notify(ctx: ExtensionContext, pi: ExtensionAPI, message: NotificationMessage): void" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SNM-12.*notify(ctx: ExtensionContext, pi: ExtensionAPI, message: NotificationMessage): void" .planning/REQUIREMENTS.md` outputs `1`
    - `grep -c "soft-dep probe (SNM-16)" .planning/REQUIREMENTS.md` outputs `1`
    - `grep -c "notify(ctx: ExtensionContext, message: NotificationMessage): void" .planning/REQUIREMENTS.md` outputs `0` (the old 2-arg form is gone from SNM-12; ADR edit in Task 3 also removes it elsewhere)
    - `grep -c "^| SNM-12 | Phase 16 | Pending |$" .planning/REQUIREMENTS.md` outputs `1` (status table row unchanged)
    - `grep -c "^- \[ \] \*\*SNM-12\*\*" .planning/REQUIREMENTS.md` outputs `1` (row remains a checkbox at the same nesting; no duplication)
    - File still parses as well-formed Markdown (no opened-but-unclosed code fence, table, or backtick run introduced)
  </acceptance_criteria>
  <done>SNM-12 row in REQUIREMENTS.md states the 3-arg `notify(ctx, pi, message)` signature, references SNM-16 as the justification for the `pi` argument, and the requirement-status table is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md SNM-15 reload-hint trigger wording</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file -- line 33 is the SNM-15 row that gets edited)
    - .planning/phases/16-renderer-public-api-alongside-v1/16-CONTEXT.md section `<decisions>` D-16-12 (binding before/after wording: trigger restricted to "any state-changing marketplace status (added, removed, updated -- not failed)")
    - .planning/phases/16-renderer-public-api-alongside-v1/16-PATTERNS.md section `### .planning/REQUIREMENTS.md (editorial: SNM-12 + SNM-15)` and "Phase 16's reload-hint trigger ladder (D-16-12, refining SNM-15)" inside Analog #5 (gives the exact byte shape)
  </read_first>
  <action>
Replace the existing SNM-15 row at .planning/REQUIREMENTS.md line 33 so the marketplace-status branch is constrained to state-changing values only. Keep the row in checkbox form with the `**SNM-15**:` label prefix and the leading "`notify()` emits the reload-hint trailer (`/reload to pick up changes`) when contents indicate state change: any plugin status in `{installed, updated, reinstalled, uninstalled}` or" prefix. Replace the trailing "any marketplace status set. No caller-supplied flag." with "any state-changing marketplace status (`added`, `removed`, `updated` -- not `failed`). No caller-supplied flag. Rationale: failed marketplace operations roll back and leave nothing to reload." Do NOT renumber or reorder any other SNM-* row. Do NOT touch the requirement-status table at line 92 (SNM-15 remains `Phase 16 | Pending`). Per D-16-12 this refinement ratifies the trigger ladder Phase 16 will implement in plan 05.
  </action>
  <verify>
    <automated>grep -c "SNM-15.*any state-changing marketplace status" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SNM-15.*any state-changing marketplace status" .planning/REQUIREMENTS.md` outputs `1`
    - `grep -c "added.*removed.*updated.*not.*failed" .planning/REQUIREMENTS.md` outputs at least `1`
    - `grep -c "any marketplace status set" .planning/REQUIREMENTS.md` outputs `0` (old wording fully replaced)
    - `grep -c "^- \[ \] \*\*SNM-15\*\*" .planning/REQUIREMENTS.md` outputs `1` (row remains a checkbox, no duplication)
    - `grep -c "^| SNM-15 | Phase 16 | Pending |$" .planning/REQUIREMENTS.md` outputs `1` (status table row unchanged)
    - File still parses as well-formed Markdown
  </acceptance_criteria>
  <done>SNM-15 row in REQUIREMENTS.md restricts the marketplace branch of the reload-hint trigger to `added`, `removed`, `updated` and explicitly excludes `failed`, with the rationale spelled out per D-16-12.</done>
</task>

<task type="auto">
  <name>Task 3: Update ADR v2-001 Decision-section signature snippet</name>
  <files>docs/adr/v2-001-structured-notify.md</files>
  <read_first>
    - docs/adr/v2-001-structured-notify.md lines 18-50 (the Decision section; line 27 currently embeds the 2-arg signature `export function notify(ctx: ExtensionContext, message: NotificationMessage): void;`)
    - .planning/phases/16-renderer-public-api-alongside-v1/16-CONTEXT.md section `<decisions>` D-16-01 footer (flags the ADR Decision-snippet edit as conditional-on-presence; the snippet IS present at line 27)
    - .planning/phases/16-renderer-public-api-alongside-v1/16-PATTERNS.md section `### docs/adr/v2-001-structured-notify.md (editorial, conditional)` (gives the rule: if the Decision embeds the signature, queue a one-line edit)
    - .planning/REQUIREMENTS.md SNM-12 (post-Task-1 form -- the ADR snippet must match the just-updated requirement byte-for-byte)
  </read_first>
  <action>
Update the signature snippet at docs/adr/v2-001-structured-notify.md line 27 from `export function notify(ctx: ExtensionContext, message: NotificationMessage): void;` to `export function notify(ctx: ExtensionContext, pi: ExtensionAPI, message: NotificationMessage): void;` so the ADR Decision section matches the SNM-12 row updated in Task 1. Do NOT change any other line in the ADR (Status remains `Accepted`, Alternative 2 disposition stays `ACCEPTED`, Phase 15 cross-references stay intact). Do NOT add a new alternatives entry or a separate "Phase 16 amendment" section -- the snippet update is a typo-grade alignment with the locked SNM-12 wording. If a second occurrence of the 2-arg `notify(ctx: ExtensionContext, message: NotificationMessage)` signature exists elsewhere in the ADR (e.g. in prose narration of the Implementation or Migration sections), update it identically; otherwise leave prose untouched.
  </action>
  <verify>
    <automated>grep -c "notify(ctx: ExtensionContext, pi: ExtensionAPI, message: NotificationMessage)" docs/adr/v2-001-structured-notify.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "notify(ctx: ExtensionContext, pi: ExtensionAPI, message: NotificationMessage): void" docs/adr/v2-001-structured-notify.md` outputs at least `1`
    - `grep -c "notify(ctx: ExtensionContext, message: NotificationMessage)" docs/adr/v2-001-structured-notify.md` outputs `0` (no surviving 2-arg signature anywhere in the ADR)
    - `grep -c "^## Status$" docs/adr/v2-001-structured-notify.md` outputs `1` AND `grep -A1 "^## Status$" docs/adr/v2-001-structured-notify.md | grep -c "Accepted"` outputs `1` (Status field unchanged)
    - File still parses as well-formed Markdown (the change is inside a fenced TypeScript block; the fence remains balanced)
  </acceptance_criteria>
  <done>ADR Decision section embeds the 3-arg `notify(ctx, pi, message)` signature matching SNM-12; ADR Status, Alternatives section, and Phase 15 cross-references are unchanged.</done>
</task>

</tasks>

<verification>
- `grep -c "SNM-12.*pi: ExtensionAPI" .planning/REQUIREMENTS.md` outputs `1`
- `grep -c "SNM-15.*state-changing marketplace status" .planning/REQUIREMENTS.md` outputs `1`
- `grep -c "notify(ctx: ExtensionContext, pi: ExtensionAPI, message: NotificationMessage)" docs/adr/v2-001-structured-notify.md` outputs at least `1`
- `grep -c "notify(ctx: ExtensionContext, message: NotificationMessage): void" docs/adr/v2-001-structured-notify.md` outputs `0`
- `npm run check` passes (this plan only touches `.md` files; lint+typecheck+tests stay GREEN because no source changed)
</verification>

<success_criteria>
- SNM-12 in REQUIREMENTS.md and the ADR Decision-section signature snippet both state the 3-arg `notify(ctx, pi, message)` form; the 2-arg form has zero remaining occurrences in either file.
- SNM-15 in REQUIREMENTS.md restricts the marketplace branch of the reload-hint trigger to state-changing statuses (`added`, `removed`, `updated`) and explicitly excludes `failed`.
- Requirement-status table rows for SNM-12 and SNM-15 are unchanged.
- `npm run check` stays GREEN.
- One atomic conventional-commit (`docs(16): refine SNM-12 + SNM-15 wording and align ADR signature`) captures all three file edits.
</success_criteria>

<output>
Create `.planning/phases/16-renderer-public-api-alongside-v1/16-01-SUMMARY.md` when done. Summary MUST record: (1) exact before/after byte shapes of both REQUIREMENTS.md edits, (2) whether the ADR had a single or double signature occurrence and the line numbers updated, (3) confirmation that `npm run check` stayed GREEN, (4) confirmation that this plan unblocks Wave 2 (plans 02-06) which assume the new signature and reload-hint wording.
</output>
