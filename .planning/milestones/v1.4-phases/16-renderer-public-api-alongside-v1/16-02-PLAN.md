---
phase: 16-renderer-public-api-alongside-v1
plan: 02
type: execute
wave: 2
depends_on:
  # Editorial-before-implementation ordering: plan 01 updates the SNM-12 / SNM-15
  # wording in .planning/REQUIREMENTS.md and the docs/adr/v2-001-structured-notify.md
  # signature snippet so this plan can cite the corrected REQ-ID text in its
  # acceptance criteria and TSDoc anchors. Although the file sets do not overlap
  # (plan 01 = REQUIREMENTS.md + ADR; plan 02 = shared/notify.ts), the dependency
  # is intentional to keep the editorial change visible before the implementing
  # commit. Renumbering this to wave 1 would save one wave of latency for
  # negligible benefit -- the GSD wave structure does not parallelize plans
  # across waves in practice; sequential ordering preserves a clean reading
  # narrative for future archaeologists tracing the SNM-12 amendment.
  - 16-01
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
autonomous: true
requirements:
  - SNM-13
must_haves:
  truths:
    - "shared/notify.ts exports a public function notifyUsageError(ctx: ExtensionContext, message: UsageErrorMessage): void per SNM-13 and D-16-02"
    - "The V2 notifyUsageError coexists with the V1 3-arg notifyUsageError(ctx, msg, usageBlock) -- V1 is NOT deleted (D-16-03)"
    - "The on-the-wire body is `${message.message}\\n\\n${message.usage}` and severity is the magic string `\"error\"`, byte-equal to V1's shape (mirrors shared/notify.ts:105)"
  artifacts:
    - path: extensions/pi-claude-marketplace/shared/notify.ts
      provides: New `notifyUsageError(ctx, UsageErrorMessage)` overload alongside the V1 3-arg `notifyUsageError`
      contains: "export function notifyUsageError(ctx: ExtensionContext, message: UsageErrorMessage): void"
  key_links:
    - from: shared/notify.ts new V2 notifyUsageError
    - to: shared/notify.ts UsageErrorMessage type (Phase 15)
      via: imported type parameter
      pattern: "message: UsageErrorMessage"
    - from: shared/notify.ts new V2 notifyUsageError
      to: ctx.ui.notify Pi API surface
      via: severity magic string "error"
      pattern: "ctx\\.ui\\.notify\\(.*, \"error\"\\)"
---

<objective>
Add the public `notifyUsageError(ctx: ExtensionContext, message: UsageErrorMessage): void` entry point to `extensions/pi-claude-marketplace/shared/notify.ts` alongside (NOT replacing) the existing V1 3-arg `notifyUsageError(ctx, msg, usageBlock)`. The function destructures the structured `UsageErrorMessage` payload (Phase 15 type) and emits `${message.message}\n\n${message.usage}` via `ctx.ui.notify(body, "error")`, byte-equal to V1.

Purpose: SNM-13 names this the sole public entry point for argv-validation errors in the V2 surface. The function is the simplest V2 entrypoint -- no `pi`, no soft-dep probe, no plugin/marketplace rows, just a structured payload destructure and a single `ctx.ui.notify` call with `"error"` severity. Landing it independently of `notify()` keeps the change atomic and unblocks plan 06's tests for the usage-error path early.

Output: Two new lines (one TSDoc comment + one function definition) added to `extensions/pi-claude-marketplace/shared/notify.ts`. The V1 wrappers (lines 56-106) are not touched. The Phase 15 type model (lines ~200-462) is not touched.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-renderer-public-api-alongside-v1/16-CONTEXT.md
@.planning/phases/16-renderer-public-api-alongside-v1/16-PATTERNS.md
@extensions/pi-claude-marketplace/shared/notify.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add V2 notifyUsageError(ctx, UsageErrorMessage) export</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (full file -- need to locate V1 `notifyUsageError` at line ~96-106, the Phase 15 `UsageErrorMessage` type definition at line ~440-460, and the file-private/public surface conventions used in this module)
    - .planning/phases/16-renderer-public-api-alongside-v1/16-CONTEXT.md sections `<decisions>` D-16-02 + D-16-03 (signature lock + coexistence requirement) and `<specifics>` "`notifyUsageError` on-the-wire shape" bullet (exact byte-equal contract `${message}\n\n${usage}` with `"error"` severity)
    - .planning/phases/16-renderer-public-api-alongside-v1/16-PATTERNS.md sections `### .../shared/notify.ts ADDITIONS` "Analog #1 (in-file): V1 wrappers" and the "V2 UsageError entry point" skeleton sketch (gives the byte-equal implementation shape)
    - .planning/REQUIREMENTS.md SNM-13 (signature lock: `notifyUsageError(ctx: ExtensionContext, message: UsageErrorMessage): void`)
    - tests/shared/notify.test.ts (V1 notifyUsageError test for the assertion shape plan 06 will reuse)
  </read_first>
  <action>
Add a new public export `export function notifyUsageError(ctx: ExtensionContext, message: UsageErrorMessage): void` to `extensions/pi-claude-marketplace/shared/notify.ts`. Placement: directly after the existing V1 `notifyUsageError(ctx, message, usageBlock)` wrapper (currently at lines ~96-106) and BEFORE the Phase 15 type-model block (which starts around line 200 with the v1.4 closed-set tuples `PLUGIN_STATUSES` / `MARKETPLACE_STATUSES` / `DEPENDENCIES`). The V2 function MUST: (1) take exactly two parameters -- `ctx: ExtensionContext` and `message: UsageErrorMessage` (the structured payload type already exported from this file by Phase 15); (2) emit `ctx.ui.notify(`${message.message}\n\n${message.usage}`, "error");` -- single statement, no intermediate `const body = ...`; (3) carry a one-line TSDoc preceding it: `/** V2 structured usage-error entry point (SNM-13, D-16-02). Coexists with V1 3-arg notifyUsageError. */`. Do NOT modify, rename, or delete the V1 3-arg `notifyUsageError(ctx, message, usageBlock)` wrapper -- both must coexist per D-16-03 (Phase 21 deletes V1). Do NOT add any new imports (`UsageErrorMessage` is defined in this same file; `ExtensionContext` is already imported from `../platform/pi-api.ts` at line 5). Do NOT introduce a `notify` function in this plan -- that lands in plan 05. Do NOT alter the Phase 15 type definitions, the `Reason` re-export, the V1 wrappers (`notifySuccess` / `notifyWarning` / `notifyError`), or the existing TSDoc block at lines 14-55. Per D-16-02 there is no `pi` argument because `UsageErrorMessage` carries no plugin rows / dependencies / soft-dep probes.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run check 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function notifyUsageError(ctx: ExtensionContext, message: UsageErrorMessage): void" extensions/pi-claude-marketplace/shared/notify.ts` outputs `1`
    - `grep -c "export function notifyUsageError(ctx: ExtensionContext, message: string, usageBlock: string): void" extensions/pi-claude-marketplace/shared/notify.ts` outputs `1` (V1 3-arg form still present, coexists per D-16-03)
    - `grep -c 'ctx.ui.notify(`${message.message}\\\\n\\\\n${message.usage}`, "error")' extensions/pi-claude-marketplace/shared/notify.ts` outputs `1` (the V2 body line emits the byte-equal `${message}\n\n${usage}` shape with `"error"` severity)
    - `grep -c "V2 structured usage-error entry point (SNM-13, D-16-02)" extensions/pi-claude-marketplace/shared/notify.ts` outputs `1` (TSDoc anchor present)
    - `npm run check` exits with status 0 (typecheck recognizes `UsageErrorMessage` as the Phase 15 structured payload; ESLint accepts the new export under the existing `shared/notify.ts` per-file override; Prettier formatting respected; existing `tests/shared/notify.test.ts` still passes because V1 wrappers are untouched)
    - The new function has exactly two parameters; verifying by `grep -c "export function notifyUsageError" extensions/pi-claude-marketplace/shared/notify.ts` outputs `2` (one V1, one V2 -- no third overload accidentally introduced)
    - The new function appears AFTER V1 notifyUsageError and BEFORE the `PLUGIN_STATUSES` tuple: `awk '/export function notifyUsageError\\(ctx: ExtensionContext, message: UsageErrorMessage\\)/{p=NR} /PLUGIN_STATUSES =/{q=NR} END{exit (p && q && p<q) ? 0 : 1}' extensions/pi-claude-marketplace/shared/notify.ts` exits 0
  </acceptance_criteria>
  <done>shared/notify.ts exports the new V2 `notifyUsageError(ctx, UsageErrorMessage)` alongside the V1 3-arg wrapper; the new function body matches the V1 on-the-wire format `${message}\n\n${usage}` with `"error"` severity; `npm run check` is GREEN; no other lines in the file changed.</done>
</task>

</tasks>

<verification>
- `grep -c "export function notifyUsageError" extensions/pi-claude-marketplace/shared/notify.ts` outputs `2`
- `npm run check` exits 0
- Existing `tests/shared/notify.test.ts` (V1) still passes -- V1 wrapper is untouched
- No new imports added; D-11 layering preserved (no `presentation/*` imports)
</verification>

<success_criteria>
- The V2 2-arg `notifyUsageError(ctx, UsageErrorMessage)` is exported from `shared/notify.ts` and reachable from any consumer that imports `notifyUsageError` from the v1.4 surface.
- V1 3-arg `notifyUsageError(ctx, message, usageBlock)` remains exported unchanged (Phase 21 will delete it).
- `npm run check` GREEN.
- Single atomic conventional-commit (`feat(16): add V2 notifyUsageError(ctx, UsageErrorMessage) entry point`).
</success_criteria>

<output>
Create `.planning/phases/16-renderer-public-api-alongside-v1/16-02-SUMMARY.md` when done. Record: (1) exact line range of the inserted V2 function in `shared/notify.ts`, (2) `npm run check` final status, (3) confirmation that V1 3-arg `notifyUsageError` and V1 severity wrappers are bit-for-bit unchanged, (4) confirmation that plan 06's tests will be able to assert against `mock.calls[0]!.arguments` of shape `[`${msg}\n\n${usage}`, "error"]`.
</output>
</content>
</invoke>
