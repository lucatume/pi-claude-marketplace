---
phase: 18-migration-wave-1-marketplace-orchestrator-family
plan: 2
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts
  - tests/orchestrators/marketplace/autoupdate.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-18-04: Phase 17.1 (V2 Grammar Amendment: Autoupdate Surface) landed 2026-05-26; the 7-entry MarketplaceStatus + optional `reasons?:` are the binding contract this plan constructs payloads against."
    - "D-18-05: 5-state autoupdate mapping LOCKED: fresh enable -> `autoupdate enabled`; fresh disable -> `autoupdate disabled`; idempotent enable -> `skipped` + `reasons: [\"already enabled\"]`; idempotent disable -> `skipped` + `reasons: [\"already disabled\"]`; not-found -> `failed`. Severity ladder: fresh -> info; skipped -> warning; failed -> error. Reload-hint ladder: fresh -> emit; skipped/failed -> suppress."
    - "D-18-06: tests stay END-TO-END through real `notify()` via `makeCtx()`; byte-exact V2 string assertions replace V1; new severity assertions added per D-18-05 ladder."
    - "D-16-06 caller-order honored: drop the alphabetic sort at autoupdate.ts lines 178-180 (renderer honors caller order); the SC-6 scopes-loop order becomes the visible iteration order for bare-form multi-marketplace flips."
    - "Wave 1 pilot (Plan 18-01) recipe followed: payload construction mirrors the add.ts pattern; differences are only in `mp.status` literal + optional `reasons:` field."
  byte_contracts:
    - "MAU fresh enable: `note.message === \"● <mp> [<scope>] (autoupdate enabled)\\n\\n/reload to pick up changes\"` -- catalog UAT fixture `enable-fresh` at tests/architecture/catalog-uat.test.ts:1239 onward."
    - "MAU fresh disable: `note.message === \"● <mp> [<scope>] (autoupdate disabled)\\n\\n/reload to pick up changes\"` -- catalog UAT fixture `disable-fresh`."
    - "MAU idempotent enable: `note.message === \"● <mp> [<scope>] (skipped) {already enabled}\"`, `note.severity === \"warning\"` -- catalog UAT fixture `enable-idempotent`."
    - "MAU idempotent disable: `note.message === \"● <mp> [<scope>] (skipped) {already disabled}\"`, `note.severity === \"warning\"` -- catalog UAT fixture `disable-idempotent`."
    - "MAU failure (not-found): `note.message === \"⊘ <mp> [<scope>] (failed)\"`, `note.severity === \"error\"` -- catalog UAT fixture `failure-not-found`."
    - "MAU empty scopes: `note.message === \"(no marketplaces)\"` -- D-16-17."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts"
      provides: "V2 notify() calls covering the 5 Phase-17.1 catalog states + the empty-scopes sentinel; presentation/* imports dropped"
      contains: "notify(opts.ctx, opts.pi"
    - path: "tests/orchestrators/marketplace/autoupdate.test.ts"
      provides: "Byte-exact V2 assertions for all 5 states + new severity assertions per D-18-05; existing makeCtx() preserved"
      contains: "(autoupdate enabled)"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts::notify"
      via: "V2 notify(ctx, pi, message) calls replacing 4 V1 wrappers"
      pattern: "notify\\(opts\\.ctx,\\s*opts\\.pi"
    - from: "extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts::buildPayload"
      to: "shared/notify.ts::MarketplaceNotificationMessage type"
      via: "discriminated `status` literal driving the renderer arm (`autoupdate enabled` / `autoupdate disabled` / `skipped` + `reasons` / `failed`)"
      pattern: "status:\\s*\"(autoupdate enabled|autoupdate disabled|skipped|failed)\""
  coverage_constraints:
    - "Catalog UAT byte-equality MUST stay GREEN at every commit boundary."
    - "`npm run check` GREEN at merge (atomic commit -- orchestrator + tests together)."
    - "Zero V1 callers remain in autoupdate.ts after this plan."
---

<objective>
Migrate `extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` from V1 severity-named wrappers (4 callsites: `notifyError` at 141 + 155, `notifySuccess` at 163 + 184) to V2 `notify(ctx, pi, NotificationMessage)`. Construct payloads against the Phase 17.1 5-state catalog (`enable-fresh`, `disable-fresh`, `enable-idempotent`, `disable-idempotent`, `failure-not-found`) using the 7-entry `MarketplaceStatus` + optional `reasons?:` field landed in Phase 17.1. Update `tests/orchestrators/marketplace/autoupdate.test.ts` (12 tests) with byte-exact V2 assertions per D-18-06 + new severity assertions per the D-18-05 ladder. Drop the alphabetic sort at lines 178-180 per D-16-06 (caller-order honored).

Purpose: deliver the autoupdate command surface in V2 grammar against the locked Phase 17.1 contract; pattern mirrors the Wave 1 pilot (Plan 18-01 add.ts).

Output: 2 modified files in one atomic commit. The 5 catalog states + empty-scopes sentinel + multi-marketplace bare-form scenarios all route through V2 `notify()` with no orchestrator-level string composition.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-CONTEXT.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-RESEARCH.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-PATTERNS.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-VALIDATION.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-00-SUMMARY.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-01-SUMMARY.md
@.planning/phases/17.1-v2-grammar-amendment-autoupdate-surface/17.1-CONTEXT.md
@extensions/pi-claude-marketplace/shared/notify.ts
@docs/output-catalog.md
@tests/architecture/catalog-uat.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate autoupdate.ts -- 4 V1 wrappers -> V2 notify() across the 5 Phase-17.1 states; drop alphabetic sort + presentation imports</name>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts (full file -- 4 V1 callsites at lines 141, 155, 163, 184; the AutoupdateOptions already carries `readonly pi: ExtensionAPI` after Plan 18-00; alphabetic sort at lines 178-180; local helpers AutoupdateRowInput + buildAutoupdateRow at lines 71-111).
    - extensions/pi-claude-marketplace/shared/notify.ts (read lines 220-260 for MARKETPLACE_STATUSES 7-entry tuple; lines 500-530 for MarketplaceNotificationMessage with optional `reasons?: readonly Reason[]`; lines 580-620 for renderMpHeader's 3 new autoupdate arms; lines 1140-1180 for `notify(ctx, pi, message)` signature).
    - tests/architecture/catalog-uat.test.ts lines 1238-1292 (the 5 autoupdate fixtures: `enable-fresh`, `disable-fresh`, `enable-idempotent`, `disable-idempotent`, `failure-not-found`; each fixture maps a V2 payload to a catalog byte form).
    - 18-CONTEXT.md D-18-04 + D-18-05 (5-state mapping table + severity/reload-hint ladder), D-18-06 (test discipline), D-16-06 (caller-order honored -- drop the sort).
    - 17.1-CONTEXT.md (Phase 17.1 amendments lineage; verify the landed grammar matches 18-RESEARCH §"Phase 17.1 + 17.2 Landed Contract Confirmation").
    - 18-RESEARCH.md "Per-File Callsite Inventory" → `autoupdate.ts` (lines 120-188; 5 catalog states + empty + bare-form mixed).
    - 18-PATTERNS.md "Pattern Assignments" → `orchestrators/marketplace/autoupdate.ts` (full V1/V2 diff + imports).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts (Wave 1 pilot, post-migration; mirror the recipe-block-comment + construction pattern -- read the recipe comment Plan 18-01 leaves there).
  </read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts</files>
  <action>
    Operate in this order:

    1. Replace imports per RESEARCH §"Now-orphaned imports to drop (Plan 18-02)":
       - DROP: `renderRow` + `MarketplaceRow` type from `../../presentation/compact-line.ts`; `MARKETPLACE_LABEL_PROBE` from `../../shared/constants/marketplace-label-probe.ts`; `notifyError, notifySuccess` from `../../shared/notify.ts`.
       - ADD: `notify` from `../../shared/notify.ts`; `type MarketplaceNotificationMessage` from the same module (if the payload accumulator is hoisted to a typed array).

    2. Remove the local `AutoupdateRowInput` interface (line 71) and `buildAutoupdateRow` helper (lines 98-111). Direct construction of `MarketplaceNotificationMessage` per-flip is cleaner -- no intermediate row type.

    3. Map the 4 V1 callsites to the V2 payloads per the D-18-05 table:
       - **Line 141 (per-scope error path, currently `notifyError(opts.ctx, errorMessage(err), err)`):** REPLACE with `notify(opts.ctx, opts.pi, { marketplaces: [{ name: <mp-name>, scope: <resolved-scope>, status: "failed", plugins: [] }] })`. The mp name + scope are derivable from the in-scope variables of the catch block; the `err` argument is no longer surfaced as a cause-chain (V2 confines `cause?: Error` to plugin variants per D-16-08; the `err` is logged internally if needed but not in the notify payload).
       - **Line 155 (missingEverywhere path, currently `notifyError(opts.ctx, errorMessage(first.cause), first.cause)`):** REPLACE with `notify(opts.ctx, opts.pi, { marketplaces: [{ name: <mp-name>, scope: <first.scope>, status: "failed", plugins: [] }] })`. Same shape as line 141.
       - **Line 163 (empty-scopes path, currently `notifySuccess(opts.ctx, renderRow({ kind: "empty", token: "no marketplaces" }, MARKETPLACE_LABEL_PROBE))`):** REPLACE with `notify(opts.ctx, opts.pi, { marketplaces: [] })`. The `(no marketplaces)` sentinel is computed by `notify()` per D-16-17.
       - **Line 184 (success block, currently building `sorted = [...rows].sort(...).map(renderRow(...))` then `notifySuccess(opts.ctx, lines.join("\\n"))`):** REPLACE with a single `notify(opts.ctx, opts.pi, { marketplaces: <accumulated-MarketplaceNotificationMessage[]> })` call. Each accumulated entry is constructed inline (not via `buildAutoupdateRow`) with the D-18-05 status + optional `reasons` rules:
          - Fresh enable: `{ name, scope, status: "autoupdate enabled", plugins: [] }`
          - Fresh disable: `{ name, scope, status: "autoupdate disabled", plugins: [] }`
          - Idempotent enable (already on): `{ name, scope, status: "skipped", reasons: ["already enabled"], plugins: [] }`
          - Idempotent disable (already off): `{ name, scope, status: "skipped", reasons: ["already disabled"], plugins: [] }`

    4. DROP the alphabetic sort at lines 178-180 (`sorted = [...rows].sort((a, b) => a.name.localeCompare(b.name, undefined, { sensitivity: "base" }))`). The renderer honors caller order per D-16-06; the SC-6 scopes-loop order (project-then-user) becomes the visible iteration order. The bare-form multi-marketplace MAU-2 test will need to absorb this order change -- handled in Task 2.

    5. Add a brief comment near the new `notify()` calls referencing the recipe established by Plan 18-01 (e.g., `// V2 construction pattern -- see add.ts (Wave 1 pilot) for the recipe; this file uses statuses [autoupdate enabled, autoupdate disabled, skipped, failed] per D-18-05.`).

    After this task: `grep -c "notifySuccess\\|notifyWarning\\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` returns 0; `grep -c "from \"../../presentation/" extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` returns 0.

    Honors D-18-04 (Phase 17.1 grammar dependency), D-18-05 (5-state mapping + ladders), D-18-06 (single notify per orchestration), D-16-06 (caller-order; sort dropped).
  </action>
  <verify>
    <automated>node --test --import tsx tests/orchestrators/marketplace/autoupdate.test.ts</automated>
  </verify>
  <done>
    autoupdate.ts compiles strict; the 4 V1 callers are removed; presentation imports + local helpers + alphabetic sort all gone; the V2 `notify()` calls cover all 5 Phase-17.1 states + the empty-scopes sentinel; the bare-form multi-marketplace path emits a single `notify()` with one accumulated `marketplaces[]` array. Task 2 lands the test updates that unlock this verify command in the same atomic commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update autoupdate.test.ts -- byte-exact V2 assertions for the 5 catalog states + new severity assertions per D-18-05; bare-form order absorbs sort removal</name>
  <read_first>
    - tests/orchestrators/marketplace/autoupdate.test.ts (full file -- 12 tests; the byte-exact V1 assertions are at lines 92, 108, 124, 138, 155, 169, 195, 197, 205, 221 per RESEARCH §"Per-File Test Surface"; the existing `makeCtx()` pattern; `pi` thread already done in Plan 18-00 Task 3).
    - 18-RESEARCH.md "Per-File Test Surface" → autoupdate.test.ts table (lines 449-466 with the 8 byte-flip targets + 2 regex flips + 4 new severity assertions + reload-hint inclusion-check).
    - 18-PATTERNS.md "tests/orchestrators/marketplace/autoupdate.test.ts" section.
    - tests/architecture/catalog-uat.test.ts (lines 1238-1292 -- binding V2 byte forms for all 5 autoupdate states).
    - tests/shared/notify-v2.test.ts (per-variant V2 byte assertions; cross-reference for autoupdate-related shapes especially `skipped` severity-warning).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts (post-Task-1 state).
  </read_first>
  <files>tests/orchestrators/marketplace/autoupdate.test.ts</files>
  <action>
    Apply the V1 -> V2 assertion flips per RESEARCH §"Per-File Test Surface":

    1. Line 92 (fresh enable): change `"● mp [project] <autoupdate>"` to `"● mp [project] (autoupdate enabled)\\n\\n/reload to pick up changes"`. Reload-hint fires per D-17.1-02 (fresh autoupdate enable triggers).

    2. Line 108 (fresh disable): change `"● mp [project] <no autoupdate>"` to `"● mp [project] (autoupdate disabled)\\n\\n/reload to pick up changes"`.

    3. Line 124 (idempotent enable): change `"● mp [project] <autoupdate> {already enabled}"` to `"● mp [project] (skipped) {already enabled}"`; ADD `assert.equal(note.severity, "warning")` per D-18-05 severity ladder (skipped -> warning).

    4. Line 138 (idempotent disable): change `"● mp [project] <no autoupdate> {already disabled}"` to `"● mp [project] (skipped) {already disabled}"`; ADD severity = "warning".

    5. Line 155 (another fresh enable): same flip as line 92.

    6. Line 169 (another idempotent disable): same flip as line 138.

    7. Lines 195 + 197 (MAU-2/CMC-33 bare-form mixed -- regex assertions): the V2 byte string now uses `(skipped) {already enabled}` for the `already` row and `(autoupdate enabled)` for the `to-flip` row. After the sort drop, the iteration order is the SC-6 scopes-loop order (project-then-user). Update the regex/byte assertion to match the V2 form per RESEARCH §"Per-File Test Surface":
       - The `already` row regex: `/● already \\[project\\] \\(skipped\\) \\{already enabled\\}/`
       - The `to-flip` row regex: `/● to-flip \\[project\\] \\(autoupdate enabled\\)/`
       - If the test asserts on the joined multi-line message with both rows present, also assert the multi-line presence of both via `assert.ok(notifications[0].message.includes("(skipped)") && notifications[0].message.includes("(autoupdate enabled)"))`.
       - Asserting the EXACT iteration order is acceptable but only if the source's scopes-loop order is deterministic. RESEARCH calls out: "The current orchestrator alphabetically sorts at lines 178-180. Per D-16-06 the planner MAY remove this sort and rely on iteration order, OR keep it -- both produce correct output." Per Task 1, the sort is REMOVED; the order follows the in-source iteration. Verify the resulting order matches the test's expectations -- if not, adjust the test to assert presence + relative-order via two `assert.ok(...indexOf...)` checks rather than a single byte string.

    8. Line 205 (bare-form empty scopes): KEEP `assert.equal(notifications[0].message, "(no marketplaces)")` unchanged. The V2 sentinel is identical to V1.

    9. Line 221 (single-marketplace user-scope enable): change `"● only [user] <autoupdate>"` to `"● only [user] (autoupdate enabled)\\n\\n/reload to pick up changes"`.

    10. ADD per-test severity assertions per D-18-05 ladder:
       - Fresh enable/disable tests (currently lines 92, 108, 155, 221): ADD `assert.equal(note.severity, undefined)` (info severity).
       - Idempotent enable/disable tests (lines 124, 138, 169): ADD `assert.equal(note.severity, "warning")`.
       - Not-found / failure tests (verify by reading the file -- any V1 `notifyError` test): ADD `assert.equal(note.severity, "error")`.

    11. NFR-5 grep assertion at line 272 (no git imports in autoupdate.ts source): KEEP unchanged.

    12. `pi` plumbing into call sites was done in Plan 18-00 Task 3; verify each `setMarketplaceAutoupdate({...})` call has `pi: ...` after `ctx`. If any call site is missing `pi`, STOP and escalate (it indicates Plan 18-00 left a gap).

    Honors D-18-05 (severity ladder), D-18-06 (byte-exact assertions through real notify() via makeCtx mock).
  </action>
  <verify>
    <automated>node --test --import tsx tests/orchestrators/marketplace/autoupdate.test.ts &amp;&amp; node --test --import tsx tests/architecture/catalog-uat.test.ts</automated>
  </verify>
  <done>
    autoupdate.test.ts: all 12 tests pass against the migrated autoupdate.ts; 8 byte-string flips + 2 regex/order flips at lines 195/197 + 4 new severity assertions land. Catalog UAT stays GREEN. `npm run check` passes when orchestrator + test land together (atomic commit per RESEARCH Risks #8). `grep -c "notifySuccess\\|notifyWarning\\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` returns 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal API refactor against the Phase 17.1 amended grammar (7-entry MarketplaceStatus + optional `reasons?:`). Byte output governed by Phase 17.1 catalog. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | (none) | V1->V2 migration of autoupdate.ts | accept | No new threats -- internal API refactor; byte output governed by the Phase 17.1 catalog (5 catalog states locked there); severity / reload-hint computed by notify() (D-16-11, D-16-12); no auth/session/input-validation changes. |
</threat_model>

<verification>
- `npm run check` GREEN after the atomic commit (orchestrator + test land together).
- Catalog UAT byte-equality GREEN end-to-end (the 5 autoupdate fixtures already exist from Phase 17.1; Plan 18-02 doesn't add new fixtures).
- `grep -r "notifySuccess\|notifyWarning\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` returns 0.
- `grep -c "from \"../../presentation/" extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` returns 0.
- Severity assertions added per D-18-05 ladder (fresh -> info; idempotent -> warning; failure -> error).
</verification>

<success_criteria>
- autoupdate.ts emits V2 `notify(opts.ctx, opts.pi, ...)` calls across all 5 Phase-17.1 catalog states (`autoupdate enabled`, `autoupdate disabled`, `skipped` + `reasons`, `failed`) + the empty-scopes sentinel; the alphabetic sort is removed per D-16-06.
- autoupdate.test.ts byte assertions match V2 catalog fixtures; severity assertions added per the D-18-05 ladder.
- Phase 17.1 grammar consumed end-to-end without further amendments.
- `npm run check` + catalog UAT GREEN.
</success_criteria>

<output>
Create `.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-02-SUMMARY.md` documenting:
- The 2 files modified.
- The mapping from V1 callsites (141/155/163/184) to V2 catalog states (enable-fresh, disable-fresh, enable-idempotent, disable-idempotent, failure-not-found, empty).
- The alphabetic-sort removal and the resulting iteration-order change.
- Severity assertions added per D-18-05.
- Confirmation that catalog UAT + `npm run check` both pass.
</output>
