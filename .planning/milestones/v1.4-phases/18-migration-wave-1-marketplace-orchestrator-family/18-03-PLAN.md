---
phase: 18-migration-wave-1-marketplace-orchestrator-family
plan: 3
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts
  - tests/orchestrators/marketplace/list.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-18-06: tests stay END-TO-END through real `notify()` via `makeCtx()`; the existing list.test.ts byte assertions ALREADY match the V2 catalog list-surface form (per RESEARCH §Per-File Test Surface 'All list.test.ts byte assertions ALREADY MATCH the V2 catalog list-surface byte form')."
    - "D-18-08-amendment: Plan 18-03 inherits the locked construction pattern from Wave 1 pilot (add.ts) -- the only difference is `mp.status` is INTENTIONALLY OMITTED (list-surface arm uses `mp.status === undefined`) and `details: MarketplaceDetails` is set when the persisted record carries `autoupdate` and/or `lastUpdatedAt`."
    - "Wave 1 pilot (Plan 18-01) recipe followed: payload construction mirrors the add.ts pattern; the list surface is the only marketplace family command using the `mp.status === undefined` arm of `renderMpHeader`."
    - "Backwards-compatible enrichment: thread `record.lastUpdatedAt` into `details.lastUpdatedAt` so the `<last-updated <iso>>` token surfaces on list rows when the persisted record carries it (V1 list.ts at lines 54-60 omitted it; V2 catalog at `:704` covers it)."
  byte_contracts:
    - "ML mixed-scopes list (multi-marketplace): byte form per catalog UAT fixture `mixed-scopes` at tests/architecture/catalog-uat.test.ts:1087-1107 -- each row is `● <name> [<scope>] [<autoupdate>] [<last-updated ...>]` with two-blank-line separators between marketplace blocks (renderer-controlled per D-16-07)."
    - "ML empty list: `note.message === \"(no marketplaces)\"` -- D-16-17."
    - "ML single-scope rows (project / user): the existing list.test.ts assertions (lines 92, 121, 147, 172) ALREADY match V2 byte form; no flip needed."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts"
      provides: "V2 notify() call constructing list-surface payload (mp.status omitted; details set when autoupdate or lastUpdatedAt present)"
      contains: "notify(opts.ctx, opts.pi"
    - path: "tests/orchestrators/marketplace/list.test.ts"
      provides: "byte assertions confirmed against V2 catalog list-surface form; optional new <last-updated> coverage test"
      contains: "(no marketplaces)"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts::notify (list-surface arm of renderMpHeader)"
      via: "single V2 notify(ctx, pi, message) call with `mp.status === undefined`"
      pattern: "notify\\(opts\\.ctx,\\s*opts\\.pi"
  coverage_constraints:
    - "Catalog UAT byte-equality MUST stay GREEN."
    - "`npm run check` GREEN at merge (atomic commit)."
    - "Zero V1 callers remain in list.ts after this plan."
---

<objective>
Migrate `extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts` from the V1 `notifySuccess(opts.ctx, renderMarketplaceList(allRecords))` callsite (line 67) to V2 `notify(opts.ctx, opts.pi, NotificationMessage)`. Construct payloads with `mp.status` INTENTIONALLY OMITTED (list-surface arm) and `details: MarketplaceDetails` set whenever the persisted record carries `autoupdate` and/or `lastUpdatedAt`. Update `tests/orchestrators/marketplace/list.test.ts` (8 tests) -- per RESEARCH the existing byte assertions ALREADY match the V2 catalog list-surface form; the assertion changes here are minimal (verification + optional new `<last-updated>` test). Drop now-orphaned `presentation/marketplace-list` imports.

Purpose: deliver the list command surface in V2 grammar; the list surface is the only marketplace-family command using the `mp.status === undefined` renderer arm; this plan exercises the optional `details: MarketplaceDetails` payload field for the first time across the marketplace family.

Output: 2 modified files in one atomic commit.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-CONTEXT.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-RESEARCH.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-PATTERNS.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-VALIDATION.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-00-SUMMARY.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-01-SUMMARY.md
@extensions/pi-claude-marketplace/shared/notify.ts
@docs/output-catalog.md
@tests/architecture/catalog-uat.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate list.ts -- 1 V1 wrapper -> V2 notify() with list-surface payload; thread record.lastUpdatedAt into details; drop presentation imports</name>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts (full file -- 1 V1 callsite at line 67; ListMarketplacesOptions already carries `readonly pi: ExtensionAPI` after Plan 18-00; the local `MarketplaceListEntry` push site is around lines 54-60).
    - extensions/pi-claude-marketplace/shared/notify.ts (read lines 500-530 for MarketplaceNotificationMessage shape including `details?: MarketplaceDetails`; lines 580-620 for renderMpHeader's list-surface arm; lines 1140-1180 for `notify(ctx, pi, message)`).
    - tests/architecture/catalog-uat.test.ts lines 1081-1108 (list fixtures: `empty` + `mixed-scopes` -- the only fixture using `details: { autoupdate, lastUpdatedAt? }`).
    - 18-CONTEXT.md D-18-06, D-18-08-amendment.
    - 18-RESEARCH.md "Per-File Callsite Inventory" → `list.ts` (lines 191-238).
    - 18-PATTERNS.md "Pattern Assignments" → `orchestrators/marketplace/list.ts` (V1/V2 diff + imports).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts (Wave 1 pilot -- read the recipe-block-comment; this plan adapts it for the list-surface variation).
  </read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts</files>
  <action>
    Operate in this order:

    1. Replace imports per RESEARCH §"Now-orphaned imports to drop (Plan 18-03)":
       - DROP: `renderMarketplaceList` + `MarketplaceListEntry` type from `../../presentation/marketplace-list.ts`; `notifySuccess` from `../../shared/notify.ts`.
       - DROP `ParsedSource` import (line 27 -- becomes unused after the V2 payload removes the `source` field that V1 carried through `MarketplaceListEntry`).
       - ADD: `notify` from `../../shared/notify.ts`; `type MarketplaceNotificationMessage` from same module (if the payload accumulator is typed).

    2. Replace the V1 callsite at line 67. Currently the orchestrator builds a `MarketplaceListEntry[]` array via the loop at lines 50-60, then calls `notifySuccess(opts.ctx, renderMarketplaceList(allRecords))`. The new shape:
       - Replace the accumulator type from `MarketplaceListEntry[]` to a structurally-equivalent shape that can be mapped to `MarketplaceNotificationMessage[]` (you may keep an intermediate row type local to the file if it simplifies the loop, or build the `MarketplaceNotificationMessage` directly in the loop).
       - Construct the V2 payload using a `.map()` (or accumulate during the loop):
          - `name`: from `record.name`.
          - `scope`: from `record.scope`.
          - `mp.status`: INTENTIONALLY OMITTED (list-surface arm).
          - `details`: SET when `record.autoupdate !== undefined || record.lastUpdatedAt !== undefined`; otherwise OMITTED. When set: `{ autoupdate: record.autoupdate ?? false, ...(record.lastUpdatedAt !== undefined && { lastUpdatedAt: record.lastUpdatedAt }) }`.
          - `plugins: []`.
       - Call `notify(opts.ctx, opts.pi, { marketplaces: <built array> })`.
       - Empty case: when `allRecords.length === 0`, pass `{ marketplaces: [] }` (the renderer emits `(no marketplaces)` per D-16-17). This may collapse naturally if the map is over an empty source array.

    3. Backwards-compatible enrichment per RESEARCH §"Per-File Callsite Inventory → list.ts" final note: thread `record.lastUpdatedAt` into `details.lastUpdatedAt` (the V1 orchestrator at lines 54-60 didn't pass it; the persisted record may carry it; V2 catalog `:704` renders `<last-updated <iso>>` when set). If `record.lastUpdatedAt` is undefined, omit `lastUpdatedAt` from `details`.

    4. Add a brief comment near the new `notify()` call referencing the Wave 1 pilot recipe (e.g., `// V2 list-surface construction -- mp.status intentionally omitted (list-surface arm of renderMpHeader); details set when autoupdate or lastUpdatedAt is present.`).

    After this task: `grep -c "notifySuccess\\|notifyWarning\\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts` returns 0; `grep -c "from \"../../presentation/" extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts` returns 0.

    Honors D-18-06 (single notify per orchestration), D-18-08-amendment (pilot pattern mirroring), and the list-surface arm convention (`mp.status === undefined`).
  </action>
  <verify>
    <automated>node --test --import tsx tests/orchestrators/marketplace/list.test.ts</automated>
  </verify>
  <done>
    list.ts compiles strict; the 1 V1 caller is gone; presentation imports dropped; the V2 `notify()` call constructs payloads with `mp.status` omitted, `details` set conditionally on `autoupdate`/`lastUpdatedAt` presence, and `plugins: []`. The empty-records case naturally yields `{ marketplaces: [] }`. Task 2 lands the test updates that unlock this verify in the same atomic commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify list.test.ts byte assertions against the migrated orchestrator; add an optional `<last-updated>` coverage test</name>
  <read_first>
    - tests/orchestrators/marketplace/list.test.ts (full file -- 8 tests; assertions at lines 65, 92, 121, 147, 172 per RESEARCH §"Per-File Test Surface" `All list.test.ts byte assertions ALREADY MATCH the V2 catalog list-surface byte form`; the existing `makeCtx()` pattern; `pi` thread done in Plan 18-00 Task 3).
    - 18-RESEARCH.md "Per-File Test Surface" → list.test.ts table (lines 481-494 -- ZERO byte-string flips; optional new test).
    - 18-PATTERNS.md "tests/orchestrators/marketplace/list.test.ts" section (analog: SELF; no flip needed).
    - tests/architecture/catalog-uat.test.ts (lines 1081-1108 -- binding V2 list-surface byte forms).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts (post-Task-1 state).
  </read_first>
  <files>tests/orchestrators/marketplace/list.test.ts</files>
  <action>
    Apply the assertion verification + optional enrichment test per RESEARCH §"Per-File Test Surface":

    1. Confirm each existing byte assertion lines up with V2 form:
       - Line 65: `assert.equal(notifications[0].message, "(no marketplaces)")` -- KEEP unchanged; matches V2 D-16-17 sentinel.
       - Line 92: `assert.equal(notifications[0].message, "● local [project]")` -- KEEP unchanged; the bare row form (no autoupdate, no lastUpdatedAt -> `details` omitted) renders as `● <name> [<scope>]` on the list surface.
       - Line 121: `assert.equal(notifications[0].message, "● official [project]")` -- KEEP unchanged.
       - Line 147: `assert.equal(notifications[0].message, "● auto [project] <autoupdate>")` -- KEEP unchanged; the catalog at `:705` confirms `<autoupdate>` marker on list-surface rows when `details.autoupdate === true`.
       - Line 172: `assert.equal(notifications[0].message, "● user-only [user]")` -- KEEP unchanged.
       - Source-grep assertions (ML-3 at line 176, NFR-5 at line 199, D-04 corollary at line 210) -- KEEP unchanged.

    2. ADD a new test exercising the `<last-updated <iso>>` marker per RESEARCH §"Per-File Test Surface" "optional `<last-updated>` test addition":
       - Setup: insert a marketplace record via `saveMarketplaceState` (or the equivalent test seam already used in list.test.ts) with `autoupdate: true` and `lastUpdatedAt: "2026-05-25T00:00:00Z"` (or any ISO 8601 string).
       - Invoke `listMarketplaces({ ctx, pi: ctx.pi as ExtensionAPI cast, cwd, scope })`.
       - Assert `notifications[0].message === "● test-mp [project] <autoupdate> <last-updated 2026-05-25T00:00:00Z>"` (verify the EXACT byte form against catalog UAT fixture `mixed-scopes` at `tests/architecture/catalog-uat.test.ts:1087-1107`, specifically the `alpha [project]` row which is the canonical `<last-updated ...>` form).
       - Name the test something like "ML-V2 / D-16-12: list surface emits `<last-updated <iso>>` marker when record carries lastUpdatedAt".

    3. If the existing list-record persistence helpers (in `persistence/`) don't accept a `lastUpdatedAt` field directly, fall back to constructing the persisted record via the lower-level state-save seam used elsewhere in the test file. Inspect existing tests in list.test.ts that set up records to identify the appropriate test fixture/helper.

    4. `pi` plumbing into call sites was done in Plan 18-00 Task 3; verify each `listMarketplaces({...})` call has `pi: ...` after `ctx`. If any call site is missing, STOP and escalate.

    Honors D-18-06 (existing makeCtx pattern preserved verbatim) and the RESEARCH-recommended `<last-updated>` enrichment coverage.
  </action>
  <verify>
    <automated>node --test --import tsx tests/orchestrators/marketplace/list.test.ts &amp;&amp; node --test --import tsx tests/architecture/catalog-uat.test.ts</automated>
  </verify>
  <done>
    list.test.ts: existing 8 tests pass against the migrated list.ts WITHOUT byte-flip changes; one new `<last-updated>` coverage test passes. Catalog UAT stays GREEN. `npm run check` passes in the atomic commit (orchestrator + test together). `grep -c "notifySuccess\\|notifyWarning\\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts` returns 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal API refactor; byte output governed by Phase 17 catalog (list-surface section). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | (none) | V1->V2 migration of list.ts | accept | No new threats -- internal API refactor; the lastUpdatedAt enrichment surfaces a value already persisted (no new data flowing into the system); byte output governed by Phase 17 catalog. |
</threat_model>

<verification>
- `npm run check` GREEN after the atomic commit.
- Catalog UAT byte-equality GREEN end-to-end.
- `grep -r "notifySuccess\|notifyWarning\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts` returns 0.
- `grep -c "from \"../../presentation/" extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts` returns 0.
- One new test exercises the `<last-updated ...>` marker on list rows.
</verification>

<success_criteria>
- list.ts emits exactly one V2 `notify(opts.ctx, opts.pi, ...)` call with `mp.status` intentionally omitted (list-surface arm); `details: MarketplaceDetails` set when `autoupdate`/`lastUpdatedAt` present.
- list.test.ts: existing assertions preserved; one new `<last-updated>` test added.
- `npm run check` + catalog UAT GREEN.
</success_criteria>

<output>
Create `.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-03-SUMMARY.md` documenting:
- The 2 files modified.
- Confirmation that existing list.test.ts byte assertions ALREADY match V2 (no flips).
- The new `<last-updated>` coverage test added.
- The `record.lastUpdatedAt` enrichment threaded through `details.lastUpdatedAt`.
- Confirmation that catalog UAT + `npm run check` both pass.
</output>
