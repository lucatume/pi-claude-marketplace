---
phase: 18-migration-wave-1-marketplace-orchestrator-family
plan: 6
type: execute
wave: 3
depends_on:
  - 18-02
  - 18-03
  - 18-04
  - 18-05
files_modified:
  - eslint.config.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-18-07: Plan 18-06 narrows ONLY MSG-Block 1 (msg-sr-1..6 severity routing) and MSG-Block 1b (msg-gr-3 per-scope rendering) in eslint.config.js. Both keep existing `files:` globs unchanged and ADD an `ignores: [\"extensions/pi-claude-marketplace/orchestrators/marketplace/**\"]` entry. MSG-Block 1b's existing `edge/handlers/**` scope is unchanged (Phase 20 owns that surface). All other MSG-Blocks (2, 3, 4a, 4b, 5, 6) need NO modification in Phase 18."
    - "SC #1: After Wave 2 + Wave 3, zero V1 callers remain in orchestrators/marketplace/**/*.ts: `grep -r \"notifySuccess|notifyWarning|notifyError\" extensions/pi-claude-marketplace/orchestrators/marketplace/` returns 0."
    - "SC #2: MSG-Block 1 + 1b `files:` globs narrowed via the additive `ignores:` entry; eslint.config.js diff is exactly 2 lines inserted."
    - "SC #3: Catalog UAT byte-equality GREEN for every marketplace-family `(section, state)` fixture (already proven by Plans 18-01..05; this plan re-verifies end-to-end)."
    - "SC #4: `npm run check` GREEN; no orchestrators outside the marketplace family have changed call-site shape (verified by `git diff` scope check)."
    - "Claude's Discretion (CONTEXT): the `orchestrators/marketplace/shared.ts` stale comment cleanup at lines 481-484 (references to V1 `notifyError` patterns from comments only -- `shared.ts` does NOT call notify wrappers itself) is folded into THIS plan per discretion. Optional cleanup; if the comments are no longer accurate after Plans 18-01..05 land, refresh them."
  byte_contracts:
    - "End-to-end catalog UAT verification: all 30+ marketplace-family fixtures at tests/architecture/catalog-uat.test.ts:1085-1292 produce byte-equal output through real `notify()`."
  artifacts:
    - path: "eslint.config.js"
      provides: "MSG-Block 1 + 1b narrowed via additive ignores entry for marketplace family"
      contains: "orchestrators/marketplace/**"
  key_links:
    - from: "eslint.config.js::MSG-Block 1"
      to: "extensions/pi-claude-marketplace/orchestrators/marketplace/**"
      via: "ignores additive entry"
      pattern: "ignores:\\s*\\[\"extensions/pi-claude-marketplace/orchestrators/marketplace/\\*\\*\""
    - from: "eslint.config.js::MSG-Block 1b"
      to: "extensions/pi-claude-marketplace/orchestrators/marketplace/**"
      via: "ignores additive entry (matching block 1's path string)"
      pattern: "ignores:\\s*\\[\"extensions/pi-claude-marketplace/orchestrators/marketplace/\\*\\*\""
  coverage_constraints:
    - "Plan 18-06 must run AFTER Plans 18-02 + 18-03 + 18-04 + 18-05 ALL land. The lint narrowing assumes every marketplace orchestrator has stopped calling V1 wrappers."
    - "If any Wave 2 plan failed to fully eliminate V1 callers, the MSG-Block 1 lints would have FIRED before this plan ran (Plans 18-01..05 each kept `npm run check` GREEN at merge); but the final end-to-end verification here is the phase gate."
    - "Phase 19 will EXTEND the same `ignores:` entry with `orchestrators/plugin/**`; Phase 20 with `orchestrators/edge/**` AND will REMOVE Block 1b's `edge/handlers/**` files entry; Phase 21 deletes the entire MSG-* plugin wiring. Additive contract preserved."
---

<objective>
Final narrowing + verification plan. ADD an `ignores: ["extensions/pi-claude-marketplace/orchestrators/marketplace/**"]` entry to BOTH MSG-Block 1 (severity routing, lines 151-169 in eslint.config.js) and MSG-Block 1b (per-scope rendering, lines 170-188) per D-18-07. Verify all 4 Phase 18 Success Criteria GREEN end-to-end: SC #1 (zero V1 callers in marketplace family); SC #2 (lint globs narrowed); SC #3 (catalog UAT byte-equality GREEN); SC #4 (`npm run check` GREEN, no out-of-scope orchestrator changes). Optionally clean stale `notifyError`-related comments in `orchestrators/marketplace/shared.ts` lines 481-484 per CONTEXT Claude's Discretion (shared.ts has no V1 callers; only comments referencing V1 patterns).

Purpose: complete Phase 18's contribution to SNM-22 (marketplace family migration partial) and finalize the lint surface so the MSG-* plugin no longer scopes the now-migrated marketplace orchestrators. Phase 19/20 inherit the additive `ignores:` contract.

Output: 1 modified file (`eslint.config.js`) + optional 1 file with comment-only changes (`orchestrators/marketplace/shared.ts`). All 4 Phase-18 success criteria proven GREEN.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-CONTEXT.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-RESEARCH.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-PATTERNS.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-VALIDATION.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-00-SUMMARY.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-01-SUMMARY.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-02-SUMMARY.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-03-SUMMARY.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-04-SUMMARY.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-05-SUMMARY.md
@eslint.config.js
@tests/architecture/catalog-uat.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the additive `ignores` entry to MSG-Block 1 + MSG-Block 1b in eslint.config.js; optionally clean stale comments in orchestrators/marketplace/shared.ts</name>
  <read_first>
    - eslint.config.js (full file -- specifically lines 145-200 covering MSG-Block 1 + 1b; the exact insertion points are between the `files:` and `plugins:` lines in each block).
    - 18-RESEARCH.md "MSG-* Lint Surface Today" section + the "Exact edit (Plan 18-06)" subsection (lines 562-622 -- the literal 2-line insert).
    - 18-PATTERNS.md "Pattern Assignments" → `eslint.config.js (MSG-Block 1 + 1b narrowing)` section + "Shared Patterns" → "Bounded MSG-* lint narrowing precedent (Phase 16)" subsection.
    - 18-CONTEXT.md D-18-07 (additive narrowing strategy).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts (read lines 480-500 -- the stale `notifyError` comment block per CONTEXT Claude's Discretion; verify the comments still reference patterns that are dead post-Wave 2; refresh or leave as-is per planner judgment).
  </read_first>
  <files>
    eslint.config.js,
    extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
  </files>
  <action>
    Operate in this order:

    1. In `eslint.config.js`, locate MSG-Block 1 (the block starting with the comment `// MSG-Block 1 (MSG-SR-1..6): cascade/severity routing -- orchestrators surface.`). After the `files: ["extensions/pi-claude-marketplace/orchestrators/**/*.ts"],` line and BEFORE the `plugins: { msg: msgPlugin },` line, INSERT a new line:

       `  ignores: ["extensions/pi-claude-marketplace/orchestrators/marketplace/**"],`

       Match the existing indentation (2 spaces in flat config block).

    2. In `eslint.config.js`, locate MSG-Block 1b (the block starting with the comment `// MSG-Block 1b (MSG-GR-3): per-scope rendering rule.`). After the closing bracket `]` of the existing 2-entry `files:` array and BEFORE the `plugins: { msg: msgPlugin },` line, INSERT the SAME `ignores:` line:

       `  ignores: ["extensions/pi-claude-marketplace/orchestrators/marketplace/**"],`

    3. Do NOT touch any other MSG-Block (Blocks 2, 3, 4a, 4b, 5, 6) -- D-18-07 confines Phase 18 to MSG-Block 1 + 1b only. Verify by `git diff eslint.config.js | wc -l` showing a small additive diff (just 2 new lines, no deletions, no other block changes).

    4. OPTIONAL (Claude's Discretion per CONTEXT): in `orchestrators/marketplace/shared.ts` at lines 481-484, review the block comment referring to V1 `notifyError` patterns. After Plans 18-01..05 land, these references are stale (V1 wrappers are no longer called from any marketplace orchestrator). Either:
       - REFRESH the comment to describe the V2 notify pattern and reference the relevant plan SUMMARY (e.g., 18-01-SUMMARY.md construction recipe), OR
       - LEAVE AS-IS and defer the comment cleanup to Phase 21 (since shared.ts itself is unchanged -- only comments are stale).
       - Document the choice in the plan SUMMARY.

       Reminder: shared.ts has NO V1 wrapper callsites of its own (verified by RESEARCH -- the file has only comments referencing V1 patterns). The comment refresh is a cosmetic improvement, not a functional change.

    Honors D-18-07 (narrow ONLY MSG-Block 1 + 1b; additive ignores; no other block changes).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    eslint.config.js contains exactly 2 new lines (one in MSG-Block 1, one in MSG-Block 1b) inserting the additive `ignores:` entry. `grep -c "extensions/pi-claude-marketplace/orchestrators/marketplace/\\*\\*" eslint.config.js` returns at least 2. No other MSG-Block has been touched. `npm run check` passes -- the marketplace orchestrators no longer trigger any MSG-SR-* or MSG-GR-3 lint rule. If the optional `shared.ts` comment cleanup is applied, the file content changes are documentation-only and do not affect linting.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final end-to-end Phase 18 success-criteria verification (SC #1, #2, #3, #4 all GREEN)</name>
  <read_first>
    - 18-RESEARCH.md "Phase Requirements → Test Map" + "Validation Architecture" (Success Criteria mapping).
    - 18-VALIDATION.md "Per-Task Verification Map" (the 6-row table including Plan 18-06 aggregate row).
    - 18-CONTEXT.md "Phase Boundary" (4 success criteria list).
    - eslint.config.js (post-Task-1 state).
    - All Wave 2 plan SUMMARYs: 18-02-SUMMARY.md, 18-03-SUMMARY.md, 18-04-SUMMARY.md, 18-05-SUMMARY.md (confirm each landed cleanly).
  </read_first>
  <files>(read-only verification; no files modified in this task)</files>
  <action>
    Execute the 4 Success Criteria checks end-to-end:

    1. **SC #1 (zero V1 callers in marketplace family):** Run `grep -rE "notifySuccess|notifyWarning|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/ || echo OK` and confirm the output is `OK` (no matches). Per V2 grammar, the marketplace family should have ZERO references to the V1 severity-named wrappers (excluding comments -- if any stale comments mention the wrapper names without invoking them, those are acceptable; `grep -rE "notify(Success|Warning|Error)\\(" extensions/pi-claude-marketplace/orchestrators/marketplace/` is the stricter callsite-only check and should also return 0).

    2. **SC #2 (MSG-Block 1 + 1b lint globs narrowed):** Run `grep -c "extensions/pi-claude-marketplace/orchestrators/marketplace/\\*\\*" eslint.config.js` and confirm the result is at least 2 (one per MSG-Block). Also run `npm run lint` (or `npm run check`'s lint subcommand) to verify that the MSG-SR-* + MSG-GR-3 rules no longer fire on any file in `orchestrators/marketplace/**`.

    3. **SC #3 (catalog UAT byte-equality GREEN for marketplace family):** Run `node --test --import tsx tests/architecture/catalog-uat.test.ts` and confirm exit code 0. The 30+ marketplace family fixtures at lines 1085-1292 (list 1081-1108; add 1113-1149; remove 1154-1184; update 1189-1233; autoupdate 1238-1292) all produce byte-equal output through real `notify()`.

    4. **SC #4 (`npm run check` GREEN; no out-of-scope orchestrator changes):**
       - Run `npm run check` and confirm exit code 0.
       - Run `git diff --name-only main..HEAD -- extensions/pi-claude-marketplace/orchestrators/plugin/ extensions/pi-claude-marketplace/orchestrators/edge/ extensions/pi-claude-marketplace/edge/handlers/plugin/ extensions/pi-claude-marketplace/edge/handlers/edge/ 2>/dev/null` and confirm the output is empty -- no orchestrators outside the marketplace family have changed call-site shape.

    Document each check result in the plan SUMMARY:
    - Exact `grep` command output for SC #1 (expected: 0 matches OR `OK`).
    - Exact `grep -c` output for SC #2 (expected: >= 2).
    - Catalog UAT exit code (expected: 0).
    - `npm run check` exit code (expected: 0).
    - The "no out-of-scope changes" verification (expected: empty diff).

    If any check fails: STOP. Phase 18 is not complete; the failing criterion must be remediated either via a follow-up plan within Phase 18, or by reverting the offending edits in the appropriate Wave 2 plan and re-merging. Escalate to operator.

    Honors all 4 ROADMAP §"Phase 18" Success Criteria + the 18-VALIDATION.md per-plan command map for Plan 18-06.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; node --test --import tsx tests/architecture/catalog-uat.test.ts &amp;&amp; ( grep -rE "notify(Success|Warning|Error)\(" extensions/pi-claude-marketplace/orchestrators/marketplace/ &amp;&amp; exit 1 || true )</automated>
  </verify>
  <done>
    All 4 Phase 18 Success Criteria proven GREEN:
    - SC #1: grep returns 0 V1 callsites in marketplace family.
    - SC #2: eslint.config.js MSG-Block 1 + 1b contain the additive `ignores:` entry; lint does not fire on marketplace orchestrators.
    - SC #3: catalog UAT exits 0 against the 30+ marketplace family fixtures.
    - SC #4: `npm run check` exits 0; no orchestrators outside the marketplace family have changed call-site shape (verified by `git diff` scope).
    Phase 18 deliverables complete; Phase 19 can begin (extends the same `ignores:` entry with `orchestrators/plugin/**`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Lint config narrowing + verification; no source-code execution changes; byte output unchanged from Phase 17 catalog. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06-01 | (none) | eslint.config.js additive ignores | accept | No new threats -- lint config change with no runtime effect; the ignores are scoped narrowly to the migrated marketplace family; non-marketplace orchestrators (plugin/, edge/) remain fully linted by MSG-Block 1 + 1b; Phase 21 will eventually delete the MSG-* plugin entirely. The additive contract preserves the security-critical no-direct-notify-outside-shared/notify.ts invariant (IL-2) via other MSG-Blocks that remain global. |
</threat_model>

<verification>
- `npm run check` exits 0.
- `node --test --import tsx tests/architecture/catalog-uat.test.ts` exits 0.
- `grep -rE "notify(Success|Warning|Error)\(" extensions/pi-claude-marketplace/orchestrators/marketplace/` returns 0 callsites (excluding comments).
- `grep -c "extensions/pi-claude-marketplace/orchestrators/marketplace/\*\*" eslint.config.js` returns at least 2.
- `git diff --name-only main..HEAD -- extensions/pi-claude-marketplace/orchestrators/plugin/ extensions/pi-claude-marketplace/orchestrators/edge/` returns empty (no out-of-scope changes).
</verification>

<success_criteria>
All 4 ROADMAP Phase 18 Success Criteria proven GREEN end-to-end:
1. Zero `notifySuccess` / `notifyWarning` / `notifyError` callers in `orchestrators/marketplace/**/*.ts`.
2. MSG-Block 1 + 1b `files:` globs narrowed in `eslint.config.js` via additive ignores entry; rules still wired for plugin/edge families.
3. Catalog UAT byte-equality GREEN for every marketplace-family `(section, state)` fixture (~30+).
4. `npm run check` GREEN; no orchestrators outside marketplace family have changed call-site shape.
</success_criteria>

<output>
Create `.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-06-SUMMARY.md` documenting:
- The 1 file modified (eslint.config.js, 2 lines added) + optionally `orchestrators/marketplace/shared.ts` comment cleanup (Claude's Discretion).
- The verification matrix: 4 Success Criteria, each with the exact command run + observed result.
- The contribution to SNM-22 closure (Phase 21 closes the requirement with V1 wrapper deletion; Phase 18 contributed the marketplace-family migration partial).
- The handoff to Phase 19: the additive `ignores:` contract; Phase 19 extends with `orchestrators/plugin/**`.
- Confirmation that `cascade.test.ts` and `shared.test.ts` required no migration (per RESEARCH §"Cascade + Shared Test Landmines").
</output>
