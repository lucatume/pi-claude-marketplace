---
phase: 18-migration-wave-1-marketplace-orchestrator-family
plan: 1
type: execute
wave: 1
depends_on:
  - 18-00
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
  - tests/orchestrators/marketplace/add.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-18-08-amendment / D-18-09-amendment: Plan 18-01 is the Wave 1 pilot; it migrates the simplest orchestrator (`add.ts`, 2 V1 callsites) so Wave 2 plans can mirror the locked NotificationMessage construction pattern verbatim."
    - "D-18-06: tests stay END-TO-END through real `notify()` via `makeCtx()`; byte-exact V2 string assertions replace V1; the `makeCtx()` pattern recording `{ message, severity }` tuples is preserved verbatim."
    - "D-18-01 precedent extension (Claude's Discretion, option A per RESEARCH §Open Question A3): the `notifyWarning` cache-leak warning at line 141 is DROPPED entirely; the underlying try/catch swallows the cache-refresh failure; no user-visible second notification."
    - "D-18-06 implicit consequence: `note.message.includes('/reload to pick up changes')` flips from `false` to `true` for github-source success (and for path-source success) because V2 catalog `<!-- catalog-state: github-source -->` and `<!-- catalog-state: path-source -->` both emit the reload-hint per D-16-12 mp-level state-change rule."
    - "Claude's Discretion (RESEARCH §Open Question 1): YES include a 6-10 line NotificationMessage construction recipe block-comment at the new `notify()` call site so Wave 2 plans literally mirror it."
  byte_contracts:
    - "MA path-source (line 160 replacement, both source kinds collapse to one V2 shape): `note.message === \"● <recordedName> [<scope>] (added)\\n\\n/reload to pick up changes\"` -- catalog UAT fixture `path-source` at tests/architecture/catalog-uat.test.ts:1117-1119 + `github-source` at lines 1122-1133 (the `<autoupdate>` marker MOVES off the (added) state-change arm onto the list surface)."
    - "MA cache-leak warning (line 141 replacement): DROPPED entirely; no V2 byte output, no second notify() call."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts"
      provides: "V2 notify() call site replacing the 2 V1 wrapper invocations; presentation/* imports dropped; NotificationMessage construction recipe block-comment for Wave 2 to mirror"
      contains: "notify(opts.ctx, opts.pi, {"
    - path: "tests/orchestrators/marketplace/add.test.ts"
      provides: "Byte-exact V2 assertions including reload-hint POSITIVE flip; existing makeCtx() preserved per D-18-06"
      contains: "(added)\\n\\n/reload to pick up changes"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts::notify"
      via: "single V2 notify(ctx, pi, message) call replacing V1 notifySuccess"
      pattern: "notify\\(opts\\.ctx,\\s*opts\\.pi"
    - from: "tests/orchestrators/marketplace/add.test.ts"
      to: "tests/architecture/catalog-uat.test.ts::FIXTURES::path-source"
      via: "byte-exact assertion mirrors catalog fixture payload shape"
      pattern: "● valid-marketplace \\[project\\] \\(added\\)"
  coverage_constraints:
    - "Catalog UAT (`tests/architecture/catalog-uat.test.ts`) byte-equality MUST stay GREEN at every commit boundary."
    - "`npm run check` MUST be GREEN at the merge of this plan (NOT mid-task -- the orchestrator + test edits must land in the SAME atomic commit per RESEARCH Risks #8)."
    - "Zero V1 callers remain in `add.ts` after this plan: `grep -c \"notifySuccess\\|notifyWarning\\|notifyError\" extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts` returns 0."
---

<objective>
Migrate `extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts` from V1 severity-named wrappers (`notifySuccess` + `notifyWarning`, 2 callsites) to the V2 structured entrypoint `notify(opts.ctx, opts.pi, NotificationMessage)`. Update `tests/orchestrators/marketplace/add.test.ts` (13 tests) in lockstep with byte-exact V2 assertions per D-18-06. Drop now-orphaned `presentation/*` composer imports and the cache-leak warning per D-18-01 precedent extension. Include a 6-10 line NotificationMessage construction recipe block-comment above the new `notify()` call site so Wave 2 plans (18-02..05) literally mirror it.

Purpose: pilot the locked V2 construction pattern on the simplest orchestrator (2 callsites, no cascade, no rollback). Wave 2's 4 parallel agents inherit the pattern by reading the merged add.ts diff (per D-18-09-amendment).

Output: 2 modified files (add.ts + add.test.ts) in one atomic commit. `add.ts` emits exactly one V2 `notify()` call per orchestration outcome with `status: "added"` and empty `plugins: []`; `add.test.ts` asserts the byte string `● <name> [<scope>] (added)\n\n/reload to pick up changes` per catalog UAT fixture.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-CONTEXT.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-RESEARCH.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-PATTERNS.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-VALIDATION.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-00-SUMMARY.md
@extensions/pi-claude-marketplace/shared/notify.ts
@docs/output-catalog.md
@tests/architecture/catalog-uat.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate add.ts -- swap 2 V1 wrappers for one V2 notify() call; drop presentation imports + cache-leak warning; add construction recipe comment</name>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts (full file -- the 2 V1 callsites are at line 141 `notifyWarning` and line 160 `notifySuccess`; the `AddMarketplaceOptions` already carries `readonly pi: ExtensionAPI` after Plan 18-00).
    - extensions/pi-claude-marketplace/shared/notify.ts (read lines 220-260 for MARKETPLACE_STATUSES + MarketplaceNotificationMessage type; read lines 1140-1180 for the V2 `notify(ctx, pi, message)` signature).
    - tests/architecture/catalog-uat.test.ts lines 1085-1149 (marketplace list + add fixtures including `path-source` and `github-source` -- V2 payload shape reference).
    - tests/shared/notify-v2.test.ts (per-variant V2 byte assertions; use as cross-reference for any edge-case shapes the catalog UAT doesn't cover directly).
    - 18-CONTEXT.md D-18-01 (cleanup-leak DROP precedent), D-18-06 (test discipline + implicit reload-hint consequence), D-18-08-amendment + D-18-09-amendment (pilot scoping).
    - 18-RESEARCH.md "Per-File Callsite Inventory" → `add.ts` section (lines 72-118) + Open Question #1 (recipe comment recommendation) + Open Question A3 (option A drop cache-leak).
    - 18-PATTERNS.md "Pattern Assignments" → `extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts` section (full V1/V2 diff + import drops).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts (lines 100-119 -- the `readonly pi: ExtensionAPI` field docstring pattern; mirror the inline JSDoc style if useful).
  </read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts</files>
  <action>
    Operate in this order:

    1. Replace the imports per RESEARCH §"Now-orphaned imports to drop (Plan 18-01)":
       - DROP: `renderRow` + `MarketplaceRow` type from `../../presentation/compact-line.ts`; `MARKETPLACE_LABEL_PROBE` from `../../shared/constants/marketplace-label-probe.ts`; `notifySuccess, notifyWarning` from `../../shared/notify.ts`.
       - ADD: `notify` from `../../shared/notify.ts`. Inline payload construction means a type alias is usually not required, but if the per-call payload is hoisted to a local `const`, also add the `type NotificationMessage` (or `type MarketplaceNotificationMessage`) import from the same module.

    2. DROP the cache-leak warning entirely (D-18-01 precedent, RESEARCH Open Question A3 option A). The lines around 140-144 (`notifyWarning(opts.ctx, \`Marketplace "${recordedName}" added; completion cache refresh deferred: ${errorMessage(err)}\`)`) get removed. The surrounding `try/catch` is kept -- the cache-refresh failure still gets caught and swallowed; only the user-facing notification disappears. If the catch block ends up entirely empty after the notify removal, leave a one-line comment such as `// D-18-01 precedent: cache-refresh failures are swallowed silently in V2 (no clean MarketplaceNotificationMessage representation)` so future readers know it's deliberate.

    3. REPLACE the primary success notify call (currently around line 160, building `successRow: MarketplaceRow` then calling `notifySuccess(opts.ctx, renderRow(successRow, MARKETPLACE_LABEL_PROBE))`) with a single V2 `notify(opts.ctx, opts.pi, { marketplaces: [{ name: recordedName, scope: opts.scope, status: "added", plugins: [] }] })` call. The `<autoupdate>` marker that V1 placed on the (added) row MOVES off this surface entirely (V2 catalog reserves `<autoupdate>` for the list-surface `mp.status === undefined` arm with `details.autoupdate === true`). Drop the now-dead local `successRow` const, the conditional spread `...(source.kind === "github" && { marker: "autoupdate" as const })`, and any unused vars exposed by the simplification.

    4. ABOVE the new `notify()` call, add a 6-10 line block-comment construction recipe per RESEARCH Open Question #1 recommendation. Recipe contents: name the discriminated payload shape (`marketplaces[0].status === "added"`; empty `plugins[]`); cite the catalog UAT fixture key (`path-source` / `github-source`) and its location (`tests/architecture/catalog-uat.test.ts:1117-1132`); explain that severity + reload-hint are computed by notify() (D-16-11, D-16-12) -- callers MUST NOT compose them; cite D-18-08-amendment as the rationale for the pilot recipe; note that Wave 2 plans (18-02..05) mirror this pattern with their own `mp.status` values. The recipe is documentation only; do NOT inline code blocks inside the block-comment beyond the discriminator literal.

    5. After both edits, the file MUST have zero V1 callers. Verify with `grep -c "notifySuccess\\|notifyWarning\\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts` returning 0. Verify with `grep -c "from \"../../presentation/" extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts` returning 0.

    Honors D-18-01 (cache-leak DROP), D-18-06 (single notify per orchestration), D-18-08-amendment (pilot scope), D-18-09-amendment (Wave 1 pilot ordering), and RESEARCH Open Question #1 (recipe comment YES).
  </action>
  <verify>
    <automated>node --test --import tsx tests/orchestrators/marketplace/add.test.ts</automated>
  </verify>
  <done>
    add.ts compiles under strict TypeScript; imports no longer reference any `presentation/*` module; the V1 callers are gone (grep returns 0 as specified above); a single V2 `notify(opts.ctx, opts.pi, { marketplaces: [{...}] })` call exists at the former line-160 location; a 6-10 line construction-recipe block-comment sits directly above it. Note: the test file itself is updated in Task 2 -- this task's verify command may fail until Task 2 lands; an alternate spot-check is `npx tsc --noEmit` on add.ts alone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update add.test.ts -- byte-exact V2 assertions; reload-hint POSITIVE flip; cache-leak tests removed</name>
  <read_first>
    - tests/orchestrators/marketplace/add.test.ts (full file -- 13 tests, ~400 lines; the byte-exact V1 strings are at lines 91, 295, 373 + a reload-hint negative at line 94 per RESEARCH §"Per-File Test Surface"; the existing `makeCtx()` is at lines 24-40).
    - 18-RESEARCH.md "Per-File Test Surface" → add.test.ts table (line 433-444 with the 4 byte-flip targets).
    - 18-PATTERNS.md "tests/orchestrators/marketplace/add.test.ts" section (V1 vs V2 mock-pi pattern; the existing `pi: ctx.pi` thread comes from Plan 18-00 Task 3 so it's already in place).
    - tests/architecture/catalog-uat.test.ts (lines 1085-1149 again -- the binding V2 byte forms for `path-source` and `github-source`).
    - tests/shared/notify-v2.test.ts (per-variant V2 byte assertions; cross-reference for any edge case the catalog doesn't cover).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts (post-Task-1 state -- so the test changes line up with the migrated orchestrator).
  </read_first>
  <files>tests/orchestrators/marketplace/add.test.ts</files>
  <action>
    Apply the V1 -> V2 assertion flips per RESEARCH §"Per-File Test Surface":

    1. Line 91 (github-source success): change `assert.equal(note.message, "● valid-marketplace [project] <autoupdate> (added)")` to `assert.equal(note.message, "● valid-marketplace [project] (added)\\n\\n/reload to pick up changes")`. The `<autoupdate>` marker disappears per V2 catalog `<!-- catalog-state: github-source -->`; the reload-hint appears per D-16-12 (mp.status `"added"` is state-changing).

    2. Line 94 reload-hint flip: change `assert.equal(note.message.includes("/reload to pick up changes"), false)` to `assert.equal(note.message.includes("/reload to pick up changes"), true)`. Honors D-18-06 implicit consequence (deliberate user-visible behavior change accepted as V2 grammar contract per the CONTEXT D-18-06 callout).

    3. Line 295 (path-source success): change `assert.equal(note.message, "● valid-marketplace [project] (added)")` to `assert.equal(note.message, "● valid-marketplace [project] (added)\\n\\n/reload to pick up changes")`.

    4. Line 373 (tilde-path source): same flip as line 295.

    5. Line 400 substring assertion (`note.message.includes("[project]")`): KEEP unchanged.

    6. KEEP unchanged: state-mutation assertions (`loadState` + key presence), `gitOps.cloneCalls.length` counts, error-throw assertions on `MarketplaceDuplicateNameError` / `StaleSourceCloneError`, NFR-5 path-source-no-git assertions, MA-9 leak-cleanup assertion, CMP-1 cross-scope independence.

    7. Cache-leak tests: per RESEARCH §"Per-File Test Surface" "Tests to DELETE per D-18-01" -- none currently exist in add.test.ts that exercise the line-141 cache-leak warning. Confirm by `grep -in "cache-leak\\|completion cache refresh deferred" tests/orchestrators/marketplace/add.test.ts` returning 0 lines. No deletions needed.

    8. `pi` plumbing through call sites was already done in Plan 18-00 Task 3; verify by reading the file. Each `await addMarketplace({...})` call has `pi: ...` after `ctx`. If any call site is missing pi (shouldn't be -- Plan 18-00 is a hard dependency), STOP and escalate.

    Net assertion change: 4 byte-string flips + 1 boolean reload-hint flip; no new tests added; no severity assertions added (Plan 18-01 keeps the existing severity = undefined for the (added) info severity, no change). The existing `makeCtx()` recording `{ message, severity }` tuples is PRESERVED VERBATIM per D-18-06.

    Honors D-18-06 (byte-exact V2 assertions through real notify() via makeCtx mock) and the D-18-06 implicit consequence callout (reload-hint flip for github-source).
  </action>
  <verify>
    <automated>node --test --import tsx tests/orchestrators/marketplace/add.test.ts &amp;&amp; node --test --import tsx tests/architecture/catalog-uat.test.ts</automated>
  </verify>
  <done>
    add.test.ts: all 13 tests pass against the migrated `add.ts`; the 4 byte-string assertions at lines 91/295/373 + the boolean at 94 are flipped to V2 form per the RESEARCH table; the existing makeCtx pattern is preserved verbatim; no severity assertions added or removed. Catalog UAT (`tests/architecture/catalog-uat.test.ts`) stays GREEN. `npm run check` passes when the orchestrator + test edits land together (the orchestrator + test changes form a single atomic commit per RESEARCH Risks #8). `grep -c "notifySuccess\\|notifyWarning\\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts` returns 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan 18-01 is an internal API refactor. The byte output is governed by the Phase 17 catalog (binding user contract); add.ts's two V1 callsites collapse into one V2 `notify(ctx, pi, message)` call. No new attack surface; same `ctx.ui.notify` host channel (IL-2 invariant). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | (none) | V1->V2 migration of add.ts | accept | No new threats -- internal API refactor; byte output unchanged from Phase 17 catalog (the (added) shape is locked there); no auth/session/input-validation changes. The cache-leak warning DROP per D-18-01 has no security implication (the underlying try/catch still swallows the rm() failure; nothing additional escapes). |
</threat_model>

<verification>
- `npm run check` GREEN after the atomic commit (orchestrator + test land together per RESEARCH Risks #8).
- Catalog UAT (`tests/architecture/catalog-uat.test.ts`) byte-equality GREEN end-to-end.
- `grep -r "notifySuccess\|notifyWarning\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts` returns 0.
- `grep -c "from \"../../presentation/" extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts` returns 0.
- A 6-10 line NotificationMessage construction recipe block-comment exists above the new `notify()` call site.
</verification>

<success_criteria>
- add.ts emits exactly one V2 `notify(opts.ctx, opts.pi, ...)` call replacing the V1 line-160 `notifySuccess`; the line-141 `notifyWarning` cache-leak is dropped entirely.
- add.test.ts byte-exact assertions match V2 catalog `path-source` / `github-source` fixtures; reload-hint flipped to TRUE.
- The recipe block-comment is sufficient for Wave 2 agents to reproduce the construction pattern without re-deriving from sources.
- `npm run check` GREEN; catalog UAT GREEN end-to-end.
</success_criteria>

<output>
Create `.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-01-SUMMARY.md` documenting:
- The 2 files modified (add.ts + add.test.ts).
- The exact location of the 6-10 line recipe block-comment in add.ts (so Wave 2 agents can find it).
- The 4 byte flips + 1 reload-hint flip in add.test.ts.
- Confirmation that catalog UAT + `npm run check` both pass.
- Locked V2 construction pattern (paraphrase of the recipe) for Wave 2 plans to mirror.
</output>
