---
phase: 18-migration-wave-1-marketplace-orchestrator-family
plan: 5
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts
  - extensions/pi-claude-marketplace/edge/register.ts
  - tests/orchestrators/marketplace/update.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-18-02: DROP marketplace-level retry-hint suffix entirely. `MarketplaceNotificationMessage { status: \"failed\", plugins: [] }` renders as bare `⊘ <mp> [<scope>] (failed)`; `err.retryHint` stays internal to `MarketplaceUpdateError` (programmatic inspection only)."
    - "D-18-03: cascade cause-chain MOVES from marketplace-level body to per-plugin `PluginFailedMessage.cause`; renders at 4-space indent via `renderPluginRow` per D-16-08."
    - "D-18-06: tests stay END-TO-END through real `notify()` via `makeCtx()`; byte-exact V2 string assertions replace V1."
    - "D-18-09-amendment: update.ts has **6 V1 callsites** (CONTEXT canonical-refs says 5; research-verified count is 6) -- line 220 (empty targets), 584 (mp-failure WITH retry-hint), 586 (mp-failure WITHOUT retry-hint), 599 (cache-leak), 631 (autoupdate-OFF manifest-refresh), 647 (autoupdate-ON cascade via ternary dispatch). All 6 migrate in this plan."
    - "RESEARCH Risks #4 (silent contract change): autoupdate-OFF manifest-refresh at line 631 NOW emits the reload-hint per D-16-12 (V1 did not); the test at line 165 ADDS a reload-hint inclusion assertion."
    - "RESEARCH Risks #5 (glyph flip): the V1 cascade-`unchanged` outcome mapped to `skipped` + `up-to-date` + ● glyph (trivial-skip treatment); V2 catalog uses ⊘ glyph (per D-16-11 severity ladder); update.test.ts lines 514-516 flip `\"  ● b [project]\"` -> `\"  ⊘ b [project]\"`."
    - "RESEARCH Risks #10 + option-a follow-through: makeMarketplaceUpdateHandler factory at `edge/handlers/marketplace/update.ts` takes `pi` as first arg via the option-a pattern (consistent with Plan 18-00); register.ts line 87 updated accordingly."
  byte_contracts:
    - "MU empty targets (line 220 replacement): `note.message === \"(no marketplaces)\"` -- D-16-17 sentinel."
    - "MU mp-level failure (lines 584 + 586 collapse) per D-18-02: `note.message === \"⊘ <name> [<scope>] (failed)\"`; severity = error; no cause-chain trailer; no retry-hint. Catalog UAT fixture `mp-failure-network` at tests/architecture/catalog-uat.test.ts:1226-1232."
    - "MU autoupdate-OFF manifest-refresh (line 631): `note.message === \"● <name> [<scope>] (updated)\\n\\n/reload to pick up changes\"` -- catalog UAT fixture `autoupdate-off-manifest-refresh` at lines 1190-1195. Reload-hint NOW present (silent contract change per RESEARCH Risks #4)."
    - "MU autoupdate-ON cascade (line 647 via ternary): payload `{ marketplaces: [{ name, scope, status: \"updated\", plugins: outcomes.map(outcomeToCascadePluginMessage) }] }` -- catalog UAT fixture `mixed-outcomes` at lines 1197-1224. Glyph flip on `skipped` outcomes (● -> ⊘ per Risks #5). Severity computed by notify() per D-16-11."
    - "MU cache-leak (line 599): DROPPED entirely per D-18-01 precedent extension."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts"
      provides: "V2 notify() across all 6 V1 callsites; retry-hint dropped; cascade restructured per D-18-03 (outcomeToCascadePluginMessage); NULL_PROBE dead; presentation imports dropped"
      contains: "notify(ctx, pi"
    - path: "extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts"
      provides: "makeMarketplaceUpdateHandler accepts `pi` as first positional arg (option-a)"
      contains: "ExtensionAPI"
    - path: "extensions/pi-claude-marketplace/edge/register.ts"
      provides: "marketplaceUpdate wiring updated to thread pi"
      contains: "makeMarketplaceUpdateHandler(pi"
    - path: "tests/orchestrators/marketplace/update.test.ts"
      provides: "Byte-exact V2 assertions; retry-hint asserts deleted; glyph-flip on skipped cascade rows; reload-hint inclusion added on autoupdate-OFF; outcomeToCascadePluginMessage tests"
      contains: "(failed)"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts::notify"
      via: "V2 notify(ctx, pi, message) covering all 6 V1 callsites"
      pattern: "notify\\(ctx,\\s*pi,"
    - from: "extensions/pi-claude-marketplace/edge/register.ts"
      to: "extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts::makeMarketplaceUpdateHandler"
      via: "pi-as-first-positional-arg factory pattern (option-a)"
      pattern: "makeMarketplaceUpdateHandler\\(pi"
  coverage_constraints:
    - "Catalog UAT byte-equality MUST stay GREEN."
    - "`npm run check` GREEN at merge (atomic commit)."
    - "Zero V1 callers remain in update.ts after this plan."
    - "All 6 V1 callsites covered (NOT just the 5 listed in CONTEXT canonical-refs)."
---

<objective>
Migrate `extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts` from V1 severity-named wrappers (**6 callsites** per RESEARCH Risks #2: 220, 584, 586, 599, 631, 647) to V2 `notify(ctx, pi, NotificationMessage)`. Apply D-18-02 (DROP retry-hint suffix; `err.retryHint` stays internal) and D-18-03 (cascade cause-chain MOVES to per-plugin `PluginFailedMessage.cause`). Rewrite `outcomeToCascadeRow` (line 408) as `outcomeToCascadePluginMessage` returning discriminated `PluginNotificationMessage` payloads -- per-outcome mapping: `updated` -> `PluginUpdatedMessage{from,to,dependencies}`; `unchanged` -> `PluginSkippedMessage{reasons:["up-to-date"]}` (glyph flips ● -> ⊘ per RESEARCH Risks #5); `skipped` -> `PluginSkippedMessage{reasons:[narrowed]}`; `failed` -> `PluginFailedMessage{reasons:[narrowed],cause?}`. Complete the `pi`-required promotion started in Plan 18-00 by wiring `makeMarketplaceUpdateHandler(pi, deps)` through `edge/register.ts:87`. Update `tests/orchestrators/marketplace/update.test.ts` (22 tests) per RESEARCH §"Per-File Test Surface": ~6 byte flips + 3 retry-hint deletes + 2 glyph flips + 1 reload-hint contract change on autoupdate-OFF + 4 `outcomeToCascadeRow` test rewrites.

Purpose: deliver the update command surface in V2 grammar; restructure cascade cause-chains; eliminate the V1 retry-hint surface (no V2 representation per D-18-02); complete the update factory's pi-required wiring deferred from Plan 18-00.

Output: 4 modified files in one atomic commit. update.ts emits V2 `notify()` across all 6 callsites; the edge handler factory + register.ts wiring threads pi per the option-a pattern; tests assert byte-exact V2 outputs including the silent reload-hint contract change.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-CONTEXT.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-RESEARCH.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-PATTERNS.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-VALIDATION.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-00-SUMMARY.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-01-SUMMARY.md
@extensions/pi-claude-marketplace/shared/notify.ts
@docs/output-catalog.md
@tests/architecture/catalog-uat.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate update.ts -- all 6 V1 callsites -> V2 notify(); DROP retry-hint + cache-leak; rewrite outcomeToCascadeRow as outcomeToCascadePluginMessage; preserve composeErrorWithCauseChain import (still used in notes composition)</name>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts (FULL file -- 6 V1 callsites at 220, 584, 586, 599, 631, 647; UpdateMarketplaceOptions + UpdateAllMarketplacesOptions already promoted to required `readonly pi: ExtensionAPI` after Plan 18-00; `outcomeToCascadeRow` helper at line 408; `NULL_PROBE` at lines 138-141 (dead after pi-required); `cascadeAutoupdates` `outcome.notes` composition at line 337 (KEEP `composeErrorWithCauseChain` import per RESEARCH Risks #9)).
    - extensions/pi-claude-marketplace/shared/notify.ts (read lines 500-560 for PluginUpdatedMessage / PluginSkippedMessage / PluginFailedMessage shapes; the `PluginUpdatedMessage` carries `from`/`to`/`dependencies`; lines 700-740 for renderPluginRow arms -- including the ⊘ glyph for skipped per D-16-11 severity ladder; lines 1140-1180 for `notify(ctx, pi, message)`).
    - tests/architecture/catalog-uat.test.ts lines 1189-1233 (3 update fixtures: `autoupdate-off-manifest-refresh`, `mixed-outcomes`, `mp-failure-network` -- binding V2 byte forms).
    - tests/shared/notify-v2.test.ts (per-variant V2 byte assertions; cross-reference for `updated` (`from`/`to`/`dependencies`), `skipped` (`reasons`), `failed` (`reasons`+`cause?`) plugin rows).
    - extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts (read full file -- the `makeMarketplaceUpdateHandler(deps: EdgeDeps)` factory; needs option-a pi promotion per RESEARCH Risks #10).
    - extensions/pi-claude-marketplace/edge/register.ts (read line 87 + surrounding context -- `marketplaceUpdate: makeMarketplaceUpdateHandler(deps),` becomes `makeMarketplaceUpdateHandler(pi, deps),`).
    - 18-CONTEXT.md D-18-02 (retry-hint DROP), D-18-03 (per-plugin cascade cause-chain), D-18-06 (test discipline), D-16-08 + D-16-11 + D-16-12.
    - 18-RESEARCH.md "Per-File Callsite Inventory" → `update.ts` (lines 335-424; including the 6-callsite count clarification) + Risks #2 (6-callsite count drift), Risks #4 (autoupdate-OFF reload-hint silent change), Risks #5 (glyph flip), Risks #9 (composeErrorWithCauseChain KEEP), Risks #10 (factory option-a).
    - 18-PATTERNS.md "Pattern Assignments" → `orchestrators/marketplace/update.ts` (V1/V2 diff including outcomeToCascadePluginMessage rewrite).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts (Wave 1 pilot recipe block-comment).
  </read_first>
  <files>
    extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts,
    extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts,
    extensions/pi-claude-marketplace/edge/register.ts
  </files>
  <action>
    Operate in this order:

    1. Replace imports in `orchestrators/marketplace/update.ts` per RESEARCH §"Now-orphaned imports to drop (Plan 18-05)":
       - DROP: `softDepStatus` from `../../platform/pi-api.ts`; `cascadeSummary` from `../../presentation/cascade-summary.ts`; `renderRow` from `../../presentation/compact-line.ts`; `appendReloadHint, reloadHint` from `../../presentation/reload-hint.ts`; `composeVersionArrow` from `../../presentation/version-arrow.ts` (the V2 renderer composes the arrow inside `renderPluginRow`); `notifyError, notifySuccess, notifyWarning` from `../../shared/notify.ts`; type imports `MarketplaceRow, PluginCascadeRow, SoftDepProbe` from `../../presentation/compact-line.ts`.
       - DROP `NULL_PROBE` constant at lines 138-141 -- dead now that `opts.pi` is required.
       - KEEP `composeErrorWithCauseChain` import from `../../presentation/cause-chain.ts` (line 92) -- still used at line 337 inside `cascadeAutoupdates` for `outcome.notes` composition (non-notify consumer; RESEARCH Risks #9 + Open Question 3). Verify by reading the line-337 callsite.
       - ADD: `notify` from `../../shared/notify.ts`; `type PluginNotificationMessage, PluginFailedMessage, PluginUpdatedMessage, PluginSkippedMessage, PluginUninstalledMessage` (as needed) from same module. No `ExtensionAPI` add (already imported at line 117).

    2. Rewrite `outcomeToCascadeRow` (line 408) as `outcomeToCascadePluginMessage` (or keep the existing name but change its return type to `PluginNotificationMessage`). The function maps a single `PluginUpdateOutcome` to a discriminated `PluginNotificationMessage`:
       - `outcome.partition === "updated"` -> `{ status: "updated", name: outcome.name, from: outcome.fromVersion, to: outcome.toVersion, dependencies: outcome.dependencies ?? [] }` (verify the exact source fields against the existing `PluginUpdateOutcome` type; the `from`/`to`/`dependencies` field names come from `PluginUpdatedMessage`).
       - `outcome.partition === "unchanged"` -> `{ status: "skipped", name: outcome.name, reasons: ["up-to-date"] }`. **Glyph flip per RESEARCH Risks #5: V1 used ● for trivial-skip; V2 uses ⊘ (renderer-controlled by severity ladder).**
       - `outcome.partition === "skipped"` -> `{ status: "skipped", name: outcome.name, reasons: [<narrowReason(outcome.reason)>] }` (use the existing narrowing helper if present; otherwise narrow inline against the closed `Reason` set).
       - `outcome.partition === "failed"` -> `{ status: "failed", name: outcome.name, reasons: [<narrowed-from-cause>], ...(outcome.cause !== undefined && { cause: outcome.cause }) }` per D-18-03.

       If the existing helper has a corresponding test export (e.g., `__test_outcomeToCascadeRow`), rename/re-export it to `__test_outcomeToCascadePluginMessage` so Task 2's test rewrites can import the new symbol.

    3. Migrate the 6 V1 callsites per RESEARCH §"Per-File Callsite Inventory → update.ts":
       - **Line 220 (`updateAllMarketplaces` empty targets):** REPLACE the `notifySuccess(opts.ctx, renderRow({ kind: "empty", token: "no marketplaces" }, NULL_PROBE))` with `notify(opts.ctx, opts.pi, { marketplaces: [] })`. Renders `(no marketplaces)` per D-16-17.
       - **Lines 584 + 586 collapse (mp-level failure):** REPLACE both arms with a single `notify(ctx, pi, { marketplaces: [{ name, scope, status: "failed", plugins: [] }] })`. **DROP the retry-hint construction entirely per D-18-02** (the `\${errorMessage(err)}\\n\${err.retryHint}` form goes away; `err.retryHint` stays as an internal Error subclass field for programmatic inspection). No cause-chain trailer (V2 confines `cause?: Error` to plugin variants per D-16-08). The two branches `if (err instanceof MarketplaceUpdateError && err.retryHint !== "")` vs the else now produce the SAME byte output; the conditional can be removed.
       - **Line 599 (cache-cleanup leak):** DROP entirely per D-18-01 precedent. The cache-cleanup `rm()` still happens; only the user-facing `notifyWarning` disappears.
       - **Line 631 (autoupdate-OFF manifest-refresh):** REPLACE `notifySuccess(ctx, renderRow(headerRow, probe))` with `notify(ctx, pi, { marketplaces: [{ name, scope, status: "updated", plugins: [] }] })`. Reload-hint now fires per D-16-12 -- silent contract change vs V1 per RESEARCH Risks #4. Task 2 adds the missing reload-hint assertion.
       - **Line 647 (autoupdate-ON cascade via ternary dispatch):** REPLACE the `cascadeSummary + appendReloadHint + ternary dispatch` block with `notify(ctx, pi, { marketplaces: [{ name, scope, status: "updated", plugins: outcomes.map((o) => outcomeToCascadePluginMessage(o, scope)) }] })`. Severity computed by `notify()` (D-16-11 -- any failed -> error; otherwise warning if any skipped/manual recovery; otherwise info). Reload-hint computed by `notify()` (D-16-12 -- any plugin status in `{installed, updated, reinstalled, uninstalled}` fires).

    4. Drop dead local helpers / sort logic that the new construction obviates: the `changedNames = outcomes.filter(...).map(...)` accumulator + `hint = reloadHint(changedNames)` composition (lines around 643-645) becomes dead; the ternary `dispatch = severity === "warning" ? notifyWarning : notifySuccess` becomes dead.

    5. KEEP unchanged: `composeErrorWithCauseChain` usage at line 337 inside `cascadeAutoupdates` (RESEARCH Open Question 3 -- `outcome.notes` still feeds non-notify consumers).

    6. Add a brief comment near the new `notify()` calls referencing the Wave 1 pilot recipe (e.g., `// V2 cascade per D-18-03 -- per-plugin PluginFailedMessage.cause / PluginUpdatedMessage{from,to,dependencies} / PluginSkippedMessage; see add.ts (Wave 1 pilot) for the construction pattern.`).

    7. Update `edge/handlers/marketplace/update.ts` per RESEARCH Risks #10 option-a: change the factory signature from `makeMarketplaceUpdateHandler(deps: EdgeDeps)` to `makeMarketplaceUpdateHandler(pi: ExtensionAPI, deps: EdgeDeps)`. Add `import type { ExtensionAPI } from "../../../platform/pi-api.ts";`. Inside the returned closure, thread `pi` into BOTH the `updateMarketplace({...})` and `updateAllMarketplaces({...})` calls (whichever branch the handler hits). Pi field placement: directly after `ctx`. This matches the Plan 18-00 pi-as-first-arg pattern for siblings.

    8. Update `edge/register.ts` at line 87: change `marketplaceUpdate: makeMarketplaceUpdateHandler(deps),` to `marketplaceUpdate: makeMarketplaceUpdateHandler(pi, deps),`. `pi` is already in scope at the surrounding `registerExtension` function (verified during Plan 18-00).

    After this task: `grep -c "notifySuccess\\|notifyWarning\\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts` returns 0; `grep -c "from \"../../presentation/" extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts` returns 1 (only `composeErrorWithCauseChain` from `cause-chain` remains).

    Honors D-18-02 (retry-hint DROP), D-18-03 (per-plugin cascade), D-18-06, D-18-09-amendment (6-callsite count), D-16-08/11/12, RESEARCH Risks #1/2/4/5/9/10.
  </action>
  <verify>
    <automated>node --test --import tsx tests/orchestrators/marketplace/update.test.ts</automated>
  </verify>
  <done>
    update.ts compiles strict; all 6 V1 callers gone; presentation imports trimmed (composeErrorWithCauseChain retained per Risks #9); NULL_PROBE deleted; outcomeToCascadePluginMessage replaces outcomeToCascadeRow; the cache-leak + retry-hint surfaces are dropped; the autoupdate-OFF arm now emits the reload-hint via notify(). The edge handler factory accepts `pi` as first arg; register.ts line 87 wires it. Task 2 lands the test updates that unlock this verify in the same atomic commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update update.test.ts -- 6 byte flips + 3 retry-hint deletes + 2 glyph flips + reload-hint add on autoupdate-OFF + outcomeToCascadePluginMessage tests</name>
  <read_first>
    - tests/orchestrators/marketplace/update.test.ts (FULL file -- 22 tests; assertions at lines 135, 136, 163, 271, 291, 328, 514-526, 567, 603, 802, 842, 879 per RESEARCH §"Per-File Test Surface"; the `__test_outcomeToCascadeRow` tests at lines 692-757 need rewriting; the existing makeCtx pattern; `pi` thread done in Plan 18-00 Task 3).
    - 18-RESEARCH.md "Per-File Test Surface" → update.test.ts table (lines 533-557 with 6 byte flips + 3 retry-hint deletes + 2 glyph flips + 1 reload-hint add + outcomeToCascadeRow rewrites).
    - 18-PATTERNS.md "tests/orchestrators/marketplace/update.test.ts" section.
    - tests/architecture/catalog-uat.test.ts (lines 1189-1233 -- binding V2 byte forms for the 3 update states).
    - tests/shared/notify-v2.test.ts (per-variant V2 byte assertions -- especially line 267 for the version-arrow form `0.5.0 → v1.0.0`).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts (post-Task-1 state, including the new outcomeToCascadePluginMessage and the __test_ export).
  </read_first>
  <files>tests/orchestrators/marketplace/update.test.ts</files>
  <action>
    Apply the V1 -> V2 assertion changes per RESEARCH §"Per-File Test Surface":

    1. Line 135 (empty targets sentinel): `first.message === "(no marketplaces)"` -- KEEP unchanged (V2 D-16-17 sentinel matches).

    2. Line 136 (empty targets no reload-hint): `first.message.includes("/reload to pick up changes") === false` -- KEEP unchanged (empty mps -> no reload-hint).

    3. Line 163 (autoupdate-OFF manifest-refresh, MU-4 + D-14): currently asserts only `assert.notEqual(first.severity, "error")` -- KEEP that assertion AND ADD:
       - `assert.equal(first.severity, undefined)` (info severity).
       - `assert.match(first.message, /\\/reload to pick up changes/)` -- silent contract change per RESEARCH Risks #4 (V2 catalog `autoupdate-off-manifest-refresh` at `docs/output-catalog.md:803-806` emits the reload-hint; V1 did not).
       - Optionally also strengthen the message assertion to the exact byte: `assert.equal(first.message, "● <name> [<scope>] (updated)\\n\\n/reload to pick up changes")` derived from the test's seeded marketplace name + scope.

    4. Line 271 (mp-failure with retry-hint regex): DELETE the `=~ /Retry the command\\./` assertion AND replace with byte assertion: `assert.equal(first.message, "⊘ rewritten [project] (failed)")` (the test's seeded mp name is `rewritten`, scope `project`; verify by reading the test context). D-18-02 drops the retry-hint.

    5. Line 291 (mp-failure without retry-hint regex): DELETE the `includes("Retry the command.") === false` assertion AND replace with byte assertion: `assert.equal(first.message, "⊘ offline [project] (failed)")`.

    6. Line 328 (third mp-failure retry-hint regex): DELETE the retry-anchor assertion AND flip to V2 bare-failed byte string per the line-271 pattern. Verify the test's seeded mp name + scope to derive the correct byte.

    7. Lines 514-517 (cascade outcome glyph flips per RESEARCH Risks #5):
       - Line 514 (the `updated` row currently asserted as `"  ● a [project]"`): rewrite to the V2 byte form derived from `tests/shared/notify-v2.test.ts:267` (authoritative for the version-arrow form). Example shape: `"  ● alpha 0.5.0 → v1.0.0 (updated)"` -- but the test's seeded outcome name (`a`) and from/to versions may differ; verify the seeded test fixtures and construct the exact byte. The shape is `  ● <name> <from> → v<to> (updated)` with two-space indent (renderPluginRow), `●` glyph (info severity for updated), version-arrow rendered by V2.
       - Line 515 (`"  ● b [project]"` was V1 `unchanged` -> trivial-skip ●): change to `"  ⊘ b [project]"` -- BUT also verify the full V2 byte form is `"  ⊘ <name> (skipped) {up-to-date}"` per catalog UAT `mixed-outcomes` at `docs/output-catalog.md:818`; the existing assertion appears to be a substring `indexOf("...")` check rather than a full equality. Adjust accordingly: either flip the substring to `"  ⊘ b [project]"` OR rewrite to use the full byte derived from the catalog.
       - Line 516 (`"  ● c [project]"` was V1 `unchanged` -- same as line 515 b): change to `"  ⊘ c [project]"`.
       - Line 517 (`"  ⊘ d [project]"` was V1 `failed` -- ⊘ glyph already): KEEP unchanged (⊘ glyph matches V2).

    8. Lines 522-526 (partition-header negative assertions for `"Updated:"`, `"Unchanged:"`, etc.): KEEP unchanged -- V2 never emits partition headers.

    9. Line 567 (cascade ends with reload-hint): KEEP `first.message =~ /\\/reload to pick up changes$/` unchanged.

    10. Line 603 (cascade all-unchanged no reload-hint): KEEP `first.message.includes("/reload to pick up changes") === false` unchanged. V2 cascade where all plugins are `skipped {up-to-date}` does NOT fire reload-hint (no plugin status in the `{installed, updated, reinstalled, uninstalled}` set; the mp.status `"updated"` itself does NOT fire reload-hint unless plugin-level state changed -- VERIFY by reading D-16-12; if the orchestrator now sets `mp.status: "updated"` even on all-unchanged cascades, the V2 contract IS to fire reload-hint and this test ALSO flips). RESEARCH §"Per-File Test Surface" calls out KEEP for line 603, so trust the research call. If verify reveals contradiction, escalate.

    11. Lines 802, 842, 879 (per-plugin cause regex assertions): KEEP all 3 unchanged -- the regexes `/alpha[^\\n]*\\(failed\\)[^\\n]*\\{permission denied\\}/` (similar for source-missing + unreadable-manifest) match V2 form.

    12. `__test_outcomeToCascadeRow` unit tests at lines 692-757 -- REWRITE all 4 tests to assert against the new `__test_outcomeToCascadePluginMessage` helper (or whatever symbol Task 1 exports). The tests now construct `PluginNotificationMessage` outputs and assert on discriminated `status` literals + per-status fields:
       - `updated` outcome -> assert returned message has `status === "updated"`, `from === <seeded-from>`, `to === <seeded-to>`, `dependencies` field present (array).
       - `unchanged` outcome -> assert `status === "skipped"`, `reasons.includes("up-to-date")`.
       - `skipped` outcome -> assert `status === "skipped"`, `reasons` contains the narrowed reason.
       - `failed` outcome -> assert `status === "failed"`, `reasons` contains the narrowed reason, `cause === <seeded-cause>` (if applicable).

    13. KEEP unchanged: D-03-INV cache-invalidation test at line 644; all `state.cloneCalls` / `state.fetchCalls` / git-mock assertions; the `__test_narrowCascadeFailure` tests if any exist (unchanged helper).

    14. `pi` plumbing through `updateMarketplace({...})` and `updateAllMarketplaces({...})` calls: done in Plan 18-00 Task 3 (since `pi?` existed there as optional; promoting to required only required appending `pi: ctx.pi cast`). Verify; if any call site is missing, fix inline.

    Honors D-18-02 (retry-hint DROP), D-18-03 (per-plugin cascade structure), D-18-06 (byte-exact V2 assertions through real notify() via makeCtx), RESEARCH Risks #4 (autoupdate-OFF reload-hint add), Risks #5 (glyph flip).
  </action>
  <verify>
    <automated>node --test --import tsx tests/orchestrators/marketplace/update.test.ts &amp;&amp; node --test --import tsx tests/architecture/catalog-uat.test.ts</automated>
  </verify>
  <done>
    update.test.ts: all 22 tests pass against the migrated update.ts; 6 byte-string flips + 3 retry-hint deletes + 2 glyph flips (lines 515/516) + 1 reload-hint inclusion add (line 163) + 4 `outcomeToCascadePluginMessage` test rewrites complete. Catalog UAT stays GREEN. `npm run check` GREEN in the atomic commit. `grep -c "notifySuccess\\|notifyWarning\\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts` returns 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal API refactor; byte output governed by Phase 17 catalog (3 update fixtures: autoupdate-off-manifest-refresh, mixed-outcomes, mp-failure-network). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05-01 | (none) | V1->V2 migration of update.ts | accept | No new threats -- internal API refactor; byte output governed by Phase 17 catalog; retry-hint stays internal to MarketplaceUpdateError (programmatic-only access, no security implication); per-plugin cause-chain at 4-space indent below the plugin row (D-16-08). |
| T-18-05-02 | Information Disclosure (none new) | Retry-hint DROP per D-18-02 | accept | The retry-hint suffix held no sensitive data; it was a generic "Retry the command." anchor. Removing it does not change information exposure posture. |
</threat_model>

<verification>
- `npm run check` GREEN after the atomic commit.
- Catalog UAT byte-equality GREEN end-to-end.
- `grep -r "notifySuccess\|notifyWarning\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts` returns 0.
- `grep -c "from \"../../presentation/" extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts` returns 1 (only `cause-chain` `composeErrorWithCauseChain` per Risks #9).
- `grep -c "makeMarketplaceUpdateHandler(pi" extensions/pi-claude-marketplace/edge/register.ts` returns 1.
- Retry-hint assertions deleted (3 sites: 271, 291, 328).
- Glyph flip applied (lines 515-516: ● -> ⊘ on skipped/unchanged cascade rows).
- Reload-hint inclusion assertion added on autoupdate-OFF manifest-refresh (line 163).
</verification>

<success_criteria>
- update.ts emits V2 `notify(ctx, pi, ...)` calls across all 6 V1 callsites.
- D-18-02: retry-hint suffix dropped; `err.retryHint` stays internal.
- D-18-03: cascade restructured with per-plugin `PluginFailedMessage.cause` and the new `outcomeToCascadePluginMessage` helper.
- The edge handler factory `makeMarketplaceUpdateHandler(pi, deps)` and register.ts wiring complete the option-a pi promotion deferred from Plan 18-00.
- update.test.ts: byte assertions + retry-hint deletes + glyph flips + reload-hint contract change + 4 helper-test rewrites.
- `npm run check` + catalog UAT GREEN.
</success_criteria>

<output>
Create `.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-05-SUMMARY.md` documenting:
- The 4 files modified.
- The 6-callsite count clarification (CONTEXT said 5; research-verified 6; line 220 is the additional one).
- The retry-hint DROP per D-18-02 and the cascade restructure per D-18-03.
- The silent reload-hint contract change on autoupdate-OFF (Risks #4) and the glyph flip on skipped cascade rows (Risks #5).
- The factory pi-promotion (option-a) for `makeMarketplaceUpdateHandler`.
- The fate of `composeErrorWithCauseChain` (KEPT per Risks #9).
- Confirmation that catalog UAT + `npm run check` both pass.
</output>
