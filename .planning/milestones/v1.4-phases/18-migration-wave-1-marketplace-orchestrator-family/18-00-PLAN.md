---
phase: 18-migration-wave-1-marketplace-orchestrator-family
plan: 0
type: execute
wave: 0
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/autoupdate.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/list.ts
  - extensions/pi-claude-marketplace/edge/register.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-18-08-amendment: Wave 0 lands first as a single atomic plumbing-only commit; threads `pi: ExtensionAPI` through the 3 marketplace orchestrators that don't currently accept it (`add.ts`, `autoupdate.ts`, `list.ts`) and through `update.ts` by making `pi?` REQUIRED. No V1->V2 notify migration in this plan."
    - "D-18-09-amendment: Wave 0 is a single-plan wave that materially shrinks every downstream migration plan and removes `register.ts` as a shared-file hotspot from Wave 2 parallelism."
    - "Behavioral neutrality: `npm run check` stays GREEN before AND after this plan -- no user-visible behavior change; the only changes are signature/wiring shapes."
    - "After Plan 18-00, every marketplace orchestrator's `*Options` interface carries `readonly pi: ExtensionAPI` non-optional; every `edge/handlers/marketplace/<file>.ts` handler factory accepts `pi` as its first positional argument (matching `makeRemoveHandler(pi)` precedent at `edge/register.ts:85`)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts"
      provides: "AddMarketplaceOptions extended with `readonly pi: ExtensionAPI`"
      contains: "readonly pi: ExtensionAPI"
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts"
      provides: "AutoupdateOptions extended with `readonly pi: ExtensionAPI`"
      contains: "readonly pi: ExtensionAPI"
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts"
      provides: "ListMarketplacesOptions extended with `readonly pi: ExtensionAPI`"
      contains: "readonly pi: ExtensionAPI"
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts"
      provides: "UpdateMarketplaceOptions / UpdateAllMarketplacesOptions promoted from `pi?` optional to `pi` required"
      contains: "readonly pi: ExtensionAPI"
    - path: "extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts"
      provides: "makeAddHandler(pi, deps) factory signature"
      contains: "makeAddHandler"
    - path: "extensions/pi-claude-marketplace/edge/handlers/marketplace/autoupdate.ts"
      provides: "makeAutoupdateHandler(pi, enable) factory signature"
      contains: "makeAutoupdateHandler"
    - path: "extensions/pi-claude-marketplace/edge/handlers/marketplace/list.ts"
      provides: "makeMarketplaceListHandler(pi) factory replacing the plain handleMarketplaceList function"
      contains: "makeMarketplaceListHandler"
    - path: "extensions/pi-claude-marketplace/edge/register.ts"
      provides: "Updated wiring for marketplaceAdd, marketplaceList, marketplaceAutoupdate, marketplaceNoautoupdate threading `pi`"
      contains: "makeAddHandler(pi"
  key_links:
    - from: "extensions/pi-claude-marketplace/edge/register.ts"
      to: "extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts::makeAddHandler"
      via: "pi as first positional arg"
      pattern: "makeAddHandler\\(pi"
    - from: "extensions/pi-claude-marketplace/edge/register.ts"
      to: "extensions/pi-claude-marketplace/edge/handlers/marketplace/autoupdate.ts::makeAutoupdateHandler"
      via: "pi as first positional arg"
      pattern: "makeAutoupdateHandler\\(pi"
    - from: "extensions/pi-claude-marketplace/edge/register.ts"
      to: "extensions/pi-claude-marketplace/edge/handlers/marketplace/list.ts::makeMarketplaceListHandler"
      via: "factory taking pi"
      pattern: "makeMarketplaceListHandler\\(pi\\)"
  coverage_constraints:
    - "`npm run check` MUST be GREEN before AND after this plan."
    - "No user-visible behavior changes; pure signature plumbing."
    - "No test byte-string assertions change; tests that already call orchestrators continue to compile and pass (existing makeCtx pattern provides ctx.pi already; tests must thread it through the orchestrator call sites where currently absent)."
---

<objective>
Pre-thread `pi: ExtensionAPI` through every marketplace orchestrator's `*Options` interface, every `edge/handlers/marketplace/<file>.ts` handler factory, and the wiring in `extensions/pi-claude-marketplace/edge/register.ts`. This Wave 0 plan lands BEFORE Wave 1 begins and is a pure plumbing change with no V1->V2 notify migration. It exists per the D-18-08 amendment (2026-05-26) to remove `register.ts` as a shared-file hotspot from Wave 2 parallelism and to keep each subsequent migration plan surgical (one orchestrator + its tests, no plumbing churn).

Purpose: enable Wave 1/2 plans to swap V1 wrappers for V2 `notify(ctx, pi, message)` calls without touching shared edge wiring. Matches Phase 16's "land infrastructure first" precedent.

Output: 7 modified files (3 orchestrators + 3 edge handlers + 1 register wiring) -- and 1 orchestrator (`update.ts`) whose existing optional `pi?` is promoted to required `pi`. `npm run check` GREEN throughout.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-CONTEXT.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-RESEARCH.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-PATTERNS.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-VALIDATION.md
@extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts
@extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts
@extensions/pi-claude-marketplace/edge/register.ts
@extensions/pi-claude-marketplace/platform/pi-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread `pi: ExtensionAPI` through the 3 marketplace orchestrator Options interfaces (add, autoupdate, list) + promote update.ts pi? to required</name>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts (full file -- the AddMarketplaceOptions interface lives around lines 75-85; the file currently has NO pi field).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts (full file -- the AutoupdateOptions interface lives around lines 55-65; the file currently has NO pi field).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts (full file -- the ListMarketplacesOptions interface lives around lines 32-38; the file currently has NO pi field).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts (read lines 80-180 -- UpdateMarketplaceOptions ~line 143 and UpdateAllMarketplacesOptions ~line 164 both currently declare `readonly pi?: ExtensionAPI` optional; promote to required).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts (read lines 100-120 -- THE reference pattern for `readonly pi: ExtensionAPI`; mirror exactly).
    - extensions/pi-claude-marketplace/platform/pi-api.ts (verify the `ExtensionAPI` export is at line 19; both names re-exported alongside `ExtensionContext`).
    - 18-PATTERNS.md "Pattern Assignments" sections for add.ts, autoupdate.ts, list.ts, update.ts -- specifically the "`pi` plumbing" subsections and "Imports to add" lists.
    - 18-RESEARCH.md Per-File Callsite Inventory + Risks #1 (pi plumbing) + Risks #10 (makeAutoupdateHandler signature shape).
  </read_first>
  <files>
    extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts,
    extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts,
    extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts,
    extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  </files>
  <action>
    Per the D-18-08 amendment Wave 0 plumbing contract:

    1. In `orchestrators/marketplace/add.ts`: add a new `import type { ExtensionAPI } from "../../platform/pi-api.ts";` import (place adjacent to the existing `ExtensionContext` import). Insert a new field on `AddMarketplaceOptions`: `readonly pi: ExtensionAPI;` directly after the `readonly ctx: ExtensionContext;` line (mirror the placement seen in `remove.ts` per the analog). Do NOT touch any notify call sites in this task -- only the interface and the import.

    2. In `orchestrators/marketplace/autoupdate.ts`: same shape -- add the `ExtensionAPI` import + `readonly pi: ExtensionAPI;` field on `AutoupdateOptions` directly after `readonly ctx: ExtensionContext;`.

    3. In `orchestrators/marketplace/list.ts`: same shape -- add the `ExtensionAPI` import + `readonly pi: ExtensionAPI;` field on `ListMarketplacesOptions` directly after `readonly ctx: ExtensionContext;`.

    4. In `orchestrators/marketplace/update.ts`: PROMOTE the existing `readonly pi?: ExtensionAPI;` fields on BOTH `UpdateMarketplaceOptions` and `UpdateAllMarketplacesOptions` to non-optional (drop the `?`). Leave the existing `ExtensionAPI` import in place. Do NOT touch the `NULL_PROBE` constant in this task (Plan 18-05 will delete it during the V1->V2 migration once the optional-fallback codepath is dead).

    No notify call sites are touched here. The file MUST still compile after this task -- any internal `opts.pi !== undefined ? ... : NULL_PROBE` ternary inside update.ts continues to work because requiring `opts.pi` is a strict superset of "optional pi might be defined". The TypeScript compiler will not complain about a narrowing-to-required.

    Honors D-18-08-amendment (Wave 0 split rationale) and Risks #1 (shared-file conflict mitigation).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    All 4 orchestrator files compile under `tsc --noEmit` strict mode; the 3 previously pi-less interfaces (`AddMarketplaceOptions`, `AutoupdateOptions`, `ListMarketplacesOptions`) now declare `readonly pi: ExtensionAPI`; `UpdateMarketplaceOptions` and `UpdateAllMarketplacesOptions` declare `readonly pi: ExtensionAPI` (non-optional); `grep -c "readonly pi: ExtensionAPI" extensions/pi-claude-marketplace/orchestrators/marketplace/{add,autoupdate,list}.ts` returns 1 per file; `grep -c "readonly pi\\?: ExtensionAPI" extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts` returns 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update the 3 marketplace edge handler factories (add, autoupdate, list) to accept `pi: ExtensionAPI` and thread it; rewire register.ts</name>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts (full file -- factory currently `makeAddHandler(deps)`; the `await addMarketplace({...})` body call needs a `pi` field added).
    - extensions/pi-claude-marketplace/edge/handlers/marketplace/autoupdate.ts (full file -- factory currently `makeAutoupdateHandler(enable: boolean)`; option-a per RESEARCH Risks #10 is `makeAutoupdateHandler(pi: ExtensionAPI, enable: boolean)`).
    - extensions/pi-claude-marketplace/edge/handlers/marketplace/list.ts (full file -- currently a plain function `handleMarketplaceList(args, ctx)`; convert to factory `makeMarketplaceListHandler(pi)`).
    - extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts (read lines 1-60 -- THE reference factory shape; pi is the first positional arg).
    - extensions/pi-claude-marketplace/edge/register.ts (full file -- lines around 50, 84, 86, 88-89 -- the 5 marketplace handler wiring lines).
    - 18-PATTERNS.md sections for `edge/handlers/marketplace/{add,autoupdate,list}.ts` + the `register.ts` analog section.
    - 18-RESEARCH.md Risks #1 + Risks #10 (factory signature option-a).
  </read_first>
  <files>
    extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts,
    extensions/pi-claude-marketplace/edge/handlers/marketplace/autoupdate.ts,
    extensions/pi-claude-marketplace/edge/handlers/marketplace/list.ts,
    extensions/pi-claude-marketplace/edge/register.ts
  </files>
  <action>
    Per RESEARCH Risks #10 option-a (pi-as-first-positional-arg matches the established sibling factory convention `makeRemoveHandler(pi)` and `makeInstallHandler(pi)`):

    1. `edge/handlers/marketplace/add.ts`: change the exported factory signature from `makeAddHandler(deps: EdgeDeps)` to `makeAddHandler(pi: ExtensionAPI, deps: EdgeDeps)`. Add `import type { ExtensionAPI } from "../../../platform/pi-api.ts";`. Inside the returned async closure, pass `pi` into the `addMarketplace({...})` call as an extra field directly after `ctx`. Mirror the placement seen in `edge/handlers/marketplace/remove.ts` per the analog.

    2. `edge/handlers/marketplace/autoupdate.ts`: change the factory signature from `makeAutoupdateHandler(enable: boolean)` to `makeAutoupdateHandler(pi: ExtensionAPI, enable: boolean)` (option-a per Risks #10). Add the `ExtensionAPI` import. Inside the body, pass `pi` into `setMarketplaceAutoupdate({...})` directly after `ctx`.

    3. `edge/handlers/marketplace/list.ts`: convert the plain exported `handleMarketplaceList(args, ctx)` function into a factory `makeMarketplaceListHandler(pi: ExtensionAPI)` that returns the same async `(args, ctx) => Promise<void>` shape. Add the `ExtensionAPI` import. Inside the returned closure, pass `pi` into `listMarketplaces({...})` directly after `ctx`. The previous `handleMarketplaceList` symbol disappears; verify by grep that no test imports it (search `grep -r "handleMarketplaceList" tests/ extensions/` and fix any importers). Naming convention follows the `makeXHandler` sibling pattern.

    4. `edge/register.ts`: update the 4 marketplace wire lines:
       - `marketplaceAdd: makeAddHandler(deps),` becomes `marketplaceAdd: makeAddHandler(pi, deps),`
       - `marketplaceList: handleMarketplaceList,` becomes `marketplaceList: makeMarketplaceListHandler(pi),` -- update the import at the top of the file from `import { handleMarketplaceList } from "..."` to `import { makeMarketplaceListHandler } from "..."`.
       - `marketplaceAutoupdate: makeAutoupdateHandler(true),` becomes `marketplaceAutoupdate: makeAutoupdateHandler(pi, true),`
       - `marketplaceNoautoupdate: makeAutoupdateHandler(false),` becomes `marketplaceNoautoupdate: makeAutoupdateHandler(pi, false),`

    Plan 18-00 does NOT touch `marketplaceUpdate: makeMarketplaceUpdateHandler(deps),` (line 87) -- update.ts already declares `pi?` optional and Plan 18-05 owns the full make-it-required + wiring decision (Risks #10 option-a or option-b on the update factory). Plan 18-00's job is the 3-file pi-introduction (add, autoupdate, list) plus update.ts's interface promotion done in Task 1.

    `pi` is already in scope at `register.ts:75` (the surrounding `registerExtension` receives `pi: ExtensionAPI` as a parameter; verify by reading the function signature).

    No notify call sites are touched here. After this task, all 5 marketplace orchestrators receive a non-optional `pi` from their edge handler; no callsite that previously omitted `pi` will compile.

    Honors D-18-08-amendment, RESEARCH Risks #1 (mitigates register.ts merge conflicts by serializing the touch through this Wave 0 plan), RESEARCH Risks #10 (option-a chosen for autoupdate factory).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    All 3 edge handler factories accept `pi` as their first positional arg; `grep -c "makeAddHandler(pi" extensions/pi-claude-marketplace/edge/register.ts` returns 1; `grep -c "makeAutoupdateHandler(pi" extensions/pi-claude-marketplace/edge/register.ts` returns 2; `grep -c "makeMarketplaceListHandler(pi)" extensions/pi-claude-marketplace/edge/register.ts` returns 1; `grep -c "handleMarketplaceList" extensions/pi-claude-marketplace/` (excluding the deleted handler file's own removal) returns 0; `npm run check` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Thread `pi` into orchestrator test call sites where currently absent (compile fix)</name>
  <read_first>
    - tests/orchestrators/marketplace/add.test.ts (read lines 1-50 + every `addMarketplace({` call site -- the existing `makeCtx()` at lines 24-40 already creates `pi: { getAllTools: () => [] }` on the mock ctx; the addMarketplace calls do NOT currently pass `pi`).
    - tests/orchestrators/marketplace/autoupdate.test.ts (read lines 1-50 + every `setMarketplaceAutoupdate({` call site).
    - tests/orchestrators/marketplace/list.test.ts (read lines 1-50 + every `listMarketplaces({` call site).
    - tests/orchestrators/marketplace/update.test.ts (read lines 1-50 + every `updateMarketplace({` and `updateAllMarketplaces({` call site -- since update.ts already had `pi?` optional, some sites may already pass it; others may not; verify both code paths compile after Task 1's promotion to required).
    - 18-PATTERNS.md `tests/orchestrators/marketplace/add.test.ts` section ("Recommended pattern: add `pi: ctx.pi`" -- the explicit code excerpt at line 805).
  </read_first>
  <files>
    tests/orchestrators/marketplace/add.test.ts,
    tests/orchestrators/marketplace/autoupdate.test.ts,
    tests/orchestrators/marketplace/list.test.ts,
    tests/orchestrators/marketplace/update.test.ts
  </files>
  <action>
    In each of the 4 test files (NOT remove.test.ts -- remove.ts already takes pi; tests already thread it):

    For every call site that currently invokes the corresponding orchestrator entry point (e.g., `addMarketplace({...})`, `setMarketplaceAutoupdate({...})`, `listMarketplaces({...})`, `updateMarketplace({...})`, `updateAllMarketplaces({...})`) and does NOT currently pass `pi`, ADD a `pi: (ctx as unknown as { pi: ExtensionAPI }).pi,` field directly after `ctx,` -- this preserves the existing `makeCtx()` mock pi shape (`{ getAllTools: () => [] }`) and casts it through the type system. Add an `import type { ExtensionAPI } from "../../../extensions/pi-claude-marketplace/platform/pi-api.ts";` to the test file if not already present (verify by grep first).

    Do NOT change any byte-string assertions, severity assertions, or test names in this task. Do NOT add new tests. The goal is purely "make the tests compile under the promoted-required interface". The byte-string flips happen in Plans 18-01 through 18-05.

    Soft-dep edge cases (tests that already supply a non-empty `getAllTools()` to flip the probe outcome) are left untouched -- their existing `pi` reference shape continues to work.

    After this task, `node --test --import tsx tests/orchestrators/marketplace/<file>.test.ts` for each of the 4 files passes WITHOUT any byte-string changes -- the existing V1 assertions still hold because Plan 18-00 doesn't migrate any notify calls; orchestrators still call V1 wrappers and emit V1 strings.

    Honors D-18-06 (existing `makeCtx()` pattern preserved verbatim) and the D-18-08-amendment (Wave 0 introduces no behavior change).
  </action>
  <verify>
    <automated>node --test --import tsx tests/orchestrators/marketplace/add.test.ts tests/orchestrators/marketplace/autoupdate.test.ts tests/orchestrators/marketplace/list.test.ts tests/orchestrators/marketplace/update.test.ts</automated>
  </verify>
  <done>
    All 4 orchestrator test files compile and pass; `grep -c "pi: (ctx as unknown" tests/orchestrators/marketplace/add.test.ts` is at least equal to the count of `addMarketplace({` invocations in that file (each call site receives the new `pi` field); equivalent invariant holds for autoupdate.test.ts, list.test.ts, update.test.ts; no byte-string assertion flips made in this task; full `npm run check` exits 0 -- a final pre-merge gate confirming the Wave 0 plumbing has zero behavioral effect.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 18 is an internal API refactor; no new external input crosses any boundary. The V1 severity-named wrappers and V2 `notify()` both flow through the same `ctx.ui.notify` host channel (IL-2 invariant unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-00-01 | (none) | Plan 18-00 plumbing changes | accept | No new threats -- this plan is a signature-only refactor; no auth, session, input validation, or cryptography changes; no new attack surface. The byte output is governed by Phase 17 catalog UAT and is unchanged in Plan 18-00 because no notify call sites migrate in this plan. Per RESEARCH.md "Security Domain": skipped (no new attack surface introduced). |
</threat_model>

<verification>
- `npm run check` exits 0 after every task commit (typecheck + ESLint + Prettier + node --test).
- `grep -r "notifySuccess\|notifyWarning\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/` STILL returns the existing V1 callers (this plan does NOT migrate them; that's Plans 18-01..05).
- The 4 modified test files pass `node --test --import tsx <file>.test.ts` with NO byte-string changes -- pure compile-fix per the promoted-required interface.
</verification>

<success_criteria>
- All 8 files modified per the task list compile and the test suite passes.
- The 3 previously pi-less orchestrator `*Options` interfaces (add, autoupdate, list) now declare non-optional `readonly pi: ExtensionAPI`.
- `update.ts`'s 2 Options interfaces have `pi?` promoted to required `pi`.
- The 3 edge handler factories (add, autoupdate, list) accept `pi: ExtensionAPI` as the first positional arg, matching the `makeRemoveHandler(pi)` precedent.
- `edge/register.ts` wires `pi` into 4 marketplace handler factory call sites (add, list, autoupdate, noautoupdate); marketplaceRemove unchanged; marketplaceUpdate unchanged (deferred to Plan 18-05).
- `npm run check` GREEN.
- No catalog UAT byte changes; no orchestrator notify call site migrations.
</success_criteria>

<output>
Create `.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-00-SUMMARY.md` when done documenting:
- The 8 files touched + the line-level summary of pi additions / factory signature changes / register.ts wiring updates.
- Confirmation that `npm run check` is GREEN.
- Confirmation that no notify call sites changed (V1 wrappers still emit V1 strings until Plans 18-01..05).
- Any deviations from the recommended option-a factory signature shape (RESEARCH Risks #10).
</output>
