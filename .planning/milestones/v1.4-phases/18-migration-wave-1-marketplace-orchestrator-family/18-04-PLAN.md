---
phase: 18-migration-wave-1-marketplace-orchestrator-family
plan: 4
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts
  - tests/orchestrators/marketplace/remove.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-18-01: DROP cleanup-leak `notifyWarning` entirely (no V2 representation); the underlying `rm()` calls stay (correctness preserved); the user-facing warning disappears. Lines 299 + 354 V1 callsites are removed."
    - "D-18-03: cascade cause-chain MOVES from marketplace-level body to per-plugin `PluginFailedMessage.cause`; renders at 4-space indent via `renderPluginRow` per D-16-08."
    - "D-18-06: tests stay END-TO-END through real `notify()` via `makeCtx()`; byte-exact V2 string assertions replace V1; existing makeCtx pattern preserved."
    - "RESEARCH Risks #7 (contract-name drift): rename the line-232 test from `\"MR-2 + MR-8 (RH-1): empty marketplace removed cleanly emits success WITHOUT reload hint\"` to `\"MR-2 + V2 D-16-12: empty marketplace removed cleanly emits success WITH mp-level reload-hint\"` AND flip the reload-hint assertion from FALSE to TRUE."
    - "Wave 1 pilot (Plan 18-01) recipe followed: payload construction mirrors the add.ts pattern; differences are in cascade restructure (per-plugin `PluginUninstalledMessage` + `PluginFailedMessage{cause?}`)."
  byte_contracts:
    - "MR clean removal (line 422 replacement): `note.message === \"● <name> [<scope>] (removed)\\n\\n/reload to pick up changes\"` -- catalog UAT fixture `clean` at tests/architecture/catalog-uat.test.ts:1155-1160. Reload-hint fires from mp.status `\"removed\"` per D-16-12 (contract change vs V1: V1 emitted NO reload-hint on empty-marketplace removal; V2 DOES per RESEARCH Risks #7)."
    - "MR partial cascade (line 407 replacement) per D-18-03: payload `{ marketplaces: [{ name, scope, status: \"failed\", plugins: [...uninstalled, ...failed] }] }` -- catalog UAT fixture `partial` at lines 1162-1183 demonstrates the 4-space-indent per-plugin cause-chain. Severity = error (any plugin/mp failed per D-16-11). Reload-hint fires because at least one plugin status is in `{uninstalled}` per D-16-12. The V1 `RETRY_ANCHOR` trailer (`\"Fix the underlying issue and retry.\"`) is DROPPED per `docs/output-catalog.md:791` (already excluded by D-17-09)."
    - "MR cleanup-leak lines 299 + 354: DROPPED entirely per D-18-01; no V2 byte output, no second notify() call."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts"
      provides: "V2 notify() covering clean + partial cascade; cleanup-leak warnings dropped; cascade restructure per D-18-03"
      contains: "notify(opts.ctx, opts.pi"
    - path: "tests/orchestrators/marketplace/remove.test.ts"
      provides: "Byte-exact V2 assertions; line-232 test renamed + reload-hint flip; cascade severity = error; retry-anchor assertion deleted"
      contains: "(removed)\\n\\n/reload to pick up changes"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts::notify (clean + partial cascade arms)"
      via: "V2 notify(ctx, pi, message) -- clean shape with empty plugins[]; partial shape with mixed uninstalled+failed plugin rows"
      pattern: "notify\\(opts\\.ctx,\\s*opts\\.pi"
    - from: "extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts"
      to: "shared/notify.ts::PluginFailedMessage.cause (4-space-indent per renderPluginRow)"
      via: "per-plugin cause-chain through PluginFailedMessage payload"
      pattern: "cause:\\s*\\w+"
  coverage_constraints:
    - "Catalog UAT byte-equality MUST stay GREEN."
    - "`npm run check` GREEN at merge (atomic commit)."
    - "Zero V1 callers remain in remove.ts after this plan."
    - "The underlying `rm()` calls inside `removePath` MUST stay (only the user-visible notify is dropped); correctness preserved."
---

<objective>
Migrate `extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` from V1 severity-named wrappers (4 callsites: `notifyWarning` at 299, 354, 407; `notifySuccess` at 422) to V2 `notify(opts.ctx, opts.pi, NotificationMessage)`. Apply D-18-01 (DROP cleanup-leak warnings at lines 299 + 354 entirely; underlying `rm()` calls stay) and D-18-03 (cascade cause-chain MOVES from marketplace-level body to per-plugin `PluginFailedMessage.cause` rendered at 4-space indent). Update `tests/orchestrators/marketplace/remove.test.ts` (16 tests) per RESEARCH §"Per-File Test Surface": 5 byte-string flips + 1 severity flip (warning -> error on cascade partial) + 1 retry-anchor delete + 1 reload-hint contract flip + 1 test name change at line 232 per RESEARCH Risks #7. Drop now-orphaned `presentation/*` composer imports.

Purpose: deliver the remove command surface in V2 grammar; restructure cascade cause-chains to per-plugin shape per D-18-03; eliminate the V1 cleanup-leak warning surface that had no V2 representation.

Output: 2 modified files in one atomic commit. The clean + partial remove states route through V2 `notify()` with no orchestrator-level string composition; the `rm()` correctness paths (underlying file-system cleanup) are preserved.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-CONTEXT.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-RESEARCH.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-PATTERNS.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-VALIDATION.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-00-SUMMARY.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-01-SUMMARY.md
@extensions/pi-claude-marketplace/shared/notify.ts
@docs/output-catalog.md
@tests/architecture/catalog-uat.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate remove.ts -- 4 V1 wrappers -> V2 notify() (clean + partial cascade); DROP cleanup-leak warnings; restructure cascade per D-18-03; preserve underlying rm() calls</name>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts (full file -- 4 V1 callsites at lines 299, 354, 407, 422; RemoveMarketplaceOptions ALREADY carries `readonly pi: ExtensionAPI` at line 105 -- no Plan-18-00 dependency for the interface). Read the cleanup-leak block (lines 294-356), the partial branch (lines 363-408), the clean branch (lines 411-422), the `narrowCascadeFailure` helper (around line 162), and the `removePath` helper (lines 135-145).
    - extensions/pi-claude-marketplace/shared/notify.ts (read lines 500-560 for PluginFailedMessage + PluginUninstalledMessage shapes; lines 700-740 for renderPluginRow's failed/uninstalled arms + the 4-space-indent cause rendering per D-16-08).
    - tests/architecture/catalog-uat.test.ts lines 1154-1184 (remove fixtures: `clean` + `partial` -- the binding V2 byte shapes including the per-plugin `cause?: Error` at 4-space indent).
    - tests/shared/notify-v2.test.ts (per-variant V2 byte assertions; cross-reference failed + uninstalled plugin row shapes).
    - 18-CONTEXT.md D-18-01 (cleanup-leak DROP), D-18-03 (cascade cause-chain restructure), D-18-06 (test discipline), D-16-08 (per-plugin cause-chain 4-space indent), D-16-11 (severity ladder), D-16-12 (reload-hint ladder).
    - 18-RESEARCH.md "Per-File Callsite Inventory" → `remove.ts` (lines 241-332) + Risks #6 (cleanup-leak structural death) + Risks #7 (test name + reload-hint flip).
    - 18-PATTERNS.md "Pattern Assignments" → `orchestrators/marketplace/remove.ts` (V1/V2 diff + import drops).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts (Wave 1 pilot recipe block-comment).
  </read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts</files>
  <action>
    Operate in this order:

    1. Replace imports per RESEARCH §"Now-orphaned imports to drop (Plan 18-04)":
       - DROP: `softDepStatus` from `../../platform/pi-api.ts`; `cascadeSummary` from `../../presentation/cascade-summary.ts`; `causeChainTrailer` from `../../presentation/cause-chain.ts`; `renderRow` from `../../presentation/compact-line.ts`; `appendReloadHint, reloadHint` from `../../presentation/reload-hint.ts`; `notifySuccess, notifyWarning` from `../../shared/notify.ts`.
       - DROP types: `MarketplaceRow, PluginCascadeRow, SoftDepProbe` from `../../presentation/compact-line.ts`.
       - DROP local constant `RETRY_ANCHOR` (line 100) -- dead.
       - DECISION on `appendLeaks` import (line 79): per RESEARCH Risks #6, the cleanup-leak accumulator (`cleanupLeaks`) can be deleted entirely OR retained as a write-only sink. RECOMMENDED: delete the `cleanupLeaks` array, the `removePath(cleanupLeaks, ...)` helper invocations (lines 308-330), and the `appendLeaks` import -- BUT preserve the underlying `rm()` calls directly inside the catch handlers (correctness must not regress). Document this choice in the plan SUMMARY.
       - ADD: `notify` from `../../shared/notify.ts`; `type PluginFailedMessage, type PluginUninstalledMessage` from same module. No `ExtensionAPI` add (already imported alongside ExtensionContext at line 89).

    2. DROP cleanup-leak warning at line 299 (D-18-01). The `notifyWarning(opts.ctx, \`Marketplace "${opts.name}" removed; completion cache cleanup deferred: ${errorMessage(err)}\`)` is removed entirely. The surrounding try/catch stays; the cache-cleanup `rm()` call inside the try block stays; only the user-facing warning disappears.

    3. DROP post-state cleanup-leak block at lines 341-356 (D-18-01). Delete the entire `if (cleanupLeaks.length > 0) { ... notifyWarning(opts.ctx, body); return; }` block. The underlying side-effect `rm()` calls (called via `removePath(...)` at lines 308-330 OR retained directly per the decision in step 1) MUST remain (correctness preserved). If `cleanupLeaks` is fully deleted, the `removePath` helper signature may change to drop the accumulator parameter -- adapt accordingly.

    4. REPLACE the CMC-31 PARTIAL branch (lines 363-408) per D-18-03 cascade restructure:
       - Currently builds `headerRow: MarketplaceRow + childRows: PluginCascadeRow[]`, calls `cascadeSummary(...)`, appends reload-hint + RETRY_ANCHOR trailer, calls `notifyWarning(opts.ctx, body)`.
       - REPLACE with a single `notify(opts.ctx, opts.pi, { marketplaces: [{ name: opts.name, scope: resolved.scope, status: "failed", plugins: [...] }] })`.
       - The `plugins[]` array contains:
          - For each `successfullyUnstaged` name: `{ status: "uninstalled", name }` typed as `PluginUninstalledMessage` (the version field is optional on uninstalled variant; provide it if available from local accumulators, otherwise omit).
          - For each `failedPlugins.{ name, cause }` entry: `{ status: "failed", name, reasons: [narrowCascadeFailure(cause)], cause }` typed as `PluginFailedMessage` -- the `cause` is the per-plugin Error, rendered at 4-space indent by `renderPluginRow` per D-16-08.
       - The `RETRY_ANCHOR` trailer is DROPPED (already excluded by D-17-09; the constant becomes dead).
       - Severity = error is computed by `notify()` (any failed -> error per D-16-11); reload-hint fires because at least one plugin status is in `{uninstalled}` per D-16-12.
       - Drop the `removedSorted = [...removedPlugins].sort(...)` block at line 403 -- the renderer doesn't sort `plugins[]` (D-16-06 caller-order); retain sort behavior only if you want deterministic output, document if so.

    5. REPLACE the CMC-31 CLEAN branch (lines 411-422). Currently builds a `MarketplaceRow + renderRow + appendReloadHint(...)`. REPLACE with:
       - `notify(opts.ctx, opts.pi, { marketplaces: [{ name: opts.name, scope: resolved.scope, status: "removed", plugins: [] }] })`.
       - The reload-hint fires from mp-level `"removed"` per D-16-12 -- the EXACT same byte form whether or not plugins were removed (a contract change vs V1 per RESEARCH Risks #7; the user accepted it as the V2 grammar's contract).
       - Severity = info (computed; second arg omitted).

    6. KEEP the `narrowCascadeFailure` helper (line 162) unchanged -- still used inside `plugins[].map()` to populate `PluginFailedMessage.reasons[0]`.

    7. Add a brief comment near the new `notify()` calls referencing the Wave 1 pilot recipe (e.g., `// V2 cascade per D-18-03 -- per-plugin PluginFailedMessage.cause renders at 4-space indent (D-16-08); see add.ts (Wave 1 pilot) for the construction pattern.`).

    After this task: `grep -c "notifySuccess\\|notifyWarning\\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns 0; `grep -c "from \"../../presentation/" extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns 0.

    Honors D-18-01 (cleanup-leak DROP), D-18-03 (per-plugin cascade cause-chain), D-18-06 (single notify per orchestration), D-16-08, D-16-11, D-16-12.
  </action>
  <verify>
    <automated>node --test --import tsx tests/orchestrators/marketplace/remove.test.ts</automated>
  </verify>
  <done>
    remove.ts compiles strict; the 4 V1 callers are gone; presentation imports + RETRY_ANCHOR + cleanup-leak surfaces dropped; the V2 `notify()` calls cover CLEAN (status="removed", plugins=[]) and PARTIAL (status="failed", plugins=mixed uninstalled+failed with per-plugin cause). The underlying file-system `rm()` calls are preserved. The `narrowCascadeFailure` helper still feeds `PluginFailedMessage.reasons[0]`. Task 2 lands the test updates that unlock this verify in the same atomic commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update remove.test.ts -- 5 byte flips + reload-hint contract flip + line-232 test rename + cascade severity flip + retry-anchor delete</name>
  <read_first>
    - tests/orchestrators/marketplace/remove.test.ts (full file -- 16 tests; assertions at lines 154, 187, 232 (test name + 259 reload-hint assertion), 262, 308, 387-392 per RESEARCH §"Per-File Test Surface"; the existing makeCtx pattern; `pi` thread already in place (remove.ts always took pi)).
    - 18-RESEARCH.md "Per-File Test Surface" → remove.test.ts table (lines 500-525 with 5 byte flips + 1 severity flip + 1 retry-anchor delete + 1 reload-hint contract flip).
    - 18-PATTERNS.md "tests/orchestrators/marketplace/remove.test.ts" section.
    - tests/architecture/catalog-uat.test.ts (lines 1154-1184 -- binding V2 byte forms for `clean` and `partial`).
    - tests/shared/notify-v2.test.ts (per-variant V2 byte assertions; cross-reference for `failed` + `uninstalled` plugin row shapes).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts (post-Task-1 state).
    - 18-RESEARCH.md Risks #7 (line 232 contract-name drift -- rename the test + flip the assertion).
  </read_first>
  <files>tests/orchestrators/marketplace/remove.test.ts</files>
  <action>
    Apply the V1 -> V2 assertion changes per RESEARCH §"Per-File Test Surface":

    1. Line 154 (CMC-31 CLEAN with no plugins, dup-name scope): change `"● dup-name [project] (removed)"` to `"● dup-name [project] (removed)\\n\\n/reload to pick up changes"`. V2 reload-hint fires from mp.status `"removed"` (D-16-12) regardless of plugin presence.

    2. Line 187 (CMC-31 CLEAN with no plugins, user-only scope): change `"● user-only [user] (removed)"` to `"● user-only [user] (removed)\\n\\n/reload to pick up changes"`.

    3. Line 232 test (per RESEARCH Risks #7) -- TEST NAME CHANGE + ASSERTION FLIP:
       - RENAME the test from `"MR-2 + MR-8 (RH-1): empty marketplace removed cleanly emits success WITHOUT reload hint"` to `"MR-2 + V2 D-16-12: empty marketplace removed cleanly emits success WITH mp-level reload-hint"`.
       - At line 259 inside this test: change `assert.equal(notifications[0].message.includes("/reload to pick up changes"), false)` to `assert.equal(notifications[0].message.includes("/reload to pick up changes"), true)`.
       - At line 262 (the byte assertion inside this test): change `"● empty [project] (removed)"` to `"● empty [project] (removed)\\n\\n/reload to pick up changes"`.

    4. Line 308 (multi-plugin case ends with reload-hint): KEEP the regex `notifications[0].message =~ /\\/reload to pick up changes$/` unchanged -- still ends with the trailer on multi-plugin removal.

    5. Line 387 (cascade partial notification count): KEEP `assert.equal(notifications.length, 1)` unchanged -- still a single notify per orchestration.

    6. Line 388 (cascade partial severity): change `assert.equal(notifications[0].severity, "warning")` to `assert.equal(notifications[0].severity, "error")`. V2 routes any plugin/mp `failed` to error per D-16-11 (the cascade partial has `mp.status = "failed"`).

    7. Lines 389-392 (retry-anchor regex): DELETE these assertions. The V1 `/Fix the underlying issue and retry\\.?$/` regex is no longer applicable per D-17-09 / D-18-03 (the retry anchor is dropped from V2 catalog).

    8. KEEP unchanged (per RESEARCH "Tests to KEEP unchanged"):
       - `MR-1: --scope omitted + name not in either scope throws MarketplaceNotFoundError` (line 103) -- throw-path test.
       - `MR-7: github-source clone dir retained when any plugin failed in cascade` (line 406) and its inverse at line 467 -- filesystem-state tests, byte-string-agnostic except for the severity flip at line 388 already covered.
       - `D-03-INV :: remove unlinks the plugin cache file` (line 519) -- cache-state test.
       - `narrowCascadeFailure` unit tests (lines 613-646) -- the `__test_narrowCascadeFailure` helper is still used in V2 path to populate `PluginFailedMessage.reasons[0]`; tests should not need to change. Verify the test file still imports `__test_narrowCascadeFailure` correctly after the Task 1 import overhaul.

    9. Cleanup-leak tests: per RESEARCH §"Tests to DELETE per D-18-01" -- none currently exist in remove.test.ts that exercise the line 354 cleanup-leak warning. Confirm by `grep -in "cleanup leak\\|cleanupLeaks\\|completion cache" tests/orchestrators/marketplace/remove.test.ts` returning 0 lines. No tests to delete.

    10. `pi` plumbing into `removeMarketplace({...})` calls: already in place (remove.ts always took `pi`). Verify; if any call site is missing, fix it inline here (it'd be a pre-existing bug, not a Plan-18-04 issue).

    Honors D-18-01 (cleanup-leak DROP -- no tests exist to delete), D-18-03 (cascade restructure), D-18-06 (byte-exact V2 assertions through real notify() via makeCtx), Risks #7 (test name flip).
  </action>
  <verify>
    <automated>node --test --import tsx tests/orchestrators/marketplace/remove.test.ts &amp;&amp; node --test --import tsx tests/architecture/catalog-uat.test.ts</automated>
  </verify>
  <done>
    remove.test.ts: all 16 tests pass against the migrated remove.ts; 5 byte-string flips + 1 severity flip (warning -> error) + 1 retry-anchor delete + 1 reload-hint contract flip (false -> true) + 1 test rename complete. Catalog UAT stays GREEN. `npm run check` GREEN in the atomic commit. `grep -c "notifySuccess\\|notifyWarning\\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal API refactor; byte output governed by Phase 17 catalog (clean + partial cascade fixtures). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Information Disclosure (none new) | Cleanup-leak warning DROP | accept | D-18-01 drops the user-visible "cleanup deferred" notification. Cleanup failures are STILL swallowed by the existing try/catch (no new exfiltration); the underlying `rm()` calls run on the same paths; only the user no longer sees a warning. No additional secret/path data exposed. No regression vs V1's containment posture (NFR-10). |
| T-18-04-02 | (none) | V1->V2 migration | accept | No new threats -- internal API refactor; byte output governed by Phase 17 catalog; severity / reload-hint computed by notify(); per-plugin cause-chain stays at 4-space indent below the plugin row (D-16-08). |
</threat_model>

<verification>
- `npm run check` GREEN after the atomic commit.
- Catalog UAT byte-equality GREEN end-to-end.
- `grep -r "notifySuccess\|notifyWarning\|notifyError" extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns 0.
- `grep -c "from \"../../presentation/" extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns 0.
- Test name at line 232 reflects the V2 D-16-12 contract; the reload-hint assertion is TRUE.
- Cascade partial severity is "error" (not "warning").
- The underlying `rm()` cleanup paths run (correctness preserved by Task 1 step 2-3).
</verification>

<success_criteria>
- remove.ts emits V2 `notify(opts.ctx, opts.pi, ...)` calls for clean (status="removed", plugins=[]) and partial (status="failed", plugins=mixed) states.
- D-18-01: cleanup-leak warnings at lines 299 + 354 DROPPED; underlying `rm()` calls preserved.
- D-18-03: cascade cause-chain MOVED to per-plugin `PluginFailedMessage.cause`.
- remove.test.ts: 5 byte flips + reload-hint contract flip + line-232 test rename + cascade severity flip + retry-anchor delete.
- `npm run check` + catalog UAT GREEN.
</success_criteria>

<output>
Create `.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-04-SUMMARY.md` documenting:
- The 2 files modified.
- The cleanup-leak DROP decision (cleanupLeaks accumulator fate; preservation of underlying rm() calls).
- The cascade restructure (V1 marketplace-level body -> V2 per-plugin `PluginFailedMessage.cause`).
- The line-232 test rename + reload-hint flip.
- Confirmation that cascade.test.ts + shared.test.ts require no changes (RESEARCH §"Cascade + Shared Test Landmines").
- Confirmation that catalog UAT + `npm run check` both pass.
</output>
