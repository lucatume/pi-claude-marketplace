---
phase: 21
plan: 21-03
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - CHANGELOG.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SNM-32]
must_haves:
  truths:
    - "Per ROADMAP §Phase-21 SC #5: `npm run check` is GREEN (typecheck + ESLint with stock rules + Prettier + tests)."
    - "Per ROADMAP §Phase-21 SC #5: test count delta from 1367 baseline matches the RESEARCH §5 arithmetic (1367 - 34 lint-rules - 4 msg-rule-registry - 1 no-legacy-markers - 133 presentation - 7 notify.test.ts = 1188 expected; ±2 for planner-discretionary additions)."
    - "CHANGELOG.md gains a v1.4 milestone-closure entry mirroring the v1.3 entry format (PATTERNS.md Closure-Document Precedents row 1)."
    - ".planning/STATE.md flips v1.4 milestone to fully-complete: completed_phases=9, percent=100, last_activity stamped to today, Stopped At and Resume File updated."
    - ".planning/PROJECT.md gets a v1.4 entry at the TOP of the Validated section enumerating Phases 15-21 + the closed SNM IDs (per PATTERNS.md Closure-Document Precedents row 3)."
    - ".planning/REQUIREMENTS.md traceability rows for SNM-24, SNM-25, SNM-27, SNM-28, SNM-29, SNM-32 flip from Pending to Complete. SNM-22 already shows Complete pre-Phase-21 per CONTEXT note; planner verifies. SNM-23 is OUT OF SCOPE for Plan 21-03 (closed in Phase 20 -- any record-keeping mismatch is a separate quick-fix per B6 recommendation a)."
    - "21-SUMMARY.md records the full SC #1..#5 verification matrix + the verbatim RESEARCH §5 test-count arithmetic per PATTERNS.md `Test-Count Accounting Precedents` row."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v1.4 milestone closure entry at top of file."
      contains: "v1.4 Structured Notification Messages"
    - path: ".planning/STATE.md"
      provides: "v1.4 milestone fully-complete frontmatter."
      contains: "completed_phases: 9"
    - path: ".planning/REQUIREMENTS.md"
      provides: "All Phase-21 SNM IDs flipped to Complete (excluding SNM-23 -- Phase 20 scope)."
      contains: "SNM-32"
  key_links:
    - from: ".planning/STATE.md"
      to: "CHANGELOG.md"
      via: "milestone-closure narrative trail"
      pattern: "v1\\.4"
    - from: ".planning/REQUIREMENTS.md"
      to: "ROADMAP.md"
      via: "Phase 21 SC mapping"
      pattern: "Phase 21"
---

<objective>
Verify Phase 21 is GREEN end-to-end and record the v1.4 milestone closure. Run `npm run check` against the post-Plan-21-02 working tree; reconcile the test-count delta against RESEARCH §5's arithmetic; append a v1.4 closure entry to `CHANGELOG.md`; flip `.planning/STATE.md` to fully-complete (9/9 phases, 100%); add a `v1.4 Structured Notification Messages` entry to `.planning/PROJECT.md` Validated section; flip the Phase-21-mapped SNM IDs (SNM-22 verify; SNM-24, SNM-25, SNM-27, SNM-28, SNM-29, SNM-32) to Complete in `.planning/REQUIREMENTS.md`; produce `21-SUMMARY.md` with the full SC verification matrix.

**SNM-23 scope note (per B6):** SNM-23 closed in Phase 20, NOT Phase 21. It is intentionally OUT OF SCOPE for this plan. If `.planning/REQUIREMENTS.md` shows SNM-23 as Pending, that is a Phase 20 record-keeping debt that must be addressed in a separate quick-fix commit (e.g., via `/gsd-quick`) -- not by extending Phase 21's scope. Task 21-03-04 below MUST NOT touch SNM-23 rows.

Purpose: Phase 21 is the v1.4 milestone-closure gate. Plans 21-01 and 21-02 ship the actual surface changes; Plan 21-03 proves the gate is GREEN and writes the closure narrative across the 4 milestone-tracking documents so the next milestone (or backlog-only phase) can start from a known-clean state.

Output: 4 documentation files updated; one `21-SUMMARY.md` produced; optionally one or two closure commits (planner discretion per CONTEXT D-21-06 "verification-heavy ... no source mutations beyond CHANGELOG + state files"). `npm run check` GREEN.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-final-teardown-green-gate/21-CONTEXT.md
@.planning/phases/21-final-teardown-green-gate/21-RESEARCH.md
@.planning/phases/21-final-teardown-green-gate/21-PATTERNS.md
@.planning/phases/21-final-teardown-green-gate/21-VALIDATION.md
@.planning/phases/21-final-teardown-green-gate/21-01-SUMMARY.md
@.planning/phases/21-final-teardown-green-gate/21-02-SUMMARY.md
@CHANGELOG.md
@CLAUDE.md
</context>

<tasks>

<task id="21-03-01" type="execute">
  <name>Task 1: Run npm run check + record test-count delta against RESEARCH §5 arithmetic; capture observed numbers for 21-SUMMARY.md</name>
  <files>(no file mutations)</files>
  <read_first>
    - .planning/phases/21-final-teardown-green-gate/21-RESEARCH.md (§5 -- 1367 baseline → 1188-1190 expected; per-deletion subtotals: 34 + 4 + 1 + 133 + 7 = 179)
    - .planning/phases/21-final-teardown-green-gate/21-VALIDATION.md (Plan 21-03 regression-matrix rows)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Test-Count Accounting Precedents row)
  </read_first>
  <action>
    1. Run `npm run check` and confirm exit code 0. If non-zero, diagnose layer (typecheck / lint / format / tests) and fix; this plan's gate is the GREEN check.
    2. Run `npm test 2>&1 | tail -20` and capture the full final summary block (e.g., `# tests N`, `# suites M`, `# pass`, `# fail`, `# skipped`, `# todo`). Record the observed `N` (the OBSERVED_TEST_COUNT) -- it MUST be substituted into Task 21-03-02 step 2 below.
    3. Compute the observed total against the RESEARCH §5 expected formula:
       ```
       1367 (pre-Phase-21)
       - 34 (tests/lint-rules/)
       - 4 (msg-rule-registry.test.ts)
       - 1 (no-legacy-markers.test.ts)
       - 133 (tests/presentation/)
       - 7 (tests/shared/notify.test.ts)
       + 0..2 (planner-discretionary compareByNameThenScope block -- Task 21-02-09 step 3 default SKIP, so expect +0)
       = 1188 expected (default) to 1190 (with discretionary additions)
       ```
       Record the observed delta in Task 21-03-05 (SUMMARY composition). If the observed total drifts >±5 from expected, investigate before flipping requirements to Complete -- silent test loss could indicate a glob misconfiguration.
    4. Re-run the architecture-test trio + key surfaces explicitly to make any silent regression loud:
       - `npm test -- tests/architecture/catalog-uat.test.ts`
       - `npm test -- tests/architecture/notify-types.test.ts`
       - `npm test -- tests/architecture/markers-snapshot.test.ts`
       - `npm test -- tests/architecture/scope-order-drift.test.ts`
       - `npm test -- tests/shared/notify-v2.test.ts`
       Each MUST exit 0 individually.
    5. Verify ROADMAP §Phase-21 SC #1..#5 post-conditions (per W5 -- SC #4 is two-sided: negative assertion that the deleted artifacts are absent AND positive assertion that the four inlined declarations are present in `shared/notify.ts`):
       - SC #1: `! test -d tests/lint-rules && ! test -f tests/architecture/msg-rule-registry.test.ts`
       - SC #2: `! grep -nE "^export function notify(Success|Warning|Error)\\(" extensions/pi-claude-marketplace/shared/notify.ts`
       - SC #3: `! grep -nE "import msgPlugin|msg/(sr-|gr-|nc-|lc-|mr-|rp-|rh-|cc-|sd-|er-|wm-|kc-|kp-)" eslint.config.js && grep -n "no-restricted-syntax" eslint.config.js | head -1`
       - SC #4 (negative): `! test -f tests/architecture/no-legacy-markers.test.ts && ! test -d extensions/pi-claude-marketplace/shared/grammar`
       - SC #4 (positive -- per W5): each of the four inlined `export const` declarations MUST be present exactly once in `shared/notify.ts`:
         ```bash
         grep -c "^export const REASONS" extensions/pi-claude-marketplace/shared/notify.ts          # = 1
         grep -c "^export const STATUS_TOKENS" extensions/pi-claude-marketplace/shared/notify.ts     # = 1
         grep -c "^export const MARKERS" extensions/pi-claude-marketplace/shared/notify.ts          # = 1
         grep -c "^export const PATTERN_CLASSES" extensions/pi-claude-marketplace/shared/notify.ts   # = 1
         ```
       - SC #5: `npm run check` exits 0 (already done in step 1).
    6. Persist the captured numbers in a scratch note (working-tree only -- no file commit yet) for use by Tasks 21-03-02 through 21-03-05.
  </action>
  <verify>
    <automated>
      <command>npm run check</command>
      <command>npm test -- tests/architecture/catalog-uat.test.ts</command>
      <command>npm test -- tests/architecture/notify-types.test.ts</command>
      <command>npm test -- tests/architecture/markers-snapshot.test.ts</command>
      <command>npm test -- tests/architecture/scope-order-drift.test.ts</command>
      <command>npm test -- tests/shared/notify-v2.test.ts</command>
      <command>! test -d tests/lint-rules</command>
      <command>! test -f tests/architecture/msg-rule-registry.test.ts</command>
      <command>! test -f tests/architecture/no-legacy-markers.test.ts</command>
      <command>! test -d extensions/pi-claude-marketplace/shared/grammar</command>
      <command>! test -d extensions/pi-claude-marketplace/presentation</command>
      <command>! test -d tests/presentation</command>
      <command>! grep -nE "^export function notify(Success|Warning|Error)\\(" extensions/pi-claude-marketplace/shared/notify.ts</command>
      <command>! grep "import msgPlugin" eslint.config.js</command>
      <command>grep -c "^export const REASONS" extensions/pi-claude-marketplace/shared/notify.ts</command>
      <command>grep -c "^export const STATUS_TOKENS" extensions/pi-claude-marketplace/shared/notify.ts</command>
      <command>grep -c "^export const MARKERS" extensions/pi-claude-marketplace/shared/notify.ts</command>
      <command>grep -c "^export const PATTERN_CLASSES" extensions/pi-claude-marketplace/shared/notify.ts</command>
    </automated>
  </verify>
  <done>
    `npm run check` GREEN. Architecture-test trio + scope-order-drift + notify-v2 each individually GREEN. Observed test count recorded; delta against expected 1188 within ±5. All 5 SCs from ROADMAP §Phase-21 verified -- SC #4 is two-sided (negative: deleted artifacts absent; positive per W5: all four inlined `export const REASONS|STATUS_TOKENS|MARKERS|PATTERN_CLASSES` declarations present exactly once in `shared/notify.ts`).
  </done>
</task>

<task id="21-03-02" type="execute">
  <name>Task 2: Append v1.4 closure entry to CHANGELOG.md mirroring the v1.3 entry format</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Closure-Document Precedents row 1 -- mirror v1.3 entry shape)
    - .planning/REQUIREMENTS.md
    - .planning/phases/21-final-teardown-green-gate/21-CONTEXT.md (Claude's Discretion: planner picks entry phrasing -- recommended single v1.4 milestone-closure section listing closed SNM IDs with one-line annotations)
  </read_first>
  <action>
    Insert a NEW entry at the TOP of `CHANGELOG.md` (immediately after the `# Changelog` header on line 1, BEFORE the existing `## [0.2.0] - 2026-05-25 -- v1.3 Consistent Messaging` entry).

    Pick the version number: bump from `0.2.0` to `0.3.0` per semantic-version convention (minor bump for the v1.4 milestone; no breaking changes from a user perspective because the catalog output shape was locked in Phase 17 and has been GREEN since). The planner has discretion on patch vs minor bump per CLAUDE.md "offer to bump the project version in `project.json` and `sonar.properties`" -- but note that this project does not have `project.json` / `sonar.properties` (verified during research); the `package.json` `version` field at line 87 is the canonical source. Plan 21-03 updates `package.json` `version: "0.2.0"` → `"0.3.0"` if and only if a real release is intended -- DEFAULT for this task is to bump the CHANGELOG entry's date+version line and leave `package.json` to a separate operator decision. If the planner sees evidence the user intends a release tag, bump `package.json` too in the same commit.

    **Per B7 -- template substitution: replace `{OBSERVED_TEST_COUNT}` with the integer captured in Task 21-03-01 step 2 (e.g., 1188).** The template contains TWO occurrences in the final "X/Y tests green" line; both MUST be substituted with the same integer. Do NOT leave any `{...}` or `${...}` placeholder in the final CHANGELOG text.

    Entry shape -- mirror v1.3's structure verbatim per PATTERNS.md Closure-Document Precedents row 1. Use today's date (2026-05-27) per the existing entry format. The template below shows the SHAPE; the executor substitutes `{OBSERVED_TEST_COUNT}` with the actual integer at both occurrences (the fix in B7: the prior template had `{OBSERVED_TEST_COUNT}/${OBSERVED_TEST_COUNT}}` with mixed `{}` / `${}` / stray closing brace -- that bug is corrected to `{OBSERVED_TEST_COUNT}/{OBSERVED_TEST_COUNT}` here):

    ```
    ## [0.3.0] - 2026-05-27 -- v1.4 Structured Notification Messages

    Every user-visible `ctx.ui.notify` callsite now flows through the V2
    `notify(ctx, pi, NotificationMessage)` and `notifyUsageError(ctx,
    UsageErrorMessage)` structured entry points; severity, reload hint,
    and per-row soft-dep markers are computed from message contents at
    notify time rather than caller-supplied. The v1.3 34-rule MSG-* drift
    guard plugin (`tests/lint-rules/`) is retired in favor of closed-set
    type encoding (PluginStatus / MarketplaceStatus / Reason / StatusToken
    / Marker unions in `shared/notify.ts`).

    User-visible changes:

    - No user-visible message-format changes vs v1.3 -- the catalog output
      shape was locked in Phase 17 (v2.0 always-marketplace-header spec)
      and has been catalog-UAT GREEN since. v1.4 is a structural / internal
      milestone.

    Internals:

    - V2 type model: `NotificationMessage` / `MarketplaceNotificationMessage`
      / `PluginNotificationMessage` (10-variant discriminated union) /
      `PluginStatus` / `MarketplaceStatus` / `Dependency` / `UsageErrorMessage`
      shipped in `shared/notify.ts` (SNM-01..11, SNM-21).
    - V2 public API: `notify()` derives severity / reload trailer / per-row
      soft-dep markers from contents (SNM-12..18, SNM-30).
    - Spec rewrite: `docs/messaging-style-guide.md` v2.0 + `docs/output-catalog.md`
      v2.0 + catalog UAT runner driven by structured fixtures (SNM-19, SNM-20,
      SNM-26, SNM-31).
    - All `notifySuccess` / `notifyWarning` / `notifyError` callsites migrated
      across `orchestrators/marketplace/*`, `orchestrators/plugin/*`,
      `orchestrators/import/*`, and `edge/handlers/*`; V1 wrappers deleted
      from `shared/notify.ts` (SNM-22). All `notifyUsageError(ctx, msg,
      usage)` callsites migrated to V2 1-arg form (SNM-23).
    - Drift-guard teardown: `tests/lint-rules/` (73 files), `tests/architecture/
      msg-rule-registry.test.ts`, `tests/architecture/no-legacy-markers.test.ts`
      deleted; `eslint.config.js` rewritten to stock rules + retained BLOCKS
      A/B/C/D/E + new `persistence/migrate.ts` block-level no-console override
      (SNM-24, SNM-25, SNM-27, SNM-28).
    - Source consolidation: `shared/grammar/` deleted; REASONS / STATUS_TOKENS
      / MARKERS / PATTERN_CLASSES inlined into `shared/notify.ts` (SNM-29).
      `presentation/` directory deleted; 5 utilities relocated (composeError
      WithCauseChain → `shared/errors.ts`; compareByNameThenScope → `shared/
      notify.ts`; EntityErrorRow → file-local in `orchestrators/plugin/
      install.ts`; sourceLogical / ParsedSource → direct from `domain/
      source.ts` via narrowed BLOCK C edge zone; renderMarketplaceList
      deleted, zero callers).
    - `edge/args-schema.ts` callback parameter renamed `notifyError` →
      `onError` for V1-era name-shadow cleanup.

    {OBSERVED_TEST_COUNT}/{OBSERVED_TEST_COUNT} tests green; lint + format + types clean.
    ```

    **Substitution step (per B7):** after pasting the template into `CHANGELOG.md`, replace BOTH occurrences of the literal token `{OBSERVED_TEST_COUNT}` with the integer captured in Task 21-03-01 step 2 (e.g., `1188`). The final line should read like `1188/1188 tests green; lint + format + types clean.` -- mirroring the v1.3 entry's "1249/1249 tests green" line.

    **Defensive verification (per B7):** after the edit, confirm no placeholder leaked into the file:
    ```bash
    ! grep -E '\{OBSERVED_TEST_COUNT\}|\$\{' CHANGELOG.md
    ```

    Do NOT touch any other entry in `CHANGELOG.md` (v1.0..v1.3 history stays unchanged).
  </action>
  <verify>
    <automated>
      <command>head -5 CHANGELOG.md | grep -c "v1.4 Structured Notification Messages"</command>
      <command>grep -c "^## \\[0\\." CHANGELOG.md</command>
      <command>head -50 CHANGELOG.md | grep -cE "SNM-(22|23|24|25|27|28|29)"</command>
      <command>head -60 CHANGELOG.md | grep -c "tests green"</command>
      <command>! grep -E '\{OBSERVED_TEST_COUNT\}|\$\{' CHANGELOG.md</command>
    </automated>
  </verify>
  <done>
    `CHANGELOG.md` opens with a `## [0.3.0] - 2026-05-27 -- v1.4 Structured Notification Messages` entry mirroring the v1.3 structural format. Closed SNM IDs are enumerated in the Internals bullets. Observed test-count line is present with BOTH occurrences of `{OBSERVED_TEST_COUNT}` substituted by the same integer (e.g., `1188/1188 tests green; …`). The defensive grep `! grep -E '\{OBSERVED_TEST_COUNT\}|\$\{' CHANGELOG.md` exits 0 -- no template artifacts leaked through (per B7). Prior history is unchanged.
  </done>
</task>

<task id="21-03-03" type="execute">
  <name>Task 3: Update .planning/STATE.md frontmatter to mark v1.4 milestone fully complete (completed_phases=9, percent=100)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Closure-Document Precedents row 2 -- STATE.md milestone-closure shape mirroring 86b9d15)
  </read_first>
  <action>
    Edit the YAML frontmatter at the top of `.planning/STATE.md`:

    1. `last_updated` -- set to a fresh ISO-8601 timestamp matching today (the executor's commit time).
    2. `last_activity` -- set to: `2026-05-27 -- Phase 21 marked complete -- v1.4 milestone closed`.
    3. `progress.completed_phases` -- bump from `8` to `9`.
    4. `progress.percent` -- bump from `89` to `100`.
    5. Leave `milestone: v1.4` and `milestone_name: Structured Notification Messages` and `status: completed` unchanged (the milestone marker was already set when Phase 20 closed; this commit completes the math).
    6. `total_phases: 9` stays unchanged.

    Body edits:

    1. **`## Current Position` section:**
       - `Phase: 20 -- COMPLETE` → `Phase: 21 -- COMPLETE`
       - `Plan: 1 of 6` → `Plan: 3 of 3`
       - `Status: Phase 20 complete` → `Status: Phase 21 complete -- v1.4 milestone closed`
       - `Last activity: 2026-05-27 -- Phase 20 marked complete` → `Last activity: 2026-05-27 -- Phase 21 marked complete -- v1.4 milestone closed`

    2. **`## Session Continuity` section:**
       - `Last session:` → fresh timestamp matching `last_updated`.
       - `Stopped At: Phase 21 context gathered` → `Stopped At: Phase 21 complete -- v1.4 milestone closed`
       - `Resume File: .planning/phases/21-final-teardown-green-gate/21-CONTEXT.md` → leave for archival reference, OR clear it (planner discretion; recommended: leave for traceability).

    3. **`## Operator Next Steps` section:**
       - Replace the stale `Run /gsd-plan-phase 15 ...` line with the milestone-closed status. Recommended:
         ```
         - v1.4 Structured Notification Messages milestone is CLOSED. Next milestone planning is operator-initiated (`/gsd-new-milestone` or equivalent).
         ```

    4. **Performance Metrics -- Velocity / By Phase / Recent Trend:** the existing per-phase plan-count rows (lines 41-58) do NOT include Phase 21 yet. Optionally add `| 21 | 3 | - | - |` to the "By Phase" table per CONTEXT Claude's Discretion. Recommended: add the row for completeness.

    Do NOT touch:
    - `## Project Reference` section.
    - `## Accumulated Context` Roadmap Evolution / Decisions / Pending Todos / Blockers/Concerns / Quick Tasks Completed lines (these record historical state; no new entries needed from Plan 21-03 itself).
    - `## Deferred Items` table (v1.3-close deferrals are still applicable).
  </action>
  <verify>
    <automated>
      <command>grep -c "completed_phases: 9" .planning/STATE.md</command>
      <command>grep -c "percent: 100" .planning/STATE.md</command>
      <command>grep -c "Phase 21 -- COMPLETE" .planning/STATE.md</command>
      <command>grep -c "v1.4 milestone closed" .planning/STATE.md</command>
    </automated>
  </verify>
  <done>
    `.planning/STATE.md` frontmatter shows `completed_phases: 9` / `percent: 100`. Current Position shows Phase 21 complete + v1.4 milestone closed. Session Continuity stamp refreshed. Operator Next Steps reflect milestone-closed state.
  </done>
</task>

<task id="21-03-04" type="execute">
  <name>Task 4: Update .planning/PROJECT.md to add v1.4 entry to Validated section + flip .planning/REQUIREMENTS.md Phase-21-mapped SNM IDs to Complete (SNM-23 OUT OF SCOPE per B6)</name>
  <files>
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Closure-Document Precedents rows 3 + 4)
    - .planning/phases/21-final-teardown-green-gate/21-CONTEXT.md (D-21-09 closure pattern + Source files Phase 21 modifies -- Plan 21-03 list)
  </read_first>
  <action>
    **Scope discipline note (per B6 recommendation a):** SNM-23 closed in Phase 20 and is intentionally OUT OF SCOPE for Plan 21-03. Do NOT touch SNM-23 rows in this task -- if the REQUIREMENTS.md per-section checkbox or traceability row shows SNM-23 as Pending, that is a Phase-20 record-keeping debt to be addressed in a separate `/gsd-quick` commit AFTER Phase 21 closes. Expanding Phase 21's scope to fix unrelated record-keeping defeats the purpose of milestone gating.

    **A. `.planning/PROJECT.md` updates:**

    1. Read `.planning/PROJECT.md` in full to find the `### Validated` section (or equivalent -- per PATTERNS.md the structural precedent is the existing v1.3 entry at the top of that section).
    2. Add a v1.4 entry at the TOP of the Validated section. Recommended shape per PATTERNS.md:
       ```
       - ✓ v1.4 Structured Notification Messages (Phases 15-21, SNM-01..SNM-32):
         Replaced the v1.3 string-based notify API + 34-rule MSG-* ESLint
         drift-guard plugin with a type-driven structured `NotificationMessage`
         payload. Two public entry points (`notify(ctx, pi, NotificationMessage)`,
         `notifyUsageError(ctx, UsageErrorMessage)`) exported from
         `shared/notify.ts`; severity / reload trailer / per-row soft-dep
         markers computed from contents. V2 grammar amendments for autoupdate
         surface (Phase 17.1) and orphan-fold (Phase 17.2). Drift-guard
         teardown: `tests/lint-rules/` (73 files), `tests/architecture/
         {msg-rule-registry,no-legacy-markers}.test.ts`, `presentation/`,
         `shared/grammar/`, `tests/presentation/` all deleted; `eslint.config.js`
         reduced to stock rules + retained BLOCKS A/B/C/D/E. Test surface:
         `tests/shared/notify-v2.test.ts` (41 V2 tests) + `tests/architecture/
         catalog-uat.test.ts` byte-equality runner.
       ```
    3. If there is an active `## Current Milestone:` block referencing `v1.4 Structured Notification Messages`, either delete that block or set its content to `(milestone planning pending)` per PATTERNS.md "Move the existing 'Current Milestone: v1.4' block to 'Validated' and either drop the 'Current Milestone:' header entirely or set it to '(milestone planning pending)' per planner discretion." Recommended: delete the active block (its content is now reflected in the Validated entry).
    4. If a Key Decisions table exists, append Phase 21 rows per CONTEXT "Source files Phase 21 modifies" (e.g., `D-21-01 aggressive grammar inline`, `D-21-02 presentation clean-sweep`, `D-21-04 stock ESLint rules`, `D-21-05 V1 wrapper deletion`, `D-21-06 3-plan / 3-wave shape`). Format mirrors existing table rows.

    **B. `.planning/REQUIREMENTS.md` updates (SNM-23 EXCLUDED per B6):**

    1. Read the v1.4 "Migration & Deletion" + "Test Coverage" sections and the Traceability table at lines 76-109.
    2. **Top-of-section checkbox flips** (lines 47-59 area):
       - SNM-24: `- [ ]` → `- [x]`
       - SNM-25: `- [ ]` → `- [x]`
       - SNM-27: `- [ ]` → `- [x]`
       - SNM-28: `- [ ]` → `- [x]`
       - SNM-29: `- [ ]` → `- [x]`
       - SNM-31: verify status -- REQUIREMENTS.md frontmatter shows `[ ]` but traceability row reads `Complete` (Phase 17). Reconcile per W7's "deterministic invariant check" guidance (per-section checkbox state MUST agree with traceability-table status).
       - SNM-32: `- [ ]` → `- [x]`
       - SNM-19, SNM-20: verify status -- both show `[ ]` in the per-section list but `Complete` in the traceability table (Phase 17). Reconcile (`[ ]` → `[x]`) so the two views agree.
       - SNM-26 already `[x]` per the existing list -- leave alone but verify.
       - **SNM-23: DO NOT TOUCH** (per B6 -- out of scope; Phase 20 closed it).
    3. **Traceability table flips** (lines 99-109):
       - SNM-22 already shows `Complete` -- verify (using regex-tolerant matcher) and reconcile any stray formatting per CONTEXT note ("REQUIREMENTS.md currently shows SNM-22 with a stray `[x]` checkbox -- planner verifies"). Use a regex that tolerates variable whitespace:
         ```bash
         grep -E "^\| SNM-22 +\| Phase [0-9]+ +\| Complete" .planning/REQUIREMENTS.md   # MUST match (count ≥ 1)
         ```
       - SNM-24: `Pending` → `Complete`
       - SNM-25: `Pending` → `Complete`
       - SNM-27: `Pending` → `Complete`
       - SNM-28: `Pending` → `Complete`
       - SNM-29: `Pending` → `Complete`
       - SNM-32: `Pending` → `Complete`
       Leave SNM-01..21, SNM-23 (per B6), SNM-26, SNM-30, SNM-31 rows as currently displayed (most already say `Complete`; if SNM-31 is the per-section list mismatch in step 2, flip the traceability row to match).
    4. **Per-section ↔ traceability invariant (per W7):** after all flips, run a deterministic check that every SNM ID's per-section checkbox state agrees with its traceability-table status. If any mismatch is detected for a Phase-21-owned ID, reconcile. If a mismatch involves SNM-23, leave it -- it's the Phase 20 debt called out in the scope note above.
    5. **Coverage block at line ~111:** update if any tally line references "complete: X / pending: Y" -- recompute to reflect the post-Phase-21 state (note: SNM-23 may remain Pending pending the separate Phase 20 record-keeping quick-fix).

    Do NOT touch the "## Out of Scope" section or the "## Milestone Goal" prose at the top of the file.
  </action>
  <verify>
    <automated>
      <command>grep -c "v1.4 Structured Notification Messages" .planning/PROJECT.md</command>
      <command>grep -E "^\| SNM-32 +\| Phase 21 +\| Complete" .planning/REQUIREMENTS.md</command>
      <command>grep -E "^\| SNM-29 +\| Phase 21 +\| Complete" .planning/REQUIREMENTS.md</command>
      <command>grep -E "^\| SNM-28 +\| Phase 21 +\| Complete" .planning/REQUIREMENTS.md</command>
      <command>grep -E "^\| SNM-27 +\| Phase 21 +\| Complete" .planning/REQUIREMENTS.md</command>
      <command>grep -E "^\| SNM-25 +\| Phase 21 +\| Complete" .planning/REQUIREMENTS.md</command>
      <command>grep -E "^\| SNM-24 +\| Phase 21 +\| Complete" .planning/REQUIREMENTS.md</command>
      <command>grep -E "^\| SNM-22 +\| Phase [0-9]+ +\| Complete" .planning/REQUIREMENTS.md</command>
    </automated>
  </verify>
  <done>
    `.planning/PROJECT.md` has a `v1.4 Structured Notification Messages` entry at the top of Validated; active-milestone block retired. `.planning/REQUIREMENTS.md` traceability rows for the Phase-21-owned SNM IDs (SNM-24, 25, 27, 28, 29, 32) all show `Complete`. SNM-22 already-Complete row verified via regex-tolerant matcher (per B6 fix -- no hardcoded whitespace strings). SNM-23 is intentionally untouched per B6 scope discipline (Phase 20 record-keeping is addressed separately). Per W7 invariant: per-section `[ ]`/`[x]` state agrees with traceability-table status for every Phase-21-owned SNM ID.
  </done>
</task>

<task id="21-03-05" type="execute">
  <name>Task 5: Compose 21-SUMMARY.md with SC verification matrix + test-count arithmetic; pre-commit gate; closure commit(s)</name>
  <files>
    .planning/phases/21-final-teardown-green-gate/21-SUMMARY.md
    CHANGELOG.md
    .planning/STATE.md
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/phases/21-final-teardown-green-gate/21-01-SUMMARY.md
    - .planning/phases/21-final-teardown-green-gate/21-02-SUMMARY.md
    - .planning/phases/21-final-teardown-green-gate/21-VALIDATION.md (Plan 21-03 regression-matrix rows + Validation Sign-Off)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Test-Count Accounting Precedents row + Per-plan SC verification matrix pattern)
    - .claude/get-shit-done/templates/summary.md
    - CLAUDE.md (Conventional Commits + pre-commit FIRST)
  </read_first>
  <action>
    1. **Compose `.planning/phases/21-final-teardown-green-gate/21-SUMMARY.md`.** Sections:
       - **Header:** `# Phase 21: Final Teardown & GREEN Gate -- SUMMARY` + the completion date.
       - **Status:** `COMPLETE -- v1.4 milestone CLOSED`.
       - **Plan inventory:** list 21-01, 21-02, 21-03 with their commit SHAs and one-line objectives. **Per N3 -- capture commit SHAs deterministically by filtering on the phase prefix** (the `git log -3 --pretty=%H` form is fragile because intervening commits would shift the window):
         ```bash
         git log --grep="^(chore|refactor|docs)\(21\)" --pretty=%H | head -3
         ```
         This matches only commits whose subject starts with `chore(21)`, `refactor(21)`, or `docs(21)` -- exactly the Phase 21 commit set.
       - **Success Criteria verification table** (mirror Plan 20-04's per-SC table per PATTERNS.md):
         ```
         | SC | description | command | observed result | exit code | status |
         | #1 | tests/lint-rules/ + msg-rule-registry.test.ts absent | ! test -d ... && ! test -f ... | both absent | 0 | GREEN |
         | #2 | V1 wrappers absent from shared/notify.ts | ! grep -nE "..." extensions/... | zero matches | 0 | GREEN |
         | #3 | eslint.config.js stock-rules + persistence/migrate.ts override | ! grep "import msgPlugin" eslint.config.js && grep "persistence/migrate" eslint.config.js | zero msgPlugin / one persistence override | 0 | GREEN |
         | #4 (neg) | no-legacy-markers.test.ts absent + shared/grammar/ absent | ! test -f ... && ! test -d ... | both absent | 0 | GREEN |
         | #4 (pos) | inlined REASONS/STATUS_TOKENS/MARKERS/PATTERN_CLASSES present in shared/notify.ts (per W5) | grep -c "^export const ..." (×4) | each = 1 | 0 | GREEN |
         | #5 | npm run check GREEN | npm run check | {observed-test-count} pass / 0 fail / 0 skipped / {todo} todo | 0 | GREEN |
         ```
       - **NO-CHANGE invariants re-verified** (mirror Plan 20-04's second table):
         ```
         | invariant | command | observed | expected | status |
         | catalog UAT byte-equality | npm test -- tests/architecture/catalog-uat.test.ts | pass | pass | GREEN |
         | V2 per-variant renderer | npm test -- tests/shared/notify-v2.test.ts | 41 pass | 41 pass | GREEN |
         | notify-types compile invariants | npm test -- tests/architecture/notify-types.test.ts | pass | pass | GREEN |
         | Phase 5/7 markers snapshot | npm test -- tests/architecture/markers-snapshot.test.ts | pass | pass | GREEN |
         | scope-order drift guard (updated to shared/notify.ts) | npm test -- tests/architecture/scope-order-drift.test.ts | pass | pass | GREEN |
         ```
       - **Test-count arithmetic (PATTERNS.md row "Cumulative test-count arithmetic"):** verbatim formula:
         ```
         1367 (pre-Phase-21)
         - 34 (tests/lint-rules/)
         - 4 (msg-rule-registry.test.ts)
         - 1 (no-legacy-markers.test.ts)
         - 133 (tests/presentation/)
         - 7 (tests/shared/notify.test.ts)
         + {discretionary additions in Plan 21-02 Task 21-02-09 step 3, default 0}
         = {observed-total}
         ```
         Followed by the observed `# tests N / # pass M / # fail 0 / # skipped 0 / # todo K` line captured in Task 21-03-01 step 2. If delta from 1188 exceeds ±5, surface the discrepancy with a one-paragraph investigation note.
       - **Closed requirements:** enumerate SNM-22, SNM-24, SNM-25, SNM-27, SNM-28, SNM-29, SNM-32 with one-line annotations referencing the closing plan + commit SHA. **Do NOT list SNM-23 as closed-in-Phase-21** (per B6 -- it closed in Phase 20).
       - **BLOCK C amendment record:** note Option A (amend `edge` zone `from:` to remove `domain/` and drop dead `presentation/` references) was chosen and landed in Plan 21-02 commit.
       - **Closure-document update record:** list the 4 milestone-tracking files updated (CHANGELOG.md, STATE.md, PROJECT.md, REQUIREMENTS.md) with one-line summaries.
       - **Phase 20 record-keeping note (per B6):** explicitly note that SNM-23's per-section checkbox / traceability status in `.planning/REQUIREMENTS.md` is a Phase 20 debt to be reconciled in a separate `/gsd-quick` commit, NOT extended into Phase 21 scope.
       - **v1.4 milestone closed marker:** explicit sentence stating the milestone is closed and pointing to operator next steps.

    2. **Stage closure files** (before pre-commit so the canonical `git diff --cached --name-only` pattern works):
       ```bash
       git add -- CHANGELOG.md .planning/STATE.md .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/phases/21-final-teardown-green-gate/21-SUMMARY.md
       ```

    3. **Pre-commit gate (canonical CLAUDE.md pattern):**
       ```bash
       pre-commit run --files $(git diff --cached --name-only)
       ```
       Fix any failures, re-stage, and re-run until clean. NEVER `--no-verify`.

    4. **Closure commit.** Per CONTEXT D-21-06 "Plan 21-03 is verification-and-closure plan ... no source mutations beyond CHANGELOG + state files" and per PATTERNS.md "Conventional Commits with phase ID prefix" example: `docs(21): close v1.4 milestone + CHANGELOG + STATE + PROJECT + REQUIREMENTS`. Title ≤72 chars; body lines ≤80 chars.

       Recommended single closure commit covering all 5 modified files. Body:
       ```
       Per D-21-06 + D-21-09 (Phase 21 Plan 21-03), close v1.4 milestone:

       - CHANGELOG.md: v1.4 milestone-closure entry mirroring v1.3 format,
         enumerating closed SNM IDs.
       - .planning/STATE.md: completed_phases 8 -> 9, percent 89 -> 100,
         Phase 21 marked COMPLETE.
       - .planning/PROJECT.md: v1.4 entry at top of Validated section
         enumerating Phases 15-21 + SNM-01..SNM-32.
       - .planning/REQUIREMENTS.md: SNM-22 (verified), SNM-24, 25, 27, 28,
         29, 32 flipped to Complete in traceability table; per-section
         checklist reconciled. SNM-23 left untouched (Phase 20 debt -- to
         be reconciled in a separate quick-fix commit per scope discipline).
       - 21-SUMMARY.md: SC #1..#5 verification matrix + NO-CHANGE invariant
         re-verification + verbatim test-count arithmetic.

       Closes SNM-32 (npm run check GREEN against the post-teardown
       surface). v1.4 Structured Notification Messages milestone CLOSED.
       ```

       Use HEREDOC form. Do NOT use `--amend`.

    5. Run `git status` after commit -- confirm working tree clean.
    6. Run the deterministic commit-set check (per N3): `git log --grep="^(chore|refactor|docs)\(21\)" --pretty=%H | head -3` -- confirm 3 SHAs returned (the Phase 21 commits in reverse chronological order: Plan 21-03 → Plan 21-02 → Plan 21-01).
  </action>
  <verify>
    <automated>
      <command>test -f .planning/phases/21-final-teardown-green-gate/21-SUMMARY.md</command>
      <command>grep -c "v1.4 milestone" .planning/phases/21-final-teardown-green-gate/21-SUMMARY.md</command>
      <command>grep -c "SC #5" .planning/phases/21-final-teardown-green-gate/21-SUMMARY.md</command>
      <command>grep -cE "SNM-(22|24|25|27|28|29|32)" .planning/phases/21-final-teardown-green-gate/21-SUMMARY.md</command>
      <command>npm run check</command>
      <command>git log --grep="^(chore|refactor|docs)\\(21\\)" --pretty=%H | head -3 | wc -l | grep -E "^3$"</command>
      <command>git status --porcelain | (! grep -q .)</command>
    </automated>
  </verify>
  <done>
    `21-SUMMARY.md` exists with SC matrix (including W5 two-sided SC #4 row) + test-count arithmetic + closed-requirements enumeration (SNM-23 explicitly NOT in the Phase 21 closed list per B6). The 4 closure documents (CHANGELOG / STATE / PROJECT / REQUIREMENTS) are updated and committed. The deterministic commit query (per N3) `git log --grep="^(chore|refactor|docs)\(21\)" --pretty=%H | head -3` returns exactly 3 SHAs. `npm run check` GREEN. Working tree clean. v1.4 Structured Notification Messages milestone is CLOSED.
  </done>
</task>

</tasks>

<verification>
End-of-phase gate (every command from VALIDATION.md regression matrix for Plan 21-03):

```
npm run check                                                                                  # SC #5 -- full pipeline GREEN
npm test 2>&1 | grep -E "^# tests [0-9]+"                                                      # observed-total in 1188±5 range
grep -cE "SNM-(22|24|25|27|28|29|32)" .planning/REQUIREMENTS.md                                # closed-requirements present (SNM-23 excluded per B6)
grep -c "v1.4 Structured Notification Messages" CHANGELOG.md                                   # CHANGELOG entry shipped
! grep -E '\{OBSERVED_TEST_COUNT\}|\$\{' CHANGELOG.md                                          # template substitution complete (B7)
grep -c "completed_phases: 9" .planning/STATE.md                                               # STATE.md milestone math
grep -c "percent: 100" .planning/STATE.md                                                      # STATE.md milestone math
grep -c "v1.4 Structured Notification Messages" .planning/PROJECT.md                           # PROJECT.md validated entry
test -f .planning/phases/21-final-teardown-green-gate/21-SUMMARY.md                            # SUMMARY artifact
git status --porcelain | (! grep -q .)                                                         # working tree clean
git log --grep="^(chore|refactor|docs)\\(21\\)" --pretty=%H | head -3 | wc -l                  # 3 Phase 21 commits land (N3)
```

Manual review per VALIDATION.md "Manual-Only Verifications":
- CHANGELOG entry shape mirrors v1.3 entry per PATTERNS.md row 1.
- `.planning/STATE.md` SessionContinuity / OperatorNextSteps reflect milestone-closed state.
- `.planning/REQUIREMENTS.md` per-section `[ ]`/`[x]` agrees with traceability-table status for every Phase-21-owned SNM ID (cross-reconciliation done in Task 21-03-04, with SNM-23 explicitly excluded per B6).
</verification>

<success_criteria>
Closes (from ROADMAP §Phase-21 SC list):
- SC #5 (`npm run check` GREEN + test-count reconciled) -- Tasks 21-03-01 + 21-03-05.

Closes (from REQUIREMENTS.md):
- SNM-32 (`npm run check` GREEN after all migrations land).

Records (no new requirement closure but milestone-tracking artifacts updated):
- v1.4 milestone closed in `.planning/STATE.md` + `.planning/PROJECT.md`.
- CHANGELOG entry for v1.4.
- REQUIREMENTS.md traceability reconciled for Phase-21-owned IDs (SNM-23 left for separate quick-fix per B6).
- `21-SUMMARY.md` SC verification matrix per PATTERNS.md.
</success_criteria>

<output>
Create `.planning/phases/21-final-teardown-green-gate/21-SUMMARY.md` (Task 21-03-05). After commit, return the standard PLAN COMPLETE marker to the orchestrator.
</output>
</content>
</invoke>
