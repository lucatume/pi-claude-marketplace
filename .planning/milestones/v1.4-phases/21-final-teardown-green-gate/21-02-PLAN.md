---
phase: 21
plan: 21-02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/shared/errors.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  - extensions/pi-claude-marketplace/orchestrators/types.ts
  - extensions/pi-claude-marketplace/edge/handlers/tools.ts
  - extensions/pi-claude-marketplace/edge/args-schema.ts
  - extensions/pi-claude-marketplace/shared/grammar/
  - extensions/pi-claude-marketplace/presentation/
  - tests/architecture/notify-types.test.ts
  - tests/architecture/scope-order-drift.test.ts
  - tests/shared/notify.test.ts
  - tests/presentation/
  - package.json
  - eslint.config.js
autonomous: true
requirements: [SNM-22, SNM-29]
must_haves:
  truths:
    - "Per ROADMAP §Phase-21 SC #2: V1 severity-named wrappers (notifySuccess, notifyWarning, notifyError) and the V1 3-arg notifyUsageError signature are absent from shared/notify.ts."
    - "Per ROADMAP §Phase-21 SC #4 (shared/grammar arm): shared/grammar/ is absent on disk; REASONS / Reason / STATUS_TOKENS / StatusToken / MARKERS / Marker / PATTERN_CLASSES / PatternClass are declared inline in shared/notify.ts as the single source of truth."
    - "presentation/ and tests/presentation/ are absent on disk; the 5 still-active utilities are relocated per D-21-02 with one correction: sourceLogical/ParsedSource already live in domain/source.ts (RESEARCH §8 CORRECTION 2), so the work is one consumer migration (edge/handlers/tools.ts) plus a BLOCK C amendment."
    - "BLOCK C strategy LOCKED: Option A per RESEARCH §8 CORRECTION 2 -- amend BLOCK C's `edge` zone to remove `./extensions/pi-claude-marketplace/domain` from its `from:` array (smallest blast radius; only tools.ts currently benefits; BLOCK C still enforces every other cross-zone direction)."
    - "renderMarketplaceList + MarketplaceListEntry are DELETED (not inlined) per RESEARCH §8 CORRECTION 6 -- they have zero live callers."
    - "tests/shared/notify.test.ts is DELETED in the same atomic commit as the V1 wrapper removal per RESEARCH §8 CORRECTION 1 -- its 7 tests are LIVE calls to notifySuccess/notifyWarning/notifyError; without the delete `npm test` fails at compile."
    - "tests/architecture/scope-order-drift.test.ts string-literal references to presentation/sort.ts are updated to shared/notify.ts per RESEARCH §8 CORRECTION 5."
    - "package.json test (line 76) and test:coverage:unit (line 80) globs no longer reference the `presentation` brace entry."
    - "edge/args-schema.ts callback parameter notifyError → onError per D-21-02; consumer handlers pass inline arrow functions so no caller-side rename is needed."
    - "causeChainTrailer import in shared/notify.ts STAYS -- used at line 1067 in V2 per-plugin renderer per RESEARCH §8 CORRECTION 9."
    - "After commit, `npm run check` (typecheck + lint + format + tests) is GREEN."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "V2-only surface: notify(ctx, pi, NotificationMessage) + notifyUsageError(ctx, UsageErrorMessage) + inlined REASONS/STATUS_TOKENS/MARKERS/PATTERN_CLASSES + compareByNameThenScope. No V1 wrappers. ~1235 lines."
      contains: "export const REASONS"
    - path: "extensions/pi-claude-marketplace/shared/errors.ts"
      provides: "+composeErrorWithCauseChain 1-line helper adjacent to causeChainTrailer."
      contains: "composeErrorWithCauseChain"
    - path: "extensions/pi-claude-marketplace/domain/source.ts"
      provides: "Canonical home for sourceLogical + ParsedSource (already present pre-Phase-21; no edit required)."
      contains: "sourceLogical"
  key_links:
    - from: "extensions/pi-claude-marketplace/edge/handlers/tools.ts"
      to: "extensions/pi-claude-marketplace/domain/source.ts"
      via: "direct import"
      pattern: "domain/source\\.ts"
    - from: "tests/architecture/notify-types.test.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts"
      via: "type-only dynamic import string at line 87 (post-update)"
      pattern: "shared/notify\\.ts"
    - from: "tests/architecture/scope-order-drift.test.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts"
      via: "error-message + allowlist string literal (lines 53 + 158, post-update)"
      pattern: "shared/notify\\.ts"
---

<objective>
Consolidate the v1.4 messaging surface into a single atomic commit per D-21-06 + D-21-08: delete V1 wrappers from `shared/notify.ts` per D-21-05 (SNM-22 closure); inline `shared/grammar/*` into `shared/notify.ts` and delete the `shared/grammar/` directory per D-21-01 (SNM-29 closure); execute the `presentation/` clean-sweep per D-21-02 (5 relocations + directory delete + `tests/presentation/` delete) with RESEARCH corrections applied; rename `edge/args-schema.ts` callback parameter to `onError`; update 9 consumer import sites; update 1 edge consumer (`tools.ts`) for the cross-zone direct import; amend BLOCK C's `edge` zone to allow `domain/`; update 2 architecture-test string references (`notify-types.test.ts:87` + `scope-order-drift.test.ts:53,158`); delete `tests/shared/notify.test.ts` (RESEARCH §8 CORRECTION 1); update `package.json` `presentation` glob in both `test` and `test:coverage:unit` scripts.

Purpose: Finish the v1.3-to-v1.4 messaging transition by making `shared/notify.ts` the single source of truth for the user-output grammar (types + closed-set runtime arrays + renderer + entry points) and retiring every V1-era leaf module that the V2 surface superseded.

Output: ~46 file edits in a single atomic commit (well under the 200-file split threshold per D-21-08); `shared/notify.ts` grows from ~1175 to ~1235 lines (+60 net); 4 directories disappear (`shared/grammar/`, `presentation/`, `tests/presentation/`) plus one test file (`tests/shared/notify.test.ts`); `npm run check` GREEN at the post-edit state.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-final-teardown-green-gate/21-CONTEXT.md
@.planning/phases/21-final-teardown-green-gate/21-RESEARCH.md
@.planning/phases/21-final-teardown-green-gate/21-PATTERNS.md
@.planning/phases/21-final-teardown-green-gate/21-VALIDATION.md
@.planning/phases/21-final-teardown-green-gate/21-01-SUMMARY.md
@CLAUDE.md
@docs/output-catalog.md
@docs/messaging-style-guide.md
</context>

<tasks>

<task id="21-02-01" type="execute">
  <name>Task 1: Add composeErrorWithCauseChain to shared/errors.ts (relocate from presentation/cause-chain.ts before consumers migrate)</name>
  <files>extensions/pi-claude-marketplace/shared/errors.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/errors.ts
    - extensions/pi-claude-marketplace/presentation/cause-chain.ts
    - .planning/phases/21-final-teardown-green-gate/21-CONTEXT.md (D-21-02 composeErrorWithCauseChain → shared/errors.ts)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Modification Patterns: shared/errors.ts row + Claude's discretion on post-inline shape)
  </read_first>
  <action>
    **Pre-condition gate (per B5):** Before adding `composeErrorWithCauseChain`, confirm `extensions/pi-claude-marketplace/shared/errors.ts` does NOT already export it. Run:
    ```bash
    grep -c "composeErrorWithCauseChain" extensions/pi-claude-marketplace/shared/errors.ts
    ```
    This MUST return `0` before proceeding. If it returns >0, the helper already exists; investigate (could be a leftover from a prior partial migration) and abort the add -- there is nothing to relocate.

    1. Read `presentation/cause-chain.ts` to capture the JSDoc style and the literal implementation of `composeErrorWithCauseChain`. The function body is essentially `const trailer = causeChainTrailer(err); return trailer === "" ? errorMessage(err) : ${errorMessage(err)}\n\n${trailer};` (1-line composition).

    2. In `extensions/pi-claude-marketplace/shared/errors.ts`, add the `composeErrorWithCauseChain` function IMMEDIATELY AFTER the existing `causeChainTrailer` export (per PATTERNS.md "place immediately AFTER causeChainTrailer (line 76 region) so the 1-line composer lives next to its dependency"). Preserve the JSDoc from `presentation/cause-chain.ts` lines 16-29 -- orchestrator-aggregation rationale + the "byte-identical to notifyError's auto-trailing" cross-reference is the established style.

    3. Export as a named function (do NOT inline at the 3 call sites -- per CONTEXT Claude's Discretion default: KEEP as exported named function for readability; the 3 callers and `shared/errors.ts` `causeChainTrailer` callsite locality justify the named export).

    4. Do NOT modify `causeChainTrailer` or any other existing export in `shared/errors.ts`.

    5. Imports needed: ensure `causeChainTrailer` is in scope (already a local function) and `errorMessage` is exported (already line 1).
  </action>
  <verify>
    <automated>
      <command>grep -c "^export function composeErrorWithCauseChain" extensions/pi-claude-marketplace/shared/errors.ts</command>
      <command>grep -c "^export function causeChainTrailer" extensions/pi-claude-marketplace/shared/errors.ts</command>
      <command>npm run typecheck</command>
    </automated>
  </verify>
  <done>
    `shared/errors.ts` exports `composeErrorWithCauseChain(err: unknown): string` immediately after `causeChainTrailer`. JSDoc mirrors the relocated style. The pre-condition grep returned 0 before the edit; the post-condition grep `grep -c "^export function composeErrorWithCauseChain" extensions/pi-claude-marketplace/shared/errors.ts` returns 1. `npm run typecheck` exits 0 with both the old `presentation/cause-chain.ts` and the new `shared/errors.ts` declaration live (consumer migration happens in later tasks).
  </done>
</task>

<task id="21-02-02" type="execute">
  <name>Task 2: Rewrite shared/notify.ts header + delete V1 wrappers + inline grammar consts + add compareByNameThenScope (re-export verified, not "added")</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts
    - extensions/pi-claude-marketplace/shared/grammar/reasons.ts
    - extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts
    - extensions/pi-claude-marketplace/shared/grammar/markers.ts
    - extensions/pi-claude-marketplace/shared/grammar/pattern-classes.ts
    - extensions/pi-claude-marketplace/presentation/sort.ts
    - .planning/phases/21-final-teardown-green-gate/21-CONTEXT.md (D-21-01 grammar inline + D-21-02 compareByNameThenScope relocation + D-21-05 V1 wrapper deletion)
    - .planning/phases/21-final-teardown-green-gate/21-RESEARCH.md (§4 line numbers VERIFIED -- lines 68, 73, 96, 127, 135-144; §8 CORRECTION 8 -- V1 3-arg branch is 135-144 not 137-145; §8 CORRECTION 9 -- causeChainTrailer STAYS)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Modification Patterns: shared/notify.ts centerpiece row -- placement guidance, JSDoc mirroring rules)
  </read_first>
  <action>
    All edits to `extensions/pi-claude-marketplace/shared/notify.ts` in a single pass. **Step ordering (per W3):** do ALL deletions first, then ALL additions, then the header rewrite last; identify deletions by unique name anchors (function names, import specifiers) rather than absolute line numbers since cumulative deletions shift lines.

    **PHASE 1 -- DELETIONS (use name anchors, not absolute lines):**

    1. **Delete the line that imports `Reason`:** delete the entire statement `import type { Reason } from "./grammar/reasons.ts";`.

    2. **Delete the re-export line for `Reason`:** delete the entire statement `export type { Reason } from "./grammar/reasons.ts";`.

    3. **Delete the V1 wrapper functions by name anchor** (each is a 4- to ~24-line block starting with the matching `export function` line and ending at its closing `}`):
       - DELETE the function whose first line matches `^export function notifySuccess\(` through its closing `}` (4 lines including JSDoc).
       - DELETE the function whose first line matches `^export function notifyWarning\(` through its closing `}` (4 lines including JSDoc).
       - DELETE the function whose first line matches `^export function notifyError\(` through its closing `}` along with its preceding JSDoc block (~24 lines total).
       - DELETE the V1 `notifyUsageError` overload signature line: the standalone `export function notifyUsageError(ctx: ExtensionContext, message: string, usageBlock?: string): void;` line along with its shared JSDoc block ABOVE the V2 overload signature.
       - DELETE the V1 branch inside the `notifyUsageError` implementation: the entire `if (typeof message === "string") { … }` block (RESEARCH §8 CORRECTION 8 confirms this is the 135-144 region pre-edit; post-deletion the lines will have shifted, so anchor on the `if (typeof message === "string")` pattern). After removing that branch, also unwrap the `else { … }` around the V2 emission so the V2 body becomes the function body.

    4. **Restructure the implementation function signature.** The new signature is:
       ```
       export function notifyUsageError(ctx: ExtensionContext, message: UsageErrorMessage): void {
         ctx.ui.notify(`${message.message}\n\n${message.usage}`, "error");
       }
       ```
       Delete the `string | UsageErrorMessage` union and the `usageBlock?: string` parameter from the V2 implementation signature. Preserve the byte-equality contract -- the V2 path output is unchanged.

    **PHASE 2 -- ADDITIONS (after all deletions are applied):**

    5. **Inline grammar consts + types per D-21-01.** Place the 4 declarations near the top alongside existing v1.4 type declarations (per CONTEXT Claude's Discretion: "near the top alongside existing type declarations for a single contiguous v1.4 surface section"). Copy the const tuples and type unions VERBATIM from the corresponding `shared/grammar/*.ts` source files. Each declaration exports BOTH the runtime const and the derived type:
       ```
       export const REASONS = [...] as const;
       export type Reason = (typeof REASONS)[number];

       export const STATUS_TOKENS = [...] as const;
       export type StatusToken = (typeof STATUS_TOKENS)[number];

       export const MARKERS = [...] as const;
       export type Marker = (typeof MARKERS)[number];

       export const PATTERN_CLASSES = [...] as const;
       export type PatternClass = (typeof PATTERN_CLASSES)[number];
       ```
       Preserve the source-file JSDoc tone (each tuple has an existing JSDoc explaining the closed-set rationale; copy it forward).

    6. **`StatusToken` export is already covered by step 5** (per B4 -- the `export type StatusToken = (typeof STATUS_TOKENS)[number]` line in step 5 IS the export; there is no separate re-export to add). This step is verification-only via the `<automated>` grep below; no source change here.

    7. **Add `compareByNameThenScope` + `Sortable` interface inline per D-21-02.** Read `presentation/sort.ts` and copy the exported `compareByNameThenScope` function + the structural `Sortable` interface VERBATIM. Place near the bottom of the file alongside other comparator/helper utilities (per CONTEXT Claude's Discretion). Preserve the JSDoc that explains the MSG-GR-3 + `sensitivity: 'base'` invariants. Export as `export function compareByNameThenScope(a: Sortable, b: Sortable): number {...}` and `export interface Sortable {...}`.

    8. **Verify `causeChainTrailer` import line 3 STAYS** -- RESEARCH §8 CORRECTION 9 confirms it's still used by the V2 per-plugin renderer at line 1067. Do NOT remove it.

    9. **Verify `assertNever` import on line 3 STAYS** -- V2 `notify()` switch uses it for exhaustiveness.

    **PHASE 3 -- HEADER REWRITE (last; line numbers have stabilized after PHASES 1+2):**

    10. **Rewrite the file header comment block.** Strip the V1 vs V2 dual-surface narrative. New header documents V2-only state:
       - Top paragraph: "shared/notify.ts is the SOLE sanctioned ctx.ui.notify call site (D-07). Severity is structural -- computed from message contents at notify time (D-16-11) rather than caller-supplied. The eslint per-file override in eslint.config.js (BLOCK B) disables `no-restricted-syntax` for this file."
       - Public-API paragraph: enumerate only the two V2 entry points:
         - `notify(ctx, pi, NotificationMessage)` -- single state-change entry; renders the marketplace/plugin tree to a string with computed severity (D-16-11), computed reload-hint trailer (D-16-12 / D-16-13), and a single soft-dep probe threaded into per-row markers (D-16-15).
         - `notifyUsageError(ctx, UsageErrorMessage)` -- argv-validation errors. On-the-wire string is `${message.message}\n\n${message.usage}` at "error" severity.
       - Closed-set-source paragraph: note that `REASONS`, `STATUS_TOKENS`, `MARKERS`, `PATTERN_CLASSES` consts + their respective type aliases (`Reason`, `StatusToken`, `Marker`, `PatternClass`) and the `compareByNameThenScope` comparator live in this file as the single source of truth for the v1.4 surface (D-21-01 + D-21-02).
       - Drop ALL references to `notifySuccess`, `notifyWarning`, `notifyError`, the V1 3-arg `notifyUsageError`, the Phase 16-20 migration window, and the "Phase 21 deletes" forward-references.
       - Update the V2 overload comment to remove any "Coexists with V1 3-arg notifyUsageError" clause; keep the SNM-13 / D-16-02 citation.

    11. **Re-anchor the existing v1.4 type-model section comment** (the "// v1.4 structured notification type model (Phase 15)" header may need a slight re-indent depending on placement of inserted grammar consts; preserve the comment block content unchanged).

    Approximate post-edit line count: ~1235 (current ~1175 - 30 V1 wrapper lines + 60 inlined grammar + 10 comparator + 20 net header rewrite).
  </action>
  <verify>
    <automated>
      <command>! grep -nE "^export function notify(Success|Warning|Error)\\(" extensions/pi-claude-marketplace/shared/notify.ts</command>
      <command>! grep -n "usageBlock" extensions/pi-claude-marketplace/shared/notify.ts</command>
      <command>! grep -nE "import .* from \"\\./grammar/" extensions/pi-claude-marketplace/shared/notify.ts</command>
      <command>grep -c "^export const REASONS" extensions/pi-claude-marketplace/shared/notify.ts</command>
      <command>grep -c "^export const STATUS_TOKENS" extensions/pi-claude-marketplace/shared/notify.ts</command>
      <command>grep -c "^export const MARKERS" extensions/pi-claude-marketplace/shared/notify.ts</command>
      <command>grep -c "^export const PATTERN_CLASSES" extensions/pi-claude-marketplace/shared/notify.ts</command>
      <command>grep -c "^export type StatusToken" extensions/pi-claude-marketplace/shared/notify.ts</command>
      <command>grep -c "^export function compareByNameThenScope" extensions/pi-claude-marketplace/shared/notify.ts</command>
      <command>grep -A 3 "^export function notifyUsageError" extensions/pi-claude-marketplace/shared/notify.ts | grep -c 'ctx\.ui\.notify('</command>
      <command>grep -n "causeChainTrailer" extensions/pi-claude-marketplace/shared/notify.ts | head -2</command>
      <command>npm test -- tests/architecture/catalog-uat.test.ts</command>
    </automated>
  </verify>
  <done>
    `shared/notify.ts` exports zero V1 wrappers; the `notifyUsageError` signature is single-overload V2-only (`(ctx, UsageErrorMessage) => void`). REASONS / STATUS_TOKENS / MARKERS / PATTERN_CLASSES consts + their `Reason` / `StatusToken` / `Marker` / `PatternClass` type aliases are declared inline (each `export const X` grep returns 1; the `^export type StatusToken` grep returns 1 -- per B4 the type IS the export, no separate re-export). The V2 `notifyUsageError` body has exactly one `ctx.ui.notify(` call (per W6 positive check). `compareByNameThenScope` + `Sortable` interface are declared inline. `causeChainTrailer` import survives (used at the V2 renderer). File header is V2-only. Targeted catalog UAT passes (post-edit smoke). Other live code (V2 renderer, type model, helpers) is structurally unchanged. Typecheck will fail at this intermediate step because grammar consumers still import from `shared/grammar/*` -- that resolves in Task 21-02-03.
  </done>
</task>

<task id="21-02-03" type="execute">
  <name>Task 3: Migrate 9 consumer import sites -- Reason type imports from shared/grammar/reasons.ts → shared/notify.ts (8 orchestrators + 1 architecture test)</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/types.ts
    extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
    extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
    extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
    extensions/pi-claude-marketplace/orchestrators/import/execute.ts
    extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts
    extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
    tests/architecture/notify-types.test.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/types.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
    - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
    - tests/architecture/notify-types.test.ts
    - .planning/phases/21-final-teardown-green-gate/21-RESEARCH.md (§2 reasons.ts consumers table -- 10 live importers; presentation/compact-line.ts deleted by Task 21-02-05 so net 9 to migrate; line numbers verified)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Consumer Import Migration Precedents: shared/grammar/reasons.ts row)
  </read_first>
  <action>
    Update the import statement on the listed line of each file. Concrete before/after (string-equal patches, depth differs per file):

    1. **`extensions/pi-claude-marketplace/orchestrators/types.ts:10`** -- change `import type { Reason } from "../shared/grammar/reasons.ts";` to `import type { Reason } from "../shared/notify.ts";`.

    2. **`extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts:62`** -- change `import type { Reason } from "../../shared/grammar/reasons.ts";` to `import type { Reason } from "../../shared/notify.ts";`.

    3. **`extensions/pi-claude-marketplace/orchestrators/plugin/install.ts:122`** -- change `import type { Reason } from "../../shared/grammar/reasons.ts";` to `import type { Reason } from "../../shared/notify.ts";`.

    4. **`extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts:98`** -- change `import type { Reason } from "../../shared/grammar/reasons.ts";` to `import type { Reason } from "../../shared/notify.ts";`.

    5. **`extensions/pi-claude-marketplace/orchestrators/plugin/update.ts:116`** -- change `import type { Reason } from "../../shared/grammar/reasons.ts";` to `import type { Reason } from "../../shared/notify.ts";`.

    6. **`extensions/pi-claude-marketplace/orchestrators/import/execute.ts:25`** -- change `import type { Reason } from "../../shared/grammar/reasons.ts";` to `import type { Reason } from "../../shared/notify.ts";`.

    7. **`extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts:73`** -- change `import type { Reason } from "../../shared/grammar/reasons.ts";` to `import type { Reason } from "../../shared/notify.ts";`.

    8. **`extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts:131`** -- change `import type { Reason } from "../../shared/grammar/reasons.ts";` to `import type { Reason } from "../../shared/notify.ts";`.

    9. **`tests/architecture/notify-types.test.ts:87`** -- change `type _Reason = import("../../extensions/pi-claude-marketplace/shared/grammar/reasons.ts").Reason;` to `type _Reason = import("../../extensions/pi-claude-marketplace/shared/notify.ts").Reason;`. This is a type-only dynamic-import string; the assertion semantics (closed-set invariance proof) are unaffected.

    Do NOT touch comment-only references in `tests/e2e/install-soft-deps.test.ts:11`, `orchestrators/plugin/install.ts:1195`/`:1215`, `orchestrators/plugin/uninstall.ts:100`, `tests/shared/notify-v2.test.ts:1443`, `tests/orchestrators/plugin/reinstall.test.ts:1217,1233,1472,1527`, `presentation/README.md` -- these are descriptive prose; cosmetic, optional. Skip per RESEARCH §2 "comment-only refs (cosmetic update optional)".

    `presentation/compact-line.ts:58` is NOT in the migration list because Task 21-02-05 deletes the entire file.
  </action>
  <verify>
    <automated>
      <command>! grep -rn "from \"\\.\\./shared/grammar/reasons" extensions/pi-claude-marketplace/orchestrators/</command>
      <command>! grep -rn "from \"\\.\\./\\.\\./shared/grammar/reasons" extensions/pi-claude-marketplace/orchestrators/</command>
      <command>! grep -n "shared/grammar/reasons" tests/architecture/notify-types.test.ts</command>
      <command>grep -c "from \"../shared/notify\"" extensions/pi-claude-marketplace/orchestrators/types.ts</command>
      <command>npm run typecheck</command>
    </automated>
  </verify>
  <done>
    All 8 orchestrator files import `Reason` from `shared/notify.ts`. `notify-types.test.ts:87` string is updated. Zero live imports of `shared/grammar/reasons.ts` remain across `extensions/` + `tests/`. `npm run typecheck` exits 0 (assuming Task 21-02-02 landed; otherwise typecheck still fails because the inline declaration must exist).
  </done>
</task>

<task id="21-02-04" type="execute">
  <name>Task 4: Migrate 3 consumer import sites -- composeErrorWithCauseChain from presentation/cause-chain.ts → shared/errors.ts</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
    extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
    - .planning/phases/21-final-teardown-green-gate/21-RESEARCH.md (§2 cause-chain.ts consumers table -- 3 consumers; line numbers verified)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Consumer Import Migration Precedents: presentation/cause-chain.ts row)
  </read_first>
  <action>
    Update the import statement on the listed line of each file:

    1. **`extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts:69`** -- change `import { composeErrorWithCauseChain } from "../../presentation/cause-chain.ts";` to `import { composeErrorWithCauseChain } from "../../shared/errors.ts";`.

    2. **`extensions/pi-claude-marketplace/orchestrators/plugin/update.ts:83`** -- change `import { composeErrorWithCauseChain } from "../../presentation/cause-chain.ts";` to `import { composeErrorWithCauseChain } from "../../shared/errors.ts";`.

    3. **`extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts:109`** -- change `import { composeErrorWithCauseChain } from "../../presentation/cause-chain.ts";` to `import { composeErrorWithCauseChain } from "../../shared/errors.ts";`.

    If any of these files already imports from `"../../shared/errors.ts"` for other symbols (e.g., `errorMessage`, `causeChainTrailer`, `assertNever`), merge into the existing import statement to satisfy `import-x/order` (verify the import is grouped correctly). If a separate line is required (e.g., to keep type vs value imports distinct), add a new line in the correct sort position per existing project convention.

    Behavior is unchanged -- the helper's signature + implementation are byte-identical to the version Task 21-02-01 placed in `shared/errors.ts`.
  </action>
  <verify>
    <automated>
      <command>! grep -rn "presentation/cause-chain" extensions/pi-claude-marketplace/</command>
      <command>grep -c "composeErrorWithCauseChain" extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts</command>
      <command>grep -c "composeErrorWithCauseChain" extensions/pi-claude-marketplace/orchestrators/plugin/update.ts</command>
      <command>grep -c "composeErrorWithCauseChain" extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts</command>
      <command>npm run typecheck</command>
      <command>npm run lint -- extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts extensions/pi-claude-marketplace/orchestrators/plugin/update.ts extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts</command>
    </automated>
  </verify>
  <done>
    All 3 consumers import `composeErrorWithCauseChain` from `shared/errors.ts`. Zero live imports of `presentation/cause-chain.ts` remain. Per W1, `npm run lint` on the 3 edited files exits 0 -- `import-x/order` is satisfied at this intermediate step before downstream tasks consume the result. Function semantics unchanged.
  </done>
</task>

<task id="21-02-05" type="execute">
  <name>Task 5: Migrate 3 consumer import sites -- compareByNameThenScope from presentation/sort.ts → shared/notify.ts; update scope-order-drift.test.ts allowlist + error-message strings (lines 53 + 158, verified); relocate EntityErrorRow into install.ts file-local; migrate edge/handlers/tools.ts to direct domain/source.ts import</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
    extensions/pi-claude-marketplace/orchestrators/import/execute.ts
    extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
    extensions/pi-claude-marketplace/edge/handlers/tools.ts
    tests/architecture/scope-order-drift.test.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
    - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
    - extensions/pi-claude-marketplace/presentation/compact-line.ts
    - extensions/pi-claude-marketplace/edge/handlers/tools.ts
    - extensions/pi-claude-marketplace/domain/source.ts
    - tests/architecture/scope-order-drift.test.ts
    - .planning/phases/21-final-teardown-green-gate/21-RESEARCH.md (§2 sort.ts consumers + §8 CORRECTION 2 BLOCK C strategy + §8 CORRECTION 5 drift-guard string + §2 EntityErrorRow consumer)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Consumer Import Migration Precedents: sort + EntityErrorRow + sourceLogical rows)
  </read_first>
  <action>
    Six concrete patches in one task (atomic with the rest of the plan):

    1. **`extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts:70`** -- change `import { compareByNameThenScope } from "../../presentation/sort.ts";` to `import { compareByNameThenScope } from "../../shared/notify.ts";`. If reinstall.ts already imports other symbols from `shared/notify.ts`, merge.

    2. **`extensions/pi-claude-marketplace/orchestrators/plugin/update.ts:84`** -- change `import { compareByNameThenScope } from "../../presentation/sort.ts";` to `import { compareByNameThenScope } from "../../shared/notify.ts";`. Merge with existing notify import if present.

    3. **`extensions/pi-claude-marketplace/orchestrators/import/execute.ts:10`** -- change `import { compareByNameThenScope } from "../../presentation/sort.ts";` to `import { compareByNameThenScope } from "../../shared/notify.ts";`. Merge with existing notify import if present.

    4. **`extensions/pi-claude-marketplace/orchestrators/plugin/install.ts`** -- `EntityErrorRow` relocation:
       - DELETE line 121: `import type { EntityErrorRow } from "../../presentation/compact-line.ts";`
       - ADD a file-local interface declaration near the top (after the import block, before the first function/type). Read `presentation/compact-line.ts` to find the `EntityErrorRow` interface declaration and copy it VERBATIM (preserve JSDoc + property shapes). Place adjacent to the install.ts internal types that use it.

    5. **`extensions/pi-claude-marketplace/edge/handlers/tools.ts:43`** -- change `import { sourceLogical } from "../../presentation/marketplace-list.ts";` to `import { sourceLogical } from "../../domain/source.ts";`.
       **`extensions/pi-claude-marketplace/edge/handlers/tools.ts:46`** -- change `import type { ParsedSource } from "../../presentation/marketplace-list.ts";` to `import type { ParsedSource } from "../../domain/source.ts";`. If the two can be merged into one combined `import { sourceLogical, type ParsedSource } from "../../domain/source.ts";` per project convention, do so; otherwise keep them as two adjacent lines respecting the `import-x/order` group ordering.

       NOTE: this import would violate BLOCK C's current `edge` zone (`from:` blocks `./extensions/pi-claude-marketplace/domain`). Task 21-02-06 amends BLOCK C in `eslint.config.js` to allow this -- both edits MUST land in the same atomic commit so the post-commit `npm run lint` is clean.

    6. **`tests/architecture/scope-order-drift.test.ts` lines 53 + 158** per RESEARCH §8 CORRECTION 5 (BOTH lines VERIFIED in this revision via `grep -n "presentation/sort" tests/architecture/scope-order-drift.test.ts` -- line 53 contains `"/extensions/pi-claude-marketplace/presentation/sort.ts",` (an allowlist path string entry), line 158 contains the error-message template with the same path):
       - Line 53 is an allowlist path string. Update from `"/extensions/pi-claude-marketplace/presentation/sort.ts",` to `"/extensions/pi-claude-marketplace/shared/notify.ts",`.
       - Line 158 is an error-message string. Update from the embedded `extensions/pi-claude-marketplace/presentation/sort.ts` reference to `extensions/pi-claude-marketplace/shared/notify.ts` keeping surrounding prose intact (e.g., "Use the canonical `compareByNameThenScope` from `extensions/pi-claude-marketplace/shared/notify.ts`").
       The drift-guard test logic itself is unchanged -- only the string literals it asserts on / reports.
  </action>
  <verify>
    <automated>
      <command>! grep -rn "presentation/sort" extensions/pi-claude-marketplace/ tests/</command>
      <command>! grep -n "presentation/compact-line" extensions/pi-claude-marketplace/orchestrators/plugin/install.ts</command>
      <command>grep -c "interface EntityErrorRow" extensions/pi-claude-marketplace/orchestrators/plugin/install.ts</command>
      <command>grep -n "domain/source" extensions/pi-claude-marketplace/edge/handlers/tools.ts</command>
      <command>! grep -n "presentation/marketplace-list" extensions/pi-claude-marketplace/edge/handlers/tools.ts</command>
      <command>! grep -n "presentation/sort\.ts" tests/architecture/scope-order-drift.test.ts</command>
      <command>grep -c "shared/notify" tests/architecture/scope-order-drift.test.ts</command>
      <command>npm run typecheck</command>
    </automated>
  </verify>
  <done>
    All 3 comparator consumers import from `shared/notify.ts`. `EntityErrorRow` is declared file-local in `install.ts`. `edge/handlers/tools.ts` imports `sourceLogical` + `ParsedSource` from `domain/source.ts`. Zero live imports of `presentation/sort.ts` or `presentation/compact-line.ts` remain. `scope-order-drift.test.ts` strings updated at BOTH lines 53 (allowlist path entry) and 158 (error-message template) -- the post-edit pattern-based assertion `! grep -n "presentation/sort\.ts" tests/architecture/scope-order-drift.test.ts` (per W2) returns no matches. Typecheck passes; lint will fail at this intermediate point because BLOCK C still blocks the new edge → domain import -- Task 21-02-06 fixes that next.
  </done>
</task>

<task id="21-02-06" type="execute">
  <name>Task 6: Amend eslint.config.js BLOCK C -- remove `./extensions/pi-claude-marketplace/domain` from `edge` zone's `from:` array (RESEARCH Option A -- narrowest blast radius); delete dead `presentation` zone references in same commit</name>
  <files>eslint.config.js</files>
  <read_first>
    - eslint.config.js
    - .planning/phases/21-final-teardown-green-gate/21-RESEARCH.md (§8 CORRECTION 2 -- three options enumerated; Option A recommended)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Modification Patterns: optional BLOCK C zone amendment row)
  </read_first>
  <action>
    In `eslint.config.js`, locate the BLOCK C `import-x/no-restricted-paths` zone configured at `target: "./extensions/pi-claude-marketplace/edge"` (in the current file at lines 369-378). The current `from:` array contains 4 entries:
    - `./extensions/pi-claude-marketplace/bridges`
    - `./extensions/pi-claude-marketplace/domain`
    - `./extensions/pi-claude-marketplace/transaction`
    - `./extensions/pi-claude-marketplace/persistence`

    Delete the `"./extensions/pi-claude-marketplace/domain",` line from this `from:` array. The 3 remaining entries stay. Update the message string at the bottom of the same zone (currently `"edge/ may only import from orchestrators/, presentation/, shared/, platform/."`) to reflect the new policy:
    `"edge/ may only import from orchestrators/, shared/, platform/, domain/."` (drop `presentation/`, add `domain/` -- the `presentation` zone is deleted in Plan 21-02 so the message must not advertise it).

    Per D-21-02 BLOCK C implications + RESEARCH §8 CORRECTION 2 recommendation: this is the smallest amendment. All OTHER zones in BLOCK C (orchestrators, bridges, domain target, transaction, persistence, platform, shared) remain untouched. The `domain` TARGET zone still blocks upward imports from edge -- but we're not changing that zone; we're changing the `edge` TARGET zone's `from:` list. Verify the two are not the same zone definition.

    **Per N2 (optional polish promoted to in-scope):** delete any other dead `presentation/` references inside BLOCK C in the same commit (the standalone `presentation` TARGET zone -- a few lines -- and any `presentation` token in other zones' `from:` arrays or message strings). This aligns with the "stock ESLint with retained BLOCKS A/B/C/D/E" promise: after Plan 21-02, BLOCK C should reference no path that no longer exists. The delta is ~3-5 lines; doing it now avoids a follow-up cleanup pass.

    Append a comment above the amended zone explaining the Phase 21 amendment: `// D-21-02 Phase 21: edge/ may now import domain/ directly; the prior re-export-via-presentation/ hack is retired.`
  </action>
  <verify>
    <automated>
      <command>npm run lint</command>
      <command>grep -A 6 'target: "./extensions/pi-claude-marketplace/edge"' eslint.config.js | (! grep -q "/domain\",")</command>
      <command>grep -A 12 'target: "./extensions/pi-claude-marketplace/edge"' eslint.config.js | grep -c "edge/ may only import"</command>
      <command>! grep -n "presentation" eslint.config.js</command>
    </automated>
  </verify>
  <done>
    BLOCK C's `edge` zone `from:` array no longer contains `./extensions/pi-claude-marketplace/domain`. The message string is updated to reflect the new allowed list (no `presentation/` mention). All other dead `presentation/` references in BLOCK C are also gone (per N2). `npm run lint` exits 0 -- the `edge/handlers/tools.ts` → `domain/source.ts` import added by Task 21-02-05 is now allowed. All other zones in BLOCK C are unchanged.
  </done>
</task>

<task id="21-02-07" type="execute">
  <name>Task 7: Rename edge/args-schema.ts callback parameter notifyError → onError (4 spots + JSDoc + file header comment)</name>
  <files>extensions/pi-claude-marketplace/edge/args-schema.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/args-schema.ts
    - .planning/phases/21-final-teardown-green-gate/21-CONTEXT.md (Plus minor cleanups section -- D-21-02 callback rename)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Modification Patterns: edge/args-schema.ts row -- 4 rename spots + 2 comment updates)
  </read_first>
  <action>
    Surgical rename inside `extensions/pi-claude-marketplace/edge/args-schema.ts`. Concrete spots (line numbers verified against current file):

    1. **Line 9 file header comment:** change `(\`notifyError\`)` to `(\`onError\`)`. The surrounding paragraph documents that notify is delivered via callback rather than imported; rename only the parenthetical identifier.

    2. **Line 28 `parseArgsOrNotify` parameter:** change `notifyError: (message: string) => void` to `onError: (message: string) => void`.

    3. **Line 33 `parseArgsOrNotify` body callsite:** change `notifyError(errorMessage(err));` to `onError(errorMessage(err));`.

    4. **Line 43 JSDoc snippet:** the JSDoc example at lines 38-54 contains the phrase `\`notifyError(usage)\`` describing the callback behavior. Change to `\`onError(usage)\``. Also update line 50 `notifyError` in the example code block (`}, notifyError);` → `}, onError);`).

    5. **Line 71 `parseCommandArgs` parameter:** change `notifyError: (message: string) => void,` to `onError: (message: string) => void,`.

    6. **Line 73 `parseCommandArgs` body -- inner call:** change `parseArgsOrNotify(args, notifyError);` to `parseArgsOrNotify(args, onError);`.

    7. **Line 84 `parseCommandArgs` body -- error path:** change `notifyError(schema.usage);` to `onError(schema.usage);`.

    NO other identifier in the file should be renamed (`errorMessage`, `notifyError(ctx, ...)` per JSDoc reference to the canonical `shared/notify.ts::notifyError` -- note: that wrapper is being deleted by Task 21-02-02, so update the JSDoc context paragraph at lines 9-13 to read "wraps the canonical `notify(ctx, pi, NotificationMessage)` / `notifyUsageError(ctx, UsageErrorMessage)` from `shared/notify.ts`" rather than referencing the deleted V1 wrapper). Re-read the comment block at lines 9-13 and update the V1 wrapper reference to V2 phrasing in the same pass.

    Caller handlers pass inline arrow functions (e.g., `(message) => { notifyUsageError(ctx, { message, usage: USAGE }); }`) per PATTERNS.md "no call-site changes needed" -- so no other files require updates from this rename. Verify by grep across `extensions/pi-claude-marketplace/edge/handlers/**` for any positional argument named `notifyError` passed to `parseCommandArgs` (should find none).
  </action>
  <verify>
    <automated>
      <command>! grep -n "notifyError" extensions/pi-claude-marketplace/edge/args-schema.ts</command>
      <command>grep -c "onError" extensions/pi-claude-marketplace/edge/args-schema.ts</command>
      <command>npm run typecheck</command>
      <command>! grep -rn "notifyError:" extensions/pi-claude-marketplace/edge/handlers/</command>
    </automated>
  </verify>
  <done>
    `edge/args-schema.ts` has zero occurrences of `notifyError`; the callback parameter is `onError` in both `parseArgsOrNotify` and `parseCommandArgs`. JSDoc references updated. File header comment updated to reference V2 entry points. Typecheck passes.
  </done>
</task>

<task id="21-02-08" type="execute">
  <name>Task 8: Delete tests/shared/notify.test.ts (RESEARCH §8 CORRECTION 1 -- 7 V1 wrapper tests that compile-fail post-Task-21-02-02)</name>
  <files>tests/shared/notify.test.ts</files>
  <read_first>
    - tests/shared/notify.test.ts
    - .planning/phases/21-final-teardown-green-gate/21-RESEARCH.md (§8 CORRECTION 1 -- 7 LIVE calls to V1 wrappers at lines 27/34/41/48/61/73/85)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Deletion Sweep Precedents: tests/shared/notify.test.ts row)
  </read_first>
  <action>
    `rm tests/shared/notify.test.ts` -- single 91-line file containing 7 tests, each calling `notifySuccess` / `notifyWarning` / `notifyError` directly. Without this delete, `npm test` fails at compile because the imports at lines 4-8 reference deleted symbols.

    Do NOT touch `tests/shared/notify-v2.test.ts` (41 V2 tests; covers the live `notify()` + `notifyUsageError()` surface per RESEARCH §1; survives Phase 21 unchanged).

    Do NOT touch any other file under `tests/shared/` (`atomic-json.test.ts`, `completion-cache.test.ts`, `errors.test.ts`, `errors-bridges.test.ts`, `fs-utils.test.ts`, `index-smoke.test.ts`, `path-safety.test.ts`, `vars.test.ts` per RESEARCH §1).
  </action>
  <verify>
    <automated>
      <command>! test -f tests/shared/notify.test.ts</command>
      <command>test -f tests/shared/notify-v2.test.ts</command>
      <command>npm test -- tests/shared/notify-v2.test.ts</command>
    </automated>
  </verify>
  <done>
    `tests/shared/notify.test.ts` absent. `tests/shared/notify-v2.test.ts` survives and passes. `npm test` (full suite) will succeed once the rest of the plan tasks complete -- this delete unblocks the compile.
  </done>
</task>

<task id="21-02-09" type="execute">
  <name>Task 9: Delete tests/presentation/ directory (11 test files, 133 tests)</name>
  <files>tests/presentation/</files>
  <read_first>
    - .planning/phases/21-final-teardown-green-gate/21-RESEARCH.md (§1 + §5 -- 11 files, 133 inner tests; per-file counts)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Deletion Sweep Precedents: tests/presentation/ row)
    - .planning/phases/21-final-teardown-green-gate/21-CONTEXT.md (D-21-02 Claude's Discretion: "tests/presentation/marketplace-list.test.ts assertion migration" -- check if any sourceLogical/ParsedSource tests are unique vs tests/domain/source.test.ts)
  </read_first>
  <action>
    1. **Pre-delete check (per CONTEXT Claude's Discretion):** read `tests/presentation/marketplace-list.test.ts` and `tests/domain/source.test.ts`. If `marketplace-list.test.ts` contains any `sourceLogical(...)` / `ParsedSource` assertions NOT already duplicated in `source.test.ts`, copy those test cases into `source.test.ts` BEFORE deleting `marketplace-list.test.ts`. Default expectation per RESEARCH §7: `tests/domain/source.test.ts` already covers `sourceLogical` (the re-export-through-presentation was only for cross-zone import); no assertion migration needed.

    2. **Pre-delete check (per CONTEXT Claude's Discretion):** read `tests/presentation/cause-chain.test.ts` and `tests/shared/errors.test.ts`. If unique `composeErrorWithCauseChain(...)` assertions exist, migrate them to `tests/shared/errors.test.ts` before deletion. Default expectation per PATTERNS.md: no migration needed; V2 byte-equality via `catalog-uat.test.ts` already covers the user-visible behavior.

    3. **Pre-delete check (sort/compareByNameThenScope):** decide whether to add a small `compareByNameThenScope` block to `tests/shared/notify-v2.test.ts` per CONTEXT Claude's Discretion + RESEARCH §5 (+0 to +2 tests planner-discretionary). Default: SKIP -- the comparator is exercised indirectly by every orchestrator test that produces multi-marketplace/multi-plugin output. Adding redundant unit coverage is low value.

    4. **Delete the entire directory:** `rm -rf tests/presentation/` -- removes 11 files (`cascade-summary.test.ts`, `cause-chain.test.ts`, `compact-line.test.ts`, `manual-recovery.test.ts`, `marketplace-list.test.ts`, `plugin-list.test.ts`, `reload-hint.test.ts`, `rollback-partial.test.ts`, `soft-dep.test.ts`, `sort.test.ts`, `version-arrow.test.ts`).

    5. Record whether any assertions were migrated in steps 1-3 (planner notes this in the eventual 21-02-SUMMARY.md).
  </action>
  <verify>
    <automated>
      <command>! test -d tests/presentation</command>
      <command>test -f tests/domain/source.test.ts</command>
      <command>test -f tests/shared/errors.test.ts</command>
      <command>test -f tests/shared/notify-v2.test.ts</command>
    </automated>
  </verify>
  <done>
    `tests/presentation/` absent. Any unique assertions migrated to natural-home test files (steps 1-3; default expectation: zero migration needed). Adjacent test files (`source.test.ts`, `errors.test.ts`, `notify-v2.test.ts`) survive.
  </done>
</task>

<task id="21-02-10" type="execute">
  <name>Task 10: Delete extensions/pi-claude-marketplace/presentation/ directory (12 source files + README.md)</name>
  <files>extensions/pi-claude-marketplace/presentation/</files>
  <read_first>
    - extensions/pi-claude-marketplace/presentation/index.ts
    - .planning/phases/21-final-teardown-green-gate/21-RESEARCH.md (§1 -- 12 source + 1 README; §8 CORRECTION 6 -- renderMarketplaceList has 0 live callers, simply delete)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Deletion Sweep Precedents: presentation/ row + Consumer Import Migration: renderMarketplaceList row)
  </read_first>
  <action>
    Single command: `rm -rf extensions/pi-claude-marketplace/presentation/` -- removes the directory entirely. Specific files deleted (verified inventory):
    `cascade-summary.ts`, `cause-chain.ts`, `compact-line.ts`, `index.ts`, `manual-recovery.ts`, `marketplace-list.ts`, `plugin-list.ts`, `reload-hint.ts`, `rollback-partial.ts`, `soft-dep.ts`, `sort.ts`, `version-arrow.ts`, `README.md` = 12 source + 1 README = 13 entries.

    Pre-condition: all 4 consumer-migration tasks (21-02-03 through 21-02-05) have already updated their imports. If any task above was skipped, the typecheck will fail; do not proceed to the commit task.

    Per RESEARCH §8 CORRECTION 6 -- `renderMarketplaceList` + `MarketplaceListEntry` have ZERO live callers; delete them WITHOUT inlining anywhere. The comment-only references at `orchestrators/marketplace/list.ts:18` and `tests/architecture/catalog-uat.test.ts:15` are descriptive prose; cosmetic update is optional and SKIPPED per default (no behavior depends on the comment).
  </action>
  <verify>
    <automated>
      <command>! test -d extensions/pi-claude-marketplace/presentation</command>
      <command>! grep -rn "from \"\\.\\./presentation/" extensions/pi-claude-marketplace/</command>
      <command>! grep -rn "from \"\\.\\./\\.\\./presentation/" extensions/pi-claude-marketplace/</command>
      <command>! grep -rn "from \"\\.\\./\\.\\./extensions/pi-claude-marketplace/presentation/" tests/</command>
      <command>npm run typecheck</command>
    </automated>
  </verify>
  <done>
    `extensions/pi-claude-marketplace/presentation/` absent. Zero live imports of `presentation/*` anywhere in extension or test code. `npm run typecheck` exits 0.
  </done>
</task>

<task id="21-02-11" type="execute">
  <name>Task 11: Delete extensions/pi-claude-marketplace/shared/grammar/ directory (4 source files)</name>
  <files>extensions/pi-claude-marketplace/shared/grammar/</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/grammar/reasons.ts
    - .planning/phases/21-final-teardown-green-gate/21-RESEARCH.md (§1 -- 4 files: markers.ts, pattern-classes.ts, reasons.ts, status-tokens.ts; §2 -- 0 live consumers of pattern-classes.ts + markers.ts, 0 net consumers of status-tokens.ts post-presentation-delete, 0 net consumers of reasons.ts post-Tasks-21-02-03)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Deletion Sweep Precedents: shared/grammar/ row)
  </read_first>
  <action>
    Single command: `rm -rf extensions/pi-claude-marketplace/shared/grammar/` -- removes the directory entirely. Specific files deleted: `markers.ts`, `pattern-classes.ts`, `reasons.ts`, `status-tokens.ts`.

    Pre-condition (clarified per W9): Task 21-02-02 already inlined REASONS / STATUS_TOKENS / MARKERS / PATTERN_CLASSES into `shared/notify.ts`. Task 21-02-03 already migrated the 9 `Reason` import consumers. **Task 21-02-10 already deleted the ENTIRE `presentation/` directory** (not just `compact-line.ts`), which removed the last `StatusToken` importer (`presentation/compact-line.ts`) along with all other presentation files. If any of those tasks was skipped, the typecheck will fail; do not proceed to the commit task.
  </action>
  <verify>
    <automated>
      <command>! test -d extensions/pi-claude-marketplace/shared/grammar</command>
      <command>! grep -rn "shared/grammar/" extensions/pi-claude-marketplace/</command>
      <command>grep -rn "shared/grammar/" tests/ | grep -vE "//|^\s*\*" | (! grep -q .)</command>
      <command>npm run typecheck</command>
    </automated>
  </verify>
  <done>
    `extensions/pi-claude-marketplace/shared/grammar/` absent. Zero live `shared/grammar/*` imports anywhere in `extensions/`. Per W9 fix: the `tests/` grep correctly checks for "no non-comment hits" -- pipe strips comment lines (`//` and ` * `) and then asserts the result is empty via `(! grep -q .)`. `npm run typecheck` exits 0.
  </done>
</task>

<task id="21-02-12" type="execute">
  <name>Task 12: Update package.json -- remove `presentation` from test (line 76) and test:coverage:unit (line 80) glob brace expansions (RESEARCH §8 CORRECTION 7)</name>
  <files>package.json</files>
  <read_first>
    - package.json
    - .planning/phases/21-final-teardown-green-gate/21-RESEARCH.md (§8 CORRECTION 7 -- lines 76 + 80 reference `presentation` in brace expansion)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Modification Patterns: package.json row -- Plan 21-02 owns the presentation portion in BOTH scripts)
  </read_first>
  <action>
    Two patches in `package.json` (string-literal edits inside the JSON values).

    **Per B1 + B3 fix: do NOT assume `presentation` is at the tail of the brace expansion. Plan 21-01 Task 21-01-04 was revised to preserve brace order, so `presentation` may be anywhere in the brace. Use `grep -n` to find the current order and remove the `presentation` token surgically from wherever it appears.**

    **Step 1 -- discover the current brace expansion:**
    ```bash
    grep -n "node --test" package.json
    ```
    Read the matched lines (`test` and `test:coverage:unit`). The brace will look like `{architecture,bridges,domain,edge,helpers,orchestrators,persistence,presentation,shared,transaction}` -- `presentation` sits between `persistence` and `shared` based on the alphabetical-ish ordering established pre-Phase-21.

    **Step 2 -- surgical token removal (handles any position):**
    For each of the two script lines:
    - If `presentation` is followed by another token (e.g., `,presentation,shared` form): delete the substring `presentation,` (the token + the trailing comma) so the surrounding tokens close up cleanly.
    - If `presentation` is the last token in the brace (e.g., `,shared,transaction,presentation` form): delete the substring `,presentation` (the leading comma + the token).
    - Apply the matching form to BOTH `test` (line 76) and `test:coverage:unit` (line 80).

    **Expected post-edit forms (assuming the default pre-edit `,presentation,shared` position):**

    1. **Line 76 (`test` script):**
       Before: `"test": "node --test \"tests/{architecture,bridges,domain,edge,helpers,orchestrators,persistence,presentation,shared,transaction}/**/*.test.ts\"",`
       After: `"test": "node --test \"tests/{architecture,bridges,domain,edge,helpers,orchestrators,persistence,shared,transaction}/**/*.test.ts\"",`

    2. **Line 80 (`test:coverage:unit` script):**
       Before: `"test:coverage:unit": "node --test --experimental-test-coverage --test-reporter=spec --test-reporter-destination=stdout --test-reporter=lcov --test-reporter-destination=coverage/unit.lcov \"tests/{architecture,bridges,domain,edge,helpers,orchestrators,persistence,presentation,shared,transaction}/**/*.test.ts\"",`
       After: `"test:coverage:unit": "node --test --experimental-test-coverage --test-reporter=spec --test-reporter-destination=stdout --test-reporter=lcov --test-reporter-destination=coverage/unit.lcov \"tests/{architecture,bridges,domain,edge,helpers,orchestrators,persistence,shared,transaction}/**/*.test.ts\"",`

    Do NOT touch any other line, dependency, or script. The `test:coverage:e2e` and `test:coverage:integration` scripts use different globs (`tests/e2e/**` and `tests/integration/**`) and are unaffected.
  </action>
  <verify>
    <automated>
      <command>! grep "presentation" package.json</command>
      <command>grep -c "node --test" package.json</command>
      <command>npm test 2>&amp;1 | grep -i "no files" | (! grep -q "presentation")</command>
    </automated>
  </verify>
  <done>
    `package.json` contains zero `presentation` substrings. Both `test` and `test:coverage:unit` script globs reflect the post-deletion directory layout. `npm test` does not emit "no files found" warnings for presentation. Other scripts unchanged.
  </done>
</task>

<task id="21-02-13" type="execute">
  <name>Task 13: Pre-commit gate + atomic single commit per D-21-08</name>
  <files>
    extensions/pi-claude-marketplace/shared/notify.ts
    extensions/pi-claude-marketplace/shared/errors.ts
    extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
    extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
    extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
    extensions/pi-claude-marketplace/orchestrators/import/execute.ts
    extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts
    extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
    extensions/pi-claude-marketplace/orchestrators/types.ts
    extensions/pi-claude-marketplace/edge/handlers/tools.ts
    extensions/pi-claude-marketplace/edge/args-schema.ts
    extensions/pi-claude-marketplace/shared/grammar/
    extensions/pi-claude-marketplace/presentation/
    tests/architecture/notify-types.test.ts
    tests/architecture/scope-order-drift.test.ts
    tests/shared/notify.test.ts
    tests/presentation/
    package.json
    eslint.config.js
  </files>
  <read_first>
    - CLAUDE.md (Git section: pre-commit FIRST; Conventional Commits; ≤72 char title; ≤80 char body; NEVER --no-verify)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Shared cross-cutting patterns: atomic single-commit discipline + Conventional Commits prefix)
    - .planning/phases/21-final-teardown-green-gate/21-VALIDATION.md (Plan 21-02 regression matrix rows)
  </read_first>
  <action>
    1. Run `npm run check` and confirm exit code 0. This is the gate. If it fails, diagnose (typecheck / lint / format / tests) and fix BEFORE committing; do NOT commit until GREEN.
    2. Stage the changes with explicit `git add --` referencing every entry in the `files_modified` frontmatter list (covers both file edits and directory deletions). Stage BEFORE running pre-commit so the canonical `git diff --cached --name-only` pattern works.
    3. Run pre-commit using the canonical CLAUDE.md pattern that handles modifications + additions + deletions deterministically (per B3):
       ```bash
       pre-commit run --files $(git diff --cached --name-only)
       ```
       Confirm exit code 0. Fix any failures, re-stage, and re-run until clean. NEVER use `--no-verify`. Branch `gsd/v1.3-replan-catalog` is NOT a worktree -- do NOT prefix with `SKIP=trufflehog`.
    4. Compose the commit body (HEREDOC). **Per W8 -- verify body line length ≤80 chars before invoking `git commit`:**
       ```bash
       body=$(cat <<'EOF'
       <body text here>
       EOF
       )
       awk 'length>80' <<< "$body" | wc -l   # MUST be 0
       ```
       If the count is non-zero, find the offending line(s) (`awk 'length>80 {print NR": "$0}' <<< "$body"`) and rewrap before committing.
    5. Commit with a single Conventional Commits message. Title (≤72 chars):
       `refactor(21): consolidate shared/notify.ts + retire V1 + presentation`

       Body (lines ≤80 chars):
       ```
       Per D-21-01 + D-21-02 + D-21-05 + D-21-06 (Phase 21 Plan 21-02), execute
       the V1 messaging surface retirement and source consolidation in one
       atomic commit:

       - Delete V1 severity wrappers from shared/notify.ts: notifySuccess,
         notifyWarning, notifyError, V1 3-arg notifyUsageError overload + V1
         impl branch (RESEARCH §8 CORRECTION 8). The V2 notify(ctx, pi,
         NotificationMessage) and notifyUsageError(ctx, UsageErrorMessage)
         remain as the sole public entry points. SNM-22.
       - Inline REASONS / STATUS_TOKENS / MARKERS / PATTERN_CLASSES const
         tuples + their Reason / StatusToken / Marker / PatternClass type
         aliases into shared/notify.ts; delete shared/grammar/. Migrate the
         9 live Reason consumers (8 orchestrators + notify-types.test.ts:87
         dynamic-import string). SNM-29.
       - presentation/ clean-sweep: relocate composeErrorWithCauseChain to
         shared/errors.ts; relocate compareByNameThenScope + Sortable to
         shared/notify.ts; relocate EntityErrorRow file-local to
         orchestrators/plugin/install.ts; delete the entire presentation/
         directory + tests/presentation/ (133 tests). renderMarketplaceList
         is DELETED (no live caller per RESEARCH §8 CORRECTION 6); the
         re-export of sourceLogical/ParsedSource is DELETED in favor of
         edge/handlers/tools.ts importing domain/source.ts directly.
       - Amend eslint.config.js BLOCK C edge zone to allow domain/ and drop
         dead presentation/ references (Option A per RESEARCH §8
         CORRECTION 2 -- smallest blast radius).
       - Update tests/architecture/scope-order-drift.test.ts strings at
         lines 53 + 158 from presentation/sort.ts to shared/notify.ts
         (RESEARCH §8 CORRECTION 5).
       - Rename edge/args-schema.ts callback parameter notifyError to
         onError (4 spots + JSDoc + file-header comment).
       - DELETE tests/shared/notify.test.ts (7 V1 wrapper tests) atomically
         with the wrapper removal (RESEARCH §8 CORRECTION 1).
       - Update package.json test + test:coverage:unit globs to drop
         presentation entry (RESEARCH §8 CORRECTION 7).

       npm run check GREEN at the post-edit state. ~46 files modified.
       ```

       Use HEREDOC form per CLAUDE.md template. Do NOT use `--amend`.
    6. Run `git status` after the commit and confirm working tree is clean. Optionally `git show --stat HEAD` to confirm file count ≤200 per CONTEXT D-21-02 Claude's Discretion (expected ~46).
  </action>
  <verify>
    <automated>
      <command>npm run check</command>
      <command>! test -d extensions/pi-claude-marketplace/shared/grammar</command>
      <command>! test -d extensions/pi-claude-marketplace/presentation</command>
      <command>! test -d tests/presentation</command>
      <command>! test -f tests/shared/notify.test.ts</command>
      <command>! grep -nE "^export function notify(Success|Warning|Error)\\(" extensions/pi-claude-marketplace/shared/notify.ts</command>
      <command>! grep -rnE 'notify(Success|Warning|Error)\(' extensions/ tests/ | grep -vE '//|^\s*\*'</command>
      <command>npm test -- tests/architecture/catalog-uat.test.ts</command>
      <command>npm test -- tests/architecture/notify-types.test.ts</command>
      <command>npm test -- tests/architecture/markers-snapshot.test.ts</command>
      <command>npm test -- tests/architecture/scope-order-drift.test.ts</command>
      <command>npm test -- tests/shared/notify-v2.test.ts</command>
      <command>git log -1 --pretty=%s | grep -E "^refactor\\(21\\): consolidate"</command>
      <command>git log -1 --pretty=%B | awk 'length>80' | wc -l | grep -E "^0$"</command>
      <command>git status --porcelain | (! grep -q .)</command>
    </automated>
  </verify>
  <done>
    Single atomic commit landed. `npm run check` exits 0. Working tree clean. Commit body line-length compliance verified (per W8 -- `awk 'length>80'` returns 0 lines). SNM-22 closed (V1 wrappers absent from `shared/notify.ts`; zero live callers anywhere). SNM-29 closed (`shared/grammar/` absent; closed-set declarations inline in `shared/notify.ts`). Phase 5/7 markers + V2 catalog + V2 per-variant tests + drift-guards all GREEN.
  </done>
</task>

</tasks>

<verification>
End-of-plan gate (every command from VALIDATION.md regression matrix for Plan 21-02):

```
npm run check                                                                                  # full pipeline GREEN
! test -d extensions/pi-claude-marketplace/shared/grammar                                      # SNM-29 directory post-condition
! test -d extensions/pi-claude-marketplace/presentation                                        # SNM-29 directory post-condition
! test -d tests/presentation                                                                   # SNM-29 directory post-condition
! test -f tests/shared/notify.test.ts                                                          # RESEARCH §8 CORRECTION 1
! grep -nE "^export function notify(Success|Warning|Error)\\(" extensions/pi-claude-marketplace/shared/notify.ts  # SNM-22 deletion
! grep -rnE 'notify(Success|Warning|Error)\(' extensions/ tests/ | grep -vE '//|^\s*\*'       # SNM-22 zero live callers
! grep -rn "shared/grammar/" extensions/pi-claude-marketplace/                                 # SNM-29 zero live imports
! grep -rn "from \"\\.\\./presentation/" extensions/pi-claude-marketplace/                     # SNM-29 zero live imports
! grep -rn "from \"\\.\\./\\.\\./presentation/" extensions/pi-claude-marketplace/              # SNM-29 zero live imports
! grep -n "presentation" package.json                                                          # package.json glob hygiene
! grep -n "tests/lint-rules" package.json                                                      # (Plan 21-01 invariant re-verify)
npm test -- tests/architecture/catalog-uat.test.ts                                             # V2 catalog byte-equality
npm test -- tests/architecture/notify-types.test.ts                                            # Reason / StatusToken compile-time invariance
npm test -- tests/architecture/markers-snapshot.test.ts                                        # Phase 5/7 markers orthogonal-survival
npm test -- tests/architecture/scope-order-drift.test.ts                                       # scope-order drift-guard against shared/notify.ts
npm test -- tests/shared/notify-v2.test.ts                                                     # V2 per-variant renderer
```

Manual review per VALIDATION.md "Manual-Only Verifications":
- BLOCK C decision recorded in `<must_haves>`: Option A -- amend `edge` zone `from:` to remove `domain/`; dead `presentation/` references purged in same commit (N2).
- `git show --stat HEAD` file count ≤200 (expected ~46).
- `package.json` lines 76 + 80 contain no `presentation` substring.
</verification>

<success_criteria>
Closes (from ROADMAP §Phase-21 SC list):
- SC #2 (V1 wrappers absent from `shared/notify.ts`) -- Tasks 21-02-02 + 21-02-08 + 21-02-13.
- SC #4 (`shared/grammar/` decision -- DELETE arm) -- Tasks 21-02-02 + 21-02-03 + 21-02-11.

Closes (from REQUIREMENTS.md):
- SNM-22 (V1 severity wrappers + V1 3-arg `notifyUsageError` deleted from `shared/notify.ts`).
- SNM-29 (`shared/grammar/` deleted; closed sets inlined into `shared/notify.ts`).
</success_criteria>

<output>
Create `.planning/phases/21-final-teardown-green-gate/21-02-SUMMARY.md` when done, mirroring Plan 20-04 SUMMARY's per-SC + NO-CHANGE-invariant double-table shape per PATTERNS.md "Per-plan SC verification matrix in SUMMARY" pattern. Record the BLOCK C strategy chosen (Option A) and any test-assertion migrations performed in Task 21-02-09 steps 1-3 (default expectation: none).
</output>
</content>
</invoke>
