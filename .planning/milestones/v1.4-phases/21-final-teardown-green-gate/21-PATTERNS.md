# Phase 21: Final Teardown & GREEN Gate - Pattern Map

**Mapped:** 2026-05-27
**Files analyzed:** 0 new files, 17 modified files, 100+ deleted files across 4 directory sweeps
**Analogs found:** N/A for new files (none created); precedent-mapping for modifications + sweeps documented below

## Phase 21 framing -- this is NOT a net-new construction phase

Phase 21 ships ZERO new files. Every change is either:

| Category | Count | Example |
|---|---|---|
| **Deletion sweeps** | 4 directories + 3 single files | `tests/lint-rules/` (73), `presentation/` (13), `tests/presentation/` (11), `shared/grammar/` (4), plus `msg-rule-registry.test.ts` + `no-legacy-markers.test.ts` + `tests/shared/notify.test.ts` |
| **In-place modifications** | 6 files | `shared/notify.ts`, `shared/errors.ts`, `edge/args-schema.ts`, `persistence/migrate.ts`, `eslint.config.js`, `package.json` |
| **Consumer import-path updates** | 11 files | 8 orchestrators updating `shared/grammar/reasons.ts` -> `shared/notify.ts`; 3 updating `presentation/cause-chain.ts` -> `shared/errors.ts`; etc. |
| **Plan 21-03 closure docs** | 4 files | `CHANGELOG.md`, `.planning/STATE.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md` |

So instead of `(file_role, file_data_flow, closest_analog)` mappings, this PATTERNS.md surfaces:

1. **Modification patterns** -- per-file precedent for the *kind* of change being made.
2. **Deletion sweep precedents** -- per-sweep precedent commits + plan-shape inheritance.
3. **Consumer import migration precedents** -- the acceptance-criteria shape that prior phases use for "update all callers of X to Y".
4. **Closure-document precedents** -- the milestone-closure templates Plan 21-03 mirrors (CHANGELOG / STATE / PROJECT / REQUIREMENTS).

## What's NOT in this PATTERNS.md

The standard pattern-mapper output `## File Classification` table with `(role, data_flow, analog, match_quality)` rows is **intentionally omitted** -- there are no new files in Phase 21, so per-file role/data-flow classification doesn't apply. The deletion sweeps and modification patterns below replace that table.

---

## Modification Patterns

Each row: the file being modified, the kind of modification, the closest in-tree precedent, and what to mirror.

| File | Modification kind | Closest analog | What to mirror |
|---|---|---|---|
| `extensions/pi-claude-marketplace/shared/notify.ts` (centerpiece) | **(a)** delete V1 wrappers (D-21-05); **(b)** inline grammar (D-21-01); **(c)** add `compareByNameThenScope` (D-21-02); **(d)** add `StatusToken` re-export; **(e)** rewrite file header lines 17-44 | The existing line-14 type re-export `export type { Reason } from "./grammar/reasons.ts"` (the Phase 16 design comment at lines 9-13 is the IDIOM); Phase 16 itself defined the V2 surface here; Phase 19 retired `composeRollbackPartialBody` from `presentation/` and added structural-variant types here (precedent for "this file absorbs presentation utilities") | (1) Inlined `REASONS` / `STATUS_TOKENS` / `MARKERS` / `PATTERN_CLASSES` consts + corresponding `Reason` / `StatusToken` / `Marker` / `PatternClass` type aliases placed near top alongside existing types (per `<decisions>` "Claude's Discretion"). (2) Delete the 3 wrapper bodies + V1 overload signature + V1 impl branch in one pass; the `else` arm becomes the entire `notifyUsageError` body. (3) Move `compareByNameThenScope` verbatim from `presentation/sort.ts` -- pure function + structural `Sortable` interface; preserve the JSDoc that explains MSG-GR-3 + `sensitivity: 'base'` invariants. (4) Update the line-14 re-export site to use a direct in-file declaration (do NOT leave a self-re-export). (5) Rewrite the lines 17-44 header to V2-only phrasing (no more "V1 wrappers"; just the two V2 entry points). |
| `extensions/pi-claude-marketplace/shared/errors.ts` | **Add** `composeErrorWithCauseChain(err: unknown): string` (1-line helper from `presentation/cause-chain.ts:30`) | The existing `causeChainTrailer` at line 46 -- same export style, same JSDoc tone, same `unknown` -> `string` shape. The `errorMessage` helper at line 1 supplies the first half. | Place `composeErrorWithCauseChain` immediately AFTER `causeChainTrailer` (line 76 region) so the 1-line composer lives next to its dependency. Mirror the JSDoc style from `presentation/cause-chain.ts:16-29` (orchestrator-aggregation rationale + "byte-identical to notifyError's auto-trailing" cross-reference). Implementation literally: `return trailer === "" ? errorMessage(err) : ${errorMessage(err)}\n\n${trailer};` (same as current `presentation/cause-chain.ts:31-32`). |
| `extensions/pi-claude-marketplace/edge/args-schema.ts` | **Rename** callback parameter `notifyError` -> `onError` (line 28 + line 71 + 2 internal call sites at lines 33, 84) | No exact rename precedent in `git log -p`; closest "parameter rename" discipline is in Phase 19's IN-04 (`refactor(19): omit scope from installedRow for family consistency`) and IN-01/IN-02 dead-code removals -- both demonstrate the "name shadow / cosmetic cleanup, byte-equal behavior" discipline. The existing file header comment at lines 9-13 ALREADY documents the rationale (`notify is delivered as a callback parameter (notifyError), NOT imported`), so the rename also requires updating that comment. | (1) Rename in 4 spots: the parameter declaration at line 28 (`parseArgsOrNotify`), the parameter declaration at line 71 (`parseCommandArgs`), the call sites at lines 33, 84. (2) Update the file header comment at line 9 from `(notifyError)` to `(onError)`. (3) No call-site changes needed in handler files -- callers pass an inline arrow function (e.g., `(message) => { notifyUsageError(ctx, { message, usage: USAGE }); }`), so the parameter name is local to `args-schema.ts`. (4) Verify the comment block at lines 38-54 doesn't reference `notifyError` (it doesn't; the JSDoc says `notifyError(usage)` -- update that to `onError(usage)`). |
| `extensions/pi-claude-marketplace/persistence/migrate.ts` | **Remove** the inline `// eslint-disable-next-line no-restricted-syntax, no-console -- IL-3: ...` directive at line 177 (redundant after the block-level override in `eslint.config.js`) | The Phase 14 / 14.2 in-file `// eslint-disable-next-line msg-* -- <reason>` precedents (now being mass-deleted in Plan 21-01) are the established pattern for inline directives. Removing them when a block-level override supersedes is the *new* discipline introduced by D-21-04 SC #3. No prior phase has done this exact swap; closest is Phase 19's WR-04 (`fix(19): drop unused marketplace param from install failure composer`) -- a "delete now-redundant code" pattern. | Single-line delete at line 177. The `console.warn(...)` call at line 178 STAYS unchanged -- only the comment line goes away. The block-level override added to `eslint.config.js` (per D-21-04 SC #3) supplies the equivalent suppression with `no-console: "off"` + `no-restricted-syntax: "off"` scoped to this file. |
| `eslint.config.js` MSG-* block deletion (lines 144-356 -- Blocks 1, 1b, 2, 3, 4a, 4b, 5, 6) + lint-rules overrides (lines 535-572) + `msgPlugin` import (line 8) | **Bulk deletion** of ~252 lines per RESEARCH §3 | Phase 14 (commit precedents `b32354f`/`b3f1dcd`/etc. -- "update tracking after wave N complete") built up the MSG-* surface; Phases 16 / 18 / 19 / 20 ADDITIVELY narrowed it (commits `e289c4c` Phase 18-06, `d0081dc` Phase 19-06, `332fff5` Phase 20-04 -- all "narrow MSG-Block 1 ignores" pattern). The narrowing discipline (D-18-07 -> D-19-08 -> D-20-07) was always framed as "additive ignores -- never remove the rule wiring -- Phase 21 deletes wholesale." | (1) Delete `import msgPlugin from "./tests/lint-rules/index.js";` at line 8. (2) Delete the comment header at lines 144-150 + all of MSG-Blocks 1 / 1b / 2 / 3 / 4a / 4b / 5 / 6 (lines 151-356). (3) Delete both `tests/lint-rules/**` override blocks (lines 535-572 -- the `disableTypeChecked` block + the relaxed-rules block). (4) Do NOT touch BLOCKS A / B / C / D / E (output discipline / shared-notify self-override / import direction / test fixtures / Pi peer chokepoint). The narrowing precedent commits above each touched EXACTLY ONE array element; Plan 21-01's deletion is the opposite direction (delete the entire scaffolding) but follows the SAME atomic-single-commit discipline: one commit, all-or-nothing, `npm run check` GREEN at the post-edit state. |
| `eslint.config.js` BLOCK A surgical edits (3 sub-updates) | **(a)** Update line 127 message string (V2-only phrasing -- swap `notifySuccess/notifyWarning/notifyError` for `notify`/`notifyUsageError`); **(b)** Update line 116 `notifyError` reference; **(c)** Update line 110-111 `console.warn` IL-3 reference; **(d)** Add a new block-level `persistence/migrate.ts` no-console override | Existing BLOCK B at lines 135-143 -- the canonical `{ files: [<single file>], rules: { "no-restricted-syntax": "off", "no-console": "off" } }` shape. Also the BLOCK D test-fixtures override at lines 504-510 (file-ignored-from-lint pattern) and the tests-wide block at lines 511-529 (file-relaxed-rules pattern). | The new `persistence/migrate.ts` override block (Plan 21-01 adds it) MUST mirror BLOCK B's shape literally: `{ files: ["extensions/pi-claude-marketplace/persistence/migrate.ts"], rules: { "no-console": "off" } }`. Per D-21-04: planner MAY narrow further or also disable `no-restricted-syntax` -- the minimal-edit default mirrors BLOCK B exactly. Place the new block AFTER BLOCK B (so the per-file overrides cluster together) and BEFORE BLOCK C. The 3 message-string updates at lines 110-127 are purely cosmetic; mirror the existing string style. |
| `eslint.config.js` post-Plan-21-02: optional BLOCK C zone amendment (per RESEARCH §8 CORRECTION 2) | **OPTIONAL** -- amend BLOCK C `edge` zone to remove `domain/` from `from:`, OR add per-file override for `edge/handlers/tools.ts` | BLOCK C lines 369-378 (existing `edge` zone) -- the canonical `import-x/no-restricted-paths` zone shape. No prior phase has amended BLOCK C; closest precedent is its initial definition in Phase 11/12. | RESEARCH recommends **Option A** (amend `edge` zone to remove `domain/`). The amendment is a 1-line removal from the `from:` array in the `edge` zone definition. Justification mirrors RESEARCH §8 CORRECTION 2: the re-export-via-presentation hack existed solely to bypass BLOCK C; removing it now is consistent with D-21-02's clean-sweep intent. Verify zero other `edge/**` files import `domain/**` post-amendment so the relaxation has minimal blast radius. |
| `package.json` (lines 76 + 80) | **Bulk edit** test/coverage script globs to drop `presentation` + `tests/lint-rules` | RESEARCH §8 CORRECTION 7 (missed by CONTEXT). No prior phase changed these globs because no prior phase deleted entire test directories named in them. Closest discipline analog: any Phase 7/8 plan that adjusted `package.json` scripts when adding/removing test surfaces (search `git log -- package.json` for prior precedent). | Plan 21-01: drop trailing `"tests/lint-rules/**/*.test.{js,ts}"` argument from `test` script (line 76). Plan 21-02: drop `,presentation` from the brace expansion `{architecture,bridges,domain,edge,helpers,orchestrators,persistence,presentation,shared,transaction}` in BOTH line 76 (`test`) and line 80 (`test:coverage:unit`). Without these edits, `npm test` prints "no files found matching pattern" warnings (does not fail the suite but is noisy). Each edit lives with the same Plan that deletes the corresponding directory so the deletion is internally consistent. |

---

## Deletion Sweep Precedents

Each row: the deletion target, the precedent phase, the precedent commit / plan, and the atomic-commit shape to mirror.

| Deletion target | Precedent phase | Precedent commit / PLAN | Atomic-commit shape to mirror |
|---|---|---|---|
| `tests/lint-rules/` (73 files: 34 rules + 34 RuleTester suites + 3 lib helpers + 2 plugin shell) | Phase 13 (CMC-35: `c4d87d4` ES-5 supersession commit, per CHANGELOG "v1.3 user-contract" section) | Commit `c4d87d4` (per CHANGELOG: "deletes the 5 legacy markers, retires the snapshot byte-equality assertion, rewrites PRD §6.12 ES-5 to a pointer, and rolls back the temporary ESLint marker-restriction additions per D-13-03 / D-13-11 / D-30"). Single atomic commit that simultaneously deleted markers, retired tests, rewrote docs, and rolled back lint additions. | Mirror Plan 13-03-02's "atomic single commit that deletes markers + retires drift-guard test + rewrites pointer doc + rolls back lint scaffolding". For Plan 21-01: one commit, all 73 files + `eslint.config.js` rewrite + `msg-rule-registry.test.ts` + `no-legacy-markers.test.ts` + `persistence/migrate.ts` inline-directive removal + `package.json` glob edit land together. Intermediate states are non-compiling. |
| `tests/architecture/msg-rule-registry.test.ts` (1 file, 4 tests) | Same as `tests/lint-rules/` -- couples to plugin deletion | Same commit shape as above (Phase 13's `c4d87d4` pattern). | Single-file delete inside the Plan 21-01 atomic commit. No standalone precedent because no prior phase deleted ONLY this file. |
| `tests/architecture/no-legacy-markers.test.ts` (1 file, 1 test) | Phase 13 (CMC-35 partial): the ES-5 markers were deleted then; the no-legacy-markers test was RETAINED as the byte-grep defense | The Phase 13 CMC-35 commit RETAINED this test as a defense-in-depth gate. D-21-03 takes the opposite arm now that the V2 closed-set type encoding structurally prevents marker re-introduction. Closest discipline analog: Phase 19 D-19-01 "DROP V1 surfaces with no V2 representation" -- the systematic "if V2 covers this structurally, delete the V1-era defense" pattern. | Single-file delete inside the Plan 21-01 atomic commit. Justification = D-21-03 ("closed-set type encoding makes ES-5 marker re-introduction structurally impossible"). |
| `extensions/pi-claude-marketplace/shared/grammar/` (4 files: `markers.ts`, `pattern-classes.ts`, `reasons.ts`, `status-tokens.ts`) | Phase 16 (D-16-04 "renderer-as-spec discipline" + the shared/notify.ts:14 `export type { Reason } from "./grammar/reasons.ts"` foreshadow); Phase 16 also added `composeRollbackPartialChildren` to `presentation/rollback-partial.ts` and retired the in-orchestrator version (Phase 14 D-14-04) -- precedent for "absorb single-purpose grammar primitives upward into the renderer". | The Phase 16 design comment at `shared/notify.ts:9-13` is the textual foreshadow. The commit precedent for "inline a 1-purpose file into a larger one + delete the file in the same commit" is Phase 19 commit `9a92075` ("refactor(19-02): migrate install.ts to V2 notify() + retire composeRollbackPartialBody") -- it deleted the body and absorbed its semantics into the notify path. | Mirror Phase 19-02's "absorb + delete in one commit" recipe inside Plan 21-02. The grammar consts + types inline into `shared/notify.ts` AND the 4 grammar files disappear AND the 9 consumer import paths migrate AND `tests/architecture/notify-types.test.ts` line 87 string updates AND the `shared/notify.ts:14` self-re-export becomes a direct declaration -- ALL in the same atomic commit. Intermediate states have broken imports. |
| `extensions/pi-claude-marketplace/presentation/` (13 files: 12 source + README.md) | Phase 19 D-19-01 ("DROP V1 surfaces with no V2 representation"); Phase 20 D-20-03 ("defense-in-depth catch-all DROP discipline"); Plan 20-02 orphaned `presentation/cascade-summary.ts` per CONTEXT 20-CONTEXT.md line 256 (orphan introduced by the V2 migration). Phase 20 also retired several composers via in-renderer absorption. | The directly inherited discipline is "tear-out-rather-than-rewrite" (D-20-03 + D-20-06). Commit precedent: `c78756d` "refactor(20): drop edge handler catch-all wrappers in bootstrap + import (SNM-23)" -- atomic deletion + accompanying test cleanup. | Mirror `c78756d`'s "deletion + consumer/test cleanup in one commit" recipe inside Plan 21-02. For Phase 21 the surface is wider (5 relocations + 1 directory delete + 1 test directory delete + 11+ consumer updates + `package.json` glob edit + `tests/architecture/scope-order-drift.test.ts` string update) but the discipline is the same: ONE atomic commit. RESEARCH §6 file-inventory caps it at ~46 files (well under the 200-file split threshold per D-21-08). |
| `tests/presentation/` (11 files, 133 tests) | Same as `presentation/` -- couples to source-tree deletion | Same `c78756d` shape (atomic source + test cleanup). RESEARCH §5 confirms the 133-test deletion is documented in Plan 21-03's SUMMARY arithmetic. | Same Plan 21-02 atomic commit. Per D-21-02 the corresponding behaviors are already covered by: V2 per-variant tests in `tests/shared/notify-v2.test.ts` (41 tests); catalog UAT byte-equality in `tests/architecture/catalog-uat.test.ts`; source-parsing in `tests/domain/source.test.ts`; error composition in `tests/shared/errors.test.ts`. No assertion migration required (RESEARCH §7 confirms the adjacent tests survive unchanged). |
| `tests/shared/notify.test.ts` (1 file, 7 tests) **-- MISSED BY CONTEXT** | RESEARCH §8 CORRECTION 1. No prior phase deleted this file -- it has been live since Phase 6 (initial V1 wrapper coverage). | No exact precedent: this is the V1-wrapper test file. Closest analog: any Phase 13-20 commit that removed test files in lockstep with their tested source (e.g., Phase 20's `c78756d` deleted the edge-handler catch-all test alongside the catch-all wrapper). | Plan 21-02 MUST delete this file atomically with the V1 wrapper deletion in `shared/notify.ts`. The 7 tests at lines 27 / 34 / 41 / 48 / 61 / 73 / 85 are LIVE calls to `notifySuccess` / `notifyWarning` / `notifyError`. Without this delete, the wrappers go away and `npm test` fails at compile. V2 coverage in `tests/shared/notify-v2.test.ts` already covers the live `notify()` surface (41 tests). |

---

## Consumer Import Migration Precedents

Each batch: the old import path -> the new home, the consumer count, the precedent PLAN.md shape, and the acceptance-criteria shape that Plan 21-02 should mirror.

| Migration batch | Count | Precedent PLAN.md | Acceptance-criteria shape to mirror |
|---|---|---|---|
| `shared/grammar/reasons.ts` -> `shared/notify.ts` (`Reason` type import) | 9 file edits (8 orchestrators + 1 architecture test) per RESEARCH §2 (CORRECTION 3) | Phase 19 Plan 19-04 (`reinstall.ts` cascade migration) and Phase 20 Plan 20-02 (`import/execute.ts` cascade migration) -- each migrated ~6-10 consumer sites in lockstep with a renderer-side change. Phase 19/20 PLAN.md `<read_first>` blocks always include the analog file + the verbatim before/after import shape from RESEARCH.md. | Mirror Phase 20 Plan 20-04's `<verification>` block (the canonical "X count = Y count" shape): `grep -r "shared/grammar/reasons" extensions/ tests/` returns 0 (or only comment matches); `grep -r "from \"\\.\\./\\.\\./shared/notify\\.ts\"" extensions/pi-claude-marketplace/orchestrators/` returns N >= 8 + existing live imports; `npm run check` exits 0. Per-site recipe in the PLAN action block: "Edit line {N} of {file}: change `import type { Reason } from \"../../shared/grammar/reasons.ts\"` to `import type { Reason } from \"../../shared/notify.ts\"`. Tests assert via TypeScript compile + the existing `notify-types.test.ts` invariant." |
| `shared/grammar/status-tokens.ts` -> `shared/notify.ts` (`StatusToken` type) | 0 live consumer edits (the only importer is `presentation/compact-line.ts`, deleted with directory) | RESEARCH §2 CORRECTION 4. The `StatusToken` re-export from `shared/notify.ts` is NEW (no current re-export exists; only `Reason` is re-exported today at line 14). | Plan 21-02 ADDS `export type { StatusToken }` (after inline declaration) for completeness even though no live consumer needs it. Acceptance criterion: `grep "export type { StatusToken }" extensions/pi-claude-marketplace/shared/notify.ts` returns 1 match. |
| `presentation/cause-chain.ts` (`composeErrorWithCauseChain`) -> `shared/errors.ts` | 3 file edits (per RESEARCH §2) | Phase 19 commit `577b896` ("refactor(19): collapse duplicate list narrowers into shared core") -- precedent for "move helper from leaf-utility module to shared/ in lockstep with consumer updates." | Per-consumer recipe: edit the import line in `orchestrators/plugin/reinstall.ts:69`, `orchestrators/plugin/update.ts:83`, `orchestrators/marketplace/update.ts:109` from `"../../presentation/cause-chain.ts"` to `"../../shared/errors.ts"`. Acceptance: `grep -r "presentation/cause-chain" extensions/` returns 0; the 3 consumers' `npm test` still pass byte-equal (the helper's signature + behavior are unchanged -- only the file location moved). |
| `presentation/sort.ts` (`compareByNameThenScope`) -> `shared/notify.ts` | 3 file edits (per RESEARCH §2) + 1 STRING update in `tests/architecture/scope-order-drift.test.ts:158` + 1 ALLOWLIST update at `tests/architecture/scope-order-drift.test.ts:53` -- **partially missed by CONTEXT, see RESEARCH §8 CORRECTION 5** | Phase 19 commit `9a92075` (atomic absorb + delete) is the structural analog. | Per-consumer recipe: edit imports in `orchestrators/plugin/reinstall.ts:70`, `orchestrators/plugin/update.ts:84`, `orchestrators/import/execute.ts:10` from `"../../presentation/sort.ts"` to `"../../shared/notify.ts"`. Update `tests/architecture/scope-order-drift.test.ts:53` (allowlist path string) AND line 158 (error-message string) from `presentation/sort.ts` to `shared/notify.ts`. Acceptance: `grep -r "presentation/sort" extensions/ tests/` returns 0; the drift-guard test still PASSes against the new allowlist path. |
| `presentation/marketplace-list.ts` (`sourceLogical` / `ParsedSource`) -> `domain/source.ts` (DIRECT) | 1 file edit (`edge/handlers/tools.ts` lines 43, 46) **-- CONTEXT claimed 9 consumers; actual = 1, RESEARCH §8 CORRECTION 2** | No exact precedent for cross-zone edge -> domain migration. The closest discipline analog is Phase 18 Plan 18-06's BLOCK 1b discussion (IN-06 in-file rationale at `eslint.config.js:185-198`) -- "when a cross-zone import would otherwise violate BLOCK C, document the rationale in-file and make the surgical exception". | Per-consumer recipe: edit `edge/handlers/tools.ts:43,46` to import from `"../../domain/source.ts"` instead of `"../../presentation/marketplace-list.ts"`. Apply RESEARCH-recommended BLOCK C amendment (Option A: remove `domain/` from the `edge` zone's `from:` array; smallest blast radius given there is only one edge consumer). Acceptance: `grep -r "presentation/marketplace-list" extensions/` returns 0; `npm run lint` exits 0 (proving BLOCK C amendment is correctly scoped); the consumer test (`tests/edge/handlers/tools.test.ts` if it exists -- planner verifies during execution) passes. |
| `presentation/compact-line.ts` (`EntityErrorRow` type) -> file-local in `orchestrators/plugin/install.ts` | 1 file edit (`install.ts:121`) -- inline the interface declaration | Phase 19's WR-04 (`fix(19): drop unused marketplace param from install failure composer`) and Phase 20's WR-05 ("(targets) stand-in" cleanup) -- precedent for "interface used by exactly one consumer becomes file-local". | Per-consumer recipe: delete the import at `install.ts:121`; add a top-of-file `interface EntityErrorRow { ... }` declaration mirroring the current `presentation/compact-line.ts` shape verbatim (planner reads the source during execution -- the interface is small per CONTEXT specifics). Acceptance: `grep -r "EntityErrorRow" extensions/` returns 1 line in `install.ts` (the file-local declaration + its uses); the file compiles. |
| `renderMarketplaceList` / `MarketplaceListEntry` -> DELETE (not inline) | 0 file edits **-- CONTEXT claimed sole-caller inlining into `orchestrators/marketplace/list.ts`; actual = ZERO live callers, RESEARCH §8 CORRECTION 6** | N/A -- no precedent needed for "delete unused symbol". | Plan 21-02 simply deletes `presentation/marketplace-list.ts` (and the test file `tests/presentation/marketplace-list.test.ts`) without any inline / relocation. Comment-only references in `orchestrators/marketplace/list.ts:18` and `tests/architecture/catalog-uat.test.ts:15` are cosmetic-update only (planner's discretion). |

---

## Closure-Document Precedents (Plan 21-03)

Each row: the closure doc Plan 21-03 updates, the precedent milestone entry, and the format Plan 21-03 should mirror.

| Closure doc | Precedent entry | Format to mirror |
|---|---|---|
| `CHANGELOG.md` (v1.4 milestone closure) | The `## [0.2.0] - 2026-05-25 -- v1.3 Consistent Messaging` entry at CHANGELOG.md line 3 (the v1.3 milestone-closure block). Earlier milestone entries (v1.0/v1.1/v1.2 -- lines 41-67) are minor-release single-line entries; v1.3 is the closest STRUCTURAL precedent for a milestone-class entry. | Mirror v1.3's structure verbatim: `## [0.3.0] - 2026-05-27 -- v1.4 Structured Notification Messages` (or planner-picked version bump), followed by ~3-paragraph block: (1) one-paragraph user-visible summary ("Every user-visible `ctx.ui.notify` callsite now flows through the `notify(ctx, pi, NotificationMessage)` structured entry point; severity is computed from contents; the v1.3 34-rule MSG-* drift-guard plugin is retired in favor of closed-set type encoding."); (2) a `User-visible changes` bullet list (if any -- per Phase 17 catalog rewrite, output is already structurally identical to v1.3 from the user's perspective; the entry may just note "no user-visible change vs v1.3 -- structural / internal milestone"); (3) an `Internals` bullet list enumerating closed SNM IDs (SNM-22, SNM-24, SNM-25, SNM-27, SNM-28, SNM-29, SNM-32 + the migration-half SNM-23 closure that lands here too). Closing line: test-count delta (e.g., "1188 tests green; lint + format + types clean.") |
| `.planning/STATE.md` (mark v1.4 milestone complete) | The commit `86b9d15` ("docs(phase-20): mark complete after Plan 20-06 verification") shows the canonical phase-closure update -- changed `STATE.md` to set `status=completed`, `completed_phases=8/9`. For MILESTONE closure (one step up), the precedent is Phase 13's `d52d7f0` ("docs(phase-13): complete phase execution") which closed the v1.3 milestone in `STATE.md`. | Mirror `86b9d15`'s STATE.md edit shape: (1) `status: completed` (already set; v1.4 marker stays); (2) `completed_phases: 9` (currently 8); (3) `percent: 100` (currently 89); (4) `last_activity: 2026-05-27 -- Phase 21 marked complete -- v1.4 milestone closed`. The yaml-front-matter shape is the canonical pattern. |
| `.planning/PROJECT.md` (Key Decisions / milestone status) | The current PROJECT.md "Current Milestone: v1.4 Structured Notification Messages" section is the active block to retire. Precedent: Phase 13 closure moved v1.3 from "Active" to "Validated" in PROJECT.md (see the existing `### Validated` block listing v1.0 / v1.1 / v1.2 / v1.3 ticked checkmarks). The v1.3 entry at the top of `### Validated` (PROJECT.md "Drift Guard & Test Alignment" block) is the structural precedent for a v1.4 entry. | Add v1.4 entry at the TOP of `### Validated`: `- ✓ v1.4 Structured Notification Messages (Phases 15-21, SNM-01..SNM-32): ...` followed by a ~5-line summary enumerating the type model (`NotificationMessage` / `MarketplaceNotificationMessage` / `PluginNotificationMessage` discriminated union / `PluginStatus` enum / `MarketplaceStatus` enum / `Dependency` closed set / `UsageErrorMessage`), the two public entry points (`notify`, `notifyUsageError`), the test surface (`tests/shared/notify-v2.test.ts` 41 tests + `tests/architecture/catalog-uat.test.ts` byte-equality runner), and the closed drift-guard surface (34-rule MSG-* plugin retired; `tests/lint-rules/` deleted; `presentation/` deleted; `shared/grammar/` deleted; `tests/architecture/no-legacy-markers.test.ts` deleted; `eslint.config.js` stock-rules-only). Move the existing "Current Milestone: v1.4" block to "Validated" and either drop the "Current Milestone:" header entirely or set it to "(milestone planning pending)" per planner discretion. |
| `.planning/REQUIREMENTS.md` (SNM IDs marked complete) | The existing v1.4 traceability table at REQUIREMENTS.md lines 96-109 is the canonical surface. The pattern is: `\| SNM-XX \| Phase YY \| Status \|` rows; status flips from `Pending` to `Complete` when the closing phase lands. RESEARCH notes that SNM-22 currently shows `[x]` in the requirement list (line 46) and `Complete` in the traceability table (line 99) -- planner should verify this is accurate post-Phase-21 (per CONTEXT lines 309 "REQUIREMENTS.md currently shows SNM-22 with a stray `[x]` checkbox -- planner verifies"). | Per-requirement edits: flip `[ ]` to `[x]` on REQUIREMENTS.md lines 47 (SNM-23), 48 (SNM-24), 49 (SNM-25), 51 (SNM-27), 52 (SNM-28), 53 (SNM-29), 59 (SNM-32). Flip traceability table rows 100-109 (SNM-23 / SNM-24 / SNM-25 / SNM-27 / SNM-28 / SNM-29 / SNM-32) from `Pending` to `Complete`. Verify SNM-22 row 99 reads `Complete` (CONTEXT notes the stray checkbox; planner reconciles). |

---

## Test-Count Accounting Precedents (Plan 21-03 SUMMARY)

| Precedent | Phase / Plan SUMMARY | Format to mirror |
|---|---|---|
| Test-count delta documentation | Phase 16/17/17.1/17.2 per-variant additions are documented in their respective SUMMARY.md "## Verification Matrix" sections. Phase 16's SUMMARY (and Phase 17's amendments) each cite "before -> after" test counts and the SC pass/fail breakdown. | Mirror Plan 20-04 SUMMARY.md's "Verification Matrix" table at lines 80-92 (above). Each SC row: `\| SC \| description \| command \| observed result \| exit code \| status \|`. The total-test-count row should be the final row, in the same shape as Plan 20-04 line 91 (`\| #5 \| npm run check ... \| 1363 pass / 0 fail / 0 skipped / 2 todo (1365 total) \| 0 \| GREEN \|`). |
| Cumulative test-count arithmetic | No direct precedent -- Plan 21-03 is the first SUMMARY that documents a MULTI-DIRECTORY deletion delta. Closest analog: Phase 14.2's per-variant additions documented incrementally (see Phase 14.2 plans for additive delta accounting). | Plan 21-03 SUMMARY MUST include the RESEARCH §5 arithmetic verbatim: `1367 (pre-Phase-21) - 34 (lint-rules) - 4 (msg-rule-registry) - 1 (no-legacy-markers) - 133 (presentation tests) - 7 (notify.test.ts V1 wrappers) + 0..2 (planner-discretionary `compareByNameThenScope` block) = 1188..1190 (expected post-Phase-21)`. The final live number after `npm test` is the SC #1 evidence; the arithmetic is the audit trail. |

---

## Shared cross-cutting patterns (apply to all 3 plans)

### Pattern: Atomic single-commit discipline (D-21-08 inherited from D-18-06 + D-19-07 + D-20-06)

**Source:** `c78756d` Phase 20-03 atomic deletion + test cleanup; `9a92075` Phase 19-02 absorb-and-delete; `c4d87d4` Phase 13 atomic ES-5 supersession.

**Apply to:** Plan 21-01 (lint teardown -- ~77 files in one commit per RESEARCH §6); Plan 21-02 (source consolidation -- ~46 files in one commit per RESEARCH §6). Plan 21-03 is verification-heavy + 4-file closure-doc edit (separate commit shape; not atomic-source-deletion).

**Shape:** ONE commit per Plan. All dependent file edits land together. Intermediate states are non-compiling. `npm run check` GREEN before commit lands. Per CLAUDE.md: `pre-commit run --all-files` BEFORE commit; `SKIP=trufflehog` prefix if committing from a worktree.

### Pattern: Per-plan SC verification matrix in SUMMARY (inherited from Phase 20)

**Source:** Plan 20-04 SUMMARY lines 80-103 (the canonical "SC verification" + "NO-CHANGE invariant re-verification" double-table shape).

**Apply to:** Plan 21-01 SUMMARY (5 SCs from D-21-04 / SNM-24 / SNM-25 / SNM-27 / SNM-28); Plan 21-02 SUMMARY (5 SCs from SNM-22 / SNM-29 + the relocation invariants per D-21-02); Plan 21-03 SUMMARY (the 5 ROADMAP §Phase-21 SCs verified end-to-end).

**Shape:** Two tables -- one for "Success Criteria proved GREEN" (one row per SC) + one for "NO-CHANGE invariants re-verified" (one row per invariant). Each row has columns: `\| name \| command \| observed \| expected \| status \|`.

### Pattern: Conventional Commits with phase ID prefix (inherited from all Phase 18-20 commits)

**Source:** Phase 20 commits `332fff5` (`chore(20): narrow ...`), `c78756d` (`refactor(20): drop ...`), `ec1e795` (`refactor(20): migrate ...`).

**Apply to:** Plan 21-01 (`chore(21): retire MSG-* drift-guard + tests/lint-rules + no-legacy-markers (SNM-24, SNM-25, SNM-27, SNM-28)`); Plan 21-02 (`refactor(21): consolidate shared/notify.ts + retire presentation/ + shared/grammar/ + V1 wrappers (SNM-22, SNM-29)`); Plan 21-03 (`docs(21): close v1.4 milestone + CHANGELOG + STATE + PROJECT + REQUIREMENTS`).

**Per CLAUDE.md:** Title 5-72 chars; body lines <=80 chars; `SKIP=trufflehog` if worktree; NEVER `--no-verify`.

---

## Metadata

**Pattern extraction scope:** `.planning/phases/{13,16,17,17.1,17.2,18,19,20}/` (precedent commits + PLAN.md shapes); current state of `extensions/pi-claude-marketplace/{shared,presentation,edge,orchestrators,domain,persistence}/` (modification targets); `eslint.config.js` lines 1-573; `CHANGELOG.md` lines 1-30; `.planning/{STATE,PROJECT,REQUIREMENTS}.md`.

**Files scanned (read-only):** 21-CONTEXT.md, 21-RESEARCH.md, `shared/notify.ts:1-160`, `shared/errors.ts` (full), `presentation/cause-chain.ts` (full), `presentation/sort.ts` (full), `edge/args-schema.ts:1-100`, `persistence/migrate.ts:160-185`, `eslint.config.js:1-160 + 500-575`, `package.json:1-90`, `tests/architecture/scope-order-drift.test.ts:148-163`, Phase 18/19/20 PLAN + SUMMARY samples, CHANGELOG.md head, STATE.md head, PROJECT.md head, REQUIREMENTS.md head + traceability rows.

**Confidence:** HIGH on modification patterns + deletion precedents + closure-doc formats (all verified against current files and prior commits). MEDIUM on the optional BLOCK C amendment (no prior phase touched BLOCK C; planner has discretion among the 3 options enumerated in RESEARCH §8 CORRECTION 2).

**Extraction date:** 2026-05-27.

---

## PATTERN MAPPING COMPLETE
