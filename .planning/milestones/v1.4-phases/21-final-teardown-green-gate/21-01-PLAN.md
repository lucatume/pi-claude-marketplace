---
phase: 21
plan: 21-01
type: execute
wave: 1
depends_on: []
files_modified:
  - eslint.config.js
  - extensions/pi-claude-marketplace/persistence/migrate.ts
  - package.json
  - tests/architecture/msg-rule-registry.test.ts
  - tests/architecture/no-legacy-markers.test.ts
  - tests/lint-rules/
autonomous: true
requirements: [SNM-24, SNM-25, SNM-27, SNM-28]
must_haves:
  truths:
    - "Per ROADMAP §Phase-21 SC #1: tests/lint-rules/ and tests/architecture/msg-rule-registry.test.ts are absent on disk."
    - "Per ROADMAP §Phase-21 SC #3: eslint.config.js wires zero msg/* rules; BLOCK A retains its ctx.ui.notify-outside-shared/notify.ts selector; persistence/migrate.ts is covered by a block-level no-console + no-restricted-syntax override."
    - "Per ROADMAP §Phase-21 SC #4 (no-legacy-markers arm): tests/architecture/no-legacy-markers.test.ts is absent on disk per D-21-03."
    - "BLOCKS A/B/C/D/E and the tests-wide relaxation override survive verbatim except for BLOCK A's surgical message-string updates per D-21-04."
    - "D-21-07: no pilot / recipe block-comment is needed for Plan 21-01 -- the work is deletion-and-config-rewrite (no mirrored cascade construction), and the atomic-single-commit discipline at Task 21-01-05 replaces pilot-first sequencing."
    - "package.json `test` script no longer contains the tests/lint-rules/**/*.test.{js,ts} argument; brace order is unchanged (RESEARCH §8 CORRECTION 7)."
    - "Inline // eslint-disable-next-line directive at persistence/migrate.ts line 177 is removed; the block-level override in eslint.config.js supplies the equivalent suppression."
    - "After commit, `npm run check` (typecheck + lint + format + tests) is GREEN."
  artifacts:
    - path: "eslint.config.js"
      provides: "Stock ESLint config with BLOCKS A/B/C/D/E + new persistence/migrate.ts override; zero MSG-* wirings; zero tests/lint-rules/** overrides; zero msgPlugin import."
      contains: "no-restricted-syntax"
    - path: "package.json"
      provides: "test script glob without tests/lint-rules/**; brace order unchanged."
      contains: "node --test"
  key_links:
    - from: "eslint.config.js"
      to: "extensions/pi-claude-marketplace/persistence/migrate.ts"
      via: "block-level files-override carve-out"
      pattern: "persistence/migrate\\.ts"
---

<objective>
Retire the v1.3 MSG-* drift-guard infrastructure end-to-end in a single atomic commit per D-21-06 + D-21-08: rewrite `eslint.config.js` to stock-rules-only (delete `msgPlugin` import + MSG-Blocks 1, 1b, 2, 3, 4a, 4b, 5, 6 + both `tests/lint-rules/**` overrides + BLOCK A surgical edits + new `persistence/migrate.ts` block-level override per D-21-04); delete `tests/lint-rules/` (73 files), `tests/architecture/msg-rule-registry.test.ts`, and `tests/architecture/no-legacy-markers.test.ts` per D-21-03; update `package.json` test glob (RESEARCH §8 CORRECTION 7); remove the now-redundant inline `eslint-disable-next-line` at `persistence/migrate.ts:177`. Closes SNM-24, SNM-25, SNM-27, SNM-28.

Purpose: The V2 closed-set type encoding in `shared/notify.ts` (PluginStatus / MarketplaceStatus / Reason / StatusToken / Marker unions) now structurally enforces what the 34-rule MSG-* plugin enforced via AST checks; the v1.3 drift-guard surface has served its purpose and is dead weight.

Output: An `eslint.config.js` of approximately 320 lines (down from 573); a deleted `tests/lint-rules/` directory; two deleted architecture-test files; a one-line inline-directive removal at `persistence/migrate.ts`; an updated `package.json` test glob; a single atomic commit gated by `npm run check` GREEN.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-final-teardown-green-gate/21-CONTEXT.md
@.planning/phases/21-final-teardown-green-gate/21-RESEARCH.md
@.planning/phases/21-final-teardown-green-gate/21-PATTERNS.md
@.planning/phases/21-final-teardown-green-gate/21-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task id="21-01-01" type="execute">
  <name>Task 1: Rewrite eslint.config.js -- delete MSG-* wirings + tests/lint-rules overrides + msgPlugin import; add persistence/migrate.ts block-level override; update BLOCK A message strings to V2-only phrasing</name>
  <files>eslint.config.js</files>
  <read_first>
    - eslint.config.js
    - .planning/phases/21-final-teardown-green-gate/21-CONTEXT.md (decisions D-21-04)
    - .planning/phases/21-final-teardown-green-gate/21-RESEARCH.md (§3 eslint.config.js block map)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Modification Patterns: eslint.config.js rows)
    - extensions/pi-claude-marketplace/persistence/migrate.ts
  </read_first>
  <action>
    Per D-21-04 + RESEARCH §3 block map (line numbers from current 573-line `eslint.config.js`):

    1. **Header import deletion (line 8):** remove `import msgPlugin from "./tests/lint-rules/index.js";`.

    2. **BLOCK A surgical edits (lines 80-134):**
       - Line 111 -- leave the `console.warn` IL-3 message string intact in spirit, but adjust phrasing to remove the now-stale instruction to use `eslint-disable-next-line` (the per-file override added below supplants it). Recommended new message: "console.warn is forbidden in the extension (IL-2) except at the single sanctioned migrateLegacyMarketplaceRecords callsite, which is allowed via a block-level files-override in this config."
       - Line 116 -- replace the reference to `notifyError(ctx, ..., cause)` with the V2 path. Recommended new message: "console.error is forbidden in the extension (IL-2). Use notify(ctx, pi, NotificationMessage) (failed status carries cause via per-plugin cause?: Error) from shared/notify.ts."
       - Line 127 -- replace the reference to `notifySuccess/notifyWarning/notifyError from shared/notify.ts (D-07)` with V2-only phrasing. Recommended new message: "Direct ctx.ui.notify is forbidden -- use notify(ctx, pi, NotificationMessage) or notifyUsageError(ctx, UsageErrorMessage) from shared/notify.ts."
       - Leave the `no-console: "error"` rule at line 132 unchanged at the BLOCK A scope.

    3. **Add new block-level override for `persistence/migrate.ts` AFTER BLOCK B (after line 143) and BEFORE the MSG-* comment header (the cluster of per-file overrides stays contiguous).** Mirror BLOCK B's shape per PATTERNS.md "Modification Patterns" row "eslint.config.js BLOCK A surgical edits":

       ```
       {
         // BLOCK B-2 (D-21-04 SC #3): Per-file override -- migrate.ts emits the single
         // sanctioned legacy-migration console.warn (IL-3). The block-level no-console
         // off and no-restricted-syntax off supersede the prior inline
         // `eslint-disable-next-line` directive.
         files: ["extensions/pi-claude-marketplace/persistence/migrate.ts"],
         rules: {
           "no-console": "off",
           "no-restricted-syntax": "off",
         },
       },
       ```

       Per W4 + D-21-04 example: BOTH `no-console: "off"` AND `no-restricted-syntax: "off"` are present unconditionally (the `console.warn` AST selector lives in BLOCK A's `no-restricted-syntax` and would otherwise still fire on this file).

    4. **Delete MSG-Block comment header (lines 144-150).** The 7-line comment block describing the MSG-* plugin.

    5. **Delete MSG-Block 1 (lines 151-174).** `files: orchestrators/**/*.ts` with `msg/sr-1..6` rules. Also captures all `ignores` arrays inside.

    6. **Delete MSG-Block 1b (lines 175-214).** `msg/gr-3-per-scope` block.

    7. **Delete MSG-Block 2 (lines 215-224).** `files: edge/handlers/**/*.ts` with `msg/sr-7` + `msg/nc-2`.

    8. **Delete MSG-Block 3 (lines 225-242).** `msg/lc-1` + `msg/lc-2`.

    9. **Delete MSG-Block 4a (lines 243-271).** `msg/mr-1`, `mr-2`, `rp-1`, `rh-1`. This block contains a `shared/notify.ts` ignore at line 262 that disappears with the block; that bounded-window exemption is captured automatically per D-21-05 "shared/notify.ts bounded-window ignores removal" note.

    10. **Delete MSG-Block 4b (lines 272-295).** `msg/cc-1-cause-chain`.

    11. **Delete MSG-Block 5 (lines 296-327).** `msg/nc-1`, `msg/sd-1`, `msg/sd-2`. This block contains `shared/notify.ts` ignore at line 319 and `shared/grammar/reasons.ts` ignore at line 318 -- both disappear with the block.

    12. **Delete MSG-Block 6 (lines 328-356).** The 15-rule meta-assertion block (`msg/er-1`..`msg/wm-5`).

    13. **RETAIN BLOCKS C/E/D + tests-wide relaxation + eslint-config.js self-override (lines 357-534) verbatim.** Do NOT touch any `import-x/no-restricted-paths` zone here -- Plan 21-02 owns the BLOCK C amendment for `edge/handlers/tools.ts` → `domain/source.ts`.

    14. **Delete `tests/lint-rules/**` disableTypeChecked override (lines 535-545).**

    15. **Delete `tests/lint-rules/**` relaxed-rules override (lines 546-572).**

    16. **Retain closing `)` at line 573.**

    Net effect: ~252 deleted lines + ~9 added lines for the new BLOCK B-2; post-edit file ≈ 320 lines.
  </action>
  <verify>
    <automated>
      <command>npm run lint</command>
      <command>! grep -nE "import msgPlugin|plugins:.*msg:|msg/(sr-|gr-|nc-|lc-|mr-|rp-|rh-|cc-|sd-|er-|wm-|kc-|kp-)" eslint.config.js</command>
      <command>grep -c "BLOCK A" eslint.config.js</command>
      <command>grep -c "persistence/migrate\\.ts" eslint.config.js</command>
      <command>grep -A 6 "persistence/migrate.ts" eslint.config.js | grep -c "no-restricted-syntax"</command>
      <command>grep -A 6 "persistence/migrate.ts" eslint.config.js | grep -c "no-console"</command>
      <command>! grep -n "shared/grammar" eslint.config.js</command>
    </automated>
  </verify>
  <done>
    `eslint.config.js` contains zero `msg/` rule wirings, zero `import msgPlugin`, zero `tests/lint-rules/**` override blocks, zero `shared/grammar` references. BLOCKS A/B/C/D/E + tests-wide relaxation + eslint.config.js self-override survive. New `persistence/migrate.ts` block-level override exists with BOTH `"no-console": "off"` AND `"no-restricted-syntax": "off"` (per W4 + D-21-04). BLOCK A messages reference `notify` and `notifyUsageError` (not the deleted V1 wrappers). `npm run lint` exits 0 against the rest of the working tree at this intermediate point (lint plugin still on disk; the orphan import was deleted so config loads cleanly).
  </done>
</task>

<task id="21-01-02" type="execute">
  <name>Task 2: Remove inline eslint-disable-next-line directive at persistence/migrate.ts:177</name>
  <files>extensions/pi-claude-marketplace/persistence/migrate.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/migrate.ts
    - .planning/phases/21-final-teardown-green-gate/21-RESEARCH.md (§3 inline directive verification -- line 177)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Modification Patterns: persistence/migrate.ts row)
  </read_first>
  <action>
    Delete the single inline directive line at `persistence/migrate.ts:177`:
    `// eslint-disable-next-line no-restricted-syntax, no-console -- IL-3: load-time migrate save fail`

    The `console.warn(...)` invocation at line 178 STAYS unchanged. The block-level override added to `eslint.config.js` by Task 21-01-01 supplies the equivalent suppression scoped to this file.

    No other edits in this file. No other inline `eslint-disable*` directives in `persistence/migrate.ts` (verify by grep before exit).
  </action>
  <verify>
    <automated>
      <command>! grep -n "eslint-disable-next-line" extensions/pi-claude-marketplace/persistence/migrate.ts</command>
      <command>grep -c "console.warn" extensions/pi-claude-marketplace/persistence/migrate.ts</command>
      <command>npm run lint -- extensions/pi-claude-marketplace/persistence/migrate.ts</command>
    </automated>
  </verify>
  <done>
    `extensions/pi-claude-marketplace/persistence/migrate.ts` contains zero `eslint-disable*` directives. The `console.warn` callsite at the legacy migration save-failure path is unchanged and lints clean under the new block-level override added by Task 21-01-01.
  </done>
</task>

<task id="21-01-03" type="execute">
  <name>Task 3: Delete tests/lint-rules/ directory (73 files); delete tests/architecture/msg-rule-registry.test.ts; delete tests/architecture/no-legacy-markers.test.ts</name>
  <files>
    tests/lint-rules/
    tests/architecture/msg-rule-registry.test.ts
    tests/architecture/no-legacy-markers.test.ts
  </files>
  <read_first>
    - tests/architecture/msg-rule-registry.test.ts
    - tests/architecture/no-legacy-markers.test.ts
    - .planning/phases/21-final-teardown-green-gate/21-RESEARCH.md (§1 file inventories -- 73 files in tests/lint-rules; 4 inner tests in msg-rule-registry; 1 inner test in no-legacy-markers)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Deletion Sweep Precedents: tests/lint-rules row + the two single-file rows)
  </read_first>
  <action>
    Three concrete deletions, executed without intermediate commits:

    1. `rm -rf tests/lint-rules/` -- deletes 73 files (34 rule sources + 34 RuleTester `.test.js` files + 3 `lib/` helpers including `frontmatter.js` + `frontmatter.d.ts` + `sr-tokens.js` + 2 plugin shell files `index.js` + `index.d.ts`). Verified inventory in RESEARCH §1.
    2. `rm tests/architecture/msg-rule-registry.test.ts` -- single 7.5K file containing 4 inner tests (4-way parity: RULE_NAMES export ↔ `eslint.config.js` rule wirings ↔ rule files on disk ↔ RuleTester suites). The plugin is deleted; the parity test is obsolete.
    3. `rm tests/architecture/no-legacy-markers.test.ts` -- single 5.9K file containing 1 inner test (the 5-ES-5-literal static-audit gate). DELETE per D-21-03; the V2 closed-set type encoding structurally rejects re-introduction.

    Do NOT touch `tests/architecture/markers-snapshot.test.ts` (orthogonal -- drift-guards the active Phase 5/7 markers `RECOVERY_PLUGIN_REINSTALL_PREFIX` + `STATE_LOCK_HELD_PREFIX` in `shared/markers.ts`).

    Do NOT touch any `tests/architecture/*.test.ts` other than the two named above.
  </action>
  <verify>
    <automated>
      <command>! test -d tests/lint-rules</command>
      <command>! test -f tests/architecture/msg-rule-registry.test.ts</command>
      <command>! test -f tests/architecture/no-legacy-markers.test.ts</command>
      <command>test -f tests/architecture/markers-snapshot.test.ts</command>
      <command>test -f tests/architecture/catalog-uat.test.ts</command>
      <command>test -f tests/architecture/notify-types.test.ts</command>
    </automated>
  </verify>
  <done>
    `tests/lint-rules/` absent. `tests/architecture/msg-rule-registry.test.ts` absent. `tests/architecture/no-legacy-markers.test.ts` absent. `tests/architecture/markers-snapshot.test.ts`, `tests/architecture/catalog-uat.test.ts`, `tests/architecture/notify-types.test.ts` survive untouched.
  </done>
</task>

<task id="21-01-04" type="execute">
  <name>Task 4: Update package.json test script -- surgically remove the tests/lint-rules glob argument (RESEARCH §8 CORRECTION 7); brace order unchanged</name>
  <files>package.json</files>
  <read_first>
    - package.json
    - .planning/phases/21-final-teardown-green-gate/21-RESEARCH.md (§8 CORRECTION 7 -- line 76 in package.json)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Modification Patterns: package.json row -- Plan 21-01 owns the tests/lint-rules portion)
  </read_first>
  <action>
    In `package.json` at line 76, the current `test` script reads:
    `"test": "node --test \"tests/{architecture,bridges,domain,edge,helpers,orchestrators,persistence,presentation,shared,transaction}/**/*.test.ts\" \"tests/lint-rules/**/*.test.{js,ts}\"",`

    **Surgical removal only** (per B1): delete the trailing argument `" \"tests/lint-rules/**/*.test.{js,ts}\""` -- i.e., the single space + the second quoted glob argument. **KEEP the brace order unchanged** (do NOT reorder `presentation` or any other brace entry). The `presentation` brace entry stays in this commit; Plan 21-02 removes `presentation` from the brace expansion in BOTH `test` and `test:coverage:unit` scripts.

    Final form for line 76:
    `"test": "node --test \"tests/{architecture,bridges,domain,edge,helpers,orchestrators,persistence,presentation,shared,transaction}/**/*.test.ts\"",`

    Do NOT touch line 80 (`test:coverage:unit`) -- its glob does NOT contain `tests/lint-rules` (only the `{architecture,...,presentation,...}` brace expansion). Plan 21-02 owns the `presentation` removal in BOTH scripts.

    Do NOT touch any other script lines, `dependencies`, `devDependencies`, `peerDependencies`, or `engines`.
  </action>
  <verify>
    <automated>
      <command>! grep "tests/lint-rules" package.json</command>
      <command>grep -c "node --test" package.json</command>
      <command>grep -c "architecture,bridges,domain,edge,helpers,orchestrators,persistence,presentation,shared,transaction" package.json</command>
      <command>npm test 2>&amp;1 | grep -i "no files" | (! grep -q "lint-rules")</command>
    </automated>
  </verify>
  <done>
    `package.json` `test` script no longer references `tests/lint-rules`. Brace expansion order is unchanged (still `{architecture,bridges,domain,edge,helpers,orchestrators,persistence,presentation,shared,transaction}` -- verified via the third grep occurring in BOTH `test` and `test:coverage:unit`). `npm test` does not emit a "no files found" warning for the lint-rules glob. Other scripts and metadata are unchanged.
  </done>
</task>

<task id="21-01-05" type="execute">
  <name>Task 5: Pre-commit gate + atomic single commit per D-21-08</name>
  <files>
    eslint.config.js
    extensions/pi-claude-marketplace/persistence/migrate.ts
    package.json
    tests/architecture/msg-rule-registry.test.ts
    tests/architecture/no-legacy-markers.test.ts
    tests/lint-rules/
  </files>
  <read_first>
    - CLAUDE.md (Git section: pre-commit FIRST; Conventional Commits; ≤72 char title; ≤80 char body; NEVER --no-verify; worktree SKIP=trufflehog note)
    - .planning/phases/21-final-teardown-green-gate/21-PATTERNS.md (Shared cross-cutting patterns: atomic single-commit discipline + Conventional Commits prefix)
    - .planning/phases/21-final-teardown-green-gate/21-VALIDATION.md (Plan 21-01 regression matrix rows)
  </read_first>
  <action>
    Verify the working tree state before committing:
    1. Run `npm run check` and confirm exit code 0. This is the gate. If it fails, diagnose (typecheck / lint / format / tests) and fix; do NOT proceed to commit until GREEN.
    2. Stage the changes with explicit `git add -- eslint.config.js extensions/pi-claude-marketplace/persistence/migrate.ts package.json tests/lint-rules/ tests/architecture/msg-rule-registry.test.ts tests/architecture/no-legacy-markers.test.ts` (git records the directory + file removals). Stage BEFORE running pre-commit so the canonical `git diff --cached --name-only` pattern works.
    3. Run pre-commit using the canonical CLAUDE.md pattern that handles modifications + additions + deletions deterministically:
       ```bash
       pre-commit run --files $(git diff --cached --name-only)
       ```
       Confirm exit code 0. Fix any failures, re-stage, and re-run until clean. NEVER use `--no-verify`. This branch (`gsd/v1.3-replan-catalog`) is NOT a worktree -- do NOT prefix with `SKIP=trufflehog`.
    4. Commit with a single Conventional Commits message. Title (≤72 chars):
       `chore(21): retire MSG-* drift-guard + tests/lint-rules + lint sweep`

       Body (each line ≤80 chars), referencing the closed requirements + decision IDs:
       ```
       Per D-21-04 + D-21-06 (Phase 21 Plan 21-01), retire the v1.3 MSG-* drift
       guard infrastructure in one atomic commit:

       - Delete tests/lint-rules/ (73 files: 34 rule sources + 34 RuleTester
         suites + lib/ helpers + plugin shell). SNM-24.
       - Delete tests/architecture/msg-rule-registry.test.ts (4-way parity test
         no longer applicable). SNM-25.
       - Delete tests/architecture/no-legacy-markers.test.ts per D-21-03; V2
         closed-set type encoding (Reason / StatusToken / Marker unions in
         shared/notify.ts) structurally rejects re-introduction. SNM-28.
       - Rewrite eslint.config.js: drop msgPlugin import + MSG-Blocks 1, 1b, 2,
         3, 4a, 4b, 5, 6 + both tests/lint-rules/** overrides. Update BLOCK A
         message strings to V2-only phrasing (notify / notifyUsageError). Add
         block-level no-console + no-restricted-syntax override for
         persistence/migrate.ts. RETAIN BLOCKS A/B/C/D/E. SNM-27.
       - Remove now-redundant inline `eslint-disable-next-line` at
         persistence/migrate.ts:177; the block-level override supplies the
         equivalent suppression.
       - Update package.json test script to drop the tests/lint-rules glob
         argument (brace order unchanged).

       npm run check GREEN at the post-edit state.
       ```

       Use the HEREDOC form per CLAUDE.md template. Do NOT use `--amend`.
    5. Run `git status` after the commit and confirm the working tree is clean.
  </action>
  <verify>
    <automated>
      <command>npm run check</command>
      <command>! test -d tests/lint-rules</command>
      <command>! test -f tests/architecture/msg-rule-registry.test.ts</command>
      <command>! test -f tests/architecture/no-legacy-markers.test.ts</command>
      <command>! grep "tests/lint-rules" package.json</command>
      <command>! grep "import msgPlugin" eslint.config.js</command>
      <command>! grep -n "eslint-disable-next-line" extensions/pi-claude-marketplace/persistence/migrate.ts</command>
      <command>git log -1 --pretty=%s | grep -E "^chore\\(21\\): retire MSG-\\*"</command>
      <command>git status --porcelain | (! grep -q .)</command>
    </automated>
  </verify>
  <done>
    Single atomic commit landed with a Conventional-Commits-compliant title and body. `npm run check` exits 0. Working tree clean. Closed: SNM-24 (tests/lint-rules gone), SNM-25 (msg-rule-registry gone), SNM-27 (eslint.config.js stock + persistence/migrate.ts override), SNM-28 (no-legacy-markers gone).
  </done>
</task>

</tasks>

<verification>
End-of-plan gate (all from VALIDATION.md regression matrix for Plan 21-01):

```
npm run check                                                                    # full pipeline GREEN
! test -d tests/lint-rules                                                       # SNM-24 post-condition
! test -f tests/architecture/msg-rule-registry.test.ts                           # SNM-25 post-condition
! test -f tests/architecture/no-legacy-markers.test.ts                           # SNM-28 post-condition
! grep -nE "import msgPlugin|msg/(sr-|gr-|nc-|lc-|mr-|rp-|rh-|cc-|sd-|er-|wm-|kc-|kp-)" eslint.config.js   # SNM-27 post-condition
! grep "tests/lint-rules" package.json                                           # RESEARCH §8 CORRECTION 7
! grep "eslint-disable-next-line" extensions/pi-claude-marketplace/persistence/migrate.ts   # D-21-04 inline-directive removal
npm test 2>&1 | grep -i "no files" | (! grep -q "lint-rules")                    # package.json glob hygiene
```

Manual review per VALIDATION.md "Manual-Only Verifications":
- BLOCK A `no-restricted-syntax` selector messages reference `notify` / `notifyUsageError` (not deleted V1 wrappers).
- `package.json` line 76 contains no `tests/lint-rules` substring; brace expansion order is unchanged.
</verification>

<success_criteria>
Closes (from ROADMAP §Phase-21 SC list):
- SC #1 (lint-rules + msg-rule-registry absent) -- Tasks 21-01-03 + 21-01-05.
- SC #3 (eslint.config.js stock rules + persistence/migrate.ts override) -- Tasks 21-01-01 + 21-01-02.
- SC #4 (no-legacy-markers reviewed → DELETE arm) -- Task 21-01-03.

Closes (from REQUIREMENTS.md):
- SNM-24 (`tests/lint-rules/` deleted in full).
- SNM-25 (`tests/architecture/msg-rule-registry.test.ts` deleted).
- SNM-27 (`eslint.config.js` cleaned + `no-restricted-syntax` retained + `no-console` per-file override added).
- SNM-28 (`no-legacy-markers.test.ts` deleted per D-21-03 DELETE arm).
</success_criteria>

<output>
Create `.planning/phases/21-final-teardown-green-gate/21-01-SUMMARY.md` when done, mirroring Plan 20-04 SUMMARY's per-SC + NO-CHANGE-invariant double-table shape per PATTERNS.md "Per-plan SC verification matrix in SUMMARY" pattern.
</output>
</content>
</invoke>
