---
phase: 19-migration-wave-2-plugin-orchestrator-family
plan: 3
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
  - tests/orchestrators/plugin/list.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-19-04 / D-19-05: Plan 19-03 runs in parallel with 19-02 / 19-04 / 19-05 in Wave 2; depends ONLY on Plan 19-01's merged diff. list.ts is a single-shot READ-ONLY surface (no state mutation; no cascade) with the list-surface plugin variants `PluginAvailableMessage`, `PluginUnavailableMessage`, `PluginUpgradableMessage` (per `shared/notify.ts:377-407`) plus optional `PluginInstalledMessage` rows for already-installed plugins."
    - "D-19-01: the `PROBE_FAILURES` summary `notifyWarning` at list.ts:777 is DROPPED entirely. The probe-failure information already manifests as per-row `(unavailable)` plugin variants where the underlying failure causes a plugin to fail probing -- the summary is structurally redundant. The orphaned module-level `PROBE_FAILURES` capture-buffer (line 260, plus all `PROBE_FAILURES.push(...)` call sites) is REMOVED entirely along with the dropped warning -- the buffer had no other consumer (verified by `grep -rEn \"PROBE_FAILURES\" extensions/pi-claude-marketplace/`)."
    - "D-19-02: the 2 surviving V1 sites (success at line 772, failure at line 783) consolidate into V2 `notify(ctx, pi, NotificationMessage)` calls per orchestration arm. list surface uses `mp.status === undefined` (bare label marketplace header) per `docs/output-catalog.md` plugin-list section (lines 133-263). Each plugin row uses one of the list-variant discriminators (`available` / `unavailable` / `upgradable`) or, for installed plugins shown alongside available ones, the `installed` discriminator from the cascade variant set."
    - "Marketplace-level optional fields (`MarketplaceNotificationMessage.details`): the list surface threads `lastUpdatedAt` and `autoupdate` flags into `mp.details` (the renderer's list-surface arm at `shared/notify.ts:619+` consumes `mp.details` for the `<autoupdate>` / `<no autoupdate>` markers and the `last updated <timestamp>` line per `docs/output-catalog.md` list section). The optional `MarketplaceDetails` shape mirrors what V1 list.ts's existing payload construction surfaced; the migration preserves whatever fields the V1 code path already produced (no expansion of detail surface in Phase 19)."
    - "Empty-list case (`MarketplaceNotificationMessage` with `plugins: []` and an empty `marketplaces: []` for a no-marketplaces-configured state): catalog-state `empty` at `docs/output-catalog.md:139-145` shows the form (currently `(empty)` text or similar -- mirror the existing catalog byte exactly). The V2 payload for empty state can be `notify(ctx, pi, { marketplaces: [] })` -- the renderer is responsible for producing the bytes. Verify against the catalog fixture."
    - "D-19-07: tests stay END-TO-END through real `notify()` via existing makeCtx() pattern; byte-exact V2 string assertions replace V1; the dropped `PROBE_FAILURES` summary's test assertions are DELETED outright; `notifications.length` assertions flip from `=== 2` (list success + PROBE_FAILURES summary) to `=== 1`."
  byte_contracts:
    - "Success replacement (list.ts:772, V1 `notifySuccess(ctx, renderPluginList(payload, probe))`): V2 byte string follows `docs/output-catalog.md` plugin-list section across all relevant catalog states (`empty` 139-145, `single-mp-mixed` 149-166, `same-plugin-both-scopes` 170-182, `project-orphan-folded` 186-196, `soft-dep-on-installed` 200-211, `unparseable-mp` 215-226, `zero-plugin-mp-block` 230-241, `multiple-mps` 245-263). Each marketplace block is a `MarketplaceNotificationMessage` with status omitted (bare label header) and `plugins: readonly PluginNotificationMessage[]` in display order. Severity: undefined (info). NO reload-hint (list is read-only; no state-changing variant emitted)."
    - "Failure replacement (list.ts:783, V1 `notifyError(ctx, errorMessage(err), err)`): V2 byte string is a synthetic `MarketplaceNotificationMessage` with `plugins: []` (or a single synthetic `PluginFailedMessage` -- Claude's Discretion per CONTEXT line 110, mirroring the install.ts aggregate-failure question; pick the shape that round-trips cleanly). Severity: `error`. No reload-hint."
    - "DROPPED summary (list.ts:777, V1 `notifyWarning` PROBE_FAILURES drain): NO V2 byte output; the entire `PROBE_FAILURES` module-level capture-buffer (line 260 + all .push(...) sites) is removed."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/list.ts"
      provides: "Single V2 `notify(ctx, pi, NotificationMessage)` call per orchestration arm. Module-level `PROBE_FAILURES` capture-buffer + its drain site removed. `presentation/*` composer imports related to list rendering pruned."
      contains: "notify(ctx, pi, {"
    - path: "tests/orchestrators/plugin/list.test.ts"
      provides: "Byte-exact V2 assertions for list-surface catalog states; PROBE_FAILURES-summary assertions DELETED; notifications.length assertions flipped from 2 to 1 where applicable; existing makeCtx() preserved verbatim per D-19-07"
      contains: "(available)|(unavailable)|(upgradable)|(installed)"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/list.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts::notify"
      via: "single V2 `notify(ctx, pi, message)` call per orchestration arm"
      pattern: "notify\\(ctx,\\s*pi"
    - from: "tests/orchestrators/plugin/list.test.ts"
      to: "docs/output-catalog.md::/claude:plugin list (lines 133-263)"
      via: "byte-exact assertions mirroring all 8 catalog states for the list surface"
      pattern: "\\((available|unavailable|upgradable|installed)\\)"
  coverage_constraints:
    - "Catalog UAT MUST stay GREEN through this plan's merge boundary -- specifically the 8 plugin-list catalog states at `docs/output-catalog.md` lines 137-263."
    - "`PROBE_FAILURES` module-level state MUST be removed: `grep -c \"PROBE_FAILURES\" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` returns 0 (declaration + every push site + the drain site gone)."
    - "Zero V1 callers remain: `grep -cE \"notify(Success|Warning|Error)\\(\" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` returns 0."
    - "No external file references PROBE_FAILURES: `grep -rEn \"PROBE_FAILURES\" extensions/pi-claude-marketplace/` returns matches ONLY inside this file's removed history (post-edit: 0 matches anywhere)."
---

<objective>
Migrate `extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` from V1 severity-named wrappers (3 callsites: `notifySuccess` at 772, `notifyWarning` at 777, `notifyError` at 783) to V2 structured `notify(ctx, pi, NotificationMessage)`. DROP the `PROBE_FAILURES` summary `notifyWarning` at line 777 entirely per D-19-01 (the probe-failure information already manifests as per-row `(unavailable)` plugin variants when the underlying probe failure causes a plugin to fail). Remove the entire `PROBE_FAILURES` module-level capture-buffer at line 260 plus all `PROBE_FAILURES.push(...)` call sites -- the buffer is orphaned after the summary drop. The 2 surviving V1 sites (success at 772 and failure at 783) consolidate into V2 `notify()` calls with list-surface payload construction.

Update `tests/orchestrators/plugin/list.test.ts` in lockstep with byte-exact V2 assertions; delete assertions targeting the dropped PROBE_FAILURES summary; flip `notifications.length` from `=== 2` (success + summary) to `=== 1`.

Drop now-orphaned `presentation/*` composer imports.

Purpose: V2-migrate the read-only list surface. The list surface introduces three plugin-variant discriminators NOT exercised by Plan 19-01's pilot (`available`, `unavailable`, `upgradable`) plus optional list-row scope-bracket suppression per SNM-11 (available/unavailable rows have no scope field by construction per `shared/notify.ts:377-394`).

Output: 2 modified files (list.ts + list.test.ts) in one atomic commit; `PROBE_FAILURES` module-level state fully removed.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-CONTEXT.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-RESEARCH.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-PATTERNS.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-VALIDATION.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-01-SUMMARY.md
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
@docs/output-catalog.md
@tests/architecture/catalog-uat.test.ts
@tests/shared/notify-v2.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate list.ts -- replace 2 surviving V1 sites (772, 783) with V2 notify() calls; DROP PROBE_FAILURES summary (777) + the entire PROBE_FAILURES module-level capture-buffer; drop presentation/* imports</name>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts (full file -- ~800 lines; verify V1 callsite line numbers per 19-RESEARCH.md Code Examples: 772, 777, 783; locate the `PROBE_FAILURES` module-level declaration at line 260 plus every push call site via `grep -nE "PROBE_FAILURES" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts`; verify `ListPluginsOptions` carries `readonly pi: ExtensionAPI`).
    - extensions/pi-claude-marketplace/shared/notify.ts lines 325-407 (per-variant PluginNotificationMessage types -- especially `PluginAvailableMessage` 377-381, `PluginUnavailableMessage` 389-394, `PluginUpgradableMessage` 402-407, `PluginInstalledMessage` 325-331) + lines 481-516 (MarketplaceNotificationMessage + MarketplaceDetails reference).
    - docs/output-catalog.md lines 133-263 (the 8 plugin-list catalog states: `empty` 139-145, `single-mp-mixed` 149-166, `same-plugin-both-scopes` 170-182, `project-orphan-folded` 186-196, `soft-dep-on-installed` 200-211, `unparseable-mp` 215-226, `zero-plugin-mp-block` 230-241, `multiple-mps` 245-263 -- binding byte forms).
    - tests/architecture/catalog-uat.test.ts (FIXTURES map entries for `/claude:plugin list` -- the V2 payload-shape reference for each list state).
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts (Plan 19-01 merged diff; recipe block-comment locatable via `grep -n "NotificationMessage cascade recipe" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts`).
    - 19-CONTEXT.md `<decisions>` D-19-01 (DROP PROBE_FAILURES summary; CONTEXT line 50) + D-19-02 (inline construction) + D-19-07 (test discipline).
    - 19-RESEARCH.md "Code Examples > list.ts V1 callsite line numbers" (line 362-371) + "PROBE_FAILURES capture buffer at line 260" (Primary Sources line 690).
    - 19-PATTERNS.md "Pattern Assignments > (a) Orchestrator Emission Migration > Single-`notify()`-call construction pattern" + "(b) Test Migration > Byte-equality assertion pattern" + (the list surface is structurally simpler than cascade so no cascade-construction pattern applies).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts (Phase 18 precedent for list-surface V2 migration; the marketplace-list and plugin-list surfaces share the bare-label-header / details-bearing form).
  </read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/list.ts</files>
  <action>
    Operate in this order:

    1. Update imports. DROP: `notifySuccess`, `notifyWarning`, `notifyError` from `../../shared/notify.ts`; `renderPluginList` from `../../presentation/...` (audit the import path) + any presentation/* composers list.ts currently consumes (e.g., `renderRow`, sort utilities, compact-line types). ADD: `notify` from `../../shared/notify.ts`; per-variant types `PluginAvailableMessage`, `PluginUnavailableMessage`, `PluginUpgradableMessage`, `PluginInstalledMessage`, `MarketplaceNotificationMessage`, and `MarketplaceDetails` (used for inline payload-construction type annotations).

    2. REMOVE the entire `PROBE_FAILURES` module-level state. Locate via `grep -nE "PROBE_FAILURES" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts`. Delete:
       - The declaration at line 260 (the `let PROBE_FAILURES: ...` or `const PROBE_FAILURES: ...` line plus any associated type alias).
       - Every `PROBE_FAILURES.push(...)` call site throughout the file (the probes during list-row construction that pushed failure records into the buffer).
       - Any `PROBE_FAILURES = []` reset call (typically at the top of the orchestrator's entry function).
       - The drain site at line 774 (the iteration that built the summary message + `notifyWarning(ctx, summary)` -- the entire `if (PROBE_FAILURES.length > 0) { ... }` block goes).

       Verify with `grep -c "PROBE_FAILURES" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` returning 0.

       Important: the underlying probe failure paths STAY -- when a plugin's probe fails during list construction, the resulting plugin row gets the `(unavailable)` discriminator (or whatever the existing fallback variant was). Only the BUFFERING + SUMMARY surfacing is removed; the probe-failure-to-unavailable-row translation is intact.

    3. CONSOLIDATE the success path at line 772 (V1 `notifySuccess(ctx, renderPluginList(payload, probe))`) into a V2 `notify()` call. Replace the V1 sequence (the surrounding `const payload: ListPayload = ...; const probe: SoftDepProbe = softDepStatus(pi); const body = renderPluginList(payload, probe); notifySuccess(ctx, body);` shape) with a single V2 `notify(ctx, pi, ...)` call. Payload construction:

       The orchestrator must iterate the resolved list of marketplaces (in display order per D-16-06; existing sort logic moves into the payload-construction loop) and construct a `marketplaces: MarketplaceNotificationMessage[]` array. For each marketplace, build:
       ```
       const mp: MarketplaceNotificationMessage = {
         name: <marketplace name>,
         scope: <marketplace scope>,
         // status: omitted (bare label header for list surface)
         ...(<has details to surface> && {
           details: {
             // Mirror whatever the V1 renderPluginList currently surfaces:
             // autoupdate flag, lastUpdatedAt timestamp, etc.
             ...(<autoupdate flag set> && { autoupdate: <bool> }),
             ...(<lastUpdatedAt available> && { lastUpdatedAt: <ISO string or Date> }),
           },
         }),
         plugins: <iterator that builds PluginNotificationMessage[] per row, picking the right discriminator>,
       };
       ```

       For each plugin row, pick the discriminator based on the row's V1 state:
       - Installed + up-to-date → `PluginInstalledMessage` with required `dependencies: readonly Dependency[]`, optional `version`, optional `scope` (when plugin scope differs from marketplace scope per orphan-fold; otherwise omit per renderScopeBracket's same-scope contract -- see `shared/notify.ts:719+`).
       - Installed + upgradable → `PluginUpgradableMessage` with required `reasons: readonly Reason[]`, optional `version`, optional `scope`.
       - Not installed + installable → `PluginAvailableMessage` with optional `version`. NO `scope` field (SNM-11 carve-out per `shared/notify.ts:373-381`).
       - Not installed + not installable → `PluginUnavailableMessage` with required `reasons: readonly Reason[]`, optional `version`. NO `scope` field.

       For the unparseable-marketplace case (catalog-state `unparseable-mp` at lines 215-226), the marketplace's `reasons` field carries the diagnostic; `plugins: []`. Mirror the existing V1 `renderPluginList` behavior for this state.

       For the empty (no marketplaces) case (catalog-state `empty` at lines 139-145), pass `notify(ctx, pi, { marketplaces: [] })` and verify the renderer produces the catalog bytes; if not, construct a minimal payload that does (Claude's Discretion -- this is structurally a Phase-16 renderer concern that should already be wired correctly, but verify).

    4. CONSOLIDATE the failure path at line 783 (V1 `notifyError(ctx, errorMessage(err), err)`) into a V2 `notify()` call. The list-failure surface lacks a dedicated catalog state (the plugin-list section covers success states only); per CONTEXT line 110 Claude's Discretion (mirroring the install.ts aggregate-failure question), pick one of:

       Option A (preferred -- minimal payload): `notify(ctx, pi, { marketplaces: [] })` plus surface the error in a top-level mp.reasons[] field if MarketplaceNotificationMessage supports it. (The MarketplaceNotificationMessage type at `shared/notify.ts:502-516` carries `readonly reasons?: readonly Reason[]` -- the optional reasons can communicate the list-load failure.) The renderer emits something equivalent to the V1 `errorMessage(err)` string under the V2 grammar.

       Option B: build a synthetic `MarketplaceNotificationMessage` with `status: "failed"` and a placeholder name (e.g., the affected marketplace if known, or "(list)"). This mirrors the marketplace-update / marketplace-remove failure surfaces.

       Pick whichever round-trips cleanly through `notify()` and minimizes test-byte churn; document choice in the SUMMARY. Severity: error (computed by `notify()` per D-16-11).

    5. EMBED a single-line reference comment ABOVE the success-path `notify()` call cross-linking the Plan 19-01 recipe (mirroring Plan 19-02's pattern): `// V2 notify() call mirrors the Plan 19-01 pilot recipe at orchestrators/plugin/uninstall.ts; list.ts substitutes the list-surface plugin variants (available / unavailable / upgradable / installed) per D-19-02.`.

    6. VERIFY post-edit invariants:
       - `grep -cE "notify(Success|Warning|Error)\(" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` returns 0.
       - `grep -c "PROBE_FAILURES" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` returns 0.
       - `grep -rEn "PROBE_FAILURES" extensions/pi-claude-marketplace/` returns 0 matches anywhere in the codebase.
       - `grep -c "renderPluginList" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` returns 0 (V1 composer no longer called from this file -- the function may still exist in `presentation/` for Phase 21 cleanup; do NOT delete it here per CONTEXT D-19-02).
       - `grep -c "presentation/" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` returns 0 (or bounded to non-notify utilities).

    Honors D-19-01 (PROBE_FAILURES summary + buffer removed), D-19-02 (inline construction; no composer modifications -- `renderPluginList` is just orphaned, not deleted), D-19-06 (disjoint file pair), D-16-04 + D-16-11 + D-16-12 + D-16-14 (renderer-as-spec).
  </action>
  <verify>
    <automated>node --import tsx --test tests/orchestrators/plugin/list.test.ts</automated>
  </verify>
  <done>
    list.ts compiles under strict TypeScript; imports no longer reference V1 wrappers or `renderPluginList`; the `PROBE_FAILURES` module-level state is fully removed (declaration + every push site + the drain site gone); exactly one V2 `notify(ctx, pi, ...)` call survives per orchestration arm (success + failure); a single-line reference comment cross-links the Plan 19-01 recipe location. Test file updated in Task 2; atomic commit captures both.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update list.test.ts -- byte-exact V2 assertions across 8 plugin-list catalog states; DELETE PROBE_FAILURES-summary assertion; flip notifications.length from 2 to 1</name>
  <read_first>
    - tests/orchestrators/plugin/list.test.ts (full file; existing makeCtx() per 19-RESEARCH.md; locate via grep: V1 byte-string assertions, notifications-length assertions, PROBE_FAILURES-summary-targeting assertions).
    - docs/output-catalog.md lines 133-263 (binding V2 byte forms for all 8 plugin-list catalog states).
    - tests/shared/notify-v2.test.ts (per-variant V2 byte fixtures including the orphan-fold scope-bracket carve-outs added in Phase 17.2).
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts (post-Task-1 state -- the V2 payload shape the test must assert against).
    - 19-CONTEXT.md D-19-07 + "Test count consequence of D-19-01".
    - 19-PATTERNS.md "Pattern Assignments > (b) Test Migration".
    - tests/orchestrators/marketplace/list.test.ts (Phase 18 list-surface migration precedent for byte-exact V2 assertions + makeCtx preservation).
  </read_first>
  <files>tests/orchestrators/plugin/list.test.ts</files>
  <action>
    Apply V1 -> V2 test rewrites in this order:

    1. SCAN: `grep -n "notifySuccess\|notifyError\|notifyWarning\|notifications.length\|PROBE_FAILURES\|probe.failed\|(available)\|(unavailable)\|(upgradable)\|(installed)" tests/orchestrators/plugin/list.test.ts`. Map V1-byte assertions to their V1 callsites (success at 772 / failure at 783 / summary at 777).

    2. KEEP `makeCtx()` VERBATIM per D-19-07.

    3. REWRITE byte assertions for the success arm (V1 line 772). Different tests exercise different list states; per test, the V2 byte string follows the matching catalog state at `docs/output-catalog.md:133-263`. Sample shapes:

       Empty:
       `assert.equal(note.message, "(empty)");` (or whatever exact form the catalog `empty` state at lines 139-145 specifies; replicate verbatim).

       Single marketplace, mixed plugins (catalog `single-mp-mixed` 149-166):
       `assert.equal(note.message, "● official [user]\n  ● alpha v1.0.0 (installed)\n  ⊘ beta (unavailable) {<reason>}\n  ○ gamma (available)\n  ⊘ delta v1.0.0 (upgradable) {v2.0.0 available}");`

       Soft-dep markers (catalog `soft-dep-on-installed` 200-211):
       The installed-row variant emits soft-dep markers when the renderer's `softDepStatus(pi)` probe returns unloaded for the declared dependency; the test wires `pi: { getAllTools: piWithSubagentsUnloaded() }` to drive the renderer. Substitute the catalog's expected byte string verbatim.

       Severity: `undefined` (info per D-16-11). NO reload-hint trailer (list is read-only; no state-changing variant per D-16-12).

    4. REWRITE byte assertions for the failure arm (V1 line 783). The V2 byte string follows the chosen failure shape from Task 1 step 4 (Option A or B). Severity: `"error"`. NO reload-hint.

    5. DELETE every test or assertion targeting the PROBE_FAILURES summary surface:
       - `grep -in "PROBE_FAILURES\|probe.*failed\|probe.*summary" tests/orchestrators/plugin/list.test.ts` to enumerate.
       - For each match: if the test exists SOLELY to exercise the PROBE_FAILURES summary surface, DELETE the entire test. If a `notifications.length === 2` assertion is part of a larger test that also asserts on the list-success bytes, flip to `=== 1` and delete the summary-specific assertions (the message-substring check on the PROBE_FAILURES summary text).

    6. ADJUST `notifications.length` assertions. Where V1 expected `=== 2` (success + summary), flip to `=== 1`. Use `grep -n "notifications.length, [2-9]" tests/orchestrators/plugin/list.test.ts` to enumerate.

    7. KEEP unchanged: state assertions (list payload shape via internal API), error-throw assertions, NFR-5 path-source no-network assertions, scope independence assertions.

    8. VERIFY post-edit:
       - `grep -cE "\\((available|unavailable|upgradable|installed)\\)" tests/orchestrators/plugin/list.test.ts` returns ≥1 per discriminator exercised.
       - `grep -c "PROBE_FAILURES" tests/orchestrators/plugin/list.test.ts` returns 0.
       - `grep -cE "notifications.length,\s*[2-9]" tests/orchestrators/plugin/list.test.ts` returns 0.

    Honors D-19-07 + D-19-01 test-count consequence.
  </action>
  <verify>
    <automated>node --import tsx --test tests/orchestrators/plugin/list.test.ts &amp;&amp; node --import tsx --test tests/architecture/catalog-uat.test.ts</automated>
  </verify>
  <done>
    All list.test.ts tests pass; byte assertions match `docs/output-catalog.md:133-263` across the 8 plugin-list catalog states; PROBE_FAILURES summary-targeting tests / assertions DELETED; test count assertions flipped to `=== 1`; `makeCtx()` preserved verbatim; severity assertions are `undefined` (success) or `"error"` (failure). Catalog UAT GREEN end-to-end. `npm run check` passes at atomic-commit merge.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal API refactor on a read-only surface. No state mutation; no side-effect change; only the rendering layer changes. Same `ctx.ui.notify` host channel (IL-2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | I (Information disclosure -- PROBE_FAILURES content silenced) | PROBE_FAILURES drain removed | accept | D-19-01 explicitly accepts that the probe-failure summary surface is silenced. The underlying probe failure information surfaces per-row through the `PluginUnavailableMessage` variant (which carries `reasons: readonly Reason[]` describing the unavailability cause). Net: information surface narrows from "summary block at end of list output" to "per-row reason in (unavailable) line" -- the user-visible content is preserved at the row level. |
| T-19-03-02 | D (Notification flooding) | Multiple notify() calls per list invocation | mitigate | D-19-01 drops the summary site; D-19-02 consolidates success + failure into single notify() per arm. List invocation emits exactly 1 notification. |
| T-19-03-03 | T (Severity manipulation) | Orchestrator-supplied severity argument | mitigate | V2 has no severity argument; renderer computes per D-16-11. |
</threat_model>

<verification>
- `node --import tsx --test tests/orchestrators/plugin/list.test.ts` exits 0.
- `node --import tsx --test tests/architecture/catalog-uat.test.ts` exits 0.
- `npm run check` exits 0 at the atomic commit boundary.
- `grep -cE "notify(Success|Warning|Error)\(" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` returns 0.
- `grep -c "PROBE_FAILURES" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` returns 0.
- `grep -rEn "PROBE_FAILURES" extensions/pi-claude-marketplace/` returns 0 matches anywhere in the codebase.
- `grep -cE "notifications.length,\s*[2-9]" tests/orchestrators/plugin/list.test.ts` returns 0.
</verification>

<success_criteria>
- list.ts emits exactly one V2 `notify(ctx, pi, NotificationMessage)` call per orchestration arm; module-level PROBE_FAILURES capture-buffer + drain site fully removed; per-row `(unavailable)` discriminator preserves the underlying probe-failure information at row granularity.
- list.test.ts assertions match V2 catalog byte forms across all 8 plugin-list catalog states; PROBE_FAILURES-summary assertions DELETED; test count assertions flipped to `=== 1`; `makeCtx()` preserved verbatim.
- `npm run check` GREEN; catalog UAT GREEN end-to-end.
</success_criteria>

<output>
Create `.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-03-SUMMARY.md` documenting:
- The 2 files modified (list.ts + list.test.ts) + the fully-removed `PROBE_FAILURES` module-level capture-buffer (declaration line + all push sites + the drain site).
- The 3 V1 callsites resolved: 2 migrated to V2 notify() (lines 772 success, 783 failure); 1 DROPPED (line 777 PROBE_FAILURES summary).
- The list-surface plugin variant set exercised (`available`, `unavailable`, `upgradable`, `installed`) including any orphan-fold scope-bracket cases per Phase 17.2 corrections.
- Choice on failure-path shape (Option A minimal payload vs Option B synthetic failed marketplace) per CONTEXT line 110 Claude's Discretion.
- Confirmation that catalog UAT + `npm run check` both pass.
</output>
