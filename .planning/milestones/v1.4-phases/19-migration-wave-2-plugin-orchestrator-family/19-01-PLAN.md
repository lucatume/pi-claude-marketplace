---
phase: 19-migration-wave-2-plugin-orchestrator-family
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - tests/orchestrators/plugin/uninstall.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-19-04 / D-19-05: Plan 19-01 is the Wave 1 pilot for Phase 19. It migrates the smallest plugin orchestrator (`uninstall.ts`, 5 V1 callsites) FIRST so Wave 2 plans (19-02..05) can mirror the locked V2 inline-cascade construction recipe verbatim. Wave 2 has zero dependencies on each other and depends solely on Plan 19-01's merged diff."
    - "D-19-01 precedent extension (RESEARCH §Code Examples >uninstall.ts + RESEARCH Open Question 2): both `notifyWarning` sites at uninstall.ts:179 (cache-refresh failure) and uninstall.ts:200 (data-dir cleanup-leak warning) are DROPPED entirely. The surrounding try/catch blocks stay; the underlying side effects (cache refresh, dir cleanup) still fire; only the user-visible second-notification disappears. The retained try/catch carries a one-line comment citing D-19-01 precedent (D-18-01 lineage) so future readers know the silencing is deliberate."
    - "D-19-07: tests stay END-TO-END through real `notify()` via the existing `makeCtx()` mock at tests/orchestrators/plugin/uninstall.test.ts:44-66; byte-exact V2 string assertions replace V1 byte strings; the `{ message, severity }` tuple recording pattern is preserved verbatim. Test count assertions that previously expected `notifications.length === 2` (success + dropped warning) flip to `notifications.length === 1`; tests specifically targeting the content of the dropped warnings are DELETED outright."
    - "D-16-11 + D-16-12 (renderer-owned severity + reload-hint): the success path's V2 byte string is `\"● <marketplace> [<scope>]\\n  ○ <plugin> v<version> (uninstalled)\\n\\n/reload to pick up changes\"` (severity undefined; reload-hint POSITIVE because `uninstalled` is in the state-changing set). The failure path's V2 byte string is `\"● <marketplace> [<scope>]\\n  ⊘ <plugin> v<version> (failed) {<reason>}\\n    cause: <error message>\"` (severity `error`; no reload-hint -- a failed uninstall did not remove anything per `docs/output-catalog.md:376`)."
    - "RESEARCH Open Question 2: Plan 19-01 embeds the pilot recipe block-comment directly above the single surviving `notify(opts.ctx, opts.pi, ...)` call site. The 10-line comment uses the header `NotificationMessage cascade recipe (Plan 19-01 pilot; Wave 2 mirrors).` so Wave 2 agents locate it via `grep -n \"NotificationMessage cascade recipe\" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts`. Discriminator section enumerates the plugin status set (uninstalled / installed / updated / reinstalled / failed / skipped / manual recovery / available / unavailable / upgradable). Reference cite: `docs/output-catalog.md:340-378` (plugin uninstall fixtures)."
  byte_contracts:
    - "Single-shot success replacement (uninstall.ts:246, `notifySuccess(ctx, appendReloadHint(body, hint))`): the V2 notify() call produces byte string matching `docs/output-catalog.md:344-348` (catalog-state `success`): `● <marketplace> [<scope>]\\n  ○ <plugin> v<version> (uninstalled)\\n\\n/reload to pick up changes`. Severity: undefined (info per D-16-11). Reload-hint trailer is appended by `notify()` per D-16-12 (uninstalled is state-changing)."
    - "Defensive-guard success replacement (uninstall.ts:232, `notifySuccess(ctx, body)`): V2 byte string is identical-shape to the line-246 success because `notify()` is the single rendering surface; whether to keep the defensive arm as a separate notify call or fold its absence into the same path is a Claude-discretion call (RESEARCH suggests `MarketplaceNotificationMessage` with `plugins: []` or with a single `PluginUninstalledMessage` -- pick whichever round-trips cleanly through `notify()` for an entity that is already absent)."
    - "Direct-path failure replacement (uninstall.ts:160, `notifyError(ctx, errorMessage(err), err)`): V2 byte string matches `docs/output-catalog.md:370-374` (catalog-state `failure-permission-denied`): `● <marketplace> [<scope>]\\n  ⊘ <plugin> v<version> (failed) {<reason>}\\n    cause: <cause message>`. Plugin variant is `PluginFailedMessage` with `reasons: readonly Reason[]` derived from the thrown error (or a closed-set fallback when no reason classifies) and `cause: err` threaded directly. Severity: `error`. NO reload-hint."
    - "DROPPED warnings (uninstall.ts:179 cache-refresh failure, uninstall.ts:200 data-dir cleanup-leak): NO V2 byte output; zero secondary notify() calls; surrounding try/catch retained with explanatory comment."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts"
      provides: "Single V2 `notify(opts.ctx, opts.pi, NotificationMessage)` call replacing the 3 surviving V1 wrapper invocations (lines 160, 232, 246); two DROPPED V1 sites (179, 200); now-orphaned `presentation/*` composer imports removed; NotificationMessage cascade recipe block-comment for Wave 2 to mirror"
      contains: "NotificationMessage cascade recipe (Plan 19-01 pilot; Wave 2 mirrors)"
    - path: "tests/orchestrators/plugin/uninstall.test.ts"
      provides: "Byte-exact V2 assertions for success + failure paths; deleted assertions for the two dropped warnings; existing `makeCtx()` preserved verbatim per D-19-07"
      contains: "(uninstalled)\\n\\n/reload to pick up changes"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts::notify"
      via: "single V2 `notify(ctx, pi, message)` call per orchestration outcome"
      pattern: "notify\\(opts\\.ctx,\\s*opts\\.pi"
    - from: "tests/orchestrators/plugin/uninstall.test.ts"
      to: "docs/output-catalog.md::/claude:plugin uninstall (lines 336-377)"
      via: "byte-exact `assert.equal(note.message, ...)` mirroring catalog fixtures `success` + `failure-permission-denied`"
      pattern: "● [a-z]+ \\[(user|project)\\]\\n  ○ [a-z]+ v[0-9.]+ \\(uninstalled\\)"
  coverage_constraints:
    - "Catalog UAT (`tests/architecture/catalog-uat.test.ts`) byte-equality MUST stay GREEN at this plan's merge boundary. The plugin-uninstall fixtures (catalog-state `success`, `success-soft-dep-omitted`, `failure-permission-denied`) drive `notify()` via the FIXTURES map; if uninstall.ts's payload-construction round-trips to those exact bytes, the UAT passes."
    - "`npm run check` MUST be GREEN at the merge of this plan (orchestrator + test edits land in the SAME atomic commit -- partial landings would either break tests or trigger MSG-Block 1 lint violations because the lint narrowing for plugin family happens only in Plan 19-06)."
    - "Zero V1 callers remain in uninstall.ts after this plan: `grep -cE \"notify(Success|Warning|Error)\\(\" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns 0 (comments tolerated; the grep targets CallExpression form only)."
    - "Recipe block-comment present and locatable: `grep -c \"NotificationMessage cascade recipe\" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns 1."
---

<objective>
Migrate `extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` from V1 severity-named wrappers (5 callsites: `notifyError` at 160; `notifyWarning` at 179, 200; `notifySuccess` at 232, 246) to the V2 structured entrypoint `notify(opts.ctx, opts.pi, NotificationMessage)`. DROP both `notifyWarning` post-state-commit sites entirely per D-19-01 precedent (D-18-01 lineage); the surrounding try/catch blocks stay so the underlying cache-refresh and data-dir cleanup paths still fire. Update `tests/orchestrators/plugin/uninstall.test.ts` in lockstep with byte-exact V2 assertions per D-19-07; delete assertions targeting the two dropped warnings. Drop now-orphaned `presentation/*` composer imports. Embed a 10-line `NotificationMessage cascade recipe (Plan 19-01 pilot; Wave 2 mirrors).` block-comment directly above the single surviving `notify()` call site so Wave 2 plans (19-02..05) literally mirror the construction pattern.

Purpose: lock the V2 inline-construction pattern on the smallest plugin orchestrator (single-shot uninstalled success + direct-path failure with cause chain -- both shapes that Plans 19-02..05 reuse in install / list / reinstall / update). Wave 2's 4 parallel agents inherit the pattern by reading the merged uninstall.ts diff plus the embedded recipe block-comment. The 2 DROPPED post-success warnings establish the D-19-01 precedent for Plans 19-02..05, each of which drops 1-5 of their own post-success warnings under the same precedent.

Output: 2 modified files (uninstall.ts + uninstall.test.ts) in one atomic commit. uninstall.ts emits exactly one V2 `notify()` call per orchestration outcome (success or failure); uninstall.test.ts asserts the byte strings `● <marketplace> [<scope>]\n  ○ <plugin> v<version> (uninstalled)\n\n/reload to pick up changes` (success) and `● <marketplace> [<scope>]\n  ⊘ <plugin> v<version> (failed) {<reason>}\n    cause: <cause message>` (failure) per the catalog UAT plugin-uninstall fixtures.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-CONTEXT.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-RESEARCH.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-PATTERNS.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-VALIDATION.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-CONTEXT.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-01-SUMMARY.md
@extensions/pi-claude-marketplace/shared/notify.ts
@docs/output-catalog.md
@tests/architecture/catalog-uat.test.ts
@tests/shared/notify-v2.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate uninstall.ts -- one V2 notify() call replacing 3 surviving V1 sites (160, 232, 246); DROP 2 post-success warnings (179, 200); drop presentation/* imports; embed the Wave-1-pilot recipe block-comment</name>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts (full file -- V1 callsites at lines 160, 179, 200, 232, 246 per 19-RESEARCH.md Code Examples; `UninstallPluginOptions` already carries `readonly pi: ExtensionAPI` per 19-RESEARCH.md "Confirmed: All plugin orchestrators already accept pi: ExtensionAPI").
    - extensions/pi-claude-marketplace/shared/notify.ts lines 325-479 (per-variant PluginNotificationMessage types) + lines 481-525 (MarketplaceNotificationMessage type) + lines 719-928 (`renderScopeBracket` + `renderPluginRow` -- read-only reference for the bytes the renderer emits) + line 1034 (the V2 `notify(ctx, pi, message)` entry signature).
    - docs/output-catalog.md lines 336-378 (the 3 plugin-uninstall catalog states: `success`, `success-soft-dep-omitted`, `failure-permission-denied` -- the binding byte forms for this plan's emissions).
    - tests/architecture/catalog-uat.test.ts (FIXTURES map entries for `/claude:plugin uninstall <plugin>@<marketplace>` -- the reference V2 payload shape constructed per state; use as a copy-paste template for the orchestrator's payload-construction code).
    - 19-CONTEXT.md `<canonical_refs>` "Source files Phase 19 modifies" → uninstall.ts entry (line 153) + `<decisions>` D-19-01 (DROP precedent) + D-19-05 (pilot recipe block-comment specification) + D-19-07 (test discipline).
    - 19-RESEARCH.md "Code Examples > uninstall.ts V1 callsite line numbers" (line 345-360) + "Phase 18 pilot recipe block-comment" (line 481-513 -- the verbatim source the Plan 19-01 recipe mirrors) + Pitfall 5 (notifications-length flip in tests) + Open Question 2 (DROP-with-comment recommendation for lines 179 + 200).
    - 19-PATTERNS.md "Pattern Assignments > (a) Orchestrator Emission Migration > Single-`notify()`-call construction pattern" (line 101-136 -- the single-shot plugin-variant template) + "Error handling / dropped post-success warnings" (line 213-241 -- the try/catch-retained-with-comment pattern).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts (Phase 18 pilot precedent; especially lines 150-179 covering the cache-leak DROP pattern AND the V2 notify() call structure that Plan 19-01's recipe block-comment mirrors structurally).
    - .planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-01-SUMMARY.md (verbatim Phase 18 recipe text + exact location info Wave 2 used to find it).
  </read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts</files>
  <action>
    Operate in this order:

    1. Update imports. DROP: `notifySuccess`, `notifyWarning`, `notifyError` from `../../shared/notify.ts`; any `presentation/*` composer currently used by uninstall.ts (the file currently imports `renderRow` / `appendReloadHint` / `reloadHint` / `causeChainTrailer` / `errorMessage` / `MARKETPLACE_LABEL_PROBE` -- audit by reading the import block and dropping every binding that becomes orphaned after Task 1 lands). ADD: `notify` from `../../shared/notify.ts`; per-variant types `PluginUninstalledMessage`, `PluginFailedMessage` from the same module (used to annotate inline payload construction so the discriminated-union narrowing is structural). Keep `errorMessage` only if Reason-classification of the thrown error genuinely requires it -- otherwise drop it too; `notify()` consumes `cause: Error` directly without intermediate stringification.

    2. DROP the cache-refresh warning at line 179 entirely (D-19-01 precedent, RESEARCH Open Question 2). Keep the surrounding try/catch; the cache-refresh side effect still fires inside `try { ... }`. Replace the `notifyWarning(ctx, ...)` line with the explanatory comment: `// D-19-01 precedent (D-18-01 lineage): cache-refresh failures are swallowed silently in V2. The cache-refresh side effect still fires; only the user-visible warning surface is gone (no clean MarketplaceNotificationMessage representation for a post-success "soft warning").`. If the catch block becomes entirely empty after the notify removal, leave the comment as the body.

    3. DROP the data-dir cleanup-leak warning at line 200 entirely under the same precedent (D-19-01). Mirror the comment from step 2 but reference data-dir cleanup: `// D-19-01 precedent (D-18-01 lineage): post-uninstall data-dir cleanup leaks are swallowed silently in V2. The cleanup side effect still fires; only the user-visible warning surface is gone.`.

    4. CONSOLIDATE the two surviving success notify sites at lines 232 (`notifySuccess(ctx, body)` -- the defensive-guard arm where the entity was already absent) and 246 (`notifySuccess(ctx, appendReloadHint(body, hint))` -- the canonical post-uninstall success arm) into V2 `notify()` calls. Both arms produce the same V2 byte shape because `notify()` is the single rendering surface; per Claude's Discretion (CONTEXT line 109-111 + RESEARCH §Open Question 2), construct each as a `MarketplaceNotificationMessage` with `plugins: [{ status: "uninstalled", name: <plugin>, ...(<version present> && { version: <version> }), ...(<plugin scope differs from mp scope> && { scope: <plugin scope> }) }]` and a bare marketplace header (status omitted -- the plugin-uninstall surface uses a label-only marketplace block per `docs/output-catalog.md:344-348`). The V1 `appendReloadHint(body, hint)` line composition is GONE; `notify()` derives the reload-hint per D-16-12 (uninstalled is in the state-changing set).

    5. REPLACE the direct-path failure at line 160 (`notifyError(ctx, errorMessage(err), err)`) with a single V2 `notify(opts.ctx, opts.pi, ...)` call constructing a `MarketplaceNotificationMessage` with a single `PluginFailedMessage` row. The plugin variant is `{ status: "failed", name: <plugin>, reasons: <classifiedReasons>, ...(<version> && { version }), ...(<plugin scope> && { scope }), cause: err instanceof Error ? err : new Error(errorMessage(err)) }`. `classifiedReasons` is derived from the thrown error class -- prefer a closed-set classifier (mirror Phase 18 marketplace/remove.ts's narrowCascadeFailure pattern if applicable; otherwise pick the closest closed-set Reason from `shared/grammar/reasons.ts` -- e.g. `"permission denied"` for EACCES). If no reason classifies, `reasons: []` is acceptable (the renderer handles empty `reasons` gracefully per D-15-01). Severity is computed by `notify()` per D-16-11 (any failed plugin → error); the orchestrator MUST NOT pass severity.

    6. EMBED the pilot recipe block-comment DIRECTLY ABOVE the single surviving `notify(opts.ctx, opts.pi, ...)` call at the consolidated success site. Use exactly this text (10 lines, mirroring Phase 18 add.ts:160-169 substituting plugin-cascade semantics):

       Line 1: `// NotificationMessage cascade recipe (Plan 19-01 pilot; Wave 2 mirrors).`
       Line 2: `// - One MarketplaceNotificationMessage per affected marketplace, emitted`
       Line 3: `//   via a single notify(opts.ctx, opts.pi, ...) call per orchestration.`
       Line 4: `// - plugins: readonly PluginNotificationMessage[] in display order`
       Line 5: `//   (orchestrator-controlled iteration per D-16-06; notify() does not sort).`
       Line 6: `// - Discriminators by status: "uninstalled" here. Plans 19-02..05 mirror`
       Line 7: `//   with their own status sets: installed/updated/reinstalled/failed/`
       Line 8: `//   skipped/manual recovery/available/unavailable/upgradable.`
       Line 9: `// - Severity + "/reload to pick up changes" trailer are computed by notify()`
       Line 10: `//   per D-16-11 + D-16-12; callers MUST NOT compose them.`
       (Optional additional reference line: `// - Reference: catalog UAT plugin-uninstall fixtures at docs/output-catalog.md:340-378.`)

       The Wave 2 agents will locate this band via `grep -n "NotificationMessage cascade recipe" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts`. Do NOT inline runnable code inside the comment -- the surrounding `notify()` call IS the runnable example.

    7. VERIFY after editing: `grep -cE "notify(Success|Warning|Error)\(" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns 0; `grep -c "NotificationMessage cascade recipe" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns 1; `grep -c "from \"../../presentation/" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns 0 OR is bounded to a small set of bindings that genuinely remain in use (e.g., if a non-notify presentation helper is consumed elsewhere in the file -- audit case-by-case; the goal is zero notify-related composer imports).

    Honors D-19-01 (DROP precedent expanded for plugin family), D-19-02 (inline construction in orchestrator), D-19-05 (pilot recipe block-comment placement), D-19-06 (disjoint file pair: only uninstall.ts touched among orchestrators), and D-16-04 + D-16-11 + D-16-12 + D-16-14 (renderer-as-spec; severity / reload-hint / soft-dep all computed by notify()).
  </action>
  <verify>
    <automated>node --import tsx --test tests/orchestrators/plugin/uninstall.test.ts</automated>
  </verify>
  <done>
    uninstall.ts compiles under strict TypeScript (`npx tsc --noEmit` returns 0 for the file in isolation); imports no longer reference `notifySuccess` / `notifyWarning` / `notifyError` from `shared/notify.ts`; `presentation/*` composer imports related to notification rendering are gone; exactly one V2 `notify(opts.ctx, opts.pi, ...)` call survives per orchestration arm (success arm + failure arm; the defensive-guard branch reuses the same `notify()` shape as the canonical success path); the 10-line construction-recipe block-comment is directly above the consolidated success-arm `notify()` call. Note: the test file is updated in Task 2 -- this task's verify command may fail until Task 2 lands; the atomic commit captures both together per RESEARCH §Validation Architecture.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update uninstall.test.ts -- byte-exact V2 assertions for success + failure; DELETE assertions for the 2 dropped warnings (notifications.length flips from 2 -> 1 where applicable)</name>
  <read_first>
    - tests/orchestrators/plugin/uninstall.test.ts (full file -- existing `makeCtx()` at lines 44-66 per 19-RESEARCH.md Code Examples line 518-523; the V1 byte-string assertions and notifications-length assertions need locating by grep before editing).
    - docs/output-catalog.md lines 336-378 (binding V2 byte forms for plugin-uninstall: catalog-state `success` lines 344-348, `success-soft-dep-omitted` lines 357-361, `failure-permission-denied` lines 370-374).
    - tests/shared/notify-v2.test.ts (per-variant V2 byte fixtures; cross-reference for cause-chain edge cases not directly in the catalog -- e.g., cause-chain depth-5 truncation).
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts (post-Task-1 state; reading this file confirms the V2 payload shape the test must assert against).
    - 19-RESEARCH.md "Per-File Test Surface" guidance via Pitfall 5 (notifications.length flip pattern) + 19-CONTEXT.md D-19-07 "Test count consequence of D-19-01" (line 94).
    - 19-PATTERNS.md "Pattern Assignments > (b) Test Migration > Byte-equality assertion pattern" (line 304-324) + "Dropped-warning count assertion pattern" (line 332-348).
    - tests/orchestrators/marketplace/add.test.ts (Phase 18 precedent: byte-exact V2 assertion form + makeCtx preservation; especially the `assert.equal(note.message, ...)` style + reload-hint POSITIVE flip).
  </read_first>
  <files>tests/orchestrators/plugin/uninstall.test.ts</files>
  <action>
    Apply the V1 -> V2 test rewrites in this order:

    1. SCAN the test file. `grep -n "notifySuccess\|notifyWarning\|notifyError\|notifications.length\|cache refresh deferred\|cleanup\|/reload to pick up changes\|(uninstalled)\|(failed)" tests/orchestrators/plugin/uninstall.test.ts`. Build a mental map of which tests target which V1 call site (success at line 246 / defensive-success at line 232 / direct-path failure at line 160 / cache-refresh warning at 179 / cleanup-leak warning at 200).

    2. KEEP the existing `makeCtx()` helper at lines 44-66 VERBATIM per D-19-07 inheriting D-18-06. The `pi: { getAllTools: () => [] }` mock-pi shape stays; the `{ message, severity }` tuple recording stays; do not factor to a shared module (Phase 21 cleanup territory).

    3. REWRITE byte assertions for the success arm (V1 `notifySuccess(ctx, appendReloadHint(body, hint))` at line 246). The V2 byte string follows catalog `success` (lines 344-348):
       `assert.equal(notifications.length, 1);`
       `assert.equal(note.message, "● <marketplace> [<scope>]\n  ○ <plugin> v<version> (uninstalled)\n\n/reload to pick up changes");`
       `assert.equal(note.severity, undefined);`
       Substitute `<marketplace>`, `<scope>`, `<plugin>`, `<version>` with the test fixture's actual values. If the test scenario lacks a version, OMIT the ` v<version>` substring (the renderer handles version-absent rows per `renderPluginRow`).

    4. REWRITE byte assertions for the defensive-guard arm (V1 `notifySuccess(ctx, body)` at line 232). The V2 byte string follows the same catalog `success` shape because both arms route through the same `notify()` call -- but the reload-hint may NOT fire if the orchestrator's payload construction differs (i.e., if the defensive arm constructs a `MarketplaceNotificationMessage` with `plugins: []` instead of with a `PluginUninstalledMessage` row, the reload-hint suppresses because there's no state-changing plugin variant). Pick the SAME construction as the canonical-success arm (with a `PluginUninstalledMessage` row) so the bytes match; rationale documented in Task 1.

    5. REWRITE byte assertions for the direct-path failure arm (V1 `notifyError(ctx, errorMessage(err), err)` at line 160). The V2 byte string follows catalog `failure-permission-denied` (lines 370-374):
       `assert.equal(notifications.length, 1);`
       `assert.equal(note.message, "● <marketplace> [<scope>]\n  ⊘ <plugin> v<version> (failed) {<reason>}\n    cause: <error message>");`
       `assert.equal(note.severity, "error");`
       The `<reason>` token comes from the closed-set Reason the orchestrator classified for the thrown error (the same classifier Task 1 wired into `PluginFailedMessage.reasons`). The 4-space cause-chain trailer is appended by `notify()` per D-16-08 from `PluginFailedMessage.cause`.

    6. DELETE any test or assertion targeting the V1 cache-refresh warning at uninstall.ts:179. Locate via `grep -n "cache refresh deferred\|completion cache" tests/orchestrators/plugin/uninstall.test.ts`. If a whole test exercises that surface, delete the test entirely (the surface no longer exists per D-19-01); if a `notifications.length === 2` style assertion is mixed into a larger test, flip to `=== 1` and delete the warning-specific message-substring assertion.

    7. DELETE any test or assertion targeting the V1 data-dir cleanup-leak warning at uninstall.ts:200. Locate via `grep -n "cleanup\|leak\|deferred\|data dir" tests/orchestrators/plugin/uninstall.test.ts`. Apply the same DELETE-or-flip rule.

    8. KEEP unchanged: state-mutation assertions (`loadState` + key presence/absence after uninstall), error-throw assertions on `PluginNotInstalledError` / similar typed errors, NFR-5 no-network assertions if present, scope independence assertions, recovery model assertions. The V2 migration changes ONLY the byte-string assertions on notification output; behavioral assertions on state mutation, error throwing, and side-effect propagation stay verbatim.

    9. VERIFY after editing: `grep -c "(uninstalled)\\\\n\\\\n/reload to pick up changes" tests/orchestrators/plugin/uninstall.test.ts` returns ≥1 (at least one V2 success assertion); `grep -c "cache refresh deferred" tests/orchestrators/plugin/uninstall.test.ts` returns 0 (cache-refresh-warning assertion deleted); `grep -c "data dir.*deferred" tests/orchestrators/plugin/uninstall.test.ts` returns 0 (cleanup-leak-warning assertion deleted); `grep -c "notifications.length, 2" tests/orchestrators/plugin/uninstall.test.ts` returns 0 (all length-2 assertions either flipped to length-1 or deleted with their warning-specific assertion).

    Honors D-19-07 (test discipline -- byte-exact V2 through real notify() via existing makeCtx pattern; deleted assertions for dropped warnings; test-count assertion consequence applied).
  </action>
  <verify>
    <automated>node --import tsx --test tests/orchestrators/plugin/uninstall.test.ts &amp;&amp; node --import tsx --test tests/architecture/catalog-uat.test.ts</automated>
  </verify>
  <done>
    All uninstall.test.ts tests pass against the migrated uninstall.ts; byte assertions match `docs/output-catalog.md:344-348` (success) and `docs/output-catalog.md:370-374` (failure-permission-denied); test count assertions flipped from `=== 2` to `=== 1` where applicable; tests targeting dropped warning content DELETED; the `makeCtx()` pattern at lines 44-66 preserved verbatim; severity assertions correctly use `undefined` (info) for success and `"error"` for failure. Catalog UAT (`tests/architecture/catalog-uat.test.ts`) stays GREEN end-to-end. `npm run check` passes when Task 1 + Task 2 land together in a single atomic commit. `grep -cE "notify(Success|Warning|Error)\(" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan 19-01 is an internal API refactor: V1 severity-named wrappers consolidate into a single V2 `notify(ctx, pi, message)` call. No new attack surface; same `ctx.ui.notify` host channel (IL-2 invariant). Catalog UAT byte-equality (Phase 17 contract) gates user-visible output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | I (Information disclosure) | V2 `PluginFailedMessage.cause` cause-chain rendering at 4-space indent | accept | Phase 19 inherits the existing V1 cause-message behavior verbatim -- `notifyError(ctx, errorMessage(err), err)` already surfaces the same cause text today via the V1 `causeChainTrailer(err)`. The V2 renderer applies the same depth-5 walk (MSG-CC-1) at the same indent. No new information disclosure; same data, different layer. |
| T-19-01-02 | D (Denial-of-service / notification flooding) | Multiple notify() calls per orchestration | mitigate | D-19-01 + D-18-01 single-notify-call-per-orchestration discipline. Plan 19-01 drops the 2 secondary notifyWarning sites entirely and consolidates the 3 success/failure sites into a single notify() call per arm. Notification volume drops from V1's worst case (success + cache-refresh + cleanup = 3 notifications) to V2's exact one. |
| T-19-01-03 | T (Tampering / severity manipulation) | Orchestrator-supplied severity argument | mitigate | V2 `notify(ctx, pi, message)` signature has no severity argument. Severity is content-derived by the renderer per D-16-11. Plan 19-01's notify() call sites pass no severity; orchestrator cannot misclassify. Phase 19 Plan 19-06 narrows MSG-Block 1 lint to exempt orchestrators/plugin/** because the V2 call has no severity arg to flag. |
</threat_model>

<verification>
- `node --import tsx --test tests/orchestrators/plugin/uninstall.test.ts` exits 0 (all tests pass against the V2-migrated orchestrator).
- `node --import tsx --test tests/architecture/catalog-uat.test.ts` exits 0 (catalog UAT byte-equality GREEN end-to-end, including plugin-uninstall fixtures).
- `npm run check` exits 0 at the atomic commit boundary (orchestrator + test changes land together).
- `grep -cE "notify(Success|Warning|Error)\(" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns 0.
- `grep -c "NotificationMessage cascade recipe" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns 1.
- `grep -c "cache refresh deferred" tests/orchestrators/plugin/uninstall.test.ts` returns 0.
- `grep -c "notifications.length, 2" tests/orchestrators/plugin/uninstall.test.ts` returns 0.
</verification>

<success_criteria>
- uninstall.ts emits exactly one V2 `notify(opts.ctx, opts.pi, NotificationMessage)` call per orchestration arm (success arm + failure arm). The 2 post-state-commit warning sites (179, 200) are DROPPED entirely with explanatory try/catch comments. The defensive-guard success arm (232) and the canonical success arm (246) share the same V2 byte shape because both route through the same `notify()` call site.
- uninstall.test.ts assertions match V2 catalog fixtures byte-for-byte; tests targeting dropped-warning content are DELETED; test count assertions flipped where applicable; the `makeCtx()` pattern at lines 44-66 preserved verbatim.
- The 10-line `NotificationMessage cascade recipe (Plan 19-01 pilot; Wave 2 mirrors).` block-comment sits directly above the surviving notify() call site and is locatable by Wave 2 agents via grep.
- `npm run check` GREEN; catalog UAT GREEN end-to-end.
</success_criteria>

<output>
Create `.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-01-SUMMARY.md` documenting:
- The 2 files modified (uninstall.ts + uninstall.test.ts).
- The exact location of the 10-line recipe block-comment in uninstall.ts (line number) so Wave 2 agents (Plans 19-02..05) can mirror it.
- The 5 V1 callsites resolved: 3 migrated to V2 notify() (lines 160 failure, 232 defensive success, 246 canonical success); 2 DROPPED (lines 179 cache-refresh, 200 data-dir cleanup-leak).
- The test changes: byte-string flips (success + failure), test count assertion flips (length-2 -> length-1), deleted tests/assertions targeting dropped warning content.
- Confirmation that catalog UAT + `npm run check` both pass.
- Locked V2 construction pattern for Plans 19-02..05 to mirror (paraphrase the recipe with concrete examples of the discriminator substitution: install -> "installed" / "failed"; list -> "available" / "unavailable" / "upgradable" / "failed"; reinstall -> "reinstalled" / "skipped" / "failed" / "manual recovery"; update -> "updated" / "skipped" / "failed").
</output>
