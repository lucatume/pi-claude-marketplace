---
phase: 19-migration-wave-2-plugin-orchestrator-family
plan: 6
type: execute
wave: 3
depends_on:
  - 19-02
  - 19-03
  - 19-04
  - 19-05
files_modified:
  - eslint.config.js
  - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-19-04 / D-19-05: Plan 19-06 is the Wave 3 final-narrowing + gate plan. It MUST run AFTER Plans 19-02 / 19-03 / 19-04 / 19-05 ALL land (Plan 19-01 is in Wave 1 and is a transitive dependency through any Wave 2 plan). The lint narrowing assumes every plugin orchestrator has stopped calling V1 wrappers; if any Wave 2 plan failed to fully eliminate V1 callers, the MSG-Block 1 / 1b lints would have FIRED before this plan ran (each Wave 2 plan keeps `npm run check` GREEN at its merge), but the final end-to-end verification here is the phase gate."
    - "D-19-08 (additive `ignores: [...]` extension): Plan 19-06 ADDS `\"extensions/pi-claude-marketplace/orchestrators/plugin/**\"` to BOTH MSG-Block 1 (line 160 in eslint.config.js, msg-sr-1..6 severity routing) and MSG-Block 1b (line 185, msg-gr-3 per-scope rendering) `ignores: [...]` arrays. The current Phase 18 state of each array is `[\"extensions/pi-claude-marketplace/orchestrators/marketplace/**\"]` per RESEARCH §Code Examples line 469-478. Resulting array: `[\"extensions/pi-claude-marketplace/orchestrators/marketplace/**\", \"extensions/pi-claude-marketplace/orchestrators/plugin/**\"]`. MSG-Block 1b's existing `files: [..., \"edge/handlers/**\"]` entry is UNCHANGED (Phase 20 owns that surface). All other MSG-Blocks (2, 3, 4a, 4b, 5, 6) need NO modification in Phase 19."
    - "Phase 19 Success Criteria (per ROADMAP §Phase 19 + 19-CONTEXT.md `<canonical_refs>` line 122-123): SC #1 = zero V1 callers in `orchestrators/plugin/**/*.ts` (verified by grep, the migration result); SC #2 = MSG-* lint plugin's `files:` globs narrowed via the additive ignores (verified by Plan 19-06 + grep); SC #3 = catalog UAT byte-equality GREEN for every plugin-family `(section, state)` fixture (the gate); SC #4 = `npm run check` GREEN; no orchestrators outside plugin family changed call-site shape (verified by `git diff` scope check)."
    - "Claude's Discretion (CONTEXT line 108): the stale D-CMC-12 / V1 notifyError comment references in `orchestrators/plugin/shared.ts` are folded into THIS plan per discretion. `shared.ts` has NO V1 wrapper callsites of its own (it's a shared helper file -- the actual notify calls live in install.ts / uninstall.ts / etc); only comment lines reference V1 patterns that are now defunct post-Wave-2. Optional cosmetic cleanup; either REFRESH the comments to describe the V2 notify pattern + cross-reference Plan 19-01's recipe location, OR LEAVE AS-IS and defer to Phase 21 (since shared.ts itself doesn't change semantically -- only comments are stale)."
    - "Phase 19 hand-off to Phase 20: Phase 20 (Migration Wave 3 -- Edge + UsageError) extends the SAME `ignores:` arrays with `\"extensions/pi-claude-marketplace/orchestrators/edge/**\"` AND removes MSG-Block 1b's `edge/handlers/**` files entry (because edge handlers will then be V2). Phase 21 deletes the entire MSG-* plugin wiring. The additive narrowing contract is preserved across Phases 18 → 19 → 20."
  byte_contracts:
    - "End-to-end catalog UAT verification: all plugin-family fixtures at `docs/output-catalog.md:133-568` produce byte-equal output through real `notify()`. The catalog UAT runner (`tests/architecture/catalog-uat.test.ts`) drives the FIXTURES map through `notify(mockCtx, mockPi, message)` and asserts byte-equality against each catalog block; Plan 19-06 runs this end-to-end as the phase gate."
  artifacts:
    - path: "eslint.config.js"
      provides: "MSG-Block 1 + 1b `ignores: [...]` arrays each EXTENDED with the `orchestrators/plugin/**` path string per D-19-08"
      contains: "extensions/pi-claude-marketplace/orchestrators/plugin/**"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts"
      provides: "Optional comment cleanup: stale D-CMC-12 / V1 notifyError pattern references refreshed or removed per CONTEXT line 108 Claude's Discretion"
      contains: "(documentation-only changes; no functional change)"
  key_links:
    - from: "eslint.config.js::MSG-Block 1"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/**"
      via: "additive ignores entry"
      pattern: "ignores:[^]+\"extensions/pi-claude-marketplace/orchestrators/plugin/\\*\\*\""
    - from: "eslint.config.js::MSG-Block 1b"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/**"
      via: "additive ignores entry (matching block 1's path string)"
      pattern: "ignores:[^]+\"extensions/pi-claude-marketplace/orchestrators/plugin/\\*\\*\""
  coverage_constraints:
    - "Plan 19-06 MUST run AFTER Plans 19-02 + 19-03 + 19-04 + 19-05 all land. Wave 1's 19-01 is a transitive dependency through any Wave 2 plan."
    - "If any Wave 2 plan failed to fully eliminate V1 callers in its orchestrator, MSG-Block 1 / 1b lints WOULD have fired before this plan runs. Each Wave 2 plan kept `npm run check` GREEN at its merge boundary (per RESEARCH §Validation Architecture per-plan command map); Plan 19-06's verification step is the phase gate."
    - "Phase 20 will EXTEND the same `ignores:` arrays with `orchestrators/edge/**`; Phase 21 deletes the entire MSG-* plugin wiring. Additive contract preserved."
    - "No other MSG-Block (2, 3, 4a, 4b, 5, 6) is touched -- D-19-08 confines Phase 19 to MSG-Block 1 + 1b only."
---

<objective>
Final narrowing + verification plan for Phase 19. Add `"extensions/pi-claude-marketplace/orchestrators/plugin/**"` to BOTH MSG-Block 1 (severity routing, line 160 in eslint.config.js) and MSG-Block 1b (per-scope rendering, line 185) `ignores: [...]` arrays per D-19-08. Verify all 4 Phase 19 Success Criteria GREEN end-to-end: SC #1 (zero V1 callers in plugin family); SC #2 (MSG-Block 1 + 1b lint globs narrowed via the additive ignores); SC #3 (catalog UAT byte-equality GREEN for every plugin-family `(section, state)` fixture); SC #4 (`npm run check` GREEN; no orchestrators outside plugin family changed call-site shape).

OPTIONALLY clean stale V1-pattern comments in `orchestrators/plugin/shared.ts` per CONTEXT line 108 Claude's Discretion (shared.ts has no V1 wrapper callsites; only comment references to defunct patterns). Either refresh the comments to describe the V2 surface (cross-referencing Plan 19-01's recipe location) or leave them for Phase 21 cleanup.

Purpose: complete Phase 19's contribution to SNM-22 (plugin family migration partial; SNM-22 closes in Phase 21 when V1 wrappers are deleted) and finalize the lint surface so the MSG-* plugin no longer scopes the now-migrated plugin orchestrators. Phase 20 inherits the additive `ignores:` contract.

Output: 1 modified file (`eslint.config.js`, 2 path strings added across 2 arrays) + optional 1 file with comment-only changes (`orchestrators/plugin/shared.ts`). All 4 Phase 19 Success Criteria proven GREEN end-to-end.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-CONTEXT.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-RESEARCH.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-PATTERNS.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-VALIDATION.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-01-SUMMARY.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-02-SUMMARY.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-03-SUMMARY.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-04-SUMMARY.md
@.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-05-SUMMARY.md
@.planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-06-SUMMARY.md
@eslint.config.js
@tests/architecture/catalog-uat.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend MSG-Block 1 + MSG-Block 1b `ignores: [...]` arrays in eslint.config.js with the `orchestrators/plugin/**` path string per D-19-08; optionally clean stale V1-pattern comments in orchestrators/plugin/shared.ts</name>
  <read_first>
    - eslint.config.js (full file; specifically lines 145-200 covering MSG-Block 1 + 1b -- the exact insertion sites are the `ignores: [...]` arrays at lines 160 and 185 per RESEARCH §Code Examples line 436-464).
    - 19-RESEARCH.md "Code Examples > eslint.config.js MSG-Block 1 + 1b current state" (line 434-478 -- the current Phase 18 state of both arrays AND the Plan 19-06 target state).
    - 19-PATTERNS.md "Pattern Assignments > (c) Lint Narrowing -- Phase 18 Plan 18-06 Entry" (line 352-425 -- the additive extension contract).
    - 19-CONTEXT.md `<decisions>` D-19-08 (additive narrowing strategy; lines covered; resulting array shape) + `<canonical_refs>` "MSG-* lint scoping" (line 200-203).
    - .planning/phases/18-migration-wave-1-marketplace-orchestrator-family/18-06-SUMMARY.md (Phase 18 precedent for the SAME insertion shape -- Plan 18-06 inserted the first path string `orchestrators/marketplace/**` into both arrays; Plan 19-06 appends the second).
    - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts (read in full; identify comment lines referencing V1 `notifyError` / `notifySuccess` / `notifyWarning` patterns + D-CMC-12 references per CONTEXT line 108).
  </read_first>
  <files>
    eslint.config.js,
    extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
  </files>
  <action>
    Operate in this order:

    1. Locate MSG-Block 1 in `eslint.config.js` (the block starting with the comment `// MSG-Block 1 (MSG-SR-1..6): cascade/severity routing -- orchestrators surface.`). The current `ignores: [...]` array at line 160 reads:
       ```
       ignores: ["extensions/pi-claude-marketplace/orchestrators/marketplace/**"],
       ```
       EDIT it to add the plugin path string. After the edit, the array reads:
       ```
       ignores: [
         "extensions/pi-claude-marketplace/orchestrators/marketplace/**",
         "extensions/pi-claude-marketplace/orchestrators/plugin/**",
       ],
       ```
       Pick whatever multi-line vs single-line array style is consistent with the rest of `eslint.config.js`. Match existing indentation (typically 2 spaces in flat config blocks). If the file currently has the single-line single-string form, the simplest diff is to expand to the multi-line form OR keep single-line: `ignores: ["extensions/pi-claude-marketplace/orchestrators/marketplace/**", "extensions/pi-claude-marketplace/orchestrators/plugin/**"]`. Either is acceptable; the parsed semantics are identical.

    2. Locate MSG-Block 1b (the block starting with the comment `// MSG-Block 1b (MSG-GR-3): per-scope rendering rule.`). Make the IDENTICAL edit to its `ignores: [...]` array at line 185 -- append `"extensions/pi-claude-marketplace/orchestrators/plugin/**"`. Note: MSG-Block 1b's `files: [..., "edge/handlers/**"]` entry is UNCHANGED (Phase 20 territory).

    3. Do NOT touch any other MSG-Block (Blocks 2, 3, 4a, 4b, 5, 6) per D-19-08. Verify by `git diff eslint.config.js | head -50` showing a small additive diff (~2-4 lines added; no deletions; no other block changes).

    4. OPTIONAL (Claude's Discretion per CONTEXT line 108): in `orchestrators/plugin/shared.ts`, audit comments referencing V1 patterns. Grep `grep -in "notifyError\|notifySuccess\|notifyWarning\|D-CMC-12" extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts`. For each hit:
       - If the comment described a V1 wrapper's specific behavior (e.g., `// notifyError auto-appends the cause-chain trailer per D-CMC-12`), REFRESH it to describe the V2 equivalent (e.g., `// notify() renders cause-chains at 4-space indent per D-16-08; see Plan 19-01 pilot recipe at orchestrators/plugin/uninstall.ts for the V2 construction pattern.`).
       - If the comment is now entirely misleading or refers to defunct V1 internal structures (e.g. dispatch-ternary explanations), DELETE the comment.
       - If the comment is generic enough to remain accurate, LEAVE AS-IS.

       Document the choice (refresh / delete / leave) per comment hit in the SUMMARY. shared.ts has NO V1 wrapper callsites of its own (it's a shared helper file) -- the audit is purely cosmetic; no functional change.

    5. RUN the verification steps inline (also covered by Task 2's automated step):
       - `grep -c "extensions/pi-claude-marketplace/orchestrators/plugin/\\*\\*" eslint.config.js` returns ≥2 (one per MSG-Block).
       - `grep -c "extensions/pi-claude-marketplace/orchestrators/marketplace/\\*\\*" eslint.config.js` returns ≥2 (unchanged from Phase 18).
       - No other MSG-Block has been touched (`git diff eslint.config.js` shows ONLY the added path strings).

    Honors D-19-08 (narrow ONLY MSG-Block 1 + 1b; additive ignores; no other block changes) + CONTEXT line 108 Claude's Discretion (optional comment cleanup).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    eslint.config.js MSG-Block 1 + 1b each contain the additive `"extensions/pi-claude-marketplace/orchestrators/plugin/**"` entry in their `ignores: [...]` array; `grep -c "extensions/pi-claude-marketplace/orchestrators/plugin/\\*\\*" eslint.config.js` returns at least 2; no other MSG-Block has been touched. `npm run check` passes -- the plugin orchestrators no longer trigger any MSG-SR-* or MSG-GR-3 lint rule. If the optional `shared.ts` comment cleanup was applied, the file content changes are documentation-only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final end-to-end Phase 19 success-criteria verification (SC #1, #2, #3, #4 all GREEN)</name>
  <read_first>
    - 19-RESEARCH.md "Validation Architecture > Phase Requirements -> Test Map" (line 632-643).
    - 19-VALIDATION.md "Per-Task Verification Map" (line 39-50; especially the aggregate Plan 19-06 row).
    - 19-CONTEXT.md `<canonical_refs>` "Phase Boundary > Success Criteria" (which derives from ROADMAP §Phase 19).
    - .planning/ROADMAP.md §"Phase 19" (the canonical SC list at lines 304-310).
    - eslint.config.js (post-Task-1 state).
    - All Wave 1 + Wave 2 plan SUMMARYs: 19-01-SUMMARY.md, 19-02-SUMMARY.md, 19-03-SUMMARY.md, 19-04-SUMMARY.md, 19-05-SUMMARY.md (confirm each landed cleanly with V1 callers eliminated).
  </read_first>
  <files>(read-only verification task; no files modified)</files>
  <action>
    Execute the 4 Success Criteria checks end-to-end:

    1. **SC #1 (zero V1 callers in plugin family):** Run `grep -rEn "notify(Success|Warning|Error)\(" extensions/pi-claude-marketplace/orchestrators/plugin/` and confirm the output is empty (zero CallExpression matches). Per V2 grammar, the plugin family should have ZERO references to V1 severity-named wrappers in CallExpression form. If any matches return:
       - Inspect each match. If it's a comment referencing the V1 wrapper name (e.g., `// notifyError used to ...`), that's tolerated (cosmetic comment cleanup is Claude's Discretion per CONTEXT line 108; not blocking).
       - If it's a CallExpression form (`notifyError(...)`, `notifySuccess(...)`, `notifyWarning(...)`), the corresponding Wave 2 plan failed -- STOP and escalate to operator.

       Use the comment-tolerant grep: `grep -rEn "^[^/]*notify(Success|Warning|Error)\(" extensions/pi-claude-marketplace/orchestrators/plugin/` (excludes lines starting with `//`). Expected: empty output.

    2. **SC #2 (MSG-Block 1 + 1b lint globs narrowed):**
       - Run `grep -c "extensions/pi-claude-marketplace/orchestrators/plugin/\\*\\*" eslint.config.js`. Confirm ≥ 2 (one per MSG-Block).
       - Run `npx eslint extensions/pi-claude-marketplace/orchestrators/plugin/` and confirm exit 0 (the MSG-SR-* + MSG-GR-3 rules no longer fire on the plugin family). Note: if ESLint reports rule violations for other reasons (unrelated to MSG-Block 1/1b), those need investigating but are out of scope for SC #2 narrowing verification -- the targeted check is MSG-* rule absence.
       - Alternatively run `npm run lint -- --no-fix extensions/pi-claude-marketplace/orchestrators/plugin/` and confirm GREEN.

    3. **SC #3 (catalog UAT byte-equality GREEN for plugin family):**
       - Run `node --import tsx --test tests/architecture/catalog-uat.test.ts` and confirm exit code 0. The plugin-family fixtures span `docs/output-catalog.md:133-568` (plugin list, install, uninstall, reinstall, update). Each FIXTURES entry's `message` payload routes through `notify(mockCtx, mockPi, message)` and asserts byte-equality against the catalog block.
       - If exit code is non-zero, the catalog UAT report identifies which `(section, state)` pair diverged. The remediation belongs to the corresponding Wave 2 plan (e.g., a byte mismatch on `/claude:plugin install / success` means Plan 19-02's payload construction is off; escalate via a follow-up Wave 2 fix-up rather than amending Plan 19-06).

    4. **SC #4 (`npm run check` GREEN; no out-of-scope orchestrator changes):**
       - Run `npm run check` and confirm exit code 0 (typecheck + ESLint + Prettier + tests all pass).
       - Run `git diff --name-only main..HEAD -- extensions/pi-claude-marketplace/orchestrators/edge/ extensions/pi-claude-marketplace/edge/handlers/ 2>/dev/null` and confirm output is empty -- no orchestrators outside the plugin family have changed call-site shape. (Note: the marketplace family was Phase 18; Plan 19-06 confirms no Phase 19 plan accidentally touched it via `git diff --name-only main..HEAD -- extensions/pi-claude-marketplace/orchestrators/marketplace/` -- this CAN show changes if Phase 18's diff hasn't yet merged to main; gauge by the branch state. The strict invariant is "no Phase-19-introduced changes to non-plugin orchestrator families".)

    5. Document each check result in the plan SUMMARY:
       - Exact `grep` command output for SC #1 (expected: empty / `OK`).
       - Exact `grep -c` output for SC #2 (expected: ≥ 2 for plugin path string; ≥ 2 for marketplace path string unchanged).
       - Catalog UAT exit code (expected: 0) + the per-section subtest pass count.
       - `npm run check` exit code (expected: 0).
       - `git diff --name-only` scope check result (expected: empty for non-plugin orchestrator families).

    If any check fails: STOP. Phase 19 is not complete; the failing criterion must be remediated either via a follow-up Wave 2 fix-up plan or by reverting the offending edits and re-merging. Escalate to operator.

    Honors all 4 ROADMAP §"Phase 19" Success Criteria + 19-VALIDATION.md per-plan command map for Plan 19-06.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; node --import tsx --test tests/architecture/catalog-uat.test.ts &amp;&amp; ( ! grep -rEn "^[^/]*notify(Success|Warning|Error)\(" extensions/pi-claude-marketplace/orchestrators/plugin/ )</automated>
  </verify>
  <done>
    All 4 Phase 19 Success Criteria proven GREEN:
    - SC #1: grep returns 0 V1 callsites in plugin family (comments tolerated; CallExpression form returns nothing).
    - SC #2: eslint.config.js MSG-Block 1 + 1b contain the additive `orchestrators/plugin/**` entry; targeted lint over `orchestrators/plugin/` is GREEN.
    - SC #3: catalog UAT exits 0 against the plugin-family fixtures (5 plugin sections covering list / install / uninstall / reinstall / update).
    - SC #4: `npm run check` exits 0; no Phase-19 changes to non-plugin orchestrator families.
    Phase 19 deliverables complete; Phase 20 (Migration Wave 3 -- Edge + UsageError) can begin (extends the same `ignores:` arrays with `orchestrators/edge/**` and removes Block 1b's `edge/handlers/**` files entry).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Lint config narrowing + verification; no source-code execution changes; byte output unchanged from Phase 17 catalog. The optional shared.ts comment cleanup is documentation-only with no semantic impact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06-01 | (none) | eslint.config.js additive ignores | accept | No new threats -- lint config change with no runtime effect. The ignores are scoped narrowly to the migrated plugin family; non-plugin orchestrators (edge/) and edge/handlers/ remain fully linted by MSG-Block 1 + 1b. Phase 21 will eventually delete the MSG-* plugin entirely. The additive contract preserves the security-critical no-direct-notify-outside-shared/notify.ts invariant (IL-2) via other MSG-Blocks (Blocks 4a + 5 still cover `shared/notify.ts` boundary enforcement). |
</threat_model>

<verification>
- `npm run check` exits 0.
- `node --import tsx --test tests/architecture/catalog-uat.test.ts` exits 0.
- `grep -rEn "^[^/]*notify(Success|Warning|Error)\(" extensions/pi-claude-marketplace/orchestrators/plugin/` returns empty (no CallExpression matches; tolerated comment references caught by the leading-non-comment grep).
- `grep -c "extensions/pi-claude-marketplace/orchestrators/plugin/\*\*" eslint.config.js` returns at least 2.
- `grep -c "extensions/pi-claude-marketplace/orchestrators/marketplace/\*\*" eslint.config.js` returns at least 2 (Phase 18 contract unchanged).
- `git diff --name-only main..HEAD -- extensions/pi-claude-marketplace/orchestrators/edge/ extensions/pi-claude-marketplace/edge/handlers/` returns empty (no out-of-scope changes from Phase 19).
</verification>

<success_criteria>
All 4 ROADMAP Phase 19 Success Criteria proven GREEN end-to-end:
1. Zero `notifySuccess` / `notifyWarning` / `notifyError` callers in `orchestrators/plugin/**/*.ts`.
2. MSG-Block 1 + 1b `files:` globs narrowed in `eslint.config.js` via additive ignores entry; rules still wired for edge family (Phase 20 territory).
3. Catalog UAT byte-equality GREEN for every plugin-family `(section, state)` fixture across the 5 plugin sections (list / install / uninstall / reinstall / update).
4. `npm run check` GREEN; no orchestrators outside plugin family have changed call-site shape (Phase 19 did not regress Phase 18 or pre-position Phase 20 work).
</success_criteria>

<output>
Create `.planning/phases/19-migration-wave-2-plugin-orchestrator-family/19-06-SUMMARY.md` documenting:
- The 1 file modified (eslint.config.js, 2 path strings added across 2 arrays) + optionally `orchestrators/plugin/shared.ts` comment cleanup (Claude's Discretion per CONTEXT line 108).
- The verification matrix: 4 Success Criteria, each with the exact command run + observed result + exit code.
- The contribution to SNM-22 closure (Phase 21 closes the requirement with V1 wrapper deletion; Phase 19 contributed the plugin-family migration partial; Phase 18 contributed the marketplace-family partial; Phase 20 will contribute the edge-family partial).
- The hand-off to Phase 20: the additive `ignores:` contract; Phase 20 extends both arrays with `orchestrators/edge/**` AND removes MSG-Block 1b's `edge/handlers/**` files entry (because edge handlers will then be V2).
- Any `presentation/*` modules that became orphan-imported after Phase 19 (per RESEARCH §State of the Art: `cause-chain.ts`, `manual-recovery.ts`, `rollback-partial.ts`, `version-arrow.ts` -- these still exist in `presentation/` but no longer have any plugin-family importer; Phase 21 deletes them. `cascade-summary.ts` stays alive until Phase 20 migrates `orchestrators/import/execute.ts`).
- Confirmation of the cascade-caller postCommitWarnings audit from Plan 19-02 Task 1 step 7 (carried forward from Plan 19-02's SUMMARY).
</output>
