---
phase: 17-spec-rewrite-catalog-uat-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/messaging-style-guide.md
  - tests/architecture/grammar-frontmatter.test.ts
  - .planning/REQUIREMENTS.md
  - docs/adr/v2-001-structured-notify.md
autonomous: true
requirements:
  - SNM-19
  - SNM-26
tags:
  - documentation
  - style-guide
  - traceability
must_haves:
  truths:
    - "Style guide v2.0 published with no YAML frontmatter (D-17-01)"
    - "Style guide first line is `# Messaging Style Guide` (the H1), not `---`"
    - "Style guide line count is between 150 and 250 (D-17-07)"
    - "Style guide preserves the ES-5 Supersession Table verbatim with a one-line `fully retired Phase 21` annotation (D-17-08)"
    - "`tests/architecture/grammar-frontmatter.test.ts` is absent from the repo (D-17-02)"
    - "REQUIREMENTS.md SNM-26 row reads `| SNM-26 | Phase 17 | Complete |` (D-17-02)"
    - "REQUIREMENTS.md per-phase distribution line lists Phase 17 (4: SNM-19, SNM-20, SNM-26, SNM-31) and Phase 21 (7: SNM-22, SNM-24, SNM-25, SNM-27, SNM-28, SNM-29, SNM-32)"
    - "`docs/adr/v2-001-structured-notify.md` Accepted-status block references Phase 17"
    - "`npm run check` exits 0 after the plan commit"
  artifacts:
    - path: "docs/messaging-style-guide.md"
      provides: "v2.0 thin pointer doc (~5-7 H2 sections, no frontmatter)"
      min_lines: 150
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated SNM-26 ownership + per-phase distribution line"
      contains: "| SNM-26 | Phase 17 | Complete |"
    - path: "docs/adr/v2-001-structured-notify.md"
      provides: "Accepted-status block with Phase 17 cross-reference"
      contains: "Phase 17"
  key_links:
    - from: "docs/messaging-style-guide.md"
      to: "extensions/pi-claude-marketplace/shared/notify.ts"
      via: "Type Model Reference section pointer"
      pattern: "shared/notify\\.ts"
    - from: "docs/messaging-style-guide.md"
      to: "tests/architecture/notify-types.test.ts"
      via: "Type Model Reference / Cross-References pointer"
      pattern: "notify-types\\.test\\.ts"
    - from: "docs/messaging-style-guide.md"
      to: "docs/output-catalog.md"
      via: "Cross-References pointer"
      pattern: "output-catalog\\.md"
---

<objective>
Rewrite `docs/messaging-style-guide.md` from v1.0 (954 lines, frontmatter-driven enumeration spec) to v2.0 (~5-7 H2 thin pointer doc, ~150-250 lines) per D-17-01 + D-17-07 + D-17-08, atomically deleting the now-dead `tests/architecture/grammar-frontmatter.test.ts` (D-17-02) and recording the consequent traceability change in `.planning/REQUIREMENTS.md`. The ADR Accepted-status block gains a one-line Phase 17 cross-reference (success criterion #5).

Purpose: SNM-19 closes the v1.4 spec-rewrite mandate; SNM-26 advances from Phase 21 to Phase 17 because the YAML frontmatter deletion (D-17-01) renders the frontmatter parity test dead at module load. Per Pitfall 1 in RESEARCH.md, the frontmatter deletion and the test deletion MUST land in the same commit -- removing the YAML keys without removing the test crashes `npm run check` because `tests/lint-rules/lib/frontmatter.js`'s `parseStyleGuideFrontmatter` throws "no YAML frontmatter found" at module load.

Output: A type-driven pointer-style v2.0 style guide whose Type Model Reference section points readers at the const tuples in `shared/notify.ts` rather than duplicating the closed sets in YAML, plus the consequent test deletion and traceability edits. The plan is one atomic commit per CLAUDE.md.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-spec-rewrite-catalog-uat-migration/17-CONTEXT.md
@.planning/phases/17-spec-rewrite-catalog-uat-migration/17-RESEARCH.md
@.planning/phases/17-spec-rewrite-catalog-uat-migration/17-PATTERNS.md
@.planning/phases/17-spec-rewrite-catalog-uat-migration/17-VALIDATION.md
@docs/messaging-style-guide.md
@docs/adr/v2-001-structured-notify.md
@tests/architecture/grammar-frontmatter.test.ts
@extensions/pi-claude-marketplace/shared/notify.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic spec rewrite + dead-test deletion + traceability edits</name>
  <files>docs/messaging-style-guide.md, tests/architecture/grammar-frontmatter.test.ts, .planning/REQUIREMENTS.md, docs/adr/v2-001-structured-notify.md</files>
  <read_first>
    - docs/messaging-style-guide.md (the file to rewrite -- read full v1.0 to understand current sections; ES-5 table at §15 lines 525-537 must be preserved verbatim)
    - extensions/pi-claude-marketplace/shared/notify.ts lines 181-487 (the const tuples PLUGIN_STATUSES, MARKETPLACE_STATUSES, DEPENDENCIES + the discriminated union types -- Type Model Reference points here)
    - extensions/pi-claude-marketplace/shared/notify.ts lines 1034-1064 (the `notify(ctx, pi, message)` public surface)
    - docs/adr/v2-001-structured-notify.md (full 197 lines -- copy the section-heading rhythm and "code blocks for type signatures, prose for rationale" tone; the Accepted-status block is at lines 1-5)
    - tests/architecture/grammar-frontmatter.test.ts (the 91-line dead test to delete -- confirms it imports from `tests/lint-rules/lib/frontmatter.js` whose module-load throws when YAML keys are absent)
    - tests/architecture/notify-types.test.ts (the compile-check the v2 style guide points at as the closed-set proof)
    - .planning/REQUIREMENTS.md lines 75-117 (the traceability table + per-phase distribution line)
    - .planning/phases/17-spec-rewrite-catalog-uat-migration/17-PATTERNS.md §"`docs/messaging-style-guide.md` v2.0" (concrete analog patterns + ES-5 table preservation rules)
    - .planning/phases/17-spec-rewrite-catalog-uat-migration/17-RESEARCH.md §Q7 (line-count budget per section) + §Q9 (REQUIREMENTS.md edit shape) + §Q10 (ADR cross-ref shape) + §"Common Pitfalls" Pitfall 1 (frontmatter + test deletion coupling)
  </read_first>
  <action>
    Perform the following four edits and commit them together as a single atomic commit per CLAUDE.md (Pitfall 1 in RESEARCH.md -- splitting them across commits breaks `npm run check`).

    EDIT 1 -- Full rewrite of `docs/messaging-style-guide.md` to v2.0 (~5-7 H2 sections, ~150-250 lines) per D-17-01 + D-17-07 + D-17-08:

    - DELETE the entire YAML frontmatter block at lines 1-65 (the `---` opening, the four keys `status_tokens` / `reasons` / `markers` / `pattern_classes`, the `inventory_row_count` line, and the closing `---`). The v2 file's first line MUST be `# Messaging Style Guide` (the H1), not `---`.
    - DELETE the v1 Table of Contents at lines 71-94. Markdown rendering and link anchors will be regenerated from the new H2 inventory.
    - DELETE v1 §16 Pattern Class Reference (~260 lines starting at line 543) -- patterns now flow from the discriminated-union switches in `shared/notify.ts::renderPluginRow` / `renderMpHeader`.
    - DELETE v1 §17 Worked Examples Gallery (~103 lines) -- per-command examples live in `docs/output-catalog.md`.
    - DELETE v1 §3 Status Tokens / §4 Reasons Enum standalone enumeration sections -- replaced by one sentence in the new §"Type Model Reference" pointing at the const tuples in `shared/notify.ts` (`PLUGIN_STATUSES`, `MARKETPLACE_STATUSES`, `DEPENDENCIES`) and `shared/grammar/reasons.ts` (`REASONS`).
    - DELETE v1 §11 Plugin List Rendering, §14 IL-3 console.warn, §18 Conventions, §19 Cross-References (they fold into the new §"Output Grammar Summary" or §"Cross-References").

    Compose the v2 body using the six H2 sections below (planner discretion on exact ordering and prose; line budgets are guidance, not contracts):

      ## Overview (~20-30 lines)
      Two-to-three paragraph intro: purpose + audience + the v1.0→v2.0 supersession statement that this guide now describes the structured type model in `shared/notify.ts` rather than a YAML enumeration. State that closed-set authority moved from frontmatter keys to const tuples in `shared/notify.ts`.

      ## Type Model Reference (~30-50 lines)
      Pointer-only narrative -- NEVER enumerate the 10 plugin statuses or 4 marketplace statuses in prose (D-17-01). Cite the const tuples by name (`PLUGIN_STATUSES`, `MARKETPLACE_STATUSES`, `DEPENDENCIES`) and tell readers to read `extensions/pi-claude-marketplace/shared/notify.ts` for membership. Cite `extensions/pi-claude-marketplace/shared/grammar/reasons.ts::REASONS` for the reasons set. Cite `tests/architecture/notify-types.test.ts` as the compile-time membership proof. Planner discretion: optionally include a small inline TS snippet showing the `NotificationMessage` discriminated-union shape (see 17-PATTERNS.md §"ADR public-surface excerpt" for the snippet form to mirror).

      ## Output Grammar Summary (~40-70 lines)
      One-page rule list capturing v2 grammar invariants: always-marketplace-header form (every notify() output starts with a marketplace header at column 0); indentation discipline (header column 0, plugin rows 2-space indent, per-plugin cause chains and rollback-partial children at 4-space indent); computed severity routing (per D-16-11); computed reload-hint with state-changing-marketplace refinement (per D-16-12); computed soft-dep markers via render-time probe (per D-16-15); inline per-plugin cause chains (per D-16-08, no separate cascade-summary line); no top-level free-text augmentations (per D-17-09 -- no `Claude plugin import summary` preamble, no `Fix the underlying issue and retry.` anchor, no `source-mismatch` diagnostic line).

      ## Severity Routing (~20-30 lines)
      First-match-wins ladder per Phase 16 D-16-11: failed → error; skipped or manual recovery → warning; else success/info. Document the `ctx.ui.notify` second-arg convention (omit for info; `"warning"` or `"error"` otherwise).

      ## ES-5 Supersession Table (~25 lines)
      VERBATIM reproduction of v1 §15 (lines 525-537) per D-17-08 -- keep the table heading, the supersession paragraph, the 5-row table itself, and the IL-2/IL-3 reaffirmation sentence at line 537. ADD one final line below the table: `> Note: The 5 ES-5 legacy markers remain blocked by \`tests/architecture/no-legacy-markers.test.ts\` and are fully retired alongside V1 wrapper deletion in Phase 21.`

      ## Cross-References (~15-25 lines)
      Pointer list (markdown links) to: `docs/output-catalog.md` (byte-equal examples / user-contract gate text), `docs/adr/v2-001-structured-notify.md` (design rationale), `extensions/pi-claude-marketplace/shared/notify.ts` (types + renderer), `tests/architecture/notify-types.test.ts` (closed-set compile-check), `tests/architecture/catalog-uat.test.ts` (user-contract gate test), `docs/prd/pi-claude-marketplace-prd.md` §6.12 (ES-5 origin).

    Target: 150-250 total lines. Final byte must be a single trailing newline (prettier discipline).

    EDIT 2 -- Delete `tests/architecture/grammar-frontmatter.test.ts` per D-17-02. Use `git rm tests/architecture/grammar-frontmatter.test.ts`. This is unconditional; the 91-line file's module-load chain becomes unloadable the moment the YAML frontmatter is gone (Pitfall 1).

    EDIT 3 -- `.planning/REQUIREMENTS.md` traceability edits per D-17-02 + RESEARCH.md §Q9. Two changes:
      a) On the SNM-26 row (currently `| SNM-26 | Phase 21 | Pending |` at line 103), change the Phase column from `Phase 21` to `Phase 17` AND change the Status column from `Pending` to `Complete`. Resulting row: `| SNM-26 | Phase 17 | Complete |`.
      b) On the per-phase distribution line (currently at line 116): change the Phase 17 entry from `Phase 17 (3: SNM-19, SNM-20, SNM-31)` to `Phase 17 (4: SNM-19, SNM-20, SNM-26, SNM-31)`, AND change the Phase 21 entry from `Phase 21 (8: SNM-22, SNM-24, SNM-25, SNM-26, SNM-27, SNM-28, SNM-29, SNM-32)` to `Phase 21 (7: SNM-22, SNM-24, SNM-25, SNM-27, SNM-28, SNM-29, SNM-32)`.

    Do NOT yet flip SNM-19, SNM-20, SNM-31 to Complete -- Plan 17-03 (or the phase-completion summary step) handles those flips after the full suite is GREEN.

    EDIT 4 -- `docs/adr/v2-001-structured-notify.md` Accepted-status cross-reference per success criterion #5 + RESEARCH.md §Q10. Edit line 3 from `- **Status:** Accepted (Phase 15, 2026-05-25)` to one of the two forms in 17-PATTERNS.md §"`docs/adr/v2-001-structured-notify.md` (one-line cross-ref edit)". Planner picks: either (a) appended to the same line: `- **Status:** Accepted (Phase 15, 2026-05-25); landed via Phase 17 -- spec + catalog UAT migration (2026-05-26)`, OR (b) a separate bullet: `- **Landed via:** Phase 17 -- spec rewrite + catalog UAT migration (2026-05-26)` inserted between line 3 and line 4. Either form satisfies the gate `grep "Phase 17" docs/adr/v2-001-structured-notify.md` returning non-empty.

    After all four edits, run `npm run format` (prettier --write) to normalize formatting on the rewritten markdown. Then run `pre-commit run --files docs/messaging-style-guide.md tests/architecture/grammar-frontmatter.test.ts .planning/REQUIREMENTS.md docs/adr/v2-001-structured-notify.md` per CLAUDE.md; resolve any failures, restage, re-run until clean. Finally run `npm run check` to confirm GREEN, then commit per the commit task below.
  </action>
  <verify>
    <automated>head -1 docs/messaging-style-guide.md | grep -q "^# Messaging Style Guide" && test "$(wc -l < docs/messaging-style-guide.md)" -ge 150 && test "$(wc -l < docs/messaging-style-guide.md)" -le 250 && test ! -f tests/architecture/grammar-frontmatter.test.ts && grep -q "| SNM-26 | Phase 17 | Complete |" .planning/REQUIREMENTS.md && grep -q "Phase 17 (4: SNM-19, SNM-20, SNM-26, SNM-31)" .planning/REQUIREMENTS.md && grep -q "Phase 21 (7: SNM-22, SNM-24, SNM-25, SNM-27, SNM-28, SNM-29, SNM-32)" .planning/REQUIREMENTS.md && head -10 docs/adr/v2-001-structured-notify.md | grep -q "Phase 17" && grep -A 8 "ES-5" docs/messaging-style-guide.md | grep -q "fully retired" && pre-commit run --files docs/messaging-style-guide.md tests/architecture/grammar-frontmatter.test.ts .planning/REQUIREMENTS.md docs/adr/v2-001-structured-notify.md && npm run check</automated>
  </verify>
  <done>
    - `docs/messaging-style-guide.md` is 150-250 lines, first line is `# Messaging Style Guide` (no YAML frontmatter), ES-5 Supersession Table preserved verbatim with the Phase 21 retirement annotation.
    - `tests/architecture/grammar-frontmatter.test.ts` does not exist.
    - `.planning/REQUIREMENTS.md` has `| SNM-26 | Phase 17 | Complete |` AND the per-phase distribution line lists `Phase 17 (4: SNM-19, SNM-20, SNM-26, SNM-31)` and `Phase 21 (7: ...)` without SNM-26.
    - `docs/adr/v2-001-structured-notify.md` Accepted-status block references Phase 17.
    - `npm run check` exits 0 (typecheck + lint + format check + full test suite GREEN with grammar-frontmatter test gone; the catalog UAT still drives V1 composers against the still-v1.0 catalog so it stays GREEN).
  </done>
</task>

<task type="auto">
  <name>Task 2: Atomic commit on the active branch</name>
  <files>(commit only -- no further file edits)</files>
  <read_first>
    - CLAUDE.md §Git (commit policy, Conventional Commits, `SKIP=trufflehog` worktree caveat, no `--no-verify`)
  </read_first>
  <action>
    Stage exactly the four files modified by Task 1 (`docs/messaging-style-guide.md`, `tests/architecture/grammar-frontmatter.test.ts` as a deletion, `.planning/REQUIREMENTS.md`, `docs/adr/v2-001-structured-notify.md`) via `git add -- docs/messaging-style-guide.md tests/architecture/grammar-frontmatter.test.ts .planning/REQUIREMENTS.md docs/adr/v2-001-structured-notify.md`. Detect whether the current working directory is inside `.worktrees/` via `git rev-parse --show-toplevel | grep -q '/.worktrees/'`; if so, prefix the commit invocation with `SKIP=trufflehog` per CLAUDE.md and additionally run `pre-commit run trufflehog --all-files` separately to confirm a clean scan. Create a single Conventional Commits message with title length 5-72 chars, body lines <= 80 chars. Suggested title: `docs(17): rewrite style guide to v2.0 + delete frontmatter test`. Body must cite SNM-19 + SNM-26, reference D-17-01 + D-17-02 + D-17-07 + D-17-08, and note that REQUIREMENTS.md SNM-26 ownership advances from Phase 21 to Phase 17 by D-17-02 forced consequence. Do NOT use `--no-verify`. Do NOT commit to `main` (verify branch via `git rev-parse --abbrev-ref HEAD` -- expect `gsd/v1.3-replan-catalog` or a `features/*` branch, never `main`).
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -E "^docs\\(17\\)" && git diff HEAD~1 --name-only | sort | grep -q "^docs/messaging-style-guide.md$" && git diff HEAD~1 --name-only | grep -q "^tests/architecture/grammar-frontmatter.test.ts$" && git diff HEAD~1 --name-only | grep -q "^.planning/REQUIREMENTS.md$" && git diff HEAD~1 --name-only | grep -q "^docs/adr/v2-001-structured-notify.md$" && [ "$(git rev-parse --abbrev-ref HEAD)" != "main" ]</automated>
  </verify>
  <done>
    - HEAD commit title matches `^docs\(17\):` (Conventional Commits).
    - Commit touches exactly the four expected paths (one of which is a deletion).
    - Current branch is not `main`.
    - Pre-commit hooks ran without `--no-verify` and passed (failures would have blocked the commit per CLAUDE.md).
  </done>
</task>

</tasks>

<verification>
After both tasks land:

```bash
# All gates must pass:
head -1 docs/messaging-style-guide.md                                    # Expect: # Messaging Style Guide
wc -l docs/messaging-style-guide.md                                       # Expect: 150 <= N <= 250
test ! -f tests/architecture/grammar-frontmatter.test.ts                  # Expect: exit 0 (absent)
grep "| SNM-26 | Phase 17 | Complete |" .planning/REQUIREMENTS.md          # Expect: match
grep "Phase 17 (4:" .planning/REQUIREMENTS.md                              # Expect: match
grep "Phase 17" docs/adr/v2-001-structured-notify.md                       # Expect: non-empty
grep -A 8 "ES-5" docs/messaging-style-guide.md | grep "fully retired"     # Expect: match
npm run check                                                              # Expect: exit 0
```
</verification>

<success_criteria>
- Style guide v2.0 has zero YAML frontmatter and stays within 150-250 lines.
- ES-5 Supersession Table preserved verbatim with Phase 21 retirement annotation.
- Frontmatter parity test deleted; `tests/lint-rules/lib/frontmatter.js`'s module-load no longer crashes because the test file is gone.
- REQUIREMENTS.md traceability table + per-phase distribution line reflect SNM-26 owner Phase 21 → Phase 17.
- ADR Accepted-status block cites Phase 17.
- `npm run check` GREEN; the still-V1 catalog UAT continues to pass against the still-V1 catalog (Plan 17-02 will rewrite the catalog; Plan 17-03 will rewrite the UAT).
- One atomic commit on a non-main branch; Conventional Commits + 80-char body + pre-commit hooks honored.
</success_criteria>

<output>
Create `.planning/phases/17-spec-rewrite-catalog-uat-migration/17-01-SUMMARY.md` recording the rewritten line count, ES-5 preservation evidence, the four files touched, and a one-line note that SNM-19 is editorially complete (status flip to Complete deferred to phase-completion summary alongside SNM-20 + SNM-31) while SNM-26 is now Complete per D-17-02.
</output>
