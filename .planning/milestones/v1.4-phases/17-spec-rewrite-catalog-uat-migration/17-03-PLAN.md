---
phase: 17-spec-rewrite-catalog-uat-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 17-02
files_modified:
  - tests/architecture/catalog-uat.test.ts
  - .planning/REQUIREMENTS.md
  - docs/output-catalog.md  # CONTINGENT: patched only if fixture authoring surfaces byte mismatches against Plan 17-02 catalog (RESEARCH.md Risk 6 run-as-source pattern)
autonomous: true
requirements:
  - SNM-31
  - SNM-19
  - SNM-20
tags:
  - test
  - catalog-uat
  - byte-equality
must_haves:
  truths:
    - "`tests/architecture/catalog-uat.test.ts` drives `notify(ctx, pi, message)` exclusively -- no V1 composer imports remain (D-17-03 pure exclusion)"
    - "Imports from `extensions/pi-claude-marketplace/presentation/*` are absent in the rewritten test file (D-11 layering)"
    - "Imports from `extensions/pi-claude-marketplace/domain/source.ts` are absent (V1 fixture-builder dependency)"
    - "Catalog parser walking logic (per-command H2 + `<!-- catalog-state: STATE -->` pairing with next fenced block) is preserved verbatim per D-17-05 + D-17-06"
    - "Fixture map is keyed by `(section, state)` tuples with `NotificationMessage` payloads + a `MockPi` factory + optional `expectedSeverity` per RESEARCH.md Pitfall 6"
    - "Every catalog-state tuple parsed from `docs/output-catalog.md` has a fixture entry (no missing-fixture failures)"
    - "Driver loop asserts byte-equality between `notify()` output and the catalog fenced block body via `mock.calls[0]!.arguments[0]`"
    - "Driver loop asserts severity arg shape: `arguments.length === 1` when `expectedSeverity` is undefined (info); `arguments.length === 2` with matching string when `expectedSeverity` is set"
    - "`tests/architecture/catalog-uat.test.ts` exits 0 (catalog UAT GREEN) when run alone"
    - "`npm run check` exits 0 at this plan's boundary"
    - "REQUIREMENTS.md status column flips to Complete for SNM-19, SNM-20, SNM-31 (SNM-26 was already flipped in Plan 17-01)"
  artifacts:
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "v2 catalog UAT driving notify() via structured fixtures"
      contains: "notify(ctx as never"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 17 requirement closures: SNM-19, SNM-20, SNM-31 flipped to Complete"
      contains: "| SNM-31 | Phase 17 | Complete |"
  key_links:
    - from: "tests/architecture/catalog-uat.test.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts"
      via: "import { notify, type NotificationMessage }"
      pattern: "from \"\\.\\./\\.\\./extensions/pi-claude-marketplace/shared/notify\\.ts\""
    - from: "tests/architecture/catalog-uat.test.ts"
      to: "docs/output-catalog.md"
      via: "readFile + loadCatalogExamples"
      pattern: "output-catalog\\.md"
---

<objective>
Rewrite `tests/architecture/catalog-uat.test.ts` from V1 composer fixture factories (`renderRow` / `cascadeSummary` / `renderManualRecovery` / `renderRollbackPartial` / `renderPluginList` / `renderMarketplaceList` / `appendReloadHint`) to a single `notify(mockCtx, mockPi, message)` invocation per fixture per D-17-03 (pure exclusion) + D-17-05 (structured `NotificationMessage` fixture map keyed by `(section, state)`). The catalog parser walking logic (lines 65-150 of v1) is preserved VERBATIM per D-17-05/D-17-06 -- only the renderer-call site and the fixture-map shape change. Byte-equality between `notify()` output and the v2.0 catalog (rewritten by Plan 17-02) is the binding user-contract gate per SNM-31.

Purpose: SNM-31 closes by replacing the V1 fixture-factory map with a `Map<(section, state), CatalogFixture>` whose entries hold a structured `NotificationMessage` payload + a `MockPi` factory + optional `expectedSeverity`. The rewritten test is the closed-loop gate: catalog text > parser > fixture lookup > `notify()` > byte-equality. After this plan lands, every byte change in the renderer or the catalog must agree, structurally enforcing the v2 user-contract.

Task shape: Task 1a authors the skeleton (imports, parser, helpers, types, empty FIXTURES map, ancillary self-tests) so the file typechecks + lints cleanly; Task 1b authors the fixture map (~30-50 entries) and wires the driver loop with byte-equality + severity-arg assertions. The split bounds Task 1b's context cost when the fixture-map authoring expands beyond a single session. Task 2 flips REQUIREMENTS.md status; Task 3 atomically commits all output as a single Conventional Commit.

Contingent catalog edit: per RESEARCH.md Risk 6, if Task 1b's fixture authoring surfaces byte mismatches against Plan 17-02's catalog, the executor MAY patch `docs/output-catalog.md` in this plan's same commit (run-as-source pattern; the catalog is treated as drift-on-implementation). The contingent file is pre-declared in `files_modified` frontmatter to legitimize the optional staging path in Task 3.

Additionally, this plan flips the REQUIREMENTS.md status column for the three remaining Phase 17 requirements (SNM-19, SNM-20, SNM-31) to Complete. SNM-26's flip already landed in Plan 17-01.

Output: A fully rewritten catalog UAT test driving `notify()`, plus the REQUIREMENTS.md status closure for the three Phase 17 SNM rows. `npm run check` GREEN at this plan's boundary closes the Phase 17 success-criterion-5 gate.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-spec-rewrite-catalog-uat-migration/17-CONTEXT.md
@.planning/phases/17-spec-rewrite-catalog-uat-migration/17-RESEARCH.md
@.planning/phases/17-spec-rewrite-catalog-uat-migration/17-PATTERNS.md
@.planning/phases/17-spec-rewrite-catalog-uat-migration/17-VALIDATION.md
@.planning/phases/17-spec-rewrite-catalog-uat-migration/17-01-PLAN.md
@.planning/phases/17-spec-rewrite-catalog-uat-migration/17-02-PLAN.md
@tests/architecture/catalog-uat.test.ts
@tests/shared/notify-v2.test.ts
@extensions/pi-claude-marketplace/shared/notify.ts
@docs/output-catalog.md
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Skeleton -- imports, parser, helpers, types, empty FIXTURES map, ancillary self-tests</name>
  <files>tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - tests/architecture/catalog-uat.test.ts (the 1981-line v1 file -- read lines 1-200 for the imports + parser + helper setup; note the ancillary self-tests at v1 lines 1951-1981 -- Task 1a preserves both verbatim)
    - tests/shared/notify-v2.test.ts (1141 lines -- D-16-18 SEED for mock-ctx / mock-pi patterns at lines 120-179; Task 1a lifts the four MockPi factories verbatim)
    - extensions/pi-claude-marketplace/shared/notify.ts lines 1034-1064 (the `notify(ctx, pi, message)` public surface -- only the type imports are needed in Task 1a; the call sites are added in Task 1b)
    - extensions/pi-claude-marketplace/shared/notify.ts lines 181-487 (the const tuples + `NotificationMessage` / `MarketplaceNotificationMessage` / `PluginNotificationMessage` type shapes -- required for the `CatalogFixture` interface)
    - .planning/phases/17-spec-rewrite-catalog-uat-migration/17-PATTERNS.md (concrete V1→V2 import diff + `CatalogFixture` interface shape + driver-loop pseudocode reference)
    - .planning/phases/17-spec-rewrite-catalog-uat-migration/17-RESEARCH.md §Q1 (notify() signature + mock shapes) + §Q5 (parser preservation rules) + §"Common Pitfalls" Pitfall 6 (severity-arg assertion -- Task 1b drives the assertion; Task 1a only defines the `expectedSeverity` field)
  </read_first>
  <acceptance_criteria>
    - V1 composer imports (`presentation/*`, `domain/source.ts`) absent.
    - v2 imports added: `notify` + `NotificationMessage` from `shared/notify.ts`; `{ mock }` from `node:test`.
    - `loadCatalogExamples` parser preserved verbatim (function name + body + regexes).
    - `MockCtx` interface + `makeCtx()` factory + `MockTool` / `MockPi` interfaces + the four pi-factory functions present.
    - `CatalogFixture` interface + `FixtureMap` type defined.
    - `FIXTURES` declared as an empty map (`{}` cast to `FixtureMap`) -- fixture authoring is Task 1b's domain.
    - The two ancillary parser self-tests (v1 lines 1951-1981) preserved verbatim and PASS when run alone.
    - The header docblock (v1 lines 1-37) is rewritten to reflect v2 design, citing SNM-31 + D-17-03 + D-17-05.
    - The catalog-walking driver loop is NOT yet wired (added in Task 1b) -- the file at Task 1a's boundary has parser self-tests passing and the main catalog-driver test stubbed (e.g., `test.skip(...)` or simply absent) so the test file compiles + lints cleanly.
  </acceptance_criteria>
  <action>
    STEP 1 -- Replace the import block at v1 lines 38-63 with the v2 import set.

      Remove these imports (V1 composer fan-out + V1 type imports):
        - The `pathSource` import from `extensions/pi-claude-marketplace/domain/source.ts`.
        - The value-import block bringing in `appendReloadHint`, `cascadeSummary`, `renderManualRecovery`, `renderMarketplaceList`, `renderRow` from `extensions/pi-claude-marketplace/presentation/index.ts`.
        - The `renderPluginList` value-import from `extensions/pi-claude-marketplace/presentation/plugin-list.ts`.
        - The `type` import block bringing in `EntityErrorRow`, `ManualRecoveryLine`, `MarketplaceRow`, `PluginCascadeRow`, `PluginInlineRow`, `PluginInlineUninstalledRow`, `SoftDepProbe` from `extensions/pi-claude-marketplace/presentation/index.ts`.
        - The `PluginListMarketplaceBlock` type-import from `extensions/pi-claude-marketplace/presentation/plugin-list.ts`.

      Add these v2 imports (the only public surface needed):
        - Change `import test from "node:test";` to `import test, { mock } from "node:test";` (add the `{ mock }` named import for the mock-ctx factory).
        - Add a value-and-type import bringing in `notify` and the `NotificationMessage` type from `extensions/pi-claude-marketplace/shared/notify.ts` (use the same `../../extensions/pi-claude-marketplace/shared/notify.ts` relative path the rest of the test tree uses; the type comes in with the TS `type` modifier).

      Keep these v1 imports unchanged: the `assert` default import from `node:assert/strict`; the `readFile` named import from `node:fs/promises`; the `path` default import from `node:path`; the `fileURLToPath` named import from `node:url`.

    STEP 2 -- Preserve the `loadCatalogExamples` parser function (v1 lines 92-150) VERBATIM. Preserve the constants `REPO_ROOT` and `CATALOG_PATH` (v1 lines 69-70) verbatim. Preserve the `CatalogExample` interface (v1 lines 72-76) verbatim.

    STEP 3 -- Replace the helper / fixture-factory section at v1 lines 152-189 with v2 helpers. Add inline:

      - A `MockCtx` interface whose `ui.notify` member is typed as `ReturnType<typeof mock.fn>`, exactly mirroring the shape at `tests/shared/notify-v2.test.ts` lines 136-142 (Option 1 inline duplication per RESEARCH.md §Q1 recommendation), plus a `makeCtx()` factory returning `{ ui: { notify: mock.fn() } }`.
      - A `MockTool` interface (with optional `name?: string` and optional `sourceInfo?: { source?: string }`), a `MockPi` interface (with `getAllTools: () => MockTool[]`), and the four factory functions `piWithBothLoaded` / `piWithSubagentsLoaded` / `piWithMcpLoaded` / `piWithNothingLoaded` -- verbatim from `tests/shared/notify-v2.test.ts` lines 144-179. Each factory returns the corresponding `getAllTools()` result so the `softDepStatus(pi)` probe inside `notify()` observes the desired loaded/unloaded combination.

    STEP 4 -- Replace the `FixtureFactory` / `FixtureMap` types at v1 lines 187-189 with the v2 shape per 17-PATTERNS.md §"Fixture map shape": define a `CatalogFixture` interface with readonly fields `message: NotificationMessage`, `pi: MockPi`, and an optional `expectedSeverity?: "warning" | "error"`. Redeclare `FixtureMap` as a `Readonly<Record<string, Readonly<Record<string, CatalogFixture>>>>`.

    STEP 4b -- Declare `FIXTURES` as an EMPTY map cast to `FixtureMap` (e.g., `const FIXTURES: FixtureMap = {};`). Task 1b populates it.

    STEP 5 -- Update the header docblock at v1 lines 1-37: rewrite to reflect v2 design (drives `notify()` via structured `NotificationMessage` fixtures, not V1 composers); cite SNM-31 + D-17-03 + D-17-05; remove v1 references to `renderRow` / `cascadeSummary` / etc.; cite that V1 wrappers' coverage continues under `tests/shared/notify.test.ts` per D-17-03.

    STEP 6 -- Preserve the two ancillary parser self-tests at v1 lines 1951-1981 verbatim. These do not depend on the renderer and PASS at Task 1a's boundary.

    STEP 7 -- Stub the main catalog-driver test: either omit it entirely from this commit-step (Task 1b adds it) OR mark it as `test.skip(...)`. The file MUST compile + lint cleanly + parser self-tests MUST pass after Task 1a's edits land in working state. No file commit at the Task 1a boundary -- Task 1b finishes the work and the commit task atomically lands both Task 1a + Task 1b output together.

    After the Task 1a edits, run `npm run format` (prettier --write) on the test file. Confirm `npm run typecheck`, `npm run lint`, and the two parser self-tests pass individually (`npm test tests/architecture/catalog-uat.test.ts` MAY still RED if Task 1b has not yet added the driver loop, but the file MUST typecheck + lint).
  </action>
  <verify>
    <automated>! grep -nE "from \"\\.\\./\\.\\./extensions/pi-claude-marketplace/presentation" tests/architecture/catalog-uat.test.ts && ! grep -nE "from \"\\.\\./\\.\\./extensions/pi-claude-marketplace/domain/source" tests/architecture/catalog-uat.test.ts && grep -q "from \"../../extensions/pi-claude-marketplace/shared/notify.ts\"" tests/architecture/catalog-uat.test.ts && grep -q "import test, { mock } from \"node:test\";" tests/architecture/catalog-uat.test.ts && grep -q "interface MockCtx" tests/architecture/catalog-uat.test.ts && grep -q "interface CatalogFixture" tests/architecture/catalog-uat.test.ts && grep -q "function loadCatalogExamples" tests/architecture/catalog-uat.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <done>
    - V1 composer imports gone; v2 imports present.
    - Parser + helpers + types + empty FIXTURES map defined.
    - Header docblock rewritten to v2 design.
    - Parser self-tests preserved verbatim.
    - File typechecks + lints cleanly at Task 1a's boundary (no driver loop wired yet).
  </done>
</task>

<task type="auto">
  <name>Task 1b: Author fixture map + wire driver loop + byte-equality assertions</name>
  <files>tests/architecture/catalog-uat.test.ts, docs/output-catalog.md</files>
  <read_first>
    - tests/architecture/catalog-uat.test.ts (the partial Task 1a output -- read the empty FIXTURES map declaration + the stubbed/absent driver-loop test slot; this is where Task 1b authors)
    - tests/shared/notify-v2.test.ts (1141 lines -- per-variant expected-byte strings starting line 189; lift verbatim where the fixture matches a catalog state)
    - extensions/pi-claude-marketplace/shared/notify.ts (full read -- fixture composition needs the discriminated-union carve-outs at lines 288-448, the `notify()` flow at lines 1034-1064, the const tuples at lines 181-292, the `Reason` type at lines 78-110, and the helper functions `composeMarketplaceBlock` / `renderMpHeader` / `renderPluginRow` / `composeReasons` / `joinTokens` / `renderVersion` / `composeVersionArrow` / `shouldEmitReloadHint` / `computeSeverity` to predict byte output)
    - docs/output-catalog.md (the v2.0 catalog from Plan 17-02 -- every catalog-state marker in this file MUST have a matching fixture entry; the fixture-count budget is approximately 30-50 entries per the catalog's ≥ 30-state floor + a few headroom states)
    - .planning/phases/17-spec-rewrite-catalog-uat-migration/17-PATTERNS.md (§"V2 driver loop" pseudocode + §"Fixture map shape" composition rules)
    - .planning/phases/17-spec-rewrite-catalog-uat-migration/17-RESEARCH.md §Q2 (per-variant fixture seeds) + §Q3 (49-state inventory) + §Q4 (Phase 16 grammar landmines per state) + §"Common Pitfalls" Pitfall 6 (severity-arg assertion) + §"Risks & Mitigations" Risk 6 (run-as-source pattern for catalog drift)
  </read_first>
  <acceptance_criteria>
    - `FIXTURES` map populated with one `CatalogFixture` entry per (section, state) tuple parsed from `docs/output-catalog.md` v2.0 (~30-50 entries; floor `examples.length >= 30` per Plan 17-02 contract).
    - Driver-loop test wired: reads catalog, walks `loadCatalogExamples`, asserts `examples.length >= 30`, looks up fixture per example, constructs fresh `ctx = makeCtx()`, invokes `notify(ctx as never, fixture.pi as never, fixture.message)`, asserts `ctx.ui.notify.mock.calls.length === 1`, extracts `callArgs[0]` (actual body), compares against `example.expected` (byte equality), asserts severity-arg shape per Pitfall 6.
    - `npm test tests/architecture/catalog-uat.test.ts` exits 0 (catalog UAT GREEN against the v2.0 catalog).
    - `npm run check` exits 0 (full suite GREEN -- typecheck + lint + format-check + test).
    - If byte mismatches surface during fixture authoring AND the cause is the catalog block diverging from what `notify()` actually emits, executor MAY patch `docs/output-catalog.md` in the SAME commit per RESEARCH.md Risk 6's run-as-source pattern (Plan 17-02 catalog is treated as drift-on-implementation; correctness winner is `notify()`).
  </acceptance_criteria>
  <action>
    STEP 1 -- Populate the `FIXTURES` map (replacing the empty `{}` from Task 1a). The outer-map keys are the 12 per-command H2 strings (`/claude:plugin list`, `/claude:plugin install <plugin>@<marketplace>`, `/claude:plugin uninstall <plugin>@<marketplace>`, `/claude:plugin reinstall`, `/claude:plugin update`, `/claude:plugin import`, `/claude:plugin bootstrap`, `/claude:plugin marketplace list`, `/claude:plugin marketplace add <source>`, `/claude:plugin marketplace remove <name>`, `/claude:plugin marketplace update <name>`, the autoupdate command surface) plus the `manual-recovery-anchors` key (per the parser's `currentSection = sectionMatch[2] ?? "manual-recovery-anchors"` fallback at v1 line 126). The inner-map keys are the catalog-state STATE strings (`empty`, `success`, `single-mp-mixed`, etc.) defined by the v2.0 catalog from Plan 17-02.

      For each (section, state) pair present in the v2.0 catalog (read `docs/output-catalog.md` at fixture-authoring time), construct a `CatalogFixture` whose `message: NotificationMessage` produces byte-equal output to the catalog's fenced block when passed through `notify()`. Use Phase 16's `tests/shared/notify-v2.test.ts` per-variant tests as the SEED -- many state byte strings can be lifted verbatim from those tests' `assert.deepEqual` second arguments. Compose multi-plugin / multi-marketplace fixtures by combining the per-variant building blocks per RESEARCH.md §Q4.

      Fixture-count budget: approximately 30-50 entries (matches the v2 catalog's ≥ 30 catalog-state floor per Plan 17-02 + headroom for newly authored composition states).

      Per-fixture composition rules:
        - `pi` picks a `MockPi` factory consistent with the catalog state's soft-dep markers. Use `piWithBothLoaded()` for states without `{requires pi-...}` markers (or where the soft-dep would not render anyway because the plugin doesn't declare the dependency); `piWithNothingLoaded()` for combined `{requires pi-subagents, requires pi-mcp}` states; `piWithSubagentsLoaded()` (mcp NOT loaded) for `{requires pi-mcp}` solo-marker states; `piWithMcpLoaded()` (subagents NOT loaded) for `{requires pi-subagents}` solo-marker states.
        - `expectedSeverity` is set ONLY when the state's payload triggers `computeSeverity` to return `"warning"` or `"error"` per Phase 16 D-16-11: failed-bearing → `"error"`; skipped or manual-recovery without any failed → `"warning"`; otherwise omit the field (info severity, no 2nd arg).
        - Plugin variants must satisfy the discriminated-union carve-outs at `shared/notify.ts` lines 288-448. Required-field summary: `installed` / `updated` / `reinstalled` REQUIRE `dependencies: readonly Dependency[]`; `unavailable` / `upgradable` / `failed` / `skipped` / `manual recovery` REQUIRE `reasons: readonly Reason[]`; `available` and `unavailable` MUST NOT carry `scope?` per SNM-11; `updated` REQUIRES `from: string` and `to: string` instead of `version?`; `failed` MAY carry `cause?: Error` and `rollbackPartial?: readonly { phase: string; cause?: Error }[]`; `manual recovery` MAY carry `cause?: Error`.

    STEP 2 -- Replace the v1 driver loop at v1 lines 1878-1949 (or the stubbed slot left by Task 1a) with the v2 driver. The v2 driver shape is documented at 17-PATTERNS.md §"V2 driver loop" and at RESEARCH.md §Q5 step 6:

      - Read the catalog text via an awaited `readFile(CATALOG_PATH, "utf8")`.
      - Call `loadCatalogExamples(catalog)` (parser preserved by Task 1a).
      - Assert `examples.length >= 30` (matches v1 floor; v2.0 catalog has ≥ 30 states per Plan 17-02 contract).
      - For each example, look up `FIXTURES[example.section]?.[example.state]`; push a `missing-fixture` failure if undefined.
      - Construct a fresh `ctx = makeCtx()` per iteration (do NOT reuse; `mock.fn()` accumulates calls).
      - Invoke `notify(ctx as never, fixture.pi as never, fixture.message)`.
      - Assert `ctx.ui.notify.mock.calls.length === 1` per fixture (one `notify()` call must trigger exactly one `ctx.ui.notify` call).
      - Extract `callArgs = ctx.ui.notify.mock.calls[0]!.arguments as [string, string?]`, then `actual = callArgs[0]`.
      - Compare `actual !== example.expected`; on mismatch push a `byte-mismatch` failure record carrying both `expected` and `actual` bytes.
      - Severity-arg assertion per Pitfall 6: if `fixture.expectedSeverity !== undefined` then require `callArgs.length === 2` AND `callArgs[1] === fixture.expectedSeverity`; else require `callArgs.length === 1`. Push `severity-mismatch` failures with `expected` + `actual` when violated.
      - At loop end, if `failures.length > 0`, format the failure list (mirror v1's `[BYTE MISMATCH]` / `[MISSING FIXTURE]` formatting at v1 lines 1930-1947; add `[SEVERITY MISMATCH]` for the new failure kind) and call `assert.fail(...)`.

    STEP 3 -- Run `npm run format` (prettier --write) on the test file. Then run the catalog UAT alone to surface byte mismatches with maximum signal via `npm test tests/architecture/catalog-uat.test.ts` -- must exit 0.

    STEP 4 -- If byte mismatches surface, the failure mode is typically:
      (a) a fixture payload diverges from the catalog block's expected bytes -- fix the fixture, OR
      (b) the catalog block diverges from what `notify()` actually emits -- patch `docs/output-catalog.md` in this plan's same commit per RESEARCH.md Risk 6's run-as-source pattern (do NOT split into a separate commit; SNM-20 byte-equality is a Plan 17-03 closure gate).
    The catalog's bytes are the binding contract; the fixture is the synthesis input; `notify()`'s emission is the correctness winner. They must all agree.

    STEP 5 -- Run `pre-commit run --files tests/architecture/catalog-uat.test.ts docs/output-catalog.md` (include the catalog only if it was patched in STEP 4); resolve failures, restage, re-run until clean. Then run `npm run check`; must exit 0 (full suite GREEN). This is the gate that closes the deliberate RED window opened by Plan 17-02.
  </action>
  <verify>
    <automated>grep -q "notify(ctx as never" tests/architecture/catalog-uat.test.ts && npm test tests/architecture/catalog-uat.test.ts && pre-commit run --files tests/architecture/catalog-uat.test.ts docs/output-catalog.md .planning/REQUIREMENTS.md && npm run check</automated>
  </verify>
  <known_limitations>
    - If byte mismatches surface during fixture authoring, executor MAY patch `docs/output-catalog.md` in this plan's same commit; the catalog is treated as drift-on-implementation per RESEARCH.md Risk 6 run-as-source pattern. The contingent edit is pre-declared in this plan's `files_modified` frontmatter so the commit task's staging step does not surprise the executor.
    - The contingent catalog patch is bounded: it MUST be limited to byte-string corrections inside fenced blocks pinned by an existing `<!-- catalog-state: STATE -->` marker; structural changes (new sections, new state markers, renamed states) are out of scope for this plan and require returning to Plan 17-02 for a follow-up.
    - The `<verify><automated>` step's final `npm run check` is the binding GREEN gate that closes Plan 17-02's deliberate RED window. If `npm run check` is still RED after fixture authoring + (optional) catalog patching, the executor MUST stop and surface the failure mode (typically: a fixture's `NotificationMessage` cannot reproduce the catalog block's bytes given the current renderer behavior -- which implies either a v2 grammar gap that needs Phase 21 escalation, or a misread of the v2 catalog Conventions text).
  </known_limitations>
  <done>
    - `FIXTURES` map populated with ~30-50 entries (one per parsed catalog-state tuple).
    - Driver loop wired and asserts byte-equality + severity-arg shape per fixture.
    - `npm test tests/architecture/catalog-uat.test.ts` exits 0.
    - `npm run check` exits 0 -- Phase 17 success criterion #5 satisfied for the test layer.
    - Catalog patched only if byte mismatches surface against Plan 17-02 output (contingent edit per RESEARCH.md Risk 6).
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip REQUIREMENTS.md status to Complete for SNM-19, SNM-20, SNM-31</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 75-117 (the traceability table -- confirm SNM-19, SNM-20, SNM-31 currently read `| SNM-XX | Phase 17 | Pending |`; SNM-26 was already flipped to Complete in Plan 17-01)
    - .planning/phases/17-spec-rewrite-catalog-uat-migration/17-01-PLAN.md (confirms Plan 17-01 flipped SNM-26 but deferred SNM-19/20/31 to phase-completion)
    - .planning/phases/17-spec-rewrite-catalog-uat-migration/17-RESEARCH.md §Q9 (REQUIREMENTS.md edit shape)
  </read_first>
  <action>
    On `.planning/REQUIREMENTS.md`, change three traceability-table rows:
      - The `SNM-19` row: change `| SNM-19 | Phase 17 | Pending |` to `| SNM-19 | Phase 17 | Complete |`.
      - The `SNM-20` row: change `| SNM-20 | Phase 17 | Pending |` to `| SNM-20 | Phase 17 | Complete |`.
      - The `SNM-31` row: change `| SNM-31 | Phase 17 | Pending |` to `| SNM-31 | Phase 17 | Complete |`.

    Do NOT touch SNM-26 (already Complete from Plan 17-01). Do NOT touch the per-phase distribution line (already updated in Plan 17-01). The verify-block re-assertion of SNM-26 below is intentional defense-in-depth: after `npm run check` GREEN proof in Task 1b, this re-confirms the Plan 17-01 flip survived through wave-3 edits (no regression). The SNM-26 grep is redundant with Plan 17-01 but the cost is one grep and the benefit is end-of-phase invariant verification.

    Run `npm run format` (prettier --write) on the file in case prettier touches the table column widths. Then run `pre-commit run --files .planning/REQUIREMENTS.md`; commit per Task 3 below.
  </action>
  <verify>
    <automated>grep -q "| SNM-19 | Phase 17 | Complete |" .planning/REQUIREMENTS.md && grep -q "| SNM-20 | Phase 17 | Complete |" .planning/REQUIREMENTS.md && grep -q "| SNM-31 | Phase 17 | Complete |" .planning/REQUIREMENTS.md && grep -q "| SNM-26 | Phase 17 | Complete |" .planning/REQUIREMENTS.md && [ "$(grep -c 'Phase 17 | Pending' .planning/REQUIREMENTS.md)" = "0" ] && pre-commit run --files .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - All four Phase 17 SNM rows (SNM-19, SNM-20, SNM-26, SNM-31) read Complete.
    - No `Phase 17 | Pending` rows remain in REQUIREMENTS.md (the phase's requirements are now all closed).
  </done>
</task>

<task type="auto">
  <name>Task 3: Atomic commit on the active branch</name>
  <files>(commit only -- no further file edits)</files>
  <read_first>
    - CLAUDE.md §Git (Conventional Commits, `SKIP=trufflehog` worktree caveat, no `--no-verify`, no `main`-branch commits)
  </read_first>
  <action>
    Stage `tests/architecture/catalog-uat.test.ts` AND `.planning/REQUIREMENTS.md` always; ADDITIONALLY stage `docs/output-catalog.md` IF Task 1b patched it as a contingent run-as-source correction (check `git status --porcelain docs/output-catalog.md` -- if dirty, include it in the `git add --` argument list). Concretely: `git status --porcelain docs/output-catalog.md` returning non-empty means the catalog was patched; in that case run `git add -- tests/architecture/catalog-uat.test.ts .planning/REQUIREMENTS.md docs/output-catalog.md`, otherwise run `git add -- tests/architecture/catalog-uat.test.ts .planning/REQUIREMENTS.md`. Detect worktree context by running `git rev-parse --show-toplevel` and grepping for `/.worktrees/`; if matched, prefix the `git commit` invocation with `SKIP=trufflehog` and run `pre-commit run trufflehog --all-files` separately first to confirm clean. Conventional Commits title (5-72 chars): `test(17): rewrite catalog UAT to drive notify() via structured fixtures`. Body must cite SNM-31 + D-17-03 + D-17-05 + D-17-06, mention that `npm run check` returns to GREEN at this commit (closing the Plan 17-02 → Plan 17-03 byte-equality window per Pitfall 2), and note that SNM-19 + SNM-20 + SNM-31 flip to Complete on this commit (SNM-26 was already Complete via Plan 17-01). Body lines max 80 chars. Do NOT use `--no-verify`. Verify branch is not `main` via `git rev-parse --abbrev-ref HEAD`.
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -E "^test\\(17\\)" && git diff HEAD~1 --name-only | grep -q "^tests/architecture/catalog-uat.test.ts$" && git diff HEAD~1 --name-only | grep -q "^.planning/REQUIREMENTS.md$" && [ "$(git rev-parse --abbrev-ref HEAD)" != "main" ] && npm run check</automated>
  </verify>
  <done>
    - HEAD commit title starts with `test(17):` (Conventional Commits).
    - Commit touches `tests/architecture/catalog-uat.test.ts` and `.planning/REQUIREMENTS.md` always; touches `docs/output-catalog.md` only if Task 1b applied a contingent catalog correction.
    - Current branch is not `main`.
    - Pre-commit hooks ran without `--no-verify` and passed.
    - `npm run check` exits 0 -- Phase 17 success criterion #5 satisfied.
  </done>
</task>

</tasks>

<verification>
After all four tasks land (Task 1a + Task 1b + Task 2 + Task 3):

```bash
# Test file shape gates:
grep -q "from \"../../extensions/pi-claude-marketplace/shared/notify.ts\"" tests/architecture/catalog-uat.test.ts
! grep -E "from \"\\.\\./\\.\\./extensions/pi-claude-marketplace/presentation" tests/architecture/catalog-uat.test.ts
! grep -E "from \"\\.\\./\\.\\./extensions/pi-claude-marketplace/domain/source" tests/architecture/catalog-uat.test.ts
grep -q "import test, { mock } from \"node:test\";" tests/architecture/catalog-uat.test.ts
grep -q "interface MockCtx" tests/architecture/catalog-uat.test.ts
grep -q "interface CatalogFixture" tests/architecture/catalog-uat.test.ts
grep -q "function loadCatalogExamples" tests/architecture/catalog-uat.test.ts
grep -q "notify(ctx as never" tests/architecture/catalog-uat.test.ts

# REQUIREMENTS.md closure gates:
grep -q "| SNM-19 | Phase 17 | Complete |" .planning/REQUIREMENTS.md
grep -q "| SNM-20 | Phase 17 | Complete |" .planning/REQUIREMENTS.md
grep -q "| SNM-26 | Phase 17 | Complete |" .planning/REQUIREMENTS.md
grep -q "| SNM-31 | Phase 17 | Complete |" .planning/REQUIREMENTS.md
[ "$(grep -c 'Phase 17 | Pending' .planning/REQUIREMENTS.md)" = "0" ]

# Full suite gate (Phase 17 success criterion #5):
npm run check
```
</verification>

<success_criteria>
- Catalog UAT drives `notify()` exclusively (D-17-03 pure exclusion); no `presentation/*` or `domain/source.ts` imports remain.
- Parser walking logic preserved verbatim (D-17-05 + D-17-06).
- Fixture map keyed by `(section, state)` tuples with structured `NotificationMessage` + `MockPi` + optional `expectedSeverity` (D-17-05).
- Catalog UAT GREEN against v2.0 catalog (byte-equality holds for every parsed (section, state) tuple).
- All four Phase 17 SNM rows in REQUIREMENTS.md read Complete.
- `npm run check` GREEN -- Phase 17 success criterion #5 satisfied.
- One atomic commit on a non-main branch; Conventional Commits + 80-char body + pre-commit hooks honored.
</success_criteria>

<output>
Create `.planning/phases/17-spec-rewrite-catalog-uat-migration/17-03-SUMMARY.md` recording: the count of catalog-state fixtures in the rewritten test, the number of `notify()` calls per test run (one per fixture), the catalog-state coverage cross-check against `docs/output-catalog.md`, confirmation that all four Phase 17 SNM rows are Complete, and `npm run check` GREEN at HEAD.

This SUMMARY closes Phase 17.
</output>
