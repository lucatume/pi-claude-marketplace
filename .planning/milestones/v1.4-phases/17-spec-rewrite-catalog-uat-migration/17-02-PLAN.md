---
phase: 17-spec-rewrite-catalog-uat-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - docs/output-catalog.md
autonomous: true
requirements:
  - SNM-20
tags:
  - documentation
  - catalog
  - user-contract
must_haves:
  truths:
    - "Catalog v2.0 retains all 14 per-command H2 sections plus the `Manual recovery anchors`, `Empty / no-op surfaces`, and `Usage errors` sections (CONTEXT.md §Section preservation)"
    - "Every catalog-state marker uses the verbatim `<!-- catalog-state: STATE -->` convention (D-17-06)"
    - "Single-plugin install / update / uninstall / reinstall render the two-line always-marketplace-header form (D-17-04 + Phase 16 D-16-04)"
    - "Catalog includes at least one orphan-fold example (plugin.scope != marketplace.scope emits the plugin-row scope bracket) per Phase 16 D-16-17"
    - "Catalog includes at least one failed-only state with no reload-hint trailer (Phase 16 D-16-12 suppression case)"
    - "Catalog Conventions section documents v1→v2 dropped surfaces (`Claude plugin import summary` preamble, `Fix the underlying issue and retry.` anchor, `source-mismatch` diagnostic line, `install-failure-with-anchor`, per-marketplace `(no plugins)` line) per D-17-09 + D-17-10"
    - "Catalog has no `<!-- catalog-state: install-failure-with-anchor -->` marker (D-17-10)"
    - "Catalog has no `<!-- catalog-state: source-mismatch -->` marker AND no `Claude plugin import summary` literal (D-17-09)"
    - "`npm run check` GREEN status is NOT enforced at this plan boundary because the catalog UAT (still V1) will byte-mismatch against the v2 catalog -- Plan 17-03 restores GREEN (Pitfall 2)"
  artifacts:
    - path: "docs/output-catalog.md"
      provides: "v2.0 user-contract catalog matching `notify()` byte output"
      contains: "<!-- catalog-state:"
  key_links:
    - from: "docs/output-catalog.md"
      to: "extensions/pi-claude-marketplace/shared/notify.ts"
      via: "Expected outputs are byte-equal to `notify()` emission"
      pattern: "always-marketplace-header"
    - from: "docs/output-catalog.md"
      to: "docs/messaging-style-guide.md"
      via: "Cross-references section pointer"
      pattern: "messaging-style-guide\\.md"
---

<objective>
Rewrite `docs/output-catalog.md` from v1.0 (971 lines, V1 composer outputs with single-plugin one-line carve-out) to v2.0 (always-marketplace-header form per Phase 16 D-16-04; silent on migration state per D-17-04). Every per-command section's expected output is rewritten to match what the v2 `notify(ctx, pi, message)` renderer in `extensions/pi-claude-marketplace/shared/notify.ts` emits given the corresponding structured `NotificationMessage`. Phase 16's `tests/shared/notify-v2.test.ts` (1141 lines, 32 per-variant tests) is the authoritative seed for byte-strings per D-16-18.

Purpose: SNM-20 closes the v1.4 catalog-rewrite mandate. The catalog becomes the binding user-contract text Plan 17-03's UAT will assert byte-equality against. Catalog v2.0 also documents the v1→v2 behavior simplifications per D-17-09 (drops `Claude plugin import summary` preamble, `Fix the underlying issue and retry.` retry anchor, `source-mismatch` diagnostic line) and D-17-10 (drops the `install-failure-with-anchor` system-level recovery state) so reviewers understand the deliberate simplifications.

Output: A v2.0 catalog whose every fenced output block is byte-equal to what `notify()` emits given a corresponding structured fixture. The plan deliberately does NOT enforce `npm run check` GREEN at its boundary -- per RESEARCH.md Pitfall 2, the still-V1 `catalog-uat.test.ts` will byte-mismatch against the v2 catalog (V1 composers emit V1 output; v2 catalog expects v2 output). Plan 17-03 restores GREEN. This is intentional and is the inverse-dependency rationale for Plan 17-03's hard dependency on Plan 17-02.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-spec-rewrite-catalog-uat-migration/17-CONTEXT.md
@.planning/phases/17-spec-rewrite-catalog-uat-migration/17-RESEARCH.md
@.planning/phases/17-spec-rewrite-catalog-uat-migration/17-PATTERNS.md
@.planning/phases/17-spec-rewrite-catalog-uat-migration/17-VALIDATION.md
@docs/output-catalog.md
@extensions/pi-claude-marketplace/shared/notify.ts
@tests/shared/notify-v2.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full rewrite of docs/output-catalog.md to v2.0 always-marketplace-header form</name>
  <files>docs/output-catalog.md</files>
  <read_first>
    - docs/output-catalog.md (the file to rewrite -- read full 971 lines to map every catalog-state marker; the 49-state inventory is documented in 17-RESEARCH.md §Q3)
    - extensions/pi-claude-marketplace/shared/notify.ts (1065 lines -- the binding spec; reading `composeMarketplaceBlock`, `renderMpHeader`, `renderPluginRow`, `composeReasons`, `joinTokens`, `renderVersion`, `composeVersionArrow`, `shouldEmitReloadHint`, `computeSeverity` is mandatory to write byte-equal expected outputs)
    - tests/shared/notify-v2.test.ts (1141 lines, the Phase 16 per-variant unit tests -- D-16-18 SEED for expected output strings; lift verbatim where the fixture matches a catalog state)
    - extensions/pi-claude-marketplace/shared/grammar/reasons.ts (the REASONS const array -- for the `Reason` literal strings allowed in `reasons: readonly Reason[]`)
    - .planning/phases/17-spec-rewrite-catalog-uat-migration/17-PATTERNS.md §"`docs/output-catalog.md` v2.0" (concrete per-command analog patterns + which V1 outputs change to v2 form)
    - .planning/phases/17-spec-rewrite-catalog-uat-migration/17-RESEARCH.md §Q3 (state inventory: 49 markers across 14 sections + 1 recovery anchor) + §Q4 (Phase 16 grammar landmines per state) + §Q8 (size + structure budget) + §"Common Pitfalls" Pitfalls 3-5 (`(no plugins)` deletion, manual-recovery deletion, V1-only states dropped)
  </read_first>
  <action>
    Full rewrite of `docs/output-catalog.md` preserving its skeleton (the per-command H2 inventory, the `<!-- catalog-state: STATE -->` HTML-comment marker convention, the triple-backtick fenced-block expected-output convention) but rewriting every fenced output block to v2 grammar. The catalog is the user-contract gate text Plan 17-03's UAT asserts byte-equality against; every fenced block under a per-command H2 section must equal the body string `notify()` emits for the corresponding structured `NotificationMessage` fixture.

    Top-level structure (planner discretion on exact wording; section budgets per 17-RESEARCH.md §Q8):

      ## Conventions (~60-80 lines)
        - Rewrite the v1 Conventions section. DROP the "Single-plugin commands do NOT use the marketplace-header form" carve-out at v1 lines 59-61 and the related "Marketplace-only commands do NOT use the header form" and "Conditional header-form commands" carve-outs -- v2 grammar is always-marketplace-header.
        - KEEP the glyph rules (`●` / `○` / `⊘`), the scope-bracket rules including the MSG-PL-6 carve-out (`(available)` / `(unavailable)` rows on the plugin-list surface omit the scope bracket; install/reinstall/import surfaces keep it), the per-scope marketplace rendering rule, the fold rule including the orphan-fold case (plugin row carries `[<scope>]` when `plugin.scope !== marketplace.scope` per Phase 16 D-16-17), the autoupdate `<autoupdate>` / `<no autoupdate>` marker rules, the reasons rendering rules.
        - ADD a paragraph documenting the v1→v2 behavior changes per D-17-09 + D-17-10. Cite by name: (1) the `Claude plugin import summary` preamble is no longer emitted; (2) the `Fix the underlying issue and retry.` retry anchor is no longer emitted; (3) the `Existing marketplace source ... does not match Claude settings source ...` source-mismatch diagnostic line is no longer emitted; (4) the per-marketplace `(no plugins)` body line is no longer emitted (empty `plugins: []` IS the structural representation per Phase 15 D-15-08 -- the renderer emits the bare marketplace header alone); (5) the `install-failure-with-anchor` manual-recovery state (system-level `agent index (manual recovery)` line) is dropped because `PluginManualRecoveryMessage` is structurally a per-plugin variant inside a marketplace block, not a system-level wrapper.
        - REWRITE the "Marketplace header rule" subsection: every command surface uses the always-marketplace-header form including single-plugin commands. State that the plugin-row `[<scope>]` bracket is OMITTED when `plugin.scope === marketplace.scope` (per Phase 16 renderer behavior + tests/shared/notify-v2.test.ts line 786 BLOCKER-1 coverage), and EMITTED on orphan-fold rows.

      ## Severity routing (~10 lines)
        - 3-tier ladder per D-16-11 (first-match-wins): failed → error; skipped or manual recovery → warning; else success/info. Drop the v1 "MSG-SR-*" labels and the V1 wrapper-name column -- v2 lets `notify()` compute severity from contents. Use a 3-row table: pattern / severity arg / trigger.

      ## Status token reference (~12-15 lines)
        - 10-row table mirroring `PLUGIN_STATUSES` from `shared/notify.ts` line 181-192 (`installed` / `updated` / `reinstalled` / `uninstalled` / `available` / `unavailable` / `upgradable` / `failed` / `skipped` / `manual recovery`). Column 1: token. Column 2: icon. Column 3: where it appears. Optionally a separate 4-row table for `MARKETPLACE_STATUSES` (`added` / `removed` / `updated` / `failed`). Do NOT include `(no plugins)` / `(no marketplaces)` rows here -- those are body sentinels, not status tokens.

      ## 14 per-command H2 sections -- REWRITE every fenced block.

      For each of the 14 sections (`/claude:plugin list`, `install`, `uninstall`, `reinstall`, `update`, `import`, `bootstrap`, `marketplace list`, `marketplace add`, `marketplace remove`, `marketplace update`, `marketplace autoupdate`, plus the recovery anchor and the empty/no-op surfaces section), rewrite every catalog-state's expected output to v2 form. Preserve the `<!-- catalog-state: STATE -->` marker text verbatim and on its own line above the fenced block (D-17-05's parser walks markers and pairs each with the NEXT fenced block).

      Per-command specifics -- the planner MUST honor the rules below; expected-output bytes come from `notify()` behavior in `shared/notify.ts` and the Phase 16 SEED at `tests/shared/notify-v2.test.ts`:

        - `/claude:plugin list` (8 states from v1: `empty` / `single-mp-mixed` / `same-plugin-both-scopes` / `project-orphan-folded` / `soft-dep-on-installed` / `unparseable-mp` / `zero-plugin-mp-block` / `multiple-mps`):
          - `empty`: v2 renderer emits `(no marketplaces)` for empty top-level `marketplaces: []` per `shared/notify.ts:1048`. v2 expected output: `(no marketplaces)`. (Phase 16 test at notify-v2.test.ts:578-588.)
          - `zero-plugin-mp-block`: DROP the `  (no plugins)` body line per Pitfall 3 -- empty `plugins: []` renders as a bare marketplace header alone with NO body. v2 expected output omits the `(no plugins)` line.
          - `project-orphan-folded`: orphan-fold case -- plugin row CARRIES `[project]` bracket (plugin.scope !== marketplace.scope) per Phase 16 D-16-17.
          - For every other state, rewrite the v1 multi-line block to match what `notify()` emits given a constructed `NotificationMessage` fixture.

        - `/claude:plugin install <plugin>@<marketplace>` (5 states: `success` / `success-with-soft-dep` / `failure-unsupported-features` / `failure-runtime-with-cause` / `failure-rollback-partial`):
          - `success` v1 form `● helper@official [user] v1.0.0 (installed)\n\n/reload to pick up changes` REWRITES to v2 two-line form:
            `● official [user]\n  ● helper v1.0.0 (installed)\n\n/reload to pick up changes`
            Marketplace header is SUB-BRANCH A bare form (`status: undefined`, `details: undefined`); reload-hint fires because plugin status is `installed` per Phase 16 D-16-12. Plugin row omits the `[user]` bracket because `plugin.scope === marketplace.scope` per Phase 16 D-16-17.
          - Apply analogous two-line rewrites to all install / update / uninstall / reinstall single-plugin success states.

        - `/claude:plugin uninstall <plugin>@<marketplace>` (3 states: `success` / `success-soft-dep-omitted` / `failure-permission-denied`):
          - `success` v2: `● official [user]\n  ○ helper v1.0.0 (uninstalled)\n\n/reload to pick up changes` (icon flips to `○` per the effective-state rule; plugin status `uninstalled` fires reload-hint).
          - `failure-permission-denied` v2: marketplace header SUB-BRANCH A bare; plugin row failed with `{permission denied}` reason + `cause:` trailer at 4-space indent (Phase 16 D-16-08 inline cause chain). No reload-hint (failed-only payload -- Phase 16 D-16-12 suppression).
          - Update the section preamble to remove the "Single-plugin command -- keeps `@<marketplace>` inline; no marketplace header" sentence and replace with v2 prose ("Single-plugin command in v2 still renders the always-marketplace-header form; the marketplace appears as a bare header and the plugin row indents underneath").

        - `/claude:plugin reinstall` (7 states): preserve the v1 marketplace-header structure (it was already multi-plugin cascade). Rewrite to use v2 `(reinstalled)` rows; ensure `single-mp-all-failed` produces a failed-only payload with NO reload-hint trailer (Phase 16 D-16-12); add the soft-dep, mixed-outcomes, became-unavailable, multi-mp, and same-mp-both-scopes states.

        - `/claude:plugin update` (5 states): same shape as reinstall but with version-arrow rows `v<from> → v<to>` per Phase 16 `composeVersionArrow`. `failed-with-rollback-partial` state must include the `rollbackPartial` 4-space-indented children + `cause:` trailer per Phase 16 D-16-08.

        - `/claude:plugin import` (5 states from v1 with TWO DROPPED):
          - DROP the `Claude plugin import summary\n\n` preamble from every state per D-17-09 -- `notify()` does not emit free-text preambles.
          - DROP the `source-mismatch` state ENTIRELY per D-17-09 (and re-confirmed in RESEARCH.md Pitfall 5). Either remove the state marker and section, OR replace with a different state that CAN be expressed in v2 grammar. Recommended: remove the `source-mismatch` H3 subsection plus marker plus block; the remaining 4 states (`fresh-mixed-both-scopes`, `scope-project-narrow`, `soft-dep-markers`, `same-mp-both-scopes`) all express cleanly in v2 grammar. Update the catalog Conventions paragraph to call out the source-mismatch removal explicitly.
          - The 4 retained import states retain their cascade structure but lose the `Claude plugin import summary\n\n` preamble line.

        - `/claude:plugin bootstrap` (2 states): keep `fresh` and `already-bootstrapped`. v2 form: `● claude-plugins-official [user] <autoupdate> (added)\n\n/reload to pick up changes` for `fresh`; the marketplace header carries the status -- there are no plugin children in either state.

        - `/claude:plugin marketplace list` (2 states: `empty` / `mixed-scopes`):
          - `empty` v2: bare `(no marketplaces)` body -- `notify()` emits this sentinel when `marketplaces: []`.
          - `mixed-scopes` v2: marketplace-list surface uses SUB-BRANCH B headers with details (`autoupdate` flag) per Phase 16 D-16-09. Lift the expected output from `tests/shared/notify-v2.test.ts` line 529-559 (the marketplace-details test). Rows have NO plugin children -- only marketplace headers.

        - `/claude:plugin marketplace add <source>` (3 states: `path-source` / `github-source` / `failure-unreachable`):
          - `path-source` v2: `● local-mp [user] (added)\n\n/reload to pick up changes` (marketplace status `added` per Phase 16 D-16-12 fires the reload-hint).
          - `github-source` v2: `● claude-plugins-official [user] <autoupdate> (added)\n\n/reload to pick up changes` (autoupdate marker via `details: { autoupdate: true }`).
          - `failure-unreachable` v2: `⊘ unreachable-mp [user] (failed)\n  cause: fatal: unable to access ...` -- failed marketplace header with `cause:` trailer at 2-space indent (Phase 16 marketplace-level cause chain). No reload-hint (failed-only).

        - `/claude:plugin marketplace remove <name>` (2 states: `clean` / `partial`):
          - `clean` v2: `● local-mp [user] (removed)\n\n/reload to pick up changes`.
          - `partial` v2: marketplace failed with `{plugins remain}` + plugin children mixing uninstalled / failed outcomes. DROP the `Fix the underlying issue and retry.` trailer per D-17-09 -- `notify()` does not emit free-text retry anchors. Reload-hint fires only if at least one plugin was uninstalled (per Phase 16 D-16-12 mixed-outcome rule); in the v2 expected output for `partial` show at least one `(uninstalled)` row so the reload-hint trailer is present at the end.

        - `/claude:plugin marketplace update <name>` (3 states: `autoupdate-off-manifest-refresh` / `mixed-outcomes` / `mp-failure-network`):
          - `autoupdate-off-manifest-refresh` v2: `● local-mp [user] (updated)` -- marketplace-only update with no plugin children. Reload-hint discipline per Phase 16 D-16-12 (state-changing marketplace fires hint).
          - `mp-failure-network` v2: `⊘ official [user] <autoupdate> (failed) {network unreachable}\n  cause: fatal: unable to access ...` -- no plugin children, no reload-hint (failed-only).

        - `/claude:plugin marketplace autoupdate <enable|disable> <name>` (3 states: `enable-mixed` / `disable-mixed` / `failure-not-found`):
          - These are marketplace-only flag-flip surfaces with no plugin children. The `<autoupdate>` / `<no autoupdate>` marker is the outcome (no status token on these rows per the v1 convention) per Phase 16 renderMpHeader behavior. `failure-not-found` v2: `⊘ missing-mp [user] (failed) {not found}` -- no cause trailer, no reload-hint.

        - `Manual recovery anchors` section: DROP the `install-failure-with-anchor` state ENTIRELY per D-17-10 (the system-level `(manual recovery)` resource shape has no v2 grammar equivalent). The retained content of this section should describe how `PluginManualRecoveryMessage` (per-plugin variant inside a marketplace block) surfaces in cascades -- the per-plugin manual-recovery row is covered indirectly via the reinstall / update / install cascade sections when a cascade includes a manual-recovery plugin variant. The planner picks whether to add a new dedicated state for a per-plugin `manual recovery` variant under a `marketplace block (one of the existing per-command sections would already cover this).

      ## Empty / no-op surfaces (~6 lines)
        - Trim the table. Keep `(no plugins)` for empty plugin lists, `(no marketplaces)` for empty marketplace lists, but DROP the per-marketplace `(no plugins)` row per Pitfall 3.

      ## Usage errors (~8-10 lines)
        - Keep the v1 narrative -- `notifyUsageError(ctx, { message, usage })` renders `${message}\n\n${usage}` with `"error"` severity (unchanged in v2 per Phase 16 D-16-03).

      ## Resolutions to apply to `docs/messaging-style-guide.md` (lines 925-963)
        - DELETE this entire section (39 lines of authoring scratchpad from v1.0; v2.0 doesn't need it).

      ## Cross-references
        - Update to point at: `docs/messaging-style-guide.md` v2.0; `docs/adr/v2-001-structured-notify.md`; `extensions/pi-claude-marketplace/shared/notify.ts`; `tests/architecture/notify-types.test.ts`; `tests/architecture/catalog-uat.test.ts`; `docs/prd/pi-claude-marketplace-prd.md`.

    Target line count: ~750-850 (net-neutral vs v1.0 per RESEARCH.md §Q8 -- single-plugin rows get longer (2-line vs 1-line), but the catalog drops the Resolutions scratchpad and the V1-only states).

    State-marker integrity rules (D-17-05/D-17-06):
      - Every `<!-- catalog-state: STATE -->` comment appears on its own line directly above (within 1-3 lines) the fenced output block it discriminates.
      - STATE strings remain lowercase + digits + hyphens only (the parser regex is `/^<!-- catalog-state: ([a-z0-9-]+) -->\s*$/`).
      - Every state's fenced block holds exactly the bytes `notify()` will emit for the corresponding fixture in Plan 17-03 -- NO fenced block contains v1-composer output.

    After the rewrite, run `npm run format` (prettier --write) to normalize markdown formatting. Run `pre-commit run --files docs/output-catalog.md`; resolve failures, restage, re-run until clean. Note: `npm run check` is EXPECTED to FAIL at this plan boundary because the still-V1 catalog UAT will byte-mismatch against the v2 catalog (Pitfall 2) -- this is normal and is restored by Plan 17-03.
  </action>
  <verify>
    <automated>test "$(grep -c '<!-- catalog-state:' docs/output-catalog.md)" -ge 30 && ! grep -q "Claude plugin import summary" docs/output-catalog.md && ! grep -q "<!-- catalog-state: install-failure-with-anchor -->" docs/output-catalog.md && ! grep -q "<!-- catalog-state: source-mismatch -->" docs/output-catalog.md && ! grep -q "Fix the underlying issue and retry" docs/output-catalog.md && ! grep -q "Existing marketplace source" docs/output-catalog.md && ! grep -q "^## Resolutions to apply to" docs/output-catalog.md && grep -q "^## \`/claude:plugin list\`" docs/output-catalog.md && grep -q "^## \`/claude:plugin install" docs/output-catalog.md && grep -q "^## \`/claude:plugin uninstall" docs/output-catalog.md && grep -q "^## \`/claude:plugin reinstall\`" docs/output-catalog.md && grep -q "^## \`/claude:plugin update\`" docs/output-catalog.md && grep -q "^## \`/claude:plugin import\`" docs/output-catalog.md && grep -q "^## \`/claude:plugin bootstrap\`" docs/output-catalog.md && grep -q "^## \`/claude:plugin marketplace list\`" docs/output-catalog.md && grep -q "^## \`/claude:plugin marketplace add" docs/output-catalog.md && grep -q "^## \`/claude:plugin marketplace remove" docs/output-catalog.md && grep -q "^## \`/claude:plugin marketplace update" docs/output-catalog.md && grep -q "^## \`/claude:plugin marketplace autoupdate" docs/output-catalog.md && grep -q "^## Manual recovery anchors" docs/output-catalog.md && grep -q "^## Empty / no-op surfaces" docs/output-catalog.md && grep -q "^## Usage errors" docs/output-catalog.md && npm run typecheck && npm run lint && npx prettier --check docs/output-catalog.md && (npm test tests/architecture/catalog-uat.test.ts 2>&1 | tee /tmp/p17-02-uat-output.log; UAT_EXIT=${PIPESTATUS[0]}; if [ "$UAT_EXIT" = "0" ]; then echo "UNEXPECTED: catalog UAT GREEN against v2 catalog while still running V1 composers -- STOP and investigate (Plan 17-02 expects RED until Plan 17-03 lands)"; exit 1; fi; grep -qE "BYTE MISMATCH|byte-mismatch|byte equality|expected.*actual|actual.*expected|AssertionError|catalog UAT failures" /tmp/p17-02-uat-output.log || (echo "UNEXPECTED: catalog UAT RED but failure shape is NOT the expected v1-UAT-vs-v2-catalog byte mismatch -- STOP and investigate"; exit 1)) && pre-commit run --files docs/output-catalog.md</automated>
  </verify>
  <known_limitations>
    - This plan deliberately leaves the catalog UAT (`tests/architecture/catalog-uat.test.ts`) in a known-RED state per RESEARCH.md Pitfall 2 + Risk 3. The V1 composer-based UAT byte-mismatches against the v2 catalog because V1 composers emit V1 grammar and the v2 catalog expects v2 grammar.
    - The `<verify><automated>` step above runs the catalog UAT alone and ASSERTS the failure SHAPE matches the expected byte-mismatch pattern (one of `BYTE MISMATCH` / `byte-mismatch` / `byte equality` / `expected.*actual` / `AssertionError` / `catalog UAT failures`). If the UAT unexpectedly GREENs OR fails with a different shape (e.g., a runtime crash, an import error, a parser malfunction), the executor MUST STOP and investigate -- that is NOT the deliberate RED window described here.
    - Plan 17-03 restores `npm run check` GREEN by rewriting the catalog UAT to drive `notify()` instead of V1 composers (D-17-03 pure exclusion). No other plan should attempt to restore GREEN by editing the catalog or the V1 composers.
    - The `npm run check` full-suite gate is NOT part of this plan's `<verify><automated>` block because the catalog UAT's deliberate RED would mask the GREEN expectation. Typecheck + lint + prettier-check are sufficient gates for the catalog file shape; the deliberate RED windowing is the inverse-dependency rationale for Plan 17-03's hard `depends_on: [17-02]`.
  </known_limitations>
  <done>
    - `docs/output-catalog.md` has at least 30 `<!-- catalog-state: STATE -->` markers (≥ Plan 17-03's `assert.ok(examples.length >= 30, ...)` floor).
    - All 14 per-command H2 sections present (12 `/claude:plugin ...` sections + `Manual recovery anchors` + `Empty / no-op surfaces` + `Usage errors`).
    - Catalog contains no `Claude plugin import summary` literal, no `Fix the underlying issue and retry` literal, no `Existing marketplace source` diagnostic literal, no `install-failure-with-anchor` state marker, no `source-mismatch` state marker, no `Resolutions to apply to` H2.
    - Single-plugin install/uninstall/update/reinstall expected outputs render the always-marketplace-header two-line form.
    - At least one orphan-fold example present (planner verifies by inspection -- Phase 16 D-16-17).
    - At least one failed-only state with no reload-hint trailer (Phase 16 D-16-12 suppression).
    - Typecheck + lint + prettier-check pass on `docs/output-catalog.md`.
    - `npm run check` MAY fail at this plan's commit boundary because the V1 catalog UAT byte-mismatches against the v2 catalog. This is acknowledged (Pitfall 2) and Plan 17-03 restores GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Atomic commit on the active branch</name>
  <files>(commit only -- no further file edits)</files>
  <read_first>
    - CLAUDE.md §Git (Conventional Commits, `SKIP=trufflehog` worktree caveat, no `--no-verify`, no `main`-branch commits)
  </read_first>
  <action>
    Stage exactly `docs/output-catalog.md` via `git add -- docs/output-catalog.md`. Detect worktree context (`git rev-parse --show-toplevel | grep -q '/.worktrees/'`); if true, prefix commit with `SKIP=trufflehog` and additionally run `pre-commit run trufflehog --all-files` separately. Commit message in Conventional Commits form: title `docs(17): rewrite output catalog to v2.0 always-marketplace-header form` (within 5-72 chars). Body must cite SNM-20 + D-17-04 + D-17-09 + D-17-10 + D-16-04 + Phase 16 D-16-12 reload-hint refinement; note the deliberate `npm run check` red interval until Plan 17-03 lands. Body lines <= 80 chars. Do NOT use `--no-verify`. Verify branch is not `main` before committing.
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -E "^docs\\(17\\)" && git diff HEAD~1 --name-only | grep -q "^docs/output-catalog.md$" && [ "$(git rev-parse --abbrev-ref HEAD)" != "main" ]</automated>
  </verify>
  <done>
    - HEAD commit title starts with `docs(17):` (Conventional Commits).
    - Commit touches exactly `docs/output-catalog.md` and no other path.
    - Current branch is not `main`.
    - Pre-commit hooks ran without `--no-verify` and passed.
  </done>
</task>

</tasks>

<verification>
After both tasks land:

```bash
# Catalog shape gates (must all pass):
grep -c "<!-- catalog-state:" docs/output-catalog.md                                   # >= 30
! grep -q "Claude plugin import summary" docs/output-catalog.md                         # absent
! grep -q "<!-- catalog-state: install-failure-with-anchor -->" docs/output-catalog.md  # absent
! grep -q "<!-- catalog-state: source-mismatch -->" docs/output-catalog.md              # absent
! grep -q "Fix the underlying issue and retry" docs/output-catalog.md                   # absent
! grep -q "Existing marketplace source" docs/output-catalog.md                          # absent
! grep -q "^## Resolutions to apply to" docs/output-catalog.md                          # absent

# Section presence:
for cmd in "list" "install" "uninstall" "reinstall" "update" "import" "bootstrap" \
           "marketplace list" "marketplace add" "marketplace remove" \
           "marketplace update" "marketplace autoupdate"; do
  grep -q "^## \\\`/claude:plugin $cmd" docs/output-catalog.md
done
grep -q "^## Manual recovery anchors" docs/output-catalog.md
grep -q "^## Empty / no-op surfaces" docs/output-catalog.md
grep -q "^## Usage errors" docs/output-catalog.md

# Static checks pass (test suite EXPECTED to fail catalog UAT until Plan 17-03 lands):
npm run typecheck
npm run lint
npx prettier --check docs/output-catalog.md
```
</verification>

<success_criteria>
- v2.0 catalog drives the user-contract; every fenced output block matches what `notify()` emits for a corresponding structured fixture.
- All 12 `/claude:plugin` H2 sections + Manual recovery + Empty + Usage sections present.
- V1-only states dropped per D-17-09 + D-17-10; v1→v2 changes documented in Conventions.
- Single-plugin command sections render two-line marketplace-header form.
- Typecheck + lint + format-check GREEN on the catalog file.
- One atomic commit on a non-main branch; Conventional Commits + 80-char body + pre-commit hooks honored.
- `npm run check` is allowed to be RED at this plan's boundary (the still-V1 catalog UAT byte-mismatches the v2 catalog); Plan 17-03 restores GREEN.
</success_criteria>

<output>
Create `.planning/phases/17-spec-rewrite-catalog-uat-migration/17-02-SUMMARY.md` recording the v2.0 line count, the state-marker count, which v1 states were dropped and why (cite D-17-09 / D-17-10), and the explicit acknowledgement that `npm run check` is expected to be RED on catalog UAT byte-equality until Plan 17-03 lands.
</output>
