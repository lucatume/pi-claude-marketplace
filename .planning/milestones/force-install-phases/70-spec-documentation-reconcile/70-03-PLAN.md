---
phase: 70-spec-documentation-reconcile
plan: 03
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - docs/output-catalog.md
  - docs/messaging-style-guide.md
autonomous: true
requirements: [DOC-02, DOC-03]
must_haves:
  truths:
    - "The `FORCE_INSTALL_HINT_TRAILER` placeholder framing is dropped; the comments state the byte form is the frozen DOC contract (D-70-01)."
    - "The exact `--force` hint byte form is locked into output-catalog.md and messaging-style-guide.md."
    - "No comment claims idempotent/benign autoupdate is `warning`; such cases are documented as info/benign (DOC-03)."
    - "The auditable `outcomeToCascadePluginMessage` comment is preserved (WR-01 rationale kept) and scrubbed of any stale `warning` claim (D-70-03)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "Frozen (not placeholder) FORCE_INSTALL_HINT_TRAILER framing"
      contains: "FORCE_INSTALL_HINT_TRAILER"
  key_links:
    - from: "extensions/pi-claude-marketplace/shared/notify.ts"
      to: "docs/output-catalog.md"
      via: "frozen --force hint byte form"
      pattern: "Re-run with --force to install the supported components"
---

<objective>
Freeze the `--force` hint trailer (D-70-01) and sweep stale "autoupdate is
warning" comments (DOC-03), while preserving and scrubbing the auditable WR-01
autoupdate comment (D-70-03).

Purpose: The hint wording is final -- drop the "placeholder" framing around
`FORCE_INSTALL_HINT_TRAILER` and lock the exact byte form into the docs so the
contract is unambiguous. Separately, remove any inline comment that mislabels a
benign/idempotent autoupdate no-op as a `warning` (the runtime already renders
those at info), and scrub the `outcomeToCascadePluginMessage` comment of stale
warning-claims while KEEPING its WR-01 scoping rationale.
Output: comment + doc edits only. This phase creates NO new symbols, NO new
tokens, and changes NO byte form except adding the (already-shipped) trailer
string into the docs.
</objective>

<artifacts_this_phase_produces>
- Comment-only edits in notify.ts / autoupdate.ts / update.ts (no behavior change).
- Doc additions: the exact `--force` hint string locked in output-catalog.md +
  messaging-style-guide.md, and the output-catalog `failure-unsupported-features`
  prose updated to say the wording is frozen (not "reconciled in the DOC pass").
- No new symbols, no new tokens, no closed-set count change (22/17/7 unchanged).
</artifacts_this_phase_produces>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/70-spec-documentation-reconcile/70-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the `--force` hint trailer and lock its byte form in docs (D-70-01)</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, docs/output-catalog.md, docs/messaging-style-guide.md</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (the `FORCE_INSTALL_HINT_TRAILER` definition + comment ~2164-2167 -- "Placeholder wording; ..."; and the render site ~3352-3355 -- "Wording here is a clear placeholder; ...")
    - docs/output-catalog.md (the `failure-unsupported-features` block ~432-442; the prose at ~442 ends "the byte-exact wording is reconciled in the DOC pass (DOC-01..03)")
    - docs/messaging-style-guide.md (the trailers / severity sections where the frozen hint string belongs)
    - .planning/phases/70-spec-documentation-reconcile/70-CONTEXT.md (D-70-01)
  </read_first>
  <action>
    Per D-70-01, keep the trailer byte form EXACTLY `Re-run with --force to install
    the supported components.` (NO wording change). Replace the placeholder framing in
    the two notify.ts comments (the `FORCE_INSTALL_HINT_TRAILER` definition comment
    ~2164 and the render-site comment ~3352): drop "Placeholder wording" /
    "clear placeholder" and instead state the byte form is FROZEN as the reconciled
    DOC contract (locked in output-catalog.md + messaging-style-guide.md); cite D-70-01.
    Do NOT change the constant's string value. Then lock the byte form into the docs:
    in docs/output-catalog.md, update the `failure-unsupported-features` prose (~442)
    so it states the `--force` hint wording is FROZEN/locked as the DOC contract,
    replacing the "reconciled in the DOC pass (DOC-01..03)" clause; and in
    docs/messaging-style-guide.md, record the exact trailer string
    `Re-run with --force to install the supported components.` in the appropriate
    trailer/legend section (cite D-70-01). Comment policy: D-70-01 anchor, no GSD
    phase/plan references.
  </action>
  <acceptance_criteria>
    - `grep -niE "placeholder" extensions/pi-claude-marketplace/shared/notify.ts | grep -iE "force|trailer|hint"` returns no match (placeholder framing gone around the trailer).
    - `grep -c "Re-run with --force to install the supported components." extensions/pi-claude-marketplace/shared/notify.ts` is unchanged (the constant value is untouched; exactly one definition).
    - `grep -n "Re-run with --force to install the supported components." docs/messaging-style-guide.md` matches (byte form locked).
    - `grep -nE "reconciled in the DOC pass" docs/output-catalog.md` returns no match.
    - `npx node --test tests/architecture/catalog-uat.test.ts` passes (the rendered trailer byte is unchanged).
  </acceptance_criteria>
  <verify>
    <automated>npx node --test tests/architecture/catalog-uat.test.ts</automated>
  </verify>
  <done>Placeholder framing dropped; trailer byte form frozen and locked in both docs; rendered bytes unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep stale "autoupdate is warning" comments + scrub the WR-01 comment (DOC-03 / D-70-03)</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/shared/notify.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts (the idempotent-flip comments ~13-33 and the `outcomeToCascadePluginMessage` region ~540-565, esp. the benign-idempotent-skip note ~563)
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts (any comment describing autoupdate/idempotent severity; ESPECIALLY the `cascadeSkipSeverity` skip-case comment ~1688-1694 whose line ~1691 reads `benign idempotent skip -> info, actionable skip -> warning; never reloads.` -- this comment is SEMANTICALLY CORRECT but its `idempotent ... warning` clauses sit within ~35 chars, so it spuriously trips the DOC-03 sweep grep and must be rephrased to separate the clauses)
    - extensions/pi-claude-marketplace/shared/notify.ts (the severity ladder + benign-skip comments)
    - .planning/phases/70-spec-documentation-reconcile/70-CONTEXT.md (D-70-03)
  </read_first>
  <action>
    DOC-03 sweep: grep notify.ts, autoupdate.ts, and update.ts for any comment that
    labels an idempotent / benign / no-op autoupdate (or autoupdate skip) as `warning`,
    and correct the wording to info/benign (the runtime already renders these at info
    per the benign-skip severity ladder). Where a comment is already correct (e.g.
    autoupdate.ts ~563 "a benign idempotent autoupdate skip -> info"), leave its
    meaning intact. One exception requires a touch: the `cascadeSkipSeverity` skip-case
    comment in plugin/update.ts (line ~1691, `benign idempotent skip -> info, actionable
    skip -> warning; never reloads.`) is semantically correct but its `idempotent`/`benign`
    and `warning` tokens sit ~35 chars apart on one line, so the DOC-03 sweep grep
    (`idempotent.{0,40}warning` etc.) false-positives on it. Lightly REPHRASE that comment
    -- WITHOUT changing its meaning -- so that on any single line `warning` is more than 40
    chars from `idempotent`/`benign`/`no-op` (wrapping `warning` onto its own continuation
    line is the simplest way). Use wording to the effect of: `benign/idempotent case ->
    info; actionable (targeted) decline -> warning (SEV-04); never reloads.` Keep it
    ASCII-only (use `->`, not unicode arrows) and keep the existing SEV-04 / D-69-02 / DOC-03
    anchors; do NOT alter the `cascadeSkipSeverity(...)` call or any non-comment line. Then
    D-70-03: at `outcomeToCascadePluginMessage` in autoupdate.ts, KEEP the auditable
    comment that records the WR-01 scoping (the missing-soft-dep-companion warning is
    intentionally NOT added to the autoupdate cascade; autoupdate severity is governed
    by SEV-03), and scrub it ONLY of any stale "warning"-claim wording for benign/
    idempotent cases -- preserve the rationale and the WR-01 / SEV-03 anchors. Make NO
    behavior change; comments only. Use WR-01 / SEV-01 / SEV-03 / D-70-03 anchors, no
    GSD phase/plan references.
  </action>
  <acceptance_criteria>
    - `grep -rniE "idempotent.{0,40}warning|benign.{0,40}warning|no-op.{0,40}warning" extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts extensions/pi-claude-marketplace/orchestrators/plugin/update.ts extensions/pi-claude-marketplace/shared/notify.ts` returns no match.
    - The `outcomeToCascadePluginMessage` comment still exists and references the WR-01 / SEV-03 autoupdate scoping (grep for `outcomeToCascadePluginMessage` then confirm the rationale comment is present).
    - No non-comment lines changed in the three files (diff shows comment-only edits): `git diff --stat` touches only comments (reviewer confirms; verified indirectly by the full test suite below being unaffected).
    - `npm run check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>No stale "autoupdate is warning" comment remains; the auditable WR-01 comment is preserved and scrubbed; no behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Comment + doc edits only; no executable behavior or input handling changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-03 | (n/a) | comments + docs | accept | No code path changes; the trailer constant value is untouched. |
| T-70-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `npm run check` exits 0; catalog-UAT GREEN; closed-set 22/17/7 unchanged.
- Placeholder framing removed; trailer byte form locked in both docs.
- No comment mislabels benign/idempotent autoupdate as warning; WR-01 comment preserved.
</verification>

<success_criteria>
- D-70-01 satisfied: hint frozen, placeholder framing dropped, byte form locked in docs.
- DOC-03 satisfied: stale "autoupdate is warning" comments swept.
- D-70-03 (comment half) satisfied: WR-01 comment preserved + scrubbed.
</success_criteria>

<output>
Create `.planning/phases/70-spec-documentation-reconcile/70-03-SUMMARY.md` when done.
</output>
