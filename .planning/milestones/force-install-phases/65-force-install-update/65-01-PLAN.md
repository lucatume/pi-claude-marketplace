---
phase: 65-force-install-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/domain/resolver.ts
  - extensions/pi-claude-marketplace/domain/index.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
  - extensions/pi-claude-marketplace/bridges/agents/types.ts
  - extensions/pi-claude-marketplace/bridges/commands/types.ts
  - extensions/pi-claude-marketplace/bridges/commands/discover.ts
  - extensions/pi-claude-marketplace/bridges/skills/types.ts
  - extensions/pi-claude-marketplace/bridges/skills/discover.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts
  - tests/domain/resolver.types.test.ts
autonomous: true
requirements: [FORCE-01, FORCE-02, FORCE-05]

must_haves:
  truths:
    - "The MaterializablePlugin type names exactly the installable + unsupported arms and excludes unavailable (NFR-7 compile-enforced)."
    - "The shared install/update positional parser recognizes --force and returns a force boolean instead of rejecting it as an unknown flag."
    - "Every shared holder the force path flows through (bridge resolved params, orchestrators/plugin/shared.ts adapters) accepts MaterializablePlugin; bodies unchanged."
  artifacts:
    - path: "extensions/pi-claude-marketplace/domain/resolver.ts"
      provides: "MaterializablePlugin = ResolvedPluginInstallable | ResolvedPluginUnsupported alias"
      contains: "MaterializablePlugin"
    - path: "extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts"
      provides: "--force recognition in parsePositionalsWithFlags + force field on result shapes"
      contains: "force"
    - path: "tests/domain/resolver.types.test.ts"
      provides: "NFR-7 type test that MaterializablePlugin excludes unavailable"
      contains: "MaterializablePlugin"
  key_links:
    - from: "extensions/pi-claude-marketplace/domain/index.ts"
      to: "extensions/pi-claude-marketplace/domain/resolver.ts"
      via: "re-export MaterializablePlugin"
      pattern: "MaterializablePlugin"
    - from: "extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts"
      to: "parseMapModelArgs callers"
      via: "force field on ParsedMapModelArgs"
      pattern: "force"
---

<objective>
Lay the compile-coupled foundation that both the install and update force paths
build on: introduce the `MaterializablePlugin` union alias (the NFR-7-safe widened
type), widen every SHARED holder the force path flows through (the five bridge
`resolved` params and the two `orchestrators/plugin/shared.ts` adapters), and teach
the shared edge positional parser to recognize `--force`.

This plan delivers no command behavior on its own — it is the interface layer the
two command plans (65-02 install, 65-03 update) consume. It compiles GREEN
independently because a narrow `ResolvedPluginInstallable` is assignable to the
widened `MaterializablePlugin` union, and the new `force` parse field is additive.

Purpose: Establish one canonical force-materializable type (D-65-03/05, FORCE-05
type half) and one canonical `--force` parse seam (D-65-05, FORCE-01/02 parse half)
so the install and update plans can run in parallel without touching shared files.
Output: `MaterializablePlugin` alias + re-export, widened bridge/adapter params,
`--force` recognition in the shared parser, and the NFR-7 type test.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-force-install-update/65-CONTEXT.md
@.planning/phases/65-force-install-update/65-RESEARCH.md
@.planning/phases/65-force-install-update/65-PATTERNS.md
@.planning/phases/65-force-install-update/65-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MaterializablePlugin alias and widen the shared bridge/adapter holders</name>
  <files>
    extensions/pi-claude-marketplace/domain/resolver.ts,
    extensions/pi-claude-marketplace/domain/index.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts,
    extensions/pi-claude-marketplace/bridges/agents/types.ts,
    extensions/pi-claude-marketplace/bridges/commands/types.ts,
    extensions/pi-claude-marketplace/bridges/commands/discover.ts,
    extensions/pi-claude-marketplace/bridges/skills/types.ts,
    extensions/pi-claude-marketplace/bridges/skills/discover.ts,
    tests/domain/resolver.types.test.ts
  </files>
  <read_first>
    extensions/pi-claude-marketplace/domain/resolver.ts (lines 60-128 for the three arm schemas + type-alias exports; lines 1076-1115 for both gates),
    extensions/pi-claude-marketplace/domain/index.ts (lines 28-42 for the resolver re-export block),
    extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts (lines 485-525 for resolvePluginVersion + pickAgentsSourceDir, confirm they read only .pluginRoot / .componentPaths, never .state),
    extensions/pi-claude-marketplace/bridges/agents/types.ts (line 72), bridges/commands/types.ts (line 39), bridges/commands/discover.ts (line 69), bridges/skills/types.ts (line 33), bridges/skills/discover.ts (line 91) — each is a single `resolved: ResolvedPluginInstallable` param,
    tests/domain/resolver.types.test.ts (the existing Phase 64 type test asserting pluginRoot on installable+unsupported and @ts-expect-error on unavailable),
    .claude/rules/typescript-comments.md (comment/test-title ID policy — cite D-65-NN / FORCE-NN / NFR-N, NEVER GSD phase/plan)
  </read_first>
  <action>
    In `domain/resolver.ts`, immediately after the per-arm type-alias exports at
    resolver.ts:125-128, export a new alias
    `MaterializablePlugin = ResolvedPluginInstallable | ResolvedPluginUnsupported`.
    Anchor its doc comment to NFR-7: it names the two force-materializable arms and
    EXCLUDES `unavailable` so no consumer can read `pluginRoot` off a
    structurally-broken plugin. Re-export `MaterializablePlugin` from
    `domain/index.ts` alongside the existing `ResolvedPlugin*` type re-exports
    (index.ts:30-35).

    Widen the SHARED holders the force path flows through from
    `ResolvedPluginInstallable` to `MaterializablePlugin`, bodies unchanged (none
    read `.state`): `resolvePluginVersion`'s `installable` param
    (orchestrators/plugin/shared.ts:491), `pickAgentsSourceDir`'s `installable`
    param (orchestrators/plugin/shared.ts:518), and the five bridge `resolved`
    params (bridges/agents/types.ts:72, bridges/commands/types.ts:39,
    bridges/commands/discover.ts:69, bridges/skills/types.ts:33,
    bridges/skills/discover.ts:91). Add `MaterializablePlugin` to each file's
    existing `domain/resolver.ts` (or `domain/index.ts`) type import. Do NOT widen
    `InstallCtx.resolved` or `PluginPreflight.installable` here — those belong to
    65-02 and 65-03.

    Extend `tests/domain/resolver.types.test.ts` with an NFR-7 assertion that
    `MaterializablePlugin` admits `installable` and `unsupported` arms but a value
    typed `MaterializablePlugin` cannot be the `unavailable` arm (use the existing
    `@ts-expect-error` idiom already in the file). Title the test with NFR-7 /
    FORCE-05, never a phase/plan reference.

    This plan compiles GREEN on its own: every current caller still passes a narrow
    `ResolvedPluginInstallable`, which is assignable to `MaterializablePlugin`.
  </action>
  <acceptance_criteria>
    - `grep -n "export type MaterializablePlugin" extensions/pi-claude-marketplace/domain/resolver.ts` shows the alias as the union of `ResolvedPluginInstallable | ResolvedPluginUnsupported` (no `ResolvedPluginUnavailable`).
    - `grep -n "MaterializablePlugin" extensions/pi-claude-marketplace/domain/index.ts` shows the re-export.
    - All five bridge param sites and both `orchestrators/plugin/shared.ts` adapter params read `MaterializablePlugin` (no remaining `resolved: ResolvedPluginInstallable` / `installable: ResolvedPluginInstallable` in those seven sites).
    - `tests/domain/resolver.types.test.ts` contains a `@ts-expect-error`-guarded assertion that `MaterializablePlugin` excludes the `unavailable` arm, titled with NFR-7/FORCE-05.
    - No comment or test title in the diff cites a GSD phase/plan/wave/task.
  </acceptance_criteria>
  <verify>
    <automated>cd extensions/pi-claude-marketplace && npm run typecheck && node --test ../../tests/domain/resolver.types.test.ts ../../tests/domain/resolver-strict.test.ts</automated>
  </verify>
  <done>`MaterializablePlugin` exists + is re-exported, the seven shared holders accept the union, the NFR-7 type test passes, and typecheck is green with no behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: Teach the shared edge positional parser to recognize --force</name>
  <files>
    extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts
  </files>
  <read_first>
    extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts (lines 38-112: ParsedPositionalsResult at 38-41, parsePositionalsWithFlags scanner at 49-67 incl. the `--map-model` arm and the `Unknown flag` rejection at 60, ParsedMapModelArgs at 70-74, parseMapModelArgs aggregator at 87-111),
    extensions/pi-claude-marketplace/edge/handlers/plugin/reinstall.ts (the `--force` token loop pattern being mirrored, the established analog),
    65-PATTERNS.md (the "load-bearing parse seam" section — the exact `--force` arm placement BEFORE the unknown-flag rejection)
  </read_first>
  <action>
    In `edge/handlers/plugin/shared.ts`, add a `readonly force: boolean` field to
    BOTH `ParsedPositionalsResult` (shared.ts:38-41) and `ParsedMapModelArgs`
    (shared.ts:70-74), mirroring the existing `mapModel` field exactly.

    In `parsePositionalsWithFlags` (shared.ts:49-67), initialize `let force = false`
    alongside `mapModel`, and add an `else if (token === "--force") { force = true; }`
    arm BEFORE the `token.startsWith("--")` unknown-flag rejection at shared.ts:60 —
    otherwise `--force` falls through to `Unknown flag: "--force".` (the Pitfall-2
    trap: install/update route through this shared scanner, NOT reinstall's inline
    loop, so the allow-list alone is insufficient). Return `force` in the result
    object. Thread `force: flagged.force` through the `parseMapModelArgs` aggregator
    return (shared.ts:107-111). Comment the new arm with D-65-05.

    Do NOT change the install/update handlers, USAGE strings, or orchestrators here
    — those are 65-02 / 65-03. After this task, existing handler tests that destructure
    `{ nonFlagPositionals, mapModel }` still compile (a new returned field does not
    break existing destructures), and an `install --force` token will parse (rather
    than error) once the handlers consume it.
  </action>
  <acceptance_criteria>
    - `grep -n "force" extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts` shows `force` on both `ParsedPositionalsResult` and `ParsedMapModelArgs`, a `token === "--force"` arm in the scanner, and `force:` in the aggregator return.
    - The `--force` arm sits BEFORE the `token.startsWith("--")` unknown-flag rejection (verify by reading: the `--force` branch precedes line ~60).
    - The new scanner arm carries a `D-65-05` comment.
    - Existing edge handler suites for install and update still pass unchanged (no destructure breakage).
  </acceptance_criteria>
  <verify>
    <automated>cd extensions/pi-claude-marketplace && npm run typecheck && node --test ../../tests/edge/handlers/plugin/install.test.ts ../../tests/edge/handlers/plugin/update.test.ts</automated>
  </verify>
  <done>`parsePositionalsWithFlags` recognizes `--force` and returns a `force` boolean on both result shapes; existing handler tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> edge parser | User-supplied `--force` token crosses into the parse seam (untrusted string). |
| resolver -> materialize holders | A resolved plugin arm crosses into bridge/adapter holders that read `pluginRoot`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Tampering | `MaterializablePlugin` widening (bridge/adapter holders) | mitigate | The alias is `installable \| unsupported` only; it EXCLUDES `unavailable`, so no widened holder can read `pluginRoot` off a structurally-broken plugin (NFR-7 compile-enforced; asserted by the resolver.types test). |
| T-65-02 | Elevation of Privilege | `--force` token in `parsePositionalsWithFlags` | mitigate | `--force` is a fixed boolean token; the scanner still rejects every OTHER unknown `--` flag via the unchanged `notifyUsageError` arm. No free-form input is admitted. |
| T-65-03 | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase (pure source change); RESEARCH Package Legitimacy Audit = N/A. |
</threat_model>

<verification>
- `npm run typecheck` green (the union widening is compile-coupled).
- `node --test tests/domain/resolver.types.test.ts` green (NFR-7 boundary holds).
- `node --test tests/edge/handlers/plugin/{install,update}.test.ts` green (no parse regression).
- No GSD phase/plan references in any added comment or test title.
</verification>

<success_criteria>
- `MaterializablePlugin = ResolvedPluginInstallable | ResolvedPluginUnsupported` exists in `domain/resolver.ts` and is re-exported from `domain/index.ts`.
- The five bridge `resolved` params and the two `orchestrators/plugin/shared.ts` adapter params accept `MaterializablePlugin`.
- `parsePositionalsWithFlags` recognizes `--force` and returns `force` on `ParsedPositionalsResult` + `ParsedMapModelArgs`.
- The resolver type test asserts `MaterializablePlugin` excludes `unavailable` (FORCE-05 type half).
- Whole repo `npm run check` would remain green (no behavior change).
</success_criteria>

<output>
Create `.planning/phases/65-force-install-update/65-01-SUMMARY.md` when done.
</output>
