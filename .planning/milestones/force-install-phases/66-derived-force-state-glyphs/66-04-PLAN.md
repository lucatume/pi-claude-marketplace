---
phase: 66-derived-force-state-glyphs
plan: 04
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
  - tests/orchestrators/reconcile/notify.test.ts
  - tests/orchestrators/reconcile/pending.test.ts
autonomous: true
requirements: [FSTAT-06]
must_haves:
  truths:
    - "the pending/preview surface renders (will force install) in place of (will install) when the planned install candidate resolves unsupported (FSTAT-06, D-66-04)"
    - "the candidate is resolved with no network via resolveStrict (FSTAT-06, NFR-5)"
    - "no will-force-update row/token is emitted — the reconcile plan has no update bucket; the absence is asserted (D-66-05)"
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts"
      provides: "force: true modifier on the will-install pending row when the planned install would degrade"
      contains: "force"
  key_links:
    - from: "orchestrators/reconcile/notify.ts pending projection"
      to: "domain/resolver.ts resolveStrict (no network)"
      via: "planned-install candidate state -> force modifier"
      pattern: "resolveStrict"
    - from: "orchestrators/reconcile/notify.ts will-install row"
      to: "shared/notify.ts PluginWillInstallMessage.force"
      via: "force render modifier (will force install)"
      pattern: "force: true"
---

<objective>
Render the pending/preview `will force install` token (FSTAT-06 / D-66-04): in the reconcile
pending projection, when a planned `pluginsToInstall` entry's install candidate resolves
`unsupported` (no-network `resolveStrict`), set `force: true` on the pushed `will install`
row so it renders `(will force install)` via the modifier added in 66-01.

`will force update` is VACUOUS — the reconcile `ReconcilePlan` has no update bucket
(confirmed: pending projection only pushes install/uninstall/enable/disable). Implement ONLY
`will force install` and assert the absence of any update/force-update row (D-66-05).

Purpose: The pending surface must preview a force operation truthfully, consistent with the
list/info/success surfaces, using the same no-network candidate signal.
Output: force modifier on the will-install pending row + unit coverage proving the token and
the will-force-update vacuity.

Depends on 66-01 for `PluginWillInstallMessage.force` and the will-install render branch.
Runs in Wave 2 parallel with 66-02 (list.ts) and 66-03 (info/install/update) — disjoint files.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-derived-force-state-glyphs/66-CONTEXT.md
@.planning/phases/66-derived-force-state-glyphs/66-RESEARCH.md
@.planning/phases/66-derived-force-state-glyphs/66-PATTERNS.md
@.planning/phases/66-derived-force-state-glyphs/66-VALIDATION.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: will force install pending modifier via no-network candidate resolve</name>
  <files>extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts</files>
  <behavior>
    - a planned pluginsToInstall entry whose candidate resolveStrict yields state === "unsupported" -> pushed will-install row carries force: true (renders "(will force install)").
    - a planned entry whose candidate resolves installable -> will-install row without force (renders "(will install)").
    - the candidate resolve uses no network (resolveStrict).
    - no update/force-update row is pushed (reconcile has no update bucket).
  </behavior>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts — the pending projection (~248-335) and the `will install` row push over `plan.pluginsToInstall` (~269-275); how the loop accesses scope/marketplace and whether a marketplaceRoot/manifest entry is available for a no-network resolve in this loop.
    - extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts — `ReconcilePlan` buckets (~203-216, 7 buckets, NONE for updates) and `PlannedPluginInstall` (~79-84: scope/plugin/marketplace/configSource).
    - extensions/pi-claude-marketplace/domain/resolver.ts — `resolveStrict` (~948) `(entry, { marketplaceRoot }) => Promise<ResolvedPlugin>`; the no-network resolver.
    - extensions/pi-claude-marketplace/shared/notify.ts — `PluginWillInstallMessage.force` (added in 66-01) and the `case "will install"` force branch.
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts — the analogous no-network candidate resolve pattern for cross-surface consistency (reference only; do not modify).
    - .claude/rules/typescript-comments.md — D-66-NN / FSTAT-NN / NFR-N IDs only.
  </read_first>
  <action>
    In the pending projection's `pluginsToInstall` loop, resolve the planned install candidate's manifest entry no-network via `resolveStrict(entry, { marketplaceRoot })` (mirror the list deriver's resolve; resolve the marketplaceRoot + manifest entry the same way the surrounding reconcile code already locates them). When the candidate `state === "unsupported"`, set `force: true` on the pushed `{ status: "will install", ... }` row; otherwise leave force unset. Do NOT add any update or `will force update` row — the plan has no update bucket; add a brief D-66-05 comment documenting the vacuity at the loop. This is PREVIEW-only token derivation (RESEARCH Open Question 2): do NOT change reconcile-apply behavior or whether reconcile auto-forces. Comments use D-66-NN / FSTAT-NN IDs only.
  </action>
  <acceptance_criteria>
    - the will-install row carries `force: true` exactly when the planned candidate resolveStrict returns `state === "unsupported"`.
    - the candidate resolve uses `resolveStrict` (no network); `node --test "tests/architecture/no-orchestrator-network.test.ts"` stays green.
    - no update/force-update row is constructed in the pending projection.
    - reconcile-apply logic is unchanged (only the pending/preview projection is touched).
    - `npm run typecheck` passes; no GSD phase/plan/wave references in comments.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && npm run typecheck && node --test "tests/architecture/no-orchestrator-network.test.ts"</automated>
  </verify>
  <done>The pending will-install row sets force:true on a degrading candidate via no-network resolveStrict; no will-force-update row; apply path untouched; no-network test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reconcile pending will-force-install unit coverage + update vacuity</name>
  <files>tests/orchestrators/reconcile/notify.test.ts, tests/orchestrators/reconcile/pending.test.ts</files>
  <behavior>
    - planned install whose candidate resolves unsupported -> pending row renders "(will force install)".
    - planned install whose candidate resolves installable -> pending row renders "(will install)".
    - the reconcile pending projection never emits an update or "will force update" row.
  </behavior>
  <read_first>
    - tests/orchestrators/reconcile/notify.test.ts and tests/orchestrators/reconcile/pending.test.ts — existing pending-projection cases, how a `pluginsToInstall` plan + candidate manifest are staged, and the rendered-row assertion style.
    - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts — the force modifier implemented in Task 1.
  </read_first>
  <action>
    Add cases asserting the `(will force install)` row for a planned install whose staged candidate resolves unsupported, the plain `(will install)` row for an installable candidate, and an explicit assertion that no update / `will force update` row is ever produced by the pending projection (FSTAT-06 vacuity, D-66-05). Test titles use FSTAT-NN / D-66-NN IDs only — never GSD references.
  </action>
  <acceptance_criteria>
    - `node --test "tests/orchestrators/reconcile/notify.test.ts" "tests/orchestrators/reconcile/pending.test.ts"` green.
    - a case asserts the `(will force install)` rendered row for an unsupported candidate.
    - a case asserts no update/`will force update` row is emitted.
    - test titles contain no GSD phase/plan/wave references.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && node --test "tests/orchestrators/reconcile/notify.test.ts" "tests/orchestrators/reconcile/pending.test.ts"</automated>
  </verify>
  <done>Pending projection tests prove the will-force-install token on a degrading candidate and the will-force-update vacuity; reconcile tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The pending projection reads already-validated plan + resolver output; it adds no new external input or boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-06 | Tampering (network policy) | reconcile pending resolveStrict | mitigate | use the no-network `resolveStrict` only; the `no-orchestrator-network` architecture test is asserted green (NFR-5 / FSTAT-06). The pending surface is read-only preview — no apply, no write. |
| T-66-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (pure internal TS + tests); Package Legitimacy Gate not applicable. |

No auth/session/access-control/crypto/input-validation surface is touched (RESEARCH Security Domain). The pending projection performs no disk mutation.
</threat_model>

<verification>
- `npm run typecheck` green.
- `node --test "tests/orchestrators/reconcile/notify.test.ts" "tests/orchestrators/reconcile/pending.test.ts" "tests/architecture/no-orchestrator-network.test.ts"` green.
- Wave-merge gate (after 66-02/03/04): `npm run check` (TEST_CONCURRENCY=1).
</verification>

<success_criteria>
- pending will-install row renders `(will force install)` when the planned candidate resolves unsupported, via the 66-01 force modifier (FSTAT-06).
- candidate resolve is no-network (FSTAT-06 / NFR-5); no-orchestrator-network test green.
- no `will force update` row exists; its vacuity is asserted (D-66-05).
- reconcile-apply behavior unchanged (preview-only).
</success_criteria>

<output>
Create `.planning/phases/66-derived-force-state-glyphs/66-04-SUMMARY.md` when done.
</output>
