---
phase: 65.1-reload-deferred-will-grammar-consistency
plan: 02
type: execute
wave: 2
depends_on: [65.1-01]
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts
  - tests/architecture/notify-closed-set-locks.test.ts
  - tests/architecture/catalog-uat.test.ts
  - tests/architecture/notify-grammar-invariant.test.ts
  - tests/shared/notify-v2.test.ts
  - tests/orchestrators/reconcile/notify.test.ts
  - tests/orchestrators/reconcile/apply.test.ts
  - tests/orchestrators/reconcile/pending.test.ts
  - tests/orchestrators/reconcile/plan.test.ts
  - docs/output-catalog.md
  - docs/messaging-style-guide.md
autonomous: true
requirements: [WILL-01, WILL-02, WILL-03, WILL-04]
must_haves:
  truths:
    - "Marketplace add no longer renders a `will add` token in the pending surface; an add with child installs renders a bare list-arm header whose children carry `will install`; an add with no children produces no pending block."
    - "Marketplace remove of a populated marketplace renders one `will uninstall` child row per recorded plugin under a bare header; the immediate source de-registration carries no `will` token; an empty remove shows nothing pending."
    - "Plugin install / uninstall / enable / disable keep their `will install` / `will uninstall` / `will enable` / `will disable` tokens."
    - "STATUS_TOKENS has 20 entries and MARKETPLACE_STATUSES has 7 entries; PLUGIN_STATUSES stays 15 and REASONS stays 32."
    - "The pending will-grammar agrees with the reload-hint oracle (WILL-02 agreement test green)."
    - "output-catalog.md and messaging-style-guide.md reflect the retired token set and `npm run check` is green."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "Closed-set tuples, union, and renderMpHeader with the two marketplace will-tokens retired"
      contains: "MARKETPLACE_STATUSES"
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts"
      provides: "Pending projection: marketplacesToAdd loop deleted, marketplacesToRemove loop emits per-plugin will-uninstall child rows"
      contains: "marketplacesToRemove"
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts"
      provides: "PlannedMarketplaceRemove.plugins recorded-plugin-name field"
      contains: "PlannedMarketplaceRemove"
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts"
      provides: "diffMarketplaces populates PlannedMarketplaceRemove.plugins from recorded state"
      contains: "remove.push"
    - path: "docs/output-catalog.md"
      provides: "Reconciled token legend + marketplace-remove-cascade catalog state"
      contains: "will uninstall"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts diffMarketplaces"
      to: "extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts PlannedMarketplaceRemove.plugins"
      via: "remove.push populates plugins from state.marketplaces[mp].plugins"
      pattern: "remove\\.push"
    - from: "extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts buildReconcilePendingNotification"
      to: "PlannedMarketplaceRemove.plugins"
      via: "per-recorded-plugin will-uninstall child push (NOT via pluginsToUninstall)"
      pattern: "will uninstall"
    - from: "tests/architecture/notify-will-reload-agreement.test.ts"
      to: "the retired pending will-add/will-remove tokens"
      via: "WILL-02 agreement now holds on the pending surface, not just realized cascades"
      pattern: "WILL-02"
---

<objective>
Retire the two marketplace-level pending tokens `will add` and `will remove` so the
`will` prefix means EXACTLY "deferred to the next /reload" (WILL-01). Marketplace add
is immediate (drop `will add`, D-65.1-02/03); marketplace remove is immediate for the
source de-registration and reload-deferred only for its plugin-uninstall cascade,
which now renders as per-plugin `will uninstall` rows under a bare header (WILL-03).
Plugin install/uninstall/enable/disable keep their tokens. The whole change — closed-set
tuples, type union, render arms, projection, planner DTO, every coupled test, and both
prose docs — lands as ONE atomic, green commit (D-65.1-04; npm run check must never be
left red at a commit boundary).

Purpose: align the pending grammar to the single reload-deferral oracle
`shouldEmitReloadHint` (D-65.1-01) before Phase 66 stacks `will force *` tokens on it.
This phase only REMOVES vocabulary; the sole new artifact (the WILL-02 agreement test)
already landed in Wave 1 (65.1-01).

Output: 20-entry STATUS_TOKENS, 7-entry MARKETPLACE_STATUSES, a per-plugin remove
cascade in pending, and reconciled docs — all green under npm run check.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-CONTEXT.md
@.planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-RESEARCH.md
@.planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-PATTERNS.md
@.planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-VALIDATION.md
</context>

<atomicity_note>
This plan is ONE atomic byte-contract change (D-65.1-04 / Pitfall 2). The entire
retirement is a single typecheck/test unit: deleting the union arms makes `assertNever`
in `renderMpHeader` and the `ReconcileBlockStatus` Extract the compile-time tripwires,
and the runtime length tripwires plus catalog-uat fixtures are the test tripwires.
Make ALL edits across Tasks 1-3 FIRST, then run the green gate, then make a SINGLE
commit. Do NOT commit source without its coupled test/doc edits — an intermediate
commit would leave `npm run check` red, violating NFR-6 and the atomic-supersession
lesson. Per-task `<verify>` commands are diagnostic checkpoints; full green is asserted
only at the Task 3 gate once every edit is in place.

Comment/test-title policy: every comment and test title uses D-65.1-NN / WILL-NN /
NFR-N / SNM-NN / DIFF-NN IDs, NEVER a GSD phase/plan reference. ASCII only (no em
dashes) in code, comments, and the commit message.
</atomicity_note>

<tasks>

<task type="auto">
  <name>Task 1: Retire the marketplace will-tokens in source (closed set, union, render arms, projection, planner DTO)</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (STATUS_TOKENS ~198-221, MARKETPLACE_STATUSES ~401-411, MpWillAdd/MpWillRemove ~886-902, MarketplaceNotificationMessage union ~925-935, renderMpHeader ~1432-1513, shouldEmitReloadHint ~2465-2521)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts (ReconcileBlockStatus Extract ~64-67, blockToMarketplaceMessage ~111-132, buildReconcilePendingNotification ~208-275, header comment blocks ~14-24/99-110/184-202)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts (PlannedMarketplaceRemove ~62-66, PlannedMarketplaceAdd ~45-60 for field-doc style)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts (diffMarketplaces ~204-219, remove.push ~215, buildUninstallBucket skip ~359-387 — Pitfall 1)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/reconcile.messaging.ts (renderWillUninstall ~87-88 and PENDING_CONTEXT ~111-120 — VERIFY ONLY, do not edit)
    - .planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-PATTERNS.md (sections for notify.ts tuples/types/renderMpHeader, reconcile/notify.ts projection, types.ts, plan.ts, and the "No analog / no new vocabulary" notes)
    - .planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-RESEARCH.md (Research Q3 closed-set impact, Research Q4 render path, Pitfall 1)
  </read_first>
  <action>
    In shared/notify.ts: delete the `"will add"` and `"will remove"` entries from
    STATUS_TOKENS (22 entries become 20) and from MARKETPLACE_STATUSES (9 become 7),
    leaving the surviving `will install`/`will uninstall`/`will enable`/`will disable`
    entries byte-identical; delete the `MpWillAdd` and `MpWillRemove` interfaces and
    their two arms in the `MarketplaceNotificationMessage` union; delete the
    `case "will add":` and `case "will remove":` arms in `renderMpHeader` (the surviving
    `case undefined:` bare-header arm and `renderWillUninstall` cover the new rendering
    with no new arm). Update the MARKETPLACE_STATUSES JSDoc that calls the two trailing
    entries the DIFF-02 pending tokens.

    In orchestrators/reconcile/notify.ts: DELETE the `marketplacesToAdd` projection loop
    entirely (add is immediate per D-65.1-02; child installs still build a bare-header
    block via the surviving `pluginsToInstall` loop). REPLACE the `marketplacesToRemove`
    loop so that, per removed marketplace, it pushes one `{ status: "will uninstall",
    name }` child row into `block.plugins` for each name in the new `o.plugins` field
    (mirror the surviving `pluginsToUninstall` loop), leaving `block.status` undefined so
    it renders the bare list-arm header. Narrow the `ReconcileBlockStatus` Extract to
    `"added" | "removed" | "failed"` and delete the `case "will add":`/`case "will remove":`
    arms in `blockToMarketplaceMessage`. Rewrite the three header JSDoc comment blocks so
    they describe: marketplacesToAdd -> dropped (immediate); marketplacesToRemove ->
    per-recorded-plugin `will uninstall` child rows under a bare header (WILL-03 / D-65.1-03).

    In orchestrators/reconcile/types.ts: add `readonly plugins: readonly string[]` to
    `PlannedMarketplaceRemove` with a JSDoc noting these are the recorded plugin names
    (`state.marketplaces[mp].plugins`), synthesized into `will uninstall` rows in the
    PENDING path only, and deliberately NOT routed through `pluginsToUninstall` (the apply
    path cascades internally; enumerating them would double-bill — Pitfall 1 / D-65.1-03).

    In orchestrators/reconcile/plan.ts: at the existing `remove.push(...)` site in
    `diffMarketplaces`, populate `plugins` from the recorded marketplace record's plugin
    names already in scope; do NOT change `buildUninstallBucket` (it must keep skipping
    removed-marketplace plugins to avoid double-billing the apply cascade) — preserving the
    reconcile-planner-purity contract.

    Do NOT edit reconcile.messaging.ts: confirm `renderWillUninstall` and the
    `PENDING_CONTEXT` `will uninstall` arm already exist; invent no new marketplace-cascade
    token. Optionally correct the stale add.ts:31-34 comment only if you open that file.
  </action>
  <acceptance_criteria>
    - `grep -c '"will add"\|"will remove"' extensions/pi-claude-marketplace/shared/notify.ts` returns 0.
    - shared/notify.ts no longer declares `MpWillAdd` or `MpWillRemove` and the `MarketplaceNotificationMessage` union has no `MpWillAdd`/`MpWillRemove` arm.
    - `renderMpHeader` has no `case "will add":` or `case "will remove":` arm.
    - orchestrators/reconcile/notify.ts has no `marketplacesToAdd` projection loop and its `marketplacesToRemove` loop pushes `{ status: "will uninstall", name }` rows from `o.plugins`; `ReconcileBlockStatus` Extract is exactly `"added" | "removed" | "failed"`.
    - `PlannedMarketplaceRemove` declares `readonly plugins: readonly string[]` and `diffMarketplaces` populates it at the `remove.push` site; `buildUninstallBucket` is unchanged.
    - reconcile.messaging.ts is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -p extensions/pi-claude-marketplace --noEmit 2>&1 | grep -v "will add\|will remove" | grep -c "error TS" || true</automated>
  </verify>
  <done>The two marketplace will-tokens are removed from the closed set, union, render arms, and projection; the recorded-plugin-name seam exists; reconcile.messaging.ts untouched. Test/doc typecheck failures are expected here and are resolved in Tasks 2-3 before the green gate.</done>
</task>

<task type="auto">
  <name>Task 2: Lockstep test updates (tripwires, byte fixtures, grammar invariant, projection/render/orchestrator tests)</name>
  <files>tests/architecture/notify-closed-set-locks.test.ts, tests/architecture/catalog-uat.test.ts, tests/architecture/notify-grammar-invariant.test.ts, tests/shared/notify-v2.test.ts, tests/orchestrators/reconcile/notify.test.ts, tests/orchestrators/reconcile/apply.test.ts, tests/orchestrators/reconcile/pending.test.ts, tests/orchestrators/reconcile/plan.test.ts</files>
  <read_first>
    - tests/architecture/notify-closed-set-locks.test.ts (length assertions ~30-43; the untouched PLUGIN_STATUSES/REASONS assertions as the shape template)
    - tests/architecture/catalog-uat.test.ts (mp-add-plugin-install fixture ~2905-2917; the Wave 1 marketplace-remove-with-installed-plugins state added in 65.1-01 — leave it intact)
    - tests/architecture/notify-grammar-invariant.test.ts (will add/will remove marketplace-header fixtures ~170-184, WILL_TOKEN_RE ~230-231, header comment ~161-168)
    - tests/shared/notify-v2.test.ts (marketplace will-add/will-remove renderMpHeader assertions ~3986-4111)
    - tests/orchestrators/reconcile/notify.test.ts (projection assertions ~57, ~120, ~128-138)
    - tests/orchestrators/reconcile/apply.test.ts (ReconcileBlockStatus Extract regex ~1132)
    - tests/orchestrators/reconcile/pending.test.ts (local-only marketplace `(will add)` row expectation ~453-454)
    - tests/orchestrators/reconcile/plan.test.ts (the `assert.deepEqual(plan.marketplacesToRemove[0], { scope, marketplace })` assertion ~190-193; note `import assert from "node:assert/strict"` makes deepEqual strict, so the new `plugins` field on PlannedMarketplaceRemove WILL break it)
    - .planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-PATTERNS.md (the per-test-file sections)
    - .planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-RESEARCH.md (Pitfall 2 full edit-site map)
  </read_first>
  <action>
    notify-closed-set-locks.test.ts: change `STATUS_TOKENS.length` assertion from 22 to
    20 and `MARKETPLACE_STATUSES.length` from 9 to 7, updating BOTH the integer literal AND
    the count word in each test title; leave PLUGIN_STATUSES (15) and REASONS (32) assertions
    byte-identical.

    catalog-uat.test.ts: edit the `mp-add-plugin-install` fixture to DROP its
    `status: "will add"` field and update its expected bytes to the bare list-arm header
    `● new-mp [user]` with the child `will install` row retained. Do not touch the Wave 1
    `marketplace-remove-with-installed-plugins` state.

    notify-grammar-invariant.test.ts: delete the two marketplace-header fixtures (the
    `will add` and `will remove` ones); narrow `WILL_TOKEN_RE` to drop `add|remove` from the
    `(will (?:...))` alternation, leaving `install|uninstall|enable|disable`; update the
    header comment count from "6 ... (will *) tokens" to 4 and drop the marketplace-header
    references.

    notify-v2.test.ts: delete the marketplace will-add/will-remove `renderMpHeader` render
    assertions (the surviving plugin-row assertions stay); if covering a removed marketplace,
    assert the bare `● mp [scope]` header instead.

    reconcile/notify.test.ts: rewrite the projection assertions that expect
    `block.status === "will add"`/`"will remove"`: an added marketplace WITH child installs
    yields `status: undefined` plus `will install` children; an add with NO children yields
    NO block; a removed marketplace yields `status: undefined` plus one `will uninstall`
    child per recorded plugin.

    reconcile/apply.test.ts: update the regex asserting `ReconcileBlockStatus` membership to
    the new 3-member set `"added" | "removed" | "failed"`.

    reconcile/pending.test.ts: rewrite the ~453-454 expectation so a local-only
    (config-declared, unrecorded) marketplace no longer yields a `(will add)` row — expect a
    bare header if it has child installs, or no row if none (D-65.1-02).

    reconcile/plan.test.ts: the `assert.deepEqual(plan.marketplacesToRemove[0], { scope:
    "project", marketplace: "mp" })` assertion at ~190-193 uses strict deepEqual
    (`node:assert/strict`); once Task 1 adds `readonly plugins: readonly string[]` to
    `PlannedMarketplaceRemove`, `diffMarketplaces` populates `plugins` on every returned
    object, so the expected literal must gain `plugins: []` (or the actual recorded plugin
    list when the helper state records plugins). Update the expected object to
    `{ scope: "project", marketplace: "mp", plugins: [] }` (Pitfall 2 — confirmed strict
    deepEqual failure otherwise).

    Use D-65.1-NN / WILL-NN IDs in any new/edited test titles or comments; never a GSD ref.
  </action>
  <acceptance_criteria>
    - notify-closed-set-locks.test.ts asserts `STATUS_TOKENS.length === 20` and `MARKETPLACE_STATUSES.length === 7`; PLUGIN_STATUSES and REASONS assertions are unchanged.
    - catalog-uat.test.ts `mp-add-plugin-install` fixture no longer contains `status: "will add"` and its expected bytes are the bare header form.
    - `grep -c "will add\|will remove" tests/architecture/notify-grammar-invariant.test.ts` returns 0, and WILL_TOKEN_RE alternation is exactly `install|uninstall|enable|disable`.
    - reconcile/apply.test.ts asserts the 3-member `ReconcileBlockStatus` Extract.
    - reconcile/notify.test.ts and reconcile/pending.test.ts no longer assert any marketplace `will add`/`will remove` row.
    - reconcile/plan.test.ts `assert.deepEqual(plan.marketplacesToRemove[0], ...)` expects `{ scope: "project", marketplace: "mp", plugins: [] }` and passes under strict deepEqual.
    - No edited test title or comment contains `65.1`, `Phase N`, or `plan N`.
  </acceptance_criteria>
  <verify>
    <automated>node --test "tests/architecture/notify-closed-set-locks.test.ts" "tests/orchestrators/reconcile/notify.test.ts" "tests/orchestrators/reconcile/apply.test.ts" "tests/orchestrators/reconcile/pending.test.ts" "tests/orchestrators/reconcile/plan.test.ts"</automated>
  </verify>
  <done>All coupled tests assert the retired grammar (20/7 tripwires, bare-header add, per-plugin remove cascade) and cite requirement/decision IDs only.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile prose docs and run the atomic green gate (single commit)</name>
  <files>docs/output-catalog.md, docs/messaging-style-guide.md</files>
  <read_first>
    - docs/output-catalog.md (glyph legend ~9-10, token table rows ~156-157, disabled-row cross-ref ~315, pending intro ~1320, mp-add-plugin-install state ~1332-1341, the plugin-pending-uninstall state ~1343-1352 as the cascade-state template, stale REASONS-29 prose ~1368)
    - docs/messaging-style-guide.md (MarketplaceStatus count ~27, the 9-discriminator sentence + DIFF-02 will-add/will-remove sentence ~58, the untouched PLUGIN_STATUSES sentence ~57 as the phrasing twin)
    - .planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-PATTERNS.md (the docs/output-catalog.md and docs/messaging-style-guide.md sections)
  </read_first>
  <action>
    docs/output-catalog.md: remove `will add`/`will remove` mentions from the glyph legend;
    DELETE both `(will add)` and `(will remove)` token-table rows; drop them from the pending
    intro token list (keep `will install`/`will uninstall`/`will enable`/`will disable`);
    change the `mp-add-plugin-install` catalog state from `● new-mp [user] (will add)` to the
    bare `● new-mp [user]` header (child `will install` row retained) and fix its prose
    sentence; ADD a `marketplace-remove-with-installed-plugins` catalog state copied from the
    plugin-pending-uninstall block (`● old-mp [user]` + `○ p1 (will uninstall)` + `○ p2
    (will uninstall)`) with a prose lead-in stating de-registration is immediate and only the
    plugin-uninstall cascade is reload-deferred (D-65.1-03 / WILL-03); drop the will-add/remove
    mention at the disabled-row cross-reference if present; opportunistically correct the stale
    "REASONS stays at 29 entries" prose to 32 (REASONS itself is out of scope, prose-only fix).

    docs/messaging-style-guide.md: change the `MarketplaceStatus // 9 literal strings` comment
    to 7; change "the closed set of 9 marketplace status discriminators" to 7; REMOVE the
    trailing sentence naming `will add`/`will remove` as the DIFF-02 pending tokens; leave the
    autoupdate-status sentence and the PLUGIN_STATUSES sentence intact.

    Then run the atomic green gate: `npm run check` (typecheck + ESLint + Prettier + tests +
    integration) MUST be green. Run `pre-commit run --files` over every changed file and fix
    until clean. Make a SINGLE commit covering ALL files from Tasks 1-3 plus this task
    (source + tests + docs together) so no intermediate commit leaves the suite red
    (D-65.1-04 atomic supersession). ASCII-only commit message; on a worktree prefix
    `SKIP=trufflehog` and run `pre-commit run trufflehog --all-files` separately first. Never
    commit to main; the active branch is `features/force-install`.
  </action>
  <acceptance_criteria>
    - `grep -c "will add\|will remove" docs/output-catalog.md docs/messaging-style-guide.md` returns 0 across both files.
    - docs/output-catalog.md contains a `marketplace-remove-with-installed-plugins` state with `(will uninstall)` cascade rows under a bare `● old-mp [user]` header.
    - docs/messaging-style-guide.md states 7 marketplace status discriminators (no 9, no will-add/will-remove sentence).
    - `npm run check` exits 0.
    - A single commit contains all 14 changed files; the commit message is ASCII-only and references WILL-01..04 / D-65.1-NN (no GSD phase/plan reference in code/comments).
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Docs reflect the reconciled token set, `npm run check` is green, and the entire retirement landed as one atomic commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure internal output-grammar/test/doc refactor. No new input parsing, network (NFR-5 preserved — pending never touches disk/network), auth, crypto, or file-write surface. The change only RETIRES two status tokens and re-groups an existing cascade. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65.1-02 | Information Disclosure | byte-exact user-visible output (pending/cascade rows) | mitigate | The output-contract invariant (no stray/injected token in user-visible output) is enforced by the catalog-uat byte-equality runner and notify-grammar-invariant regex; retiring `will add`/`will remove` is validated byte-for-byte. Pre-existing `redactAbsolutePaths` / basename-only error subjects are unchanged. |
| T-65.1-03 | Tampering | closed-set token vocabulary | mitigate | `assertNever` in `renderMpHeader`, the `ReconcileBlockStatus` Extract, and the runtime length tripwires (20 / 7) are compile-time + runtime guards that any drift from the frozen set fails the build (NFR-6). |
| T-65.1-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; code-only refactor on the existing stack. No Package Legitimacy Audit required (RESEARCH.md confirms no external dependency introduced). |

No `high`-severity threat: this phase adds and removes no attack surface. The relevant
invariant is output-contract integrity, preserved by the byte-equality tests above.
</threat_model>

<verification>
- `npm run check` green (typecheck + ESLint + Prettier + unit tests + integration).
- Closed-set tripwires: `node --test "tests/architecture/notify-closed-set-locks.test.ts"` asserts 20 / 7.
- Byte contract: `node --test "tests/architecture/catalog-uat.test.ts"` green (bare-header add + remove cascade).
- WILL-02 agreement (from 65.1-01) still green on the post-retirement tree, now also satisfied on the pending surface.
- `grep -rn "will add\|will remove" extensions/pi-claude-marketplace/ tests/ docs/` returns no marketplace-token occurrences.
- Single atomic commit; no intermediate red; ASCII-only message; no GSD reference in code/comments/test titles.
</verification>

<success_criteria>
- WILL-01: every pending `will`-token corresponds to a reload-deferred action; no immediate action renders a bare `will` token.
- WILL-02: the pending will-grammar agrees with the per-command reload-hint discipline (agreement test green on both realized and pending surfaces).
- WILL-03: marketplace add renders no `will add`; marketplace remove renders `will` only for its reload-deferred plugin-uninstall cascade; plugin install/uninstall/enable/disable keep their tokens.
- WILL-04: output-catalog.md, messaging-style-guide.md, the closed set (20 / 7), and the byte-exact catalog/notify tests reflect the reconciled grammar; `npm run check` green.
</success_criteria>

<output>
Create `.planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-02-SUMMARY.md` when done.
</output>
