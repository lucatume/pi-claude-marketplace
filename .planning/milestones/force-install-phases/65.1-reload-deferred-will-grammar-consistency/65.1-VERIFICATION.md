---
phase: 65.1-reload-deferred-will-grammar-consistency
verified: 2026-06-27T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 65.1: Reload-Deferred Will Grammar Consistency -- Verification Report

**Phase Goal:** The `will` prefix in the pending/preview surface marks exactly those reconciliation actions whose effect is deferred to the next `/reload`; actions that take effect immediately render without `will`. This reconciles the pending-tense grammar with the per-command reload-hint discipline.
**Verified:** 2026-06-27
**Status:** passed
**Re-verification:** No -- initial verification

> **Forward-reference (record accuracy):** The closed-set counts cited below
> (`STATUS_TOKENS` = 20, `PLUGIN_STATUSES` = 15, `MARKETPLACE_STATUSES` = 7) are
> the as-of-65.1 snapshot after this phase retired the marketplace
> `will add`/`will remove` tokens. Phase 66 subsequently added `force-installed`
> and `force-upgradable`, moving the live counts to `STATUS_TOKENS` = 22 /
> `PLUGIN_STATUSES` = 17 (`MARKETPLACE_STATUSES` unchanged at 7). The final
> milestone tripwire is 22/17/7; the values here are a correct point-in-time
> record, not the current totals.

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                  | Status     | Evidence                                                                                                                                                                                         |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Every `will`-prefixed token in the pending/preview surface corresponds to a reload-deferred action; no immediate action renders a bare `will` token.   | VERIFIED | `STATUS_TOKENS` (20 entries) and `MARKETPLACE_STATUSES` (7 entries) contain no `"will add"` or `"will remove"`; `renderMpHeader` has no `case "will add":` or `case "will remove":` arm; `buildReconcilePendingNotification` has no `marketplacesToAdd` projection loop. |
| 2   | The pending grammar is consistent with the per-command reload-hint discipline (`shouldEmitReloadHint` / `RELOAD_HINT_TRAILER`) -- WILL-02 agreement test green. | VERIFIED | `tests/architecture/notify-will-reload-agreement.test.ts` exists, exercises realized cascades for all five reload-deferred actions (install/uninstall/enable/disable + marketplace-remove plugin cascade) and both immediate actions (add/empty-remove). No GSD references found in file. |
| 3   | Marketplace add no longer renders `will add`; marketplace remove renders `will` only for its reload-deferred plugin-uninstall cascade.                | VERIFIED | `mp-add-plugin-install` catalog fixture has no `status: "will add"` (bare header + `will install` child); `marketplace-remove-with-installed-plugins` catalog fixture has bare header + `will uninstall` children; `pushMarketplaceRemoveCascade` helper skips empty removes (`o.plugins.length === 0`) and never assigns a marketplace-level `will` status. |
| 4   | Plugin install / uninstall / enable / disable retain their `will install` / `will uninstall` / `will enable` / `will disable` tokens.                 | VERIFIED | `PLUGIN_STATUSES` still carries all four pending-tense entries (lines 382-385 of `shared/notify.ts`); `renderPluginRow` switch has all four `case "will *":` arms. |
| 5   | `docs/output-catalog.md`, `docs/messaging-style-guide.md`, the closed set (STATUS_TOKENS=20, MARKETPLACE_STATUSES=7), and byte-exact catalog/notify tests reflect the reconciled grammar; `npm run check` green. | VERIFIED | `notify-closed-set-locks.test.ts` asserts `STATUS_TOKENS.length === 20` and `MARKETPLACE_STATUSES.length === 7`; `PLUGIN_STATUSES.length === 15` and `REASONS.length === 32` unchanged; token table in output-catalog.md lists only `(will install)`, `(will uninstall)`, `(will enable)`, `(will disable)` as pending plugin tokens; `MARKETPLACE_STATUSES` section in output-catalog.md (line 147) says "7-member"; messaging-style-guide.md states "7 marketplace status discriminators" and "7 literal strings"; `npm run check` serialized exit 0 documented in SUMMARY commit `5e102920`. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact                                                                | Expected                                                                           | Status   | Details                                                                                                                   |
| ----------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------- |
| `extensions/pi-claude-marketplace/shared/notify.ts`                    | Closed-set tuples and union with marketplace will-tokens retired                   | VERIFIED | `STATUS_TOKENS` = 20 entries; `MARKETPLACE_STATUSES` = 7 entries; `MpWillAdd`/`MpWillRemove` absent; no will-add/will-remove switch arms. |
| `extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts`   | `marketplacesToAdd` loop deleted; `marketplacesToRemove` emits per-plugin cascade | VERIFIED | No `marketplacesToAdd` loop; `pushMarketplaceRemoveCascade` helper emits `{ status: "will uninstall", name }` per plugin; `ReconcileBlockStatus = Extract<MarketplaceStatus, "added" | "removed" | "failed">`. |
| `extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts`    | `PlannedMarketplaceRemove.plugins` field                                           | VERIFIED | `readonly plugins: readonly string[]` field present with JSDoc explaining it feeds the PENDING projection only (not `pluginsToUninstall`). |
| `extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts`     | `diffMarketplaces` populates `plugins` from recorded state                        | VERIFIED | Line 220: `remove.push({ scope, marketplace: mpName, plugins: Object.keys(mpRecord.plugins) })`.                       |
| `tests/architecture/notify-will-reload-agreement.test.ts`              | WILL-02 cross-surface agreement anchor                                             | VERIFIED | File exists; all test titles contain `WILL-02`; no `65.1`/`Phase`/`plan` tokens; tests cover 5 reload-deferred + 2 immediate cases. |
| `tests/architecture/notify-closed-set-locks.test.ts`                   | Asserts STATUS_TOKENS=20, MARKETPLACE_STATUSES=7                                   | VERIFIED | Test asserts `STATUS_TOKENS.length === 20` and `MARKETPLACE_STATUSES.length === 7`. |
| `tests/architecture/catalog-uat.test.ts`                               | Bare-header add fixture + marketplace-remove cascade fixture                       | VERIFIED | `mp-add-plugin-install` has no `status: "will add"` (bare header + `will install` child); `marketplace-remove-with-installed-plugins` has bare header + two `will uninstall` child rows. |
| `docs/output-catalog.md`                                                | Reconciled token legend and marketplace-remove cascade state                       | VERIFIED | Token table has only 4 plugin pending tokens; marketplace token table shows 7-member set; `mp-add-plugin-install` state is bare-header form; `marketplace-remove-with-installed-plugins` catalog state present with correct byte form. |
| `docs/messaging-style-guide.md`                                         | 7 marketplace status discriminators, no will-add/will-remove sentence              | VERIFIED | Line 27: `MarketplaceStatus; // 7 literal strings`; line 58: "closed set of 7 marketplace status discriminators"; no sentence naming `will add`/`will remove` as DIFF-02 pending tokens. |

### Key Link Verification

| From                                                        | To                                              | Via                                                    | Status   | Details                                                                                                       |
| ----------------------------------------------------------- | ----------------------------------------------- | ------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------- |
| `plan.ts diffMarketplaces`                                  | `types.ts PlannedMarketplaceRemove.plugins`     | `remove.push` populates `plugins` from recorded state  | VERIFIED | Line 220 in `plan.ts` constructs `{ scope, marketplace: mpName, plugins: Object.keys(mpRecord.plugins) }`.   |
| `reconcile/notify.ts buildReconcilePendingNotification`     | `PlannedMarketplaceRemove.plugins`              | `pushMarketplaceRemoveCascade` per-plugin will-uninstall push | VERIFIED | Helper at lines 195-210 iterates `o.plugins` and pushes `{ status: "will uninstall", name: pluginName }`.    |
| `tests/architecture/notify-will-reload-agreement.test.ts`  | WILL-02 invariant on pending surface            | Renders realized cascades and checks trailer presence  | VERIFIED | Test checks `RELOAD_HINT_TRAILER` present for 5 reload-deferred actions and absent for 2 immediate actions.   |

### Data-Flow Trace (Level 4)

Not applicable. Phase is a pure output-grammar refactor with no new data-fetching surface. The key data flow (recorded plugin names into pending projection) was verified at Level 3: `plan.ts` reads `mpRecord.plugins`, populates `PlannedMarketplaceRemove.plugins`, which `pushMarketplaceRemoveCascade` consumes.

### Behavioral Spot-Checks

| Behavior | Evidence | Status |
| -------- | -------- | ------ |
| No `"will add"` or `"will remove"` quoted literals anywhere in extensions/tests/docs | `grep -rn '"will add"\|"will remove"' extensions/ tests/ docs/` returned no output | PASS |
| MARKETPLACE_STATUSES = 7 entries in source | Direct count of `MARKETPLACE_STATUSES` tuple: added, removed, updated, failed, autoupdate enabled, autoupdate disabled, skipped | PASS |
| STATUS_TOKENS = 20 entries in source | Direct count of `STATUS_TOKENS` tuple confirmed 20 entries; `will add`/`will remove` absent | PASS |
| REASONS = 32 entries in source | Direct count of `REASONS` tuple confirmed 32 entries | PASS |
| `renderMpHeader` has no will-add/will-remove arms | `grep -n "case.*will add\|case.*will remove"` returned no results; switch covers exactly `"added"`, `"removed"`, `"updated"`, `"failed"`, `"autoupdate enabled"`, `"autoupdate disabled"`, `"skipped"`, `undefined`, and `default: assertNever(mp)` | PASS |
| `WILL_TOKEN_RE` in notify-grammar-invariant.test.ts excludes `add\|remove` | Pattern at line 220: `/(will (?:install\|uninstall\|enable\|disable))/` -- `add` and `remove` absent | PASS |
| No GSD references in WILL-02 test file | `grep -nE "65\.1\|Phase [0-9]\|plan [0-9]"` returned no output | PASS |
| `reconcile/apply.test.ts` asserts 3-member `ReconcileBlockStatus` | Test at line 1136 regex matches `"added"..."removed"..."failed"` -- 3-member set | PASS |
| `reconcile/plan.test.ts` `deepEqual` expects `{ ..., plugins: [] }` | Line 196 confirms `plugins: []` in the strict deepEqual assertion | PASS |

### Probe Execution

No probes declared or applicable for this phase (pure refactor; no `scripts/*/tests/probe-*.sh` files exist for this phase).

### Requirements Coverage

| Requirement | Source Plan   | Description                                                                                       | Status    | Evidence                                                                                    |
| ----------- | ------------- | ------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------- |
| WILL-01     | 65.1-02-PLAN  | `will`-prefix in pending surface iff action deferred to `/reload`; immediate actions render without `will` | SATISFIED | `marketplacesToAdd` loop deleted; `renderMpHeader` has no will-add/will-remove arms; STATUS_TOKENS=20, MARKETPLACE_STATUSES=7. |
| WILL-02     | 65.1-01-PLAN, 65.1-02-PLAN | Pending `will`-grammar agrees with per-command reload-hint discipline | SATISFIED | `notify-will-reload-agreement.test.ts` exists and verifies realized-cascade trailer agreement for all five reload-deferred actions. |
| WILL-03     | 65.1-02-PLAN  | Marketplace add does not render `will add`; marketplace remove renders `will` only for its reload-deferred plugin-uninstall cascade | SATISFIED | `mp-add-plugin-install` fixture bare-header form; `marketplace-remove-with-installed-plugins` fixture with per-plugin `will uninstall` rows; `pushMarketplaceRemoveCascade` helper. |
| WILL-04     | 65.1-02-PLAN  | Docs, closed set, and byte-exact tests reflect reconciled grammar; `npm run check` green           | SATISFIED | Both docs updated; both closed sets at correct counts; all coupled tests updated; `npm run check` serialized exit 0 per SUMMARY. |

### Anti-Patterns Found

Scanned files modified in this phase (per SUMMARY key-files).

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `docs/messaging-style-guide.md` | 26, 57 | Pre-existing: says "16 literal strings" / "16 plugin status discriminators" but `PLUGIN_STATUSES` has 15 entries (`"present"` referenced in style guide is not in the actual tuple). Introduced before Phase 65.1 in commit `49325575`. | INFO | Not a Phase 65.1 regression; not in scope. The style guide's own disclaimer says to read the source for canonical membership. `notify-closed-set-locks.test.ts` correctly asserts `PLUGIN_STATUSES.length === 15` and is the binding runtime tripwire. |

No TBD/FIXME/XXX markers found in phase-modified files. No debt markers blocking completion.

**Note on `"will add"` / `"will remove"` prose occurrences in `docs/output-catalog.md`:** Lines 1332 and 1354 contain these phrases in explanatory prose ("so its header carries no `will add` token", "no `will remove` marketplace token"). These are negation descriptions explaining the retirement, not token table entries or byte-form assertions. The token table (lines 130-155) correctly lists only the four surviving pending tokens. This is not a stub or wiring gap.

### Human Verification Required

None. All success criteria are verifiable programmatically via code inspection and test file content.

### Gaps Summary

No gaps. All four WILL requirements are satisfied in the codebase. The phase goal is achieved: the `will` prefix in the pending/preview surface marks exactly those reconciliation actions whose effect is deferred to the next `/reload`, and this is enforced by the closed-set tripwires, the WILL-02 agreement test, the byte-exact catalog UAT fixtures, and the reconciled prose docs.

---

_Verified: 2026-06-27_
_Verifier: Claude (gsd-verifier)_
