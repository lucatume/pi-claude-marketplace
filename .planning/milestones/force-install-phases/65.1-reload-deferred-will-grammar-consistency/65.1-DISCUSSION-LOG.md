# Phase 65.1: Reload-Deferred Will Grammar Consistency - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-27
**Phase:** 65.1-reload-deferred-will-grammar-consistency
**Areas discussed:** Authoritative classifier, Immediate-action rendering, Marketplace remove split, Byte-contract update

**Origin:** Inserted mid-milestone during Phase 66 discussion. Resolving FSTAT-06's `will force update` revealed the `pending` surface has no `will update` (reconciliation never updates versions), and that the existing `will` grammar is inconsistent — marketplace add is immediate yet renders `will add`. User chose to insert a phase (before Phase 66) to make `will` mean reload-deferred only.

---

## Authoritative classifier

| Option | Description | Selected |
|--------|-------------|----------|
| Anchor to reload-hint discipline | Pending `will` carries iff the command path emits the `/reload to pick up changes` trailer. One source of truth; WILL-02 verification anchor. | ✓ |
| Independent pending classifier | Derive reload-deferral inside pending. Divergence risk. | |

**User's choice:** Anchor to reload-hint discipline → D-65.1-01.

---

## Immediate-action rendering

| Option | Description | Selected |
|--------|-------------|----------|
| Drop from pending | Immediate actions are omitted; all-immediate change → `Pending: next reload will apply 0 actions.` | ✓ |
| Present-tense token | Render `(added)` etc. Muddies 'pending', adds tokens. | |

**User's choice:** Drop from pending → D-65.1-02.

---

## Marketplace remove split

| Option | Description | Selected |
|--------|-------------|----------|
| Cascade as will uninstall rows | Marketplace de-registration immediate (no `will remove`); reload-deferred cascade renders per-plugin `will uninstall`; empty marketplace shows nothing pending. Retires marketplace will-tokens. | ✓ |
| Keep marketplace-level will remove | Retain a single `will remove` row when a cascade exists. | |

**User's choice:** Cascade as will uninstall rows → D-65.1-03.

---

## Byte-contract update

| Option | Description | Selected |
|--------|-------------|----------|
| Tests now, prose docs in 70 | Update notify/catalog-uat tests in 65.1; defer output-catalog/style-guide to Phase 70 (matches Phase 66). | |
| Everything in 65.1 | Update tests AND prose docs fully in 65.1; self-contained. | ✓ |

**User's choice:** Everything in 65.1 → D-65.1-04. (Overrode the defer-docs recommendation; wants the phase self-contained.)

## Claude's Discretion

- Classifier seam location (shared helper vs assertion); whether marketplace reconcile rows are removed from the MOD-03 render map or short-circuited upstream.

## Deferred Ideas

- Force-display tokens — Phase 66 (re-planned on the corrected grammar).
- Final PRD §11 reconcile — Phase 70.
