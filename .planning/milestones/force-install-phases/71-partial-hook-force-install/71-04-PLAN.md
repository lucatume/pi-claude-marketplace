---
phase: 71-partial-hook-force-install
plan: 04
type: execute
wave: 4
depends_on: [71-03]
files_modified:
  - tests/architecture/catalog-uat.test.ts
  - docs/output-catalog.md
  - docs/messaging-style-guide.md
  - tests/shared/notify-v2.test.ts
  - tests/shared/snm37-behavioral-smoke.test.ts
  - tests/shared/snm38-indent-ladder.test.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/orchestrators/plugin/list.test.ts
autonomous: true
requirements: [PHOOK-04, PHOOK-05]
must_haves:
  truths:
    - "install --force on a partial-hook plugin stages a hooks.json that is a STRICT SUBSET of the source: the dropped event/group/handler is absent from the written file"
    - "Without --force, install of an unsupported partial-hook plugin still blocks/errors with the --force hint (SEV-02), and a direct --force degrade renders at info with no Warning: (SEV-01 / D-71-06)"
    - "Each catalog-uat {unsupported hooks} fixture is classified by its actual hooks.json bytes: non-bucket-A/unsupported-matcher rows flip to unsupported/force-installed; genuinely malformed rows stay unavailable (Q3)"
    - "The byte-exact catalog/notify/indent-ladder suites are GREEN with the reconciled partial-hook rows"
  artifacts:
    - path: "docs/output-catalog.md"
      provides: "reconciled partial-hook unsupported / force-installed rows with the aggregate {unsupported hooks} brace"
    - path: "tests/orchestrators/plugin/install.test.ts"
      provides: "PHOOK-04 strict-subset staging assertion + no-force-blocks assertion"
  key_links:
    - from: "install --force partial-hook"
      to: "staged hooks.json"
      via: "writeHookConfig stages parseHooksConfig.value (filtered subset)"
      pattern: "hooks\\.json"
  prohibitions:
    - "Do NOT stage any dropped event/group/handler in the materialized hooks.json (PHOOK-04 strict subset)"
    - "Do NOT flip a genuinely malformed (S1/S2) fixture to unsupported (Q3 / PHOOK-03)"
    - "Do NOT add a new REASONS member or change the closed-set count"
    - "Do NOT emit a Warning: summary on any --force path (FORCE-04 / SEV-01)"
    - "Do NOT reference GSD phase/plan/wave numbers in source, tests, or docs prose"
---

<objective>
Lock the byte-exact output contract and prove PHOOK-04. First, audit each `catalog-uat` `{unsupported hooks}` fixture's actual `hooks.json` bytes (Q3) and classify it: non-bucket-A / unsupported-matcher cases flip from `unavailable` to `unsupported` (and `force-installed` after `--force`); genuinely malformed (S1/S2) cases stay `unavailable`. Reconcile the byte forms in `catalog-uat.test.ts`, `docs/output-catalog.md`, `docs/messaging-style-guide.md`, and the notify / indent-ladder suites. Then assert the PHOOK-04 strict-subset property: a partial-hook `install --force` stages a `hooks.json` with the dropped event/group absent, and without `--force` the plugin still blocks. Confirm SEV-01 (force degrade = info, no `Warning:`) and SEV-02 (no-force = error + `--force` hint) hold with no severity-layer source change (D-71-06).

Purpose: Closes the phase. The strict-subset assertion is the security-relevant verification (V5 containment); the byte-exact reconcile keeps the user-contract GREEN.

Output: reconciled catalog/docs/notify fixtures + the strict-subset install test + the list partial-hook row.

Locked decisions / requirements: PHOOK-04 (filtered staging, no-force blocks), PHOOK-05 (byte-exact partial-hook rows), D-71-06 (severity), D-71-04 (REASONS stays 32), Q3 (audit before flipping bytes).
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-partial-hook-force-install/71-RESEARCH.md
@.planning/phases/71-partial-hook-force-install/71-PATTERNS.md
@.planning/phases/71-partial-hook-force-install/71-03-PLAN.md
@./CLAUDE.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit + reconcile catalog-uat / docs / notify partial-hook byte forms (Q3)</name>
  <files>tests/architecture/catalog-uat.test.ts, docs/output-catalog.md, docs/messaging-style-guide.md, tests/shared/notify-v2.test.ts, tests/shared/snm37-behavioral-smoke.test.ts, tests/shared/snm38-indent-ladder.test.ts</files>
  <read_first>
    - tests/architecture/catalog-uat.test.ts (delta 279/1223; epsilon 283/857; unsupported-hooks rows 584/1775; info "old plugin declares hooks" 2388)
    - docs/output-catalog.md, docs/messaging-style-guide.md (the unavailable<->unsupported hooks rows)
    - tests/shared/notify-v2.test.ts, tests/shared/snm37-behavioral-smoke.test.ts, tests/shared/snm38-indent-ladder.test.ts
    - 71-RESEARCH.md Deliverable 4 + Pitfall 4 (do not reclassify a structural fixture); Q3
    - ./CLAUDE.md (byte-exact output contract; ASCII commits)
  </read_first>
  <action>
    For EACH `{unsupported hooks}` fixture referenced in catalog-uat.test.ts (delta, epsilon, the 584/1775 rows, and the 2388 info row), grep/read its embedded `hooks.json` bytes and classify: if the config is non-bucket-A (e.g. a `Stop` event) or carries an unsupported matcher on a supported event, the row becomes `unsupported` (and `force-installed` after a `--force` install); if the config is genuinely malformed JSON or a malformed handler (`type:"command"` with no `command`), it STAYS `unavailable` (Pitfall 4 / PHOOK-03 — do not flip it). Record the classification inline so the reconcile is auditable.

    Reconcile the byte forms accordingly in catalog-uat.test.ts, docs/output-catalog.md, and docs/messaging-style-guide.md: flipped rows render `unsupported` / `force-installed` with the SINGLE aggregate `{unsupported hooks}` brace on the list row and the enumerated `event(matcher) (unsupported)` detail on the info surface (matching Plan 03's renderer). Add any missing partial-hook `unsupported` / `force-installed` rows. Update tests/shared/notify-v2.test.ts (reason rendering), snm37-behavioral-smoke.test.ts and snm38-indent-ladder.test.ts (indent ladder) for the new rows. Keep REASONS at 32. Keep all docs prose ASCII (no em/unicode dashes) and free of GSD phase references; use PHOOK-05 / D-71-04 / D-71-06 anchors.
  </action>
  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts tests/shared/notify-v2.test.ts tests/shared/snm37-behavioral-smoke.test.ts tests/shared/snm38-indent-ladder.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every `{unsupported hooks}` fixture is classified by its actual bytes; only non-bucket-A/unsupported-matcher rows flip, structural rows stay unavailable.
    - catalog-uat, notify-v2, snm37, snm38 are green with reconciled rows; REASONS unchanged.
    - docs/output-catalog.md + messaging-style-guide.md reflect the reconciled token set in ASCII.
  </acceptance_criteria>
  <done>The byte-exact catalog/notify/docs surfaces are reconciled and green; no structural fixture was misclassified.</done>
</task>

<task type="auto">
  <name>Task 2: PHOOK-04 strict-subset staging + no-force-blocks + severity coverage</name>
  <files>tests/orchestrators/plugin/install.test.ts, tests/orchestrators/plugin/list.test.ts</files>
  <read_first>
    - tests/orchestrators/plugin/install.test.ts (2941 LIFE-01 bridge-write harness to extend)
    - tests/orchestrators/plugin/list.test.ts (partial-hook row)
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (hooksPhase 707-740 — reference, no change)
    - extensions/pi-claude-marketplace/bridges/hooks/stage.ts (writeHookConfig 199-210 — reference, no change)
    - tests/fixtures/hooks-posttooluse-and-stop.json, tests/fixtures/hooks-pretooluse-matcher-mix.json (Plan 01)
    - 71-RESEARCH.md Deliverable 2 (seam map) + "Severity" + Test Map PHOOK-04 row
  </read_first>
  <action>
    Extend `tests/orchestrators/plugin/install.test.ts` (around the LIFE-01 bridge-write case at 2941): drive an `install --force` on a partial-hook plugin (a supported component + the `hooks-posttooluse-and-stop.json` shape) and assert the staged `<scopeRoot>/pi-claude-marketplace/hooks/<plugin>/hooks.json` is a STRICT SUBSET — read the written file and assert the dropped `Stop` event (and, using the matcher-mix fixture, the dropped regex group) is ABSENT while the supported group is PRESENT (this is the PHOOK-04 verification). Assert that WITHOUT `--force` the same plugin blocks/errors (it resolves `unsupported`) and that the no-force error carries the `--force` hint (SEV-02), while the `--force` degrade emits at info with NO `Warning:` summary (SEV-01 / D-71-06) and reads `force-installed` (FSTAT-07). Add a `tests/orchestrators/plugin/list.test.ts` assertion that the partial-hook row renders `force-installed` (after force-install) / `unsupported` with the aggregate `{unsupported hooks}` marker. No source change to install.ts / stage.ts is expected — the filtered subset is inherited from `parseHooksConfig.value`. Use PHOOK-04 / SEV-01 / SEV-02 / FSTAT-07 / D-71-06 anchors; never GSD phase numbers.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/list.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The staged hooks.json omits every dropped event/group and keeps the supported group (strict-subset assertion).
    - No-force install blocks with the `--force` hint; `--force` degrade is info with no `Warning:` and reads force-installed.
    - list partial-hook row carries the aggregate `{unsupported hooks}` marker.
  </acceptance_criteria>
  <done>PHOOK-04 strict-subset staging and the SEV-01/02 severity arms are proven green for partial-hook plugins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filtered hooks.json -> staged file on disk (hooksDir) | the materialized config crosses into the scope-contained hooks directory; it MUST be a strict deterministic subset of the validated source (V5 output containment) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-07 | Tampering | staged hooks.json subset integrity | mitigate | The PHOOK-04 strict-subset test reads the written file and asserts every dropped event/group/handler is absent; `writeHookConfig` stages `parseHooksConfig.value` (the pure filtered subset), so the bridge can never stage a dropped handler. This is the phase's security-relevant containment invariant. |
| T-71-08 | Elevation of Privilege | --force bypassing structural failures | mitigate | The no-force-blocks assertion plus the retained structural-precedence cases (Plan 02) confirm `--force` degrades only `unsupported`, never `unavailable`/structural (FORCE-05). Existing LIFE-03 symlink-escape + NFR-10 containment in stage.ts are unaffected. |
| T-71-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- `node --test tests/architecture/catalog-uat.test.ts tests/shared/notify-v2.test.ts tests/shared/snm37-behavioral-smoke.test.ts tests/shared/snm38-indent-ladder.test.ts` green (Task 1).
- `node --test tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/list.test.ts` green (Task 2).
- Phase gate: `npm run check` green (typecheck + lint + format + test + integration, NFR-6) before `/gsd-verify-work`.
</verification>

<success_criteria>
- Partial-hook `install --force` stages a strict-subset hooks.json; dropped handlers never staged (PHOOK-04).
- No-force blocks with `--force` hint; force degrade renders at info; byte-exact catalog/notify/docs GREEN (PHOOK-05).
- Structural fixtures stay `unavailable`; REASONS stays 32.
</success_criteria>

<output>
Create `.planning/phases/71-partial-hook-force-install/71-04-SUMMARY.md` when done.
</output>
