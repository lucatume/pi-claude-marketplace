---
phase: 71-partial-hook-force-install
plan: 03
type: execute
wave: 3
depends_on: [71-02]
files_modified:
  - extensions/pi-claude-marketplace/shared/probe-classifiers.ts
  - extensions/pi-claude-marketplace/shared/concerns/hooks.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
  - tests/shared/probe-classifiers.test.ts
  - tests/orchestrators/plugin/cross-surface-reason-parity.test.ts
  - tests/orchestrators/plugin/info.test.ts
autonomous: true
requirements: [PHOOK-05]
must_haves:
  truths:
    - "narrowUnsupportedKinds maps kind 'hooks' to the EXISTING 'unsupported hooks' REASONS member; REASONS stays a closed set of 32 (no new literal, no tripwire change)"
    - "The compact list row carries a SINGLE aggregate {unsupported hooks} marker regardless of how many events/matchers dropped (first-wins dedup)"
    - "/claude:plugin info enumerates each dropped handler as event(matcher) (unsupported) for group/handler drops and event (unsupported) for event drops, via the STRICT info reader path"
    - "The Stop (unsupported) info line that previously rendered via the lenient bail reader now renders identically via the strict path (Pitfall 1 guarded)"
    - "list and info render the aggregate {unsupported hooks} token byte-identically (cross-surface parity)"
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/probe-classifiers.ts"
      provides: "narrowUnsupportedKinds third case 'hooks' -> 'unsupported hooks'; widened return union"
      contains: "unsupported hooks"
    - path: "extensions/pi-claude-marketplace/shared/concerns/hooks.ts"
      provides: "appendHooksBlock matcher-group (unsupported) rendering; widened HookSummaryEntry"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts"
      provides: "strict reader carries dropped-handler (unsupported) enumeration"
  key_links:
    - from: "info.ts readHookSummaryEntries (strict)"
      to: "appendHooksBlock dropped arm"
      via: "merge droppedHooks into projected entries"
      pattern: "dropped"
  prohibitions:
    - "Do NOT add a new REASONS member or change the closed-set count (D-71-04: REASONS stays 32)"
    - "Do NOT emit more than one {unsupported hooks} aggregate token on the list row"
    - "Do NOT leave the dropped enumeration only on the lenient reader (it must render on the strict path post-resolution)"
    - "Do NOT reference GSD phase/plan/wave numbers in source or test titles"
---

<objective>
Render the dropped-hooks signal. Extend `narrowUnsupportedKinds` with a third case mapping kind `"hooks"` to the pre-existing `"unsupported hooks"` REASONS member (closed set stays 32, D-71-04) so the compact `list` row carries one aggregate `{unsupported hooks}` marker. Extend `appendHooksBlock` (and `HookSummaryEntry`) to render dropped entries at matcher-group granularity, and move the `(unsupported)` enumeration onto the STRICT info reader so a now-resolving partial-hook plugin still surfaces its dropped detail (D-71-05, Pitfall 1).

Purpose: PHOOK-05 reason + info plumbing. Severity needs no new code — once the plugin resolves `unsupported` it flows through the Phase 65 force gate + Phase 69 severity automatically (verified in Plan 04).

Output: classifier third case + info enumerated detail + cross-surface parity; migrated probe-classifiers / cross-surface-reason-parity / info tests.

Locked decisions implemented here:
- D-71-04: single aggregate `{unsupported hooks}` on list; reuse existing member.
- D-71-05: per-handler enumeration lives in `info`.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-partial-hook-force-install/71-RESEARCH.md
@.planning/phases/71-partial-hook-force-install/71-PATTERNS.md
@.planning/phases/71-partial-hook-force-install/71-02-PLAN.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: narrowUnsupportedKinds third case + cross-surface parity</name>
  <files>extensions/pi-claude-marketplace/shared/probe-classifiers.ts, tests/shared/probe-classifiers.test.ts, tests/orchestrators/plugin/cross-surface-reason-parity.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/probe-classifiers.ts (narrowUnsupportedKinds 146-160; header doc 125-145)
    - extensions/pi-claude-marketplace/shared/notify.ts (REASONS 89-130; "unsupported hooks" member at 98)
    - tests/shared/probe-classifiers.test.ts; tests/orchestrators/plugin/cross-surface-reason-parity.test.ts
    - call sites that consume the return union: reconcile/notify.ts ~526, marketplace/update.ts ~679, list/info/install error surface
    - 71-RESEARCH.md Deliverable 3 ("List row" section); 71-PATTERNS.md "shared/probe-classifiers.ts" section
  </read_first>
  <action>
    Add a third arm to `narrowUnsupportedKinds`: `kind === "hooks"` maps to `"unsupported hooks"` (the existing REASONS member at notify.ts:98 — add NO new literal). Widen the return union to `("lsp" | "unsupported source" | "unsupported hooks")[]`. The first-wins dedup (149-156) keeps the aggregate single even when `"hooks"` appears once. Update the header doc-comment (135-138) to name the `hooks -> unsupported hooks` case alongside `lspServers -> lsp`. Fix any call site whose `ContentReason` typing breaks on the widened union (reconcile/notify.ts ~526, marketplace/update.ts ~679, and the list/info/install error surfaces) so typecheck stays green.

    Add a `tests/shared/probe-classifiers.test.ts` case asserting `["hooks"] -> ["unsupported hooks"]` and that a mixed `["hooks","lspServers"]` dedups to two distinct tokens. Extend `tests/orchestrators/plugin/cross-surface-reason-parity.test.ts` so the `{unsupported hooks}` aggregate renders byte-identically across list and info for a partial-hook plugin. Use D-71-04 / PHOOK-05 / RSTATE-05 anchors only.
  </action>
  <verify>
    <automated>node --test tests/shared/probe-classifiers.test.ts tests/orchestrators/plugin/cross-surface-reason-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `narrowUnsupportedKinds(["hooks"])` returns `["unsupported hooks"]`; return union widened; all call sites compile.
    - No new REASONS literal; closed-set count unchanged.
    - Cross-surface parity test green for the partial-hook aggregate token.
  </acceptance_criteria>
  <done>The `"hooks"` kind renders the single aggregate `{unsupported hooks}` marker identically across surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: info strict-path dropped-handler enumeration + appendHooksBlock matcher arm</name>
  <files>extensions/pi-claude-marketplace/shared/concerns/hooks.ts, extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, tests/orchestrators/plugin/info.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/concerns/hooks.ts (HookSummaryEntry union 64-71; appendHooksBlock 91-109, lenient arm at 101-102; doc-comment 77-89)
    - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts (readHookSummaryEntries strict 316-334; projectHookSummaryEntries 276-298; readLenientHookSummary 363-405; composeResolvedComponents 461-514; reader routing 502-505)
    - tests/orchestrators/plugin/info.test.ts (1966 Stop (unsupported) lenient canary; 2004 mixed PostToolUse+Stop)
    - 71-RESEARCH.md Deliverable 3 ("info" section + Pitfall 1); 71-PATTERNS.md "shared/concerns/hooks.ts" and "orchestrators/plugin/info.ts" sections
  </read_first>
  <action>
    Extend the `HookSummaryEntry` lenient arm (or add a dropped arm) in shared/concerns/hooks.ts to carry an optional `matcher` so a dropped entry can render at matcher-group granularity. In `appendHooksBlock` (91-109) render dropped entries as `event(matcher) (unsupported)` when a matcher is present (P2-P5 group drops and P6 handler drops) and `event (unsupported)` when not (P1 event drops). Keep the existing supported tool-event `event(matcher)` and non-tool `event` arms exactly. Update the doc-comment (77-89) to describe the matcher-bearing dropped arm.

    In info.ts: since a partial-hook plugin now records `hooksConfigPath`, info routes to the STRICT reader `readHookSummaryEntries` (316-334), which today projects supported entries only. Feed the dropped enumeration onto that strict path — prefer the threaded `resolved.droppedHooks` (authoritative, from Plan 02); fall back to the strict reader's own `parsed.dropped` (it re-parses anyway). Merge supported (plain) and dropped (`(unsupported)`-suffixed) entries in declaration order so the rendered block mirrors FSTAT-07 dropped-component detail. Ensure the previously lenient-only `Stop (unsupported)` line now renders via the strict path (Pitfall 1).

    Migrate `tests/orchestrators/plugin/info.test.ts:1966` and `:2004`: the rows flip from `(unavailable)`/lenient to `unsupported`/force-installed and must render the SAME `Stop (unsupported)` line via the strict path; add a matcher-group case asserting `PreToolUse(.*) (unsupported)` for an intra-event group drop. Use D-71-05 / PHOOK-05 / FSTAT-07 anchors only.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/info.test.ts tests/shared/concerns-hooks.test.ts 2>/dev/null || node --test tests/orchestrators/plugin/info.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `appendHooksBlock` renders `event(matcher) (unsupported)` for matcher-bearing drops and `event (unsupported)` for event drops.
    - info's strict reader emits the dropped enumeration (Stop line preserved post-resolution).
    - Migrated info tests assert the dropped lines via the strict path.
  </acceptance_criteria>
  <done>info enumerates dropped handlers at matcher-group granularity on the strict path; the Stop (unsupported) canary holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolver unsupported kinds -> render surface | render-time projection of the typed unsupported list; no untrusted data crosses, only typed kind tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-05 | Information Disclosure | dropped-handler info detail | accept | The enumerated `event(matcher)` detail is the plugin's own declared hook config, surfaced to the operator on an explicit `info` request; no secret or cross-plugin data is exposed. |
| T-71-06 | Tampering | REASONS closed set | mitigate | The third classifier case reuses the existing `"unsupported hooks"` member; probe-classifiers and cross-surface-parity tests pin the closed-set token, preventing an accidental new literal (D-71-04). |
| T-71-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- `npm run typecheck` green (widened union + all call sites).
- `node --test tests/shared/probe-classifiers.test.ts tests/orchestrators/plugin/cross-surface-reason-parity.test.ts tests/orchestrators/plugin/info.test.ts` green.
</verification>

<success_criteria>
- list row = single `{unsupported hooks}`; info enumerates `event(matcher) (unsupported)`; tokens byte-identical across surfaces.
- REASONS stays 32; no tripwire change.
</success_criteria>

<output>
Create `.planning/phases/71-partial-hook-force-install/71-03-SUMMARY.md` when done.
</output>
