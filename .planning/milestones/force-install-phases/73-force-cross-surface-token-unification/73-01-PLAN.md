---
phase: 73-force-cross-surface-token-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.messaging.ts
  - tests/shared/notify-v2.test.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/orchestrators/plugin/update.test.ts
  - tests/architecture/catalog-uat.test.ts
  - docs/output-catalog.md
  - docs/messaging-style-guide.md
autonomous: true
requirements: [XSURF-01, XSURF-02, XSURF-03]

must_haves:
  truths:
    - "A no-`--force` install of an `unsupported` (force-installable) plugin renders `⊖ … (unsupported) {reasons}` plus the install-worded `--force` hint trailer, not `⊘ … (unavailable)`."
    - "A no-`--force` install failure of a structural `unavailable` plugin (e.g. `{unsupported source}` non-path, missing source) still renders `⊘ … (unavailable)` with NO trailer."
    - "`info` non-locally-resolvable arm derives its status (and reason source) from `resolved.state`; existing non-path structural `unavailable` rows are byte-unchanged."
    - "A targeted manual `update` (no `--force`) of a force-upgradable plugin renders the `force-upgradable` token (`● … (force-upgradable) {degrade reason}`) plus an update-worded `--force` trailer, at `warning` severity."
    - "The same force-upgradable candidate skipped by a bulk update renders the same `force-upgradable` row at `info` severity (no summary line; counted in the tally)."
    - "The force-upgradable update-decline reason matches how `list` describes the same plugin (its degrade kinds, e.g. `{lsp}` / `{unsupported source}`), NOT the misleading `{no longer installable}`, and is BYTE-IDENTICAL to the `list (force-upgradable)` reason brace for the same fixture."
    - "The list-inventory `force-upgradable` row stays byte-frozen (no `--force` trailer)."
    - "Closed-set tripwire counts (REASONS=32, STATUS_TOKENS=23, PLUGIN_STATUSES=18) are unchanged."
    - "`npm run check` is green."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "`PluginUnsupportedMessage.forceHint?` + `PluginForceUpgradableMessage.forceHint?`; widened install trailer gate; new update-worded force trailer literal + its gate"
      contains: "forceHint"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts"
      provides: "`INSTALL_STATUSES` += `unsupported`; `InstallMsg` += `PluginUnsupportedMessage`; `INSTALL_RENDER` unsupported arm"
      contains: "unsupported"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/update.messaging.ts"
      provides: "`UPDATE_STATUSES` += `force-upgradable`; `UpdateMsg` += `PluginForceUpgradableMessage`; `UPDATE_RENDER` force-upgradable arm"
      contains: "force-upgradable"
  key_links:
    - from: "orchestrators/plugin/install.ts::composeUnavailableMessage"
      to: "PluginUnsupportedMessage (status: unsupported, forceHint: true)"
      via: "split on entityErrorRow.forceable === true"
      pattern: "forceable"
    - from: "orchestrators/plugin/update.ts manual-decline producer (catch arm)"
      to: "force-upgradable projection arm"
      via: "err.shape.forceable === true → derive narrowResolverReasons(err.shape.reasons, err.shape.unsupportedKinds)"
      pattern: "err.shape.forceable"
    - from: "shared/notify.ts trailer composer"
      to: "update-worded force trailer"
      via: "p.status === force-upgradable && p.forceHint === true"
      pattern: "forceHint === true"
---

<objective>
Extend the Phase-72 resolver-state-driven render token to the two surfaces it did not cover (install-failure, update-decline) and repair the latent `info.ts` non-resolvable divergence, so a force-installable (`unsupported`) plugin reads consistently with `list`/`info` everywhere and the misleading `{no longer installable}` update-decline wording is replaced with a force-aware reason pointing at `--force`.

Purpose: Close cross-surface inconsistency surfaced by the force-install milestone UAT — XSURF-01 (install-failure token), XSURF-02 / IN-01 (info non-resolvable arm keys on `resolved.state`), XSURF-03 (update-decline force-aware reason + `--force` affordance). Severity is NOT changed (SEV-02 / SEV-04 preserved); only the render token + reason wording moves.

Output: Two new optional `forceHint?` fields, one widened install trailer gate, one new update-worded trailer + gate, two command status-set + render-map additions (`INSTALL_RENDER.unsupported`, `UPDATE_RENDER.force-upgradable`), three producer flips, and byte-exact catalog/test reconciliation.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/73-force-cross-surface-token-unification/73-CONTEXT.md
@.planning/phases/73-force-cross-surface-token-unification/73-RESEARCH.md
@.claude/rules/typescript-comments.md

# Source-of-truth sites (read the cited line ranges, do not re-read whole files)
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/shared/errors.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/update.messaging.ts
</context>

<artifacts_this_phase_produces>
New / widened type-model surface (executors and downstream phases depend on these exact shapes):

- `PluginUnsupportedMessage.forceHint?: boolean` (shared/notify.ts) — set ONLY by the install-failure producer; absent on every list/info row (which stay byte-frozen).
- `PluginForceUpgradableMessage.forceHint?: boolean` (shared/notify.ts) — set ONLY by the update-decline producer; absent on the list-inventory row.
- New frozen DOC trailer literal `FORCE_UPDATE_HINT_TRAILER` (shared/notify.ts) — update-worded analog of `FORCE_INSTALL_HINT_TRAILER`. Proposed bytes: `Re-run with --force to update with the supported components.` (resolves RESEARCH Open Question 1 / A3). It is a NEW frozen catalog/style-guide DOC string.
- Trailer gate widened: install trailer now fires on `(p.status === "unavailable" || p.status === "unsupported") && p.forceHint === true`. A SECOND gate fires the update trailer on `p.status === "force-upgradable" && p.forceHint === true`.
- `INSTALL_STATUSES` gains `"unsupported"`; `InstallMsg` gains `PluginUnsupportedMessage`; `INSTALL_RENDER` gains an `unsupported` arm (`⊖`/`(unsupported)`).
- `UPDATE_STATUSES` gains `"force-upgradable"`; `UpdateMsg` gains `PluginForceUpgradableMessage`; `UPDATE_RENDER` gains a `force-upgradable` arm (`●`/`(force-upgradable)`).

Closed sets are UNCHANGED: `unsupported`, `force-upgradable`, and the degrade reasons (`lsp`, `unsupported hooks`, `unsupported source`) all already exist. Do NOT edit `tests/architecture/notify-closed-set-locks.test.ts` counts.
</artifacts_this_phase_produces>

<design_resolutions>
Resolved open questions / locked design choices the executor MUST follow:

- **A1 (XSURF-03 = token flip, Option A):** flip the manual force-upgradable decline to the existing `force-upgradable` token, the same one `list` uses. Confirmed by maintainer CONTEXT ("reuse the force-upgradable framing").
- **A2 / reason brace (LOCKED — FIX the reason, do NOT keep `{no longer installable}`; SOURCE THE DISCRIMINANT FROM THE THROWN ERROR SHAPE, NOT `resolved`):** the critical design guidance overrides the RESEARCH "minimal" option. A `(force-upgradable)` row carrying `{no longer installable}` is internally contradictory. **`resolved` is NOT in scope at the decline producer.** It is `const resolved = await resolveStrict(...)` declared INSIDE the `try` (update.ts:747); the decline producer is the `catch (err)` arm (update.ts:760-777), where `resolved` is out of scope — writing `resolved.state`/`resolved.unsupported` there is a `TS2304` and fails Task 2's `tsc` gate. Mirror the install-side precedent (`classifyEntityShapeError`, install.ts:1657-1709) and source everything from `err.shape` instead:
  - `requireInstallable` (resolver.ts:1162-1185) is the no-`--force` decline throw at update.ts:757 (`requireInstallable(resolved, "update")`). It constructs `PluginShapeError` with `kind: "no-longer-installable"`, `forceable: r.state === "unsupported"`, and `unsupportedKinds: r.state === "unsupported" ? r.unsupported : []` (resolver.ts:1170-1184). `err.shape` (errors.ts:404-434) carries `kind`, `reasons`, `forceable`, and the optional `unsupportedKinds`.
  - Branch the catch on `err instanceof PluginShapeError && err.shape.forceable === true` (true ⇔ resolver verdict was `unsupported`, i.e. the force-upgradable decline). Derive the degrade reason exactly as the install side does: `narrowResolverReasons(err.shape.reasons, err.shape.unsupportedKinds)` (install.ts:1701) — NOT `narrowUnsupportedKinds(resolved.unsupported)`. Using the install-path helper is deliberate for cross-surface byte parity (see Task 3 byte-equality assertion). These reasons are already in REASONS — NO closed-set bump.
  - When `err.shape.forceable !== true` (structural — force cannot help), keep the existing `reasons: ["no longer installable"]` decline path unchanged.
  - Thread a producer discriminant into the `skipped` outcome (a dedicated field, NOT the reason string) so the projection (update.ts:1675-1697) flips ONLY the force-upgradable arm to the `force-upgradable` token while every other `skipped` reason stays `status: "skipped"`.
- **SEV-04 preservation by STATUS, not reason string (LOCKED):** `cascadeSkipSeverity` (update.ts:1578-1591) currently keys targeted=warning/bulk=info on `reasons.includes("no longer installable")` (line 1586-1588). Because the reason string is being dropped for the force-upgradable decline, severity MUST be re-keyed onto the force-upgradable STATUS path: the new `force-upgradable` projection arm applies `cardinality === "single" ? "warning" : "info"` directly. Do NOT touch the `cascadeSkipSeverity` cardinality logic itself; just move the trigger from the reason string to the status arm. A regression assertion MUST prove targeted=warning / bulk=info still hold. If — and only if — the wording fix genuinely cannot preserve SEV-04 without a REASONS bump, STOP and surface that in the SUMMARY rather than silently keeping `{no longer installable}`.
- **A3 (update-worded trailer):** add `FORCE_UPDATE_HINT_TRAILER` (bytes above). Gate it on `PluginForceUpgradableMessage.forceHint === true` so the list-inventory row (which never sets `forceHint`) stays byte-frozen.
- **A4 (XSURF-02 latent):** non-path sources never resolve `unsupported` today, so XSURF-02 produces NO live byte change — it is latent-divergence repair (parity with `list`). No catalog flip for XSURF-02.
- **A5 (force-upgradable "list-only" invariant):** VERIFIED in planning — the "STRUCTURALLY list-only" claim is JSDoc only (notify.ts:765-766). NO `tests/architecture/` test enforces a surface allowlist forbidding `update` from emitting `force-upgradable`. `notify-stamp-coverage.test.ts:64` excludes `force-upgradable` from the TRANSITION-stamp set (needsReload-true rows); the update-decline row sets `needsReload: false`, so it is NOT a transition and the stamp gate is satisfied. The only edit is widening the `PluginForceUpgradableMessage` JSDoc surface description (no test edit).
- **Anti-patterns (LOCKED):** split the install arm on `entityErrorRow.forceable === true`, source the update-decline discriminant from `err.shape.forceable`, and split the info arm on `resolved.state` — NEVER on the reason brace (`{unsupported source}` appears on both the `unsupported` and the structural `unavailable` non-path arm; the non-path structural case is `forceable: false` → stays `⊘`).
</design_resolutions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: notify.ts type + trailer foundation, with Wave-0 renderer byte tests</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, tests/shared/notify-v2.test.ts</files>
  <read_first>
    - shared/notify.ts:682-715 (PluginUnavailableMessage.forceHint? precedent + PluginUnsupportedMessage that omits it)
    - shared/notify.ts:762-778 (PluginForceUpgradableMessage, "STRUCTURALLY list-only" JSDoc, reuses ICON_INSTALLED `●`)
    - shared/notify.ts:2228 (FORCE_INSTALL_HINT_TRAILER literal)
    - shared/notify.ts:3415-3424 (the install trailer gate currently `p.status === "unavailable" && p.forceHint === true`)
  </read_first>
  <behavior>
    - Test: rendering a `PluginUnsupportedMessage` with `forceHint: true` emits `⊖ <name> … (unsupported) {reasons}` AND a following 4-space-indented `Re-run with --force to install the supported components.` line.
    - Test: rendering a `PluginUnsupportedMessage` WITHOUT `forceHint` emits the `⊖ … (unsupported)` row with NO trailer (list/info parity stays byte-frozen).
    - Test: rendering a `PluginForceUpgradableMessage` with `forceHint: true` emits `● <name> v<ver> (force-upgradable) {reasons}` AND a following 4-space-indented `Re-run with --force to update with the supported components.` line.
    - Test: rendering a `PluginForceUpgradableMessage` WITHOUT `forceHint` (list-inventory) emits the `● … (force-upgradable)` row with NO trailer (byte-frozen).
  </behavior>
  <action>
    Add `readonly forceHint?: boolean;` to `PluginUnsupportedMessage` (shared/notify.ts:709-715), mirroring `PluginUnavailableMessage.forceHint?`. Update its JSDoc: it no longer "DELIBERATELY OMITS" forceHint — the install-error surface now sets it; list/info still omit it and render byte-frozen. Add `readonly forceHint?: boolean;` to `PluginForceUpgradableMessage` (notify.ts:771-778) and widen its JSDoc surface description from "STRUCTURALLY list-only" to: also emitted by the manual update-decline surface (where it carries `forceHint` + a version + the degrade reason); the list-inventory row omits `forceHint` and stays byte-frozen. Add a new frozen literal `FORCE_UPDATE_HINT_TRAILER = "Re-run with --force to update with the supported components.";` next to `FORCE_INSTALL_HINT_TRAILER` (notify.ts:2228). Widen the existing install trailer gate (notify.ts:3422) to `if ((p.status === "unavailable" || p.status === "unsupported") && p.forceHint === true)`. Add a SECOND gate immediately after it: `if (p.status === "force-upgradable" && p.forceHint === true) { lines.push(\`    ${FORCE_UPDATE_HINT_TRAILER}\`); }`. Comment anchors: cite XSURF-01 / XSURF-03 / SEV-02 / SEV-04 and decision IDs only — NO Phase/Plan/Wave references per the comment policy. Add the four renderer byte cases above to tests/shared/notify-v2.test.ts.
  </action>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && node --test "tests/shared/notify-v2.test.ts" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `PluginUnsupportedMessage` and `PluginForceUpgradableMessage` each carry an optional `forceHint?: boolean`.
    - `FORCE_UPDATE_HINT_TRAILER` exists with the exact bytes above.
    - The install trailer gate fires for `unsupported` (with `forceHint`) and the new update gate fires for `force-upgradable` (with `forceHint`); both omit the trailer when `forceHint` is unset.
    - The four new notify-v2 byte cases pass; `tsc --noEmit` is clean.
    - No closed-set count edited.
  </acceptance_criteria>
  <done>The render layer can emit `⊖ (unsupported)` + install trailer and `● (force-upgradable)` + update trailer, gated by `forceHint`, with frozen byte forms locked by notify-v2 tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: producer flips — install (XSURF-01), info (XSURF-02), update-decline (XSURF-03) + messaging maps</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts, extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update.messaging.ts, tests/orchestrators/plugin/install.test.ts, tests/orchestrators/plugin/update.test.ts</files>
  <read_first>
    - install.ts:1505-1529 (composeUnavailableMessage — the XSURF-01 split point) and install.ts:1582-1604 (composeInstallFailureMessage branch 3 routing on entityErrorRow.status)
    - install.ts:1657-1709 (classifyEntityShapeError — the err.shape precedent: branches on err.shape.kind, reads err.shape.forceable, derives reasons via narrowResolverReasons(err.shape.reasons, err.shape.unsupportedKinds))
    - install.messaging.ts:39 (INSTALL_STATUSES), 48-52 (InstallMsg union), 69-96 (INSTALL_RENDER, unavailable arm at 85-94 is the clone source)
    - info.ts:1045-1056 (non-locally-resolvable arm hardcoding `status: "unavailable"`); info.ts:990 + the path-arm split (buildNotInstalledPathRow) as the mirror precedent; narrowUnsupportedKinds already imported (used at info.ts:819,864)
    - shared/errors.ts:404-434 (PluginShapeErrorShape — `no-longer-installable` arm carries `reasons`, `forceable`, optional `unsupportedKinds`)
    - domain/resolver.ts:1162-1185 (requireInstallable throw: `kind: "no-longer-installable"`, `forceable: r.state === "unsupported"`, `unsupportedKinds: r.state === "unsupported" ? r.unsupported : []` — this is the no-`--force` decline throw at update.ts:757)
    - update.ts:744-777 (manual-decline producer — `resolved` is declared at update.ts:747 INSIDE the `try`; the `catch (err)` arm at 760-777 has `resolved` OUT OF SCOPE and currently emits `partition: "skipped", reasons: ["no longer installable"]`)
    - update.ts:1578-1591 (cascadeSkipSeverity — keys severity on `reasons.includes("no longer installable")` at 1586-1588; DO NOT touch cardinality logic) and update.ts:1675-1697 (outcomeToCascadePluginMessage skipped arm — the projection point)
    - update.ts:505-548 (the CASCADE/autoupdate force path → partition: "failed", NOT the manual decline — must stay untouched)
    - update.messaging.ts:30 (UPDATE_STATUSES), 38-42 (UpdateMsg), 52-70 (UPDATE_RENDER)
  </read_first>
  <behavior>
    - Test (XSURF-01): a no-`--force` install of a force-degradable (`forceable: true`) `unsupported` plugin produces an `unsupported` row → byte form flips `⊘ … (unavailable) {…}` to `⊖ … (unsupported) {…}` and KEEPS the `Re-run with --force to install …` trailer; reasons array unchanged.
    - Test (XSURF-01 negative): a structural `unavailable` install failure (`forceable: false`) — including the non-path `{unsupported source}` case (install.test.ts:470,511) and a missing-source defect — STAYS `⊘ … (unavailable)` with NO trailer.
    - Test (XSURF-03 targeted): a targeted manual `update` (no `--force`) of a force-upgradable plugin (catch arm sees `err.shape.forceable === true`) produces `status: "force-upgradable"`, `severity: "warning"`, `needsReload: false`, version threaded, reasons = the `narrowResolverReasons(err.shape.reasons, err.shape.unsupportedKinds)`-derived degrade kinds (NOT `["no longer installable"]`), and `forceHint: true` → renders `● … (force-upgradable) {degrade reason}` + the update trailer.
    - Test (XSURF-03 bulk): the same candidate skipped by a bulk update produces the SAME row at `severity: "info"`.
    - Test (XSURF-03 structural stays decline): a structural `unavailable` manual decline (`err.shape.forceable !== true`) stays `(skipped) {no longer installable}` (force cannot help). The cascade/autoupdate force-path failure (update.ts:505-527) stays `partition: "failed"` — unaffected.
  </behavior>
  <action>
    XSURF-01 (install): take RESEARCH Note A — keep `classifyEntityShapeError` emitting `status: "unavailable"` (it classifies the THROWN shape) and branch at COMPOSE time. In composeInstallFailureMessage branch 3 (install.ts:1587-1590), when `entityErrorRow.status === "unavailable"` route into the compose helper; inside the helper split on `entityErrorRow.forceable === true` → return `{ status: "unsupported", name, reasons: entityErrorRow.reasons, …version, severity: "error", forceHint: true }`; else keep `{ status: "unavailable", …, severity: "error" }` (no forceHint). Both arms stay `severity: "error"` (D-70-02). Return type widens to `PluginUnavailableMessage | PluginUnsupportedMessage` (rename helper to e.g. `composeNotInstallableMessage` if clarity helps). In install.messaging.ts: add `"unsupported"` to `INSTALL_STATUSES` (line 39); add `| PluginUnsupportedMessage` to `InstallMsg` (import from notify.ts); add an `unsupported` arm to `INSTALL_RENDER` cloning the `unavailable` arm (lines 85-94) but `ICON_UNINSTALLABLE`→`ICON_UNSUPPORTED` and `"(unavailable)"`→`"(unsupported)"` (the trailer is composed centrally, not in the arm).

    XSURF-02 (info): in the `!isLocallyResolvable(parsedSource)` arm (info.ts:1046-1055) derive both fields from `resolved.state`: `const reasons = resolved.state === "unsupported" ? narrowUnsupportedKinds(resolved.unsupported) : narrowResolverNotes(resolved.notes);` and `status: resolved.state === "unsupported" ? "unsupported" : "unavailable"`. The union is already narrowed to `ResolvedPluginUnsupported | ResolvedPluginUnavailable` by the `resolved.state !== "installable"` guard, so `resolved.unsupported` is reachable. (Here `resolved` IS in scope — info.ts does not wrap it in a throwing try the way update.ts does.) No new import. No catalog flip (latent — A4).

    XSURF-03 (update): the decline producer is the `catch (err)` arm at update.ts:760-777 where `resolved` is OUT OF SCOPE — source the discriminant from the thrown `err.shape`, mirroring `classifyEntityShapeError` (install.ts:1657-1709). The throw is `requireInstallable(resolved, "update")` at update.ts:757, which constructs `PluginShapeError` `{ kind: "no-longer-installable", forceable: r.state === "unsupported", unsupportedKinds: r.state === "unsupported" ? r.unsupported : [] }` (resolver.ts:1170-1184). In the catch: when `err instanceof PluginShapeError && err.shape.forceable === true` (the force-upgradable decline), derive the degrade reasons via `narrowResolverReasons(err.shape.reasons, err.shape.unsupportedKinds)` (the SAME install-side helper at install.ts:1701, chosen for cross-surface byte parity — NOT `narrowUnsupportedKinds(resolved.unsupported)`, which is unreachable here) AND set a dedicated producer discriminant field on the `skipped` outcome marking the force-upgradable decline distinctly from the structural `no-longer-installable` case (do NOT rely on the reason string). When `err.shape.forceable !== true` (structural — force cannot help) keep `reasons: ["no longer installable"]` and the plain skipped path. In outcomeToCascadePluginMessage's `skipped` arm (update.ts:1675-1697): for the force-upgradable decline (keyed on that discriminant field), return `status: "force-upgradable"`, `name`, `scope: target.scope`, `version: outcome.fromVersion`, `reasons` (the degrade kinds), `severity: cardinality === "single" ? "warning" : "info"` (the SEV-04 split moved onto the status path — NOT keyed on the reason string), `needsReload: false`, `forceHint: true`. All OTHER skipped reasons keep `status: "skipped"` + `cascadeSkipSeverity(reasons, cardinality)` UNCHANGED (the `cascadeSkipSeverity` cardinality logic itself is untouched). In update.messaging.ts: add `"force-upgradable"` to `UPDATE_STATUSES` (line 30); add `| PluginForceUpgradableMessage` to `UpdateMsg` (import from notify.ts); add a `force-upgradable` arm to `UPDATE_RENDER` byte-identical to renderPluginRow's arm — `pluginRow(ICON_INSTALLED, p, mpScope, "(force-upgradable)", probe)`.

    Update tests: flip the install force-degradable byte fixtures (`⊘ (unavailable)` → `⊖ (unsupported)`, KEEP trailer); keep the non-path `{unsupported source}` / structural fixtures `⊘ (unavailable)`. Flip the targeted (update.test.ts:1041-1074, ~350-385) and bulk decline fixtures to the `force-upgradable` token + the derived degrade reason + trailer; ADD an explicit regression assertion that targeted `severity === "warning"` and bulk `severity === "info"` still hold (the SEV-04 split). Verify the autoupdate FORCE-05 structural case (update.test.ts:1005-1031) stays a decline. Comment/test-title anchors: requirement IDs (XSURF-NN) + decision IDs only — NO Phase/Plan/Wave.
  </action>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && npx tsc --noEmit -p tsconfig.json && node --test "tests/orchestrators/plugin/install.test.ts" "tests/orchestrators/plugin/update.test.ts" "tests/orchestrators/plugin/info.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - Install: force-degradable failure renders `⊖ (unsupported)` + install trailer; structural `unavailable` (incl. non-path `{unsupported source}`) stays `⊘ (unavailable)` no trailer.
    - Info: non-path arm derives status + reasons from `resolved.state`; existing non-path `unavailable` rows byte-unchanged.
    - Update: the decline discriminant is sourced from `err.shape.forceable` (NOT `resolved`, which is out of scope in the catch arm); targeted force-upgradable decline renders `● (force-upgradable) {degrade reason}` + update trailer at `warning`; bulk at `info`; reason is the `narrowResolverReasons`-derived list-consistent degrade kind, NOT `{no longer installable}`.
    - SEV-04 regression assertion present and passing (targeted=warning, bulk=info).
    - Structural/cascade declines unchanged; `cascadeSkipSeverity` cardinality logic untouched.
    - `tsc --noEmit` clean (no `TS2304` on `resolved` in the catch arm; the `satisfies` render maps are total).
  </acceptance_criteria>
  <done>All three producers emit the resolver-state-driven token; the update decline sources its discriminant from `err.shape`; install/update/info command tests pass with flipped byte forms and the SEV-04 split proven preserved.</done>
</task>

<task type="auto">
  <name>Task 3: byte-exact catalog + style-guide reconciliation, full green gate</name>
  <files>docs/output-catalog.md, docs/messaging-style-guide.md, tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - docs/output-catalog.md:444-456 (failure-unsupported-features) and 466-469 (failure-unavailable-structural — stays `⊘`)
    - docs/output-catalog.md:866-890 (decline-force-upgradable-targeted + skip-force-upgradable-bulk — currently `⊘ … (skipped) {no longer installable}`)
    - docs/output-catalog.md (the `list (force-upgradable)` inventory row for the SAME fixture — the byte-parity reference for the update-decline reason brace)
    - docs/output-catalog.md:11-12, 136-138 (glyph legend + status-token table surface columns)
    - docs/output-catalog.md ~1754 (autoupdate FORCE-05 structural decline — stays `(skipped) {no longer installable}`, do not conflate)
    - docs/messaging-style-guide.md:136 (install-failure severity/trailer prose)
    - tests/architecture/catalog-uat.test.ts:1673-1723 (the two force-upgradable decline fixture objects — `status: "skipped", reasons: ["no longer installable"]` today) and the catalog-uat byte-equality gate
  </read_first>
  <action>
    Reconcile the catalog-UAT fixtures in tests/architecture/catalog-uat.test.ts to the new producer outputs: `decline-force-upgradable-targeted` → `status: "force-upgradable", severity: "warning", needsReload: false, forceHint: true, version, reasons: [<degrade kind>]`; `skip-force-upgradable-bulk` → same with `severity: "info"`. Flip `failure-unsupported-features` (XSURF-01) to the `unsupported` row form if it is modeled as a fixture. In docs/output-catalog.md: flip `failure-unsupported-features` (444-456) `⊘ helper (unavailable) {…}` → `⊖ helper (unsupported) {…}`, KEEP the `Re-run with --force …` install trailer line, update prose ("the `unavailable` variant" → the `unsupported` variant); leave `failure-unavailable-structural` (466-469) `⊘ (unavailable)`. Flip `decline-force-upgradable-targeted` + `skip-force-upgradable-bulk` (866-890) `⊘ hello v1.0.0 (skipped) {no longer installable}` → `● hello v1.0.0 (force-upgradable) {<degrade reason matching list>}` + the new `Re-run with --force to update with the supported components.` trailer; preserve the targeted=warning summary-line prose and the bulk=info tally prose. Widen the glyph-legend / status-token surface columns (11-12, 136-138): `⊖`/`(unsupported)` now also covers the install-failure surface; `(force-upgradable)` now also covers the update-decline surface. In docs/messaging-style-guide.md:136 update the install-failure severity prose (force-degradable arm uses `(unsupported)` not `(unavailable)`, still `error` + trailer) and add `FORCE_UPDATE_HINT_TRAILER` to the frozen-DOC-contract list.

    CROSS-SURFACE BYTE-PARITY ASSERTION (non-blocking fold-in): add an explicit assertion to catalog-uat.test.ts that the `update`-decline `force-upgradable` reason brace is BYTE-IDENTICAL to the `list (force-upgradable)` reason brace for the SAME fixture (same plugin / same degrade kinds), not merely "a degrade kind". This is what justifies sourcing the update-decline reason via `narrowResolverReasons(err.shape.reasons, err.shape.unsupportedKinds)` (the install-path helper) rather than `narrowUnsupportedKinds`: the assertion must confirm the two helpers emit identical bytes for the fixture. If they do NOT emit identical bytes for the fixture, reconcile the helper choice (use whichever the `list` row uses) and surface the discrepancy in the SUMMARY rather than shipping a divergent brace. Run the full gate.
  </action>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && node --test "tests/architecture/catalog-uat.test.ts" "tests/architecture/notify-closed-set-locks.test.ts" && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - catalog-uat fixtures for the two force-upgradable declines use `status: "force-upgradable"` + the derived degrade reason + correct severities; the catalog-uat byte-equality gate passes.
    - A cross-surface byte-equality assertion proves the `update`-decline reason brace === the `list (force-upgradable)` reason brace for the same fixture (helper choice reconciled if they diverge).
    - output-catalog.md install-failure + update-decline blocks reflect the new tokens/trailers byte-exact; structural `⊘` blocks unchanged.
    - messaging-style-guide.md prose updated; new trailer listed as a frozen DOC contract.
    - `notify-closed-set-locks.test.ts` passes WITHOUT count edits.
    - `npm run check` is green (typecheck + lint + format:check + test + test:integration).
  </acceptance_criteria>
  <done>The byte-exact catalog/style-guide and the install/update notify tests reflect the reconciled cross-surface tokens; a cross-surface byte-equality assertion locks the update-decline reason brace to the `list` brace; `npm run check` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure render-token/reason wording change across three producers. No auth, input parsing, crypto, network, or data-handling surface is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Information disclosure | new/widened `forceHint?` fields on `PluginUnsupportedMessage` / `PluginForceUpgradableMessage` | accept | The fields are booleans carrying no `pluginRoot` or path data (NFR-7 discriminated-union containment unaffected); the trailer text interpolates no plugin/marketplace identifier. |
| T-73-02 | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (code-only first-party edit); RESEARCH Package Legitimacy Audit = N/A. No install task exists, so no slopcheck checkpoint required. |
| T-73-SC | Tampering | supply chain | accept | No dependency added or upgraded. |

NFRs preserved: NFR-5 (no-network — the update decline is a pre-resolve verdict, no fetch), NFR-7 (the `satisfies` render maps + discriminated unions stay total), NFR-10 (path containment untouched).
</threat_model>

<verification>
- `npx tsc --noEmit` clean — the `INSTALL_RENDER` / `UPDATE_RENDER` `satisfies` totality gates and the trailer-gate exhaustiveness are the primary compile-time proof, and prove the update catch arm has no `TS2304` (`resolved` out of scope) by sourcing the discriminant from `err.shape`.
- `node --test tests/shared/notify-v2.test.ts` — `⊖ (unsupported)` + install trailer and `● (force-upgradable)` + update trailer byte forms (and their no-trailer/byte-frozen counterparts).
- `node --test tests/orchestrators/plugin/{install,update,info}.test.ts` — producer-side byte flips + SEV-04 regression (targeted=warning / bulk=info).
- `node --test tests/architecture/{catalog-uat,notify-closed-set-locks}.test.ts` — catalog byte-equality reconciled; cross-surface byte-parity (update-decline brace === list brace) asserted; closed-set counts unchanged.
- `npm run check` — final green gate (typecheck + lint + format:check + test + test:integration).
</verification>

<success_criteria>
1. (XSURF-01) Install-failure surface renders `⊖ (unsupported)` for a force-installable plugin (not `⊘ (unavailable)`) with the SEV-02 `--force` hint preserved; structural `unavailable` stays `⊘` no trailer.
2. (XSURF-02) `info.ts` non-locally-resolvable arm derives status + reason source from `resolved.state`; existing non-path `unavailable` rows byte-unchanged.
3. (XSURF-03) Manual force-upgradable update decline sources its discriminant from `err.shape.forceable` (the catch arm — `resolved` is out of scope), renders the `force-upgradable` token with a force-aware reason matching `list` (NOT `{no longer installable}`) and an update-worded `--force` trailer; SEV-04 split (targeted=warning, bulk=info) preserved and asserted.
4. Byte-exact catalog/style-guide and install/update notify tests reconciled; the update-decline reason brace is byte-identical to the `list (force-upgradable)` brace for the same fixture; closed sets unchanged; `npm run check` green.
</success_criteria>

<output>
Create `.planning/phases/73-force-cross-surface-token-unification/73-01-SUMMARY.md` when done.
</output>
