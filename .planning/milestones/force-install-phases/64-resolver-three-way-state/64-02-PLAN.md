---
phase: 64-resolver-three-way-state
plan: 02
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - extensions/pi-claude-marketplace/shared/probe-classifiers.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - tests/orchestrators/plugin/cross-surface-reason-parity.test.ts
autonomous: true
requirements: [RSTATE-05]

must_haves:
  truths:
    - "Per-kind unsupported-component markers (e.g. lspServers → lsp) are derived from the typed unsupported component list via one shared helper, distinct from the structural-reason notes path (D-64-02, RSTATE-05)"
    - "The same shared helper drives list, info, and the install error surface, so a given unsupported plugin renders identical per-kind markers (including soft-dep markers) across all three surfaces (RSTATE-05)"
    - "Structural reasons (malformed hooks.json, source escape) remain on the notes/structural path on the unavailable arm and are NOT re-routed through the per-kind list helper (D-64-07, D-64-02)"
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/probe-classifiers.ts"
      provides: "Shared render-time helper mapping the unsupported string[] kind list to the closed REASON marker set"
      contains: "lsp"
    - path: "tests/orchestrators/plugin/cross-surface-reason-parity.test.ts"
      provides: "Cross-surface parity assertion that list/info/install render identical per-kind unsupported markers"
      contains: "parity"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/list.ts"
      to: "shared per-kind marker helper"
      via: "render-time derivation of unsupported markers"
      pattern: "probe-classifiers"
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts"
      to: "shared per-kind marker helper"
      via: "render-time derivation of unsupported markers"
      pattern: "probe-classifiers"
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      to: "shared per-kind marker helper"
      via: "collapse the third duplicate of the per-kind marker derivation (RSTATE-05 cross-surface parity)"
      pattern: "probe-classifiers"
---

<objective>
Introduce the single shared render-time helper (D-64-02) that derives per-kind
unsupported-component markers from the typed `unsupported: string[]` list, and route
`list`, `info`, and the `install` error surface through it so per-kind markers render
identically across every surface and (future) force state. Structural reasons stay on
the existing notes path on the `unavailable` arm — they are NOT re-derived from the list.

Purpose: Satisfy RSTATE-05 — per-kind reasons must be identical across `list` and `info`
(including soft-dep markers) and across all force states — by construction, collapsing the
three near-duplicate marker derivations into one sanctioned helper.

Output: One shared helper in `shared/probe-classifiers.ts`, three consumers re-wired to it,
the `install.ts` duplicate collapsed, and a cross-surface parity test.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-resolver-three-way-state/64-CONTEXT.md
@.planning/phases/64-resolver-three-way-state/64-RESEARCH.md
@.planning/phases/64-resolver-three-way-state/64-PATTERNS.md
@.planning/phases/64-resolver-three-way-state/64-01-SUMMARY.md
@docs/output-catalog.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared per-kind unsupported-marker helper + route list / info / install through it</name>
  <files>extensions/pi-claude-marketplace/shared/probe-classifiers.ts, extensions/pi-claude-marketplace/orchestrators/plugin/list.ts, extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/probe-classifiers.ts (narrowResolverNotes 87-123 — the existing notes→REASON mapping; header at line 9 marking this the sanctioned cross-orchestrator import surface)
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts (line 362 sharedNarrowResolverNotes(resolved.notes); the migrated state branch from Plan 64-01)
    - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts (lines 649-695 buildNotInstallablePathRowFields; 664 + 803 narrowResolverNotes; the migrated state branches from Plan 64-01)
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (narrowResolverReasons ~1687-1707 — the third duplicate; MANIFEST_FIELD_REASONS / lspServers→"lsp" map ~1633-1641)
    - .planning/phases/64-resolver-three-way-state/64-PATTERNS.md (Pattern F; probe-classifiers helper section; A2/OpenQ2 hooks-marker caveat)
    - .planning/phases/64-resolver-three-way-state/64-RESEARCH.md (Pattern 4 + Open Questions #2: marker family scope)
    - docs/output-catalog.md (byte forms for list/info unsupported-component reason markers)
  </read_first>
  <action>
    Add ONE shared render-time helper in `shared/probe-classifiers.ts` that maps the typed
    `unsupported: string[]` kind list to the closed REASON marker set (e.g. `"lspServers"` → `lsp`),
    with first-wins dedup matching the existing `narrowResolverNotes` semantics (WR-01). This is
    the D-64-02 marker family: per-kind markers come from the list, NOT from `notes` string-matching.

    Marker-family scope decision (resolving research A2 / Open Question #2): the `unsupported hooks`
    marker is a STRUCTURAL reason — a malformed/unsupported hooks.json routes to `unavailable`
    (D-64-07) and its reason stays in the `notes`/structural path via the existing `narrowResolverNotes`.
    Therefore the NEW list-based helper covers only the force-degradable per-kind markers carried on the
    `unsupported` arm's `unsupported[]` list (`lsp` and any future component kinds). Do NOT route the
    hooks-structural reason through the new list helper. Verify the resulting byte forms for the
    `list` and `info` unsupported-component rows are unchanged against docs/output-catalog.md.

    Route `list.ts` and `info.ts` to derive per-kind markers from the `unsupported` arm's list via the
    new helper (on the `unsupported` arm where the list exists), keeping the structural-reason rendering
    for the `unavailable` arm on the existing `narrowResolverNotes(notes)` path. Collapse the third
    duplicate in `install.ts` (`narrowResolverReasons`) into the shared helper so all three surfaces use
    one derivation — the cross-surface parity test (SURF-01, Task 2) is the safety net. Preserve the
    install error surface's existing closed `Reason` output bytes.

    Comments/test titles use decision/requirement IDs only (D-64-02, RSTATE-05, WR-01, SURF-01,
    HOOK-04/D-58-02 for the lspServers carve-out) — never "Phase 64".
  </action>
  <verify>
    <automated>npm run typecheck && node --test tests/orchestrators/plugin/list.test.ts tests/orchestrators/plugin/info.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `shared/probe-classifiers.ts` exports a new helper whose input is a string[] (the `unsupported` component-kind list), not the free-form `notes` array, and which emits the closed REASON marker set (maps `lspServers` → `lsp`).
    - `list.ts` and `info.ts` derive the per-kind unsupported markers (for the `unsupported` arm) through the shared helper; the `unavailable` arm's structural reasons still render via `narrowResolverNotes(notes)`.
    - `install.ts` no longer carries a standalone copy of the per-kind list→REASON mapping for the `unsupported`-list kinds; it imports/uses the shared helper (the structural notes-prefix narrowing for hooks may remain on its own path).
    - `npm run typecheck` exits 0 and `node --test tests/orchestrators/plugin/list.test.ts tests/orchestrators/plugin/info.test.ts` passes.
    - The `list`/`info` unsupported-component reason-row byte forms match docs/output-catalog.md (no token changes).
  </acceptance_criteria>
  <done>One shared list-based per-kind marker helper exists; list, info, and install all derive per-kind unsupported markers through it; structural reasons remain on the notes path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cross-surface per-kind marker parity test</name>
  <files>tests/orchestrators/plugin/cross-surface-reason-parity.test.ts</files>
  <behavior>
    - For a plugin resolving `unsupported` with a given per-kind component (e.g. an lspServers-bearing plugin), the per-kind marker rendered by `list` equals the marker rendered by `info` equals the marker emitted on the `install` error surface (RSTATE-05 cross-surface identity, SURF-01).
    - Soft-dep markers are included in the parity assertion.
    - A structural-`unavailable` plugin's structural reason renders identically across the surfaces via the notes path (regression guard that structural reasons were NOT re-routed through the list helper).
  </behavior>
  <read_first>
    - tests/orchestrators/plugin/cross-surface-reason-parity.test.ts (existing SURF-01 parity assertions — extend, do not replace the existing structural-parity coverage)
    - extensions/pi-claude-marketplace/shared/probe-classifiers.ts (the new shared helper from Task 1)
    - .planning/phases/64-resolver-three-way-state/64-PATTERNS.md (Pattern F)
    - .claude/rules/typescript-comments.md (test-title ID policy)
  </read_first>
  <action>
    Extend `tests/orchestrators/plugin/cross-surface-reason-parity.test.ts` with a case asserting that
    the per-kind unsupported marker (derived from the `unsupported` arm's list via the shared helper) is
    byte-identical across `list`, `info`, and the `install` error surface for the same fixture, including
    soft-dep markers (RSTATE-05). Keep the existing structural-reason parity assertions as the
    regression guard that structural reasons stay on the notes path. Test titles use `RSTATE-05` /
    `SURF-01` / `D-64-02` anchors — never "Phase 64".
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/cross-surface-reason-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/orchestrators/plugin/cross-surface-reason-parity.test.ts` passes.
    - The test file contains at least one assertion comparing the per-kind unsupported marker across `list`, `info`, and `install` for one fixture.
    - A test title references `RSTATE-05`; no test title contains "Phase 64".
    - `npm run check` is green at the plan boundary.
  </acceptance_criteria>
  <done>Cross-surface parity for per-kind unsupported markers is asserted; RSTATE-05 is verified; `npm run check` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolver `unsupported[]` list → render helper | The typed component-kind list crosses into a render-time mapping; the only risk is mis-mapping a kind to the wrong (or a structural) marker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-05 | Information disclosure (wrong reason rendered) | shared per-kind marker helper | mitigate | The helper emits only the closed REASON set; the cross-surface parity test (Task 2) and the unchanged docs/output-catalog.md byte forms guarantee no new or leaked token. Structural reasons stay on a separate path so a structural defect is never re-rendered as a benign per-kind marker. |
| T-64-06 | Tampering (marker drift across surfaces) | list / info / install duplication | mitigate | Collapsing three duplicate derivations into one shared helper removes the drift surface; SURF-01 parity test enforces identity. |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (pure in-repo refactor; RESEARCH "Package Legitimacy Audit: Not applicable"). |

No HIGH-severity unmitigated threats — this is a render-time consolidation; the resolver's NFR-7/NFR-10 compile guarantees from Plan 64-01 are untouched.
</threat_model>

<verification>
- `npm run typecheck` exits 0.
- `node --test tests/orchestrators/plugin/cross-surface-reason-parity.test.ts tests/orchestrators/plugin/list.test.ts tests/orchestrators/plugin/info.test.ts` passes.
- `npm run check` is green.
- `list`/`info` unsupported-component reason rows match docs/output-catalog.md byte forms (no token changes this phase).
</verification>

<success_criteria>
- ROADMAP success criterion 5 (per-kind unsupported-component reasons render identically across `list` and `info`, including soft-dep markers, and across all force states) is satisfied.
- RSTATE-05 covered.
- D-64-02 (render-time per-kind markers via one shared helper; no structured `{kind,reason}[]` on the resolver output) is realized.
</success_criteria>

<output>
Create `.planning/phases/64-resolver-three-way-state/64-02-SUMMARY.md` when done.
</output>
</content>
