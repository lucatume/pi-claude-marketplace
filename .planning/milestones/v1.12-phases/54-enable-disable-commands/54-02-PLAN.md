---
phase: 54-enable-disable-commands
plan: 02
type: execute
wave: 2
depends_on: ["54-01"]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts
  - extensions/pi-claude-marketplace/edge/router.ts
  - extensions/pi-claude-marketplace/edge/register.ts
  - extensions/pi-claude-marketplace/edge/completions/provider.ts
  - extensions/pi-claude-marketplace/edge/handlers/tools.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
  - tests/architecture/notify-types.test.ts
  - tests/architecture/notify-grammar-invariant.test.ts
  - tests/shared/notify-v2.test.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
  - tests/edge/handlers/plugin/enable-disable.test.ts
autonomous: true
requirements: [ENBL-01, ENBL-02, ENBL-03, ENBL-04]
must_haves:
  truths:
    - "User runs `/claude:plugin enable <plugin>@<marketplace>` and `enabled: true` is written to `claude-plugins.json` at the resolved scope through saveConfig"
    - "User runs `/claude:plugin disable <plugin>@<marketplace>` and `enabled: false` is written to the config file; Pi artefacts (skills/commands/agents/mcp) are removed via cascadeUnstagePlugin while the state record's `version`, `resolvedSource`, `compatibility`, `installedAt` are preserved (ENBL-02 pin)"
    - "User runs `/claude:plugin enable <plugin>@<marketplace>` on a previously-disabled plugin: the orchestrator reads the cached manifest (PI-2; no network), runs the 5-phase install ledger with `version: installed.version` (the pinned version), re-materializes artefacts, and writes the state record with non-empty resources arrays — ENBL-03 cache-only re-materialization"
    - "User runs `/claude:plugin enable <p>@<mp> --local` and the change writes to `claude-plugins.local.json` (creating it if absent); base `claude-plugins.json` mtime is unchanged (Pitfall 54-5)"
    - "list and info surfaces render a disabled plugin as `⊘ <name> [scope] (disabled)` — the NEW closed-set `(disabled)` PluginStatus token (D-54-01); structurally distinct from `(unavailable)` (ENBL-04 three-orthogonal-facts)"
    - "Idempotent enable/disable renders as a `(skipped)` row with `{already enabled}` / `{already disabled}` REASONS — both in BENIGN_REASONS so the row routes to `info` severity (UXG-02 / D-28-06 ladder reused)"
    - "Invalid config (CFG-03) aborts the operation BEFORE entering the cascade: emits `(failed) {invalid manifest}` carrying `path.basename(targetConfigPath)` (NEVER absolute path; T-53-02-02 information-disclosure mitigation reused from Phase 53); state.json mtime is UNCHANGED"
    - "Enable against a deleted/corrupted clone (manifest read ENOENT) aborts pre-ledger with `(failed) {source missing}` and emits a single notify; state and config UNCHANGED"
    - "orchestrators/plugin/enable-disable.ts imports NO platform/git / DEFAULT_GIT_OPS / refreshGitHubClone surface — the architectural gate (FORBIDDEN_TARGETS armed in Plan 01) is GREEN with the file present (NFR-5)"
    - "shared/notify.ts: PLUGIN_STATUSES length is 16 (was 15), STATUS_TOKENS length is 22 (was 21), REASONS length is 31 (was 29 — adds `already enabled` + `already disabled`), BENIGN_REASONS contains both new members"
    - "docs/output-catalog.md gains `## /claude:plugin enable` and `## /claude:plugin disable` H2 sections AND a `disabled` inventory row state under list/info; tests/architecture/catalog-uat.test.ts FIXTURES contains matching paired entries; tests/architecture/notify-grammar-invariant.test.ts has a new subject-first proof for the `(disabled)` row"
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts"
      provides: "setPluginEnabled(opts) orchestrator — single function parameterized by `enable: boolean`, mirrors setMarketplaceAutoupdate shape; composes resolveCrossScopePluginTarget + withStateGuard(CFG-03 abort + cascadeUnstagePlugin OR runPhases install-ledger) + saveConfig + single terminal notify"
      exports: ["setPluginEnabled", "EnableDisablePluginOptions"]
    - path: "extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts"
      provides: "makeEnableDisableHandler(pi, enable: boolean) edge factory — mirrors makeAutoupdateHandler shape; parses `<plugin>@<marketplace>` + `--scope` via parseRequiredPluginMarketplaceRef and `--local` via a local scan; rejects unknown flags with notifyUsageError"
      exports: ["makeEnableDisableHandler"]
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "(disabled) PluginStatus + PluginDisabledMessage variant + renderer arm (ICON_UNINSTALLABLE glyph reused) + (already enabled) / (already disabled) REASONS + BENIGN_REASONS additions"
      contains: "PluginDisabledMessage"
    - path: "extensions/pi-claude-marketplace/edge/router.ts"
      provides: "SubcommandHandlers.enable + .disable + TOP_LEVEL_SUBCOMMANDS members + TOP_LEVEL_USAGE entries + switch arms"
    - path: "extensions/pi-claude-marketplace/edge/register.ts"
      provides: "enable: makeEnableDisableHandler(pi, true) + disable: makeEnableDisableHandler(pi, false) wiring"
    - path: "extensions/pi-claude-marketplace/edge/completions/provider.ts"
      provides: "PluginRefMode extended with `enable` + `disable` (installed-plugin completion mode); pluginRefBranchConfig switch arms"
    - path: "docs/output-catalog.md"
      provides: "## /claude:plugin enable + ## /claude:plugin disable H2 sections; list/info disabled inventory row catalog state"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "FIXTURES entries for new catalog states (enable-fresh, disable-fresh, enable-idempotent, disable-idempotent, enable-source-missing, disable-invalid-config, list-disabled-inventory, info-disabled-inventory)"
    - path: "tests/architecture/notify-types.test.ts"
      provides: "Length-lock bumps (_l1 15→16, _l1s 21→22, _l4 29→31); PluginDisabledMessage shape proof; negative-presence proofs (_NoCauseOnDisabled, _NoReasonsOnDisabled, _NoDependenciesOnDisabled)"
    - path: "tests/architecture/notify-grammar-invariant.test.ts"
      provides: "Subject-first proof for the new (disabled) row"
    - path: "tests/shared/notify-v2.test.ts"
      provides: "Byte-equality tests for the new (disabled) inventory row + (already enabled)/(already disabled) skipped rows + enable installed-cascade + disable terminal row"
    - path: "tests/orchestrators/plugin/enable-disable.test.ts"
      provides: "Bodies for the 10 ENBL behaviors (Wave 0 skipped scaffolds from Plan 01 are flipped to active tests)"
    - path: "tests/edge/handlers/plugin/enable-disable.test.ts"
      provides: "Bodies for the 5 edge USAGE/parse/forward behaviors (Wave 0 skipped scaffolds from Plan 01 are flipped to active tests)"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts::setPluginEnabled"
      to: "extensions/pi-claude-marketplace/persistence/config-io.ts::saveConfig"
      via: "atomic config write-back inside withStateGuard closure"
      pattern: "saveConfig\\("
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts::setPluginEnabled"
      to: "extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts::cascadeUnstagePlugin"
      via: "disable branch reuses uninstall's cascade (skills → commands → agents → mcp)"
      pattern: "cascadeUnstagePlugin\\("
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts::setPluginEnabled"
      to: "extensions/pi-claude-marketplace/transaction/phase-ledger.ts::runPhases"
      via: "enable branch reuses install's 5-phase ledger with version pinned from state record"
      pattern: "runPhases\\("
    - from: "extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts::makeEnableDisableHandler"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts::setPluginEnabled"
      via: "edge factory forwards parsed options to the orchestrator"
      pattern: "setPluginEnabled\\("
    - from: "extensions/pi-claude-marketplace/shared/notify.ts::PLUGIN_STATUSES"
      to: "extensions/pi-claude-marketplace/shared/notify.ts::renderPluginRow"
      via: "new `disabled` case arm + new PluginDisabledMessage variant in PluginNotificationMessage union"
      pattern: "case \"disabled\""
---

<objective>
Plan 02 lands the COMPLETE user-visible Phase 54 surface in ONE atomic commit per Pitfall 54-2 / atomic-supersession discipline (v1.3 / v1.10 / v1.11 / Phase 53 5402f56 lineage):
- The `setPluginEnabled` orchestrator (single function parameterized by `enable: boolean`).
- The `makeEnableDisableHandler` edge factory + `--local` flag parsing.
- The router + register + completion provider wiring.
- The NEW closed-set `(disabled)` `PluginStatus` token + `PluginDisabledMessage` variant + renderer arm (D-54-01 LOCKED — `⊘ <name> [scope] (disabled)`).
- The `(already enabled)` / `(already disabled)` `REASONS` additions (BENIGN — route idempotent rows to info via UXG-02 ladder).
- The `docs/output-catalog.md` enable + disable H2 sections + list/info `disabled` inventory state.
- The `tests/architecture/catalog-uat.test.ts` FIXTURES extension.
- The length-lock numeric updates in `tests/architecture/notify-types.test.ts` (`_l1` 15→16, `_l1s` 21→22, `_l4` 29→31) + shape proofs + negative-presence proofs.
- The `tests/architecture/notify-grammar-invariant.test.ts` subject-first proof for `(disabled)`.
- The exhaustive-switch arms in `edge/handlers/tools.ts` and `orchestrators/plugin/list.ts::scopeOf`.
- The flip of the Wave 0 RED scaffolds to GREEN bodies (Plan 01 → Plan 02 hand-off).
- Strip `@ts-expect-error` markers on the Wave 0 import sentinels (the source files now exist).

Purpose: Plan 01 left the planner ready and the network gate armed. Plan 02 brings the user-facing surface alive in lockstep: any partial split would either break the discriminated-union exhaustiveness gate (typecheck RED), the closed-set length-lock (`_l1`/`_l1s`/`_l4` RED), the catalog-uat byte-equality gate (forward-walk RED), or the catalog-uat orphan gate (inverse-walk RED).

Output: ENBL-01..04 fully closed; D-54-01 token landed; cache-only enable proven structurally network-free by the FORBIDDEN_TARGETS gate Plan 01 armed; ENBL-04 three-orthogonal-facts distinction enforced both visually (token differs) and structurally (discriminated union variants differ).

Atomic commit envelope: ~15 files in ONE commit. Mirrors Phase 53 Plan 02's 19-file commit shape (5402f56).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-enable-disable-commands/54-CONTEXT.md
@.planning/phases/54-enable-disable-commands/54-RESEARCH.md
@.planning/phases/54-enable-disable-commands/54-PATTERNS.md
@.planning/phases/54-enable-disable-commands/54-VALIDATION.md
@.planning/phases/54-enable-disable-commands/54-01-SUMMARY.md

@extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts
@extensions/pi-claude-marketplace/edge/handlers/marketplace/autoupdate.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
@extensions/pi-claude-marketplace/transaction/with-state-guard.ts
@extensions/pi-claude-marketplace/transaction/phase-ledger.ts
@extensions/pi-claude-marketplace/persistence/config-io.ts
@extensions/pi-claude-marketplace/persistence/config-merge.ts
@extensions/pi-claude-marketplace/persistence/locations.ts
@extensions/pi-claude-marketplace/persistence/state-io.ts
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/edge/router.ts
@extensions/pi-claude-marketplace/edge/register.ts
@extensions/pi-claude-marketplace/edge/completions/provider.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts
@extensions/pi-claude-marketplace/edge/handlers/tools.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
@docs/output-catalog.md
@tests/architecture/catalog-uat.test.ts
@tests/architecture/notify-types.test.ts
@tests/architecture/notify-grammar-invariant.test.ts
@tests/architecture/no-orchestrator-network.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land the (disabled) closed-set token + variant + renderer + catalog + FIXTURES + length-locks atomically (the notify.ts / output-catalog.md / catalog-uat / notify-types / notify-grammar-invariant lockstep)</name>
  <files>
    extensions/pi-claude-marketplace/shared/notify.ts,
    extensions/pi-claude-marketplace/edge/handlers/tools.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/list.ts,
    docs/output-catalog.md,
    tests/architecture/catalog-uat.test.ts,
    tests/architecture/notify-types.test.ts,
    tests/architecture/notify-grammar-invariant.test.ts,
    tests/shared/notify-v2.test.ts
  </files>
  <read_first>
    extensions/pi-claude-marketplace/shared/notify.ts,
    extensions/pi-claude-marketplace/edge/handlers/tools.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/list.ts,
    docs/output-catalog.md,
    tests/architecture/catalog-uat.test.ts,
    tests/architecture/notify-types.test.ts,
    tests/architecture/notify-grammar-invariant.test.ts,
    tests/shared/notify-v2.test.ts,
    .planning/phases/53-pure-reconcile-planner-dry-run-preview/53-02-SUMMARY.md
  </read_first>
  <behavior>
    - PLUGIN_STATUSES tuple gains `"disabled"` (length 15 → 16).
    - STATUS_TOKENS tuple gains `"disabled"` (length 21 → 22).
    - REASONS tuple gains `"already enabled"` AND `"already disabled"` (length 29 → 31, in alphabetical order if the tuple is sorted — verify by reading lines 72-102; otherwise append at the end per the file's existing convention).
    - BENIGN_REASONS includes `"already enabled"` AND `"already disabled"` so a `(skipped)` row carrying ONLY those reasons routes to `info` severity via computeSeverity (UXG-02 / D-28-06 BENIGN_REASONS first-match ladder; mirror the existing `already autoupdate` / `already no autoupdate` precedent).
    - `PluginDisabledMessage` variant interface exists with shape: `{ readonly status: "disabled"; readonly name: string; readonly version?: string; readonly scope?: Scope; }`. NO `reasons`, NO `cause`, NO `dependencies`, NO `rollbackPartial`. Mirrors `PluginUninstalledMessage` shape verbatim except for the status literal.
    - `PluginNotificationMessage` discriminated union includes `PluginDisabledMessage` as a member.
    - `renderPluginRow` switch gains a `case "disabled":` arm that emits `joinTokens([ICON_UNINSTALLABLE, p.name, renderScopeBracket(p.scope, mpScope), renderVersion(p.version), "(disabled)", composeReasons(undefined, false, false, probe)])` — subject-first grammar; `⊘` glyph reused from `will disable`; orphan-fold scope bracket honoured.
    - `edge/handlers/tools.ts` exhaustive switches (3 sites at lines 165, 190, 233 — the 3 different switches over `PluginStatus`) gain `case "disabled":` arms mirroring the closest semantic peer (`uninstalled` for inventory-style enumeration; `unavailable` if the site treats disabled-from-the-tools-perspective as not-currently-callable).
    - `orchestrators/plugin/list.ts::scopeOf` (line 731) gains a `case "disabled":` arm returning `p.scope` (mirror the `installed`/`uninstalled` arms — disabled rows carry an explicit `scope?` since they are NOT in the SNM-11 carve-out).
    - `docs/output-catalog.md` gains TWO new H2 sections — `## /claude:plugin enable` and `## /claude:plugin disable` — each with the catalog-state subsections from the behavior in Task 2 (enable-fresh, enable-idempotent, enable-source-missing, enable-marketplace-not-added, enable-invalid-config / disable-fresh, disable-idempotent, disable-marketplace-not-added, disable-invalid-config). The list and info sections gain a `disabled` inventory row state showing `⊘ foo-plugin [user] v1.2.3 (disabled)`.
    - `tests/architecture/catalog-uat.test.ts` FIXTURES contains matching entries paired with each catalog state added to `docs/output-catalog.md` — at least 10 new fixtures total. Inverse-walk (FIXTURES → catalog orphan detection) must report zero orphans; forward-walk (byte-equality) GREEN.
    - `tests/architecture/notify-types.test.ts`: `_Assert_PluginStatusesLen` underlying literal updates 15 → 16; `_Assert_StatusTokensLen` 21 → 22; `_Assert_ReasonsLen` 29 → 31. Variant shape proof block added for `PluginDisabledMessage` (mirroring the `PluginUninstalledMessage` block). Negative-presence proofs added: `_NoCauseOnDisabled`, `_NoReasonsOnDisabled`, `_NoDependenciesOnDisabled`, `_NoRollbackPartialOnDisabled`.
    - `tests/architecture/notify-grammar-invariant.test.ts`: a new fixture/proof for the `(disabled)` row asserting subject-first — `name` appears in the rendered line BEFORE the `(disabled)` token; status token NEVER precedes the subject (output-row-grammar-subject-first memory).
    - `tests/shared/notify-v2.test.ts` gains byte-equality tests for: the `(disabled)` inventory row (with and without `version`; with and without orphan-fold scope), and the `(skipped) {already enabled}` + `(skipped) {already disabled}` rows.
  </behavior>
  <action>
    This task is ATOMIC: every byte landed below ships in the SAME commit as Task 2. Land all of Task 1's edits, then immediately land Task 2's edits, then commit ONCE. There is NO valid intermediate state between Task 1 and Task 2 — the test suite gates would RED.

    In `extensions/pi-claude-marketplace/shared/notify.ts`:
    1. Add `"disabled"` to `PLUGIN_STATUSES` (line 281-297). Preserve the tuple's existing alphabetical/logical ordering — verify by reading the file.
    2. Add `"disabled"` to `STATUS_TOKENS` (line 162-184). Same ordering discipline.
    3. Add `"already enabled"` AND `"already disabled"` to `REASONS` (line 72-102). Same ordering discipline.
    4. Add `"already enabled"` AND `"already disabled"` to the existing `BENIGN_REASONS` set (line 129-134 area). Mirror the `already autoupdate` / `already no autoupdate` placement.
    5. Add the `PluginDisabledMessage` interface immediately after `PluginUninstalledMessage` (the closest analog at lines 448-453). Shape: status literal `"disabled"`, `name: string`, optional `version?: string`, optional `scope?: Scope`. No other fields. Docstring cites D-54-01 + ENBL-04 (NOT pre-decisional narrative; per source-comment-cleanup-policy).
    6. Add `PluginDisabledMessage` to the `PluginNotificationMessage` discriminated union (lines 637-641 area).
    7. Add `case "disabled":` arm in `renderPluginRow` (after the `unavailable` arm, before the `upgradable` arm — verify by reading lines 1596-1625). Body: see behavior block above. Subject-first; `ICON_UNINSTALLABLE` glyph; `renderScopeBracket(p.scope, mpScope)` for proper orphan-fold; `composeReasons(undefined, false, false, probe)` (the inventory row carries no reasons — soft-dep probe is still threaded for symmetry per the existing pattern).
    8. Update the assertNever default arm of `renderPluginRow` — no change to the assertNever call itself; the new arm above it satisfies exhaustiveness.

    In `extensions/pi-claude-marketplace/edge/handlers/tools.ts`:
    9. Add `case "disabled":` arms at all 3 switch sites (lines 165, 190, 233 — the file's 3 switches over `PluginStatus`). For each site, determine the closest semantic peer from the surrounding arms by reading the function's purpose (e.g., a switch that maps status → "is the plugin currently usable from a tool?" should map `disabled` → false like `unavailable`; a switch that maps status → tool-display name should map `disabled` → `"disabled"` or similar). Verify with the test suite — typecheck RED if any switch loses exhaustiveness.

    In `extensions/pi-claude-marketplace/orchestrators/plugin/list.ts`:
    10. Add `case "disabled":` arm to `scopeOf` (line 731 area). Return `p.scope` — disabled rows carry an explicit scope (the carve-out at SNM-11 applies only to `available` / `unavailable`).

    In `docs/output-catalog.md`:
    11. Add the H2 section `## /claude:plugin enable` with the following catalog states (verify exact subsection headers by reading the `## /claude:plugin marketplace autoupdate` section as a template):
        - `enable-fresh`: A previously-disabled plugin is re-materialized. Body shows the marketplace header + `● foo-plugin (installed) ⟶ /reload`. RELOAD_HINT_TRAILER fires (status is `installed`).
        - `enable-idempotent`: An already-enabled plugin. Body shows `⊘ foo-plugin (skipped) {already enabled}`. NO `/reload` trailer (skipped is not a state-changer). INFO severity.
        - `enable-source-missing`: The cached clone is gone. Body shows `⊘ foo-plugin (failed) {source missing}`. ERROR severity. Includes `Error:` summary line per GRAM-01.
        - `enable-marketplace-not-added`: The marketplace is unknown in target scope. Body shows the standalone `MarketplaceNotAddedMessage` variant `⊘ <mp-name> [<scope?>] (failed) {not added}`. ERROR severity.
        - `enable-invalid-config`: CFG-03 abort. Body shows `⊘ foo-plugin (failed) {invalid manifest}` with the surfaced summary citing `path.basename(targetConfigPath)` — NEVER an absolute path (T-53-02-02 mitigation).
    12. Add the H2 section `## /claude:plugin disable` with parallel states (`disable-fresh`, `disable-idempotent`, `disable-marketplace-not-added`, `disable-invalid-config`). The `disable-fresh` body shows `⊘ foo-plugin (uninstalled) ⟶ /reload` — disable uses the existing `(uninstalled)` PluginStatus for the action-row (state-changer; trailer fires); the inventory token `(disabled)` is for LIST/INFO only (Open Question #2 in RESEARCH.md resolved: inventory token vs cascade-row token are different surfaces).
    13. Extend the existing `## /claude:plugin list` section with a `disabled-inventory` catalog state showing `⊘ foo-plugin [user] v1.2.3 (disabled)` under the marketplace header. Extend `## /claude:plugin info` analogously.

    In `tests/architecture/catalog-uat.test.ts`:
    14. Add FIXTURES entries paired with EACH new catalog state above. Mirror the existing FIXTURES-entry shape (read the file's existing entries for the autoupdate command for the canonical pattern). Verify there are zero orphans on the inverse walk (every FIXTURES entry has a matching catalog state, AND every catalog state has a matching FIXTURES entry).

    In `tests/architecture/notify-types.test.ts`:
    15. Line 139: change `extends 15` to `extends 16`. Verify the underlying tuple matches.
    16. Line 152: change `extends 21` to `extends 22`.
    17. Line 854: change `extends 29` to `extends 31`.
    18. Add a variant-shape proof block for `PluginDisabledMessage` (mirror the `_AssertPluginUninstalledMessageShape` block exactly — same fields, different status literal).
    19. Add negative-presence proofs (`_NoCauseOnDisabled`, `_NoReasonsOnDisabled`, `_NoDependenciesOnDisabled`, `_NoRollbackPartialOnDisabled`) mirroring the existing negative-presence proofs (`_NoCauseOnUninstalled` etc.).

    In `tests/architecture/notify-grammar-invariant.test.ts`:
    20. Add a fixture + proof for the `(disabled)` row asserting subject-first byte placement (`name` appears before `(disabled)`; status token NEVER precedes the subject).

    In `tests/shared/notify-v2.test.ts`:
    21. Add byte-equality tests for: `(disabled)` row (with version; without version; with orphan-fold scope; without scope); `(skipped) {already enabled}` row; `(skipped) {already disabled}` row. Mirror the existing byte-equality test shape.

    Do NOT touch the orchestrator / edge handler / router / register / completion / orchestrator-test / edge-test files in this task — those bytes are Task 2's. Tasks 1 and 2 commit TOGETHER atomically.
  </action>
  <verify>
    <automated>npm test -- tests/architecture/catalog-uat.test.ts tests/architecture/notify-types.test.ts tests/architecture/notify-grammar-invariant.test.ts tests/shared/notify-v2.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/architecture/catalog-uat.test.ts` GREEN — forward-walk byte-equality AND inverse-walk orphan detection both pass; 8+ new FIXTURES entries added with matching catalog states.
    - `node --test tests/architecture/notify-types.test.ts` GREEN — length locks now read 16 / 22 / 31; variant + negative-presence proofs pass.
    - `node --test tests/architecture/notify-grammar-invariant.test.ts` GREEN — `(disabled)` row passes the subject-first invariant.
    - `node --test tests/shared/notify-v2.test.ts` GREEN — all new byte-equality tests pass.
    - `grep -nP "^\\s*\"disabled\"" extensions/pi-claude-marketplace/shared/notify.ts | wc -l` returns at least 2 (PLUGIN_STATUSES + STATUS_TOKENS entries).
    - `grep -nP "^\\s*\"already (enabled|disabled)\"" extensions/pi-claude-marketplace/shared/notify.ts | wc -l` returns at least 2 (REASONS entries).
    - `grep -nP "case \"disabled\":" extensions/pi-claude-marketplace/shared/notify.ts` returns at least 1 hit (the renderer arm).
    - `grep -nP "## /claude:plugin (en|dis)able" docs/output-catalog.md` returns 2 hits (the two new H2 sections).
    - `pre-commit run --files <all-changed>` GREEN.
  </acceptance_criteria>
  <done>
    The closed-set + renderer + catalog + FIXTURES + length-locks lockstep is in place. Every drift gate (catalog-uat / notify-types / notify-grammar-invariant / notify-v2) is GREEN. Task 2's orchestrator + edge handler land in the SAME commit; the commit cannot land until BOTH tasks are complete simultaneously.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land the orchestrator + edge handler + router/register/completion wiring + Wave 0 test flips (the user-visible surface that consumes Task 1's token)</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts,
    extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts,
    extensions/pi-claude-marketplace/edge/router.ts,
    extensions/pi-claude-marketplace/edge/register.ts,
    extensions/pi-claude-marketplace/edge/completions/provider.ts,
    tests/orchestrators/plugin/enable-disable.test.ts,
    tests/edge/handlers/plugin/enable-disable.test.ts
  </files>
  <read_first>
    extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts,
    extensions/pi-claude-marketplace/edge/handlers/marketplace/autoupdate.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/install.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts,
    extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts,
    extensions/pi-claude-marketplace/transaction/with-state-guard.ts,
    extensions/pi-claude-marketplace/persistence/config-io.ts,
    extensions/pi-claude-marketplace/persistence/config-merge.ts,
    extensions/pi-claude-marketplace/persistence/locations.ts,
    extensions/pi-claude-marketplace/edge/router.ts,
    extensions/pi-claude-marketplace/edge/register.ts,
    extensions/pi-claude-marketplace/edge/completions/provider.ts,
    extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts,
    tests/orchestrators/plugin/enable-disable.test.ts,
    tests/edge/handlers/plugin/enable-disable.test.ts,
    tests/orchestrators/marketplace/autoupdate.test.ts,
    tests/orchestrators/plugin/uninstall.test.ts,
    tests/edge/handlers/marketplace/autoupdate.test.ts,
    tests/edge/handlers/plugin/uninstall.test.ts
  </read_first>
  <behavior>
    - `setPluginEnabled(opts)` orchestrator at `extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts`:
      - Accepts `EnableDisablePluginOptions = { ctx, pi, cwd, marketplace, plugin, enable, scope?, local? }`.
      - Resolves target scope via `resolveCrossScopePluginTarget` (orchestrators/plugin/shared.ts:132). On `marketplace-absent` or `other-scope`: emits standalone `MarketplaceNotAddedMessage` and returns.
      - Computes `targetConfigPath = opts.local === true ? locations.configLocalJsonPath : locations.configJsonPath`.
      - Enters `withStateGuard(locations, async (state) => { ... })`. INSIDE the closure (Pitfall 54-1; A6-confirmed lock semantics):
        - `loadConfig(targetConfigPath)`. If `invalid`: emit `(failed) {invalid manifest}` carrying `path.basename(targetConfigPath)` (T-53-02-02 mitigation; reuses the Phase 53 preview pattern); state UNTOUCHED; config UNTOUCHED; return.
        - Idempotency: read `state.marketplaces[mp].plugins[plugin]`; compute `currentlyDisabled = isRecordedButDisabled(installed)` (defined in `orchestrators/reconcile/plan.ts` — the Plan 01 helper; export it from `reconcile/plan.ts` if needed OR duplicate the 4-AND check locally and link via a docstring citing the planner predicate). If `currentlyDisabled === !enable`: emit `(skipped) {already enabled}` or `(skipped) {already disabled}` (BENIGN reasons → info severity); state and config UNTOUCHED; return.
        - Enable branch: read `state.marketplaces[mp]`. Call `loadMarketplaceManifest(mp.manifestPath)` (PI-2 cached read, no network). If ENOENT or read fails: emit `(failed) {source missing}` (pre-ledger abort per Open Question #4); state UNTOUCHED; config UNTOUCHED; return. Find the entry: `manifest.plugins.find(e => e.name === plugin)`. If undefined: emit `(failed) {not in manifest}`. Resolve via `resolveStrict` + `requireInstallable`. Build `InstallCtx` with `version: installed.version` (PRESERVE the pin — Pitfall 54-4; NEVER call `resolvePluginVersion`). Run `runPhases(phases, ctxLocal)` (reuse the literal 5-phase array from install.ts:674; do NOT re-derive). On failure: throw to unwind the ledger; on success: state record is updated with non-empty resources by the statePhase.
        - Disable branch: `cascadeUnstagePlugin(plugin, marketplace, locations, installed)` (reused from orchestrators/marketplace/shared.ts). On failure: throw to surface as a structured `(failed)` row via the outer catch. On success: mutate `state.marketplaces[mp].plugins[plugin]` to reset `resources.{skills,prompts,agents,mcpServers}` to `[]` and bump `updatedAt` to `new Date().toISOString()`. PRESERVE `version`, `resolvedSource`, `compatibility`, `installedAt` (ENBL-02 version pin).
        - Config write-back: build the patched ScopeConfig as `{ ...current, schemaVersion: 1, plugins: { ...(current.plugins ?? {}), [`${plugin}@${marketplace}`]: { ...(current.plugins?.[key] ?? {}), enabled: enable } } }`. Call `saveConfig(targetConfigPath, patched, locations.scopeRoot)` — the SOLE sanctioned writer per SPLIT-02 (architecture test allow-list verified in Plan 51-03).
      - `withStateGuard` saves state.json on no-throw exit.
      - ONE terminal `notify(ctx, pi, { marketplaces: [{ name: marketplace, scope, plugins: [<row>] }] })` per IL-2 — the row is a `PluginInstalledMessage` (enable success), `PluginUninstalledMessage` (disable success — re-using existing tokens; D-54-01's `(disabled)` token is for LIST/INFO inventory, NOT cascade rows), or a `PluginSkippedMessage` (idempotent case).
      - Imports ZERO platform/git surface; FORBIDDEN_TARGETS gate stays GREEN (verified by Plan 01's network test).
    - `makeEnableDisableHandler(pi, enable)` edge factory at `extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts`:
      - Returns `(args, ctx) => Promise<void>`.
      - Parses `<plugin>@<marketplace>` + `--scope` via `parseRequiredPluginMarketplaceRef` (verified at `edge/handlers/plugin/shared.ts:114`).
      - Parses `--local` via a local scan over the args string AFTER `parseCommandArgs` strips the `--scope` flag. Local scan: split args by whitespace, look for `--local` boolean, reject unknown long flags via `notifyUsageError`.
      - On parse failure or unknown flag: `notifyUsageError({ message, usage })`; return.
      - On success: call `setPluginEnabled({ ctx, pi, cwd: ctx.cwd, marketplace, plugin, enable, ...(scope && {scope}), ...(local && {local: true}) })`.
    - `edge/router.ts`:
      - `SubcommandHandlers` interface gains `enable` + `disable` method fields.
      - `TOP_LEVEL_SUBCOMMANDS` tuple gains `"enable"` + `"disable"`.
      - `TOP_LEVEL_USAGE` gains usage lines: `enable <plugin>@<marketplace> [--scope user|project] [--local]` + `disable <plugin>@<marketplace> [--scope user|project] [--local]`.
      - Switch in the main routing function gains `case "enable":` + `case "disable":` arms forwarding to the handlers map.
    - `edge/register.ts`:
      - Imports `makeEnableDisableHandler` and adds two wire entries: `enable: makeEnableDisableHandler(pi, true)` + `disable: makeEnableDisableHandler(pi, false)`.
    - `edge/completions/provider.ts`:
      - `PluginRefMode` union gains `"enable"` and `"disable"`.
      - `pluginRefBranchConfig` switch gains `case "enable":` and `case "disable":` arms returning the installed-plugin completion config (same shape as `case "uninstall":` since both target installed plugins).
    - `tests/orchestrators/plugin/enable-disable.test.ts`:
      - All `test.skip(...)` blocks from Plan 01 flip to `test(...)` with real bodies covering: ENBL-01 base + --local config write-back; ENBL-02 disable preserves version pin + resources reset; ENBL-02 reconcile no-op after disable; ENBL-03 enable from cache (mocked loadMarketplaceManifest, NO network); ENBL-03 version pin preserved; ENBL-03 missing clone → `(failed) {source missing}`; idempotency arms; CFG-03 invalid-config abort with `path.basename`; --local creates `.local.json` + leaves base mtime unchanged.
      - `@ts-expect-error` markers on the Plan-01 import sentinels are STRIPPED (source files now exist).
    - `tests/edge/handlers/plugin/enable-disable.test.ts`:
      - All `test.skip(...)` blocks flip to `test(...)` with real bodies: missing positional → USAGE; malformed ref → USAGE; unknown flag → USAGE; `--local` forwarded; `--scope` forwarded.
      - `@ts-expect-error` markers stripped.
  </behavior>
  <action>
    Create `extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts`:
    1. Top-of-file header comment: short purpose statement + decision-ID references (D-54-01) + requirement IDs (ENBL-01..04). Per source-comment-cleanup-policy: no historical narrative; no v1 lineage; cite phase + plan + decisions only.
    2. Define `EnableDisablePluginOptions` interface (see behavior block).
    3. Implement `setPluginEnabled(opts)` per the behavior contract. Use `withStateGuard` for the lock; `loadConfig` inside the closure; `cascadeUnstagePlugin` for disable; the literal `runPhases([skillsPhase, commandsPhase, agentsPhase, mcpPhase, statePhase], ctxLocal)` import for enable. The phase definitions are INSIDE install.ts as closures over the InstallCtx — DO NOT duplicate them; instead, factor a thin reuse seam: read install.ts to determine whether the phase array is already exported OR whether you must extract a helper. Recommended approach: extract `buildInstallPhases(opts): readonly Phase<InstallCtx>[]` as a NEW exported function in install.ts (the phases close over locations / cwd / marketplace / plugin / resolved / version / pluginDataDir — pass these as opts). The setPluginEnabled enable branch calls `buildInstallPhases` with `version: installed.version`. If extracting the phases is too invasive, the fallback is to call the existing `installPlugin` orchestrator with a `skipManifestRefresh: true` flag — but this requires plumbing a new option through install's surface and is a worse refactor. Verify the extract-phases approach during execution.
    4. The `isRecordedButDisabled` predicate from Plan 01's `reconcile/plan.ts` is private to that file. EXPORT it from `reconcile/plan.ts` for reuse here (the predicate is pure and exporting it does NOT couple the orchestrator to the planner — it's a one-way read of state). Alternative: duplicate the 4-AND check locally and add a docstring linking to the planner predicate. Recommendation: export from planner since the rule is the same architectural fact (Phase 54 + Phase 55 will both consume it). Update Plan 01's file accordingly via Plan 02's commit (a single-line change: `function isRecordedButDisabled` → `export function isRecordedButDisabled`).
    5. The terminal `notify(...)` call: build the `MarketplaceNotificationMessage` carrying ONE plugin row per the behavior block. Enable success uses `PluginInstalledMessage` (the existing variant; `(installed)` token; state-changer → trailer fires). Disable success uses `PluginUninstalledMessage` (existing variant; `(uninstalled)` token). Idempotent skip uses `PluginSkippedMessage` carrying `reasons: ["already enabled"]` or `["already disabled"]`.

    Create `extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts`:
    6. Mirror the `edge/handlers/marketplace/autoupdate.ts` shape verbatim (verified at lines 1-55). Substitute the orchestrator import, the usage strings, and the parse logic. For `--local`: after `parseCommandArgs` consumes the args (extracts `--scope`), scan the remaining positional tokens for `--local`; reject unknown long flags (anything matching `/^--[a-z]/` that is not `--scope` or `--local`).
    7. Add header comment per source-comment-cleanup-policy: cite phase + decisions; no narrative.

    Modify `extensions/pi-claude-marketplace/edge/router.ts`:
    8. SubcommandHandlers interface (line 26-46): add the two method signatures.
    9. TOP_LEVEL_SUBCOMMANDS (line 52-64): add the two literals.
    10. TOP_LEVEL_USAGE (line 82-93): add the two usage lines.
    11. Switch (line 136-161): add the two arms forwarding to `handlers.enable` / `handlers.disable`.

    Modify `extensions/pi-claude-marketplace/edge/register.ts`:
    12. Import `makeEnableDisableHandler` from the new edge file.
    13. Add the two factory wirings (mirror the autoupdate dual-wiring at lines 92-93).

    Modify `extensions/pi-claude-marketplace/edge/completions/provider.ts`:
    14. Extend `PluginRefMode` (line 174) with `"enable"` and `"disable"`.
    15. Add case arms in `pluginRefBranchConfig` (line 186-219) returning installed-plugin completion (mirror `case "uninstall":`).

    Flip Wave 0 scaffolds in `tests/orchestrators/plugin/enable-disable.test.ts`:
    16. Strip `@ts-expect-error` markers on the import lines.
    17. Replace every `test.skip(name, body)` with `test(name, realBody)`. Real body per behavior contract. For the cache-only test: mock `loadMarketplaceManifest` to return a synthetic manifest; verify NO `fetch` / `clone` calls (use a counting stub on platform/git module exports — or rely on the architectural FORBIDDEN_TARGETS gate which is the load-bearing guarantee). For CFG-03: write a 0-byte file at `targetConfigPath`, run setPluginEnabled, assert the failed-row carries `path.basename(targetConfigPath)` and that `await stat(stateJsonPath)` mtime is unchanged.

    Flip Wave 0 scaffolds in `tests/edge/handlers/plugin/enable-disable.test.ts`:
    18. Strip `@ts-expect-error` markers.
    19. Replace `test.skip(...)` with `test(...)` carrying real bodies per the behavior contract.

    Land EVERY edit from Task 1 AND Task 2 in ONE atomic commit. Commit message: `feat(54): enable/disable commands with (disabled) closed-set token + cache-only re-materialization`. Pre-commit hooks GREEN.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` GREEN end-to-end (typecheck + ESLint + Prettier + tests + integration). Baseline ≥1635 unit + 7 integration; expected at least +15 tests from this plan.
    - `node --test tests/orchestrators/plugin/enable-disable.test.ts` GREEN — all 10+ ENBL behaviors covered.
    - `node --test tests/edge/handlers/plugin/enable-disable.test.ts` GREEN — all 5 edge behaviors covered.
    - `node --test tests/architecture/no-orchestrator-network.test.ts` GREEN — the FORBIDDEN_TARGETS gate (armed in Plan 01) is now ACTIVE on the new orchestrator file; the new orchestrator imports zero forbidden surface.
    - `node --test tests/architecture/config-state-write-seams.test.ts` GREEN — `setPluginEnabled` writes config ONLY via `saveConfig`; no raw `atomicWriteJson(configJsonPath, ...)` call.
    - `grep -nP "saveConfig\\(" extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts` returns at least 1 hit (the config write-back call).
    - `grep -nP "loadMarketplaceManifest\\(" extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts` returns at least 1 hit (cache-only enable read).
    - `grep -nP "platform/git|DEFAULT_GIT_OPS|refreshGitHubClone" extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts | grep -v '^\\s*\\*\\|^\\s*//'` returns 0 hits.
    - `grep -nc "makeEnableDisableHandler" extensions/pi-claude-marketplace/edge/register.ts` returns at least 1.
    - `grep -nc "\"enable\":\\|\"disable\":" extensions/pi-claude-marketplace/edge/router.ts` returns at least 2 (SubcommandHandlers fields + switch arms).
    - `pre-commit run --files <all-changed-files>` GREEN; `SKIP=trufflehog` IF committing from a worktree (CLAUDE.md guideline; otherwise omit).
    - ONE commit (combined Task 1 + Task 2 edits) — Conventional Commits format.
  </acceptance_criteria>
  <done>
    ENBL-01 through ENBL-04 are CLOSED. The `(disabled)` closed-set token is in place. Cache-only re-materialization is structurally enforced by the network gate. The atomic-supersession contract (Pitfall 54-2) is honored: every drift gate (catalog-uat / notify-types / notify-grammar-invariant / notify-v2 / config-state-write-seams / no-orchestrator-network / reconcile-planner-purity) is GREEN. The Wave 0 RED scaffolds Plan 01 created are now GREEN. `npm run check` GREEN; `pre-commit run --all-files` GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → edge handler | Untrusted args string carrying `<plugin>@<marketplace>` + `--scope` + `--local`; flag injection / unknown-flag rejection happens here. |
| Edge handler → orchestrator | Trusted parsed shape (positionals + scope + local) flows through a typed options object. |
| Orchestrator → file system (config write) | All writes go through `saveConfig` which runs `assertPathInside(scopeRoot, filePath)` then `atomicWriteJson` — NFR-10 + NFR-1. |
| Orchestrator → file system (state write) | `withStateGuard` saves state.json on no-throw exit — already covered by cross-process state lock. |
| Orchestrator → cached marketplace clone | Read-only `loadMarketplaceManifest` + filesystem reads via `resolveStrict`; NO network surface (NFR-5; gate armed in Plan 01). |
| Notification renderer → ctx.ui.notify | All user-visible output through `notify()` (IL-2); the new `(disabled)` token + `(already enabled)` / `(already disabled)` reasons land in the same atomic commit as the renderer + catalog (Pitfall 54-2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-02-01 | Tampering | orchestrators/plugin/enable-disable.ts (config write-back race) | mitigate | `loadConfig(targetConfigPath)` happens INSIDE the `withStateGuard` closure (Pitfall 54-1; A6-confirmed). A concurrent flip from another process either fails fast at lock acquisition (preferred per `with-state-guard.ts`) OR retries against the fresh post-flip state. Test: two parallel `setPluginEnabled` calls with different `enable` arguments produce a deterministic final state (last-writer-wins via lock serialization, NOT interleaved overwrite). |
| T-54-02-02 | Information Disclosure | orchestrators/plugin/enable-disable.ts (CFG-03 abort emits absolute path) | mitigate | The `(failed) {invalid manifest}` row carries `path.basename(targetConfigPath)` ONLY. Test fixture: invoke setPluginEnabled with an invalid config at `/tmp/<long-secret-path>/claude-plugins.json`; assert the rendered notify body contains `claude-plugins.json` but NOT `/tmp/<long-secret-path>`. Reuses the Phase 53 T-53-02-02 mitigation pattern. |
| T-54-02-03 | Tampering | orchestrators/plugin/enable-disable.ts (version pin loss) | mitigate | Pitfall 54-4 mitigation: the enable branch builds `InstallCtx` with `version: installed.version` (the existing state record's pin) — NEVER calls `resolvePluginVersion`. Test fixture: state record `version = "v1.2.3"`; enable; assert post-state `version === "v1.2.3"` (byte-identical). |
| T-54-02-04 | Information Disclosure | orchestrators/plugin/enable-disable.ts (network leak) | mitigate | Plan 01 added the file to `FORBIDDEN_TARGETS`. The architectural gate fails red if any platform/git / DEFAULT_GIT_OPS / refreshGitHubClone / gitOps symbol appears at module-import level. Belt-and-braces: orchestrator imports zero git surface; uses only `loadMarketplaceManifest` (cached read) + `resolveStrict` (filesystem only) + `cascadeUnstagePlugin` (filesystem only) + `runPhases` (in-memory). |
| T-54-02-05 | Tampering | orchestrators/plugin/enable-disable.ts (--local write touching base) | mitigate | Pitfall 54-5 mitigation: when `opts.local === true`, `targetConfigPath = locations.configLocalJsonPath` UNCONDITIONALLY (never falls back to base on ENOENT — creates the local file fresh from `{ schemaVersion: 1, plugins: { [key]: { enabled } } }` instead). Test fixture: `--local enable` against a fresh project with no `.local.json`; assert `claude-plugins.json` mtime is UNCHANGED post-operation; `claude-plugins.local.json` now exists and contains the entry. |
| T-54-02-06 | Spoofing | edge/handlers/plugin/enable-disable.ts (unknown-flag silent accept) | mitigate | Local `--local` scan rejects any unknown long flag via `notifyUsageError` (MSG-NC-2). Test fixture: `enable foo@bar --bogus` produces USAGE error; orchestrator is NEVER called. |
| T-54-02-07 | Repudiation | shared/notify.ts (closed-set drift) | mitigate | Atomic-supersession lockstep (Pitfall 54-2) — Tasks 1 + 2 land in ONE commit; every drift gate (catalog-uat byte-equality + inverse-walk orphan + notify-types length-locks + notify-grammar-invariant subject-first + discriminated-union exhaustiveness via assertNever) is GREEN as a precondition for the commit landing. Any intermediate state would fail at least one gate. |
| T-54-02-SC | Tampering | no package installs in this plan | accept | This plan introduces zero new dependencies. Package legitimacy gate is not triggered. |
</threat_model>

<verification>
After Tasks 1 + 2 ship together (ONE commit):

- `npm run check` GREEN: typecheck + ESLint + Prettier + tests + integration. Baseline ≥1635 unit + 7 integration tests (Phase 53 close); expected +15 tests in this plan (5 from Plan 01 + 10+ from this plan's tasks).
- All architectural drift gates GREEN:
  - `tests/architecture/catalog-uat.test.ts` (forward-walk byte-equality + inverse-walk orphan detection)
  - `tests/architecture/notify-types.test.ts` (length-locks 16 / 22 / 31; variant + negative-presence proofs)
  - `tests/architecture/notify-grammar-invariant.test.ts` (subject-first proof for `(disabled)`)
  - `tests/architecture/no-orchestrator-network.test.ts` (the gate Plan 01 armed is now ACTIVE; orchestrator imports zero git surface)
  - `tests/architecture/config-state-write-seams.test.ts` (no raw `atomicWriteJson(configJsonPath, ...)` in the new orchestrator; SPLIT-02 ownership preserved)
  - `tests/architecture/reconcile-planner-purity.test.ts` (Plan 01's planner change is still pure; exporting `isRecordedButDisabled` does not break purity)
- Behavior gates GREEN:
  - `tests/orchestrators/plugin/enable-disable.test.ts` (10+ ENBL behaviors)
  - `tests/edge/handlers/plugin/enable-disable.test.ts` (5 edge behaviors)
  - `tests/orchestrators/reconcile/plan.test.ts` (Plan 01's 5 planner tests still GREEN)
  - `tests/shared/notify-v2.test.ts` (new (disabled) inventory + (already enabled/disabled) skip rows byte-equal)
- ONE atomic git commit: `feat(54): enable/disable commands with (disabled) closed-set token + cache-only re-materialization`. Pre-commit hooks GREEN; `SKIP=trufflehog` if committing inside a worktree.
- Closes ENBL-01 / ENBL-02 / ENBL-03 / ENBL-04 in REQUIREMENTS.md.
</verification>

<success_criteria>
- A Pi user runs `/claude:plugin disable foo@mp` and after `/reload`: the plugin's skills/commands/agents/mcp are not loaded; the state record retains `version` / `resolvedSource` / `compatibility` / `installedAt`; the config file at `<scopeRoot>/claude-plugins.json` carries `plugins["foo@mp"].enabled === false`.
- A Pi user runs `/claude:plugin enable foo@mp` with the network unplugged: the plugin is re-materialized from the cached marketplace clone (the existing `<scopeRoot>/pi-claude-marketplace/marketplaces/<mp>/` directory); the state record's `version` field is byte-identical to its pre-disable value; the config carries `enabled === true`.
- A Pi user runs `/claude:plugin disable foo@mp --local`: writes go to `<scopeRoot>/claude-plugins.local.json` only; `claude-plugins.json` mtime is unchanged.
- `list` and `info` render a disabled plugin as `⊘ foo-plugin [user] v1.2.3 (disabled)` — visually distinct from `⊘ foo-plugin [user] v1.2.3 (unavailable) {<reason>}`; the discriminated-union variant differs (`PluginDisabledMessage` vs `PluginUnavailableMessage`).
- Re-running `disable foo@mp` on an already-disabled plugin renders `⊘ foo-plugin (skipped) {already disabled}` at info severity; no `/reload` trailer; no `Warning:` label (BENIGN_REASONS routing).
- A hand-edit-induced invalid `claude-plugins.json` causes the operation to abort with `(failed) {invalid manifest}` carrying only the basename; state.json and the config file are byte-unchanged.
- Atomic-supersession integrity: ONE commit; every drift gate GREEN; no intermediate RED state in `git log`.
- ENBL-01..04 marked Closed in REQUIREMENTS.md.
</success_criteria>

## Artifacts this phase produces

| Artifact | Phase 54 Plan | Purpose |
|----------|---------------|---------|
| `extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts` (NEW, `setPluginEnabled`) | Plan 02 | Single orchestrator parameterized by `enable: boolean`; mirrors `setMarketplaceAutoupdate`; composes resolveCrossScopePluginTarget + withStateGuard(CFG-03 abort + cascade OR install-ledger) + saveConfig + single terminal notify. |
| `extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts` (NEW, `makeEnableDisableHandler`) | Plan 02 | Edge factory; parses `<plugin>@<marketplace>` + `--scope` + `--local`; rejects unknown flags. |
| `extensions/pi-claude-marketplace/edge/router.ts` (MOD) | Plan 02 | SubcommandHandlers fields + TOP_LEVEL_SUBCOMMANDS + TOP_LEVEL_USAGE + switch arms for `enable` / `disable`. |
| `extensions/pi-claude-marketplace/edge/register.ts` (MOD) | Plan 02 | `makeEnableDisableHandler(pi, true)` + `(pi, false)` wirings. |
| `extensions/pi-claude-marketplace/edge/completions/provider.ts` (MOD) | Plan 02 | `PluginRefMode` extension + branch config for installed-plugin completion. |
| `extensions/pi-claude-marketplace/shared/notify.ts` (MOD) | Plan 02 | `(disabled)` closed-set token (PLUGIN_STATUSES + STATUS_TOKENS); `PluginDisabledMessage` variant + union arm; renderer arm; `(already enabled)` / `(already disabled)` REASONS + BENIGN_REASONS additions. |
| `extensions/pi-claude-marketplace/edge/handlers/tools.ts` (MOD) | Plan 02 | `case "disabled":` arms at the 3 PluginStatus exhaustive switches. |
| `extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` (MOD) | Plan 02 | `case "disabled":` arm in `scopeOf`. |
| `docs/output-catalog.md` (MOD) | Plan 02 | `## /claude:plugin enable` + `## /claude:plugin disable` H2 sections + list/info `disabled` inventory row catalog state. |
| `tests/architecture/catalog-uat.test.ts` (MOD) | Plan 02 | FIXTURES entries paired with each new catalog state (zero-orphan inverse walk). |
| `tests/architecture/notify-types.test.ts` (MOD) | Plan 02 | Length-lock bumps 15→16, 21→22, 29→31; PluginDisabledMessage shape + negative-presence proofs. |
| `tests/architecture/notify-grammar-invariant.test.ts` (MOD) | Plan 02 | Subject-first proof for `(disabled)` row. |
| `tests/shared/notify-v2.test.ts` (MOD) | Plan 02 | Byte-equality tests for new inventory + skip + cascade rows. |
| `tests/orchestrators/plugin/enable-disable.test.ts` (MOD) | Plan 02 | 10+ ENBL behaviors implemented; Wave 0 scaffolds flipped from `test.skip` to `test`. |
| `tests/edge/handlers/plugin/enable-disable.test.ts` (MOD) | Plan 02 | 5 edge USAGE/parse/forward behaviors implemented; Wave 0 scaffolds flipped. |
| `export` of `isRecordedButDisabled` from `orchestrators/reconcile/plan.ts` | Plan 02 (single-line edit) | Plan 01 created the helper as file-private; Plan 02 exports it for reuse by `setPluginEnabled`'s idempotency check (single source of truth for the empty-resources predicate). |

<output>
Create `.planning/phases/54-enable-disable-commands/54-02-SUMMARY.md` recording:
- Closed: ENBL-01, ENBL-02, ENBL-03, ENBL-04. Mark REQUIREMENTS.md rows Closed.
- D-54-01 landed: `(disabled)` closed-set token + variant + renderer arm + catalog state + FIXTURES entry, atomic.
- `PLUGIN_STATUSES`: 15 → 16; `STATUS_TOKENS`: 21 → 22; `REASONS`: 29 → 31; `BENIGN_REASONS`: +2 members.
- Bytes-touched table: all ~15 files; test-count delta; commit hash.
- A1 / A2 / A3 / A4 / A5 / A6 confirmation log (cite the source-of-truth read for each).
- ENBL-04 three-orthogonal-facts proof: variant differs (`PluginDisabledMessage` vs `PluginUnavailableMessage` — discriminated union arity is now N+1); token differs (`(disabled)` vs `(unavailable)`); glyph shared per Open Question #3 resolution.
- Phase 55 hand-off: the new `pluginsToEnable` bucket (Plan 01) + the cache-only enable path (Plan 02) compose into Phase 55's load-time reconcile-apply call site. Phase 55 will drive `setPluginEnabled` (or a renamed apply seam) per-entry serially under the existing per-scope lock per RECON-06.
</output>
