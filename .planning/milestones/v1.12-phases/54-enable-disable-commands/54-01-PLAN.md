---
phase: 54-enable-disable-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts
  - tests/architecture/no-orchestrator-network.test.ts
  - tests/orchestrators/reconcile/plan.test.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
  - tests/edge/handlers/plugin/enable-disable.test.ts
autonomous: true
requirements: [ENBL-02]
must_haves:
  truths:
    - "planReconcile produces a non-empty pluginsToEnable bucket when a recorded plugin has all four resources arrays empty AND its merged-config entry has enabled !== false"
    - "planReconcile does NOT also emit pluginsToInstall for the same recorded-but-disabled plugin (Pitfall 54-6 mutual-exclusion gate)"
    - "pluginsToEnable stays empty for a recorded plugin with non-empty resources (steady-state no-op preserved)"
    - "pluginsToEnable stays empty for an enabled=false declared plugin (the disable branch still owns that case)"
    - "no-orchestrator-network.test.ts FORBIDDEN_TARGETS includes orchestrators/plugin/enable-disable.ts so it gates the file the moment Plan 02 creates it"
    - "Wave 0 RED scaffolds exist for tests/orchestrators/plugin/enable-disable.test.ts and tests/edge/handlers/plugin/enable-disable.test.ts so Plan 02 lands GREEN tests in lockstep with the new orchestrator + edge handler"
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts"
      provides: "isRecordedButDisabled predicate + recorded-but-disabled branch in classifyDeclaredPlugin + DeclaredPluginAccumulator.enable bucket + pluginsToEnable wiring in planReconcile return"
      contains: "isRecordedButDisabled"
    - path: "tests/architecture/no-orchestrator-network.test.ts"
      provides: "FORBIDDEN_TARGETS entry for enable-disable.ts (NFR-5 architectural gate; ENOENT-safe per current skip-path)"
      contains: "orchestrators/plugin/enable-disable.ts"
    - path: "tests/orchestrators/reconcile/plan.test.ts"
      provides: "5 new tests covering the Phase 53→54 hand-off slot (isRecordedButDisabled true→enable bucket; non-empty resources→no enable; enabled=false→still disable; install+enable mutual exclusion; back-to-back plan invariance)"
    - path: "tests/orchestrators/plugin/enable-disable.test.ts"
      provides: "Wave 0 RED scaffold with imports asserting setPluginEnabled exists; test bodies are structural placeholders that Plan 02 fills in"
    - path: "tests/edge/handlers/plugin/enable-disable.test.ts"
      provides: "Wave 0 RED scaffold with imports asserting makeEnableDisableHandler factory exists; test bodies are structural placeholders that Plan 02 fills in"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts::classifyDeclaredPlugin"
      to: "isRecordedButDisabled"
      via: "branch check inside the recorded===true && !enabledExplicitFalse arm"
      pattern: "isRecordedButDisabled\\("
    - from: "tests/architecture/no-orchestrator-network.test.ts::FORBIDDEN_TARGETS"
      to: "orchestrators/plugin/enable-disable.ts"
      via: "array literal extension"
      pattern: "orchestrators/plugin/enable-disable\\.ts"
---

<objective>
Plan 01 is the Wave 1 BYTE-NEUTRAL foundation for Phase 54. It wires the Phase 53→54 reconcile-planner hand-off (the `isRecordedButDisabled` predicate that flips `pluginsToEnable` from structurally-empty to a real bucket) and lands the architectural network gate + Wave 0 RED test scaffolds that Plan 02 fills in.

Purpose: split the phase into a (1) tiny byte-neutral planner-and-gates commit and (2) a single atomic catalog-amendment commit (Plan 02). The split mirrors Phase 53's proven shape (Plan 01 byte-neutral foundation + Plan 02 atomic supersession) and keeps the `(disabled)` token + variants + renderer + catalog + FIXTURES + length-locks lockstep contained in ONE Plan-02 commit (Pitfall 54-2 atomic-supersession discipline; v1.3/v1.10/v1.11/Phase 53 lineage).

Output: A planner that detects "currently disabled" state records (empty `resources.*` arrays — A1 verified safe in Plan 01 task 1's read of `install.ts::statePhase`), a FORBIDDEN_TARGETS entry that activates Plan 02's NFR-5 guarantee the moment `enable-disable.ts` is created, and two RED scaffolds for the orchestrator + edge handler tests so Plan 02's GREEN bodies land in lockstep with the new files.

Closes (partially): ENBL-02 success criterion #2 ("reconcile's desired-materialized set is `declared AND enabled`, so a disabled entry is never re-materialized") — the planner now treats a recorded-but-disabled plugin as `will enable` when config says `enabled !== false`, and (already covered in Phase 53) as a steady-state no-op when config says `enabled === false`. Full closure of ENBL-02 lands in Plan 02 with the orchestrator's actual disable cascade and config write-back.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-enable-disable-commands/54-CONTEXT.md
@.planning/phases/54-enable-disable-commands/54-RESEARCH.md
@.planning/phases/54-enable-disable-commands/54-PATTERNS.md
@.planning/phases/54-enable-disable-commands/54-VALIDATION.md

@extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
@extensions/pi-claude-marketplace/persistence/state-io.ts
@tests/architecture/no-orchestrator-network.test.ts
@tests/orchestrators/reconcile/plan.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire isRecordedButDisabled predicate into reconcile planner + extend network FORBIDDEN_TARGETS</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts,
    tests/architecture/no-orchestrator-network.test.ts,
    tests/orchestrators/reconcile/plan.test.ts
  </files>
  <read_first>
    extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts,
    extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/install.ts,
    extensions/pi-claude-marketplace/persistence/state-io.ts,
    tests/architecture/no-orchestrator-network.test.ts,
    tests/orchestrators/reconcile/plan.test.ts
  </read_first>
  <behavior>
    - isRecordedButDisabled(record) returns true iff record.resources.skills.length === 0 AND record.resources.prompts.length === 0 AND record.resources.agents.length === 0 AND record.resources.mcpServers.length === 0
    - classifyDeclaredPlugin pushes to acc.enable (NEW bucket on DeclaredPluginAccumulator) when: recorded === true AND !enabledExplicitFalse AND isRecordedButDisabled(state.marketplaces[mp]?.plugins[plugin]) === true
    - classifyDeclaredPlugin still pushes to acc.install when recorded === false (no double-bill: a recorded-but-disabled plugin IS in recordedKeys so the install branch does NOT fire — Pitfall 54-6 mutual-exclusion gate)
    - classifyDeclaredPlugin still pushes to acc.disable when enabledExplicitFalse === true AND recorded === true (Phase 53 disable branch unchanged)
    - planReconcile return wires pluginsToEnable: pluginDiff.enable (replacing the Phase 53 placeholder `pluginsToEnable: []` at plan.ts:364)
    - empty-plan fast path includes totalEnables in the zero-sum check (the `if (totalAdds + totalRemoves + ... === 0)` guard expands)
    - FORBIDDEN_TARGETS includes "extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts" — gate is ENOENT-safe (skips when file doesn't exist; activates the moment Plan 02 creates the file)
    - New tests cover: (a) recorded + empty resources + enabled!==false → enable bucket non-empty; (b) recorded + non-empty resources + enabled!==false → enable bucket empty (steady state); (c) recorded + empty resources + enabled===false → disable bucket (NOT enable); (d) NOT recorded + enabled!==false → install bucket only (Pitfall 54-6 mutual exclusion proof); (e) back-to-back planReconcile against same inputs returns deepEqual plans (purity preserved)
  </behavior>
  <action>
    Read `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` lines 615-680 to confirm A1 (the `statePhase` writes `resources.{skills,prompts,agents,mcpServers}` from `c.stagedXxxNames` arrays; the only zero-array outcome is a plugin declaring zero components, which `requireInstallable` rules out structurally — record this confirmation in the SUMMARY's A1 line).

    In `extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts`:
    1. Add file-private helper `isRecordedButDisabled(record): boolean` above `classifyDeclaredPlugin` (around line 200). Signature: takes a single record of type `ExtensionState["marketplaces"][string]["plugins"][string]` (the inline-typed `PluginInstallRecord` shape from `state-io.ts`). Body: returns the four-way AND of `record.resources.{skills,prompts,agents,mcpServers}.length === 0`. Add a docstring referencing ENBL-02 + Pattern 4 + A1 (empty-resources marker; SPLIT-01 preserved — no new schema field).
    2. Extend `DeclaredPluginAccumulator` interface (around plan.ts:190-194): add `readonly enable: PlannedPluginEnable[]` (the type already exists in `reconcile/types.ts`).
    3. In `classifyDeclaredPlugin`, modify the trailing `!recorded` block (around line 258-265). After the `acc.install.push(...)` branch, ADD a sibling `if (recorded) { ... }` block: read `state.marketplaces[marketplace]?.plugins[plugin]`; if defined AND `isRecordedButDisabled(record)` is true, push `{ scope, plugin, marketplace }` to `acc.enable` and return. Otherwise return (steady state). Note: this requires threading `state` through `classifyDeclaredPlugin`'s parameter list — extend the signature to accept `state: ExtensionState` and update the single call site at `diffPlugins` line 308.
    4. Update `diffPlugins` accumulator initialization to include `enable: []` and return `enable: acc.enable` from the PluginDiff (extend the `PluginDiff` interface in `types.ts` if needed — verify whether it already has the `enable` field; if not, add `readonly enable: readonly PlannedPluginEnable[]`).
    5. Replace the literal `pluginsToEnable: []` at plan.ts:364 with `pluginsToEnable: pluginDiff.enable`.
    6. Expand the empty-plan fast-path guard (the `if (totalAdds + ... === 0)` block around plan.ts:347-356) to include `totalEnables = pluginDiff.enable.length` so a back-to-back zero-action call still short-circuits to `emptyReconcilePlan(scope)`.
    7. Delete the Phase 53 hand-off comment at plan.ts:18 + lines 262-265 (the "Phase 54 hand-off (Pitfall 53-4): `pluginsToEnable` is structurally empty" docstring + the inline comment in `classifyDeclaredPlugin`). Replace with a one-line note crediting the hand-off as closed in Phase 54 (cite ENBL-02 and the helper name) per the source-comment-cleanup-policy memory.

    In `tests/architecture/no-orchestrator-network.test.ts`:
    8. Add `"extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts"` to the `FORBIDDEN_TARGETS` array (after the reconcile entries at line 60-62). Add a short inline comment crediting Phase 54 + Pitfall 54-3 + ENBL-03 (cache-only re-materialization). The ENOENT-skip path at lines 86-91 keeps the gate green until Plan 02 creates the file.

    In `tests/orchestrators/reconcile/plan.test.ts`:
    9. Add 5 new `test(...)` blocks per the behavior contract above. Fixture pattern: mirror existing tests in the same file (build a `MergedConfig` literal + `ExtensionState` literal with one marketplace containing one plugin, vary `resources` arity + `enabled` flag, call `planReconcile`, assert on the returned plan's `pluginsToEnable` / `pluginsToInstall` / `pluginsToDisable` buckets). Test (e) (purity) calls `planReconcile` twice with the same inputs and `assert.deepEqual` the results.

    Do NOT create `orchestrators/plugin/enable-disable.ts` in this task — the file lands in Plan 02. The FORBIDDEN_TARGETS entry is the inert gate; the ENOENT skip path keeps `npm run check` green.

    Do NOT touch `shared/notify.ts`, `docs/output-catalog.md`, `tests/architecture/catalog-uat.test.ts`, `tests/architecture/notify-types.test.ts`, `tests/architecture/notify-grammar-invariant.test.ts`, `tests/shared/notify-v2.test.ts`, `edge/router.ts`, `edge/register.ts`, `edge/completions/provider.ts`, OR any orchestrator other than `reconcile/plan.ts` — those bytes belong in Plan 02 (atomic-supersession lockstep per Pitfall 54-2).
  </action>
  <verify>
    <automated>npm test -- tests/architecture/no-orchestrator-network.test.ts tests/orchestrators/reconcile/plan.test.ts tests/architecture/reconcile-planner-purity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/orchestrators/reconcile/plan.test.ts` GREEN; the 5 new tests pass with the planner's new branch.
    - `node --test tests/architecture/no-orchestrator-network.test.ts` GREEN; `enable-disable.ts` is in `FORBIDDEN_TARGETS` and the ENOENT-skip path holds since the file doesn't exist yet.
    - `node --test tests/architecture/reconcile-planner-purity.test.ts` GREEN — `isRecordedButDisabled` is a pure synchronous predicate (no imports added to plan.ts; no I/O/notify/network surface).
    - `grep -n "isRecordedButDisabled" extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts | grep -v '^\s*\*\|^\s*//'` returns at least 2 hits (definition + call site).
    - `grep -nc "pluginsToEnable: \[\]" extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts` returns 0 (the Phase 53 placeholder is gone).
    - `grep -c "enable-disable.ts" tests/architecture/no-orchestrator-network.test.ts` returns at least 1.
    - The DeclaredPluginAccumulator interface lists `enable` alongside `install` / `disable` / `dangling`.
  </acceptance_criteria>
  <done>
    The reconcile planner produces `pluginsToEnable` rows when state has empty-resources records AND config says `enabled !== false`. The Pitfall 54-6 mutual-exclusion proof passes (install + enable cannot fire for the same plugin in the same pass). The network gate is armed for the orchestrator file Plan 02 will create. Phase 53 hand-off TODO comments are deleted (per source-comment-cleanup-policy: history lives in git, not comments).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 RED scaffolds for orchestrator + edge handler tests</name>
  <files>
    tests/orchestrators/plugin/enable-disable.test.ts,
    tests/edge/handlers/plugin/enable-disable.test.ts
  </files>
  <read_first>
    tests/orchestrators/plugin/uninstall.test.ts,
    tests/orchestrators/marketplace/autoupdate.test.ts,
    tests/edge/handlers/plugin/uninstall.test.ts,
    tests/edge/handlers/marketplace/autoupdate.test.ts,
    .planning/phases/54-enable-disable-commands/54-VALIDATION.md
  </read_first>
  <behavior>
    - `tests/orchestrators/plugin/enable-disable.test.ts` imports from `extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts` (the file does NOT exist yet — the import sentinel signals Plan 02 to create it).
    - The orchestrator test file contains skipped-but-documented test names for: ENBL-01 base config write-back, ENBL-01 --local config write-back, ENBL-02 disable preserves version pin + resources reset, ENBL-02 reconcile no-op after disable, ENBL-03 enable from cache (manifest read mock, no network), ENBL-03 version pin preserved on enable, ENBL-03 missing clone aborts pre-ledger with `(failed) {source missing}`, idempotency arm `(skipped) {already enabled}` / `(skipped) {already disabled}` at info severity, CFG-03 invalid-config abort with `path.basename` (NEVER absolute path), --local creates `claude-plugins.local.json` and leaves base file mtime unchanged (Pitfall 54-5).
    - The edge handler test file contains skipped-but-documented test names for: missing positional → USAGE error, malformed `<plugin>@<marketplace>` → USAGE error, unknown flag → USAGE error, `--local` parse + forward to orchestrator, `--scope user|project` parse + forward to orchestrator.
    - `npm test -- tests/orchestrators/plugin/enable-disable.test.ts tests/edge/handlers/plugin/enable-disable.test.ts` exits 0 (skipped tests are not failures).
    - Plan 02 unmutes (`test.skip(` → `test(`) and fills in the test bodies in the same atomic commit as the orchestrator + edge handler creation.
  </behavior>
  <action>
    Create `tests/orchestrators/plugin/enable-disable.test.ts` mirroring the file shell of `tests/orchestrators/plugin/uninstall.test.ts` (imports block + describe-wrapping if used in the codebase; otherwise top-level `test(...)`). The scaffold body MUST:
    - Import `setPluginEnabled` from `../../../extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts` (TypeScript-typed import; the import line is the architectural sentinel that goes RED if Plan 02 doesn't ship the orchestrator).
    - Use `test.skip(...)` (or `it.skip(...)` depending on the file's existing style — verify by reading uninstall.test.ts) for every documented behavior so the suite exits 0 in Wave 1 and Plan 02 flips them to `test(...)` in lockstep with the new file.
    - Each skipped test body is a one-line `assert.fail("Plan 02 implements this — see <behavior name>")` so accidentally flipping it on in Wave 1 RED-fails loudly (Wave 0 RED contract; Plan 02 replaces the body with the real implementation).
    - Top-of-file header docstring crediting Phase 54 Plan 01 Wave 0 scaffold + Plan 02 fills the bodies (per source-comment-cleanup-policy: cite the closure phase + plan only, no narrative history).

    Create `tests/edge/handlers/plugin/enable-disable.test.ts` mirroring the file shell of `tests/edge/handlers/plugin/uninstall.test.ts`. The scaffold body MUST:
    - Import `makeEnableDisableHandler` from `../../../../extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts` (same sentinel pattern).
    - `test.skip(...)` for each USAGE / parse / forward behavior listed above.
    - Same `assert.fail(...)` one-line bodies.
    - Same header docstring shape.

    To avoid TypeScript erroring on the `import` lines (the source files don't exist yet in Wave 1), check whether the codebase uses `// @ts-expect-error` for this pattern (read `uninstall.test.ts` for precedent). If no precedent: use `@ts-expect-error: Plan 02 creates the source file in lockstep` on the import statements. Strip these comments in Plan 02 once the source files exist.

    Add NO other test files. Do NOT touch validation strategy doc — Plan 02 updates VALIDATION.md statuses.
  </action>
  <verify>
    <automated>npm test -- tests/orchestrators/plugin/enable-disable.test.ts tests/edge/handlers/plugin/enable-disable.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both new test files exist and `node --test` reports them as containing only `# skip` lines (zero failures, zero passes other than the implicit per-file "test" frame).
    - `npm run check` exits 0 — TypeScript compilation succeeds (the `@ts-expect-error` markers on the imports correctly absorb the missing-source errors; once Plan 02 creates the source files, the markers are stripped and the imports resolve).
    - `grep -c "test.skip" tests/orchestrators/plugin/enable-disable.test.ts` is at least 10 (matching the 10 ENBL behaviors listed in the behavior block).
    - `grep -c "test.skip" tests/edge/handlers/plugin/enable-disable.test.ts` is at least 5.
    - Both files import from the (yet-to-exist-in-Wave-1) Plan-02 source paths.
  </acceptance_criteria>
  <done>
    The two RED scaffolds exist with the import-sentinel + skip-with-fail-body shape Plan 02 fills in. `npm run check` GREEN. The scaffolds make the Plan 02 commit smaller (the test file shells + test names are already drafted; Plan 02 only adds bodies + flips `skip` to `test`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconcile/plan.ts -> consumer | Pure planner output flowing to preview + (future) apply path; the predicate's misclassification could trigger the wrong action bucket downstream. |
| FORBIDDEN_TARGETS gate -> orchestrators/plugin/enable-disable.ts | Architectural seam armed in Plan 01; Plan 02's source file is the first thing it gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01-01 | Tampering | reconcile/plan.ts (isRecordedButDisabled) | mitigate | A1 confirmation step in Task 1: read `install.ts::statePhase` to verify only the disable path could produce empty `resources.*` arrays. Test (a) proves the predicate fires; Test (b) proves a steady-state record does NOT trigger enable. |
| T-54-01-02 | Tampering | reconcile/plan.ts (mutual exclusion) | mitigate | Test (d) directly asserts that a NOT-recorded enabled plugin lands in `pluginsToInstall` ONLY (never enable AND install) — Pitfall 54-6 architectural proof. |
| T-54-01-03 | Spoofing | tests/architecture/no-orchestrator-network.test.ts (FORBIDDEN_TARGETS expansion) | mitigate | ENOENT-skip path on the gate keeps the test GREEN before Plan 02 lands the file; Plan 02's commit flips the gate active in lockstep with the file's creation — no pre-existing platform/git import can sneak in. |
| T-54-01-SC | Tampering | no package installs in this plan | accept | This plan introduces zero new dependencies (pure code rewire); the package-legitimacy gate is not triggered. |
</threat_model>

<verification>
After Task 2 completes:
- `npm run check` GREEN end-to-end (typecheck + ESLint + Prettier + tests + integration). Baseline ≥1635 unit + 7 integration (Phase 53 close).
- The reconcile planner now produces `pluginsToEnable` rows on the empty-resources marker; the Phase 53→54 hand-off slot is filled.
- The network gate is armed for `orchestrators/plugin/enable-disable.ts` and stays GREEN via ENOENT-skip until Plan 02 creates the file (at which point the gate activates and would fail-red if the orchestrator imported `platform/git`).
- The two Wave 0 RED scaffolds (`tests/orchestrators/plugin/enable-disable.test.ts` + `tests/edge/handlers/plugin/enable-disable.test.ts`) exist with documented skipped tests + import sentinels.
- Plan 01 is BYTE-NEUTRAL for `shared/notify.ts`, `docs/output-catalog.md`, `tests/architecture/catalog-uat.test.ts`, `tests/architecture/notify-types.test.ts`, `tests/architecture/notify-grammar-invariant.test.ts`, `tests/shared/notify-v2.test.ts`, `edge/router.ts`, `edge/register.ts`, `edge/completions/provider.ts`, and every orchestrator other than `reconcile/plan.ts`.
- One atomic git commit. Convention: `feat(54): wire reconcile planner pluginsToEnable bucket + arm network gate + RED scaffolds`. Pre-commit hooks GREEN.
</verification>

<success_criteria>
- planReconcile returns a non-empty `pluginsToEnable` array iff a recorded plugin has empty `resources.*` AND its declared entry is not `enabled === false`.
- The empty-plan fast path correctly observes the new bucket size (a zero-action plan still short-circuits to `emptyReconcilePlan(scope)`).
- Pitfall 54-6 mutual exclusion is structurally proven: tests show a recorded-but-disabled plugin produces EXACTLY one row (`pluginsToEnable`), and an unrecorded plugin produces EXACTLY one row (`pluginsToInstall`).
- The FORBIDDEN_TARGETS extension is in place; the ENOENT-skip path keeps the gate GREEN; Plan 02's source file will activate the gate.
- Both RED scaffolds exist and `npm run check` is GREEN.
- One commit; pre-commit GREEN; commit message follows Conventional Commits (CLAUDE.md guideline).
- ENBL-02 partial closure (the planner side): the merged-config `enabled` flag now drives `pluginsToEnable` / `pluginsToDisable` symmetrically.
</success_criteria>

## Artifacts this phase produces

| Artifact | Phase 54 Plan | Purpose |
|----------|---------------|---------|
| `isRecordedButDisabled(record): boolean` predicate in `orchestrators/reconcile/plan.ts` | Plan 01 | Phase 53→54 hand-off slot; reads state.resources arity as the implicit "currently disabled" marker (A1; SPLIT-01 preserved — no schema bump). |
| `DeclaredPluginAccumulator.enable: PlannedPluginEnable[]` bucket | Plan 01 | New accumulator field threaded into `planReconcile`'s `pluginsToEnable` return slot (replaces the Phase 53 placeholder `[]`). |
| `PluginDiff.enable` interface field | Plan 01 | Type-system contract pairing the accumulator with the diff result. |
| `FORBIDDEN_TARGETS` entry for `orchestrators/plugin/enable-disable.ts` | Plan 01 | NFR-5 architectural gate armed for Plan 02's orchestrator file. ENOENT-skip-safe. |
| `tests/orchestrators/plugin/enable-disable.test.ts` Wave 0 RED scaffold | Plan 01 | Documented `test.skip` shells with import sentinels for the 10 ENBL behaviors Plan 02 implements. |
| `tests/edge/handlers/plugin/enable-disable.test.ts` Wave 0 RED scaffold | Plan 01 | Documented `test.skip` shells for the 5 edge USAGE / parse behaviors Plan 02 implements. |
| 5 new tests in `tests/orchestrators/reconcile/plan.test.ts` | Plan 01 | Proof obligations for the new predicate, mutual exclusion (Pitfall 54-6), and back-to-back planner purity. |

<output>
Create `.planning/phases/54-enable-disable-commands/54-01-SUMMARY.md` recording:
- Closed: ENBL-02 partial (planner side only — orchestrator-side closure lands in Plan 02).
- A1 confirmation: explicit citation of `install.ts::statePhase` (lines 617-664) as the only `resources.*` write path; empty-array outcome impossible for an installable plugin (statePhase reads from `c.stagedXxxNames` which come from the resolver's component lists — `requireInstallable` rules out the zero-component degenerate).
- A6 confirmation: read of `transaction/with-state-guard.ts:66-83` confirmed the lock semantics (fail-fast on contention; loser retries; no stale-snapshot retry). Plan 02's `loadConfig` MUST live INSIDE the `withStateGuard` closure to honor Pitfall 54-1.
- Bytes-touched table: which files, which lines, which tests added.
- Plan 02 hand-off: the orchestrator + edge handler + token lockstep is the next plan; the import sentinels in the Wave 0 scaffolds + the FORBIDDEN_TARGETS entry will RED-fail the moment Plan 02 ships, gating the atomic-supersession contract.
</output>
