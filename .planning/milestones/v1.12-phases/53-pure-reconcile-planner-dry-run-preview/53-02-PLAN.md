---
phase: 53
plan: 02
type: execute
wave: 2
depends_on:
  - 53-01
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/preview.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/preview.ts
  - extensions/pi-claude-marketplace/edge/router.ts
  - extensions/pi-claude-marketplace/edge/register.ts
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
  - tests/architecture/notify-types.test.ts
  - tests/architecture/notify-grammar-invariant.test.ts
  - tests/architecture/no-orchestrator-network.test.ts
  - tests/orchestrators/reconcile/notify.test.ts
  - tests/orchestrators/reconcile/preview.test.ts
  - tests/edge/handlers/plugin/preview.test.ts
  - tests/shared/notify-v2.test.ts
autonomous: true
requirements:
  - DIFF-01
  - DIFF-02
must_haves:
  truths:
    - "A user runs /claude:plugin preview and sees a structured cascade showing exactly the marketplaces and plugins the next /reload's reconcile would add/install/uninstall/enable/disable, with no writes and no network calls"
    - "Running /claude:plugin preview twice in succession produces byte-identical output and zero file/state mutation (idempotent + read-only per DIFF-01 SC #2)"
    - "Preview rows render subject-first: glyph name [scope] (will add|will remove|will install|will uninstall|will enable|will disable) per D-53-02 / DIFF-02 SC #3"
    - "An invalid claude-plugins.json or claude-plugins.local.json (CFG-03 invalid signal) routes through notify() as a structured (failed) row carrying the file path and parse error — NEVER as an empty-desired-state diff that would render as a mass-uninstall preview (Pitfall 53-1; CFG-03 abort applies at preview time exactly as it will at Phase 55 apply time)"
    - "Disabled plugins (config enabled: false, not yet materialized) NEVER appear as a pending install — only as a will disable row when they are already materialized (Pitfall 53-2)"
    - "/claude:plugin preview tab completion works for --scope and is auto-picked-up from edge/router.ts TOP_LEVEL_SUBCOMMANDS by the completion provider"
    - "The reload-hint trailer (/reload to pick up changes) is NEVER emitted on preview output — every new will * token is excluded from the shouldEmitReloadHint trigger set"
    - "Every new will * STATUS_TOKENS / PLUGIN_STATUSES / MARKETPLACE_STATUSES literal, every new renderer arm, every new discriminated-union variant, every new docs/output-catalog.md state, and every new tests/architecture/catalog-uat.test.ts FIXTURES entry land in the SAME atomic commit (DIFF-02 lockstep; Pitfall 53-3)"
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "6 new STATUS_TOKENS (will add/remove/install/uninstall/enable/disable) + 2 new MARKETPLACE_STATUSES (will add/will remove) + 4 new PLUGIN_STATUSES (will install/will uninstall/will enable/will disable) + 6 new discriminated-union variant interfaces + 6 new renderer arms + shouldEmitReloadHint untouched (no new trigger entries)"
      contains: "\"will add\""
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/preview.ts"
      provides: "previewReconcile(opts) read-only orchestrator: loads MergedConfig + state per scope, calls planReconcile, projects via buildReconcilePreviewNotification, emits via notify() — exactly one notify() call per invocation per IL-2"
      exports: ["previewReconcile", "PreviewReconcileOptions"]
    - path: "extensions/pi-claude-marketplace/edge/handlers/plugin/preview.ts"
      provides: "makePreviewHandler(pi) thin shim: parses [--scope user|project] arg, rejects positional, dispatches to previewReconcile"
      exports: ["makePreviewHandler"]
    - path: "extensions/pi-claude-marketplace/edge/router.ts"
      provides: "preview added to TOP_LEVEL_SUBCOMMANDS tuple + SubcommandHandlers.preview + case \"preview\": arm in routeClaudePlugin + TOP_LEVEL_USAGE line"
      contains: "case \"preview\":"
    - path: "extensions/pi-claude-marketplace/edge/register.ts"
      provides: "preview: makePreviewHandler(pi) entry in the SubcommandHandlers literal"
      contains: "makePreviewHandler"
    - path: "docs/output-catalog.md"
      provides: "New `## /claude:plugin preview` H2 section with >= 5 catalog states (empty-steady-state, mp-add-plugin-install, plugin-pending-uninstall, enable-disable-transitions, source-mismatch, invalid-config-abort)"
      contains: "## `/claude:plugin preview`"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "New `/claude:plugin preview` FIXTURES outer key with >= 5 inner-state entries each pairing a NotificationMessage payload with a MockPi factory; byte-equality vs the new catalog section"
      contains: "/claude:plugin preview"
    - path: "tests/architecture/notify-types.test.ts"
      provides: "Length-lock numbers updated: STATUS_TOKENS 15 -> 21, MARKETPLACE_STATUSES 7 -> 9, PLUGIN_STATUSES 11 -> 15; new variant shape proofs"
      contains: "extends 21"
    - path: "tests/architecture/notify-grammar-invariant.test.ts"
      provides: "New variants asserted in the grammar invariant (subject-first row, status-after-subject)"
    - path: "tests/architecture/no-orchestrator-network.test.ts"
      provides: "FORBIDDEN_TARGETS extended to include orchestrators/reconcile/preview.ts (+ plan.ts and notify.ts for belt-and-braces — they are pure but the gate is cheap)"
      contains: "orchestrators/reconcile/preview.ts"
    - path: "tests/orchestrators/reconcile/preview.test.ts"
      provides: "Idempotency proof: run preview twice against a populated fixture, assert byte-identical output and zero file/state mutation; CFG-03 abort proof: invalid base/local config surfaces a (failed) row, NEVER an empty-desired-state diff"
    - path: "tests/edge/handlers/plugin/preview.test.ts"
      provides: "Shim parse + dispatch tests: USAGE on positional arg / unknown flag, --scope user/--scope project routing, no-arg default routes to scope-undefined for both-scope fan-out"
    - path: "tests/shared/notify-v2.test.ts"
      provides: "Byte-equality tests for the 6 new variant rows (mirroring the existing per-variant block at lines 380-700 of shared/notify.ts)"
  key_links:
    - from: "extensions/pi-claude-marketplace/edge/router.ts"
      to: "extensions/pi-claude-marketplace/edge/handlers/plugin/preview.ts"
      via: "case \"preview\": return handlers.preview(rest, ctx);"
      pattern: "case \"preview\":"
    - from: "extensions/pi-claude-marketplace/edge/handlers/plugin/preview.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/reconcile/preview.ts"
      via: "import previewReconcile"
      pattern: "previewReconcile"
    - from: "extensions/pi-claude-marketplace/orchestrators/reconcile/preview.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts"
      via: "import planReconcile (from Plan 01)"
      pattern: "planReconcile"
    - from: "extensions/pi-claude-marketplace/orchestrators/reconcile/preview.ts"
      to: "extensions/pi-claude-marketplace/persistence/config-merge.ts"
      via: "import loadMergedScopeConfig + CFG-03 trichotomy inspection"
      pattern: "loadMergedScopeConfig"
    - from: "tests/architecture/catalog-uat.test.ts"
      to: "docs/output-catalog.md"
      via: "byte-equality between rendered FIXTURES payload and catalog fenced code blocks"
      pattern: "/claude:plugin preview"
---

<objective>
Land the user-visible DIFF-01 SC #2 + DIFF-02 surface for the `/claude:plugin preview` read-only command as ONE atomic commit (per Pitfall 53-3 and the v1.3/v1.10/v1.11 atomic-supersession lessons). This single commit produces the six new pending-tense `(will ...)` status tokens (per D-53-02), the discriminated-union variant interfaces, the renderer arms (with subject-first row grammar per DIFF-02 / locked project convention), the read-only orchestrator `previewReconcile` (mirroring `orchestrators/marketplace/info.ts` — no withStateGuard, no gitOps), the edge handler shim, the router subcommand registration (per D-53-01 `preview`), the register.ts wiring, the `## /claude:plugin preview` catalog section with >= 5 catalog states, the `catalog-uat` FIXTURES entries pairing each catalog state with a byte-fixture, the length-lock bumps in `notify-types.test.ts` (STATUS_TOKENS 15 -> 21, MARKETPLACE_STATUSES 7 -> 9, PLUGIN_STATUSES 11 -> 15), the `no-orchestrator-network.test.ts` FORBIDDEN_TARGETS extension, the byte-equality proofs in `notify-v2.test.ts`, the idempotency proof in `preview.test.ts`, the CFG-03 abort proof in `preview.test.ts`, and the edge shim tests in `preview.test.ts`.

Purpose: Discharge DIFF-01 SC #2 (read-only preview command runs twice -> identical output, no mutation) and DIFF-02 SC #3 (subject-first row grammar with closed-set `(will ...)` tokens landing in lockstep with catalog + UAT fixtures) atomically — the v1.3/v1.10/v1.11 lesson is that any of these landing alone makes the catalog-uat byte-equality runner go RED, so the closed-set lockstep is a hard pre-commit gate, not a CI-driven fix-forward. The Plan 01 byte-neutral foundation has already shipped (pure planner + types + projection skeleton + Phase 52 convergence proof); this plan takes over the projection's `marketplace.status` and `plugin.status` field assignments to wire in the new tokens.

Output: 15 files touched (5 new edge/orchestrator/handler files, 1 modified `shared/notify.ts` with byte deltas in 6 places, 4 modified architecture/test files for length-locks + FIXTURES + FORBIDDEN_TARGETS, 1 modified `notify-v2.test.ts` with byte-equality tests, 1 modified `docs/output-catalog.md` with the new H2 section, 1 modified `edge/router.ts`, 1 modified `edge/register.ts`, 1 modified `orchestrators/reconcile/notify.ts` from Plan 01 — the placeholder status assignments are replaced with the real tokens). ONE atomic commit. No new dependencies. No new SPLIT-01 cast sites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-pure-reconcile-planner-dry-run-preview/53-CONTEXT.md
@.planning/phases/53-pure-reconcile-planner-dry-run-preview/53-RESEARCH.md
@.planning/phases/53-pure-reconcile-planner-dry-run-preview/53-PATTERNS.md
@.planning/phases/53-pure-reconcile-planner-dry-run-preview/53-VALIDATION.md
@.planning/phases/53-pure-reconcile-planner-dry-run-preview/53-01-SUMMARY.md

@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts
@extensions/pi-claude-marketplace/orchestrators/import/execute.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
@extensions/pi-claude-marketplace/persistence/config-merge.ts
@extensions/pi-claude-marketplace/persistence/state-io.ts
@extensions/pi-claude-marketplace/persistence/locations.ts
@extensions/pi-claude-marketplace/edge/router.ts
@extensions/pi-claude-marketplace/edge/register.ts
@extensions/pi-claude-marketplace/edge/handlers/marketplace/info.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts
@extensions/pi-claude-marketplace/edge/args.ts
@extensions/pi-claude-marketplace/edge/completions/provider.ts
@docs/output-catalog.md
@tests/architecture/catalog-uat.test.ts
@tests/architecture/notify-types.test.ts
@tests/architecture/notify-grammar-invariant.test.ts
@tests/architecture/no-orchestrator-network.test.ts
@tests/shared/notify-v2.test.ts
@tests/orchestrators/marketplace/info.test.ts
@tests/edge/handlers/marketplace/info.test.ts
@tests/edge/handlers/plugin/list.test.ts
</context>

<artifacts_this_phase_produces>
Closed-set additions to `shared/notify.ts`:

- `STATUS_TOKENS` extended from 15 -> 21 entries by appending: `"will add"`, `"will remove"`, `"will install"`, `"will uninstall"`, `"will enable"`, `"will disable"`.
- `MARKETPLACE_STATUSES` extended from 7 -> 9 entries by appending: `"will add"`, `"will remove"`.
- `PLUGIN_STATUSES` extended from 11 -> 15 entries by appending: `"will install"`, `"will uninstall"`, `"will enable"`, `"will disable"`.

New discriminated-union variants (joined to `MarketplaceNotificationMessage` / `PluginNotificationMessage` respectively):

- `MarketplaceWillAddMessage`: `{ name, scope, status: "will add" }` (no `details`, no `reasons` — these are preview, not realized-failure rows).
- `MarketplaceWillRemoveMessage`: `{ name, scope, status: "will remove" }`.
- `PluginWillInstallMessage`: `{ status: "will install", name, scope? }`. NO `dependencies` — soft-dep probe is meaningless before installation. NO `reasons`. NO `version` (the recorded version doesn't exist yet for an install).
- `PluginWillUninstallMessage`: `{ status: "will uninstall", name, scope? }`.
- `PluginWillEnableMessage`: `{ status: "will enable", name, scope? }`. (Phase 53 produces zero of these per Pitfall 53-4; the variant ships so Phase 54 doesn't have to revisit the type model.)
- `PluginWillDisableMessage`: `{ status: "will disable", name, scope? }`.

New renderer arms in `renderMpHeader` and `renderPluginRow` for each of the 6 variants, emitting subject-first `<glyph> <name> [<scope>] (<status>)` per the locked project convention. Glyph assignments per RESEARCH Pattern 5 sub-decision: `will add` / `will install` / `will enable` use the existing `ICON_INSTALLED` (filled circle); `will remove` / `will uninstall` use the existing `ICON_UNINSTALLED` (open circle); `will disable` uses the existing `ICON_UNINSTALLABLE` (⊘). No new icon constants.

`shouldEmitReloadHint`: confirm by inspection AND by a new test that none of the 6 new `will *` tokens triggers the `/reload to pick up changes` trailer. The function's existing implementation should already exclude them (it triggers only on the 4 state-change tokens at the head of `PLUGIN_STATUSES` per the notify.ts:251-256 comment); the new tokens are appended at the END of the tuple, not at the head. If the function uses a positive trigger set (e.g., a `RELOAD_TRIGGER_TOKENS` constant), verify the new tokens are absent; if it uses an exclusion ladder, verify the new tokens are not in the trigger half.

`computeSeverity`: confirm the 6 new tokens route to `info` (not `error` / `warning`). The function's existing arms route `failed` / `manual recovery` to higher severities and `skipped` to warning. `will *` is none of those — it routes to the default `info` arm.

New orchestrator + edge surfaces:

- `orchestrators/reconcile/preview.ts` exporting `previewReconcile(opts: PreviewReconcileOptions): Promise<void>` and the `PreviewReconcileOptions` interface. The orchestrator: (1) computes `scopes = opts.scope === undefined ? ["project", "user"] : [opts.scope]`; (2) for each scope, calls `locationsFor(scope, opts.cwd)` then `loadMergedScopeConfig(loc)` then `loadState(loc.extensionRoot)`; (3) inspects `outcome.base.status` AND `outcome.local.status` — if EITHER is `"invalid"`, accumulates a `(failed) {invalid manifest}`-style row for that scope (Pitfall 53-1 CFG-03 abort) and DOES NOT call `planReconcile` for that scope; (4) for each scope where both base and local are valid (or absent), calls `planReconcile(outcome.merged, state, scope)`; (5) accumulates the per-scope `ReconcilePlan` array and passes it to `buildReconcilePreviewNotification(plans)` (from Plan 01, now updated to emit the real tokens); (6) calls exactly ONE `notify(opts.ctx, opts.pi, message)` per invocation (IL-2); (7) when the plan is empty across all scopes, the message renders as the catalog's `empty-steady-state` state (the projection chooses the byte form; recommendation per RESEARCH Open Question 3 is a free-form advisory like "Preview: next reload will apply 0 actions." — finalize at write time, then lock with the catalog state).
- `edge/handlers/plugin/preview.ts` exporting `makePreviewHandler(pi: ExtensionAPI): (args, ctx) => Promise<void>` shim per RESEARCH Pattern 3 — parses args via the existing `parseArgs` helper, rejects unknown flags via `notifyUsageError`, rejects positional via `notifyUsageError`, dispatches to `previewReconcile({ ctx, pi, cwd: ctx.cwd, ...(parsed.scope !== undefined && { scope: parsed.scope }) })`. USAGE string: `"Usage: /claude:plugin preview [--scope user|project]"`.

Router + register changes:

- `edge/router.ts`: add `"preview"` to `TOP_LEVEL_SUBCOMMANDS` tuple; add `preview` field to `SubcommandHandlers` interface; add `case "preview": return handlers.preview(rest, ctx);` arm in `routeClaudePlugin` switch positioned alphabetically (after `import`, before `marketplace`); add a `preview [--scope user|project]` line to `TOP_LEVEL_USAGE` block.
- `edge/register.ts`: add `import { makePreviewHandler } from "./handlers/plugin/preview.ts";` and add the `preview: makePreviewHandler(pi),` entry in the `SubcommandHandlers` literal inside `registerClaudePluginCommand`. No `deps` parameter needed — preview has no `gitOps` or write seams.

Catalog + FIXTURES:

- `docs/output-catalog.md`: new `## /claude:plugin preview` H2 section placed alphabetically (after `## /claude:plugin marketplace` family if it ends with `update`, or between `marketplace info` and the next subcommand; pick the natural insertion point that mirrors the router order). At minimum 5 catalog states, each H3 + `<!-- catalog-state: NAME -->` comment + fenced \`\`\`text byte-exact code block:
  - `<!-- catalog-state: empty-steady-state -->` — empty plan; renders the free-form advisory body line "Preview: next reload will apply 0 actions." (no cascade marketplaces).
  - `<!-- catalog-state: mp-add-plugin-install -->` — one marketplace `(will add)` header with one child plugin `(will install)`.
  - `<!-- catalog-state: plugin-pending-uninstall -->` — one existing marketplace (no header status, list-style) with one child plugin `(will uninstall)`.
  - `<!-- catalog-state: enable-disable-transitions -->` — one marketplace with two plugin children, one `(will enable)` and one `(will disable)`.
  - `<!-- catalog-state: source-mismatch -->` — one marketplace `(failed) {source mismatch}` row using the existing `"source mismatch"` REASONS member (no new REASONS literal — Pitfall 53-7).
  - `<!-- catalog-state: invalid-config-abort -->` — Pitfall 53-1 CFG-03 abort: one marketplace `(failed) {invalid manifest}` row using the existing `"invalid manifest"` REASONS member (the file path is project-relative or `path.basename`, never absolute — Security per RESEARCH Threat Patterns "Information disclosure").
- `tests/architecture/catalog-uat.test.ts`: new `"/claude:plugin preview"` outer FIXTURES key with the 6 inner-state entries above, each pairing a `NotificationMessage` payload with a `MockPi` factory (default `piWithBothLoaded()` for preview rows since the soft-dep probe is meaningless). The inverse-walk gate (`every FIXTURES (section, state) has a matching catalog annotation`) covers the new section.

Length-locks:

- `tests/architecture/notify-types.test.ts`: update the `_Assert_PluginStatusesLen` / `_Assert_MarketplaceStatusesLen` type asserts from `extends 11` / `extends 7` to `extends 15` / `extends 9`; add/update the `_Assert_StatusTokensLen` to `extends 21`; add per-variant shape proofs for the 6 new variants asserting structural shape (the `scope` is `Scope | undefined` for plugin rows, required `Scope` for marketplace headers; status is the literal token; no `dependencies` / `reasons` / `details` on plugin will-* variants).

Network/architecture gates:

- `tests/architecture/no-orchestrator-network.test.ts`: extend `FORBIDDEN_TARGETS` to include `extensions/pi-claude-marketplace/orchestrators/reconcile/preview.ts` (and as belt-and-braces, `plan.ts` + `notify.ts` from the reconcile family — the Plan 01 purity gate already covers `plan.ts`, but adding it here is cheap and defends against a future regression).

Tests:

- `tests/orchestrators/reconcile/preview.test.ts`: (1) idempotency — call `previewReconcile` twice against a populated fixture, capture both `ctx.ui.notify` invocations, assert byte-equal; (2) no-mutation — assert the config file mtime, state file mtime, and on-disk bytes are unchanged across both runs; (3) CFG-03 abort — set up a fixture where `claude-plugins.json` is malformed (invalid arm of the loadConfig trichotomy), assert the emitted notification cascade contains a `(failed) {invalid manifest}` row for that scope AND assert `planReconcile` was NOT called for that scope (use a spy on the planner module's exported symbol OR assert via the absence of a "would install" row); (4) network/write isolation — wrap the run in a forbidden-fs mock (Node `fs.writeFile` etc.) and assert no write happens; spy on `gitOps` (must be undefined / never called); (5) scope-fan-out — no `--scope` arg routes to both scopes in `["project", "user"]` order; explicit `--scope user` routes to user only; (6) the `notify()` call count is exactly 1 per invocation per IL-2.
- `tests/edge/handlers/plugin/preview.test.ts`: (1) USAGE on positional arg ("preview foo" -> notifyUsageError with the USAGE string); (2) USAGE on unknown flag ("preview --bogus" -> notifyUsageError); (3) `--scope user` -> dispatches to previewReconcile with `scope: "user"`; (4) `--scope project` -> dispatches with `scope: "project"`; (5) no args -> dispatches with `scope: undefined`; (6) `--scope foo` (invalid value) -> notifyUsageError.
- `tests/shared/notify-v2.test.ts`: byte-equality tests for the 6 new variants. Each test constructs the variant payload + invokes `notify(ctx, pi, message)` + asserts the exact string passed to `ctx.ui.notify`. Subject-first form: `<glyph> <name> [<scope>] (<token>)` for plugin rows; `<glyph> <name> [<scope>] (<token>)` for marketplace headers. Verify the orphan-fold contract holds for the new plugin variants (when `plugin.scope === mp.scope`, the `[scope]` bracket is omitted on the plugin row).
- `tests/architecture/notify-grammar-invariant.test.ts`: the existing grammar invariant (subject-first, status-after-subject) iterates closed-set tokens; the new 6 tokens must be covered automatically by the iteration. If the test enumerates a hard-coded set, extend it; if it iterates `STATUS_TOKENS` directly, no change required.

Plan 01 hand-off resolution:

- `orchestrators/reconcile/notify.ts` (touched in Plan 01): replace the placeholder status assignments with the real `"will add"`, `"will remove"`, `"will install"`, `"will uninstall"`, `"will enable"`, `"will disable"` strings. The function signature, the `MarketplaceBlock` shape, the `ensureMarketplaceBlock` factory, the bucket walk order, and the `compareByNameThenScope` ordering are unchanged. The empty-plan case emits the catalog's `empty-steady-state` byte form (free-form advisory body line).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 6 will-* tokens + variants + renderer arms to shared/notify.ts; update length-locks; finalize reconcile/notify.ts projection (catalog + FIXTURES land in Task 2 in lockstep)</name>
  <files>
    extensions/pi-claude-marketplace/shared/notify.ts,
    extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts,
    tests/architecture/notify-types.test.ts,
    tests/shared/notify-v2.test.ts
  </files>
  <read_first>
    extensions/pi-claude-marketplace/shared/notify.ts (read full file — focus on lines 152-300 for the closed sets, lines 380-700 for the per-variant interfaces + discriminated union joins, lines 700+ for renderMpHeader + renderPluginRow + the assertNever default arms; identify the existing icon constants ICON_INSTALLED, ICON_UNINSTALLED, ICON_UNINSTALLABLE; identify the renderScopeBracket helper signature for the orphan-fold contract; identify shouldEmitReloadHint's trigger set and confirm the new tokens land OUTSIDE it; identify computeSeverity's arms and confirm the new tokens route to info),
    extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts (Plan 01 placeholder version — read full file; identify the per-bucket walk and the status assignment seam),
    extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts (Plan 01 types — read full file),
    extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts (Plan 01 planner — read full file to confirm bucket contents),
    tests/architecture/notify-types.test.ts (read full file — focus on _Assert_PluginStatusesLen / _Assert_MarketplaceStatusesLen / _Assert_StatusTokensLen length-lock blocks; identify any per-variant shape proofs to extend),
    tests/shared/notify-v2.test.ts (read full file — note the per-variant byte-equality test pattern from lines 1-200; identify the MockPi / ctx fixtures; identify the renderScopeBracket orphan-fold test patterns)
  </read_first>
  <behavior>
    - In `shared/notify.ts`:
      - Append to `STATUS_TOKENS` tuple (currently 15 entries ending with `"no plugins"` per notify.ts:152-168): `"will add", "will remove", "will install", "will uninstall", "will enable", "will disable"`. Length becomes 21.
      - Append to `MARKETPLACE_STATUSES` tuple (currently 7 entries ending with `"skipped"` per notify.ts:283-291): `"will add", "will remove"`. Length becomes 9.
      - Append to `PLUGIN_STATUSES` tuple (currently 11 entries ending with `"present"` per notify.ts:260-272): `"will install", "will uninstall", "will enable", "will disable"`. Length becomes 15.
      - Update the closed-set tuple header comments to reflect the new lengths and document that the 6 new tokens are pending-tense per D-53-02 / DIFF-02 and are STRUCTURALLY EXCLUDED from `shouldEmitReloadHint`'s trigger set (preview rows are pre-transition; `/reload to pick up changes` is grammatically false for them).
      - Add the 6 new variant interfaces per the artifacts_this_phase_produces inventory. Marketplace variants (`MpWillAdd`, `MpWillRemove`) extend the existing `MpCommon` / marketplace base interface; plugin variants (`PluginWillInstallMessage`, `PluginWillUninstallMessage`, `PluginWillEnableMessage`, `PluginWillDisableMessage`) follow the existing pattern of `PluginInstalledMessage` / `PluginUninstalledMessage` (lines 380-560 area). Plugin variants have `scope?: Scope` (orphan-fold contract per renderScopeBracket); NO `dependencies`; NO `reasons`; NO `version`; NO `cause`; NO `rollbackPartial`.
      - Add 2 new arms to `renderMpHeader` (`case "will add":` -> emits `ICON_INSTALLED + " " + mp.name + " " + renderScopeBracket(undefined, mp.scope) + " (will add)"`; `case "will remove":` -> emits `ICON_UNINSTALLED + " " + mp.name + " " + renderScopeBracket(undefined, mp.scope) + " (will remove)"`). The exact byte form is the catalog state's locked form — match it byte-for-byte in Task 2.
      - Add 4 new arms to `renderPluginRow` (`case "will install":`, `case "will uninstall":`, `case "will enable":`, `case "will disable":`). Each arm emits the subject-first row with the appropriate glyph (will install/enable -> ICON_INSTALLED; will uninstall -> ICON_UNINSTALLED; will disable -> ICON_UNINSTALLABLE) and the orphan-fold scope bracket via `renderScopeBracket(p.scope, mp.scope)`. The exact byte form matches the catalog state's locked form — verify byte-for-byte against `docs/output-catalog.md` once Task 2 lands the catalog.
      - Join the 6 new variants into `MarketplaceNotificationMessage` (existing union at notify.ts:670 area) and `PluginNotificationMessage` (existing union at notify.ts:562 area). The discriminated-union type now has 9 + 6 = 15-ish total variants (exact count depends on existing structure; verify after the edit that the type is exhaustively narrowable via assertNever in the renderer's default arm).
      - Verify `shouldEmitReloadHint` does NOT include any of the 6 new tokens in its trigger ladder; if it uses a positive trigger set, the function body needs zero edits (the new tokens are appended outside the head of the tuple). If it uses an exclusion-based ladder, audit the arms and confirm; if any new token leaks into the trigger half, fix.
      - Verify `computeSeverity` routes the 6 new tokens to `info`. The function's existing arms cover `failed` / `manual recovery` (error) and `skipped` (warning); will-* tokens are none of those and route to the default `info` arm.
    - In `orchestrators/reconcile/notify.ts` (Plan 01 placeholder version):
      - Replace the placeholder status assignments in the bucket walk with the real `"will add"`, `"will remove"`, `"will install"`, `"will uninstall"`, `"will enable"`, `"will disable"` strings.
      - Marketplace add bucket -> `block.status = "will add"`. Marketplace remove bucket -> `block.status = "will remove"`. Plugin install bucket -> `block.plugins.push({ status: "will install", name: plugin, scope })`. Plugin uninstall bucket -> `{ status: "will uninstall", ... }`. Plugin enable -> `{ status: "will enable", ... }` (empty in Phase 53 per Pitfall 53-4 but the path exists). Plugin disable -> `{ status: "will disable", ... }`. Source-mismatch bucket -> marketplace block with `status: "failed"` and `reasons: ["source mismatch"]` (Pitfall 53-7 — reuse the existing REASONS member, do NOT add a new one).
      - When ALL plans are empty (every plan's every bucket is empty), the function returns `{ marketplaces: [] }` with a sentinel that the orchestrator turns into the free-form advisory body line. The exact mechanism (return a marker on the message, or have the orchestrator detect emptiness via `plans.every(p => isPlanEmpty(p))` and route to a different message) is a Task 2 decision — pick the cleanest path that aligns with the catalog's `empty-steady-state` byte form.
      - Confirm the function remains pure (no new imports of effectful modules). The Plan 01 architecture purity gate at `tests/architecture/reconcile-planner-purity.test.ts` covers `plan.ts` only; `notify.ts` is not gated but should be pure by discipline.
    - In `tests/architecture/notify-types.test.ts`:
      - Update length-lock asserts: `_Assert_PluginStatusesLen` from `extends 11` to `extends 15`. `_Assert_MarketplaceStatusesLen` from `extends 7` to `extends 9`. `_Assert_StatusTokensLen` to `extends 21` (add the assert if not already present).
      - Add per-variant shape proofs for the 6 new variants. Each proof: `type _MarketplaceWillAddExpected = { readonly name: string; readonly scope: Scope; readonly status: "will add" };` then `type _Assert_MarketplaceWillAdd = MarketplaceWillAddMessage extends _MarketplaceWillAddExpected ? (_MarketplaceWillAddExpected extends MarketplaceWillAddMessage ? true : never) : never;` (bidirectional proof; mirrors existing per-variant proofs). Plugin variants assert `scope?: Scope` (optional). NEGATIVE proofs: `type _NoDependenciesOnPluginWillInstall = "dependencies" extends keyof PluginWillInstallMessage ? never : true;` and similar for `reasons` / `version` / `cause` / `rollbackPartial`.
    - In `tests/shared/notify-v2.test.ts`:
      - Add byte-equality tests for the 6 new variants. Mirror the existing per-variant test pattern (a test name like `"renders will-install plugin row byte-exact"`, a payload literal, an invocation of `notify(mockCtx, mockPi, message)`, and an assertion on `mockCtx.ui.notify` mock call args).
      - Include the orphan-fold variant proofs: a `PluginWillInstallMessage` with `scope` equal to the marketplace `scope` -> no `[scope]` bracket on the plugin row; a `PluginWillInstallMessage` with `scope` different from the marketplace `scope` -> `[scope]` bracket present.
      - Include the `shouldEmitReloadHint` proof: a cascade containing only will-* tokens -> no `/reload to pick up changes` trailer.
      - Include the `computeSeverity` proof: a cascade containing only will-* tokens -> `info` severity (the `notify()` call to `ctx.ui.notify` omits the severity arg, mirroring the v1.5 UXG-02 info-severity discipline).
  </behavior>
  <action>
    CRITICAL: Task 1 lands the closed-set + variant + renderer + length-locks + notify-v2 byte tests changes, which are individually CI-RED until Task 2 lands the catalog + FIXTURES in the SAME atomic commit. DO NOT commit between Task 1 and Task 2 — they are one logical commit. The git workflow is: complete Task 1 edits, then immediately complete Task 2 edits, then run pre-commit, then commit ONCE. Per Pitfall 53-3, if pre-commit fails (catalog-uat byte-equality red because Task 2's catalog isn't there yet, or notify-types length-lock red because the test file is updated but the source tuple isn't), the failure is expected DURING the editing pass and resolved when Task 2 completes — do NOT commit a partial state. Per D-53-02 the new tokens are FUTURE-TENSE `will *` form (not `to install` etc. — the research suggested `to install`, but the user-locked decision in CONTEXT.md is `will install`). Verify every new STATUS_TOKENS / PLUGIN_STATUSES / MARKETPLACE_STATUSES literal is the `will *` form, not `to *`. Per Pitfall 53-4, the `PluginWillEnableMessage` variant ships but the planner produces zero of them — the catalog state for `enable-disable-transitions` therefore is hand-constructed in Task 2's FIXTURES, NOT routed through `planReconcile` (the FIXTURES payload is a direct `NotificationMessage` literal). Per RESEARCH Pattern 5 sub-decision, NO new icon constants — reuse ICON_INSTALLED / ICON_UNINSTALLED / ICON_UNINSTALLABLE. Per Pitfall 53-7, the source-mismatch state reuses the existing `"source mismatch"` REASONS member (currently in REASONS at notify.ts:87); do NOT add a new REASONS literal — REASONS stays at 29 entries.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; npm run typecheck 2>&amp;1 | head -40 ; echo "---NOTE: catalog-uat is expected RED until Task 2 lands; do not commit between tasks---"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"will add"\|"will remove"\|"will install"\|"will uninstall"\|"will enable"\|"will disable"' extensions/pi-claude-marketplace/shared/notify.ts` returns >= 12 (each token appears at least in the tuple definition AND in a renderer case arm; tokens with both plugin AND marketplace presence appear 3+ times: tuple + renderer arm for each surface).
    - `grep -c "extends 21\|extends 15\|extends 9" tests/architecture/notify-types.test.ts` returns >= 3 (the three length-locks updated).
    - `grep -c "will install\|will uninstall\|will enable\|will disable\|will add\|will remove" tests/shared/notify-v2.test.ts` returns >= 6 (one test per new variant minimum).
    - `npm run typecheck` exits 0 (the type model is internally consistent even with the catalog-uat / FIXTURES still pending).
    - `orchestrators/reconcile/notify.ts` no longer contains the Plan 01 placeholder status strings; `grep -E 'status:\s*"(installed|added)"' extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts` returns 0 (the projection now emits the new will-* tokens, not the placeholder past-tense tokens).
    - Task 1 stops short of running `npm run check` because `catalog-uat` is expected RED at this point — DO NOT attempt to fix-forward; Task 2 closes the lockstep.
  </acceptance_criteria>
  <done>
    `shared/notify.ts` carries 6 new tokens (3 tuple updates), 6 new variants, 6 new renderer arms; `tests/architecture/notify-types.test.ts` length-locks bumped + per-variant proofs added; `tests/shared/notify-v2.test.ts` carries byte-equality tests for the 6 variants; `orchestrators/reconcile/notify.ts` projection emits the real tokens. The set is internally type-consistent; the catalog-uat byte-equality runner is RED until Task 2 lands the catalog + FIXTURES — that is the expected midway state of the atomic-supersession lockstep.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship preview orchestrator + edge shim + router + catalog states + catalog-uat FIXTURES + idempotency/CFG-03/shim tests (closes the DIFF-02 atomic lockstep in one commit with Task 1)</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/reconcile/preview.ts,
    extensions/pi-claude-marketplace/edge/handlers/plugin/preview.ts,
    extensions/pi-claude-marketplace/edge/router.ts,
    extensions/pi-claude-marketplace/edge/register.ts,
    docs/output-catalog.md,
    tests/architecture/catalog-uat.test.ts,
    tests/architecture/notify-grammar-invariant.test.ts,
    tests/architecture/no-orchestrator-network.test.ts,
    tests/orchestrators/reconcile/preview.test.ts,
    tests/edge/handlers/plugin/preview.test.ts
  </files>
  <read_first>
    extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts (read lines 1-200 — the closest analog for the read-only orchestrator: no withStateGuard, no gitOps, scope fan-out, single notify() call),
    extensions/pi-claude-marketplace/edge/handlers/marketplace/info.ts (read full file — analog for the edge shim factory + USAGE handling pattern),
    extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts (read lines 1-100 — analog for the parseArgs pattern with a --scope flag and no positional, closer than info.ts which has a positional),
    extensions/pi-claude-marketplace/edge/args.ts (read parseArgs export — confirm it returns { positional, scope, ... } and how it signals unknown flags),
    extensions/pi-claude-marketplace/edge/router.ts (read full file — note TOP_LEVEL_SUBCOMMANDS at line 50-61, SubcommandHandlers interface at line 25-45, routeClaudePlugin switch at line 132+, TOP_LEVEL_USAGE at line 79-89),
    extensions/pi-claude-marketplace/edge/register.ts (read full file — note the SubcommandHandlers literal in registerClaudePluginCommand at line 75+),
    extensions/pi-claude-marketplace/edge/completions/provider.ts (skim lines 30-100 — confirm topLevelCompletions reads TOP_LEVEL_SUBCOMMANDS, so adding "preview" propagates the completion automatically),
    extensions/pi-claude-marketplace/persistence/config-merge.ts (read full file — note loadMergedScopeConfig return shape with base.status / local.status / merged),
    extensions/pi-claude-marketplace/persistence/state-io.ts (read loadState signature and ExtensionState shape),
    extensions/pi-claude-marketplace/persistence/locations.ts (read locationsFor + ScopedLocations shape — note configJsonPath, configLocalJsonPath, extensionRoot, agentsDir, mcpJsonPath),
    extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts (the Plan 01 planner — confirm signature is planReconcile(merged, state, scope)),
    extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts (the Task-1-updated projection — confirm signature is buildReconcilePreviewNotification(plans) and it now emits will-* tokens),
    docs/output-catalog.md (read the section around marketplace info / plugin list — identify the placement convention and the H2 / H3 / fenced-block byte structure),
    tests/architecture/catalog-uat.test.ts (read full file — focus on the FIXTURES map structure starting around line 227+, the MockPi factories at lines 156-180, the inverse-walk gate around line 2213; identify the catalog-section-parsing seam loadCatalogExamples and its sectionRe coupling),
    tests/architecture/no-orchestrator-network.test.ts (read full file — note FORBIDDEN_TARGETS structure at lines 50-56 and the stripComments walker),
    tests/orchestrators/marketplace/info.test.ts (read lines 1-200 for the read-only orchestrator test patterns: scope fan-out, single-notify, no-mutation assertion patterns),
    tests/edge/handlers/marketplace/info.test.ts (read full file — analog for the edge shim test patterns)
  </read_first>
  <behavior>
    - In `extensions/pi-claude-marketplace/orchestrators/reconcile/preview.ts` (NEW):
      - Header docstring per RESEARCH Pattern 3: "Read-only preview surface for `/claude:plugin preview` (D-53-01). MUST NOT touch the network (NFR-5) -- no `platform/git`, no `DEFAULT_GIT_OPS`, no `refreshGitHubClone`. The grep-gate test in `tests/architecture/no-orchestrator-network.test.ts` enforces this structurally. NEVER writes any file (NFR-5 read-surface discipline). IL-2: exactly one `notify()` call per invocation. CFG-03 (Pitfall 53-1): when EITHER base or local config arm is `invalid`, surface a `(failed) {invalid manifest}` row for that scope and DO NOT call `planReconcile` for it — invalid is never silently coerced to empty desired state. Idempotency (DIFF-01 SC #2): two consecutive invocations against unchanged state + config produce byte-identical output."
      - Imports: type-only from `pi-api.ts`, `shared/types.ts`, `shared/notify.ts` (for the message types); value imports from `persistence/locations.ts` (locationsFor), `persistence/config-merge.ts` (loadMergedScopeConfig), `persistence/state-io.ts` (loadState), `shared/notify.ts` (notify), `./plan.ts` (planReconcile), `./notify.ts` (buildReconcilePreviewNotification, plus an empty-plan detector if the projection exposes one; otherwise compute emptiness locally via `plans.every(...)`).
      - Export `PreviewReconcileOptions { ctx, pi, cwd, scope? }` and `previewReconcile(opts)`.
      - Implementation: compute `scopes = opts.scope === undefined ? ["project", "user"] : [opts.scope]`. For each scope: `loc = locationsFor(scope, opts.cwd)`; `outcome = await loadMergedScopeConfig(loc)`; if `outcome.base.status === "invalid"` OR `outcome.local.status === "invalid"`, accumulate an invalid-config marketplace failure row (the row carries `name: <scope-or-file-tag>`, `scope`, `status: "failed"`, `reasons: ["invalid manifest"]` — exact byte form locked by the catalog state `invalid-config-abort`). The file path in the failure row body uses `path.basename(loc.configJsonPath)` or `path.basename(loc.configLocalJsonPath)` to avoid absolute-path information disclosure (RESEARCH Security Threat Pattern). Do NOT call `planReconcile` for that scope. Otherwise: `state = await loadState(loc.extensionRoot)`; `plan = planReconcile(outcome.merged, state, scope)`; accumulate `plan`.
      - After all scopes: build the message. If every accumulated plan is empty AND there are zero invalid-config rows, the message is the catalog's `empty-steady-state` byte form — a free-form advisory body line "Preview: next reload will apply 0 actions." (exact byte form locked by the catalog state). The mechanism: emit a synthesized `MarketplaceNotificationMessage`-equivalent shape OR use a dedicated marker. Recommended pattern: have `buildReconcilePreviewNotification` accept a second arg or return a tagged-union shape `{ kind: "empty" } | { kind: "cascade", marketplaces: [...] }`; the orchestrator switches on the tag and calls `notify()` either way. Alternative: the projection always returns `CascadeNotificationMessage` and the orchestrator wraps the empty case by constructing the advisory body inline. Pick the cleanest path that keeps the projection pure and the orchestrator deterministic; document the choice in a header comment.
      - Non-empty case: `const message = buildReconcilePreviewNotification([...plans, ...invalidConfigRows]);` then `notify(opts.ctx, opts.pi, message);`. Exactly one `notify()` call.
    - In `extensions/pi-claude-marketplace/edge/handlers/plugin/preview.ts` (NEW):
      - Per RESEARCH Pattern: header docstring + `USAGE = "Usage: /claude:plugin preview [--scope user|project]"`; export `makePreviewHandler(pi: ExtensionAPI): (args: string, ctx: ExtensionCommandContext) => Promise<void>`. The factory closes over `pi` and returns an async handler that: (a) tries `parsed = parseArgs(args)`; on throw, calls `notifyUsageError(ctx, { message: errorMessage(err), usage: USAGE })` and returns; (b) rejects `parsed.positional.length > 0` with USAGE; (c) calls `previewReconcile({ ctx, pi, cwd: ctx.cwd, ...(parsed.scope !== undefined && { scope: parsed.scope }) })`.
      - The factory imports `previewReconcile` from `../../../orchestrators/reconcile/preview.ts` and `notifyUsageError`, `errorMessage`, `parseArgs` per the analog `edge/handlers/plugin/list.ts`. No `pluginsToInstall` / `gitOps` dependencies (preview has no write seams).
    - In `extensions/pi-claude-marketplace/edge/router.ts` (MODIFIED):
      - Add `"preview"` to `TOP_LEVEL_SUBCOMMANDS` tuple at line 50-61. Pick the insertion point that mirrors alphabetical/grouping convention (after `"info"` is natural).
      - Add `preview: (args: string, ctx: ExtensionCommandContext) => Promise<void>;` to `SubcommandHandlers` interface at line 25-45 (after `pluginInfo`).
      - Add `case "preview": return handlers.preview(rest, ctx);` to `routeClaudePlugin` switch at line 132+, positioned after `case "info":` and before `case "import":` (alphabetical insertion).
      - Add a `preview [--scope user|project]` line to `TOP_LEVEL_USAGE` block at line 79-89, positioned after the `info` line.
      - The completion provider at `edge/completions/provider.ts` reads `TOP_LEVEL_SUBCOMMANDS` and auto-picks up "preview" — no edit there. Verify by reading provider.ts and confirming the iteration pattern.
    - In `extensions/pi-claude-marketplace/edge/register.ts` (MODIFIED):
      - Add `import { makePreviewHandler } from "./handlers/plugin/preview.ts";` to the imports block.
      - Add `preview: makePreviewHandler(pi),` to the `SubcommandHandlers` literal in `registerClaudePluginCommand` at line 75+, positioned to match the SubcommandHandlers interface order in router.ts.
    - In `docs/output-catalog.md` (MODIFIED):
      - Insert a new `## /claude:plugin preview` H2 section. Placement: alphabetically/group-wise — between `## /claude:plugin info` (or last plugin command section) and `## /claude:plugin marketplace` (if the file is grouped by command family); confirm by reading the surrounding context.
      - Section intro prose (~2-4 sentences): "The read-only diff/preview surface (D-53-01). Renders the bidirectional difference between the merged config (claude-plugins.json + claude-plugins.local.json) and the recorded state (state.json) for the next reload's reconcile. Runs against both scopes when --scope is omitted. NEVER writes any file, NEVER touches the network (NFR-5). Running it twice produces byte-identical output (DIFF-01 SC #2)."
      - 6 catalog states. Each state: H3 header + `<!-- catalog-state: NAME -->` HTML comment + 1-2 lines of explanatory prose + a fenced \`\`\`text byte-exact rendered string. The byte forms MUST match what `shared/notify.ts`'s renderer produces for the corresponding `NotificationMessage` payload — verify by running the catalog-uat test after writing and adjusting either the catalog or the renderer to align (the renderer arm's byte form is the binding contract; catalog mirrors it exactly).
        - `empty-steady-state`: "Preview: next reload will apply 0 actions." (one body line, no cascade).
        - `mp-add-plugin-install`: "Preview: next reload will apply 2 actions.\n\n● new-mp [user] (will add)\n  ● new-plugin (will install)" (or whatever subject-first form the renderer produces; orphan-fold means the child plugin's `[user]` bracket is omitted when its scope matches the marketplace's; if they differ, the bracket appears).
        - `plugin-pending-uninstall`: similar pattern with `(will uninstall)` on a child plugin under an existing-no-status marketplace header.
        - `enable-disable-transitions`: marketplace block with two plugin children, one `(will enable)` and one `(will disable)`.
        - `source-mismatch`: marketplace header `(failed) {source mismatch}` reusing the existing REASONS member (no new REASONS literal).
        - `invalid-config-abort`: marketplace header `(failed) {invalid manifest}` with the basename of the offending config file in the row body (not absolute path).
    - In `tests/architecture/catalog-uat.test.ts` (MODIFIED):
      - Add the `"/claude:plugin preview"` outer key to `FIXTURES` with 6 inner-state entries matching the catalog state names. Each entry: `{ pi: piWithBothLoaded(), message: <NotificationMessage payload> }`. The payload is constructed by hand (not by calling `previewReconcile` — the catalog-uat is a byte-equality runner that exercises `notify()` directly); the `enable-disable-transitions` state ESPECIALLY is hand-constructed since the Phase 53 planner produces zero `will enable` rows per Pitfall 53-4.
      - The `invalid-config-abort` state's `expectedSeverity` is `"error"` (CFG-03 is a hard error per the success criteria).
      - The other 5 states have no `expectedSeverity` key (default info per the v1.5 UXG-02 + v1.11 GRAM contract).
      - The inverse-walk gate (every FIXTURES entry has a catalog annotation) is exercised by adding 6 annotations to `docs/output-catalog.md` — confirm 0 orphans.
    - In `tests/architecture/notify-grammar-invariant.test.ts` (MODIFIED):
      - If the test enumerates `STATUS_TOKENS` directly, no edit needed (the new 6 tokens propagate automatically). If it hardcodes a token list, extend with the 6 new tokens.
      - Add a positive assertion: every `will *` token's renderer output starts with a glyph, followed by a name, followed by an optional `[scope]` bracket, followed by `(<token>)`. No token precedes the subject.
    - In `tests/architecture/no-orchestrator-network.test.ts` (MODIFIED):
      - Extend `FORBIDDEN_TARGETS` (at lines 50-56) to include `"extensions/pi-claude-marketplace/orchestrators/reconcile/preview.ts"` AND (belt-and-braces) `"extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts"` AND `"extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts"`. The `plan.ts` entry overlaps with the Plan 01 reconcile-planner-purity test (which is stricter); the overlap is harmless and defends against future regression.
    - In `tests/orchestrators/reconcile/preview.test.ts` (NEW):
      - 6+ tests per the artifacts_this_phase_produces inventory: idempotency (twice -> byte-equal), no-mutation (mtime + bytes unchanged), CFG-03 abort (invalid base -> failed row, planReconcile not called for that scope), CFG-03 abort (invalid local -> failed row), network/write isolation (no fs.writeFile / gitOps), scope fan-out (no `--scope` -> both scopes in project-first order; `--scope user` -> user only), single-notify (notify() called exactly once per invocation).
      - Fixture setup: use a populated state fixture (mirror the Plan 01 `loadPopulatedState` or `tests/persistence/fixtures/legacy/_loader.ts` patterns) + a corresponding populated `claude-plugins.json` produced via `buildConfigFromState` (so the steady-state case produces empty plans and the `empty-steady-state` advisory). For the CFG-03 fixture, write a malformed `claude-plugins.json` (e.g., `{` truncated) and assert the loadConfig trichotomy returns `invalid` -> orchestrator emits failed row.
      - The no-mutation assertion: capture mtime of `claude-plugins.json`, `claude-plugins.local.json`, `state.json` before invocation; assert unchanged after; read the byte content before/after and assert byte-equal. Use a temp scope root under `os.tmpdir()` so the test is isolated.
    - In `tests/edge/handlers/plugin/preview.test.ts` (NEW):
      - 6 tests per the artifacts_this_phase_produces inventory: USAGE on positional, USAGE on unknown flag, --scope user routing, --scope project routing, no-args fan-out, --scope foo (invalid value) USAGE.
      - Spy on the orchestrator (mock `previewReconcile` via module-level mock or via an injected dep — pick whichever pattern matches `tests/edge/handlers/plugin/list.test.ts`).
      - Spy on `notifyUsageError` for the USAGE assertions; assert the USAGE string passed matches the constant `"Usage: /claude:plugin preview [--scope user|project]"`.
  </behavior>
  <action>
    Task 2 closes the DIFF-02 atomic lockstep. Run `npm run check` ONLY after both Task 1 AND Task 2 edits are complete; the catalog-uat byte-equality runner is RED between the two tasks by design (Pitfall 53-3). The single commit message: `feat(reconcile): add /claude:plugin preview read-only diff command + 6 will-* tokens (DIFF-01 + DIFF-02 lockstep; D-53-01 preview / D-53-02 will-* form) [Phase 53 Plan 02]`. Commit body lists: 6 new STATUS_TOKENS / variants / renderer arms; new preview orchestrator + edge shim + router + register edits; 6 catalog states + 6 FIXTURES entries; length-locks updated; FORBIDDEN_TARGETS extended; 4 new test files. Per CONTEXT.md D-53-01 the subcommand is `preview`, not `diff` or `reconcile --dry-run` — verify every string literal `"preview"` in router/register/handler/USAGE matches. Per CONTEXT.md D-53-02 the tokens are `will *` form — verify NO occurrence of `to install` / `to add` / `to enable` etc. anywhere in the source / docs / tests added by this plan (a `grep -rn "to install\|to uninstall\|to add\|to remove\|to enable\|to disable" extensions/ docs/ tests/` filtered to this plan's file list should return 0 matches in the new content). Per Pitfall 53-1 the CFG-03 abort emits a `(failed) {invalid manifest}` row, NEVER an empty-desired-state diff — the `tests/orchestrators/reconcile/preview.test.ts` CFG-03 abort test is the structural defense; the catalog state `invalid-config-abort` is the byte-equality defense. Per Pitfall 53-3 the commit's `git diff --stat` should show exactly the files in this plan's `files_modified` list — anything outside is suspect. Per RESEARCH Security Threat Patterns the invalid-config row's file path is `path.basename(...)` (project-relative basename), NEVER absolute — verify in the orchestrator code AND in the catalog state's byte form. Per Pitfall 53-7 the source-mismatch row reuses the existing `"source mismatch"` REASONS member; do NOT add a new REASONS literal — REASONS stays at 29.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; SKIP=trufflehog pre-commit run --files $(printf '%s ' extensions/pi-claude-marketplace/shared/notify.ts extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts extensions/pi-claude-marketplace/orchestrators/reconcile/preview.ts extensions/pi-claude-marketplace/edge/handlers/plugin/preview.ts extensions/pi-claude-marketplace/edge/router.ts extensions/pi-claude-marketplace/edge/register.ts docs/output-catalog.md tests/architecture/catalog-uat.test.ts tests/architecture/notify-types.test.ts tests/architecture/notify-grammar-invariant.test.ts tests/architecture/no-orchestrator-network.test.ts tests/orchestrators/reconcile/notify.test.ts tests/orchestrators/reconcile/preview.test.ts tests/edge/handlers/plugin/preview.test.ts tests/shared/notify-v2.test.ts) &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"preview"' extensions/pi-claude-marketplace/edge/router.ts` returns >= 3 (TOP_LEVEL_SUBCOMMANDS entry + case "preview": arm + TOP_LEVEL_USAGE prose).
    - `grep -c "makePreviewHandler" extensions/pi-claude-marketplace/edge/register.ts` returns >= 2 (import + handler literal).
    - `grep -c "## .\?/claude:plugin preview" docs/output-catalog.md` returns >= 1 (the new H2 section exists).
    - `grep -c "catalog-state: " docs/output-catalog.md | head -1` shows the previous count + 6 (6 new annotations added).
    - `grep -c '"/claude:plugin preview"' tests/architecture/catalog-uat.test.ts` returns >= 1 (the outer FIXTURES key exists).
    - `grep -rn '"to install\|"to uninstall\|"to add\|"to remove\|"to enable\|"to disable' extensions/pi-claude-marketplace/orchestrators/reconcile/ extensions/pi-claude-marketplace/edge/handlers/plugin/preview.ts extensions/pi-claude-marketplace/shared/notify.ts docs/output-catalog.md tests/architecture/catalog-uat.test.ts tests/architecture/notify-types.test.ts tests/shared/notify-v2.test.ts tests/orchestrators/reconcile/preview.test.ts tests/edge/handlers/plugin/preview.test.ts | grep -v '^#' | wc -l` returns 0 (D-53-02: `will *` form, NOT `to *` form anywhere in the new content).
    - `grep -c "orchestrators/reconcile/preview\.ts" tests/architecture/no-orchestrator-network.test.ts` returns >= 1 (FORBIDDEN_TARGETS extended).
    - `node --test tests/architecture/catalog-uat.test.ts` passes — the byte-equality runner is GREEN, including the inverse-walk (zero orphans for the new section).
    - `node --test tests/architecture/notify-types.test.ts` passes — the new length-locks AND the new per-variant proofs are satisfied.
    - `node --test tests/architecture/notify-grammar-invariant.test.ts` passes — subject-first grammar holds for the 6 new variants.
    - `node --test tests/architecture/no-orchestrator-network.test.ts` passes — the extended FORBIDDEN_TARGETS gate holds (preview.ts imports no `platform/git` / no `gitOps`).
    - `node --test tests/shared/notify-v2.test.ts` passes — the 6 new byte-equality tests are GREEN.
    - `node --test tests/orchestrators/reconcile/preview.test.ts` passes — idempotency, no-mutation, CFG-03 abort, network/write isolation, scope fan-out, single-notify all GREEN.
    - `node --test tests/edge/handlers/plugin/preview.test.ts` passes — 6 shim tests GREEN.
    - `npm run check` exits 0 with overall unit-test count strictly greater than the Plan 01 close baseline; integration test count unchanged at >= 7.
    - Pre-commit hook passes; commit lands as ONE atomic commit; Conventional Commits message (<= 72 char title, body lines <= 80 chars).
    - `git diff --name-only HEAD~1 HEAD` (after commit) shows exactly the union of Task 1's and Task 2's `files_modified` (the merged plan's `files_modified` field, 15 entries). Anything else in the diff is a defect.
    - A manual smoke against the implementation (or a dedicated test): build a temp scope root with a populated `claude-plugins.json` whose desired state is byte-identical to the populated state; invoke `previewReconcile({ ctx, pi, cwd: tempRoot })`; assert exactly one `notify()` call with the `empty-steady-state` advisory body line; invoke again; assert byte-equal output.
  </acceptance_criteria>
  <done>
    `/claude:plugin preview` is a working read-only, no-network, no-write subcommand emitting subject-first `(will ...)` rows through the structured `notify()` v2 cascade. The 6 new tokens + variants + renderer arms + catalog states + FIXTURES entries + length-locks + FORBIDDEN_TARGETS extension + idempotency proof + CFG-03 abort proof + scope-fan-out shim + grammar invariant land in ONE atomic commit per DIFF-02 lockstep. `npm run check` GREEN. DIFF-01 SC #2 + SC #3 closed; DIFF-02 SC #3 closed. Phase 53 SHIPS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI args -> edge/handlers/plugin/preview.ts | Untrusted input string crosses here; parseArgs validates; positional or unknown flags route to notifyUsageError. |
| disk -> orchestrators/reconcile/preview.ts | Reads claude-plugins.json (CFG-03 trichotomy), claude-plugins.local.json, state.json — all already-validated upstream (typebox CONFIG_SCHEMA, STATE_VALIDATOR). Invalid signals NEVER coerced to empty desired state. |
| orchestrators/reconcile/preview.ts -> shared/notify.ts | Structured NotificationMessage payload — type-validated by the discriminated union; the renderer's assertNever default enforces exhaustiveness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-02-01 | Tampering | Invalid claude-plugins.json silently treated as empty desired state (CFG-03 violation) | mitigate | Pitfall 53-1 mitigation: previewReconcile inspects `outcome.base.status` AND `outcome.local.status` BEFORE invoking planReconcile; invalid arm routes through notify() as a structured `(failed) {invalid manifest}` row with the file basename. `tests/orchestrators/reconcile/preview.test.ts` CFG-03 abort tests are the structural defense; the catalog state `invalid-config-abort` is the byte-equality defense. Per the security_enforcement directive in planning_context: "CFG-03 invalid config aborts BEFORE planning so an invalid file can never render as a mass-uninstall preview." |
| T-53-02-02 | Information Disclosure | Absolute file paths leaked through (failed) row body for invalid-config-abort | mitigate | Use `path.basename(loc.configJsonPath)` or `path.basename(loc.configLocalJsonPath)` in the row body — never the absolute path. Verified by the catalog state `invalid-config-abort`'s byte-exact form and by a dedicated assertion in `tests/orchestrators/reconcile/preview.test.ts`. |
| T-53-02-03 | Tampering | Preview rows leak the /reload to pick up changes trailer (grammatically false for pre-transition output) | mitigate | shouldEmitReloadHint trigger set is the existing 4 head-of-PLUGIN_STATUSES tokens; the 6 new `will *` tokens are appended at the END of the tuples and STRUCTURALLY EXCLUDED from the trigger set. Verified by a test in tests/shared/notify-v2.test.ts: a cascade containing only `will *` tokens produces no /reload trailer. |
| T-53-02-04 | Tampering | Preview path side-effects (writes, network) | mitigate | tests/architecture/no-orchestrator-network.test.ts extended FORBIDDEN_TARGETS gates the gitOps / platform/git surface for preview.ts AND plan.ts AND notify.ts. tests/architecture/config-state-write-seams.test.ts (already in place from Phase 51) gates the write-side for the new files automatically because its walker covers EXTENSION_ROOT recursively. tests/orchestrators/reconcile/preview.test.ts's no-mutation test asserts file mtime + byte invariance across two invocations. |
| T-53-02-05 | Denial of Service | A user crafts a config file with millions of plugin entries to stall preview | accept | Bounded by upstream JSON parse cost in loadConfig (Phase 51); preview's planner walk is O(N+M). No regex per entry. The Pi UI's notify channel may truncate; that's the host's concern. |
| T-53-02-06 | Tampering | Adversarial plugin name with embedded ANSI escapes leaks through to terminal | accept | The notify channel is owned by the Pi host; sanitization is the host's responsibility. The renderer emits literal strings without quoting. This is consistent with all existing notify v2 emission sites; not a Phase 53 regression. |
| T-53-02-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs zero packages; no Package Legitimacy Audit required. |
</threat_model>

<verification>
- `npm run check` GREEN. Catalog UAT byte-equality runner GREEN. Length-locks GREEN. Grammar invariant GREEN. Network gate GREEN. notify-v2 byte-equality tests GREEN. Preview orchestrator tests GREEN (idempotency, no-mutation, CFG-03 abort, single-notify, scope fan-out). Edge shim tests GREEN.
- `git diff --name-only HEAD~1 HEAD` after Plan 02 commit shows exactly the 15 files in this plan's `files_modified`.
- `grep -rn '"to (install|uninstall|add|remove|enable|disable)"' extensions/pi-claude-marketplace/orchestrators/reconcile/ docs/output-catalog.md tests/architecture/catalog-uat.test.ts | grep -v '^#' | wc -l` returns 0 (D-53-02: `will *`, not `to *`).
- Manual smoke (or automated equivalent): a working preview command emits subject-first `(will ...)` rows, an empty-steady-state advisory, an invalid-config-abort failure row, identical bytes across two consecutive invocations.
</verification>

<success_criteria>
1. A user can run `/claude:plugin preview [--scope user|project]` and see exactly the marketplaces + plugins the next /reload's reconcile would add/install/uninstall/enable/disable. Running it twice produces byte-identical output. No file/state mutation. No network calls. (DIFF-01 SC #2)
2. Preview output follows subject-first `<glyph> <name> [<scope>] (<status>)` row grammar with the 6 new closed-set `(will ...)` tokens (per D-53-02). Every new token, every new renderer arm, every new discriminated-union variant, every new catalog state, every new FIXTURES entry, every length-lock update, every FORBIDDEN_TARGETS extension lands in the SAME atomic commit. (DIFF-02 SC #3)
3. A malformed or invalid `claude-plugins.json` or `claude-plugins.local.json` surfaces as a structured `(failed) {invalid manifest}` row — NEVER as an empty-desired-state diff that would render as a mass-uninstall preview (CFG-03 + Pitfall 53-1 + security_enforcement directive). The file path in the row body is `path.basename(...)`, never absolute.
4. Disabled plugins (config `enabled: false`, not yet materialized) NEVER appear as a pending install — only as `(will disable)` when already materialized (Pitfall 53-2).
5. The 6 new `will *` tokens are STRUCTURALLY EXCLUDED from `shouldEmitReloadHint`'s trigger set — preview output never emits `/reload to pick up changes`.
6. Tab completion for `/claude:plugin preview --scope <TAB>` works (auto-propagated from TOP_LEVEL_SUBCOMMANDS).
7. `npm run check` GREEN. Pre-commit hook passes. Conventional Commits message. ONE atomic commit.
</success_criteria>

<output>
Create `.planning/phases/53-pure-reconcile-planner-dry-run-preview/53-02-SUMMARY.md` when done, including:
- The 15-file diff summary (one bulleted line per file with a 1-sentence description of the change).
- The 6 new `will *` tokens enumerated explicitly: `will add`, `will remove`, `will install`, `will uninstall`, `will enable`, `will disable`. Confirm D-53-02 honored (NOT `to *` form).
- The 3 length-lock updates: STATUS_TOKENS 15 -> 21, MARKETPLACE_STATUSES 7 -> 9, PLUGIN_STATUSES 11 -> 15. REASONS unchanged at 29.
- The 6 catalog states + 6 FIXTURES entries enumeration; confirm zero orphans.
- The CFG-03 abort behavior with the byte form of the `invalid-config-abort` state and the `path.basename(...)` verification.
- The empty-steady-state advisory's exact byte form ("Preview: next reload will apply 0 actions." or whatever was chosen) so Phase 55 wiring can mirror.
- The idempotency proof: which fixture, which mtime check, which byte comparison.
- The Phase 54 hand-off: the `PluginWillEnableMessage` variant ships, the renderer arm ships, the catalog state ships, but the planner produces zero of them in Phase 53. Phase 54 will wire the bucket to a real disabled-state check.
- DIFF-01 SC #1 / SC #2 / SC #3 + DIFF-02 SC #3 status: CLOSED.
- The Phase 52 convergence proof status (closed in Plan 01) referenced for traceability.
- `npm run check` final unit test count + integration count.
</output>
