---
phase: 53
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/domain/source.ts
  - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/README.md
  - tests/architecture/reconcile-planner-purity.test.ts
  - tests/orchestrators/reconcile/plan.test.ts
  - tests/orchestrators/reconcile/plan-convergence.test.ts
  - tests/orchestrators/reconcile/notify.test.ts
autonomous: true
requirements:
  - DIFF-01
must_haves:
  truths:
    - "samePlannedSource has a single pure home in domain/source.ts and import/execute.ts re-imports it"
    - "planReconcile is a pure function that computes the 7-bucket bidirectional diff between MergedConfig and ExtensionState"
    - "planReconcile imports zero effectful modules (no node:fs, no notify, no save*, no withState*, no platform/git, no gitOps, no atomicWriteJson)"
    - "planReconcile(mergeScopeConfigs(buildConfigFromState(state), {}), state, scope) deepEqual emptyReconcilePlan(scope) for any populated state (Phase 52 convergence proof discharged)"
    - "buildReconcilePreviewNotification(plans) projects a ReconcilePlan[] to a CascadeNotificationMessage in pure code (no I/O)"
    - "npm run check stays GREEN at >= 1571 (Phase 52 baseline) + N new unit tests"
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts"
      provides: "ReconcilePlan + 7 PlannedXxx interfaces + emptyReconcilePlan factory"
      exports: ["ReconcilePlan", "PlannedMarketplaceAdd", "PlannedMarketplaceRemove", "PlannedPluginInstall", "PlannedPluginUninstall", "PlannedPluginEnable", "PlannedPluginDisable", "PlannedSourceMismatch", "emptyReconcilePlan"]
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts"
      provides: "Pure planReconcile(merged, state, scope) function (DIFF-01 foundation)"
      exports: ["planReconcile"]
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts"
      provides: "Pure buildReconcilePreviewNotification(plans) projection (plan -> CascadeNotificationMessage)"
      exports: ["buildReconcilePreviewNotification"]
    - path: "extensions/pi-claude-marketplace/domain/source.ts"
      provides: "samePlannedSource as a new sibling export to sourceLogical / parsePluginSource"
      contains: "export function samePlannedSource"
    - path: "tests/architecture/reconcile-planner-purity.test.ts"
      provides: "Grep-gate proving plan.ts has zero effectful imports"
    - path: "tests/orchestrators/reconcile/plan.test.ts"
      provides: "Exhaustive desired-x-actual matrix coverage for planReconcile (7 buckets)"
    - path: "tests/orchestrators/reconcile/plan-convergence.test.ts"
      provides: "Phase 52 deferred proof: build-from-state + merge + plan = empty"
    - path: "tests/orchestrators/reconcile/notify.test.ts"
      provides: "Plan -> CascadeNotificationMessage projection tests"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts"
      to: "extensions/pi-claude-marketplace/domain/source.ts"
      via: "import samePlannedSource"
      pattern: "from\\s+[\"'][^\"']*domain/source[^\"']*[\"']"
    - from: "extensions/pi-claude-marketplace/orchestrators/import/execute.ts"
      to: "extensions/pi-claude-marketplace/domain/source.ts"
      via: "import samePlannedSource (replaces local definition)"
      pattern: "samePlannedSource.*from.*domain/source"
    - from: "tests/orchestrators/reconcile/plan-convergence.test.ts"
      to: "extensions/pi-claude-marketplace/persistence/migrate-config.ts"
      via: "discharges Section D deferred proof at tests/persistence/migrate-config.test.ts:468-475"
      pattern: "buildConfigFromState"
---

<objective>
Land the byte-neutral planner foundation for the Phase 53 reconcile/preview surface (DIFF-01 only). One atomic commit produces: (1) the `samePlannedSource` extraction from `orchestrators/import/execute.ts` into the pure `domain/source.ts` module so the planner imports a leaf-pure helper without dragging the import orchestrator's effectful transitive closure; (2) the new `orchestrators/reconcile/` directory with `types.ts` (`ReconcilePlan` + 7 `Planned*` interfaces + `emptyReconcilePlan`), `plan.ts` (pure `planReconcile(MergedConfig, ExtensionState, Scope) -> ReconcilePlan` 7-bucket bidirectional diff), and `notify.ts` (pure `buildReconcilePreviewNotification(plans) -> CascadeNotificationMessage` projection mirroring `buildImportNotificationMarketplaces`); (3) the architecture purity grep-gate at `tests/architecture/reconcile-planner-purity.test.ts`; (4) the exhaustive matrix tests at `tests/orchestrators/reconcile/plan.test.ts`; (5) the Phase 52 deferred convergence proof at `tests/orchestrators/reconcile/plan-convergence.test.ts`; (6) the plan-to-message projection tests at `tests/orchestrators/reconcile/notify.test.ts`.

Purpose: Discharge the Phase 52 hand-off obligation (convergence proof must land in Phase 53), lock the pure planner contract structurally before the user-visible bytes commit in Plan 02 (Pitfall 53-3 guards against partial token-set ships by keeping commit 1 byte-neutral on `shared/notify.ts`), and establish the seven action buckets that Phase 55 will execute and Plan 02 will render.

Output: 4 new source modules (1 in domain, 3 in a new orchestrators/reconcile directory), 1 modified source module (import/execute.ts), 1 README, 4 new test files. No new tokens in `shared/notify.ts` (zero-byte delta there). No new files in `edge/`. No new entries in catalog or FIXTURES. No SPLIT-01 cast sites added.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-pure-reconcile-planner-dry-run-preview/53-CONTEXT.md
@.planning/phases/53-pure-reconcile-planner-dry-run-preview/53-RESEARCH.md
@.planning/phases/53-pure-reconcile-planner-dry-run-preview/53-PATTERNS.md
@.planning/phases/53-pure-reconcile-planner-dry-run-preview/53-VALIDATION.md

@extensions/pi-claude-marketplace/domain/source.ts
@extensions/pi-claude-marketplace/orchestrators/import/execute.ts
@extensions/pi-claude-marketplace/orchestrators/import/marketplaces.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts
@extensions/pi-claude-marketplace/persistence/config-merge.ts
@extensions/pi-claude-marketplace/persistence/state-io.ts
@extensions/pi-claude-marketplace/persistence/migrate-config.ts
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/shared/types.ts
@tests/architecture/no-orchestrator-network.test.ts
@tests/persistence/migrate-config.test.ts
@tests/orchestrators/import/execute.test.ts
</context>

<artifacts_this_phase_produces>
Symbols this phase creates (consumed by Plan 02 and by Phase 55 in v1.12):

Types (in `orchestrators/reconcile/types.ts`):
- `ReconcilePlan` (interface): `{ scope, marketplacesToAdd, marketplacesToRemove, pluginsToInstall, pluginsToUninstall, pluginsToEnable, pluginsToDisable, sourceMismatches }` — all `readonly` arrays.
- `PlannedMarketplaceAdd`: `{ scope, marketplace, source, configSource }` where `source: string` (raw user input per SP-7) and `configSource: "base" | "local"`.
- `PlannedMarketplaceRemove`: `{ scope, marketplace }`.
- `PlannedPluginInstall`: `{ scope, plugin, marketplace, configSource }`.
- `PlannedPluginUninstall`: `{ scope, plugin, marketplace }`.
- `PlannedPluginEnable`: `{ scope, plugin, marketplace }`.
- `PlannedPluginDisable`: `{ scope, plugin, marketplace }`.
- `PlannedSourceMismatch`: `{ scope, marketplace, declaredSource, recordedSource, cause }` where `cause: "source-mismatch" | "unknown-stored"`.

Functions:
- `emptyReconcilePlan(scope: Scope): ReconcilePlan` (in `types.ts`) — empty-array factory; Phase 52 convergence test uses it as the deepEqual target.
- `planReconcile(merged: MergedConfig, state: ExtensionState, scope: Scope): ReconcilePlan` (in `plan.ts`) — pure bidirectional 7-bucket diff.
- `buildReconcilePreviewNotification(plans: readonly ReconcilePlan[]): CascadeNotificationMessage` (in `notify.ts`) — pure plan -> `MarketplaceNotificationMessage[]` projection ordered by name-then-scope; called by Plan 02's `previewReconcile` orchestrator.

Re-exports:
- `samePlannedSource` (in `domain/source.ts`) — extracted from `orchestrators/import/execute.ts`. Public export so both `orchestrators/import/execute.ts` (existing site at line 529) and `orchestrators/reconcile/plan.ts` (new site) import it.

Tests:
- `tests/architecture/reconcile-planner-purity.test.ts` — grep-gate proving `plan.ts` imports nothing effectful.
- `tests/orchestrators/reconcile/plan.test.ts` — 7-bucket matrix coverage.
- `tests/orchestrators/reconcile/plan-convergence.test.ts` — Phase 52 deferred proof.
- `tests/orchestrators/reconcile/notify.test.ts` — projection tests.

What Plan 02 then consumes (this plan does NOT add): `MarketplaceWillAddMessage` / `MarketplaceWillRemoveMessage` / `PluginWillInstallMessage` / `PluginWillUninstallMessage` / `PluginWillEnableMessage` / `PluginWillDisableMessage` variants in `shared/notify.ts`; the 6 new `STATUS_TOKENS` literals (`will add`, `will remove`, `will install`, `will uninstall`, `will enable`, `will disable`); the `MarketplaceNotificationMessage` + `PluginNotificationMessage` union extensions; the renderer arms; the catalog states; the `FIXTURES` entries; the `orchestrators/reconcile/preview.ts` orchestrator; the `edge/handlers/plugin/preview.ts` shim; the router + completion provider edits.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract samePlannedSource into domain/source.ts; rewire import/execute.ts; add reconcile types + pure planner + notify projection (RED tests authored first)</name>
  <files>
    extensions/pi-claude-marketplace/domain/source.ts,
    extensions/pi-claude-marketplace/orchestrators/import/execute.ts,
    extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts,
    extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts,
    extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts,
    extensions/pi-claude-marketplace/orchestrators/reconcile/README.md,
    tests/architecture/reconcile-planner-purity.test.ts,
    tests/orchestrators/reconcile/plan.test.ts,
    tests/orchestrators/reconcile/plan-convergence.test.ts,
    tests/orchestrators/reconcile/notify.test.ts
  </files>
  <read_first>
    extensions/pi-claude-marketplace/domain/source.ts,
    extensions/pi-claude-marketplace/orchestrators/import/execute.ts (read lines 1-260 for samePlannedSource at 186-216 + the only other call site at 529),
    extensions/pi-claude-marketplace/orchestrators/import/marketplaces.ts (read lines 1-180 for the buildClaudeImportPlan pure-planner shape and scopedPlan inner function),
    extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts (read lines 1-90 for the read-only orchestrator header docstring discipline),
    extensions/pi-claude-marketplace/persistence/config-merge.ts (read full file for MergedConfig + ScopeConfig + mergeScopeConfigs + loadMergedScopeConfig + ScopeLoadOutcome shapes),
    extensions/pi-claude-marketplace/persistence/state-io.ts (read STATE_SCHEMA + ExtensionState exports + lines 80-160 for shape; pay attention to marketplaces / plugins record structure including soft-degraded entries),
    extensions/pi-claude-marketplace/persistence/migrate-config.ts (read full file for buildConfigFromState projection that this plan's convergence proof feeds back through planReconcile),
    extensions/pi-claude-marketplace/shared/notify.ts (read lines 240-700 for MarketplaceNotificationMessage / PluginNotificationMessage / CascadeNotificationMessage / NotificationMessage union shapes; do NOT add new variants here — Plan 02 owns that),
    extensions/pi-claude-marketplace/shared/types.ts (Scope type definition),
    tests/architecture/no-orchestrator-network.test.ts (full file — the grep-gate template the purity test mirrors; note the stripComments walker + ENOENT skip behavior; do NOT modify FORBIDDEN_TARGETS in this plan — Plan 02 adds the preview.ts entries),
    tests/persistence/migrate-config.test.ts (lines 1-80 for loadPopulatedState; lines 280-368 for the Section D deferred proof comment and the migrate-then-reconcile data-level proof — DO NOT delete or duplicate that comment; the planner-level proof lives in the new tests/orchestrators/reconcile/plan-convergence.test.ts and the existing Section D comment should be updated to point at the new file location),
    tests/orchestrators/import/execute.test.ts (lines 1-260 to confirm samePlannedSource's only test-site is the comment at :241; the helper has zero direct unit tests, so extraction is behavior-preserving as long as both call sites import the new home),
    tests/architecture/notify-grammar-invariant.test.ts (skim — do NOT modify in this plan; Plan 02 owns it)
  </read_first>
  <behavior>
    - Test (purity gate): `tests/architecture/reconcile-planner-purity.test.ts` reads `orchestrators/reconcile/plan.ts`, strips comments, and asserts the source contains NONE of: `from "node:fs"`, `from "node:fs/promises"`, any `from "..*platform/git..*"`, the bare identifier `gitOps`, the bare identifier `notify`, `saveState`, `saveConfig`, `atomicWriteJson`, `withStateGuard`, `withLockedStateTransaction`. Use the same `stripComments` regex pair as `tests/architecture/no-orchestrator-network.test.ts` so header comments documenting "this module never imports notify" do not self-invalidate the gate. The grep MUST be the comment-stripped source, NOT the raw text. Implement as a single `node:test` test named `DIFF-01: planReconcile is pure (no fs/network/notify/save/lock imports)` that produces an empty `offenders` array.
    - Test (planner matrix): `tests/orchestrators/reconcile/plan.test.ts` exhaustively covers the 7 buckets across the desired-x-actual matrix. At minimum 12 tests, one per cell of the truth tables:
      - Marketplace cells (4 cells): (declared, recorded, same-source) -> NO action; (declared, recorded, source-mismatch) -> 1 PlannedSourceMismatch with cause "source-mismatch" carrying both source strings; (declared, recorded, unknown-stored) -> 1 PlannedSourceMismatch with cause "unknown-stored"; (declared, not recorded) -> 1 PlannedMarketplaceAdd carrying the raw user-input source string + configSource ("base" or "local") from MergedConfig provenance; (not declared, recorded) -> 1 PlannedMarketplaceRemove.
      - Plugin cells (6 cells under the three-state declared model): (declared+enabled-undefined OR enabled-true, not recorded) -> 1 PlannedPluginInstall (D-04 consume-time default: only `=== false` excludes); (declared+enabled-true, recorded) -> NO action (steady state); (declared+enabled-false, recorded) -> 1 PlannedPluginDisable; (declared+enabled-false, not recorded) -> NO action; (not declared, recorded) -> 1 PlannedPluginUninstall; (declared+enabled-true, recorded, but Phase 54 will-disable-flag set in state — this bucket stays structurally empty in Phase 53 per Pitfall 53-4; assert the bucket length is 0 for any input the Phase 53 planner sees).
      - Edge cells: empty MergedConfig + empty state -> emptyReconcilePlan; empty MergedConfig + populated state -> every marketplace + plugin in the uninstall/remove buckets, NOTHING in the install/add buckets (this is the "naked uninstall everything" hazard the CFG-03 abort prevents at the orchestrator level, but the planner itself MUST produce this bucket structure — the abort is the orchestrator's responsibility); populated MergedConfig + empty state -> every marketplace + enabled plugin in the add/install buckets, NOTHING in uninstall/remove.
      - Dangling reference: a plugin entry whose `${plugin}@${marketplace}` marketplace name does not appear in MergedConfig.marketplaces AND does not appear in state.marketplaces -> the planner records a PlannedSourceMismatch with `cause: "source-mismatch"` and `declaredSource: ""` and `recordedSource: "<marketplace not declared>"` (or similar sentinel; pick a stable byte form and document in the planner header docstring; Phase 55 surfaces it as a planning-time advisory).
      - Plugin-key parser: a plugin key containing multiple `@` symbols parses by `lastIndexOf("@")` — assert `evil@evil@marketplace` parses to plugin "evil@evil", marketplace "marketplace".
    - Test (Phase 52 convergence proof): `tests/orchestrators/reconcile/plan-convergence.test.ts` is a single test discharging the Phase 52 Section D deferral. Use the existing `loadPopulatedState` helper from `tests/persistence/migrate-config.test.ts` — either re-export it from a new local helper or copy the implementation inline with a comment cross-referencing the Phase 52 site. Test body: `const state = await loadPopulatedState(); const merged = mergeScopeConfigs(buildConfigFromState(state), {}); const plan = planReconcile(merged, state, "project"); assert.deepEqual(plan, emptyReconcilePlan("project"));`. The test MUST also run with `scope: "user"` for symmetry. Both assertions deepEqual against `emptyReconcilePlan(scope)`. Update the existing Section D comment at `tests/persistence/migrate-config.test.ts:468` to point at the new file (replace "DEFERRED to Phase 53" with "DISCHARGED in tests/orchestrators/reconcile/plan-convergence.test.ts (Phase 53 Plan 01)"); keep the data-level proof block at Section D in migrate-config.test.ts unchanged otherwise.
    - Test (projection): `tests/orchestrators/reconcile/notify.test.ts` covers `buildReconcilePreviewNotification`. At minimum: empty plan list -> empty `marketplaces` array on the returned message; one plan with one MarketplaceAdd -> one MarketplaceNotificationMessage with whatever structural shape the projection chooses (Plan 02 will define the exact `status` field; for Plan 01 the projection MAY emit a placeholder string like the existing `(added)` or a TODO sentinel — the tests assert structural shape, NOT byte-exact rendered output; byte-exact assertions land in Plan 02's catalog-uat extension); ordering test: two plans with marketplaces ("zebra" in user scope, "alpha" in project scope) projected and the result list is ordered by name-then-scope (alpha before zebra), mirroring `compareByNameThenScope` at `import/execute.ts:496`; one plan with one PluginInstall under one MarketplaceAdd -> the marketplace block carries the plugin in its `plugins` array.
    - Production code:
      - In `extensions/pi-claude-marketplace/domain/source.ts`: add `export function samePlannedSource(stored: unknown, plannedRaw: string): boolean | "unknown-stored"` as a sibling to `sourceLogical` / `parsePluginSource`. Copy the body verbatim from `orchestrators/import/execute.ts:186-216`. The function consumes `parsePluginSource` and `sourceLogical` (already in this module). Add a brief header doc-comment cross-referencing both callers (`import/execute.ts` historical site + `orchestrators/reconcile/plan.ts` new site).
      - In `extensions/pi-claude-marketplace/orchestrators/import/execute.ts`: delete the local `samePlannedSource` definition at 186-216; add `import { samePlannedSource } from "../../domain/source.ts";` to the import block at the top of the file. The single call site at line 529 needs no change — same identifier, same signature, same semantics. The accompanying header comment block at 178-185 (if any) describing the helper should be moved into `domain/source.ts` next to the new export OR retained in `execute.ts` with a "moved to domain/source.ts in Phase 53 Plan 01" pointer (developer's choice — both are acceptable; keep the change minimal).
      - In `extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts`: export the 7 `Planned*` interfaces and `ReconcilePlan` + `emptyReconcilePlan` per the artifacts_this_phase_produces inventory. All array fields are `readonly Planned*[]`. `emptyReconcilePlan(scope)` returns the object literal with all 7 array fields initialized to empty arrays.
      - In `extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts`: implement `planReconcile(merged, state, scope)` with:
        - Type-only imports from `persistence/config-merge.ts` (`MergedConfig`), `persistence/state-io.ts` (`ExtensionState`), `shared/types.ts` (`Scope`), `./types.ts` (planner types + `emptyReconcilePlan`).
        - Value import: `samePlannedSource` from `../../domain/source.ts`.
        - Marketplace walk: iterate `Object.entries(merged.marketplaces ?? {})` and `Object.entries(state.marketplaces ?? {})` partitioning into the 4 marketplace buckets (add / remove / steady-state / source-mismatch) per the matrix tests above. For the steady-state cell, call `samePlannedSource(recordedRecord.source, declaredEntry.source)` — if `true`, NO action; if `false`, push to `sourceMismatches` with `cause: "source-mismatch"`, `declaredSource: declaredEntry.source`, `recordedSource: sourceLogical(parsePluginSource(recordedRecord.source))` (use the existing `sourceLogical` for stable diagnostic form); if `"unknown-stored"`, push with `cause: "unknown-stored"` and the recorded source as `String(recordedRecord.source)`.
        - Plugin walk: iterate `Object.entries(merged.plugins ?? {})` and the recorded plugins across `state.marketplaces[mp].plugins` records. Plugin key parser: `const at = key.lastIndexOf("@"); const plugin = key.slice(0, at); const marketplace = key.slice(at + 1);` — document the `@`-in-plugin-name caveat in a header docstring mirroring `migrate-config.ts` D-01 / Pitfall 52-6 prose. Apply the three-state model: a plugin entry with `enabled === false` is declared-but-disabled; with `enabled === true` or `enabled === undefined` is declared-and-enabled (D-04 consume-time default). Partition into the 6 plugin buckets (install / uninstall / enable / disable / steady-state / no-op-when-disabled-and-absent) per the matrix tests.
        - Phase 54 hand-off comment: a top-level docstring noting that `pluginsToEnable` is structurally empty for any Phase 53-only input because state.json carries no "currently disabled" marker; Phase 54 will wire the bucket to a real `state.disabled === true` check. The matrix test for the (declared+enabled, recorded, locally-disabled) cell asserts the empty bucket and includes a comment referencing Pitfall 53-4.
        - Dangling reference: a `${plugin}@${marketplace}` whose marketplace exists in neither map produces a `PlannedSourceMismatch` with the sentinel byte form documented above and Phase 55-deferred semantics.
        - Header docstring states: "DIFF-01 pure bidirectional diff. NEVER touches the disk or network. Disabled-entry rule (Pitfall 53-2): `enabled === false` excludes from desired-materialized; `=== true` or `undefined` includes (D-04). Phase 53 produces `pluginsToEnable` as a structurally-empty bucket — Phase 54 wires it to a real disabled-state check. Source comparison delegates to `samePlannedSource` (moved to `domain/source.ts` in Phase 53 Plan 01)."
      - In `extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts`: implement `buildReconcilePreviewNotification(plans: readonly ReconcilePlan[]): CascadeNotificationMessage` mirroring `buildImportNotificationMarketplaces` at `orchestrators/import/execute.ts:292-497`. Use the same `MarketplaceBlock { key, name, scope, status?, plugins }` shape and `ensureMarketplaceBlock(byMp, scope, mpName)` factory keyed by `${scope}:${mpName}`. Walk the 7 buckets and per bucket either set `block.status` or push to `block.plugins`. For Plan 01 the projection MAY use placeholder status strings (e.g., `"added"`, `"installed"`) so the unit shape is exercised without depending on the Plan 02 token set; the projection's status assignment is finalized in Plan 02 alongside the renderer arms. Sort the final `marketplaces` array using the existing `compareByNameThenScope` (import from `import/execute.ts` or copy the 6-line helper — copying is preferred to keep the reconcile family self-contained). Return `{ marketplaces: [...] }` typed as `CascadeNotificationMessage`. The function is pure; no I/O.
      - In `extensions/pi-claude-marketplace/orchestrators/reconcile/README.md`: brief pattern notes (~30-60 lines) covering: purpose of the directory, the purity discipline, the 7-bucket model, the Phase 54 hand-off for the enable/disable buckets, the Plan 01 vs Plan 02 split, and pointers to the analog modules in `orchestrators/import/` and `orchestrators/marketplace/`.
    - Plan 02 hand-off (DO NOT IMPLEMENT in Plan 01): leave the projection's status assignments as placeholders that Plan 02 will replace with the `will add` / `will remove` / `will install` / `will uninstall` / `will enable` / `will disable` tokens (D-53-02). Plan 01's tests assert structural shape only. Plan 02 takes the projection's caller-visible status field as the seam where the new tokens land.
  </behavior>
  <action>
    Apply Pitfall 53-3 hard pre-commit discipline: this plan's single commit MUST NOT touch `extensions/pi-claude-marketplace/shared/notify.ts`, MUST NOT add entries to `docs/output-catalog.md`, MUST NOT add entries to `tests/architecture/catalog-uat.test.ts`'s `FIXTURES` map, MUST NOT change length-lock numbers in `tests/architecture/notify-types.test.ts`, MUST NOT modify `tests/architecture/notify-grammar-invariant.test.ts`. Confirm with `git diff --name-only` before commit; any of those files in the diff is a defect. Per CONTEXT.md D-53 discretion, the `samePlannedSource` extraction is approved as the Pattern 2 cleanup; the new home is `domain/source.ts` (not a new `source-compare.ts` file — extending the existing pure module per RESEARCH Open Question 5 recommendation). Per CONTEXT.md "Inherited constraints" the planner must be pure (no fs, no network imports) — mirror the architecture-test enforcement style used by `tests/architecture/no-orchestrator-network.test.ts`. Per CONTEXT.md "Inherited constraints" the convergence proof MUST land in this phase (this plan); failure to discharge the Phase 52 deferral is a phase-gate failure. Per Pitfall 53-4, the `pluginsToEnable` bucket is structurally empty in Phase 53; the matrix test asserts the empty bucket for every cell where the Phase 53 state model could produce it. Per Pitfall 53-2 D-04 default: `enabled === undefined` includes in the desired-materialized set; only `=== false` excludes. Per Pitfall 53-7, the `sourceMismatches` bucket carries truthful causes (`"source-mismatch"` or `"unknown-stored"`); do NOT introduce `{network unreachable}` or any reason vocabulary in Plan 01 (the rendered reason is a Plan 02 concern). Per the architecture purity test's grep gate, ANY use of the bare identifier `notify` in `plan.ts` source (even in a comment that survives `stripComments`) fails the gate; use the precomputed `stripComments` regex pair to ensure comment prose is stripped before grepping. After all files are written, run the new test suites in isolation and then `npm run check`. Discharges DIFF-01 SC #1 (purity + matrix) and the Phase 52 SC#4 (convergence proof) deferral; DIFF-01 SC #2 (idempotent preview command) and SC #3 (catalog grammar) land in Plan 02. No new dependencies. No new SPLIT-01 cast sites. The commit message: `feat(reconcile): land pure planReconcile + types + projection + purity gate (DIFF-01 foundation; Phase 52 convergence proof discharged) [Phase 53 Plan 01]`.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; SKIP=trufflehog pre-commit run --files extensions/pi-claude-marketplace/domain/source.ts extensions/pi-claude-marketplace/orchestrators/import/execute.ts extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts extensions/pi-claude-marketplace/orchestrators/reconcile/README.md tests/architecture/reconcile-planner-purity.test.ts tests/orchestrators/reconcile/plan.test.ts tests/orchestrators/reconcile/plan-convergence.test.ts tests/orchestrators/reconcile/notify.test.ts tests/persistence/migrate-config.test.ts &amp;&amp; npm run typecheck &amp;&amp; node --test tests/architecture/reconcile-planner-purity.test.ts tests/orchestrators/reconcile/plan.test.ts tests/orchestrators/reconcile/plan-convergence.test.ts tests/orchestrators/reconcile/notify.test.ts tests/orchestrators/import/execute.test.ts &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only HEAD` AFTER the commit shows exactly the 10 files listed in this plan's `files_modified` (plus the one-line comment update at `tests/persistence/migrate-config.test.ts:468`). `shared/notify.ts`, `docs/output-catalog.md`, `tests/architecture/catalog-uat.test.ts`, `tests/architecture/notify-types.test.ts`, `tests/architecture/notify-grammar-invariant.test.ts` are NOT in the diff. Verify with `git diff --name-only HEAD~1 HEAD | grep -E '(shared/notify\.ts|docs/output-catalog\.md|catalog-uat\.test\.ts|notify-types\.test\.ts|notify-grammar-invariant\.test\.ts)$' | grep -v '^#' | wc -l` returns 0.
    - `grep -E "^export function samePlannedSource" extensions/pi-claude-marketplace/domain/source.ts` returns exactly 1 line.
    - `grep -E "^function samePlannedSource" extensions/pi-claude-marketplace/orchestrators/import/execute.ts` returns 0 lines (the local definition was deleted).
    - `grep -E "samePlannedSource" extensions/pi-claude-marketplace/orchestrators/import/execute.ts | grep -v '^#'` shows the call site at the existing line plus the new `import` statement (>= 2 matches, no local function definition).
    - `node --test tests/architecture/reconcile-planner-purity.test.ts` passes (1 test GREEN).
    - `node --test tests/orchestrators/reconcile/plan.test.ts` passes (>= 12 tests GREEN covering the desired-x-actual matrix; the (declared+enabled, recorded, locally-disabled-future-Phase-54) cell explicitly asserts `pluginsToEnable.length === 0`).
    - `node --test tests/orchestrators/reconcile/plan-convergence.test.ts` passes (2 tests GREEN: project scope + user scope, both deepEqual `emptyReconcilePlan(scope)`).
    - `node --test tests/orchestrators/reconcile/notify.test.ts` passes (>= 4 tests GREEN: empty list / single MarketplaceAdd / name-then-scope ordering / plugin nested under marketplace).
    - `node --test tests/orchestrators/import/execute.test.ts` still passes byte-identically (no regression from the extraction; the existing call site at execute.ts:529 still behaves the same).
    - `npm run check` exits 0 with unit-test count >= 1571 + 4 (Phase 52 baseline + the new test files; matrix file alone adds >= 12, so the actual count will be substantially higher; gate is "no regression and >= 1571"); integration test count unchanged at >= 7.
    - The Phase 52 Section D comment at `tests/persistence/migrate-config.test.ts` is updated in-place from "DEFERRED to Phase 53" to point at the new `tests/orchestrators/reconcile/plan-convergence.test.ts` file; the data-level proof block in migrate-config.test.ts itself is byte-unchanged otherwise.
    - The README at `extensions/pi-claude-marketplace/orchestrators/reconcile/README.md` documents the 7-bucket model AND the Plan 01 vs Plan 02 split AND the Phase 54 hand-off for enable/disable buckets.
    - The pre-commit hook run passes; no `SKIP=` flag beyond `trufflehog` (per worktree convention if applicable; otherwise plain `pre-commit run` is sufficient).
    - Commit message follows Conventional Commits (`feat(reconcile): ...`), is <= 72 chars in the title, body lines <= 80 chars.
  </acceptance_criteria>
  <done>
    DIFF-01 foundation (pure planner + types + projection + purity gate) lands as one byte-neutral commit. Phase 52 Section D convergence-proof deferral is discharged. `samePlannedSource` has a single home in `domain/source.ts` consumed by both the import family and the new reconcile family. Zero changes to `shared/notify.ts`, the catalog, or the catalog-uat FIXTURES — Plan 02 will land those atomically with the user-visible bytes. `npm run check` GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MergedConfig -> planner | Already-typebox-validated data (CFG-03 trichotomy) crosses from `persistence/config-merge.ts` into the pure planner. The planner consumes it AS-IS; no re-validation. |
| ExtensionState -> planner | Already STATE_VALIDATOR-validated at load. Same pattern. |
| domain/source.ts -> reconcile/plan.ts | Pure-to-pure import. No effect transfer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01-01 | Tampering | `orchestrators/reconcile/plan.ts` (planner purity) | mitigate | Architecture grep-gate at `tests/architecture/reconcile-planner-purity.test.ts` enforces NO fs / network / notify / save / lock imports. Comment-stripped grep prevents header-prose self-invalidation. |
| T-53-01-02 | Tampering | Plugin key parser (`evil@evil@@evil` adversarial input) | accept | The planner does NOT execute or dispatch — it only records buckets. `lastIndexOf("@")` gives a deterministic parse; Phase 55 apply path is where actual side effects land and owns the runtime safety gate. CONFIG_SCHEMA (Phase 51) is the upstream syntactic gate. |
| T-53-01-03 | Information Disclosure | `sourceMismatches` carries declared/recorded source strings | accept | Source strings are user-authored config values, not credentials. The renderer in Plan 02 will not emit absolute paths in failure rows (see Plan 02 threat model). For Plan 01 the structural shape is tested without rendered output. |
| T-53-01-04 | Denial of Service | Adversarial `MergedConfig` with billions of plugin entries | accept | Bounded by user-authored config file size; typebox CONFIG_SCHEMA enforces JSON parse cost upstream. The planner is O(N+M) over declared + recorded entries. No parse / regex compilation per entry. |
| T-53-01-05 | Tampering | `samePlannedSource` extraction breaks import path semantics | mitigate | The helper has 2 call sites (one in execute.ts at :529, one new in reconcile/plan.ts). The existing `tests/orchestrators/import/execute.test.ts` exercises the import path end-to-end; that suite must stay GREEN. Same-function semantics, no shape change. |
| T-53-01-SC | Tampering | npm/pip/cargo installs | mitigate | None. This plan installs zero packages; no Package Legitimacy Audit required. |
</threat_model>

<verification>
- `npm run check` GREEN (>= 1571 + new test counts).
- `git diff --name-only HEAD~1 HEAD` shows ONLY the 10 declared files (+ one-line comment touch at `tests/persistence/migrate-config.test.ts:468`); ZERO touches to `shared/notify.ts`, `docs/output-catalog.md`, `tests/architecture/catalog-uat.test.ts`, `tests/architecture/notify-types.test.ts`, `tests/architecture/notify-grammar-invariant.test.ts`.
- Pre-commit hook passes (`SKIP=trufflehog` only inside worktrees per CLAUDE.md convention).
- `node --test tests/architecture/reconcile-planner-purity.test.ts` passes — the gate that protects DIFF-01 SC #1 purity invariant across the rest of v1.12.
- `node --test tests/orchestrators/reconcile/plan-convergence.test.ts` passes — the Phase 52 SC#4 obligation is discharged.
- `node --test tests/orchestrators/import/execute.test.ts` passes byte-identically — confirms the `samePlannedSource` extraction is behavior-preserving.
</verification>

<success_criteria>
1. The pure `planReconcile(MergedConfig, ExtensionState, Scope) -> ReconcilePlan` function exists in `orchestrators/reconcile/plan.ts` with the 7-bucket bidirectional shape. The architecture purity gate enforces NO fs / network / notify / save / lock imports. (DIFF-01 SC #1 foundation)
2. The Phase 52 deferred convergence proof is discharged: `planReconcile(mergeScopeConfigs(buildConfigFromState(state), {}), state, scope) deepEqual emptyReconcilePlan(scope)` for both project and user scopes against a populated state fixture.
3. `samePlannedSource` lives at `domain/source.ts` as a sibling export to `sourceLogical` and `parsePluginSource`; both call sites (`orchestrators/import/execute.ts:529` and the new `orchestrators/reconcile/plan.ts`) import from there. The existing import-path test suite stays GREEN.
4. `buildReconcilePreviewNotification(plans) -> CascadeNotificationMessage` exists in `orchestrators/reconcile/notify.ts` as a pure projection mirroring the `buildImportNotificationMarketplaces` template; projection assigns structural shape (Plan 02 finalizes the `will *` token strings).
5. `npm run check` GREEN. Zero changes to `shared/notify.ts`, the catalog, or the catalog-uat FIXTURES (Plan 02 owns those byte deltas).
6. The plan ships as ONE atomic commit. Pre-commit hook passes. Conventional Commits message.
</success_criteria>

<output>
Create `.planning/phases/53-pure-reconcile-planner-dry-run-preview/53-01-SUMMARY.md` when done, including:
- Files created (10 new + 1 modified import/execute.ts + 1 modified migrate-config.test.ts comment touch).
- The empty-bucket assertion for the Phase 54 hand-off cell (`pluginsToEnable` structurally empty for any Phase 53 input).
- The Phase 52 convergence-proof discharge: which file holds the proof, what it asserts, both scopes covered.
- The `samePlannedSource` extraction record: 2 call sites verified, behavior-preserving.
- Test count delta (Phase 52 close: 1571 unit + 7 integration -> Plan 01 close: 1571 + N unit + 7 integration).
- Plan 02 hand-off recipe: which seam in `orchestrators/reconcile/notify.ts` Plan 02 takes over to wire the `will *` token strings into the projection's marketplace.status and plugin.status fields.
</output>
