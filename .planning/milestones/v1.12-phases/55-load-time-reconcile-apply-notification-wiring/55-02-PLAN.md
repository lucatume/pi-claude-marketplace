---
phase: 55
plan: 02
type: execute
wave: 2
depends_on:
  - 55-01
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/index.ts
  - docs/output-catalog.md
  - tests/orchestrators/reconcile/apply.test.ts
  - tests/edge/index-handler.test.ts
  - tests/shared/notify-v2.test.ts
  - tests/architecture/catalog-uat.test.ts
  - tests/architecture/notify-types.test.ts
  - tests/architecture/notify-grammar-invariant.test.ts
autonomous: true
requirements:
  - RECON-01
  - RECON-02
  - RECON-03
  - RECON-04
  - RECON-05
must_haves:
  truths:
    - "On every Pi startup and /reload (event = resources_discover), declared-but-missing marketplaces and plugins are added/installed automatically — verifiable by a single-process unit test that runs the handler against a fixture where state.json is empty but claude-plugins.json declares mp-a + plugin-a, and observes the orchestrators were called serially in the documented order."
    - "Installed-but-undeclared marketplaces/plugins (scoped to entries recorded in state.json — the planner is the ownership gate) are removed/uninstalled automatically; entries NOT in state.json are never touched."
    - "A network failure during reconcile soft-fails for that one entry: the failed entry is rendered in the cascade with a (failed) row + reason, every other entry continues, and the failure never propagates past the resources_discover boundary or blocks Pi load."
    - "Reconcile results surface through a SINGLE notify() call per applyReconcile invocation, in catalog-conformant grammar; the new variant's shouldEmitReloadHint arm is structurally `false` so no `/reload to pick up changes` trailer ever appears."
    - "A back-to-back applyReconcile call (immediately after the first one returns) applies zero changes and rewrites no files: state.json byte-unchanged, claude-plugins.json byte-unchanged."
    - "An invalid claude-plugins.json (base or local) for a scope produces a (failed) {invalid manifest} row carrying path.basename(filePath) — never the absolute path; that scope's apply pass is skipped (CFG-03 abort, never mass-uninstall)."
    - "Migration runs INSIDE the per-scope read-pass withStateGuard closure BEFORE loadMergedScopeConfig; the migrate-then-reconcile ordering rail is wired (closing the Phase 52 deferred obligation for the single-process arm)."
    - "applyReconcile is called inside try/catch in index.ts; even a catastrophic throw renders a single last-ditch error notify and continues to aggregateDiscoveredResources — Pi load is never blocked (NFR-2)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts"
      provides: "applyReconcile orchestrator — per-scope read pass under withStateGuard, per-entry apply loop with no outer lock, single notify() emission"
      contains: "export async function applyReconcile"
      min_lines: 120
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts"
      provides: "buildReconcileAppliedCascade projection helper (sibling of buildReconcilePreviewNotification)"
      contains: "buildReconcileAppliedCascade"
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "ReconcileAppliedCascadeMessage variant + StandaloneKind addition + dispatchInfoMessage arm + isInfoKind extension"
      contains: "reconcile-applied-cascade"
    - path: "extensions/pi-claude-marketplace/index.ts"
      provides: "resources_discover handler binds ctx (drops unknown cast), calls applyReconcile inside try/catch BEFORE aggregateDiscoveredResources"
      contains: "applyReconcile"
    - path: "docs/output-catalog.md"
      provides: "## reconcile-applied-cascade H2 section + paired catalog states (success cascade, soft-fail per-entry, CFG-03 invalid-config row)"
      contains: "reconcile-applied-cascade"
  key_links:
    - from: "extensions/pi-claude-marketplace/index.ts"
      to: "applyReconcile"
      via: "resources_discover handler"
      pattern: "applyReconcile\\(\\{\\s*ctx,\\s*pi,\\s*cwd"
    - from: "applyReconcile"
      to: "migrateFirstRunConfig + loadMergedScopeConfig + planReconcile"
      via: "withStateGuard closure per scope (read pass)"
      pattern: "withStateGuard\\(loc,\\s*async"
    - from: "applyReconcile"
      to: "addMarketplace / removeMarketplace / installPlugin / uninstallPlugin / setPluginEnabled"
      via: "notifications: { mode: 'orchestrated' } per-entry calls"
      pattern: "notifications:\\s*\\{\\s*mode:\\s*\"orchestrated\""
    - from: "ReconcileAppliedCascadeMessage"
      to: "shouldEmitReloadHint = false"
      via: "StandaloneKind isInfoKind arm"
      pattern: "reconcile-applied-cascade"
---
> **Commit discipline (project CLAUDE.md):** run `pre-commit run --files <changed files>` before every `git commit`; fix failures, restage, re-run until clean. NEVER use `--no-verify`.


<objective>
Land the load-time apply path atomically: the new `applyReconcile` orchestrator + `index.ts` `resources_discover` wiring (ctx now bound — the `unknown` cast drops) + the new `ReconcileAppliedCascadeMessage` standalone variant + its renderer dispatch arm + the projection helper that folds per-entry orchestrator outcomes into a `CascadeNotificationMessage`-shape body, ALL co-landing with the catalog amendment (`docs/output-catalog.md` + `tests/architecture/catalog-uat.test.ts` FIXTURES + `tests/architecture/notify-types.test.ts` length-locks + `tests/architecture/notify-grammar-invariant.test.ts` no-reload-trailer assertion) in ONE commit.

Reconcile cascade row tokens REUSE existing transition tokens (`added`, `removed`, `installed`, `uninstalled`, `enabled`, `disabled`, `failed`) and existing REASONS members — no new STATUS_TOKENS / PLUGIN_STATUSES / MARKETPLACE_STATUSES / REASONS / MARKERS literals (per RESEARCH Pattern 5 Option A — the locked decision). The only new closed-set entry is the StandaloneKind `"reconcile-applied-cascade"` itself; `shouldEmitReloadHint` is structurally false for any StandaloneKind, which closes Pitfall 4 (no `/reload to pick up changes` trailer on the apply cascade — it would be a lie).

Empty-steady-state on a non-CFG-03 load is SILENT (no notify) per RESEARCH Pattern 5 / A4 and NFR-2 friendliness. The CFG-03 path is NEVER silent: invalid base or local config emits a `(failed) {invalid manifest}` row carrying `path.basename(filePath)` (T-53-02-02 information-disclosure mitigation — mirror Phase 53 preview.ts:69-77 verbatim).

Purpose: This is the user-visible heart of Phase 55. After this plan ships, every Pi startup and `/reload` reconciles installed reality to the merged config without operator action, soft-fails per entry on network failure, and reports through a single structured cascade in catalog-conformant grammar.

Output: `applyReconcile` orchestrator + wired `index.ts` handler + new variant + renderer arm + projection helper + paired catalog/UAT entries + length-locks + grammar-invariant assertion + RECON-01..05 unit coverage. Single atomic commit (Pitfall 53-3 / v1.3 atomic-supersession lesson). RECON-06 is the next plan (Plan 03 / Wave 3); it requires the apply.ts surface this plan delivers but the catalog work must NOT cross-cut into a separate commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-load-time-reconcile-apply-notification-wiring/55-CONTEXT.md
@.planning/phases/55-load-time-reconcile-apply-notification-wiring/55-RESEARCH.md
@.planning/phases/55-load-time-reconcile-apply-notification-wiring/55-PATTERNS.md
@.planning/phases/55-load-time-reconcile-apply-notification-wiring/55-VALIDATION.md
@.planning/phases/55-load-time-reconcile-apply-notification-wiring/55-01-SUMMARY.md
@extensions/pi-claude-marketplace/index.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/preview.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts
@extensions/pi-claude-marketplace/persistence/migrate-config.ts
@extensions/pi-claude-marketplace/persistence/config-merge.ts
@extensions/pi-claude-marketplace/persistence/state-io.ts
@extensions/pi-claude-marketplace/transaction/with-state-guard.ts
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/platform/pi-api.ts
@docs/output-catalog.md
@tests/orchestrators/reconcile/preview.test.ts
@tests/architecture/catalog-uat.test.ts
@tests/architecture/notify-types.test.ts
@tests/architecture/notify-grammar-invariant.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ReconcileAppliedCascadeMessage variant + renderer dispatch arm + projection helper + catalog amendment (lockstep)</name>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (entire file — locate `ReconcilePreviewEmptyMessage` at line 1006, `NotificationMessage` union at line 1037, `StandaloneKind` at line 1059, `isInfoKind` at line 1074, `shouldEmitReloadHint` and `buildSummaryLine` and `computeSeverity` arms at lines 1805, 1962, 2031, `dispatchInfoMessage` switch at line 2503, `MARKETPLACE_STATUSES` / `PLUGIN_STATUSES` / `STATUS_TOKENS` / `REASONS` tuples — confirm CARD member counts must NOT change in this plan)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts (entire file — buildReconcilePreviewNotification at line 163; this plan adds a sibling buildReconcileAppliedCascade)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts (entire file — ReconcilePlan 7-bucket shape; new per-entry outcome type belongs in this file OR a sibling apply-outcomes.ts)
    - docs/output-catalog.md (locate any existing `## reconcile-preview-empty` H2 section for shape; add a new `## reconcile-applied-cascade` H2 section AFTER it)
    - tests/architecture/catalog-uat.test.ts (FIXTURES map shape — new entries paired with new catalog states; the byte-equality runner)
    - tests/architecture/notify-types.test.ts (length-locks for StandaloneKind size, _StandaloneKindExpected proofs)
    - tests/architecture/notify-grammar-invariant.test.ts (subject-first row grammar proofs — must extend to cover the new variant's emitted bytes; assert NO `/reload to pick up changes` trailer in the new variant's bytes)
    - tests/shared/notify-v2.test.ts (per-variant byte-equality tests; the new variant gets per-arm tests covering at least 3 catalog states)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/preview.ts (lines 69-77 for the basename CFG-03 invalid-config block builder — reuse the pattern verbatim in apply.ts later)
  </read_first>
  <behavior>
    - shared/notify.ts exports a new `ReconcileAppliedCascadeMessage` interface alongside `ReconcilePreviewEmptyMessage`:
      ```
      interface ReconcileAppliedCascadeMessage {
        readonly kind: "reconcile-applied-cascade";
        readonly marketplaces: readonly MarketplaceNotificationMessage[];
      }
      ```
      The marketplaces array reuses the existing per-status MarketplaceNotificationMessage discriminated union shape (no new arm needed) — every row inside the cascade carries an existing status token (`added` / `removed` / `failed` / undefined for list-only) and existing PLUGIN_STATUSES tokens (`installed` / `uninstalled` / `enabled` / `disabled` / `failed`) so REUSE Option A from RESEARCH Pattern 5 is realized.
    - NotificationMessage union gains the new variant; StandaloneKind union gains `"reconcile-applied-cascade"`; isInfoKind gains the corresponding arm; computeSeverity / buildSummaryLine / shouldEmitReloadHint / dispatchInfoMessage all gain matching arms (assertNever discipline). For dispatchInfoMessage, the renderer composes the per-mp header + per-plugin row using the existing renderMpHeader + renderPluginRow helpers — no new icon or token literal.
    - shouldEmitReloadHint arm for `"reconcile-applied-cascade"` returns `false` structurally — closes Pitfall 4 (the apply cascade would otherwise emit `Run /reload to pick up changes` after rendering `installed` rows, which is a lie because the reconcile already ran on /reload).
    - computeSeverity arm derives severity from contents: any (failed) row → `error`; any (skipped) row → `warning`; otherwise `success`. Mirror the existing CascadeNotificationMessage discipline.
    - buildSummaryLine arm renders a summary line on error / warning severity ONLY (GRAM-04 anti-divergence guarantee from Phase 50 — the summary-line discipline applies to the new variant too).
    - orchestrators/reconcile/notify.ts gains an exported `buildReconcileAppliedCascade(plans: readonly ReconcilePlan[], outcomes: readonly PerEntryOutcome[]): ReconcileAppliedCascadeMessage` helper. Its body folds outcomes into (scope, marketplace) MarketplaceBlocks (reuse the existing MarketplaceBlock + ensureMarketplaceBlock helpers — they are file-private so either export them or copy the pattern). Token mapping (per RESEARCH Pattern 5 Option A):
        marketplace added (success)        → block.status = "added"
        marketplace add failed             → block.status = "failed", reasons = [outcome.reason]
        marketplace removed (success)      → block.status = "removed"
        marketplace remove failed          → block.status = "failed", reasons = [outcome.reason]
        plugin install success             → child row { status: "installed", name, version? }
        plugin install failed              → child row { status: "failed", name, reasons }
        plugin uninstall success           → child row { status: "uninstalled", name }
        plugin uninstall failed            → child row { status: "failed", name, reasons }
        plugin enable success              → child row { status: "enabled", name }
        plugin enable failed               → child row { status: "failed", name, reasons }
        plugin disable success             → child row { status: "disabled", name }
        plugin disable failed              → child row { status: "failed", name, reasons }
        sourceMismatch (report-only)       → block.status = "failed", reasons = ["source mismatch"] + optional plugin child row carrying the dangling plugin name (mirror reconcile/notify.ts:124-137 verbatim)
        invalidBlock (CFG-03)              → top-level block { name: basename(filePath), scope, status: "failed", reasons: ["invalid manifest"] }
      Block ordering: compareByNameThenScope (project-first per MSG-GR-3) — reuse the existing comparator.
    - Define an exported `PerEntryOutcome` type in `orchestrators/reconcile/types.ts` (or a sibling `apply-outcomes.ts` if types.ts must stay frozen): discriminated union covering the cross product of {entity-kind, success|failure}. Each variant carries the minimum fields needed by the projection (scope, marketplace, plugin?, reason?, version?).
    - docs/output-catalog.md gains a new `## reconcile-applied-cascade` H2 section with at LEAST 3 catalog states demonstrating the user-visible bytes: (a) success cascade with mixed marketplace add + plugin install rows across both scopes; (b) soft-fail per-entry (one (failed) {network unreachable} row mixed with one successful install row); (c) CFG-03 invalid-config row carrying ONLY the basename. Each state has a paired FIXTURES entry in tests/architecture/catalog-uat.test.ts.
    - tests/architecture/notify-types.test.ts: bump StandaloneKind length-lock by +1 (or however the existing proof is shaped — extend the `_StandaloneKindExpected` union; mirror how Phase 53 extended it for `reconcile-preview-empty`).
    - tests/architecture/notify-grammar-invariant.test.ts: add proofs that the new variant's emitted bytes (a) follow the subject-first row grammar `<glyph> <name> [scope] (status) {reason}` and (b) contain NO `/reload to pick up changes` substring anywhere in the emitted body — even on a cascade containing transition tokens that would otherwise trigger the trailer.
    - tests/shared/notify-v2.test.ts: per-variant byte-equality tests for the 3 catalog states above (notify-v2 is the per-variant test surface; catalog-uat is the FIXTURES inverse-walk).
  </behavior>
  <action>
    Land all of the following in ONE atomic commit (atomic-supersession, Pitfall 53-3 — analogous to how Phase 53 Plan 02 landed 19 files in one commit and how Phase 54 Plan 02 landed 20 files in one commit):

    1. `shared/notify.ts`: add `ReconcileAppliedCascadeMessage` interface, extend `NotificationMessage` union, extend `StandaloneKind`, extend `isInfoKind`, add arms to `computeSeverity`, `buildSummaryLine`, `shouldEmitReloadHint` (returns `false`), and `dispatchInfoMessage` (renders cascade body via existing renderMpHeader + renderPluginRow). Use `assertNever` defaults — DO NOT relax the exhaustiveness discipline. Honor source-comment-cleanup-policy.

    2. `orchestrators/reconcile/types.ts` (or `apply-outcomes.ts`): add an exported `PerEntryOutcome` discriminated union covering marketplace add / marketplace remove / plugin install / plugin uninstall / plugin enable / plugin disable / source-mismatch / invalid-block, each in success + failed shape.

    3. `orchestrators/reconcile/notify.ts`: add an exported `buildReconcileAppliedCascade(plans, outcomes): ReconcileAppliedCascadeMessage` projection helper. Reuse the existing `MarketplaceBlock` + `ensureMarketplaceBlock` + `compareByNameThenScope` discipline; export `MarketplaceBlock`/`ensureMarketplaceBlock` if necessary to share with the new helper without copy-paste (keep the helpers file-private if a single-file design fits naturally).

    4. `docs/output-catalog.md`: add `## reconcile-applied-cascade` H2 section with the 3 catalog states.

    5. `tests/architecture/catalog-uat.test.ts`: add paired FIXTURES entries for the 3 catalog states (zero orphans — both directions of the inverse walk must be clean).

    6. `tests/architecture/notify-types.test.ts`: extend the StandaloneKind length-lock proof and the `_StandaloneKindExpected` union to include the new kind.

    7. `tests/architecture/notify-grammar-invariant.test.ts`: add the subject-first row grammar proof for the new variant; add the explicit NO-`/reload` trailer proof on a cascade rich enough to contain transition tokens.

    8. `tests/shared/notify-v2.test.ts`: add per-variant byte-equality tests for the 3 catalog states.

    NOTE: apply.ts itself is NOT part of Task 1. Task 1 lands the TYPE + RENDERER + PROJECTION + CATALOG surface so Task 2 can build the orchestrator against frozen types. This split is deliberate: the catalog + notify-types proofs must land with the variant; the orchestrator's behavior tests need the variant to be renderable.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; npm test -- tests/shared/notify-v2.test.ts tests/architecture/catalog-uat.test.ts tests/architecture/notify-types.test.ts tests/architecture/notify-grammar-invariant.test.ts</automated>
  </verify>
  <done>
    - `ReconcileAppliedCascadeMessage` exported from shared/notify.ts; StandaloneKind extended; isInfoKind / computeSeverity / buildSummaryLine / shouldEmitReloadHint / dispatchInfoMessage all gain matching arms with assertNever discipline preserved
    - shouldEmitReloadHint arm for `"reconcile-applied-cascade"` returns `false` (Pitfall 4 structurally closed)
    - `buildReconcileAppliedCascade` exported from `orchestrators/reconcile/notify.ts`
    - `PerEntryOutcome` exported from `orchestrators/reconcile/types.ts` (or sibling)
    - docs/output-catalog.md has `## reconcile-applied-cascade` H2 section with at least 3 catalog states
    - tests/architecture/catalog-uat.test.ts has paired FIXTURES (orphan-walk clean both directions)
    - tests/architecture/notify-types.test.ts length-lock for StandaloneKind bumped (e.g. 6 → 7)
    - tests/architecture/notify-grammar-invariant.test.ts asserts subject-first grammar AND no `/reload` trailer for the new variant
    - tests/shared/notify-v2.test.ts byte-equality GREEN on at least 3 new fixtures (one success cascade, one soft-fail row, one CFG-03 invalid-config row)
    - No new STATUS_TOKENS / PLUGIN_STATUSES / MARKETPLACE_STATUSES / REASONS / MARKERS literals (RESEARCH Pattern 5 Option A)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement applyReconcile orchestrator + wire index.ts resources_discover handler with bound ctx</name>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/reconcile/preview.ts (entire file — applyReconcile mirrors its shape; verbatim reuse of buildInvalidConfigBlock pattern at lines 69-77; verbatim reuse of narrowStateLoadFailReason at lines 87-93; per-scope fan-out at lines 95-146)
    - extensions/pi-claude-marketplace/index.ts (current handler at lines 15-30 — uses the `unknown` cast that elides ctx; replace with the natural `(event, ctx)` signature; the `applyReconcile` call lands inside try/catch BEFORE aggregateDiscoveredResources)
    - extensions/pi-claude-marketplace/persistence/migrate-config.ts (migrateFirstRunConfig signature — called inside the per-scope read-pass closure BEFORE loadMergedScopeConfig)
    - extensions/pi-claude-marketplace/persistence/config-merge.ts (loadMergedScopeConfig signature)
    - extensions/pi-claude-marketplace/persistence/state-io.ts (loadState behavior — withStateGuard calls loadState internally; the read pass therefore observes the D-13 existsSync gate at the right instant per Pitfall 2 / Pattern 2)
    - extensions/pi-claude-marketplace/transaction/with-state-guard.ts (lines 66-176 — withStateGuard signature, proper-lockfile non-reentrant warning at lines 111-153; CR-01 nested-lock lesson)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts (planReconcile signature — pure, called inside the read-pass closure)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts AND remove.ts (orchestrated-mode option from Plan 01 — apply.ts passes notifications: { mode: "orchestrated" })
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts AND uninstall.ts AND enable-disable.ts (orchestrated-mode option — installPlugin already had it pre-Phase-55; the other three got it in Plan 01)
    - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts (lines 11-19 — CR-01 lesson comment; verify the apply loop does NOT wrap orchestrators in an outer withStateGuard)
    - extensions/pi-claude-marketplace/platform/pi-api.ts (ExtensionContext + ExtensionAPI exports; ResourcesDiscoverEvent + ResourcesDiscoverResult exports)
    - node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/types.d.ts (lines 802 and 808 — confirm `pi.on("resources_discover", handler)` arity matches `ExtensionHandler<E, R> = (event, ctx) => ...`)
    - tests/orchestrators/reconcile/preview.test.ts (entire file — sibling test shape; fixture style; mock ctx pattern)
    - tests/integration/concurrent-install.test.ts (lines 100-187 — reference for what RECON-06 will look like in Plan 03; not directly used here but informs the harness design)
  </read_first>
  <behavior>
    - Per-scope ordering: applyReconcile fans out across BOTH scopes (project FIRST, then user — mirrors preview.ts:98 and MSG-GR-3 row ordering).
    - Per-scope READ PASS (locked): `withStateGuard(loc, async (state) => { ... })` covers (i) `await migrateFirstRunConfig(loc, state)` FIRST (Pitfall 52-2 lock-covered concurrent first-load race; Pitfall 52-4 D-13 existsSync gate is observed at withStateGuard's internal loadState — i.e. BEFORE the closure runs — which preserves legacy autoupdate capture per Pattern 2 / Pitfall 2), THEN `await loadMergedScopeConfig(loc)` (sees either the just-written config OR the pre-existing one — never ENOENT after migrate), THEN the CFG-03 invalid-arm check (mirror preview.ts:111-122), THEN the loadState-failure catch (mirror preview.ts:131-143 — wait, state is already the closure parameter so this catch arm does not apply; only the merge-config / planReconcile arm matters for the read pass), THEN `planReconcile(outcome.merged, state, loc.scope)`. Returns `{ plan: ReconcilePlan | undefined, invalidBlocks: MarketplaceNotificationMessage[] }`.
    - The withStateGuard closure RETURNS the plan + invalid blocks; the lock releases on closure return.
    - Per-scope APPLY PASS (NO outer lock — Pattern 3 / Pitfall 3 / CR-01): for each scope's plan (skip if `plan === undefined` because CFG-03 aborted), drive the existing orchestrators in this order: pluginsToUninstall → marketplacesToRemove → marketplacesToAdd (network; soft-fail per entry) → pluginsToInstall (network; soft-fail per entry) → pluginsToEnable → pluginsToDisable → sourceMismatches (report-only). Per Pitfall 8 — document the order at the top of apply.ts.
    - Each driven orchestrator call passes `notifications: { mode: "orchestrated" }`. Wrap each call in try/catch:
      - Try: call orchestrator, push the typed outcome into the perEntryOutcomes accumulator.
      - Catch (unknown): classify via the existing closed-set reason ladder (`narrowProbeError` / `classifyAddError` / etc.) and push a failed outcome into the accumulator. NEVER let an orchestrator throw past applyReconcile (Pitfall 5).
    - After both scopes complete, accumulate everything into `buildReconcileAppliedCascade(plans, outcomes)` plus the invalidBlocks (CFG-03 rows are stitched in as top-level marketplace blocks via the projection helper). Sort via compareByNameThenScope (preserve project-first per scope).
    - Empty-and-clean (no invalid blocks AND every plan empty AND every outcome empty) → SILENT (do NOT call notify) per Pattern 5 / A4 and NFR-2 friendliness.
    - Non-empty → exactly ONE `notify(ctx, pi, message)` call (IL-2) with `message.kind = "reconcile-applied-cascade"`.
    - index.ts: drop the `unknown` cast; restore the natural `pi.on("resources_discover", async (event, ctx) => ...)` signature; call `applyReconcile({ ctx, pi, cwd: event.cwd })` inside try/catch BEFORE `aggregateDiscoveredResources` so newly-materialized artefacts are picked up on the SAME load. The last-ditch catch arm calls `ctx.ui.notify(\`reconcile aborted: ${(err as Error).message}\`, "error")` inside its OWN try/catch (NFR-2: NEVER throw past resources_discover).
    - Coverage (RECON-01 / 02 / 03 / 05 in tests/orchestrators/reconcile/apply.test.ts):
        * RECON-01: state.json empty; claude-plugins.json declares mp-a (path source) + plugin-a@mp-a. Run applyReconcile. Assert addMarketplace called once, installPlugin called once; both in orchestrated mode; state.json now records mp-a + plugin-a; notify() called once with a cascade containing `added` mp row + `installed` plugin row.
        * RECON-02: state.json records mp-a + plugin-a; claude-plugins.json is empty. Run applyReconcile. Assert uninstallPlugin called once (BEFORE removeMarketplace per the order), then removeMarketplace called once; state.json now empty; notify() rendered `uninstalled` plugin row + `removed` mp row. ALSO: state.json carries an EXTRA record `mp-b` that is NOT recorded in the planner-eligible set (e.g. simulate via a hand-edit pattern that the planner declines to surface) — verify no `removeMarketplace` call is made for it. The ownership guard IS the planner (RECON-02 per RESEARCH Pattern 6) — the unit test just verifies the planner-driven invariant holds end-to-end.
        * RECON-03: state.json empty; claude-plugins.json declares mp-a (github source). Inject a failing `gitOps` into addMarketplace via DI such that the clone fails with a network-class error. Assert: applyReconcile completes WITHOUT throwing; the cascade contains a `(failed) {network unreachable}` (or whatever closed-set reason the existing classifier surfaces) row for mp-a; the loop continued past the failure (cover with a second declared mp-b that succeeds).
        * RECON-05: Run applyReconcile twice in succession against the same fixture. After the first call, capture state.json bytes + mtime AND claude-plugins.json bytes + mtime. After the second call, assert byte-equality on both files AND that notify() was called ZERO times on the second invocation (silent empty-steady-state).
    - Light handler-wiring coverage (RECON-04 wiring proof) in tests/edge/index-handler.test.ts: register the handler against a mock pi; fire the `resources_discover` event with a mock ctx; assert applyReconcile was called with the bound ctx + pi + event.cwd; assert aggregateDiscoveredResources was called AFTER applyReconcile; assert a catastrophic applyReconcile throw is caught and the handler still returns a ResourcesDiscoverResult.
  </behavior>
  <action>
    Implement `extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts`. The file's top-of-file comment is a brief contract: per-scope read pass under withStateGuard (migrate-then-load-then-plan), per-entry apply pass with NO outer lock (CR-01), per-entry try/catch + soft-fail (NFR-5 / RECON-03), single notify() emission (IL-2 / RECON-04), silent on empty-steady-state (NFR-2 / A4). Source-comment-cleanup-policy: keep requirement ID references; drop phase narrative.

    Public surface: `export interface ApplyReconcileOptions { ctx, pi, cwd, scope? }` (the optional scope mirrors preview.ts:60 — when omitted, fan out across both; when set, run only that scope) and `export async function applyReconcile(opts: ApplyReconcileOptions): Promise<void>`. Body returns `void` (the side effect is the notify + the state mutations performed by the driven orchestrators).

    Reuse: `buildInvalidConfigBlock(scope, filePath)` pattern from preview.ts:69-77 verbatim (basename — T-53-02-02). `narrowStateLoadFailReason` from preview.ts:87-93 (state-load classification — not actually reachable under withStateGuard since the guard's internal loadState surfaces failures earlier, but keep the import path symmetric with preview.ts in case the lock-acquisition failure path needs surfacing).

    Per-entry classifier: use the existing `narrowProbeError` for network/IO classification; `classifyAddError` for marketplace-add classification; for plugin failures lean on the structured `InstallPluginOutcome.error` / `UninstallPluginOutcome.reason` that orchestrated-mode returns (Plan 01 already populates these).

    Wire `index.ts`: drop lines 16-19 (the `unknown` cast). Replace with `pi.on("resources_discover", async (event, ctx): Promise<ResourcesDiscoverResult> => { try { await applyReconcile({ ctx, pi, cwd: event.cwd }); } catch (err) { try { ctx.ui.notify(\`reconcile aborted: ${(err as Error).message}\`, "error"); } catch {} } const discovered = await aggregateDiscoveredResources(locationsFor("user", homedir()), locationsFor("project", event.cwd)); return { skillPaths: [...discovered.skillPaths], promptPaths: [...discovered.promptPaths] }; });`. Verify against `node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/types.d.ts:802,808` — `ExtensionHandler<E, R> = (event, ctx) => ...` and `on(event: "resources_discover", handler: ExtensionHandler<ResourcesDiscoverEvent, ResourcesDiscoverResult>)`.

    A1 sanity check (RESEARCH Assumption A1, LOW confidence): inside this task, READ pi-coding-agent's extension loader to confirm `resources_discover` is fired AFTER all extension factory functions return — i.e. that `softDepStatus(pi)` at notify time will observe a stable pi-subagents / pi-mcp-adapter status. Grep `node_modules/@earendil-works/pi-coding-agent/dist/main.js` and `node_modules/@earendil-works/pi-coding-agent/dist/modes/` for `resources_discover` emit sites and document the load-order finding in apply.ts as a brief inline comment (preserve the requirement ID, drop phase narrative). If A1 is REFUTED at plan-time (extension loader fires resources_discover BEFORE other extensions register), STOP and surface the finding — A1 refutation forces deferring the cascade emission to session_start (RESEARCH Open Question 4), which is a different design and requires Plan revision.

    Implement `tests/orchestrators/reconcile/apply.test.ts`: cover RECON-01, RECON-02, RECON-03, RECON-05 per the behavior block above. Use the preview.test.ts fixture style (in-memory ScopedLocations + tmpdir + mock ctx). Inject a `gitOps` mock into addMarketplace via Plan 01's orchestrated-mode option chain (or via the existing DI seam already used by the marketplace tests).

    Implement `tests/edge/index-handler.test.ts`: light coverage of the handler wiring per the RECON-04 wiring-proof behavior.

    Commit message convention: ONE atomic commit covering shared/notify.ts variant addition + orchestrators/reconcile/{apply,notify,types}.ts + index.ts + docs/output-catalog.md + the 4 test files modified in Task 1 + the new apply.test.ts + index-handler.test.ts. Per repository policy, commit from inside a worktree uses `SKIP=trufflehog` (per CLAUDE.md). Do NOT amend Task 1's commit — Task 1 already passed verify; Task 2 produces a separate atomic commit.

    Document in apply.ts the per-entry apply order at the top of the file: `uninstall → remove → add → install → enable → disable → source-mismatch (report-only)` per Pitfall 8 — justified by data dependency (a marketplace must exist before a plugin install; a plugin must be uninstalled before its marketplace can be removed).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; npm run check</automated>
  </verify>
  <done>
    - `extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts` created with the documented contract; per-scope read pass uses withStateGuard with migrate-then-load-then-plan ordering; per-entry apply pass has NO outer lock and uses Plan 01's orchestrated-mode option chain
    - `extensions/pi-claude-marketplace/index.ts` resources_discover handler binds ctx (drops the `unknown` cast), calls applyReconcile inside try/catch BEFORE aggregateDiscoveredResources
    - `tests/orchestrators/reconcile/apply.test.ts` covers RECON-01, RECON-02, RECON-03, RECON-05 with the behaviors documented above
    - `tests/edge/index-handler.test.ts` covers the resources_discover wiring with a catastrophic-throw catch arm
    - A1 (RESEARCH Assumption A1) verified at plan time by reading pi-coding-agent loader internals; finding recorded inline; if REFUTED, plan revision requested before proceeding
    - `npm run check` GREEN end-to-end with the Task 1 surface + this task's behavior tests
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pi runtime → resources_discover handler | Pi dispatches the event with `(event, ctx)`. ctx.ui.notify is the sole sanctioned user-output sink (IL-2). |
| filesystem → applyReconcile | `claude-plugins.json` / `claude-plugins.local.json` / `state.json` are untrusted JSON; CFG-03 + loadState classification surface invalid input as structured rows, never as silent empty desired state. |
| network → addMarketplace / installPlugin | Per-entry network operations under apply.ts may fail (transient DNS, auth, rate limit). NFR-5 mandates soft-fail per entry. |
| orchestrator → applyReconcile error fields | An orchestrator's `outcome.error.message` could carry filesystem paths or auth tokens. apply.ts must NEVER render `error.message` into a notify body — only the closed-set `outcome.reason`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-02-01 | Information disclosure | CFG-03 invalid-config block path | mitigate | `buildInvalidConfigBlock(scope, filePath)` uses `path.basename(filePath)` — never the absolute path. Mirror preview.ts:69-77 verbatim (T-53-02-02 precedent). |
| T-55-02-02 | Information disclosure | Orchestrator failure cascade rows | mitigate | apply.ts's projection helper consumes only `outcome.reason` (closed-set ContentReason). `outcome.error.message` is NEVER read into a notify body or a row reasons field. Verify in the per-entry projection code. |
| T-55-02-03 | Denial-of-service | Catastrophic exception in applyReconcile blocks Pi load | mitigate | Index.ts wraps applyReconcile in try/catch with a last-ditch error notify (inside its own try/catch). aggregateDiscoveredResources is invoked unconditionally after — Pi load never blocks (NFR-2). |
| T-55-02-04 | Tampering | Mass-uninstall from a malformed claude-plugins.json | mitigate | CFG-03 abort: any `invalid` arm in `loadMergedScopeConfig` for a scope skips that scope's planner — the apply pass for that scope has ZERO buckets. An invalid config can never produce a removeMarketplace / uninstallPlugin call. The unit test fixture for CFG-03 verifies this (mirror Phase 53 preview.test.ts CFG-03 fixture). |
| T-55-02-05 | Tampering | Unmanaged-entry removal | mitigate | Ownership guard is structural: `planReconcile`'s `marketplacesToRemove` / `pluginsToUninstall` buckets only include entries recorded in state. apply.ts trusts the planner and walks only the plan's buckets — never scans the filesystem or state.json directly for "extras" to remove (Pattern 6). |
| T-55-02-06 | Tampering | Double-apply via two concurrent processes | mitigate | Per-scope read pass under withStateGuard serializes the migrate-then-load-then-plan step. Cross-process double-apply prevention proven by Plan 03's RECON-06 two-process test. (This plan's unit test cover single-process behavior; Plan 03 covers the integration.) |
| T-55-02-07 | Repudiation | Silent failure on a soft-fail entry | mitigate | RECON-03 + RECON-04: per-entry failures produce a `(failed) {reason}` row in the cascade. The cascade emits a SINGLE notify(); silent failures are structurally impossible because the per-entry try/catch always pushes an outcome. |
| T-55-02-08 | Denial-of-service | Reload-hint loop on auto-reconcile cascade | mitigate | StandaloneKind structurally suppresses `shouldEmitReloadHint`: arm returns `false`. The cascade rows can carry transition tokens without ever emitting `Run /reload to pick up changes`. The grammar-invariant test asserts the trailer absence (Pitfall 4 closed). |
| T-55-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan. Package legitimacy gate N/A per 55-RESEARCH.md Package Legitimacy Audit. |
</threat_model>

<verification>
- Architecture proofs: `tests/architecture/notify-types.test.ts` length-locks GREEN with StandaloneKind extended; `tests/architecture/catalog-uat.test.ts` byte-equality GREEN with new FIXTURES; `tests/architecture/notify-grammar-invariant.test.ts` GREEN with the no-`/reload`-trailer assertion on the new variant.
- Per-variant byte-equality: `tests/shared/notify-v2.test.ts` GREEN on the 3 new catalog states.
- Behavioral coverage: `tests/orchestrators/reconcile/apply.test.ts` GREEN on RECON-01 (declared-but-missing → add+install at load), RECON-02 (installed-but-undeclared → remove+uninstall scoped to managed entries; ownership guard verified end-to-end), RECON-03 (per-entry network failure soft-fails; pass continues; no throw escapes), RECON-05 (back-to-back reconcile = byte-unchanged config + state, ZERO notify on the second call).
- Wiring proof: `tests/edge/index-handler.test.ts` GREEN on (a) applyReconcile called with bound ctx + pi + event.cwd, (b) aggregateDiscoveredResources called AFTER applyReconcile, (c) catastrophic throw caught and handler still returns ResourcesDiscoverResult.
- No-orchestrator-network gate: apply.ts is INTENTIONALLY not added to FORBIDDEN_TARGETS (it drives addMarketplace which uses network — per-entry soft-fail is the NFR-5 satisfaction). Verify the existing gate at `tests/architecture/no-orchestrator-network.test.ts` stays GREEN unchanged.
- Config-state write-seam gate: apply.ts NEVER calls atomicWriteJson directly (only the driven orchestrators write state; only the future Phase 56 write-back writes config). Verify `tests/architecture/config-state-write-seams.test.ts` allow-list does NOT need an apply.ts entry; the test stays GREEN unchanged.
- Full suite gate: `npm run check` GREEN end-to-end.
</verification>

<success_criteria>
- RECON-01 GREEN: declared-but-missing entries are added/installed automatically on every Pi startup and /reload.
- RECON-02 GREEN: installed-but-undeclared entries (scoped to state-recorded — the planner is the ownership gate) are removed/uninstalled automatically.
- RECON-03 GREEN: per-entry network failures soft-fail and never propagate past the resources_discover boundary.
- RECON-04 GREEN: results surface through ONE notify() per applyReconcile, in catalog-conformant grammar, NEVER emitting `/reload to pick up changes`.
- RECON-05 GREEN: back-to-back reconcile is a byte-unchanged no-op (state.json + claude-plugins.json mtime AND bytes preserved; zero notify on the second invocation).
- Atomic supersession: one commit lands the variant + renderer + projection + catalog + FIXTURES + length-locks + grammar invariant + apply.ts + index.ts wiring + behavior tests (Pitfall 53-3).
- CR-01 lesson preserved: NO outer lock around the apply loop; each driven orchestrator owns its per-scope lock.
- Migrate-then-load-then-plan ordering rail wired (Phase 52 deferred single-process obligation discharged here; the two-process obligation discharges in Plan 03).
- `npm run check` GREEN.
</success_criteria>

<artifacts_this_phase_produces>
- Plan 01 (prior wave, DONE): orchestrated-mode foundation across addMarketplace / removeMarketplace / uninstallPlugin / setPluginEnabled.
- Plan 02 (this plan): atomic apply.ts + index.ts wiring + ReconcileAppliedCascadeMessage variant + catalog amendment + RECON-01/02/03/04/05 coverage.
- Plan 03 (next wave): RECON-06 two-process race test + Phase 52 deferred lock-coverage proof (integration test only — no new source surface).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/55-load-time-reconcile-apply-notification-wiring/55-02-SUMMARY.md` when done.
</output>
