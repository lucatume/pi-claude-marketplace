---
phase: 55
plan: 03
type: execute
wave: 3
depends_on:
  - 55-01
  - 55-02
files_modified:
  - tests/integration/load-reconcile-race.test.ts
  - tests/integration/load-reconcile-race-child.ts
autonomous: true
requirements:
  - RECON-06
must_haves:
  truths:
    - "Two Pi processes calling applyReconcile against the same scope simultaneously cannot double-apply: state.json ends up with exactly one mp-a record and exactly one plugin-a record."
    - "No interleaved write: state.json after both processes exit is valid JSON parseable by loadState (the schema validator does not throw)."
    - "No orphaned staging directories survive the race (agents-staging / data dir consistent with the final state.json)."
    - "Neither process throws past its handler boundary (NFR-2 preserved) — if a process loses the read-pass lock it either skips the apply pass with an empty plan OR records the lock-held outcome as a soft-fail in its cascade; the test does NOT assert 'exactly one winner' (Pitfall 10 — the read pass is fast; the apply loop has no shared lock so racing is benign)."
    - "Phase 52 deferred Pitfall 52-2 (concurrent first-load race on migrateFirstRunConfig) is structurally closed: when both processes start with claude-plugins.json ENOENT, exactly one writes the config (lock serialization in withStateGuard), the other observes 'valid' and short-circuits in migrate's trichotomy. Verifiable via a separate test scenario in the same file."
    - "Phase 52 deferred Pitfall 52-4 (D-13 gate race) is structurally closed: withStateGuard's internal loadState observes existsSync(configJsonPath) under the lock, so the gate observation matches the data the same process operates on for the entire closure."
  artifacts:
    - path: "tests/integration/load-reconcile-race.test.ts"
      provides: "RECON-06 two-process simultaneous-start race test + Phase 52 deferred Pitfall 52-2 / 52-4 lock-coverage proof"
      contains: "RECON-06"
      min_lines: 100
    - path: "tests/integration/load-reconcile-race-child.ts"
      provides: "Forkable child entry point calling applyReconcile via stub ctx + stub pi over IPC"
      contains: "applyReconcile"
  key_links:
    - from: "tests/integration/load-reconcile-race.test.ts"
      to: "tests/integration/load-reconcile-race-child.ts"
      via: "child_process.fork(CHILD_PATH, ...)"
      pattern: "fork\\(CHILD_PATH"
    - from: "load-reconcile-race-child.ts"
      to: "applyReconcile"
      via: "stub ctx + stub pi over IPC"
      pattern: "applyReconcile\\(\\{"
---
> **Commit discipline (project CLAUDE.md):** run `pre-commit run --files <changed files>` before every `git commit`; fix failures, restage, re-run until clean. NEVER use `--no-verify`.


<objective>
Close RECON-06 (concurrent Pi processes cannot double-apply or interleave reconciliation) AND discharge the Phase 52 deferred two-process lock-coverage obligation (Pitfalls 52-2 + 52-4 — `tests/persistence/migrate-config.test.ts:29-39` flagged this hand-off to Phase 55). Single integration test file plus a sibling forkable child entry point. Zero new source surface in the extension (the apply.ts + index.ts surface landed in Plan 02; this plan is pure test coverage).

Purpose: The cross-process scope lock (proper-lockfile via withStateGuard) is the load-bearing invariant for RECON-06. Plan 02's apply.ts read pass uses withStateGuard so the lock IS in play; this plan proves the race converges correctly under realistic startup conditions. Mirror the prior art at `tests/integration/concurrent-install.test.ts` (used since Phase 07 for the D-15 / PI-15 concurrent-install proof) — fork + IPC ready-sync + barrier release — but adapt the assertion shape per RESEARCH Pitfall 10 (the read pass is fast; do NOT assert "exactly one winner").

Output: One new integration test file `tests/integration/load-reconcile-race.test.ts` and one new child entry point `tests/integration/load-reconcile-race-child.ts`. Phase 55 GREEN-gate ready (`npm run check` GREEN with full integration suite).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-load-time-reconcile-apply-notification-wiring/55-CONTEXT.md
@.planning/phases/55-load-time-reconcile-apply-notification-wiring/55-RESEARCH.md
@.planning/phases/55-load-time-reconcile-apply-notification-wiring/55-PATTERNS.md
@.planning/phases/55-load-time-reconcile-apply-notification-wiring/55-VALIDATION.md
@.planning/phases/55-load-time-reconcile-apply-notification-wiring/55-02-SUMMARY.md
@tests/integration/concurrent-install.test.ts
@tests/integration/concurrent-install-child.ts
@tests/persistence/migrate-config.test.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
@extensions/pi-claude-marketplace/transaction/with-state-guard.ts
@extensions/pi-claude-marketplace/persistence/migrate-config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RECON-06 two-process race + Phase 52 deferred Pitfall 52-2/52-4 lock-coverage proof</name>
  <read_first>
    - tests/integration/concurrent-install.test.ts (entire file — fork + IPC + ready-sync harness; lines 120-176 specifically — runRace pattern; assertion ergonomics; note the StateLockHeldError 'one-winner' shape that does NOT apply here per Pitfall 10)
    - tests/integration/concurrent-install-child.ts (entire file — child entry point shape; IPC message envelope; tmp HOME/cwd handling)
    - tests/persistence/migrate-config.test.ts (lines 25-47 — the deferred hand-off contract this plan discharges; read for the exact obligations being closed)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts (entire file from Plan 02 — confirm the public ApplyReconcileOptions shape this test invokes)
    - extensions/pi-claude-marketplace/transaction/with-state-guard.ts (lines 60-176 — proper-lockfile semantics; StateLockHeldError shape; `retries: 0` non-reentrant lock; lock release on closure return)
    - extensions/pi-claude-marketplace/persistence/migrate-config.ts (migrateFirstRunConfig trichotomy — Pitfall 52-5: absent vs invalid vs valid; how the lock-losing process observes 'valid' after the lock-winning process writes)
    - extensions/pi-claude-marketplace/persistence/locations.ts (locationsFor signature — used in the child to compute the project-scope ScopedLocations)
    - .planning/phases/52-first-run-migration/52-01-SUMMARY.md (Pitfall 52-2 deferred obligation language)
    - .planning/phases/55-load-time-reconcile-apply-notification-wiring/55-RESEARCH.md (Pattern 7 + Pitfall 10 — the test design constraint; the RIGHT assertion shape)
  </read_first>
  <behavior>
    - Scenario A (RECON-06 core race): two processes both call applyReconcile against the same project-scope cwd, starting from state.json empty + claude-plugins.json declaring a single path-source mp-a + plugin-a@mp-a. Use a path-source marketplace (NOT github) so the test is network-free. Both processes fork; IPC ready-sync barrier; both released simultaneously.
      Assertions (post-test, after both children exit):
        1. state.json exists, parses cleanly via loadState (no schema-validation throw)
        2. state.marketplaces has EXACTLY ONE record for mp-a
        3. state.plugins has EXACTLY ONE record for plugin-a@mp-a
        4. No orphaned staging dirs under the agents-staging path (mirror concurrent-install's leftover-staging assertion)
        5. Both children exit code 0 (NFR-2 — neither process throws past its handler boundary; both either succeed or soft-fail-and-continue)
        6. Per Pitfall 10: do NOT assert "exactly one winner" — the read pass is microseconds; the loser may also report success against a plan that's already converged. The state-consistency assertions above are what matters.
    - Scenario B (Phase 52 Pitfall 52-2 concurrent first-load race): two processes start with claude-plugins.json ENOENT and state.json non-empty (a populated state record carrying legacy autoupdate field). Both call applyReconcile. Assertions:
        1. After both children exit, claude-plugins.json exists and parses cleanly
        2. The config contains exactly one marketplace entry (no double-merge)
        3. The autoupdate field is captured byte-stably (one of the processes writes it; the other observes 'valid' and short-circuits — Pitfall 52-5)
        4. state.json is unchanged content-wise (migrate generates a config from state, does NOT mutate state)
    - Scenario C (Phase 52 Pitfall 52-4 D-13 gate race verification): a single process running applyReconcile against a populated state with legacy autoupdate + ENOENT claude-plugins.json observes the gate AT loadState time (before the closure runs), preserves legacy autoupdate, captures it via migrate inside the closure, writes the config. The integration assertion is that the resulting config carries the autoupdate field — a single-process happy path; the deferred contract from `tests/persistence/migrate-config.test.ts:29-39` is the single-process lock-covered convergence proof, and Plan 02's apply.test.ts may also exercise this — this scenario provides the integration-level cover.
    - All three scenarios live in the same test file. Each scenario uses its own tmp HOME and tmp project cwd (mirror concurrent-install.test.ts's fixture-setup pattern).
    - Child entry point (`load-reconcile-race-child.ts`):
        * Receives IPC `{ cwd, home }` payload + a 'go' signal.
        * Stubs ctx (`ui.notify` accumulates into an array — reported back over IPC for the test's diagnostic visibility but the test does NOT assert on specific notify bytes; that's Plan 02's coverage).
        * Stubs pi (`getAllTools` returns []; matches the concurrent-install-child.ts pattern).
        * Calls `applyReconcile({ ctx, pi, cwd })`.
        * Reports `{ ok: true, notifyArgs: [...] }` or `{ ok: false, error: err.message }` back over IPC.
        * Exits 0.
    - Per the gaps-only re-verification gotchas memory: this is integration-only work — no STATE.md narrative-field changes from this plan; the `phase complete` step after Wave 3 will be the closure narrative.
  </behavior>
  <action>
    Create `tests/integration/load-reconcile-race.test.ts` mirroring `tests/integration/concurrent-install.test.ts` for the fork + IPC + ready-sync harness. Adapt the assertions per RESEARCH Pitfall 10 — state-consistency oriented, NOT exactly-one-winner oriented.

    Create `tests/integration/load-reconcile-race-child.ts` mirroring `tests/integration/concurrent-install-child.ts` for the child shape (IPC `ready` signal, IPC `go` payload, exit 0 on completion).

    Use a path-source marketplace fixture (no github source) so the test is fully network-free and deterministic. Use a small marketplace.json declaring a single plugin with no soft-deps to keep the install fast.

    Honor the Mortor file naming and Git workflow rules from CLAUDE.md (worktree commits use `SKIP=trufflehog`).

    Per source-comment-cleanup-policy: drop phase narrative from test comments; keep references to RECON-06 and Pitfall 52-2/52-4 for traceability.

    Do NOT extend the FORBIDDEN_TARGETS list in `tests/architecture/no-orchestrator-network.test.ts` — apply.ts legitimately uses network (per-entry NFR-5 soft-fail); the architecture gate intentionally excludes it.

    Do NOT add apply.ts to the ALLOWED_CONFIG_JSON_WRITERS allow-list in `tests/architecture/config-state-write-seams.test.ts` — apply.ts MUST NOT write `claude-plugins.json` directly (write-back is Phase 56's surface); the gate stays GREEN unchanged.

    After the test lands, verify the full integration suite runs cleanly: `npm test -- tests/integration/`. Then verify `npm run check` GREEN end-to-end (baseline ≥1675 unit + 9 integration per validation strategy).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; npm test -- tests/integration/load-reconcile-race.test.ts &amp;&amp; npm run check</automated>
  </verify>
  <done>
    - `tests/integration/load-reconcile-race.test.ts` GREEN with at least 3 scenarios (Scenario A: RECON-06 core race; Scenario B: Pitfall 52-2 concurrent first-load race; Scenario C: Pitfall 52-4 D-13 gate single-process integration cover)
    - `tests/integration/load-reconcile-race-child.ts` is forkable; receives IPC ready/go signals; calls applyReconcile via stub ctx + stub pi; exits 0
    - Assertions per RESEARCH Pitfall 10: state-consistency oriented (exactly one mp-a record, exactly one plugin-a record, no orphaned staging dirs); NOT exactly-one-winner oriented
    - Both children exit with code 0 in every scenario (NFR-2 — neither process throws past its handler boundary)
    - Phase 52 deferred obligations (`tests/persistence/migrate-config.test.ts:29-39`) discharged: Pitfall 52-2 (concurrent first-load) + Pitfall 52-4 (D-13 gate race) covered by Scenarios B + C
    - `tests/architecture/no-orchestrator-network.test.ts` GREEN unchanged (apply.ts intentionally absent from FORBIDDEN_TARGETS)
    - `tests/architecture/config-state-write-seams.test.ts` GREEN unchanged (apply.ts intentionally absent from ALLOWED_CONFIG_JSON_WRITERS)
    - `npm run check` GREEN end-to-end
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child process → parent test harness | IPC envelope is in-process for the test only; no external network or filesystem exposure beyond the tmp HOME / cwd directories. |
| concurrent process A → concurrent process B | Cross-process scope lock (proper-lockfile via withStateGuard) is the load-bearing invariant. The test validates it; an absence of the lock would be the threat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03-01 | Tampering | Concurrent double-apply produces an inconsistent state.json | mitigate | This plan's RECON-06 test scenario A asserts state.json post-condition is exactly one mp-a record + exactly one plugin-a record; mitigation is structural (withStateGuard + proper-lockfile) and validated empirically by this test. |
| T-55-03-02 | Tampering | Concurrent first-load races overwrite migrated claude-plugins.json | mitigate | Scenario B asserts the lock-covered migrate trichotomy: exactly one process writes; the other observes 'valid' (Pitfall 52-5) and short-circuits. |
| T-55-03-03 | Denial-of-service | Test flakiness from microsecond read-pass race | mitigate | Per RESEARCH Pitfall 10: the test does NOT assert "exactly one winner" — both processes may report success against a converged plan. Assertions are state-consistency oriented and stable. |
| T-55-03-04 | Information disclosure | tmp HOME / cwd contents leak between scenarios | mitigate | Each scenario uses its own tmp directory tree (mirror concurrent-install.test.ts's fixture-setup pattern). Cleanup runs after the scenario. |
| T-55-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan. Package legitimacy gate N/A per 55-RESEARCH.md. |
</threat_model>

<verification>
- Integration test GREEN: `npm test -- tests/integration/load-reconcile-race.test.ts` exits 0 with all 3 scenarios passing.
- Architecture gates unchanged: `tests/architecture/no-orchestrator-network.test.ts` and `tests/architecture/config-state-write-seams.test.ts` GREEN unchanged.
- Phase 52 deferred obligation closed: scenarios B + C exercise the Pitfall 52-2 + 52-4 paths the `tests/persistence/migrate-config.test.ts:29-39` hand-off flagged.
- Full suite gate: `npm run check` GREEN end-to-end (≥1675 unit + 9 integration per the validation strategy's baseline).
- No-flake check: run the new test file 5 times locally back-to-back; verify GREEN every time. (Pitfall 10's design rationale — state-consistency assertions are race-stable; the "exactly one winner" assertion would have been flaky.)
</verification>

<success_criteria>
- RECON-06 GREEN: two-process simultaneous-start race converges without double-apply, without interleaved write, with consistent state.json + no orphaned staging dirs.
- Phase 52 deferred Pitfall 52-2 + 52-4 obligations discharged via Scenarios B + C.
- Both children exit with code 0 in every scenario (NFR-2 preserved).
- `npm run check` GREEN end-to-end.
- Phase 55 ready for `/gsd-verify-work` per the VALIDATION.md sign-off rubric.
</success_criteria>

<artifacts_this_phase_produces>
- Plan 01 (DONE): orchestrated-mode foundation across addMarketplace / removeMarketplace / uninstallPlugin / setPluginEnabled.
- Plan 02 (DONE): atomic apply.ts + index.ts wiring + ReconcileAppliedCascadeMessage variant + catalog amendment + RECON-01/02/03/04/05 coverage.
- Plan 03 (this plan): `tests/integration/load-reconcile-race.test.ts` + `tests/integration/load-reconcile-race-child.ts` closing RECON-06 + Phase 52 deferred Pitfall 52-2/52-4 lock-coverage proof.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/55-load-time-reconcile-apply-notification-wiring/55-03-SUMMARY.md` when done.
</output>
