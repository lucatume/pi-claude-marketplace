---
phase: 56-write-back-integration-documentation
plan: 04
type: execute
wave: 3
depends_on: ["56-01", "56-02", "56-03"]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
  - tests/orchestrators/import/execute.test.ts
  - tests/orchestrators/marketplace/list.test.ts
  - tests/orchestrators/marketplace/info.test.ts
  - tests/orchestrators/plugin/list.test.ts
  - tests/orchestrators/plugin/info.test.ts
  - tests/architecture/config-state-consistency.test.ts
  - tests/architecture/no-split-01-cast-reads.test.ts
  - README.md
autonomous: true
requirements: [WB-01, WB-03, CFG-04, SPLIT-01, SPLIT-02]
must_haves:
  truths:
    - "A user running `/claude:plugin import` records every successfully-added marketplace and every successfully-installed plugin into the targeted scope's claude-plugins.json via ONE batched write per scope under ONE lock (WB-03)."
    - "The 7 SPLIT-01 autoupdate cast-read sites are REWIRED to consult `loadMergedScopeConfig(loc).merged.marketplaces[name]?.entry.autoupdate`; no `as unknown as Record<string, unknown>).autoupdate` substring survives in orchestrators (Pitfall 6 closed)."
    - "The README documents the claude-plugins.json (commit) vs claude-plugins.local.json (gitignore) convention in a new ## Configuration files section placed between Scoping and the /claude:plugin reference (CFG-04)."
    - "The config-state-consistency architecture test runs the FULL WB-01 SC#4 round-trip proof for every mutating command: after any single mutation, planReconcile against the post-mutation merged config and post-mutation state is empty, AND unknown forward-compat keys survive the write-back."
    - "The no-split-01-cast-reads architecture test ALLOW-LIST shrinks to size 0 (all 7 cast sites removed)."
    - "saveConfig remains the SOLE writer of claude-plugins.json / claude-plugins.local.json: SPLIT-02 architecture test allow-list stays size 1 (config-io.ts only); the silent-widening guard remains GREEN."
    - "The milestone v1.12 GREEN gate (npm run check) is GREEN end-to-end; total test count strictly increases from the Phase 55 close baseline (≥1709 unit + 10 integration)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/import/execute.ts"
      provides: "per-scope batched post-pass writing all imported marketplaces + plugins via writeBatchedConfigEntries under ONE withLockedStateTransaction (WB-03)"
      contains: "writeBatchedConfigEntries"
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts"
      provides: "SPLIT-01 rewire: autoupdate read from MergedConfig"
      contains: "loadMergedScopeConfig"
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts"
      provides: "SPLIT-01 rewire (2 sites at lines 68 + 122): autoupdate read from MergedConfig"
      contains: "loadMergedScopeConfig"
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts"
      provides: "SPLIT-01 rewire (line 455): autoupdate read from MergedConfig"
      contains: "loadMergedScopeConfig"
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts"
      provides: "SPLIT-01 rewire (2 sites at lines 430 + 452): autoupdate read from MergedConfig"
      contains: "loadMergedScopeConfig"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/list.ts"
      provides: "SPLIT-01 rewire (line 558): autoupdate read from MergedConfig"
      contains: "loadMergedScopeConfig"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts"
      provides: "SPLIT-01 rewire (line 240): autoupdate read from MergedConfig"
      contains: "loadMergedScopeConfig"
    - path: "README.md"
      provides: "CFG-04 documentation: ## Configuration files section + .local gitignore convention"
      contains: "## Configuration files"
    - path: "tests/architecture/config-state-consistency.test.ts"
      provides: "WB-01 SC#4 round-trip + reconcile no-op proof, LIVE (all `test.skip` flipped)"
      contains: "planReconcile"
    - path: "tests/architecture/no-split-01-cast-reads.test.ts"
      provides: "SPLIT-01 rewire completion gate: ALLOWED_SPLIT_01_AUTOUPDATE_CAST_FILES.size === 0"
      contains: "ALLOWED_SPLIT_01_AUTOUPDATE_CAST_FILES"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/import/execute.ts"
      to: "extensions/pi-claude-marketplace/persistence/config-write-back.ts"
      via: "writeBatchedConfigEntries import"
      pattern: "writeBatchedConfigEntries"
    - from: "extensions/pi-claude-marketplace/orchestrators/{marketplace,plugin}/{list,info,update,shared}.ts"
      to: "extensions/pi-claude-marketplace/persistence/config-merge.ts"
      via: "loadMergedScopeConfig import"
      pattern: "loadMergedScopeConfig"
    - from: "README.md"
      to: "extensions/pi-claude-marketplace/persistence/config-io.ts"
      via: "documented file paths claude-plugins.json + claude-plugins.local.json"
      pattern: "claude-plugins.json"
---

<objective>
Close the v1.12 milestone GREEN gate: import write-back (WB-03), SPLIT-01 read-path rewire of all 7 cast sites (Pitfall 6), README documentation (CFG-04), Wave 0 architecture tests flipped LIVE (WB-01 SC#4 round-trip + reconcile no-op + SPLIT-01 ALLOW-LIST → 0), SPLIT-02 architecture-test verification (Phase 52 A1 protocol — read, confirm no allow-list edit needed), and the milestone v1.12 GREEN gate (npm run check exits 0 with strictly more tests than the Phase 55 close baseline).

Purpose: This is the milestone close. After this plan, v1.12 is shippable: declarative config files are authoritative, every mutating command writes back, import batches its writes, the autoupdate read-path consults the config (not state), the README tells the user which file to commit, and the architecture tests structurally prove the disciplines hold.
Output: import/execute.ts with a per-scope batched post-pass; 7 SPLIT-01 cast-read sites rewired (across 6 files); README ## Configuration files section; config-state-consistency.test.ts running the full round-trip proof against every mutating command; no-split-01-cast-reads.test.ts allow-list shrunk to size 0; verification of SPLIT-02 architecture test; milestone v1.12 GREEN.
</objective>

<commit_discipline>
Run `pre-commit run --files <changed files>` BEFORE every `git commit`. Fix failures, restage, re-run. NEVER `--no-verify`. NEVER amend after a hook failure. The atomic-supersession discipline (v1.3 lesson) applies to: SPLIT-01 rewire (all 7 sites + the allow-list shrink) MAY ship per file because the allow-list shrink is per-file; the architecture-test `ALLOWED_SPLIT_01_AUTOUPDATE_CAST_FILES.size === N` sibling assertion must be updated atomically with each rewire commit to keep the test GREEN at every commit boundary.
</commit_discipline>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-write-back-integration-documentation/56-CONTEXT.md
@.planning/phases/56-write-back-integration-documentation/56-RESEARCH.md
@.planning/phases/56-write-back-integration-documentation/56-PATTERNS.md
@.planning/phases/56-write-back-integration-documentation/56-VALIDATION.md
@.planning/phases/56-write-back-integration-documentation/56-01-SUMMARY.md
@.planning/phases/56-write-back-integration-documentation/56-02-SUMMARY.md
@.planning/phases/56-write-back-integration-documentation/56-03-SUMMARY.md

# Frozen Phase 54-55 analogs
@extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts

# Files to modify
@extensions/pi-claude-marketplace/orchestrators/import/execute.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
@README.md

# Plan 01 helpers
@extensions/pi-claude-marketplace/persistence/config-write-back.ts
@extensions/pi-claude-marketplace/persistence/config-merge.ts

# Architecture tests to verify / flip live
@tests/architecture/config-state-write-seams.test.ts
@tests/architecture/config-state-consistency.test.ts
@tests/architecture/no-split-01-cast-reads.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Import write-back batched post-pass (WB-03)</name>
  <files>extensions/pi-claude-marketplace/orchestrators/import/execute.ts, tests/orchestrators/import/execute.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/import/execute.ts (entirely — current import flow, orchestrated-mode per-entry calls, result-accumulator shape, executeScopedPlan structure)
    - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts (lines 349-403 — the lock-body idiom to mirror in the batched post-pass)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts (lines 120-179 — the per-scope read-pass under lock that Phase 56 must not disturb; verified template for the import batched post-pass shape)
    - extensions/pi-claude-marketplace/persistence/config-write-back.ts (writeBatchedConfigEntries + BatchedConfigPatch signatures from Plan 01)
    - extensions/pi-claude-marketplace/persistence/locations.ts (ScopedLocations + locationsFor helpers)
    - .planning/phases/56-write-back-integration-documentation/56-PATTERNS.md sections on import batched post-pass + Pitfall 8 race-window comment
    - .planning/phases/56-write-back-integration-documentation/56-RESEARCH.md sections on import (WB-03) + Pitfall 8
  </read_first>
  <behavior>
    - After all per-entry orchestrated-mode addMarketplace / installPlugin calls complete for a scope, run a per-scope post-pass under ONE withLockedStateTransaction:
      1. targetConfigPath = locations.configJsonPath (import does NOT support --local — see RESEARCH Architecture Patterns project structure; --local is per-command, not on import surface).
      2. loadConfig(targetConfigPath); if invalid → emit a per-scope outcome (deferred: do not save state inside this post-pass — state was already saved by the per-entry orchestrators), continue with next scope.
      3. current = cfg.status === "valid" ? cfg.config : { schemaVersion: 1 }.
      4. Build BatchedConfigPatch:
         - batch.marketplaces[name] = { source: <verbatim rawSource for that mp> } for every SUCCESSFULLY-ADDED marketplace in result.addedMarketplaces.
         - batch.plugins[`${plugin}@${marketplace}`] = {} for every SUCCESSFULLY-INSTALLED plugin in result.installedPlugins.
      5. writeBatchedConfigEntries(current, targetConfigPath, locations.scopeRoot, batch).
      6. NO tx.save() inside this closure — state was already committed by per-entry orchestrators (RESEARCH Pitfall 8 race-window is BOUNDED and self-healing).
    - When batch is empty (no successful additions for the scope): SKIP the post-pass entirely (RECON-05 byte-stability).
    - Per-scope failure (CFG-03 invalid-config) does NOT abort other scopes' post-passes.

    Inline race-window comment required (Pitfall 8): document that the bounded race between per-entry lock release and batched-save lock acquire is self-healing on next reconcile.

    Tests:
    - happy path: import 2 marketplaces + 3 plugins into project scope; after import, claude-plugins.json contains exactly those entries; ONE batched write fired (count the saveConfig calls via fs.stat mtime — exactly one mtime touch on the project config file).
    - empty post-pass: import where every per-entry call failed → claude-plugins.json byte-unchanged.
    - mixed: 1 success + 1 failure per scope → only the successful entry appears in the batched write.
    - CFG-03: pre-seed an invalid claude-plugins.json; import should surface the per-scope abort but other scopes' post-passes still run.
  </behavior>
  <action>
    Edit `extensions/pi-claude-marketplace/orchestrators/import/execute.ts` to add the per-scope batched post-pass at the end of executeScopedPlan (or wherever the per-scope orchestrated calls aggregate; locate via grep on `addedMarketplaces` / `installedPlugins` accumulator). Imports add: writeBatchedConfigEntries from persistence/config-write-back.ts, loadConfig from persistence/config-io.ts, withLockedStateTransaction from transaction/with-state-guard.ts.

    Collect-functions: implement two small helpers `collectMarketplacePatches(result)` and `collectPluginPatches(result)` returning `Record<string, Partial<MarketplaceConfigEntry>>` and `Record<string, Partial<PluginConfigEntry>>` respectively. Source values: `source: rawSource` verbatim per the planReconcile contract (PATTERNS §"Verbatim rawSource").

    Source-comment hygiene: terse `// WB-03 / Pitfall 8: bounded race window self-heals on next reconcile`; DO NOT add lineage / phase narrative.
  </action>
  <verify>
    <automated>npm test -- tests/orchestrators/import/execute.test.ts &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "writeBatchedConfigEntries" extensions/pi-claude-marketplace/orchestrators/import/execute.ts` returns at least one match.
    - `grep -n "withLockedStateTransaction" extensions/pi-claude-marketplace/orchestrators/import/execute.ts` returns at least one match.
    - Test file contains at least one test asserting the BATCHED nature: after import of N entries, the targeted config mtime changes exactly ONCE (use fs.stat before/after).
    - Test file contains at least one test asserting CFG-03 per-scope abort does not block other scopes.
    - `npm test -- tests/orchestrators/import/execute.test.ts` exits 0.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>
    import write-back wired as ONE batched post-pass per scope under ONE lock; race-window documented; tests cover happy/empty/mixed/CFG-03 paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: SPLIT-01 read-path rewire (7 cast-read sites across 6 files) + allow-list shrink to 0</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts, extensions/pi-claude-marketplace/orchestrators/plugin/list.ts, extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, tests/architecture/no-split-01-cast-reads.test.ts, tests/orchestrators/marketplace/list.test.ts, tests/orchestrators/marketplace/info.test.ts, tests/orchestrators/plugin/list.test.ts, tests/orchestrators/plugin/info.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts (line 68 region — current cast read; full read of the function that contains it)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts (lines 68 + 122 regions — 2 cast sites; full read of their containing functions)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts (line 455 region — full read of containing function)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts (lines 430 + 452 regions — 2 cast sites; both are likely in the renderer/projection helper)
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts (line 558 region)
    - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts (line 240 region)
    - extensions/pi-claude-marketplace/persistence/config-merge.ts (loadMergedScopeConfig + MergedConfig.marketplaces[name].entry shape)
    - .planning/phases/56-write-back-integration-documentation/56-RESEARCH.md sections on SPLIT-01 / Pitfall 6 + State of the Art (rewire target)
    - .planning/phases/56-write-back-integration-documentation/56-PATTERNS.md section on SPLIT-01 read-path rewire
  </read_first>
  <behavior>
    For each of the 7 cast-read sites:
    - REPLACE `(record as unknown as Record<string, unknown>).autoupdate as boolean | undefined ?? false` (or similar variant) with a read from the merged config: `merged.marketplaces[name]?.entry.autoupdate ?? false`.
    - The CALLER may need to `await loadMergedScopeConfig(locations)` (if it doesn't already have a merged view); thread the merged view through the function instead of computing it twice when the function is called inside a tight loop.
    - Some sites are inside a list-rendering loop — pre-compute `merged` ONCE before the loop, then read inside.
    - DELETE the `// SPLIT-01:` comment markers at each rewired site.
    - In the architecture test `tests/architecture/no-split-01-cast-reads.test.ts`, SHRINK the `ALLOWED_SPLIT_01_AUTOUPDATE_CAST_FILES` set per file rewired AND update the matching `assert.equal(ALLOWED_SPLIT_01_AUTOUPDATE_CAST_FILES.size, N)` sibling assertion in the same commit. End state: size === 0.

    Sites preserved (NOT rewired this plan):
    - `persistence/state-io.ts:76` — schema-level COMMENT (D-12 decision narrative), not a cast read; leave intact. Per RESEARCH A6, this is the 8th occurrence that does NOT count as an actionable code site.
    - `persistence/migrate-config.ts:102` — D-13 legacy-field-capture site that intentionally reads the cast value (it MIGRATES the legacy state.json autoupdate into the config); leave intact.

    User-visible byte forms (list / info output) are UNCHANGED because the rewire reads the same value from a different source. Existing tests SHOULD stay GREEN. If any test asserts behavior with a state.autoupdate field set in fixtures but no config equivalent, update the FIXTURE to mirror reality (the autoupdate field is now config-side; the state record no longer has it written by the orchestrators).

    Watch list.test.ts / info.test.ts fixtures: any test that today sets `state.marketplaces[name].autoupdate = true` and expects the list output to render the autoupdate marker must be UPDATED to set `config.marketplaces[name].autoupdate = true` instead. This is the test-update lockstep the rewire requires.
  </behavior>
  <action>
    Rewire each cast site to a MergedConfig read. Pre-compute `merged = await loadMergedScopeConfig(locations)` once per render pass (e.g. once at the top of list.ts's render function; once at the top of info.ts's render function; once per shared.ts helper invocation).

    For `marketplace/shared.ts:430` + `:452`: these are inside the shared renderer/projection. If the helper takes a record-and-scope pair, change the signature to `(record, scope, merged)` so the caller passes the pre-computed merged view in.

    Per-commit discipline: prefer ONE atomic commit per file rewired (each commit shrinks the allow-list by 1 and updates the size assertion). This keeps `npm run check` GREEN at every commit boundary. If using a single commit for all 6 files, the allow-list goes directly to size 0 in the same commit.

    Update test fixtures where they depend on state.autoupdate as the truth source. The byte-form assertions on `Usage:` strings, list rendering, info rendering should NOT change — the read source changes, not the rendered output.

    Source-comment hygiene: terse `// autoupdate now lives in the config (SPLIT-01)` is acceptable but optional; the truth lives in the imports.
  </action>
  <verify>
    <automated>npm test -- tests/architecture/no-split-01-cast-reads.test.ts tests/orchestrators/marketplace/list.test.ts tests/orchestrators/marketplace/info.test.ts tests/orchestrators/marketplace/update.test.ts tests/orchestrators/plugin/list.test.ts tests/orchestrators/plugin/info.test.ts tests/architecture/catalog-uat.test.ts &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "as unknown as Record<string,\\s*unknown>).autoupdate" extensions/pi-claude-marketplace/orchestrators/` returns no matches (all 7 sites rewired).
    - `grep -rn "// SPLIT-01:" extensions/pi-claude-marketplace/orchestrators/` returns no matches (markers deleted).
    - `persistence/state-io.ts:76` SPLIT-01 schema-level COMMENT remains (acceptable; D-12 narrative).
    - `persistence/migrate-config.ts:102` SPLIT-01 legacy-capture site remains (acceptable; D-13).
    - `tests/architecture/no-split-01-cast-reads.test.ts` ALLOWED_SPLIT_01_AUTOUPDATE_CAST_FILES.size === 0; sibling assertion `assert.equal(..., 0)` matches.
    - `tests/architecture/no-split-01-cast-reads.test.ts` passes (no offenders detected).
    - All orchestrator test suites GREEN.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>
    7 SPLIT-01 cast-read sites rewired to MergedConfig; allow-list size 0; Pitfall 6 closed; user-visible byte forms unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WB-01 SC#4 round-trip + reconcile no-op proof (flip config-state-consistency.test.ts to LIVE)</name>
  <files>tests/architecture/config-state-consistency.test.ts</files>
  <read_first>
    - tests/architecture/config-state-consistency.test.ts (entirely — current shape from Plan 01 with `test.skip` placeholders)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts (planReconcile + emptyReconcilePlan)
    - extensions/pi-claude-marketplace/persistence/config-merge.ts (mergeScopeConfigs)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts (post-Plan 02 shape)
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (post-Plan 03 shape)
    - .planning/phases/56-write-back-integration-documentation/56-RESEARCH.md Code Examples Example 3 (round-trip test shape)
  </read_first>
  <behavior>
    For each mutating command (marketplace add/remove/autoupdate/noautoupdate + plugin install/uninstall/reinstall/update + enable/disable), a test case asserts:
    1. Seed: `claude-plugins.json` contains fixture with at least one `futureField: "preserve me"` at the entry level AND a `futureTopLevel: "preserve me too"` at the top level. State is in a corresponding starting shape.
    2. Run the mutating command via its orchestrator (standalone mode).
    3. Read back the config; assert `futureField` and `futureTopLevel` survived.
    4. Re-load merged config + state; call planReconcile(merged, state, scope); assert the result deep-equals emptyReconcilePlan(scope) — i.e. reconcile IS a no-op.

    Plus one test for the orchestrated-mode SKIP discipline: same fixture, but with `notifications: { mode: "orchestrated" }`. After the call, the config file bytes are UNCHANGED (mtime + bytes; state may have changed). This proves the WR-09 discipline structurally.

    Use a single shared `tmp scope root` per test (mkdtemp + cleanup in afterEach). Real saveConfig writes occur — no module mocking.
  </behavior>
  <action>
    Edit `tests/architecture/config-state-consistency.test.ts` to flip the `test.skip` placeholder(s) into LIVE tests. Add one test per mutating command following the shape in `<behavior>`. Use the post-Plan 02/03 orchestrator signatures.

    For commands that need a precondition (e.g. uninstall needs a plugin already installed, remove needs a marketplace already added), the test SEEDS the state + config in the starting shape before running the command.

    Top-of-file rationale comment block explains:
    - WB-01 SC#4 is the round-trip integrity contract.
    - Unknown-key preservation is D-09 (lenient schema).
    - The reconcile no-op assertion is the convergence proof that complements RECON-05 byte-stability.
    - This test is the milestone GREEN gate goal-backward criterion.
  </action>
  <verify>
    <automated>npm test -- tests/architecture/config-state-consistency.test.ts &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `tests/architecture/config-state-consistency.test.ts` contains no remaining `test.skip` calls (all flipped to live).
    - At least one LIVE test per mutating command (8 commands minimum): marketplace add/remove/autoupdate/noautoupdate + plugin install/uninstall/reinstall/update + enable/disable (9 total with enable/disable counting as one merged command shape).
    - At least one orchestrated-mode SKIP discipline test.
    - All tests PASS.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>
    config-state-consistency architecture test is LIVE for every mutating command; round-trip integrity + reconcile no-op proven; orchestrated-mode SKIP structurally guarded by a test.
  </done>
</task>

<task type="auto">
  <name>Task 4: CFG-04 README documentation + SPLIT-02 architecture-test verification + milestone GREEN gate</name>
  <files>README.md, tests/architecture/config-state-write-seams.test.ts</files>
  <read_first>
    - README.md (entirely — current structure: Features (line 21), Prerequisites (32), Usage (38), Name mapping (94), Scoping (122), /claude:plugin reference (130), Marketplace (134), Plugin (205), Bootstrap (284), Import (299), Contributing (316), AI disclaimer (320), License (328))
    - .planning/phases/56-write-back-integration-documentation/56-RESEARCH.md Code Examples Example 4 (ready-to-paste README section) + Open Question #4 (insertion-point recommendation)
    - tests/architecture/config-state-write-seams.test.ts (entirely — verify the allow-list size + regex patterns; A1 Phase 52 verification protocol applies: READ + CONFIRM no edit needed)
  </read_first>
  <behavior>
    README:
    - Insert a NEW `## Configuration files` section between line 128 (end of Scoping) and line 130 (start of `/claude:plugin reference`).
    - Section body per RESEARCH Code Examples Example 4: explain `claude-plugins.json` per-scope locations (user vs project table), the role of mutating commands writing back into it, the `claude-plugins.local.json` entry-level override semantics, the `--local` flag, and the `.gitignore` convention with a literal `.pi/claude-plugins.local.json` line for project scope.
    - All content English-only (IL-1) and follows Google Markdown style guide per global instructions.

    SPLIT-02 architecture-test verification:
    - READ `tests/architecture/config-state-write-seams.test.ts` end-to-end.
    - CONFIRM: ALLOWED_CONFIG_JSON_WRITERS has exactly 1 entry (config-io.ts) and the sibling `assert.deepEqual([...ALLOWED_CONFIG_JSON_WRITERS].sort(), [...])` matches.
    - CONFIRM: FORBIDDEN_CONFIG_JSON_PATTERN regex targets `atomicWriteJson(<...>configJsonPath, ...)` — Phase 56 callers go through `saveConfig` (which calls `atomicWriteJson` inside config-io.ts), so the regex does NOT match the new write-back call sites in the orchestrators.
    - VERIFY: `npm test -- tests/architecture/config-state-write-seams.test.ts` exits 0 with NO edits to the test file in this plan.
    - Document the verification outcome in the plan SUMMARY: "Phase 52 A1 protocol applied — SPLIT-02 architecture test stays at allow-list size 1; saveConfig remains the SOLE sanctioned writer."

    Milestone GREEN gate:
    - Final `npm run check` exits 0.
    - Test count strictly INCREASES from Phase 55 close baseline (≥1709 unit + 10 integration). Record final counts in plan SUMMARY.
    - All 24 v1.12 requirements show CLOSED in REQUIREMENTS.md Traceability table after this plan.
  </behavior>
  <action>
    Edit `README.md` to insert the `## Configuration files` section using the body from RESEARCH Code Examples Example 4 verbatim (English only, Google Markdown style). Adjust internal cross-references if any line numbers in other README sections shift.

    READ `tests/architecture/config-state-write-seams.test.ts` and confirm the verification points in `<behavior>`. If any verification surprise (e.g. a new direct `atomicWriteJson` callsite slipped in), STOP and route through Plan 02/03 fix-up — do NOT widen the allow-list as a workaround; the allow-list widening is reserved for genuine new sanctioned writers, not accidental bypasses.

    Run `npm run check` after every edit; the milestone GREEN gate must pass before this task is considered done.

    Update `.planning/REQUIREMENTS.md` Traceability table to mark WB-01, WB-02, WB-03, WB-04, CFG-04 as Done. Update ROADMAP.md Phase 56 entry to `**Plans:** 4/4 plans complete` and check off each plan.
  </action>
  <verify>
    <automated>npm test -- tests/architecture/config-state-write-seams.test.ts &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^## Configuration files" README.md` returns at least one match.
    - `grep -n "claude-plugins.local.json" README.md` returns at least 2 matches (table + gitignore section).
    - `grep -n "\\.pi/claude-plugins\\.local\\.json" README.md` returns at least one match (gitignore convention).
    - `tests/architecture/config-state-write-seams.test.ts` is byte-unchanged compared to before this task (`git diff tests/architecture/config-state-write-seams.test.ts` is empty).
    - `npm run check` exits 0.
    - Test count ≥ 1709 unit (Phase 55 baseline) + new Plans 01/02/03/04 tests; final count recorded in plan SUMMARY.
    - REQUIREMENTS.md Traceability table shows WB-01, WB-02, WB-03, WB-04, CFG-04 as Done (Plan 56-04, today's date).
  </acceptance_criteria>
  <done>
    CFG-04 documented; SPLIT-02 architecture test verified (Phase 52 A1 protocol); milestone v1.12 GREEN gate closed; all 24 requirements CLOSED; REQUIREMENTS + ROADMAP updated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → import command args | scope flag parsed by edge handler; import does NOT support --local (per RESEARCH project structure) |
| import orchestrator → physical claude-plugins.json | targetConfigPath always locations.configJsonPath; never local-file unless future surface |
| concurrent Pi processes → claude-plugins.json (import batched save) | proper-lockfile per-scope; bounded race window between per-entry lock release and batched-save lock acquire is self-healing on next reconcile (Pitfall 8) |
| Read-path consumer → MergedConfig | loadMergedScopeConfig reads base + local under no lock (read-only; reconcile and renderers already do this); writes only happen through the locked write seam |
| README → user expectations | documented gitignore convention; if user ignores convention, claude-plugins.local.json gets committed (out of scope; user's choice) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-04-01 | Tampering | import batched post-pass writes a stale config snapshot because per-entry orchestrators committed state outside the post-pass lock | accept | Pitfall 8 self-healing race: a concurrent process between the last per-entry lock release and the batched-save lock acquire can OBSERVE the partially-committed state; the next reconcile reflects the divergence and emits a plan to fix it (NFR-3 retry safety). Inline race-window comment documents the bound. |
| T-56-04-02 | Tampering | import write-back overwrites a user's already-edited claude-plugins.json (user edited the file mid-import) | mitigate | withLockedStateTransaction holds the per-scope lock; loadConfig reads the file FRESH inside the lock so the user's edits are seen before the batched patch is applied; the patch spreads OVER the freshly-read config (preserves unknown keys + non-imported entries) |
| T-56-04-03 | Information disclosure | SPLIT-01 rewire surfaces the autoupdate flag from a DIFFERENT field source (MergedConfig.marketplaces[name].entry.autoupdate vs state record cast) | accept | User-visible byte forms unchanged; the rewire is internal-only. Test suites assert the rendered output is the same. |
| T-56-04-04 | Tampering | SPLIT-01 rewire reads from MergedConfig but the config file is invalid (CFG-03) — read-path needs to handle the invalid trichotomy gracefully | mitigate | `loadMergedScopeConfig` calls `loadConfig` under the hood; the trichotomy is exposed. Read-path callers (list/info renderers) MUST handle the invalid arm by either falling back to a safe default (autoupdate: false) or surfacing the CFG-03 abort via the standard structured-row path. Verify each rewire site handles invalid gracefully — list/info reads do NOT mutate, so a fallback default is acceptable. |
| T-56-04-05 | Information disclosure | README documents file paths that include `~/.pi/` and `<cwd>/.pi/` — exposes home-directory layout | accept | Public knowledge; the Pi conventions document the same paths. Acceptable per RESEARCH Security Domain. |
| T-56-04-06 | Tampering | Phase 56 inadvertently introduces a direct atomicWriteJson(configJsonPath) bypass that escapes the SPLIT-02 architecture test | mitigate | Phase 52 A1 protocol: READ + VERIFY config-state-write-seams.test.ts at plan close. The verification step in Task 4 ensures the architecture test catches a regression even if all per-orchestrator tests pass. Silent-widening guard (ALLOWED_CONFIG_JSON_WRITERS.size === 1 sibling assertion) forces a deliberate edit if the allow-list is widened. |
| T-56-04-SC | Tampering | npm/pip/cargo installs | mitigate | NOT APPLICABLE — Phase 56 installs zero new packages; legitimacy gate skipped per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `npm test -- tests/orchestrators/import/execute.test.ts` exits 0 (WB-03 batched post-pass tests GREEN).
- `npm test -- tests/architecture/no-split-01-cast-reads.test.ts` exits 0 with `ALLOWED_SPLIT_01_AUTOUPDATE_CAST_FILES.size === 0`.
- `npm test -- tests/architecture/config-state-consistency.test.ts` exits 0 with NO `test.skip` calls remaining.
- `npm test -- tests/architecture/config-state-write-seams.test.ts` exits 0 with ALLOWED_CONFIG_JSON_WRITERS.size === 1 (saveConfig only).
- `npm test -- tests/architecture/catalog-uat.test.ts` exits 0 (byte-equality preserved end-to-end across all 4 plans).
- `npm run check` exits 0 (milestone v1.12 GREEN gate).
- Total test count strictly INCREASES from Phase 55 baseline (≥1709 unit + 10 integration).
- REQUIREMENTS.md Traceability shows WB-01..04 + CFG-04 = Done; all 24 v1.12 requirements = CLOSED.
- ROADMAP.md Phase 56 entry shows `**Plans:** 4/4 plans complete`.
</verification>

<success_criteria>
- WB-03 closed: import writes ONE batched multi-entry patch per scope under ONE lock with race-window comment.
- SPLIT-01 closed: all 7 cast-read sites rewired to MergedConfig; allow-list size 0; markers deleted; user-visible bytes unchanged.
- CFG-04 closed: README documents the config workflow + .local gitignore convention.
- WB-01 SC#4 closed: round-trip integrity + post-mutation reconcile no-op proven LIVE for every mutating command.
- SPLIT-02 verified: Phase 52 A1 protocol applied; architecture test allow-list size 1 preserved; saveConfig remains SOLE writer.
- Milestone v1.12 GREEN gate closed: `npm run check` exits 0 with all 24 requirements Done.
</success_criteria>

<output>
Create `.planning/phases/56-write-back-integration-documentation/56-04-SUMMARY.md` when done.
Record final test counts (unit + integration) and the milestone v1.12 closure narrative in the SUMMARY.
</output>
