---
phase: 56-write-back-integration-documentation
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/autoupdate.ts
  - tests/orchestrators/marketplace/add.test.ts
  - tests/orchestrators/marketplace/remove.test.ts
  - tests/orchestrators/marketplace/autoupdate.test.ts
  - tests/edge/handlers/marketplace/add.test.ts
  - tests/edge/handlers/marketplace/remove.test.ts
  - tests/edge/handlers/marketplace/autoupdate.test.ts
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
autonomous: true
requirements: [WB-01, WB-02, WB-04]
must_haves:
  truths:
    - "A user running `marketplace add <name> <source> [--scope] [--local]` records the new marketplace entry into the targeted physical config file (base or local) under the scope lock; the other physical file is byte-unchanged."
    - "A user running `marketplace remove <name> [--scope] [--local]` deletes the marketplace entry AND every plugin entry whose key ends in `@<name>` from the targeted config file (cascade)."
    - "A user running `marketplace autoupdate <name>` / `marketplace noautoupdate <name>` records `autoupdate: true/false` on the entry under the targeted file; idempotent flips DO NOT touch the config file (mtime byte-stable, RECON-05 preserved)."
    - "Every orchestrated-mode call (notifications.mode === 'orchestrated', reconcile-driven) SKIPS write-back (WR-09)."
    - "Every CFG-03 invalid-config arm aborts the command with a truthful per-file message (`path.basename(targetConfigPath)`) and skips both state save and config write."
    - "The bootstrap command inherits WB-04 by composition: it calls addMarketplace (writes back the marketplace entry) then setMarketplaceAutoupdate (writes back the autoupdate flag) — 2 separate writes, no batched helper needed (per RESEARCH A2)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts"
      provides: "write-back wired on success; `local?: boolean` option threaded; CFG-03 invalid-config arm aborts; WR-09 orchestrated skip"
      contains: "writeMarketplaceConfigEntry"
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts"
      provides: "cascade write-back on success via deleteMarketplaceConfigEntryWithCascade; --local + WR-09 + CFG-03 disciplines"
      contains: "deleteMarketplaceConfigEntryWithCascade"
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts"
      provides: "write-back on fresh flip only; --local + WR-09 + CFG-03 disciplines"
      contains: "writeMarketplaceConfigEntry"
    - path: "extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts"
      provides: "[--local] usage extension + scanner integration"
      contains: "extractLocalFlag"
    - path: "extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts"
      provides: "[--local] usage extension + scanner integration"
      contains: "extractLocalFlag"
    - path: "extensions/pi-claude-marketplace/edge/handlers/marketplace/autoupdate.ts"
      provides: "[--local] usage extension + scanner integration (both autoupdate/noautoupdate verbs)"
      contains: "extractLocalFlag"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/marketplace/{add,remove,autoupdate}.ts"
      to: "extensions/pi-claude-marketplace/persistence/config-write-back.ts"
      via: "named-import write/delete helpers"
      pattern: "from \"../../persistence/config-write-back\\.ts\""
    - from: "extensions/pi-claude-marketplace/edge/handlers/marketplace/{add,remove,autoupdate}.ts"
      to: "extensions/pi-claude-marketplace/edge/handlers/shared.ts"
      via: "extractLocalFlag import"
      pattern: "extractLocalFlag"
---

<objective>
Wire write-back into the three mutating marketplace orchestrators (`add`, `remove`, `autoupdate` / `noautoupdate`) and add the `--local` flag to their three edge handlers, exactly mirroring the Phase 54 `enable-disable.ts` discipline. WB-04 bootstrap is satisfied by composition (no new code in `bootstrap.ts`) — `addMarketplace` and `setMarketplaceAutoupdate` writing back in standalone mode means bootstrap inherits both writes automatically.

Purpose: Close the marketplace side of WB-01 / WB-02 / WB-04. Plan 03 closes the plugin side in parallel; Plan 04 closes import (WB-03), the SPLIT-01 read-path rewire, the README (CFG-04), and the milestone GREEN gate.
Output: Three marketplace orchestrators with write-back + `--local` + CFG-03 + WR-09 disciplines, three marketplace edge handlers gaining `--local`, updated tests, catalog/UAT updates for any `Usage:`-rendering states (Pitfall 7).
</objective>

<commit_discipline>
Run `pre-commit run --files <changed files>` BEFORE every `git commit`. Fix failures, restage, re-run. NEVER `--no-verify`. NEVER amend after a hook failure. One commit per behavior boundary is acceptable, but if catalog/UAT fixtures change in lockstep with handler code, land BOTH in the SAME commit (the v1.3 atomic-supersession lesson — Pitfall 7).
</commit_discipline>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-write-back-integration-documentation/56-CONTEXT.md
@.planning/phases/56-write-back-integration-documentation/56-RESEARCH.md
@.planning/phases/56-write-back-integration-documentation/56-PATTERNS.md
@.planning/phases/56-write-back-integration-documentation/56-VALIDATION.md
@.planning/phases/56-write-back-integration-documentation/56-01-SUMMARY.md

# Frozen Phase 54 analogs — copy discipline verbatim
@extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts

# Files to modify
@extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts
@extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts
@extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts
@extensions/pi-claude-marketplace/edge/handlers/marketplace/autoupdate.ts

# New helpers from Plan 01
@extensions/pi-claude-marketplace/persistence/config-write-back.ts
@extensions/pi-claude-marketplace/edge/handlers/shared.ts

# Phase 55 frozen orchestrated-mode source-of-truth
@extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire write-back + --local + WR-09 into marketplace/add.ts and marketplace/autoupdate.ts (orchestrators) + their edge handlers</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts, extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts, extensions/pi-claude-marketplace/edge/handlers/marketplace/autoupdate.ts, tests/orchestrators/marketplace/add.test.ts, tests/orchestrators/marketplace/autoupdate.test.ts, tests/edge/handlers/marketplace/add.test.ts, tests/edge/handlers/marketplace/autoupdate.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts (lines 305-445 — full frozen WR-01/WR-09 lock body; THIS is the shape to mirror)
    - extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts (lines 1-115 — handler shape: extractLocalFlag call at 92, USAGE string with `[--local]` at 23-27, forwarding to orchestrator)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts (entirely — current shape uses withStateGuard at line 313; opts type; rawSource handling; success/failure arms)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts (entirely — current shape uses withStateGuard at line 193; fresh-vs-idempotent flip discrimination)
    - extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts (entirely — current parsing + scope handling)
    - extensions/pi-claude-marketplace/edge/handlers/marketplace/autoupdate.ts (entirely — current parsing; both autoupdate and noautoupdate verbs)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts (samePlannedSource at the top — proves the rawSource verbatim contract)
    - .planning/phases/56-write-back-integration-documentation/56-PATTERNS.md §"orchestrators/marketplace/add.ts (and remove.ts, plugin/install.ts, plugin/uninstall.ts)" + §"Verbatim rawSource for Marketplace Patches"
    - .planning/phases/56-write-back-integration-documentation/56-RESEARCH.md §"Code Examples" Example 2 (verbatim add wiring shape) + §"Pitfall 5" (idempotent skip) + §"Pitfall 3" (WR-09 orchestrated skip)
  </read_first>
  <behavior>
    Orchestrator add.ts:
    - Add `local?: boolean` to the options type.
    - Convert the existing `withStateGuard(locations, ...)` to `withLockedStateTransaction(locations, async (tx) => { ... await tx.save(); })` (PATTERNS option 1; matches enable-disable). The full state mutation is preserved; the only behavioral change is that `tx.save()` becomes explicit AND a config write-back call is added inside the SAME closure.
    - Before any lock acquisition: `const targetConfigPath = opts.local === true ? locations.configLocalJsonPath : locations.configJsonPath; const configBasename = path.basename(targetConfigPath);`.
    - Inside the lock, BEFORE state mutation: `const cfg = await loadConfig(targetConfigPath); if (cfg.status === "invalid") { /* CFG-03: route through the existing failure path so the user sees a structured (failed) row; basename only — never absolute path (T-53-02-02 / T-54-02-02) */ }`.
    - AFTER successful state mutation that sets `recordedName`: `if (opts.notifications?.mode !== "orchestrated") { const current = cfg.status === "valid" ? cfg.config : { schemaVersion: 1 as const }; await writeMarketplaceConfigEntry(current, targetConfigPath, locations.scopeRoot, recordedName, { source: opts.rawSource }); }`. The `source` field is `opts.rawSource` VERBATIM (PATTERNS §"Verbatim rawSource"; planReconcile compares against this exact string via samePlannedSource).
    - Failure / not-recorded arms: SKIP both `await tx.save()` and the write-back call — `return` early or guard the save.

    Orchestrator autoupdate.ts:
    - Add `local?: boolean` to the options type (both the fresh-flip and idempotent paths share the option).
    - Convert `withStateGuard` (line 193) to `withLockedStateTransaction`.
    - Same target-path selection + `loadConfig` + CFG-03 abort pattern as add.ts.
    - AFTER a FRESH flip (status === "autoupdate enabled" OR "autoupdate disabled"): `if (opts.notifications?.mode !== "orchestrated") { await writeMarketplaceConfigEntry(current, targetConfigPath, locations.scopeRoot, name, { autoupdate: enable }); }`.
    - IDEMPOTENT arms (`already autoupdate` / `already no autoupdate`): NO write-back, NO tx.save (Pitfall 5 mtime-drift guard).
    - FAILED arms (not added, etc.): NO write-back.

    Edge handlers add.ts + autoupdate.ts (both verbs):
    - USAGE strings gain trailing `[--local]` (Phase 54 frozen shape at edge/handlers/plugin/enable-disable.ts:23-27).
    - Call `extractLocalFlag(args, ctx, usage)` from `edge/handlers/shared.ts` BEFORE the existing positional parser. If undefined returned (unknown flag rejected), early-return.
    - Pass `residualArgs` to the existing positional parser (it sees `--scope` and positionals only).
    - Forward `local: true` to the orchestrator when set.

    Unit tests (extend existing):
    - add.ts: `--local` writes to claude-plugins.local.json; base is byte-unchanged. NOT `--local` writes to claude-plugins.json. orchestrated-mode SKIPS both files (config byte-unchanged). CFG-03 invalid local file aborts with a structured (failed) row whose body contains `path.basename(localPath)` — no absolute path leak.
    - autoupdate.ts: FRESH flip writes back; IDEMPOTENT flip does NOT (assert mtime / bytes unchanged). Failed (mp not added) does NOT write back. orchestrated-mode SKIPS write-back.
    - Edge handlers: `--local` parsed at any position; unknown flag → usage error; usage string contains `[--local]`.
  </behavior>
  <action>
    Apply the `<behavior>` changes to the four source files and extend the four test files. Imports added to orchestrators: `import { writeMarketplaceConfigEntry } from "../../persistence/config-write-back.ts"; import { loadConfig } from "../../persistence/config-io.ts"; import { withLockedStateTransaction } from "../../transaction/with-state-guard.ts"; import path from "node:path";`. Imports added to edge handlers: `import { extractLocalFlag } from "../shared.ts";` (resolve relative path against handler location — handlers under `edge/handlers/marketplace/` import from `../shared.ts`).

    Source-comment hygiene per project memory: do NOT leave historical narrative ("Phase 56 added this in 2026-06-10") in source comments. DO keep terse decision-ID references (e.g. `// WB-01 / WR-09: orchestrated-mode reconcile is config-driven, skip write-back`) for traceability per source-comment-cleanup-policy.

    Each test extension uses an isolated tmp scope root (`mkdtemp` under `os.tmpdir`) so write-back actually touches disk; assertions read the file back via `fs.readFile` and compare to expected JSON. The Wave 0 `tests/architecture/config-state-consistency.test.ts` `test.skip` for add.ts MUST be flipped to LIVE by this task (covers WB-01 SC#4 round-trip for the add path).

    Catalog UAT lockstep: USAGE strings render only via `notifyUsageError`. Audit `tests/architecture/catalog-uat.test.ts` and `docs/output-catalog.md` for any literal `Usage:` body matching marketplace add/remove/autoupdate (Pitfall 7). If any fixture exercises a usage-error path AND contains the old USAGE byte form, update the fixture AND the catalog state IN THE SAME COMMIT as the handler change.
  </action>
  <verify>
    <automated>npm test -- tests/orchestrators/marketplace/add.test.ts tests/orchestrators/marketplace/autoupdate.test.ts tests/edge/handlers/marketplace/add.test.ts tests/edge/handlers/marketplace/autoupdate.test.ts tests/architecture/catalog-uat.test.ts tests/architecture/config-state-consistency.test.ts &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "writeMarketplaceConfigEntry" extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` returns at least one match per file.
    - `grep -n "if (opts.notifications?.mode !== \"orchestrated\")" extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` returns at least one match per file (WR-09 structural guard).
    - `grep -n "extractLocalFlag" extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/autoupdate.ts` returns at least one match per file.
    - `grep -n "\\[--local\\]" extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/autoupdate.ts` returns at least one match per file (USAGE extension).
    - Both orchestrator test files have at least one test asserting `--local` writes to the local file with the base file byte-unchanged (and one for the inverse).
    - autoupdate.ts test asserts FRESH flip writes back; IDEMPOTENT flip leaves the targeted file byte-unchanged (mtime + bytes).
    - `npm run check` exits 0.
    - `tests/architecture/config-state-write-seams.test.ts` exits 0 (allow-list size 1 preserved — saveConfig is still the SOLE writer, called through the new write-back helpers).
  </acceptance_criteria>
  <done>
    add.ts + autoupdate.ts + their edge handlers wire write-back + `--local` + WR-09 + CFG-03 disciplines; tests cover the new paths; catalog UAT byte-equality preserved or updated in lockstep; `npm run check` GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire cascade write-back + --local into marketplace/remove.ts orchestrator + edge handler</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts, extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts, tests/orchestrators/marketplace/remove.test.ts, tests/edge/handlers/marketplace/remove.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts (entirely — current shape uses withStateGuard at line 341; cascade unstage of plugins; existing success and failure arms)
    - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts (lines 305-445 — frozen lock body to mirror)
    - extensions/pi-claude-marketplace/persistence/config-write-back.ts (the new `deleteMarketplaceConfigEntryWithCascade` helper from Plan 01)
    - extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts (entirely — current parsing)
    - .planning/phases/56-write-back-integration-documentation/56-PATTERNS.md §"orchestrators/marketplace/remove.ts (cascade variant)"
    - .planning/phases/56-write-back-integration-documentation/56-RESEARCH.md §"Pitfall 4" (cascade-incomplete remove)
  </read_first>
  <behavior>
    Orchestrator remove.ts:
    - Add `local?: boolean` to opts.
    - Convert withStateGuard to withLockedStateTransaction + explicit tx.save().
    - Target-path selection + loadConfig + CFG-03 abort identical to add.ts pattern.
    - On SUCCESS arm (marketplace existed AND was removed): `if (opts.notifications?.mode !== "orchestrated") { await deleteMarketplaceConfigEntryWithCascade(current, targetConfigPath, locations.scopeRoot, name); }`. The cascade is centralized in the helper (Pitfall 4 closed structurally).
    - On NOT-RECORDED arm (marketplace did not exist; Phase 47 ATTR-06 `not added` path): NO write-back (nothing to delete from config that could exist — leave the file alone).
    - On FAILED arm: NO write-back.
    - Mirror the WR-09 orchestrated-mode skip discipline.

    Edge handler remove.ts:
    - USAGE gains `[--local]`.
    - extractLocalFlag → residualArgs → existing parser.
    - Forward `local: true` when set.

    Tests:
    - Cascade correctness: fixture with `marketplaces["mp1"]`, `plugins["foo@mp1"]`, `plugins["bar@mp2"]`. After `marketplace remove mp1`, the file contains only `plugins["bar@mp2"]` and no `marketplaces["mp1"]`.
    - --local respects target selection.
    - orchestrated-mode SKIPS write-back.
    - CFG-03 invalid-config aborts.
  </behavior>
  <action>
    Apply the `<behavior>` changes to `remove.ts` and `edge/handlers/marketplace/remove.ts`. Imports for orchestrator: `import { deleteMarketplaceConfigEntryWithCascade } from "../../persistence/config-write-back.ts"; import { loadConfig } from "../../persistence/config-io.ts"; import { withLockedStateTransaction } from "../../transaction/with-state-guard.ts"; import path from "node:path";`. Edge handler imports `extractLocalFlag` from `../shared.ts`. The orchestrator's existing cascade unstage of plugin records (state-side) is left intact — the new config-side cascade is ORTHOGONAL and lives in the helper.

    Extend test files with: cascade correctness assertion, --local target selection (base byte-unchanged when --local), orchestrated-mode SKIP assertion, CFG-03 abort assertion. Use a real tmp scope root.

    Source-comment hygiene: terse `// WB-01 / WR-09 / Pitfall 4: cascade lives in deleteMarketplaceConfigEntryWithCascade` is acceptable; DO NOT add prose explaining the phase number / lineage.

    Catalog UAT lockstep applies if any remove fixture exercises a usage-error path with the old USAGE byte form.
  </action>
  <verify>
    <automated>npm test -- tests/orchestrators/marketplace/remove.test.ts tests/edge/handlers/marketplace/remove.test.ts tests/architecture/catalog-uat.test.ts &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "deleteMarketplaceConfigEntryWithCascade" extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns at least one match.
    - `grep -n "extractLocalFlag" extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts` returns at least one match.
    - `grep -n "\\[--local\\]" extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts` returns at least one match (USAGE extension).
    - Test asserts cascade: after `marketplace remove mp1`, a fixture containing `plugins["foo@mp1"]` and `plugins["bar@mp2"]` retains only the latter.
    - Test asserts WR-09 SKIP and CFG-03 abort.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>
    remove.ts orchestrator wires cascade write-back; remove edge handler accepts `--local`; cascade structurally lives in the helper (Pitfall 4 closed); `npm run check` GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bootstrap composition smoke + plan-level integration smoke (WB-04)</name>
  <files>tests/orchestrators/plugin/bootstrap.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts (entirely — current composition: marketplace add at line 99, setMarketplaceAutoupdate at line 122)
    - .planning/phases/56-write-back-integration-documentation/56-RESEARCH.md §"Bootstrap shape" / Pattern 3 + §"Assumptions Log" A2 (keep composed shape)
    - .planning/phases/56-write-back-integration-documentation/56-PATTERNS.md §"orchestrators/plugin/bootstrap.ts (composed 2-write)"
  </read_first>
  <behavior>
    - Bootstrap source code is NOT modified by this plan. The composed shape (calls addMarketplace then setMarketplaceAutoupdate) means WB-04 is satisfied transitively once Task 1 lands the write-backs in those two orchestrators.
    - A new bootstrap test asserts: after `bootstrapClaudePlugin({ marketplaceName: "mp-a", source: "owner/repo", scope: "user" })`, the targeted `claude-plugins.json` contains `marketplaces["mp-a"] = { source: "owner/repo", autoupdate: true }` (autoupdate is true because bootstrap calls setMarketplaceAutoupdate with enable=true per its current contract).
    - The test confirms TWO writes occur (acceptable per A2; the alternative batched-helper redesign was rejected).
  </behavior>
  <action>
    Extend `tests/orchestrators/plugin/bootstrap.test.ts` with at least one test case named `WB-04: bootstrap records marketplace + autoupdate=true into the config via composed addMarketplace + setMarketplaceAutoupdate writes`. Use a tmp scope root; invoke `bootstrapClaudePlugin` with a minimal options object matching the current signature; read back `claude-plugins.json` from disk; assert the marketplace entry shape exactly. NO change to `bootstrap.ts` source.

    Do NOT add a write-back inside `bootstrap.ts` itself — the composed pattern is the locked decision (A2). Adding a redundant write would be a Pitfall 5 mtime drift on top of the two composed writes.
  </action>
  <verify>
    <automated>npm test -- tests/orchestrators/plugin/bootstrap.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/orchestrators/plugin/bootstrap.test.ts` contains a test referencing `WB-04` in its name.
    - The test asserts the config file ends with `marketplaces[<name>] = { source, autoupdate: true }`.
    - `bootstrap.ts` source is byte-unchanged compared to before this task (`git diff extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts` is empty).
  </acceptance_criteria>
  <done>
    WB-04 covered by a composed-write smoke test; bootstrap source unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → edge handler args | `--local` parsed at edge boundary via extractLocalFlag; unknown flags rejected via notifyUsageError |
| edge handler → orchestrator | `local: boolean` is a typed boolean option; no untrusted string survives to the orchestrator |
| orchestrator → physical config file | `targetConfigPath = opts.local ? configLocalJsonPath : configJsonPath`; ALWAYS one of the two scope-rooted paths; never user-supplied |
| concurrent Pi processes → claude-plugins.json | proper-lockfile per-scope `.state-lock`; withLockedStateTransaction non-re-entrant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-02-01 | Tampering | marketplace orchestrators (orchestrated-mode call writes back to base file, clobbering the user's authored local override) | mitigate | WR-09 structural guard: `if (opts.notifications?.mode !== "orchestrated") { writeBack(...) }` (Phase 55 frozen pattern); structural grep acceptance criterion enforces presence |
| T-56-02-02 | Tampering | marketplace/remove.ts (cascade-incomplete: leaves dangling plugin entries pointing at deleted marketplace) | mitigate | Pitfall 4 closed structurally: the cascade lives in `deleteMarketplaceConfigEntryWithCascade` helper; the orchestrator's only job is to pass the marketplace name — the filter `*@<name>` is centralized |
| T-56-02-03 | Tampering | --local writes leaking to base file on ENOENT-fallback instinct | mitigate | Pitfall 2 closed: `targetConfigPath` selected ONCE before lock; loadConfig's `absent` arm yields an empty starting shape that saveConfig writes BACK to the local path (saveConfig creates the file); no conditional fallback |
| T-56-02-04 | Tampering | autoupdate idempotent-flip mtime drift breaking RECON-05 fixed-point proof | mitigate | Pitfall 5 closed: idempotent arms `return` BEFORE the write-back call; mtime byte-stable; test case asserts mtime + bytes unchanged after idempotent flip |
| T-56-02-05 | Information disclosure | CFG-03 invalid-config message leaks absolute targetConfigPath | mitigate | T-53-02-02 / T-54-02-02 mitigation pattern: surface `path.basename(targetConfigPath)` only; replicate Phase 54 `configBasename` discipline |
| T-56-02-06 | Tampering | Unknown long flag on the edge handler smuggled into the orchestrator | mitigate | extractLocalFlag rejects any `--<unknown>` token with notifyUsageError and returns undefined; handler early-returns; Phase 54 WR-02 frozen pattern |
| T-56-02-SC | Tampering | npm/pip/cargo installs | mitigate | NOT APPLICABLE — Phase 56 installs ZERO new packages (verified RESEARCH §Standard Stack); legitimacy gate skipped per Package Legitimacy Audit |
</threat_model>

<verification>
- `npm test -- tests/orchestrators/marketplace/{add,remove,autoupdate}.test.ts` exits 0.
- `npm test -- tests/edge/handlers/marketplace/{add,remove,autoupdate}.test.ts` exits 0.
- `npm test -- tests/orchestrators/plugin/bootstrap.test.ts` exits 0 (WB-04 composed-write smoke).
- `npm test -- tests/architecture/catalog-uat.test.ts` exits 0 (byte-equality preserved OR fixtures updated in lockstep with handler changes).
- `npm test -- tests/architecture/config-state-consistency.test.ts` exits 0 (the WB-01 SC#4 round-trip `test.skip` for the add path is FLIPPED to live in Task 1).
- `npm test -- tests/architecture/config-state-write-seams.test.ts` exits 0 (saveConfig is still the sole writer; allow-list size 1).
- `npm run check` exits 0 from start to end of plan.
</verification>

<success_criteria>
- Marketplace add/remove/autoupdate orchestrators wire write-back through the Plan 01 helpers with WR-09 + Pitfall 5 (idempotent skip) + Pitfall 2 (--local discipline) + Pitfall 4 (cascade) all structurally guarded.
- Marketplace add/remove/autoupdate edge handlers accept `--local` via the lifted `extractLocalFlag` scanner.
- Bootstrap WB-04 is satisfied by composition (no `bootstrap.ts` source change); a smoke test proves the end-state config matches.
- catalog-uat byte-equality stays GREEN (or fixtures + catalog states updated atomically with handler changes per Pitfall 7).
- All unit + architecture tests GREEN; `npm run check` GREEN at plan close.
</success_criteria>

<output>
Create `.planning/phases/56-write-back-integration-documentation/56-02-SUMMARY.md` when done.
</output>
