---
phase: 56-write-back-integration-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/persistence/config-write-back.ts
  - extensions/pi-claude-marketplace/edge/handlers/shared.ts
  - tests/persistence/config-write-back.test.ts
  - tests/edge/handlers/shared.test.ts
  - tests/architecture/config-state-consistency.test.ts
  - tests/architecture/no-split-01-cast-reads.test.ts
autonomous: true
requirements: [WB-01, WB-02, WB-03, WB-04]
must_haves:
  truths:
    - "A single sanctioned config-write-back module exposes entry-level patch helpers that route through saveConfig."
    - "A single sanctioned --local scanner lives in edge/handlers/shared.ts and is consumable by every mutating edge handler."
    - "Wave 0 RED architecture tests (config-state-consistency, no-split-01-cast-reads) exist and fail with a deterministic placeholder until later plans wire the orchestrators and rewire the cast reads."
    - "saveConfig remains the SOLE writer of claude-plugins.json / claude-plugins.local.json (SPLIT-02 architecture test stays GREEN)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/persistence/config-write-back.ts"
      provides: "writeMarketplaceConfigEntry, writePluginConfigEntry, deleteMarketplaceConfigEntryWithCascade, deletePluginConfigEntry, writeBatchedConfigEntries"
      exports: ["writeMarketplaceConfigEntry", "writePluginConfigEntry", "deleteMarketplaceConfigEntryWithCascade", "deletePluginConfigEntry", "writeBatchedConfigEntries", "BatchedConfigPatch"]
    - path: "extensions/pi-claude-marketplace/edge/handlers/shared.ts"
      provides: "extractLocalFlag canonical scanner"
      exports: ["extractLocalFlag"]
    - path: "tests/architecture/config-state-consistency.test.ts"
      provides: "WB-01 SC#4 round-trip + reconcile no-op gate (RED until Plan 04 lands)"
      contains: "config-state-consistency"
    - path: "tests/architecture/no-split-01-cast-reads.test.ts"
      provides: "SPLIT-01 rewire completion gate (RED until Plan 04 rewires the 7 cast-read sites)"
      contains: "no-split-01-cast-reads"
  key_links:
    - from: "extensions/pi-claude-marketplace/persistence/config-write-back.ts"
      to: "extensions/pi-claude-marketplace/persistence/config-io.ts"
      via: "saveConfig import"
      pattern: "from \"./config-io.ts\""
    - from: "extensions/pi-claude-marketplace/edge/handlers/shared.ts"
      to: "extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts"
      via: "extractLocalFlag re-export or direct delegation"
      pattern: "extractLocalFlag"
---

<objective>
Land the byte-neutral foundation for Phase 56: a shared `persistence/config-write-back.ts` helper module (the entry-level patch surface that wraps `saveConfig`), a shared `edge/handlers/shared.ts::extractLocalFlag` scanner lifted from Phase 54's `enable-disable.ts`, and the two new Wave 0 architecture tests (`config-state-consistency.test.ts` and `no-split-01-cast-reads.test.ts`) that the later plans drive to GREEN.

Purpose: Plans 02 / 03 / 04 must be mechanical wire-ups of frozen helpers. Without this plan, each later plan would duplicate the helper shape and risk drift. The Wave 0 tests are the goal-backward gates that Plan 04 closes the milestone against.
Output: One new persistence helper module, one new edge-handlers cross-cutting helper, one new architecture test (RED, gating Plan 04 round-trip integrity), one new architecture test (RED, gating SPLIT-01 rewire), plus unit tests for the new helpers. No orchestrators or edge handlers wire to them yet — the existing `writeConfigEntry` private function inside `enable-disable.ts` STAYS untouched (Plan 03 migrates the consumer).
</objective>

<commit_discipline>
Run `pre-commit run --files <changed files>` (or `pre-commit run --all-files`) BEFORE every `git commit` for files in this plan. Fix failures, restage, re-run until clean. NEVER use `--no-verify`. NEVER amend after a hook failure (the commit did not happen). All commits in this phase use Conventional Commits; commit messages list the requirement IDs they touch.
</commit_discipline>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-write-back-integration-documentation/56-CONTEXT.md
@.planning/phases/56-write-back-integration-documentation/56-RESEARCH.md
@.planning/phases/56-write-back-integration-documentation/56-PATTERNS.md
@.planning/phases/56-write-back-integration-documentation/56-VALIDATION.md

# Frozen Phase 54 analogs — copy shape verbatim
@extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts
@extensions/pi-claude-marketplace/persistence/config-io.ts
@extensions/pi-claude-marketplace/persistence/config-merge.ts

# SPLIT-02 architecture test — verify (do NOT edit)
@tests/architecture/config-state-write-seams.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared persistence/config-write-back.ts helper module + unit tests</name>
  <files>extensions/pi-claude-marketplace/persistence/config-write-back.ts, tests/persistence/config-write-back.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts (lines 60-300 — frozen writeConfigEntry at 267-289 IS the shape to lift; do NOT delete the existing private function in this plan — Plan 03 migrates the consumer)
    - extensions/pi-claude-marketplace/persistence/config-io.ts (entirely — saveConfig signature, ScopeConfig + MarketplaceConfigEntry + PluginConfigEntry types, assertPathInside write-site enforcement)
    - extensions/pi-claude-marketplace/persistence/config-merge.ts (lines 1-60 — type imports + MergedConfig shape; we DO NOT import this in the new module per Pitfall 1)
    - .planning/phases/56-write-back-integration-documentation/56-RESEARCH.md §"Code Examples" Example 1 (verbatim ready-to-use shape)
    - .planning/phases/56-write-back-integration-documentation/56-PATTERNS.md §"persistence/config-write-back.ts (NEW module)"
  </read_first>
  <behavior>
    - writeMarketplaceConfigEntry(current, targetConfigPath, scopeRoot, marketplace, patch): preserves all existing fields on the entry (unknown forward-compat keys per D-09); preserves all OTHER marketplaces; sets schemaVersion: 1; passes through to saveConfig.
    - writePluginConfigEntry(current, targetConfigPath, scopeRoot, plugin, marketplace, patch): same shape; key is `${plugin}@${marketplace}`; preserves the existing entry's unknown keys.
    - deleteMarketplaceConfigEntryWithCascade(current, targetConfigPath, scopeRoot, marketplace): deletes current.marketplaces[marketplace] AND every key in current.plugins ending in `@${marketplace}` (Pitfall 4); preserves all other entries; saveConfig writes the result.
    - deletePluginConfigEntry(current, targetConfigPath, scopeRoot, plugin, marketplace): deletes one plugin key; preserves all other entries.
    - writeBatchedConfigEntries(current, targetConfigPath, scopeRoot, batch: BatchedConfigPatch): applies N marketplace patches AND N plugin patches in memory; ONE saveConfig call.
    - Unit tests cover: unknown-key preservation on every helper (round-trip with a futureField); cascade-delete correctness on marketplace remove (a different-marketplace plugin entry survives); batched-write correctness (N entries patched in one save).
  </behavior>
  <action>
    Create `extensions/pi-claude-marketplace/persistence/config-write-back.ts` exporting the five helpers + `BatchedConfigPatch` interface listed in `<behavior>`. Mirror the structure from RESEARCH.md §"Code Examples" Example 1 (use it as the literal source-of-truth shape, do not paraphrase). Imports: `{ saveConfig, type ScopeConfig, type MarketplaceConfigEntry, type PluginConfigEntry } from "./config-io.ts"`. NO import of `config-merge.ts`, `mergeScopeConfigs`, or `loadMergedScopeConfig` (Pitfall 1 — these helpers ONLY accept a physical ScopeConfig from `loadConfig`, never a merged view). Each helper builds the patched ScopeConfig with spread + override (preserves D-09 unknown keys), pins `schemaVersion: 1`, and delegates the write to `saveConfig(targetConfigPath, patched, scopeRoot)`. The cascade variant filters `current.plugins` entries whose key ends in `@${marketplace}` BEFORE writing. The batched variant applies every `batch.marketplaces[name]` and `batch.plugins[key]` patch (spread over existing entries) and calls `saveConfig` ONCE.

    Create `tests/persistence/config-write-back.test.ts` with at minimum:
    - `writeMarketplaceConfigEntry preserves unknown forward-compat keys on the entry and at top level`
    - `writeMarketplaceConfigEntry creates the marketplace record when absent`
    - `writePluginConfigEntry preserves unknown keys + flat-key `${plugin}@${marketplace}` shape`
    - `deleteMarketplaceConfigEntryWithCascade removes the marketplace + all plugin keys ending in `@<marketplace>`; same-suffix-different-marketplace entries survive (e.g. `foo@other` is kept when removing `mp1`)`
    - `deletePluginConfigEntry removes exactly one plugin key`
    - `writeBatchedConfigEntries applies N marketplace + N plugin patches in ONE saveConfig call (use a fs-mock counter)`
    - All tests use a tmp scope-root via `node:os.tmpdir()` + `mkdtemp` and call the real saveConfig (no module mocking) so NFR-1 atomic seam is exercised end-to-end. The test file MUST run under `npm test -- tests/persistence/config-write-back.test.ts` in under 5s.
  </action>
  <verify>
    <automated>npm test -- tests/persistence/config-write-back.test.ts &amp;&amp; npm test -- tests/architecture/config-state-write-seams.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `extensions/pi-claude-marketplace/persistence/config-write-back.ts` exists.
    - All 5 named exports (`writeMarketplaceConfigEntry`, `writePluginConfigEntry`, `deleteMarketplaceConfigEntryWithCascade`, `deletePluginConfigEntry`, `writeBatchedConfigEntries`) plus `BatchedConfigPatch` interface are present.
    - `grep -n "mergeScopeConfigs\|loadMergedScopeConfig\|config-merge" extensions/pi-claude-marketplace/persistence/config-write-back.ts` returns no matches (Pitfall 1 structural guard).
    - `grep -n "atomicWriteJson" extensions/pi-claude-marketplace/persistence/config-write-back.ts` returns no matches (saveConfig is the SOLE writer; SPLIT-02 stays GREEN without allow-list edit).
    - All new unit tests pass.
    - `npm test -- tests/architecture/config-state-write-seams.test.ts` stays GREEN with ALLOWED_CONFIG_JSON_WRITERS unchanged at 1 entry (config-io.ts).
  </acceptance_criteria>
  <done>
    New helper module + unit tests committed; SPLIT-02 architecture test GREEN with no allow-list edit; Pitfall 1 import gate clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lift extractLocalFlag to edge/handlers/shared.ts + scanner unit tests</name>
  <files>extensions/pi-claude-marketplace/edge/handlers/shared.ts, tests/edge/handlers/shared.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts (lines 1-115 — the canonical extractLocalFlag at 49-84 plus its in-handler call at line 92)
    - extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts (entirely — existing module conventions; this file is plugin-subtree-specific and DOES NOT host extractLocalFlag today)
    - extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts (entirely — same shape, marketplace-subtree-specific)
    - .planning/phases/56-write-back-integration-documentation/56-RESEARCH.md §"Pattern 2" + Open Question #2 (recommendation: cross-cutting `edge/handlers/shared.ts`, NEW file)
  </read_first>
  <behavior>
    - Position-independent scanner: tokens are split by whitespace; `--local` may appear anywhere in args.
    - Consumes `--scope <value>` as two tokens (downstream parser handles the value).
    - Rejects any unknown `--<flag>` via `notifyUsageError(ctx, { message, usage })` and returns `undefined`.
    - On success returns `{ local: boolean, residualArgs: string }` where residualArgs has `--local` REMOVED (downstream parser sees only positionals + `--scope`).
    - Order-insensitive: `--local first` and `--local last` both work.
    - Empty / whitespace-only args returns `{ local: false, residualArgs: "" }`.
  </behavior>
  <action>
    Create NEW file `extensions/pi-claude-marketplace/edge/handlers/shared.ts` (note: this is a NEW file at the `edge/handlers/` directory root, distinct from the existing `edge/handlers/plugin/shared.ts` and `edge/handlers/marketplace/shared.ts` which remain subtree-specific). Export a single named function `extractLocalFlag(args, ctx, usage)` with the EXACT body from `edge/handlers/plugin/enable-disable.ts:49-84` (lift verbatim per PATTERNS.md §"Pattern 2"). Imports: `{ notifyUsageError } from "../../shared/notify.ts"` and `{ type ExtensionCommandContext } from "../../platform/pi-api.ts"` (resolve the correct relative path against the new file's location). The existing private function inside `enable-disable.ts` is NOT touched in this plan — Plan 03 migrates that handler (along with the 7 others) to import from the shared module in one atomic commit.

    Create `tests/edge/handlers/shared.test.ts` covering:
    - `--local first`: `--local --scope user foo@bar` → `{ local: true, residualArgs: "--scope user foo@bar" }`
    - `--local last`: `--scope user foo@bar --local` → `{ local: true, residualArgs: "--scope user foo@bar" }`
    - `--local middle`: `foo@bar --local --scope user` → `{ local: true, residualArgs: "foo@bar --scope user" }`
    - `no --local`: `foo@bar --scope user` → `{ local: false, residualArgs: "foo@bar --scope user" }`
    - `empty args`: `""` → `{ local: false, residualArgs: "" }`
    - `unknown flag`: `foo@bar --unknown` → calls `notifyUsageError` with the right message + usage, returns `undefined`.
    Use a fake `ctx` that records `ui.notify` calls (mirror Phase 54's edge handler test pattern; do not import the real Pi runtime).
  </action>
  <verify>
    <automated>npm test -- tests/edge/handlers/shared.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `extensions/pi-claude-marketplace/edge/handlers/shared.ts` exists with exactly one named export `extractLocalFlag`.
    - The body byte-matches the source at `edge/handlers/plugin/enable-disable.ts:49-84` modulo identifier renames and import-path adjustments (verified by reading both functions side-by-side).
    - All 6 scanner test cases pass.
    - The existing `edge/handlers/plugin/enable-disable.ts:49-84` function is UNCHANGED (Plan 03 owns the migration).
  </acceptance_criteria>
  <done>
    New shared scanner committed; existing Phase 54 handler unchanged; 6 scanner test cases GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 RED architecture tests (config-state-consistency + no-split-01-cast-reads)</name>
  <files>tests/architecture/config-state-consistency.test.ts, tests/architecture/no-split-01-cast-reads.test.ts</files>
  <read_first>
    - tests/architecture/config-state-write-seams.test.ts (entirely — STRUCTURAL analog for the test-file shape: ReadonlySet allow-lists, file-tree walk, regex-driven offender detection, "exactly N" sibling assertion)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts (planReconcile signature; emptyReconcilePlan factory; ReconcilePlan shape — needed to write the no-op assertion)
    - extensions/pi-claude-marketplace/persistence/config-merge.ts (mergeScopeConfigs signature)
    - .planning/phases/56-write-back-integration-documentation/56-RESEARCH.md §"Code Examples" Example 3 (round-trip test shape)
    - .planning/phases/56-write-back-integration-documentation/56-PATTERNS.md §"tests/architecture/config-state-consistency.test.ts (NEW)"
    - extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts (line 68 region — SPLIT-01 cast pattern: `(record as unknown as Record<string, unknown>).autoupdate`)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts (lines 68 + 122 — 2 SPLIT-01 sites)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts (line 455 — SPLIT-01 site)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts (lines 430 + 452 — 2 SPLIT-01 sites)
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts (line 558 — SPLIT-01 site)
    - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts (line 240 — SPLIT-01 site)
  </read_first>
  <behavior>
    config-state-consistency.test.ts:
    - At least one test case scaffolded as `test.skip(...)` (Plan 02/03/04 flip these to live tests as they wire write-back). The skipped test MUST import `planReconcile`, `mergeScopeConfigs`, `emptyReconcilePlan` to keep import-chain validity GREEN under typecheck.
    - One LIVE smoke test that imports the new `config-write-back.ts` helpers + `planReconcile` and asserts: after `writeMarketplaceConfigEntry(emptyConfig, ..., { source: "owner/repo" })`, reading the file back and running `planReconcile(mergeScopeConfigs(readBack, {}), emptyState, "user")` produces an EMPTY plan modulo the one declared marketplace appearing in `marketplacesToAdd` (because state is empty). Document the asymmetry inline: the FULL round-trip-after-mutating-command no-op proof requires Plan 04 to land the orchestrator wire-up.

    no-split-01-cast-reads.test.ts:
    - Walks the codebase under `extensions/pi-claude-marketplace/orchestrators/` searching for the literal cast pattern `as unknown as Record<string, unknown>).autoupdate` (regex: `/as unknown as Record<string,\s*unknown>\)\.autoupdate/`).
    - Maintains an ALLOW-LIST `ALLOWED_SPLIT_01_AUTOUPDATE_CAST_FILES: ReadonlySet<string>` initially populated with the 6 known cast-read files (relative paths from repo root): `extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts`, `extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts`, `extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts`, `extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts`, `extensions/pi-claude-marketplace/orchestrators/plugin/list.ts`, `extensions/pi-claude-marketplace/orchestrators/plugin/info.ts`.
    - Asserts every match's file is in the allow-list (test PASSES at this plan's commit; serves as a tracked baseline).
    - Sibling test asserts `ALLOWED_SPLIT_01_AUTOUPDATE_CAST_FILES.size === 6` (silent-widening guard mirroring the write-seams test pattern).
    - Plan 04 SHRINKS the allow-list to size 0 as each cast site is rewired; the "exactly 6" sibling assertion forces a deliberate edit per rewire.
  </behavior>
  <action>
    Create `tests/architecture/config-state-consistency.test.ts` mirroring the structural shape of `tests/architecture/config-state-write-seams.test.ts` (top-of-file rationale comment block, imports, test cases). The file uses `node:test` and `node:assert/strict`. Implement the LIVE smoke test described in `<behavior>` plus one `test.skip("WB-01 SC#4: after a mutating command, reconcile is a no-op AND unknown keys survive", ...)` whose body imports `addMarketplace` from `orchestrators/marketplace/add.ts` (the import alone proves the export exists; the body is the Plan 02/04 target). Skipped tests MUST still type-check and import-chain-validate under `npm run check`.

    Create `tests/architecture/no-split-01-cast-reads.test.ts`. Top-of-file comment block names the 6 baseline sites, the rewire target (`loadMergedScopeConfig(loc).merged.marketplaces[name]?.entry.autoupdate`), and the "exactly N" silent-widening guard rationale. Walk the orchestrators directory recursively (use the file-walk pattern from `config-state-write-seams.test.ts:106-132`); for every file matching the regex, assert the file is in `ALLOWED_SPLIT_01_AUTOUPDATE_CAST_FILES`. Sibling test asserts the allow-list size === 6.

    BOTH tests must PASS at commit (the live consistency smoke + the SPLIT-01 baseline are GREEN); the `test.skip` flips to live in Plan 04.
  </action>
  <verify>
    <automated>npm test -- tests/architecture/config-state-consistency.test.ts tests/architecture/no-split-01-cast-reads.test.ts &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `tests/architecture/config-state-consistency.test.ts` exists, has at least 1 LIVE passing test + 1 `test.skip` placeholder; type-checks under `npm run check`.
    - `tests/architecture/no-split-01-cast-reads.test.ts` exists; `ALLOWED_SPLIT_01_AUTOUPDATE_CAST_FILES.size === 6`; walker test PASSES (matches all 6 files, none extra).
    - `grep -c "as unknown as Record<.*unknown>).autoupdate" extensions/pi-claude-marketplace/orchestrators/marketplace/*.ts extensions/pi-claude-marketplace/orchestrators/plugin/*.ts | grep -v '^#' | awk -F: '{ s += $2 } END { print s }'` reports the same number of matches the test counts (cross-check the regex against ground truth).
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>
    Two new architecture tests committed; both GREEN; `npm run check` baseline preserved from Phase 55 close (≥1709 unit + 10 integration + the new tests).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → command args | Untrusted `--local` and other flags parsed by extractLocalFlag |
| caller → saveConfig | ScopeConfig payload validated by CONFIG_VALIDATOR.Check inside saveConfig (caller-bug guard) |
| in-memory config → disk | Path containment via assertPathInside(scopeRoot, filePath, ...) inside saveConfig |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01-01 | Tampering | config-write-back.ts (caller passes attacker-controlled targetConfigPath) | mitigate | targetConfigPath is ALWAYS derived from `ScopedLocations.configJsonPath` / `configLocalJsonPath` in callers (later plans); the helper itself trusts the input; defense-in-depth is `saveConfig`'s `assertPathInside(scopeRoot, filePath, "saveConfig")` which throws PathContainmentError before the disk touch (NFR-10 write-site enforcement) |
| T-56-01-02 | Tampering | config-write-back.ts (helper accidentally serializes a MergedConfig view) | mitigate | Pitfall 1 structural guard: NO import of `config-merge.ts` / `mergeScopeConfigs` / `loadMergedScopeConfig` in the new module (grep-verified in acceptance_criteria); the type signature requires `ScopeConfig` (returned by `loadConfig`, never a merged view) |
| T-56-01-03 | Tampering | edge/handlers/shared.ts::extractLocalFlag (attacker injects unknown long flag) | mitigate | Unknown `--<flag>` triggers `notifyUsageError` and returns `undefined`; downstream parser never receives unknown flags (Phase 54 WR-02 frozen pattern) |
| T-56-01-04 | Information disclosure | config-write-back.ts (error surfaces leak absolute config path) | accept | Phase 56 helper layer surfaces PathContainmentError → propagates to orchestrator; orchestrators in Plans 02/03 are responsible for `path.basename(targetConfigPath)` rendering at the user-visible boundary (T-53-02-02 / T-54-02-02 mitigation pattern, replicated downstream) |
</threat_model>

<verification>
- `npm test -- tests/persistence/config-write-back.test.ts tests/edge/handlers/shared.test.ts tests/architecture/config-state-consistency.test.ts tests/architecture/no-split-01-cast-reads.test.ts` exits 0.
- `npm test -- tests/architecture/config-state-write-seams.test.ts` exits 0 (no allow-list edit; baseline preserved).
- `npm run check` exits 0 (typecheck + lint + format + full unit + integration).
- Baseline unit-test count strictly INCREASES from the Phase 55 close baseline (≥1709 → ≥1709 + new tests in this plan).
</verification>

<success_criteria>
- New persistence helper module exists, is consumable, and has unit-test coverage.
- New shared edge scanner exists, lifted verbatim from Phase 54.
- Two Wave 0 architecture tests exist and are GREEN at this plan's commit; both serve as goal-backward gates that Plan 04 closes the milestone against.
- saveConfig remains the SOLE config writer (SPLIT-02 architecture test stays GREEN with allow-list size 1).
- Phase 54 enable-disable.ts handler and orchestrator are UNCHANGED (Plan 03 migrates them in lockstep with the other 7 handlers).
</success_criteria>

<output>
Create `.planning/phases/56-write-back-integration-documentation/56-01-SUMMARY.md` when done.
</output>
