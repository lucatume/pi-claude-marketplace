---
phase: 56-write-back-integration-documentation
plan: 03
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/update.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/orchestrators/plugin/uninstall.test.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/orchestrators/plugin/update.test.ts
  - tests/edge/handlers/plugin/install.test.ts
  - tests/edge/handlers/plugin/uninstall.test.ts
  - tests/edge/handlers/plugin/reinstall.test.ts
  - tests/edge/handlers/plugin/update.test.ts
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
autonomous: true
requirements: [WB-01, WB-02]
must_haves:
  truths:
    - "A user running `install <plugin>@<marketplace> [--scope] [--local]` records the new plugin entry into the targeted physical config file under the scope lock."
    - "A user running `uninstall <plugin>@<marketplace> [--scope] [--local]` deletes the plugin entry from the targeted config file."
    - "A user running `reinstall <plugin>@<marketplace> [--scope] [--local]` and `update <plugin>@<marketplace> [--scope] [--local]` write back ONLY when the patched entry differs from the existing entry (deep-equal short-circuit per A7) — otherwise NO config write (RECON-05 byte-stable convergence)."
    - "Every orchestrated-mode call (notifications.mode === 'orchestrated') SKIPS write-back across all 4 orchestrators (WR-09)."
    - "Every CFG-03 invalid-config arm aborts the command with a basename-only message and SKIPS state save + config write."
    - "The Phase 54 enable-disable.ts orchestrator MIGRATES from its private writeConfigEntry function to the shared writePluginConfigEntry helper; the edge handler MIGRATES from its private extractLocalFlag to the shared one — both as a byte-neutral consolidation."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      provides: "writePluginConfigEntry call on success; --local + WR-09 + CFG-03 disciplines"
      contains: "writePluginConfigEntry"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts"
      provides: "deletePluginConfigEntry call on success; --local + WR-09 + CFG-03 disciplines"
      contains: "deletePluginConfigEntry"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts"
      provides: "deep-equal short-circuited writePluginConfigEntry call on success"
      contains: "writePluginConfigEntry"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/update.ts"
      provides: "deep-equal short-circuited writePluginConfigEntry call on success"
      contains: "writePluginConfigEntry"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts"
      provides: "migration from private writeConfigEntry to shared writePluginConfigEntry helper"
      contains: "writePluginConfigEntry"
    - path: "extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts"
      provides: "migration from private extractLocalFlag to shared scanner import"
      contains: "from \"../shared.ts\""
    - path: "extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts"
      provides: "[--local] usage extension + shared scanner integration"
      contains: "extractLocalFlag"
    - path: "extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts"
      provides: "[--local] usage extension + shared scanner integration"
      contains: "extractLocalFlag"
    - path: "extensions/pi-claude-marketplace/edge/handlers/plugin/reinstall.ts"
      provides: "[--local] usage extension + shared scanner integration"
      contains: "extractLocalFlag"
    - path: "extensions/pi-claude-marketplace/edge/handlers/plugin/update.ts"
      provides: "[--local] usage extension + shared scanner integration"
      contains: "extractLocalFlag"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/{install,uninstall,reinstall,update,enable-disable}.ts"
      to: "extensions/pi-claude-marketplace/persistence/config-write-back.ts"
      via: "named-import write/delete helpers"
      pattern: "config-write-back"
    - from: "extensions/pi-claude-marketplace/edge/handlers/plugin/{install,uninstall,reinstall,update,enable-disable}.ts"
      to: "extensions/pi-claude-marketplace/edge/handlers/shared.ts"
      via: "extractLocalFlag import"
      pattern: "from \"../shared.ts\""
---

<objective>
Wire write-back + `--local` into the four mutating plugin orchestrators (`install`, `uninstall`, `reinstall`, `update`) and their edge handlers, and migrate the Phase 54 `enable-disable.ts` orchestrator + edge handler from their private helpers to the shared ones from Plan 01 (`writePluginConfigEntry` + `extractLocalFlag`). For `reinstall` and `update`, gate the write-back on a deep-equal short-circuit so RECON-05 mtime-stability holds when the patch is a near-no-op (per RESEARCH Open Question #1 / A7).

Purpose: Close the plugin side of WB-01 / WB-02. Plan 02 (marketplace orchestrators) runs in parallel in Wave 2 — different file sets, no overlap. Plan 04 closes import (WB-03), the SPLIT-01 read-path rewire, the README (CFG-04), and the milestone GREEN gate.
Output: Four plugin orchestrators with write-back disciplined per Phase 54 frozen patterns, four plugin edge handlers gaining `--local`, the Phase 54 `enable-disable.ts` pair migrated to the shared helpers (private `writeConfigEntry` and `extractLocalFlag` removed in lockstep), tests + catalog/UAT updates where Usage fixtures matter.
</objective>

<commit_discipline>
Run `pre-commit run --files <changed files>` BEFORE every `git commit`. Fix failures, restage, re-run. NEVER `--no-verify`. NEVER amend after a hook failure. Catalog/UAT byte fixtures change in lockstep with handler USAGE strings (Pitfall 7) — land BOTH in the SAME commit.
</commit_discipline>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-write-back-integration-documentation/56-CONTEXT.md
@.planning/phases/56-write-back-integration-documentation/56-RESEARCH.md
@.planning/phases/56-write-back-integration-documentation/56-PATTERNS.md
@.planning/phases/56-write-back-integration-documentation/56-VALIDATION.md
@.planning/phases/56-write-back-integration-documentation/56-01-SUMMARY.md

# Frozen Phase 54 analogs (THE shape to mirror) and the migration target
@extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts

# Files to modify
@extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/reinstall.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/update.ts

# New helpers from Plan 01
@extensions/pi-claude-marketplace/persistence/config-write-back.ts
@extensions/pi-claude-marketplace/edge/handlers/shared.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: install.ts + uninstall.ts orchestrators + edge handlers wire write-back + --local + WR-09 + CFG-03</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts, tests/orchestrators/plugin/install.test.ts, tests/orchestrators/plugin/uninstall.test.ts, tests/edge/handlers/plugin/install.test.ts, tests/edge/handlers/plugin/uninstall.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts (lines 305-445 — the frozen WR-01/WR-09 lock-body discipline)
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (entirely — current shape uses withStateGuard at line 813; orchestrated-mode option already present from Phase 19/47; success arm where state.plugins gets the record)
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts (entirely — current shape uses withStateGuard at line 311; success arm; alreadyGone / marketplaceAbsent not-recorded arms)
    - extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts (entirely — current parser + scope handling)
    - extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts (entirely — current parser)
    - .planning/phases/56-write-back-integration-documentation/56-PATTERNS.md sections on plugin orchestrator wire-up
    - .planning/phases/56-write-back-integration-documentation/56-RESEARCH.md Pitfall 3 (WR-09) + Pitfall 5 (idempotent skip) + Pitfall 2 (--local discipline)
  </read_first>
  <behavior>
    install.ts:
    - Add `local?: boolean` to opts.
    - Inside the existing top-level withStateGuard closure (or convert to withLockedStateTransaction + explicit tx.save — option 1 mirrors enable-disable exactly; preferred), add target-path selection + loadConfig before the success commit.
    - On SUCCESS arm (plugin record is written to state.plugins): when `opts.notifications?.mode !== "orchestrated"`, call writePluginConfigEntry(current, targetConfigPath, locations.scopeRoot, plugin, marketplace, {}). The patch is empty {} because the canonical plugin shape today is { enabled?: boolean } and the default for a fresh install is "declared, no explicit enabled flag" — preserves D-04 (consume-time default). DO NOT set enabled: true explicitly.
    - FAILED / not-recorded arms (marketplace-not-added, version conflict): NO write-back.
    - CFG-03 invalid-config: abort with structured (failed) row containing path.basename(targetConfigPath).

    uninstall.ts:
    - Add `local?: boolean` to opts.
    - On SUCCESS arm (plugin record removed from state.plugins): when standalone, call deletePluginConfigEntry(current, targetConfigPath, locations.scopeRoot, plugin, marketplace).
    - ALREADY-GONE arm (Phase 47 ATTR-04 loud): NO write-back. Document inline with a terse `// WB-01 / Pitfall 5: uninstall alreadyGone leaves config untouched; planReconcile surfaces the declared-but-missing on next load`.
    - MARKETPLACE-ABSENT / FAILED arms: NO write-back.

    Edge handlers install.ts + uninstall.ts:
    - USAGE strings gain `[--local]`.
    - extractLocalFlag from `edge/handlers/shared.ts` called before existing parser.
    - Forward local: true to orchestrator when set.

    Tests (extend existing):
    - install: --local writes to local file; base byte-unchanged. orchestrated-mode SKIPS. FAILED (marketplace-not-added) does NOT write. CFG-03 abort.
    - uninstall: success deletes the plugin key from the targeted file; ALREADY-GONE arm leaves config byte-unchanged. orchestrated-mode SKIPS.
    - Edge handlers: --local at any position; unknown flag triggers usage error; usage string contains [--local].
  </behavior>
  <action>
    Apply behavior changes to the four source files and extend the four test files. Orchestrator imports add: writePluginConfigEntry / deletePluginConfigEntry from persistence/config-write-back.ts, loadConfig from persistence/config-io.ts, withLockedStateTransaction from transaction/with-state-guard.ts, path from node:path. Edge handler imports add: extractLocalFlag from ../shared.ts.

    Watch existing install.ts complexity: install.ts has a withStateGuard at line 813 for the simple/single-plugin success path plus more involved flows. Wire the write-back at the OUTER success arm (the one that sets state.plugins[key] = record) — NOT inside any sub-routine. If the outer arm cannot easily host the write-back without major restructuring, document inline why and route through the existing post-success seam. The MUST is: standalone-mode success runs exactly one writePluginConfigEntry call; orchestrated-mode success runs zero.

    Source-comment hygiene: terse `// WB-01 / WR-09: orchestrated mode is config-driven, skip write-back` style; DO NOT add lineage / phase-number narrative.

    Catalog UAT lockstep applies if any plugin install/uninstall fixture exercises a usage-error path with old USAGE bytes.
  </action>
  <verify>
    <automated>npm test -- tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/uninstall.test.ts tests/edge/handlers/plugin/install.test.ts tests/edge/handlers/plugin/uninstall.test.ts tests/architecture/catalog-uat.test.ts &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "writePluginConfigEntry" extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` returns at least one match.
    - `grep -n "deletePluginConfigEntry" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns at least one match.
    - `grep -n "opts.notifications?.mode !== \"orchestrated\"" extensions/pi-claude-marketplace/orchestrators/plugin/install.ts extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns at least one match per file (WR-09 structural guard).
    - `grep -n "extractLocalFlag" extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts` returns at least one match per file.
    - `grep -n "\\[--local\\]" extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts` returns at least one match per file.
    - Test files contain at least one test per orchestrator named for --local target selection (base byte-unchanged when --local; local byte-unchanged when not --local).
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>
    install + uninstall orchestrators + edge handlers wire write-back, --local, WR-09, CFG-03 disciplines; tests cover the new paths; `npm run check` GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reinstall.ts + update.ts orchestrators + edge handlers wire deep-equal short-circuited write-back + --local + CFG-03</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/reinstall.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/update.ts, tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/plugin/update.test.ts, tests/edge/handlers/plugin/reinstall.test.ts, tests/edge/handlers/plugin/update.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts (entirely — current shape uses withLockedStateTransaction at line 205; success / no-op classification)
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts (entirely — current shape uses withStateGuard at lines 856 + 925; preflight + commit-phase; classification of changed-vs-unchanged update)
    - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts (lines 305-445 — frozen lock-body discipline)
    - extensions/pi-claude-marketplace/edge/handlers/plugin/reinstall.ts + update.ts (entirely)
    - .planning/phases/56-write-back-integration-documentation/56-RESEARCH.md Open Question #1 + Assumptions A5 + A7 (deep-equal short-circuit decision)
    - .planning/phases/56-write-back-integration-documentation/56-PATTERNS.md sections on reinstall/update (partial match — write semantics TBD by design; this plan locks them via deep-equal short-circuit)
  </read_first>
  <behavior>
    reinstall.ts:
    - Add `local?: boolean` to opts.
    - On SUCCESS arm (artifacts re-staged + state record updated): when standalone, build the patched plugin entry shape that would result from writePluginConfigEntry(current, ..., plugin, marketplace, {}). DEEP-EQUAL compare it against the existing entry at current.plugins[`${plugin}@${marketplace}`] using node:assert/strict's deepEqual semantics OR a hand-rolled deep-equal walk (entries are simple JSON objects). If equal → SKIP the write entirely (RECON-05 mtime-stability). If different → call writePluginConfigEntry.
    - FAILED / not-recorded arms: NO write-back.
    - Document inline: `// WB-01 / A7 / Pitfall 5: deep-equal short-circuit preserves RECON-05 mtime invariant`.

    update.ts:
    - Same deep-equal short-circuit shape.
    - update.ts has TWO withStateGuard closures (preflight at 856, commit-phase at 925). The write-back lives in the COMMIT-PHASE closure on the SUCCESS arm, mirroring the existing commit-phase mutation point.
    - On CHANGED success (version actually shifted): write-back fires (the patch is `{}` because the plugin entry shape today carries no version field — but the deep-equal compare still works against the existing entry).
    - On UP-TO-DATE / SKIPPED arms: NO write-back.
    - On FAILED arms: NO write-back.

    Edge handlers reinstall.ts + update.ts:
    - USAGE strings gain `[--local]`.
    - extractLocalFlag from `edge/handlers/shared.ts` called before existing parser.
    - Forward local: true to orchestrator when set.

    Tests (extend existing):
    - reinstall: success with EQUAL existing entry leaves config byte-unchanged (mtime + bytes assertion). success with DIFFERENT existing entry (e.g. unknown forward-compat key gets preserved-and-restated) writes back. orchestrated-mode SKIPS regardless. --local target selection.
    - update: changed success writes back; up-to-date does NOT write back; orchestrated-mode SKIPS regardless.
    - Edge handlers: --local at any position; unknown flag triggers usage error; usage contains [--local].
  </behavior>
  <action>
    Apply behavior changes. Imports per Task 1 pattern.

    For the deep-equal short-circuit, implement it INSIDE the orchestrator (not in the helper) because the orchestrator has the context to know which fields are "the patched entry" vs "the existing entry". Use `node:assert/strict.deepEqual` wrapped in try/catch (throws when not equal — read its return semantics carefully; for a predicate-style check use a manual deep-equal walk that returns boolean). Recommended: small inline helper `function entriesEqual(a: PluginConfigEntry, b: PluginConfigEntry): boolean` using `JSON.stringify` of stably-sorted keys (entries are flat shallow objects so stable-key JSON compare is sufficient; matches Phase 45 manifestContentKey pattern). Avoid adding a runtime dependency.

    Source-comment hygiene applies. Catalog UAT lockstep if USAGE fixtures exist.
  </action>
  <verify>
    <automated>npm test -- tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/update.test.ts tests/edge/handlers/plugin/reinstall.test.ts tests/edge/handlers/plugin/update.test.ts tests/architecture/catalog-uat.test.ts &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "writePluginConfigEntry" extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` returns at least one match per file.
    - reinstall.test.ts contains a test asserting deep-equal short-circuit (existing entry equals patched entry → file bytes + mtime unchanged after the operation).
    - update.test.ts contains a test asserting up-to-date update does NOT write the config; CHANGED update DOES write the config.
    - `grep -n "extractLocalFlag" extensions/pi-claude-marketplace/edge/handlers/plugin/reinstall.ts extensions/pi-claude-marketplace/edge/handlers/plugin/update.ts` returns at least one match per file.
    - `grep -n "\\[--local\\]" extensions/pi-claude-marketplace/edge/handlers/plugin/reinstall.ts extensions/pi-claude-marketplace/edge/handlers/plugin/update.ts` returns at least one match per file.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>
    reinstall + update orchestrators + edge handlers wire deep-equal short-circuited write-back + --local + CFG-03 + WR-09; RECON-05 invariant preserved for no-op patches; `npm run check` GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate Phase 54 enable-disable.ts (orchestrator + edge handler) to the shared helpers</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts, tests/orchestrators/plugin/enable-disable.test.ts, tests/edge/handlers/plugin/enable-disable.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts (entirely — current private writeConfigEntry at lines 267-289; call site at lines 391-398)
    - extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts (entirely — current private extractLocalFlag at lines 49-84; call sites)
    - extensions/pi-claude-marketplace/persistence/config-write-back.ts (Plan 01 shared writePluginConfigEntry signature)
    - extensions/pi-claude-marketplace/edge/handlers/shared.ts (Plan 01 shared extractLocalFlag signature)
    - .planning/phases/56-write-back-integration-documentation/56-PATTERNS.md section on edge/handlers/shared.ts (NEW shared scanner)
  </read_first>
  <behavior>
    Orchestrator:
    - DELETE the private writeConfigEntry function (lines 267-289).
    - At its single call site (lines 391-398), replace with: `await writePluginConfigEntry(current, targetConfigPath, locations.scopeRoot, plugin, marketplace, { enabled: enable });` — note the patch is `{ enabled: enable }` not the destructured spread of the existing entry. The shared helper's body does the entry preservation (Pitfall 1 / D-09).
    - Import statement: `import { writePluginConfigEntry } from "../../persistence/config-write-back.ts";`.
    - All other source bytes unchanged (the lock body, target-path selection, WR-09 guard, idempotency skip, CFG-03 abort, configBasename info-disclosure mitigation all stay byte-identical).

    Edge handler:
    - DELETE the private extractLocalFlag function (lines 49-84).
    - Replace its call site with `import { extractLocalFlag } from "../shared.ts";` (relative path; edge/handlers/plugin/enable-disable.ts → ../shared.ts is edge/handlers/shared.ts).
    - All other source bytes unchanged.

    Tests:
    - The existing enable-disable test suite MUST stay GREEN with NO test-file edits (the migration is byte-neutral at the public-surface level).
    - If any tests imported the private writeConfigEntry / extractLocalFlag (very unlikely — they're file-local), those tests need a one-line import-path update to the shared module. Otherwise no test edits.
  </behavior>
  <action>
    Surgical migration. Read both files in full first. Confirm the only call sites of the private helpers are inside their own files. Delete the function definitions, replace the call sites, add the import lines. NO behavior change. The point is to remove duplication; the diffs should be small (~30 lines removed from each file, ~1 import line added to each).

    If grep reveals any other consumer of the private helpers (very unlikely; both are file-scoped `function` declarations not `export`s), STOP and update those consumers in the same commit.

    Source-comment hygiene: terse `// Shared scanner; see edge/handlers/shared.ts` is acceptable; DO NOT add migration narrative.
  </action>
  <verify>
    <automated>npm test -- tests/orchestrators/plugin/enable-disable.test.ts tests/edge/handlers/plugin/enable-disable.test.ts tests/architecture/catalog-uat.test.ts &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^function writeConfigEntry\|^async function writeConfigEntry" extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts` returns no matches (the private function is gone).
    - `grep -n "^function extractLocalFlag" extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts` returns no matches.
    - `grep -n "writePluginConfigEntry\|from \"../../persistence/config-write-back" extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts` returns at least one match.
    - `grep -n "extractLocalFlag\|from \"../shared" extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts` returns at least one match.
    - `npm test -- tests/orchestrators/plugin/enable-disable.test.ts` exits 0 with no test source modifications (or with only an import-path adjustment if tests imported the private helpers).
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>
    Phase 54 enable-disable.ts migrated to shared helpers; private writeConfigEntry + private extractLocalFlag deleted; no behavior change; existing test suite GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → edge handler args | --local parsed at edge boundary via shared extractLocalFlag; unknown flags rejected |
| edge handler → orchestrator | local: boolean is a typed option |
| orchestrator → physical config file | targetConfigPath is one of ScopedLocations.configJsonPath / configLocalJsonPath; never user-supplied |
| concurrent Pi processes → claude-plugins.json | proper-lockfile per-scope; withLockedStateTransaction non-re-entrant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-03-01 | Tampering | install/uninstall orchestrators in orchestrated mode write back and clobber user's authored local override | mitigate | WR-09 structural guard: `if (opts.notifications?.mode !== "orchestrated") { writeBack(...) }`; structural grep acceptance criterion enforces presence on each file |
| T-56-03-02 | Tampering | reinstall/update write back on a no-op patch, breaking RECON-05 mtime invariant | mitigate | Deep-equal short-circuit BEFORE writePluginConfigEntry; test case asserts mtime + bytes unchanged after no-op reinstall/update; A7 decision recorded |
| T-56-03-03 | Tampering | --local writes leaking to base on instinctive ENOENT fallback | mitigate | Pitfall 2 closed: targetConfigPath selected ONCE before lock; loadConfig absent arm creates the local file via saveConfig; no conditional fallback |
| T-56-03-04 | Information disclosure | CFG-03 invalid-config arm leaks absolute targetConfigPath | mitigate | path.basename(targetConfigPath) in user-visible messages; replicate Phase 54 configBasename discipline (T-53-02-02 / T-54-02-02) |
| T-56-03-05 | Tampering | Unknown long flag on edge handler smuggled to orchestrator | mitigate | extractLocalFlag rejects unknown --<flag> via notifyUsageError; handler early-returns; Phase 54 WR-02 frozen pattern |
| T-56-03-06 | Tampering | Phase 54 migration to shared helpers introduces a regression vs the private functions | mitigate | Byte-neutral migration: only function deletions + call-site replacement + import line; existing test suite stays GREEN without behavior assertions changing |
| T-56-03-SC | Tampering | npm/pip/cargo installs | mitigate | NOT APPLICABLE — Phase 56 installs zero new packages; legitimacy gate skipped per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `npm test -- tests/orchestrators/plugin/{install,uninstall,reinstall,update,enable-disable}.test.ts` exits 0.
- `npm test -- tests/edge/handlers/plugin/{install,uninstall,reinstall,update,enable-disable}.test.ts` exits 0.
- `npm test -- tests/architecture/catalog-uat.test.ts` exits 0 (byte-equality preserved OR fixtures updated atomically with USAGE changes per Pitfall 7).
- `npm test -- tests/architecture/config-state-write-seams.test.ts` exits 0 (saveConfig sole writer, allow-list size 1).
- `npm run check` exits 0 at the end of every task and at plan close.
</verification>

<success_criteria>
- install, uninstall, reinstall, update orchestrators wire write-back through Plan 01 helpers; WR-09 + Pitfall 5 (idempotent/up-to-date skip) + Pitfall 2 (--local discipline) + deep-equal short-circuit (reinstall/update) + CFG-03 abort all structurally present.
- Plugin edge handlers accept --local via the lifted scanner; USAGE strings extended.
- Phase 54 enable-disable.ts pair migrated to shared helpers; private duplicates deleted; behavior unchanged.
- catalog-uat byte-equality stays GREEN or updated atomically with USAGE changes.
- `npm run check` GREEN throughout.
</success_criteria>

<output>
Create `.planning/phases/56-write-back-integration-documentation/56-03-SUMMARY.md` when done.
</output>
