---
phase: 69-force-path-severity
plan: 04
type: execute
wave: 4
depends_on: [69-03]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
autonomous: true
requirements: [SEV-05, SEV-03]
must_haves:
  truths:
    - "The load-time backfill `force-installed` row carries a factual `{reasons}` brace (the re-resolved dropped-component kinds) when reasons are present (SEV-05, D-69-04)."
    - "A backfill row with no dropped kinds stays brace-less -- byte-identical to today."
    - "The backfill promotion row stays at info severity (benign promotion; not a new degradation) -- confirmed per SEV-03 (RESEARCH A3, D-68-04)."
    - "`installed`, `force-installed`, and `force-upgradable` rows all route reasons through the existing `composeReasons` + `narrowUnsupportedKinds` seam -- no new per-state mechanism (D-69-04)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts"
      provides: "PluginBackfilledOutcome carries the re-resolved unsupported kinds"
      contains: "unsupported"
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts"
      provides: "backfill force-installed row populates reasons via narrowUnsupportedKinds(...)"
      contains: "narrowUnsupportedKinds"
  key_links:
    - from: "reconcile/apply.ts maybeBackfillPlugin re-resolve (resolved.unsupported)"
      to: "PluginBackfilledOutcome.unsupported"
      via: "thread the re-resolved dropped kinds onto the outcome"
      pattern: "unsupported"
    - from: "reconcile/notify.ts plugin-backfilled force-installed arm"
      to: "shared/probe-classifiers.ts narrowUnsupportedKinds"
      via: "reasons: narrowUnsupportedKinds(outcome.unsupported)"
      pattern: "narrowUnsupportedKinds"
---

<objective>
Wire SEV-05 (D-69-04) for the one remaining producer gap: the Phase 68
load-time backfill `force-installed` row currently passes `reasons: []`, so a
re-materialized-but-still-degraded plugin renders a bare `(force-installed)` with
NO factual `{reasons}` brace. The render seam (`composeReasons` via
`forceInstalledRow`) already composes the brace -- the gap is purely
producer-side: `PluginBackfilledOutcome` does not carry the re-resolved
unsupported component kinds.

Thread the re-resolved `unsupported[]` from the offline backfill re-resolve onto
`PluginBackfilledOutcome` and populate the row's `reasons` via the shared
`narrowUnsupportedKinds` helper -- exactly as `install.ts` does. This makes the
`{dropped kinds}` brace render when reasons are present and stay brace-less
(byte-identical) when not.

Also CONFIRM the SEV-03 disposition for this surface (D-68-04 deferred the
backfill row severity to this phase): a backfill PROMOTION is a benign
re-materialization of now-supported components, NOT a new degradation -- it stays
at **info** (RESEARCH A3). The only byte change on this row is the new brace.

Purpose: SEV-05 -- every row carries a factual `{reasons}` brace whenever reasons
are present, including `installed` / `force-installed` / `force-upgradable`.
Output: the backfill outcome threading + the populated brace, landed in lockstep
with the reconcile `docs/output-catalog.md` state + `catalog-uat` fixture.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/69-force-path-severity/69-CONTEXT.md
@.planning/phases/69-force-path-severity/69-RESEARCH.md
@.planning/phases/69-force-path-severity/69-PATTERNS.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry re-resolved unsupported kinds on PluginBackfilledOutcome and populate the brace</name>
  <files>extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts, docs/output-catalog.md, tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts (lines 100-113 `PluginBackfilledOutcome` -- add a field alongside `installable`)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts (lines 985-1042 `maybeBackfillPlugin` -- the `resolved` from `resolveRecordedPluginOffline`; the outcome construction at 1029-1041 where `installable: resolved.state === "installable"` already reads the re-resolved state; `resolved.unsupported` is the dropped-kind list on the `unsupported` arm)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts (lines 498-527 the `plugin-backfilled` case -- the `force-installed` branch at 516-526 currently `reasons: []`, `severity: "info"`)
    - extensions/pi-claude-marketplace/shared/probe-classifiers.ts (lines 146-160 `narrowUnsupportedKinds` -- the SOLE shared per-kind marker helper; `lspServers`->`lsp`, else `unsupported source`, first-wins dedup)
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (line 1411 the analog: `reasons: [...reasons, ...narrowUnsupportedKinds(installCtx.resolved.unsupported)]`)
    - docs/output-catalog.md (the reconcile backfill `(force-installed)` catalog state; force-installed brace form at 418-422)
    - .planning/phases/69-force-path-severity/69-RESEARCH.md (SEV-05 section 321-345; SEV-03 surface (b) 265-271; A3 514)
    - .planning/phases/69-force-path-severity/69-PATTERNS.md (reconcile backfill assignment 201-224; reasons-brace population 58-68)
  </read_first>
  <behavior>
    - A partial backfill (re-resolved `unsupported` non-empty) renders `(force-installed) {<dropped kinds>}` where the kinds come from `narrowUnsupportedKinds(resolved.unsupported)`.
    - A backfill with no dropped kinds (e.g. fully promoted `installed` row, or a degenerate empty set) renders brace-less -- byte-identical to today.
    - The backfill `force-installed` row stays at info severity (benign promotion; SEV-03 A3 / D-68-04).
    - The `lsp` / `unsupported source` token mapping matches the install/list/info surfaces (cross-surface byte parity via the shared helper).
  </behavior>
  <action>
    Add a `readonly unsupported: readonly string[]` field to
    `PluginBackfilledOutcome` (`apply-outcomes.ts:108-113`), carrying the
    re-resolved dropped-component kinds. In `maybeBackfillPlugin`
    (`apply.ts:1029-1041`) set it from the re-resolved plugin --
    `resolved.state === "unsupported" ? resolved.unsupported : []` (the
    `installable` arm already drops to the `(installed)` projection, so its
    unsupported set is empty). In `reconcile/notify.ts` the `force-installed`
    branch of the `plugin-backfilled` case (516-526), replace `reasons: []` with
    `reasons: narrowUnsupportedKinds(outcome.unsupported)` -- mirroring
    `install.ts:1411`; import `narrowUnsupportedKinds` from
    `shared/probe-classifiers.ts`. Do NOT hand-roll a kind->reason map and do NOT
    add a new per-state reasons mechanism (D-69-04 -- reuse the shared seam). Keep
    `severity: "info"` on this row (RESEARCH A3 -- a benign promotion is not a new
    degradation; the SEV-03 newly-degrades warning fires only on the autoupdate
    cascade, handled in 69-03). Anchor with `SEV-05 / D-69-04` comments (and an
    `SEV-03 / A3` note on the info severity rationale). This is BYTE-VISIBLE: in
    the SAME commit update the reconcile backfill state in `docs/output-catalog.md`
    to show the `{dropped kinds}` brace and add/extend the matching `catalog-uat`
    fixture (info severity -> no `expectedSeverity`). Include a fixture proving a
    no-dropped-kinds backfill renders brace-less (byte-identical).
  </action>
  <verify>
    <automated>node --test "tests/orchestrators/reconcile/notify.test.ts" "tests/architecture/catalog-uat.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `PluginBackfilledOutcome` carries `unsupported: readonly string[]`; `maybeBackfillPlugin` sets it from the re-resolved state.
    - The partial-backfill fixture renders `(force-installed) {<dropped kinds>}` via `narrowUnsupportedKinds`; the no-kinds fixture renders brace-less (byte-identical to today).
    - The backfill row stays info (no `expectedSeverity` on the fixture).
    - Producer change + `docs/output-catalog.md` block + `catalog-uat` fixture land in ONE commit (no RED window).
    - `node --test "tests/orchestrators/reconcile/notify.test.ts" "tests/architecture/catalog-uat.test.ts"` green; `npm run check` green.
  </acceptance_criteria>
  <done>The backfill force-installed row carries a factual reasons brace through the shared seam; benign promotion stays info; the byte change is lockstepped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline re-resolve -> PluginBackfilledOutcome -> reconcile notify | Internal; the re-resolved `unsupported[]` is a closed typed component-kind list, mapped through the shared `narrowUnsupportedKinds` to closed-set reasons. No external input, no network (NFR-5 cache-only re-resolve). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-04 | Information disclosure | backfill `{reasons}` brace text | accept | Reasons are closed-set tokens (`lsp` / `unsupported source`) from the shared narrower -- no path internals or attacker-controlled free text reach the brace. Same composer already used on install/list/info rows. |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |

No `high` threats: producer-side reasons threading through an existing render seam; no new network/file-write/auth/untrusted-parse surface.
</threat_model>

<verification>
- `npm run check` green (NFR-6).
- The fully-promoted `(installed)` backfill row and any no-dropped-kinds row remain byte-identical to today.
- `installed` / `force-installed` / `force-upgradable` reasons all flow through `composeReasons` + `narrowUnsupportedKinds` (no new mechanism) -- SEV-05 / D-69-04 satisfied.
- Known flake: tmpdir ENOTEMPTY affects reconcile/load integration tests under PARALLEL runs only -- if hit, re-run `TEST_CONCURRENCY=1 npm test` to confirm green.
</verification>

<success_criteria>
- SEV-05 satisfied: the backfill force-installed row carries a factual `{reasons}` brace when dropped kinds are present, brace-less otherwise; all three force-state rows reuse the shared composer (D-69-04).
- SEV-03 backfill disposition confirmed: benign promotion stays info (A3 / D-68-04).
- Byte-equality gate green in a single lockstep commit.
</success_criteria>

<output>
Create `.planning/phases/69-force-path-severity/69-04-SUMMARY.md` when done.
</output>
