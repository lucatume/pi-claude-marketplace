---
phase: 66-derived-force-state-glyphs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/edge/handlers/tools.ts
  - tests/architecture/notify-closed-set-locks.test.ts
  - tests/architecture/notify-grammar-invariant.test.ts
  - tests/architecture/notify-stamp-coverage.test.ts
  - tests/architecture/catalog-uat.test.ts
  - tests/shared/notify-v2.test.ts
  - docs/output-catalog.md
autonomous: true
requirements: [FSTAT-02, FSTAT-04, FSTAT-06]
must_haves:
  truths:
    - "force-installed renders with the ◉ glyph, distinct from ● installed (FSTAT-02, D-66-03)"
    - "force-upgradable renders with the ● glyph, like the existing upgradable arm (FSTAT-04, D-66-02/03)"
    - "the pending will-install row renders (will force install) when its force modifier is set (FSTAT-06, D-66-04)"
    - "every render/projection/glyph/stamp site handles the two new statuses (assertNever compile-forced)"
    - "npm run check stays green: closed sets bump 20->22 / 15->17, catalog + fixtures + byte tests updated in lockstep (D-66-05)"
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "force-installed/force-upgradable union arms, ICON_FORCE_INSTALLED, render switch arms, will-install force modifier, info-row glyph, PL-4 filter, closed-set tuples"
      contains: "ICON_FORCE_INSTALLED"
    - path: "extensions/pi-claude-marketplace/edge/handlers/tools.ts"
      provides: "projectRowStatus arms mapping force-installed/force-upgradable -> installed"
      contains: "force-installed"
    - path: "docs/output-catalog.md"
      provides: "byte-form rows for force-installed, force-upgradable, will force install"
      contains: "force-installed"
  key_links:
    - from: "shared/notify.ts PLUGIN_STATUSES"
      to: "renderPluginRow switch + pluginInfoStatusGlyph + tools.ts projectRowStatus"
      via: "assertNever exhaustiveness over the closed set"
      pattern: "force-installed"
    - from: "docs/output-catalog.md force rows"
      to: "tests/architecture/catalog-uat.test.ts FIXTURES"
      via: "byte-equality UAT gate (same commit)"
      pattern: "force-installed"
---

<objective>
Establish the notify.ts status vocabulary foundation for derived force-state: add the
`force-installed` (glyph `◉`) and `force-upgradable` (glyph `●`) realized statuses, the
`will force install` render modifier, and thread both new statuses through every
compile-enforced render/projection/glyph/stamp site. Land the closed-set tripwire bumps,
the grammar-invariant widening, the catalog rows, and the byte/fixture tests in ONE
lockstep change so `npm run check` stays green (the Phase 65.1 / commit 5e102920
precedent).

This is the atomic FOUNDATION wave. It makes the new statuses RENDER (not yet derive or
emit from orchestrators) — Wave 2 plans (66-02 list deriver, 66-03 info/success, 66-04
reconcile pending) wire the orchestrators that emit these statuses, and depend on this
vocabulary existing.

Purpose: Without the closed-set members, glyphs, and render arms, no consumer surface can
reference `force-installed`/`force-upgradable` (typecheck error at every assertNever site).
This plan unblocks all of Wave 2.
Output: notify.ts vocabulary extension + tools projection + all coupled architecture/byte
tests + catalog rows, single green commit.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-derived-force-state-glyphs/66-CONTEXT.md
@.planning/phases/66-derived-force-state-glyphs/66-RESEARCH.md
@.planning/phases/66-derived-force-state-glyphs/66-PATTERNS.md
@.planning/phases/66-derived-force-state-glyphs/66-VALIDATION.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: notify.ts status vocabulary + tools projection (source)</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/edge/handlers/tools.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts — STATUS_TOKENS (~198-219), PLUGIN_STATUSES (~371-387), the ICON_* block (~1272-1285) incl. ICON_INSTALLED/ICON_DISABLED and the `●` glyph-sharing doc-comment, the `PluginInstalledMessage`/`PluginUpgradableMessage`/`PluginDisabledMessage` interfaces (~554-674) and `PluginWillInstallMessage` (~740-744), the `PluginNotificationMessage` union (~790-805), the `renderPluginRow` switch incl. `case "upgradable"` (~1879), `case "will install"` (~1888-1898), `case "disabled"` (~1936-1950) and the `assertNever` tail (~1951), `PluginInfoRowBase.status` Extract (~1043), `pluginInfoStatusGlyph` switch (~2704-2718), and the PL-4 description filter (~3168-3182). The "disabled" token (D-54-01) is the master analog touching every one of these sites.
    - extensions/pi-claude-marketplace/edge/handlers/tools.ts — `projectRowStatus` (~159-177), the `case "installed": case "upgradable": return "installed";` arm (~164-166) and the exhaustive `throw` tail (~186-189).
    - .claude/rules/typescript-comments.md — use D-66-NN / FSTAT-NN / NFR-N / SC-N IDs in comments; NEVER GSD phase/plan/wave references.
  </read_first>
  <action>
    In shared/notify.ts: append `"force-installed"` then `"force-upgradable"` to STATUS_TOKENS (20->22) and to PLUGIN_STATUSES (15->17) — append only, after `"disabled"`; `PluginStatus` picks them up via the indexed-access alias. Add `export const ICON_FORCE_INSTALLED = "◉";` (U+25C9) to the ICON block with a D-66-03 / FSTAT-02 comment noting it is distinct from ICON_INSTALLED. Add two message interfaces modeled on `PluginUpgradableMessage` — `PluginForceInstalledMessage` (status `"force-installed"`) and `PluginForceUpgradableMessage` (status `"force-upgradable"`), each carrying `name`, `reasons: readonly ContentReason[]`, optional `version?`/`scope?`/`description?` — and add both arms to the `PluginNotificationMessage` union after `PluginDisabledMessage`. Add a `readonly force?: boolean` modifier to `PluginWillInstallMessage` (D-66-04 / FSTAT-06). In `renderPluginRow`: add `case "force-installed"` rendering with `ICON_FORCE_INSTALLED` and the `(force-installed)` token, and `case "force-upgradable"` rendering with `ICON_INSTALLED` and the `(force-upgradable)` token (mirror the `upgradable` arm), both before the assertNever tail; and branch the existing `case "will install"` arm so `p.force === true` renders `(will force install)` else `(will install)`. Widen the `PluginInfoRowBase.status` Extract to add `"force-installed"` only (force-upgradable is list-only) and add `case "force-installed": return ICON_FORCE_INSTALLED;` to `pluginInfoStatusGlyph`. Add `force-installed` and `force-upgradable` to the PL-4 description-carrying status set. In edge/handlers/tools.ts: add `case "force-installed": case "force-upgradable":` to the existing `return "installed";` arm in `projectRowStatus`. Do NOT add a `will force update` arm — it is vacuous (no reconcile update bucket); document the absence with a brief D-66-05 comment near the will-install arm. Comments use D-66-NN / FSTAT-NN IDs only.
  </action>
  <acceptance_criteria>
    - `grep -c 'force-installed\|force-upgradable' extensions/pi-claude-marketplace/shared/notify.ts` returns a count covering both tuples, both interfaces, both union arms, both render arms, info Extract + glyph, and PL-4 filter.
    - `ICON_FORCE_INSTALLED = "◉"` (U+25C9) is exported and distinct from ICON_INSTALLED.
    - `PluginWillInstallMessage` carries `readonly force?: boolean`; the `will install` render arm emits the literal string `(will force install)` when force is true and `(will install)` otherwise.
    - tools.ts `projectRowStatus` maps both force statuses to `"installed"`.
    - No `will force update` token or arm exists anywhere; a comment documents the vacuity.
    - Comments/titles contain no GSD phase/plan/wave references (per .claude/rules/typescript-comments.md).
    - `npm run typecheck` passes (every assertNever site now has its arm). NOTE: the closed-set lock test is intentionally still RED until Task 2.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && npm run typecheck</automated>
  </verify>
  <done>notify.ts and tools.ts compile with both new statuses fully handled at every exhaustive site; will-install force modifier renders; no will-force-update surface; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lockstep closed-set bumps, grammar/stamp tests, catalog rows + fixtures, byte assertions</name>
  <files>tests/architecture/notify-closed-set-locks.test.ts, tests/architecture/notify-grammar-invariant.test.ts, tests/architecture/notify-stamp-coverage.test.ts, tests/architecture/catalog-uat.test.ts, tests/shared/notify-v2.test.ts, docs/output-catalog.md</files>
  <read_first>
    - tests/architecture/notify-closed-set-locks.test.ts — the exact-length tripwires (REASONS=32 line 30, STATUS_TOKENS=20 line 34, PLUGIN_STATUSES=15 line 38, MARKETPLACE_STATUSES=7 line 42).
    - tests/architecture/notify-grammar-invariant.test.ts — `WILL_TOKEN_RE` (~219-220) and the `will install` fixture (~175-179).
    - tests/architecture/notify-stamp-coverage.test.ts — `TRANSITION_STATUS_LIST` (~55-61, currently 5 members ending in `"disabled"`, `satisfies readonly PluginStatus[]`) and the surrounding stamp invariant explanation.
    - tests/architecture/catalog-uat.test.ts — the `FIXTURES` map and the `installed`/`upgradable` fixture entries; the byte-equality runner that reads docs/output-catalog.md.
    - tests/shared/notify-v2.test.ts — existing render byte-assertions for installed/upgradable/will-install rows.
    - docs/output-catalog.md — the current token/glyph legend rows and the installed/upgradable/will-install example blocks.
  </read_first>
  <action>
    Bump the closed-set tripwires: STATUS_TOKENS 20->22, PLUGIN_STATUSES 15->17; leave MARKETPLACE_STATUSES=7 and REASONS=32 unchanged (D-66-05). Widen `WILL_TOKEN_RE` so the will-token alternation also matches `will force install` (e.g. allow an optional `force ` before `install`), and add a `force: true` will-install fixture asserting the `(will force install)` row, mirroring the existing will-install fixture. Add `"force-installed"` to `TRANSITION_STATUS_LIST` in notify-stamp-coverage (rationale: the install/update success cascade emits `force-installed` as a realized transition with `severity: "info"` + `needsReload: true`, identical to `installed`); do NOT add `force-upgradable` (it is list-inventory only, `needsReload: false`). Add representative byte rows to docs/output-catalog.md for a `force-installed` (◉ ... (force-installed)) row, a `force-upgradable` (● ... (force-upgradable)) row, and a `will force install` pending row, and add the matching `FIXTURES` entries to catalog-uat.test.ts in this SAME change so the byte gate stays green. Add notify-v2 render assertions: `◉ <name> ... (force-installed)` distinct from `● ... (installed)`; `● ... (force-upgradable)`; and `(will force install)` under `force: true`. Test titles use FSTAT-NN / D-66-NN / NFR-N IDs only.
  </action>
  <acceptance_criteria>
    - `node --test "tests/architecture/notify-closed-set-locks.test.ts"` green with STATUS_TOKENS=22, PLUGIN_STATUSES=17, MARKETPLACE_STATUSES=7, REASONS=32.
    - `node --test "tests/architecture/notify-grammar-invariant.test.ts"` green; `WILL_TOKEN_RE` matches `(will force install)`; a force:true fixture asserts the row.
    - `node --test "tests/architecture/notify-stamp-coverage.test.ts"` green with `force-installed` in TRANSITION_STATUS_LIST and `force-upgradable` excluded.
    - `node --test "tests/architecture/catalog-uat.test.ts"` green: every new docs/output-catalog.md force row has a matching FIXTURES entry (byte equality holds).
    - `node --test "tests/shared/notify-v2.test.ts"` green asserting ◉ vs ● distinctness and the `(will force install)` modifier.
    - Test/describe titles contain no GSD phase/plan/wave references.
    - `npm run check` green (TEST_CONCURRENCY=1).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && node --test "tests/architecture/notify-closed-set-locks.test.ts" "tests/architecture/notify-grammar-invariant.test.ts" "tests/architecture/notify-stamp-coverage.test.ts" "tests/architecture/catalog-uat.test.ts" "tests/shared/notify-v2.test.ts"</automated>
  </verify>
  <done>All five coupled test files green; catalog rows + fixtures byte-aligned; closed sets at 22/17/7/32; force-installed in the transition stamp set; npm run check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds no new trust boundary. It extends an internal closed-set vocabulary + renderer; all input (status values) originates from already-validated resolver/persisted state, not external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Information Disclosure | rendered status rows | accept | Rows display only plugin name/version/status already shown today; no new data class is surfaced. No PII, no secrets. |
| T-66-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase (pure internal TS + docs + tests). RESEARCH "Standard Stack" confirms zero `npm install`; Package Legitimacy Gate not applicable. |

NFR-5 (no network on list/info/pending) is unaffected by this plan — it changes no network behavior; the existing `no-orchestrator-network` architecture test continues to guard. No auth/session/access-control/crypto/input-validation surface is touched (per RESEARCH Security Domain).
</threat_model>

<verification>
- `npm run typecheck` green after Task 1 (every assertNever site handled).
- `npm run check` green after Task 2 (TEST_CONCURRENCY=1).
- All 66-01 source + test + catalog changes land in a SINGLE commit (lockstep gate, D-66-05; 65.1 / commit 5e102920 precedent) — do not split the source change from the closed-set/catalog change across commits.
</verification>

<success_criteria>
- STATUS_TOKENS=22, PLUGIN_STATUSES=17 (MARKETPLACE_STATUSES=7, REASONS=32 unchanged).
- `◉` (ICON_FORCE_INSTALLED) renders for force-installed, distinct from `●`; force-upgradable reuses `●`.
- `(will force install)` renders via the will-install force modifier; no `will force update` surface exists.
- Both new statuses handled at every render/projection/glyph/stamp site; `force-installed` in TRANSITION_STATUS_LIST.
- Catalog rows + FIXTURES byte-aligned; `npm run check` green; one lockstep commit.
</success_criteria>

<output>
Create `.planning/phases/66-derived-force-state-glyphs/66-01-SUMMARY.md` when done.
</output>
