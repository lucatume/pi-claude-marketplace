---
phase: 65.1-reload-deferred-will-grammar-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/architecture/notify-will-reload-agreement.test.ts
  - tests/architecture/catalog-uat.test.ts
autonomous: true
requirements: [WILL-02, WILL-04]
must_haves:
  truths:
    - "A WILL-02 agreement test exists asserting the pending will-grammar matches the reload-hint oracle (shouldEmitReloadHint)."
    - "A catalog-uat state renders the marketplace-remove-with-installed-plugins cascade byte form: bare header plus per-plugin (will uninstall) rows."
    - "Both new artifacts pass against the current (pre-retirement) tree so the Wave 1 commit boundary stays green."
  artifacts:
    - path: "tests/architecture/notify-will-reload-agreement.test.ts"
      provides: "WILL-02 cross-surface agreement anchor between pending will-tokens and the reload-hint trailer"
      contains: "WILL-02"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "marketplace-remove-with-installed-plugins byte fixture (WILL-03 cascade target shape)"
      contains: "marketplace-remove-with-installed-plugins"
  key_links:
    - from: "tests/architecture/notify-will-reload-agreement.test.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts shouldEmitReloadHint"
      via: "render realized cascade + assert RELOAD_HINT_TRAILER presence"
      pattern: "shouldEmitReloadHint|reload to pick up changes"
---

<objective>
Land the two genuinely-new, additive test artifacts that anchor the will-grammar
retirement BEFORE the retirement itself: the WILL-02 cross-surface agreement test
and the `marketplace-remove-with-installed-plugins` catalog-uat byte state. Both
are purely additive and MUST pass against the current (pre-retirement) tree so
this plan commits green (NFR-6 / D-65.1-04 atomic-supersession constraint).

Purpose: D-65.1-01 names a verification anchor for WILL-02 — a test asserting the
pending `will`-grammar and the per-command reload-hint discipline agree. Standing
it up first (green-on-arrival) gives Wave 1 a concrete oracle to satisfy and lands
the WILL-03 cascade target byte shape so the retirement only has to reach it.

Output: one new test file + one new catalog-uat state.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-CONTEXT.md
@.planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-RESEARCH.md
@.planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-PATTERNS.md
@.planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the marketplace-remove-with-installed-plugins catalog-uat byte state</name>
  <files>tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - tests/architecture/catalog-uat.test.ts (read the surviving `plugin-pending-uninstall` fixture ~2918-2929 and the `mp-add-plugin-install` fixture ~2905-2917 to match the exact STATE_FIXTURES shape and the byte-assertion mechanics)
    - .planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-PATTERNS.md (section "tests/architecture/catalog-uat.test.ts — byte fixtures", Edit 2)
    - .planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-RESEARCH.md (Research Question 4 + "Existing bare-header + will-uninstall byte form")
    - extensions/pi-claude-marketplace/orchestrators/reconcile/reconcile.messaging.ts (confirm `renderWillUninstall` ~87-88 already emits the `(will uninstall)` row form being asserted)
  </read_first>
  <action>
    Add a NEW catalog-uat state keyed `marketplace-remove-with-installed-plugins`,
    structurally copied from the surviving `plugin-pending-uninstall` fixture. Its
    message is one marketplace block with NO `status` field (renders the bare
    `case undefined:` list-arm header) and a `plugins` array of two
    `{ status: "will uninstall", name: <name> }` child rows (e.g. names `p1`, `p2`)
    under scope `user`. The expected bytes are the bare header `● <mp> [user]`
    followed by `  ○ p1 (will uninstall)` and `  ○ p2 (will uninstall)` rows,
    matching the existing `plugin-pending-uninstall` byte form exactly. This is the
    WILL-03 reload-deferred cascade target shape (D-65.1-03: de-registration is
    immediate, only the plugin-uninstall cascade is reload-deferred). Do NOT add or
    edit the `mp-add-plugin-install` fixture here — that edit belongs to the Wave 1
    retirement plan (65.1-02). Reuse the existing `pi` harness helper
    (`piWithBothLoaded()` or whichever the sibling fixture uses); invent no new
    status token and no new render arm.
  </action>
  <acceptance_criteria>
    - tests/architecture/catalog-uat.test.ts contains a state literally keyed `marketplace-remove-with-installed-plugins`.
    - The new state's message marketplace block has no `status` property and a `plugins` array whose every entry has `status: "will uninstall"`.
    - `node --test "tests/architecture/catalog-uat.test.ts"` exits 0 against the current pre-retirement tree.
    - The `mp-add-plugin-install` fixture is UNCHANGED by this task (still sets `status: "will add"`).
  </acceptance_criteria>
  <verify>
    <automated>node --test "tests/architecture/catalog-uat.test.ts"</automated>
  </verify>
  <done>The new cascade byte state exists and the catalog-uat suite is green on the current tree.</done>
</task>

<task type="auto">
  <name>Task 2: Add the WILL-02 reload-hint agreement test (new isolated file)</name>
  <files>tests/architecture/notify-will-reload-agreement.test.ts</files>
  <read_first>
    - tests/architecture/notify-grammar-invariant.test.ts (read the existing fixture-iteration + render-and-match mechanics and the WILL_VARIANT_FIXTURES / WILL_TOKEN_RE patterns ~163-231 to reuse the assertion style; do NOT edit this file in this plan)
    - extensions/pi-claude-marketplace/shared/notify.ts (read `shouldEmitReloadHint` ~2465-2521 and `RELOAD_HINT_TRAILER` ~2043 — the D-65.1-01 single oracle)
    - .planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-RESEARCH.md (Research Question 1 DEFINITIVE TABLE — the per-action reload-deferred vs immediate ground truth)
    - .planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-PATTERNS.md (section "WILL-02 agreement test")
  </read_first>
  <action>
    Create tests/architecture/notify-will-reload-agreement.test.ts asserting the
    WILL-02 invariant (D-65.1-01): the pending `will`-grammar agrees with the
    per-command reload-hint discipline. For each REALIZED-transition cascade that
    keeps a pending `will` token — plugin install, uninstall, enable, disable, and
    the marketplace-remove plugin-uninstall child cascade — construct the realized
    notification message and assert `shouldEmitReloadHint(message)` returns true
    AND the rendered output contains the `/reload to pick up changes` trailer
    (`RELOAD_HINT_TRAILER`). For the RETIRED actions — a realized marketplace
    `(added)` cascade and a realized header-only marketplace `(removed)` cascade
    with empty plugins — assert `shouldEmitReloadHint` returns false AND the
    rendered output does NOT contain the trailer. Use exported `shouldEmitReloadHint`
    if exported, otherwise assert via the rendered-trailer presence only (Claude's
    discretion on the seam per D-65.1-01). Title every test with the `WILL-02`
    requirement ID; NEVER reference a GSD phase/plan number (comment/test-title
    policy). This file is written to pass on the CURRENT tree because it inspects
    realized-cascade reload behavior, which already matches the oracle — it is the
    anchor the Wave 1 pending-surface retirement must converge to.
  </action>
  <acceptance_criteria>
    - tests/architecture/notify-will-reload-agreement.test.ts exists.
    - Every `test(...)`/`describe(...)` title in the file contains `WILL-02` and contains no `65.1`, `Phase`, or `plan` token.
    - The file asserts the trailer IS present for plugin install/uninstall/enable/disable and the marketplace-remove plugin-uninstall cascade, and IS NOT present for the marketplace add cascade and the empty marketplace-remove cascade.
    - `node --test "tests/architecture/notify-will-reload-agreement.test.ts"` exits 0 against the current pre-retirement tree.
  </acceptance_criteria>
  <verify>
    <automated>node --test "tests/architecture/notify-will-reload-agreement.test.ts"</automated>
  </verify>
  <done>The WILL-02 agreement anchor exists and is green on the current tree; it cites WILL-02 and uses no GSD references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only adds test files. No runtime input, network, auth, file-write, or user-facing surface is introduced or modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65.1-01 | Information Disclosure | byte-exact output contract (pending/cascade rows) | accept | No new attack surface. The output-contract invariant (no token leakage/injection into user-visible output) is preserved and strengthened by the byte-equality catalog-uat runner and the WILL-02 agreement test added here. Pre-existing redactions (`redactAbsolutePaths`, basename-only error subjects) are unchanged. |
| T-65.1-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; code-only test additions on the existing stack. No Package Legitimacy Audit required. |
</threat_model>

<verification>
- `node --test "tests/architecture/catalog-uat.test.ts"` green.
- `node --test "tests/architecture/notify-will-reload-agreement.test.ts"` green.
- `npm test` (unit suite) green at wave-merge.
- Grep confirms no GSD phase/plan reference in the new test file:
  `grep -nE "65\.1|Phase [0-9]|plan [0-9]" tests/architecture/notify-will-reload-agreement.test.ts` returns nothing.
</verification>

<success_criteria>
- The WILL-02 agreement test file and the `marketplace-remove-with-installed-plugins` catalog-uat state both exist and pass on the current tree.
- The plan commits green (no intermediate red), preserving the D-65.1-04 atomic-supersession constraint for the downstream retirement.
- Test titles cite WILL-02 / D-65.1-NN, never a GSD reference.
</success_criteria>

<output>
Create `.planning/phases/65.1-reload-deferred-will-grammar-consistency/65.1-01-SUMMARY.md` when done.
</output>
