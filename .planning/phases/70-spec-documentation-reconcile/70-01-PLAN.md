---
phase: 70-spec-documentation-reconcile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/architecture/catalog-uat.test.ts
  - docs/output-catalog.md
  - docs/messaging-style-guide.md
autonomous: true
requirements: [DOC-02]
must_haves:
  truths:
    - "A no-`--force` install of a structurally `unavailable` plugin renders at error severity (leading summary line fires) with NO `--force` hint trailer."
    - "List-surface `unavailable` rows still render at info (no severity stamp) -- the error stamp is install-failure-only."
    - "catalog-UAT is GREEN; the closed-set lock counts stay 22/17/7 (no new token)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      provides: "composeUnavailableMessage stamps severity:error on the structural unavailable install-failure arm"
      contains: "composeUnavailableMessage"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "failure-structural-unavailable fixture asserts expectedSeverity error"
      contains: "failure-structural-unavailable"
  key_links:
    - from: "tests/architecture/catalog-uat.test.ts"
      to: "docs/output-catalog.md"
      via: "catalog-state: failure-structural-unavailable byte-equality"
      pattern: "failure-structural-unavailable"
---

<objective>
Finalize the SEV-02 residual that Phase 69 deferred (D-70-02): stamp the
no-`--force` install of a structurally `unavailable` plugin at **error** severity
with NO `--force` hint, and reconcile the byte forms (install-orchestrator test,
catalog-UAT fixture, output-catalog block, style-guide legend) in lockstep.

Purpose: Closes the half of SEV-02 left byte-frozen at error-vs-info parity --
a structural install failure must read as an error, not a benign info row, while
the SAME `PluginUnavailableMessage` variant continues to render at info on the
list surface (per-row caller-stamped severity is the discriminator).
Output: install.ts severity stamp + matching test/catalog/style-guide updates.
This phase creates NO new public symbols and NO new closed-set tokens.
</objective>

<artifacts_this_phase_produces>
- Changed behavior: `composeUnavailableMessage` (install.ts) now stamps
  `severity: "error"` unconditionally on the install-failure `unavailable` row;
  `forceHint: true` remains gated on `entityErrorRow.forceable === true`.
- Updated fixture: `failure-structural-unavailable` in catalog-uat now carries
  a row-level `severity: "error"` and a fixture-level `expectedSeverity: "error"`.
- No new symbols, no new tokens, no closed-set count change (22/17/7 unchanged).
</artifacts_this_phase_produces>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/70-spec-documentation-reconcile/70-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stamp error severity on the structural unavailable install-failure arm</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, tests/orchestrators/plugin/install.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (the `composeUnavailableMessage` function ~1506-1527, and `composeInstallFailureMessage` branch 3 ~1580-1588 which is its only caller)
    - tests/orchestrators/plugin/install.test.ts (the `PI-4: non-path source -> V2 unavailable/{unsupported source}` test ~470-512 -- this is the structural-unavailable install-failure proof; lines ~504-507 currently assert `severity === undefined` and line ~510 asserts a rendered string with NO summary line)
    - .planning/phases/70-spec-documentation-reconcile/70-CONTEXT.md (D-70-02)
  </read_first>
  <behavior>
    - The PI-4 structural `{unsupported source}` install failure now stamps the notify second-arg `severity` as `"error"` (was `undefined`/info).
    - The rendered body now leads with the summary line `A plugin operation has failed.` followed by a blank line, then the existing `● mp [project]\n  ⊘ hello (unavailable) {unsupported source}` cascade (same summary line the `failure-unsupported-features` error row already produces).
    - The row carries NO `forceHint` and NO `--force` hint trailer (force cannot help a structural defect).
  </behavior>
  <action>
    In `composeUnavailableMessage`, change the conditional spread so the row ALWAYS
    stamps `severity: "error"` on the install-failure `unavailable` row, and gate
    ONLY `forceHint: true` on `entityErrorRow.forceable === true`. Concretely: split
    the single `...(entityErrorRow.forceable === true && { forceHint: true, severity: "error" })`
    spread into an unconditional `severity: "error" as const` field plus a
    `...(entityErrorRow.forceable === true && { forceHint: true })` spread. Per D-70-02
    this stamps the structural arm at error with no hint; the forceable `unsupported`
    arm keeps both the hint and error. Update the function's doc comment (~1506-1514)
    to drop the "structural `unavailable` arm stays byte-frozen -- no hint, no severity
    stamp" wording and state instead that the structural arm stamps error severity
    (so the leading summary line fires) but carries NO `--force` hint (force cannot
    degrade-install a structural defect); cite D-70-02. SURGICAL CONSTRAINT:
    `composeUnavailableMessage` is the install-failure derivation only -- do NOT touch
    list-surface unavailable rows (they omit `severity` and stay info). Then update the
    `PI-4` test: change the `assert.equal(notifications[0]?.severity, undefined)` to
    `"error"`, update the expected rendered string to prepend `A plugin operation has
    failed.\n\n`, and scrub the test's `unavailable is NOT in the error-severity set`
    comment to reflect the install-failure error stamp (cite D-70-02). Comment/test-title
    policy: use D-70-02 / SEV-02 anchors, never GSD phase/plan numbers.
  </action>
  <acceptance_criteria>
    - `npx node --test tests/orchestrators/plugin/install.test.ts` passes.
    - The PI-4 test asserts `notifications[0]?.severity === "error"` and the expected rendered string begins with `A plugin operation has failed.`.
    - `grep -n "severity: \"error\" as const" extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` shows the unconditional stamp inside `composeUnavailableMessage`.
    - `grep -nE "byte-frozen|no severity stamp" extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` returns no match inside the `composeUnavailableMessage` doc comment.
    - No list-surface unavailable test (e.g. tests/shared/notify-v2.test.ts, tests/orchestrators/plugin/info.test.ts) was modified.
  </acceptance_criteria>
  <verify>
    <automated>npx node --test tests/orchestrators/plugin/install.test.ts</automated>
  </verify>
  <done>The structural unavailable install failure stamps error severity with no `--force` hint; PI-4 test green; list surface untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile catalog-UAT fixture + output-catalog + style-guide for the error stamp</name>
  <files>tests/architecture/catalog-uat.test.ts, docs/output-catalog.md, docs/messaging-style-guide.md</files>
  <read_first>
    - tests/architecture/catalog-uat.test.ts (the `failure-structural-unavailable` fixture ~867-886 -- currently NO `expectedSeverity`; and the adjacent `failure-unsupported-features` fixture ~843-865 which already stamps `severity: "error"` + `expectedSeverity: "error"` as the byte template)
    - docs/output-catalog.md (the two blocks at ~430-453: `failure-unsupported-features` ~432-442 with its leading `A plugin operation has failed.` summary line, and `failure-structural-unavailable` ~444-453)
    - docs/messaging-style-guide.md (the Severity Routing section ~115-142 and the `PluginUnavailableMessage` variant notes ~42, ~64, ~67)
    - .planning/phases/70-spec-documentation-reconcile/70-CONTEXT.md (D-70-02)
  </read_first>
  <action>
    Reconcile the byte forms so catalog-UAT stays GREEN with the new error stamp.
    (1) In catalog-uat `failure-structural-unavailable`: add `severity: "error"` to
    the single plugin row payload (after `reasons`) and add `expectedSeverity: "error"`
    to the fixture (mirroring `failure-unsupported-features`); update the fixture
    comment (~867-868) to drop the "info severity" claim and state the structural
    install failure now stamps error with no `--force` hint (cite D-70-02). (2) In
    docs/output-catalog.md, in the `failure-structural-unavailable` fenced block
    (~448-451), prepend the summary line `A plugin operation has failed.` and a blank
    line above the `● official [user]` cascade (byte-identical to the
    `failure-unsupported-features` block's leading summary); and rewrite the prose
    (~453) to state the row renders at `error` severity (so the leading summary line
    fires) but carries NO `--force` hint trailer (force cannot help a structural
    defect) -- replacing the current "NO ... severity stamp (info, no leading summary
    line)" sentence. (3) In docs/messaging-style-guide.md, add a note (in the Severity
    Routing section or the `PluginUnavailableMessage` variant notes) that an
    install-failure `unavailable` row carries a caller-stamped `severity: "error"`
    (BOTH the force-degradable `--force`-hint arm and the structural arm), while
    list-surface `unavailable` rows omit `severity` and render info; cite D-70-02.
    Do NOT touch the list-surface unavailable fixtures (`single-mp-mixed`,
    `unavailable-single-scope`, the description fixture) -- they stay info. Do NOT
    add or rename any closed-set token (counts stay 22/17/7).
  </action>
  <acceptance_criteria>
    - `npx node --test tests/architecture/catalog-uat.test.ts` passes.
    - `npx node --test tests/architecture/notify-closed-set-locks.test.ts` passes (STATUS_TOKENS 22, PLUGIN_STATUSES 17, MARKETPLACE_STATUSES 7 unchanged).
    - `grep -n "expectedSeverity" tests/architecture/catalog-uat.test.ts` shows the `failure-structural-unavailable` fixture now carries `expectedSeverity: "error"`.
    - The output-catalog `failure-structural-unavailable` fenced block begins with `A plugin operation has failed.`.
    - `grep -nE "info, no leading summary line" docs/output-catalog.md` returns no match.
    - docs/messaging-style-guide.md mentions an install-failure `unavailable` row stamps error severity (grep for `unavailable` near `error` / `install-failure`).
    - `npm run check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx node --test tests/architecture/catalog-uat.test.ts tests/architecture/notify-closed-set-locks.test.ts</automated>
  </verify>
  <done>catalog-UAT GREEN with the structural-unavailable error stamp; output-catalog + style-guide reflect the byte form; closed set unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only escalates a severity LABEL on an already-rendered install-failure row; no new untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Information disclosure | composeUnavailableMessage error row | accept | Reason text is the existing closed-set `Reason`; no path/secret interpolation added (the `--force` hint is suppressed on this arm). |
| T-70-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan -- docs + one-line severity stamp only. |
</threat_model>

<verification>
- `npm run check` exits 0 (typecheck + ESLint + Prettier + tests).
- catalog-UAT GREEN; closed-set lock counts 22/17/7 unchanged.
- A no-`--force` install of a structural `unavailable` plugin renders at error with the leading summary line and NO `--force` hint; list-surface unavailable rows still render info.
</verification>

<success_criteria>
- D-70-02 satisfied: structural unavailable install failure stamps error, no `--force` hint.
- DOC-02 byte forms (catalog + style-guide) reconciled for the error stamp; catalog-UAT GREEN.
- No new tokens; 22/17/7 unchanged.
</success_criteria>

<output>
Create `.planning/phases/70-spec-documentation-reconcile/70-01-SUMMARY.md` when done.
</output>
