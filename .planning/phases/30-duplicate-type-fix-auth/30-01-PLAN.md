---
phase: "30"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/platform/git.ts
autonomous: true
requirements:
  - AUTH-10
must_haves:
  truths:
    - "platform/git.ts exports exactly one GitCredentials type declaration"
    - "npm run check exits 0 with no type errors"
    - "All existing tests remain GREEN with no count change"
  artifacts:
    - path: extensions/pi-claude-marketplace/platform/git.ts
      provides: "Canonical GitCredentials type export for Phase 31+ consumers"
      contains: "export type GitCredentials"
  key_links:
    - from: extensions/pi-claude-marketplace/platform/git.ts
      to: node_modules/isomorphic-git/index.d.ts
      via: "GitCredentials shape matches isomorphic-git GitAuth"
      pattern: "GitCredentials.*username.*password"
---

<objective>
Export a single canonical `GitCredentials` type from `platform/git.ts` so
Phase 31 (`platform/git-credential.ts`) and Phase 33 (auth wiring) can import
it without creating duplication. The AUTH-10 requirement frames this as a
"duplicate removed" prerequisite -- the canonical single export IS the fix.

Purpose: Unblock the auth wiring chain (Phases 31-36). `platform/git.ts` is the
single file that may import isomorphic-git types (D-13 boundary); `GitCredentials`
must originate here.

Output: `GitCredentials` type appended to `platform/git.ts`; `npm run check` stays
GREEN; no functional change to clone/fetch behavior.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export canonical GitCredentials type from platform/git.ts</name>
  <files>extensions/pi-claude-marketplace/platform/git.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/platform/git.ts -- read in full before editing; note
      the existing interface set (CloneOptions, FetchOptions, etc.) and the end of the file
    - node_modules/isomorphic-git/index.d.ts lines 516-529 -- confirm GitAuth shape:
      `{ username?: string; password?: string; headers?: Record<string, string> }`
      and AuthCallback / AuthFailureCallback / AuthSuccessCallback type aliases
  </read_first>
  <action>
Append a single `GitCredentials` export to the bottom of
`extensions/pi-claude-marketplace/platform/git.ts`.

The type alias matches the isomorphic-git `GitAuth` shape (source of truth:
`node_modules/isomorphic-git/index.d.ts` line 516):

```
username?: string
password?: string
headers?: Record<string, string>
```

Add this block after the last existing export in the file:

```
/**
 * Credential shape consumed by isomorphic-git's onAuth / onAuthFailure callbacks.
 * Matches isomorphic-git's GitAuth; re-exported here so Phase 31+
 * consumers import from platform/git.ts (D-13 boundary) rather than
 * directly from isomorphic-git.
 */
export type GitCredentials = {
  username?: string;
  password?: string;
  headers?: Record<string, string>;
};
```

Do NOT import `GitAuth` from isomorphic-git -- the type is re-declared structurally
to keep isomorphic-git an implementation detail behind the D-13 boundary. Structural
equivalence (TypeScript duck-typing) means the isomorphic-git callbacks will accept
`GitCredentials` values without a cast.

Do NOT modify any existing interfaces, function signatures, or import statements.
  </action>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export type GitCredentials" extensions/pi-claude-marketplace/platform/git.ts`
      returns exactly `1`
    - `npm run check` exits 0 (typecheck + lint + format + tests all pass)
    - `grep -c "GitCredentials" extensions/pi-claude-marketplace/platform/git.ts`
      returns exactly `1` (one declaration, zero duplicates)
    - No existing test file references `GitCredentials` (the type has no consumers
      yet; Phase 31 adds them)
    - Test count in `npm run check` output is identical to the pre-edit baseline
      (no tests added or removed)
  </acceptance_criteria>
  <done>
    `platform/git.ts` exports exactly one `GitCredentials` type; `npm run check`
    exits 0; no existing behavior or test count changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| platform/git.ts → isomorphic-git | Type-only boundary; no credential values flow at this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | GitCredentials type shape | accept | Type-only export in this phase; no runtime credential handling yet. Actual credential security enforced in Phase 31 (AUTH-09). |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; type-only change. |
</threat_model>

<verification>
1. `grep -c "export type GitCredentials" extensions/pi-claude-marketplace/platform/git.ts`
   must return `1`
2. `npm run check` must exit 0
3. Test count unchanged from Phase 29 baseline (1168 tests)
</verification>

<success_criteria>
- `platform/git.ts` has exactly one `GitCredentials` export
- `npm run check` exits 0
- No functional change to clone/fetch: all existing tests GREEN, count unchanged
- Phase 31 can `import type { GitCredentials } from "../platform/git.js"` without
  TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/30-duplicate-type-fix-auth/30-01-SUMMARY.md` when done
</output>
