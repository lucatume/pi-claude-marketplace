---
phase: 35-orchestrator-call-sites-output-catalog-auth
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/helpers/git-mock.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
tags:
  - auth
  - test-helper-widening
  - wave-zero-prep
must_haves:
  truths:
    - "tests/helpers/git-mock.ts's `state.cloneCalls` element type includes `auth?: GitAuthBundle` so Plan 35-01 and Plan 35-02 tests can read the auth field off the recorded calls"
    - "tests/helpers/git-mock.ts's `state.fetchCalls` element type includes `auth?: GitAuthBundle` for the same reason on the fetch side"
    - "The helper's runtime behavior is byte-unchanged -- it already pushes `{ ...opts }` so the auth field is already captured at runtime; only the TypeScript type declaration changes"
    - "All existing consumers of `makeMockGitOps` compile without modification (the widening is additive)"
    - "npm run check exits 0"
  artifacts:
    - path: "tests/helpers/git-mock.ts"
      provides: "Widened `cloneCalls` and `fetchCalls` element types including `auth?: GitAuthBundle`. The helper imports `GitAuthBundle` type-only from `orchestrators/marketplace/shared.ts` (the canonical Phase 34 export)."
      contains: "auth?: GitAuthBundle"
  key_links:
    - from: "tests/helpers/git-mock.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts"
      via: "type-only import of GitAuthBundle"
      pattern: "import type \\{ GitAuthBundle"
---

<objective>
Wave 0 prerequisite for Plans 35-01 and 35-02. Both plans need
`tests/helpers/git-mock.ts` to expose the recorded `auth` field on
`state.cloneCalls` (Plan 35-01) and `state.fetchCalls` (Plan 35-02). The
helper ALREADY records the field at runtime via `state.cloneCalls.push({
...opts })` / `state.fetchCalls.push({ ...opts })` (lines 95 + 119); only
the TypeScript element-type declarations at lines 48-49 need widening
with `auth?: GitAuthBundle`.

Splitting this out as Wave 0 keeps Plans 01 and 02 parallel-safe (no file
conflict on `tests/helpers/git-mock.ts`). The widening is fully additive
and backward-compatible -- the 30+ existing consumers across
`tests/orchestrators/marketplace/*.test.ts` and
`tests/orchestrators/plugin/*.test.ts` continue to compile without
modification.

Purpose: unblock Plans 35-01 and 35-02 to run in parallel during Wave 1.

Output:
- `tests/helpers/git-mock.ts`: `cloneCalls` and `fetchCalls` element types
  widened to include `auth?: GitAuthBundle`; type-only import of
  `GitAuthBundle` from `orchestrators/marketplace/shared.ts`.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# The file Plan 35-00 modifies
@tests/helpers/git-mock.ts

# Source of the GitAuthBundle type
@extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts

# Phase 34 summary establishing GitAuthBundle as the canonical orchestrator-tier export
@.planning/phases/34-gitops-interface-threading-auth/34-01-SUMMARY.md

# The earlier sibling helper for comparison (already pattern-matched in Phase 34)
@tests/orchestrators/marketplace/shared.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen tests/helpers/git-mock.ts cloneCalls + fetchCalls element types with auth?: GitAuthBundle (per AUTH-01, AUTH-02)</name>
  <files>tests/helpers/git-mock.ts</files>
  <read_first>
    - tests/helpers/git-mock.ts (full file -- understand lines 48-49 where `cloneCalls` and `fetchCalls` element types are declared; lines 78-79 for the array initialization; lines 95 + 119 for the runtime push that already captures opts.auth via spread)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts (lines 75-79 for the GitAuthBundle export; line 113 confirms the field name spelled as `auth?` on GitOps.fetch)
    - tests/orchestrators/marketplace/shared.test.ts (full file -- this Phase 34 test ALREADY uses a local widened CallLog with auth?: GitAuthBundle; the new git-mock.ts widening must be byte-compatible so this test still compiles)
  </read_first>
  <action>
    In `tests/helpers/git-mock.ts`:

    1. Add a type-only import for `GitAuthBundle` from
       `orchestrators/marketplace/shared.ts`. Insert it AFTER the existing
       imports (the current imports group; the exact ordering depends on
       eslint-plugin-import-x rules). If the file currently has NO
       type-only imports from `orchestrators/`, add a new
       `import type { GitAuthBundle } from "../../extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts";`
       line. Verify the path's `../..` count matches the file's location:
       `tests/helpers/git-mock.ts` -> `extensions/pi-claude-marketplace/...`
       requires `../../extensions/pi-claude-marketplace/`.

    2. Widen the `cloneCalls` element type at line 48 from
       ```
       cloneCalls: { dir: string; url: string; ref?: string; singleBranch?: boolean }[];
       ```
       to
       ```
       cloneCalls: { dir: string; url: string; ref?: string; singleBranch?: boolean; auth?: GitAuthBundle }[];
       ```

    3. Widen the `fetchCalls` element type at line 49 from
       ```
       fetchCalls: { dir: string; remote?: string; ref?: string }[];
       ```
       to
       ```
       fetchCalls: { dir: string; remote?: string; ref?: string; auth?: GitAuthBundle }[];
       ```

    4. Do NOT modify the runtime `state.cloneCalls.push({ ...opts });`
       (line 95) or `state.fetchCalls.push({ ...opts });` (line 119) --
       both already capture every field on `opts` via spread, including
       the new `auth` field. The widening is type-only.

    5. Do NOT modify the array initializations at lines 78-79 (`cloneCalls:
       [],` and `fetchCalls: [],` -- empty arrays inherit the new types).

    6. Do NOT add `auth: GitAuthBundle | undefined` -- the field is
       OPTIONAL on the GitOps interface (Phase 34), so the helper's type
       must mirror that with `auth?: GitAuthBundle` (not `auth: GitAuthBundle
       | undefined`). This matters because TypeScript's
       `exactOptionalPropertyTypes` mode (if enabled in tsconfig.json)
       distinguishes the two; the optional-field form is the safe
       universal shape.

    7. Verify the existing consumers compile. Specifically scan:
       `tests/orchestrators/marketplace/{add,update,autoupdate,list,remove}.test.ts`,
       `tests/orchestrators/plugin/{install,update,uninstall,reinstall,list,bootstrap}.test.ts`,
       `tests/orchestrators/import/execute.test.ts`,
       any other file matching `tests/**/*.test.ts` that imports
       `makeMockGitOps`. None should require modification -- the new
       optional field is purely additive on the recorded element type.

    AUTH-09 reminder: the helper records `auth` by reference (via spread).
    A test that LOGS `state.cloneCalls[0].auth` could in theory print
    `credentialOps` reference details, but the helper itself does NOT log
    -- it only stores. AUTH-09 holds for the helper.

    Pre-commit hygiene: run
    `npx eslint --fix tests/helpers/git-mock.ts` and
    `npx prettier --write tests/helpers/git-mock.ts`.
  </action>
  <acceptance_criteria>
    - `grep -n "auth?: GitAuthBundle" tests/helpers/git-mock.ts` matches exactly 2 lines (one each on cloneCalls and fetchCalls element types).
    - `grep -n "import type \\{ GitAuthBundle" tests/helpers/git-mock.ts` matches exactly 1 line.
    - `grep -n "state\\.cloneCalls\\.push" tests/helpers/git-mock.ts` matches at least 1 line and the push form is `{ ...opts }` (byte-unchanged).
    - `grep -n "state\\.fetchCalls\\.push" tests/helpers/git-mock.ts` matches at least 1 line and the push form is `{ ...opts }` (byte-unchanged).
    - The Phase 34 `tests/orchestrators/marketplace/shared.test.ts` continues to compile and pass -- its local `CallLog.fetch` type already includes `auth?: GitAuthBundle` and remains structurally compatible with the now-widened helper.
    - `npx tsc --noEmit` exits 0.
    - `npx eslint tests/helpers/git-mock.ts` exits 0.
    - `npx prettier --check tests/helpers/git-mock.ts` exits 0.
    - `node --test tests/orchestrators/marketplace/shared.test.ts` exits 0 (Phase 34 regression check).
    - `node --test tests/orchestrators/marketplace/add.test.ts` exits 0 (current 29 tests still pass on the pre-Plan-35-01 add.ts surface).
    - `node --test tests/orchestrators/marketplace/update.test.ts` exits 0 (current tests still pass on the pre-Plan-35-02 update.ts surface).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && npx tsc --noEmit && node --test tests/orchestrators/marketplace/shared.test.ts tests/orchestrators/marketplace/add.test.ts tests/orchestrators/marketplace/update.test.ts</automated>
  </verify>
  <done>
    `tests/helpers/git-mock.ts` `cloneCalls` and `fetchCalls` element
    types are widened with `auth?: GitAuthBundle`. The runtime push remains
    byte-unchanged (already captured opts.auth via spread). TypeScript
    compiles cleanly. Existing tests pass without modification. Wave 1
    Plans 35-01 + 35-02 + 35-03 are now unblocked for parallel execution.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test helper -> consumer tests | The widened element types flow into every test that reads `state.cloneCalls` / `state.fetchCalls`. Type widening is purely additive; no consumer's existing code-path changes shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-00-01 | Tampering | A consumer relies on the absence of the `auth` field via exhaustive-property iteration | mitigate | Search shows no consumer iterates over `Object.keys(state.cloneCalls[i])` or `Object.entries(...)`; every consumer reads named fields by dotted path. Adding an optional field cannot break a named-field reader. Acceptance: all existing `*.test.ts` files pass `node --test` without modification. |
| T-35-00-02 | Information Disclosure | Test fixtures expose a recorded `auth` bundle to a `console.log` | accept | The helper itself does not log; consumer test files are gated by the AUTH-09 architecture gate's logic spreading to test code is OUT OF SCOPE (gate only covers production files per the existing test). |
| T-35-00-SC | Tampering | Package legitimacy | n/a | No npm install. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- All pre-Plan-35 tests in `tests/orchestrators/marketplace/*.test.ts` and
  `tests/orchestrators/plugin/*.test.ts` pass without modification.
- The Phase 34 `shared.test.ts` continues to pass (its local widened
  `CallLog` type remains compatible).
- Wave 1 Plans 01 / 02 / 03 are now disjoint on `files_modified` and can
  run in parallel.
</verification>

<success_criteria>
1. `tests/helpers/git-mock.ts` `cloneCalls` element type includes `auth?: GitAuthBundle`.
2. `tests/helpers/git-mock.ts` `fetchCalls` element type includes `auth?: GitAuthBundle`.
3. The helper imports `GitAuthBundle` as type-only from `orchestrators/marketplace/shared.ts`.
4. Runtime push behavior is byte-unchanged (still `{ ...opts }`).
5. All existing tests pass without modification.
6. `npx tsc --noEmit` + `npx eslint` + `npx prettier --check` all GREEN on the modified file.
</success_criteria>

<output>
Create `.planning/phases/35-orchestrator-call-sites-output-catalog-auth/35-00-SUMMARY.md` when done.
</output>
