---
phase: quick-260612-9dd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
  - extensions/pi-claude-marketplace/persistence/config-write-back.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 13 SonarCloud violations reported on PR #51 are resolved at the flagged sites"
    - "All 3 CPD duplication sets flagged on PR #51 are folded into a single shared implementation each"
    - "User-visible output (notifications, messages, public APIs) is unchanged after the fold (catalog-uat + notify byte-form gates remain GREEN)"
    - "`npm run check` is GREEN (typecheck + ESLint + Prettier + tests) on every committed task"
  artifacts:
    - path: "extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts"
      provides: "Shared opening-parse helper consumed by add.ts and remove.ts"
      contains: "extractLocalFlag"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts"
      to: "setPluginEnabled"
      via: "shared applyPluginToggles helper called by both enable + disable wrappers"
      pattern: "applyPluginToggles|setPluginEnabled.*enable:"
    - from: "extensions/pi-claude-marketplace/shared/notify.ts (computeSeverity cascade arm)"
      to: "shared severity-ladder helper"
      via: "structural-subset param typed over { marketplaces: { status, reasons?, plugins: { status, reasons? }[] }[] }"
      pattern: "allBenign"
    - from: "extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts and remove.ts"
      to: "shared opener in edge/handlers/marketplace/shared.ts"
      via: "extractLocalFlag + parseCommandArgs prologue"
      pattern: "extractLocalFlag.*parseCommandArgs|shared.ts.*open"
---

<objective>
Resolve the 13 SonarCloud violations reported on PR #51 and fold the three new
duplication sets flagged by CPD. All sites and dispositions are pre-decided in
the locked scoping block of `<scoping_already_done>`; this plan executes them.

Purpose: Restore PR #51 to a clean SonarCloud + CPD posture without changing
any user-visible behavior. Every change is structural (dead code, redundant
spread, redundant jump, repeated union, parallel loops, parallel severity
ladder, parallel handler prologue) -- the catalog-uat byte-equality gate and
the notify byte-form suites are the gate that proves behavior neutrality.

Output: 3 atomic commits (one per task), each leaving `npm run check` GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve all 13 SonarCloud violations (S7744 x9 + S3863 x2 + S4323 x1 + S3626 x1)</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/import/execute.ts,
    extensions/pi-claude-marketplace/persistence/config-write-back.ts,
    extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts,
    extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts,
    extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts
  </files>
  <action>
    Apply the four mechanical rewrites at the exact sites locked in the
    scoping block. Each rewrite is local and behavior-neutral; the gate is
    that `npm run check` stays GREEN at the end.

    (a) S7744 "The empty object is useless" -- 9 spread-of-undefined sites.
    Replace `{ ...(x ?? {}) }` with `{ ...x }` at exactly these 9 spread
    sites (and ONLY these -- do NOT touch any `Object.entries(x ?? {})`
    elsewhere; there the `?? {}` is load-bearing because
    `Object.entries(undefined)` throws):
      - extensions/pi-claude-marketplace/orchestrators/import/execute.ts:917
        `{ ...(ensure.marketplaces ?? {}) }`
      - extensions/pi-claude-marketplace/orchestrators/import/execute.ts:924
        `{ ...(ensure.plugins ?? {}) }`
      - extensions/pi-claude-marketplace/persistence/config-write-back.ts:62
        `{ ...(current.marketplaces ?? {}), [marketplace]: merged }`
      - extensions/pi-claude-marketplace/persistence/config-write-back.ts:78
        `{ ...(current.marketplaces ?? {}) }`
      - extensions/pi-claude-marketplace/persistence/config-write-back.ts:118
        `{ ...(current.plugins ?? {}), [key]: merged }`
      - extensions/pi-claude-marketplace/persistence/config-write-back.ts:135
        `{ ...(current.plugins ?? {}) }`
      - extensions/pi-claude-marketplace/persistence/config-write-back.ts:174
        `{ ...(current.marketplaces ?? {}) }`
      - extensions/pi-claude-marketplace/persistence/config-write-back.ts:180
        `{ ...(current.plugins ?? {}) }`
      - extensions/pi-claude-marketplace/persistence/config-write-back.ts:182
        `plugins[key] = { ...(plugins[key] ?? {}), ...patch };`
    Verify TS strict still accepts each site -- spreading a possibly-undefined
    value is legal under TS strict and the resulting empty-object spread is
    a no-op. If any site shows a new typecheck error, surface it and stop
    (do NOT add a non-null assertion -- the locked scope is "remove the
    redundant guard", not "tighten the type contract").

    (b) S3863 "imported multiple times" -- merge the two `notify.ts` type
    imports at the top of
    extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts
    (lines 26-27) into one:
      `import type { ContentReason, Dependency, Reason } from "../../shared/notify.ts";`
    Keep alphabetical member order so ESLint sort-imports / import-x rules
    stay GREEN. No other edits to apply-outcomes.ts.

    (c) S4323 "Replace this union type with a type alias" -- in
    extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts the
    union `"github" | "path" | "unknown" | undefined` is repeated at lines
    433, 463, and 603 (return type + local-typed const + bare let). Add a
    file-local alias near the top of the file (after the imports):
      `type RecordedSourceKind = "github" | "path" | "unknown";`
    Then substitute `RecordedSourceKind | undefined` at all three sites:
      - line 433: function return type `Promise<RecordedSourceKind | undefined>`
      - line 463: const annotation `const sourceKind: RecordedSourceKind | undefined =`
      - line 603: `let sourceKindAtRecord: RecordedSourceKind | undefined;`
    Name `RecordedSourceKind` is at executor's discretion -- pick a name that
    reads at the call sites (alternatives if it clashes: `RecordedSource`,
    `MarketplaceRecordedSource`). Do NOT re-export the alias -- it stays
    file-local (no other file references this union today). Per
    `.claude/rules/typescript-comments.md`, do not add a comment narrating
    "extracted in PR #51"; an alias name is self-documenting.

    (d) S3626 "Remove this redundant jump" -- in
    extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts at
    line 382, the `return;` inside
    `if (record !== undefined && isRecordedButDisabled(record)) { acc.enable.push(...); return; }`
    is redundant because the function falls through to its closing brace
    immediately after (the trailing comment
    `// Declared-enabled, recorded, populated: steady state, no action.`
    is the only thing between the `}` of that `if` and the function's `}`).
    Delete only that `return;` statement. Keep the `acc.enable.push(...)`
    call AND the trailing explanatory comment intact. Do NOT remove
    `return;` statements anywhere else in the function -- the earlier
    `acc.disable.push` and `acc.install.push` blocks still need their
    returns (they ARE followed by more branches).

    All four rewrites land in one commit. No comment additions; no GSD
    history-tracking references (phase/plan/milestone numbers) per
    `.claude/rules/typescript-comments.md`.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run check</automated>
    Additionally grep-check (`grep -v '^#' &lt;file&gt; | grep -c '\\.\\.\\.(' &lt;the modified files&gt;`)
    that the 9 `?? {}` spread occurrences are gone from the listed files at
    the listed lines (count drops from 9 to 0 in those scope-narrowed locations).
  </verify>
  <done>
    `npm run check` exit 0 (typecheck + ESLint + Prettier + tests).
    The 9 listed spread sites no longer match `\.\.\.\(.*\?\? \{\}\)`.
    apply-outcomes.ts has one merged type import (line range 26 only).
    remove.ts has `RecordedSourceKind` (or chosen alias) defined once and
    used at lines 433/463/603.
    plan.ts line 382 redundant `return;` is deleted; explanatory comment
    retained.
    No output / message / public-API change.
    One atomic commit: `refactor(sonar): resolve 13 PR #51 violations`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fold reconcile apply enable/disable + notify cascade severity ladder (Folds 1 + 2)</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts,
    extensions/pi-claude-marketplace/shared/notify.ts
  </files>
  <action>
    Two byte-neutral structural folds in one commit. Both are pure code
    deduplication -- no output bytes change. The catalog-uat byte-equality
    gate + notify-v2 byte-form suite (110 tests) are the gate.

    FOLD 1 -- `applyPluginEnables` (lines ~449-485) and `applyPluginDisables`
    (lines ~501-535) in apply.ts are byte-parallel loops over
    `plan.pluginsToEnable` / `plan.pluginsToDisable` calling
    `setPluginEnabled({ ..., enable: true|false, ... })` and pushing
    `PerEntryOutcome`s differing ONLY in:
      - input ops list (`plan.pluginsToEnable` vs `plan.pluginsToDisable`)
      - `enable` boolean passed to `setPluginEnabled`
      - success status literal expected back (`"enabled"` vs `"disabled"`)
      - success outcome kind (`"plugin-enabled"` vs `"plugin-disabled"`)
      - failure outcome kind (`"plugin-enable-failed"` vs
        `"plugin-disable-failed"`)
    Extract ONE shared helper (suggested name `applyPluginToggles`, but the
    executor may pick a clearer name) parameterized on those 5 axes. Both
    `applyPluginEnables` and `applyPluginDisables` either become thin
    one-line wrappers OR are deleted in favor of two direct call sites at
    the original call sites in `applyReconcile` -- executor's discretion;
    prefer the thinner shape that doesn't add an indirection level just to
    name the operation.

    Constraint: the pushed outcome objects MUST still satisfy the
    `PerEntryOutcome` discriminated union defined in
    `orchestrators/reconcile/apply-outcomes.ts` (variants `plugin-enabled` /
    `plugin-disabled` / `plugin-enable-failed` / `plugin-disable-failed`).
    Use literal-typed parameters so TS strict accepts the construction --
    e.g. parameterize as
    `{ successKind: "plugin-enabled" | "plugin-disabled"; failedKind: "plugin-enable-failed" | "plugin-disable-failed"; successStatus: "enabled" | "disabled"; enable: boolean }`
    and let the union narrow at each call site. Do NOT use `as` casts to
    paper over a generic-string parameter -- if TS narrowing requires a
    factory-function shape (e.g. `buildSuccess: (info) => PerEntryOutcome`
    closures supplied per call site), prefer that to a cast.

    Preserve the `// skipped (idempotent) -> intentionally drop; the steady
    state isn't a user-visible action.` comment on the shared helper (move
    it once; do not duplicate it). The `try`/`catch` shape and the
    `classifyOrchestratorThrow(err)` failure path are byte-identical between
    the two and stay in the helper.

    FOLD 2 -- in shared/notify.ts the 4-arm severity ladder (any failed ->
    error; any manual-recovery plugin -> warning; any plugin-level
    actionable skip via `!allBenign(p.reasons)` -> warning; any mp-level
    actionable skip via `!allBenign(mp.reasons)` -> warning; else
    undefined) exists twice -- in `reconcileAppliedSeverity` (~lines
    1856-1885) and inline in `computeSeverity`'s cascade arm (~lines
    1926-1963). Extract one file-private helper over the structural subset
    `{ marketplaces: readonly { status; reasons?; plugins: readonly { status; reasons? }[] }[] }`.
    This repo already established the "structural-subset param typing"
    pattern for exactly this kind of cross-message-shape fold (the
    `_MarketplaceDetailsExpected` / cascade-shape lineage); reuse the
    pattern rather than introducing a tagged interface.

    Suggested helper name: `cascadeSeverity(message: { marketplaces: ... })`
    or `actionableCascadeSeverity` -- executor's choice. Both call sites
    invoke it with their own typed argument and the structural subtype
    relation makes the call type-check.

    Preserve the per-arm D-28-08 / D-28-09 explanatory comments AT THE
    HELPER (move them from the inline `computeSeverity` arms; do not
    duplicate at both call sites). The `reconcileAppliedSeverity` wrapper
    may either remain as a 1-line forwarder (it still has its own JSDoc
    describing the empty-and-clean caller short-circuit) OR be deleted in
    favor of inlining `cascadeSeverity(message)` at its one caller --
    executor's discretion. Prefer keeping the wrapper if it simplifies the
    callers' types, since its JSDoc is load-bearing context for NFR-2 / A4
    behavior.

    CRITICAL byte-neutrality: this is a pure code-structure fold. The
    catalog-uat byte-equality gate and the notify byte-form suites (110
    tests) are the gate that proves no output bytes shift. If either is
    RED, the fold has changed behavior -- diagnose and revert before
    committing.

    Per `.claude/rules/typescript-comments.md`, no GSD phase/plan/milestone
    references in new helper-level comments; decision IDs (D-28-08,
    D-28-09) are allowed and SHOULD be preserved.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run check</automated>
    Additionally confirm the catalog-uat + notify-v2 byte-form suites are
    in the executed test set and PASS (they are part of `npm run check`
    by default; if `npm run check` is GREEN they are GREEN).
  </verify>
  <done>
    `npm run check` exit 0.
    apply.ts: one shared helper handles both enable + disable loops; the
    inner `try`/`catch` + outcome-push body appears ONCE, not twice.
    notify.ts: one cascade severity helper called from both
    `reconcileAppliedSeverity` and `computeSeverity`'s cascade arm; the
    4-arm ladder body appears ONCE, not twice.
    No output / message / public-API change (catalog-uat + notify byte
    tests GREEN).
    One atomic commit:
    `refactor(cpd): fold reconcile apply toggles + cascade severity ladder`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fold marketplace edge handler opening parse (Fold 3)</name>
  <files>
    extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts,
    extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts,
    extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts
  </files>
  <action>
    Both `makeAddHandler` (add.ts:29-59) and `makeRemoveHandler`
    (remove.ts:28-51) open with the same prologue:
      1. `const localFlag = extractLocalFlag(args, ctx, USAGE);`
         `if (localFlag === undefined) return;`  (WB-01)
      2. `const parsed = parseCommandArgs(localFlag.residualArgs, { positional: [{ name: <"source"|"name"> }] as const, usage: USAGE }, (message) => notifyUsageError(ctx, { message: message === USAGE ? "Missing required argument." : message, usage: USAGE }));`
         (MSG-NC-2 substitution behavior identical)
      3. `if (parsed === undefined) return;`
    Differing ONLY in the USAGE string and the single positional name
    (`"source"` vs `"name"`).

    Extract a shared opening-parse helper into
    `extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts`
    (which already hosts `makeSingleNameMarketplaceHandler` from a previous
    CPD fold -- same surface). Suggested signature (executor may adjust):

      `function openMarketplaceCommand<N extends string>(args, ctx, opts: { usage: string; positionalName: N }): { scope?: Scope; localFlag: LocalFlag } & { [K in N]: string } | undefined`

    Returning `undefined` when either the local-flag extraction or the
    positional parse fails (i.e. when the handler should early-return),
    and otherwise returning the union of the `parsed` row + the
    `localFlag` so the call site can still see both. The literal `N`
    parameter lets TS surface `result.source` (in add) or `result.name`
    (in remove) without a cast.

    Then `makeAddHandler` becomes:
      `const opened = openMarketplaceCommand(args, ctx, { usage: USAGE, positionalName: "source" }); if (opened === undefined) return; ...; rawSource: opened.source, ...`
    and `makeRemoveHandler` symmetrically:
      `const opened = openMarketplaceCommand(args, ctx, { usage: USAGE, positionalName: "name" }); if (opened === undefined) return; ...; opened.name, ...`

    If the executor finds the literal-parameter typing impractical, an
    acceptable fallback is two narrowly-typed wrapper functions in
    shared.ts (`openMarketplaceCommandWithSource`,
    `openMarketplaceCommandWithName`) that share an internal generic
    implementation. Do NOT use `as { source: string }` casts at the call
    site; rely on TS narrowing.

    Preserve the WB-01 comment ("extract `--local` BEFORE positional
    parsing so flag position cannot change the outcome") and the MSG-NC-2
    comment (the duplicate-usage substitution rationale) AT THE HELPER --
    these are the load-bearing rationale and must move once with the code
    (do not delete; do not duplicate at both call sites).

    Adoption scope: check whether other edge handlers (in particular
    `extensions/pi-claude-marketplace/edge/handlers/marketplace/autoupdate.ts`
    if it exists, and any plugin-side handlers under
    `edge/handlers/plugin/` with the same shape) could share the helper.
    Adopt only if their opening prologue is byte-identical in BEHAVIOR
    (same `extractLocalFlag` + `parseCommandArgs` + `notifyUsageError`
    substitution shape, same one-positional structure). Do NOT force the
    fold onto handlers whose prologue diverges -- the CPD report only
    flagged the add/remove pair, so the minimum viable change is those
    two.

    Per `.claude/rules/typescript-comments.md`, keep WB-01 and MSG-NC-2
    decision IDs in the moved comments; do not add a comment narrating
    when the helper was extracted.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run check</automated>
    Manual grep-check that add.ts and remove.ts no longer each contain
    a `parseCommandArgs(localFlag.residualArgs` call -- both should now
    call the shared helper.
  </verify>
  <done>
    `npm run check` exit 0.
    `edge/handlers/marketplace/shared.ts` exports a new
    opening-parse helper used by both `add.ts` and `remove.ts`.
    `add.ts` and `remove.ts` no longer each contain the
    `extractLocalFlag` + `parseCommandArgs` + `notifyUsageError`
    prologue -- it appears exactly once, in shared.ts.
    No output / message / public-API change (handler usage errors render
    byte-identical to before).
    One atomic commit:
    `refactor(cpd): fold marketplace edge handler opening parse`.
  </done>
</task>

</tasks>

<verification>
- Each task ships as ONE atomic commit (3 commits total).
- After each task: `npm run check` GREEN before staging the next task.
- After Task 1: the 9 spread sites, the duplicate import, the 3 union
  repetitions, and the redundant `return;` are all gone; behavior identical.
- After Task 2: apply.ts has one toggle helper (not two parallel loops),
  notify.ts has one cascade severity helper (not two parallel ladders); all
  notify byte-form and catalog-uat tests still GREEN proving zero
  user-visible output drift.
- After Task 3: marketplace `add` + `remove` handler prologues converge in
  shared.ts; handler usage errors render byte-identical to before.
- Pre-commit hook discipline per CLAUDE.md: run
  `pre-commit run --files <changed files>` BEFORE each `git commit`. Fix
  failures and re-run until clean. NEVER use `--no-verify`. No worktree
  involved (work on the current branch), so no `SKIP=trufflehog` needed.
- Conventional Commits per CLAUDE.md: titles &lt;=72 chars, body lines &lt;=80
  chars. Suggested titles given per task; executor may tighten.
- No new comments referencing GSD phases/plans/milestones (per
  `.claude/rules/typescript-comments.md`); decision/requirement IDs
  (D-28-08, D-28-09, WB-01, MSG-NC-2, etc.) ARE allowed and SHOULD be
  preserved when moving existing comments.
- Re-run SonarCloud on the resulting commits (next PR push) to confirm the
  13 violations and 3 duplication sets are cleared.
</verification>

<success_criteria>
- Task 1 commit: 13/13 SonarCloud violations resolved; `npm run check` GREEN.
- Task 2 commit: 2/3 CPD duplication sets folded (apply enable/disable +
  notify cascade severity); `npm run check` GREEN; catalog-uat + notify
  byte-form tests GREEN proving no output bytes shifted.
- Task 3 commit: 3/3 CPD duplication sets folded (marketplace edge handler
  opening parse); `npm run check` GREEN.
- All three commits follow Conventional Commits; pre-commit hook ran clean
  on each (no `--no-verify`).
- No user-visible output, message, or public-API change anywhere across the
  three commits.
</success_criteria>

<output>
Create `.planning/quick/260612-9dd-resolve-sonarcloud-pr-51-violations-and-/260612-9dd-SUMMARY.md` when done.
</output>
