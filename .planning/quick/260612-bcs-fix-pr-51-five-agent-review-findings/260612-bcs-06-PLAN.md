---
quick_task: 260612-bcs-fix-pr-51-five-agent-review-findings
sub_plan: 06
type: execute
wave: 6
depends_on: [260612-bcs-02, 260612-bcs-04, 260612-bcs-05]
files_modified:
  - tests/orchestrators/reconcile/apply.test.ts
  - tests/orchestrators/reconcile/plan.test.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
  - tests/persistence/config-io.test.ts
  - tests/persistence/config-write-back.test.ts
autonomous: true
byte_contract: byte-neutral
requirements:
  - T1
  - T3
  - T4
  - T5
  - T6

must_haves:
  truths:
    - "Load-time reconcile ENABLE row is exercised end-to-end through applyReconcile; an orchestrated enable-success test is also added (T1)."
    - "Direct pluginsToUninstall bucket through applyReconcile is pinned with a one-plugin-deleted fixture: applyPluginUninstalls renders one (uninstalled) row + WR-06 converged-row-drop (T3)."
    - "applySourceMismatches + applied-cascade source-mismatch arm exercised end-to-end via applyReconcile, including the dangling-reference plugin child row from Plan 04's Y2 variants (T4)."
    - "Predicate-drift test pins isRecordedButDisabled and isCurrentlyDisabled agreement over the populated/empty x installable matrix (T5)."
    - "Smaller arms pinned: classifyReadPassThrow lock-held arm; loadConfig non-ENOENT EISDIR arm (directory-named-claude-plugins.json); writeMarketplaceConfigEntry partial patch saveConfig loud refusal (T6)."
    - "All rendered bytes stay identical; this commit is test-only; catalog-uat byte gate GREEN with NO docs/output-catalog.md edit."
  artifacts:
    - path: tests/orchestrators/reconcile/apply.test.ts
      provides: "T1 (load-time ENABLE), T3 (direct pluginsToUninstall), T4 (applySourceMismatches), T6 (classifyReadPassThrow lock-held arm)."
    - path: tests/orchestrators/reconcile/plan.test.ts
      provides: "T5 (predicate-drift matrix: populated/empty x installable true/false)."
    - path: tests/orchestrators/plugin/enable-disable.test.ts
      provides: "T1 (orchestrated enable-success: `status: enabled` + version)."
    - path: tests/persistence/config-io.test.ts
      provides: "T6 (loadConfig non-ENOENT EISDIR arm via directory-named claude-plugins.json)."
    - path: tests/persistence/config-write-back.test.ts
      provides: "T6 (writeMarketplaceConfigEntry partial patch saveConfig loud refusal)."
  key_links:
    - from: tests/orchestrators/reconcile/apply.test.ts (WR-09 disable-axis at :443)
      to: applyReconcile load-time ENABLE arm (notify.ts:320-335 plugin-enabled)
      via: mirror seed shape; assert (installed) row + state re-populated + config files byte-unchanged + second reconcile silent
      pattern: "applyReconcile.*enabled"
    - from: tests/orchestrators/reconcile/plan.test.ts
      to: src/orchestrators/reconcile/plan.ts::isRecordedButDisabled + src/orchestrators/plugin/enable-disable.ts::isCurrentlyDisabled
      via: matrix over populated/empty resources x installable true/false
      pattern: "isRecordedButDisabled.*isCurrentlyDisabled"
---

<objective>
Close the remaining test gaps from CONTEXT.md that did NOT land alongside
their behaviour fixes in earlier plans: T1, T3, T4, T5, T6. T2 is already
covered by Plan 02 (D-UPD behavior tests in update.test.ts +
autoupdate.test.ts).

Purpose: This is a test-only commit. Every behaviour these tests exercise
must already work post Plans 01-05; this commit pins the contracts so
future regressions trip a test, not a review agent.

Output: One atomic Conventional Commit on `features/v1.12-config-files`,
test-only, byte-neutral, `npm run check` GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260612-bcs-fix-pr-51-five-agent-review-findings/260612-bcs-CONTEXT.md
@CLAUDE.md
@.claude/rules/typescript-comments.md

# Sources for fixture shapes (read-only -- no source edits in this commit)
@src/orchestrators/reconcile/apply.ts
@src/orchestrators/reconcile/plan.ts
@src/orchestrators/reconcile/notify.ts
@src/orchestrators/plugin/enable-disable.ts
@src/persistence/config-io.ts
@src/persistence/config-write-back.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: T1 + T3 + T4 + T5 + T6 (all GREEN on first run, test-only commit)</name>
  <files>
    tests/orchestrators/reconcile/apply.test.ts,
    tests/orchestrators/reconcile/plan.test.ts,
    tests/orchestrators/plugin/enable-disable.test.ts,
    tests/persistence/config-io.test.ts,
    tests/persistence/config-write-back.test.ts
  </files>
  <behavior>
    Add the following tests. Because all behaviour they exercise already
    landed in Plans 01-05, every test should be GREEN on first run.

    - T1 (load-time ENABLE through applyReconcile): mirror the
      WR-09 disable-axis test at `tests/orchestrators/reconcile/apply.test.ts:443`.
      Seed a disabled record + config-enabled. Assert: applyReconcile
      renders `(installed)`, state is re-populated, both config files are
      byte-unchanged, a second applyReconcile is silent (no rows
      rendered). Also add an orchestrated enable-success test in
      `tests/orchestrators/plugin/enable-disable.test.ts` asserting
      `status: "enabled"` + version on the returned outcome.
    - T3 (direct pluginsToUninstall through applyReconcile): seed a
      marketplace that stays DECLARED while ONE plugin entry is deleted
      from config. Run applyReconcile. Assert: `applyPluginUninstalls`
      renders one `(uninstalled)` row at the apply layer + WR-06
      converged-row-drop. Today only the marketplace-remove cascade
      exercises this path -- this test covers the direct bucket path.
    - T4 (applySourceMismatches + applied-cascade source-mismatch arm):
      exercise `apply.ts:515-524` and `notify.ts:353-364` end-to-end via
      applyReconcile, including the dangling-reference plugin child row
      from Plan 04's Y2 widening (cause: `dangling-reference`, `plugin`
      required).
    - T5 (predicate-drift agreement): in
      `tests/orchestrators/reconcile/plan.test.ts`, add a matrix test over
      `populated/empty resources` x `installable true/false` asserting
      `isRecordedButDisabled` (`plan.ts:285-295`) and
      `isCurrentlyDisabled` (`enable-disable.ts:167-183`) agree on every
      cell. Use a table-driven test.
    - T6 (smaller arms):
        * `classifyReadPassThrow` lock-held arm at `apply.ts:193-203`:
          pin via a forced `StateLockHeldError`.
        * `loadConfig` non-ENOENT read-failure arm at
          `config-io.ts:128-133`: drive it portably with a DIRECTORY
          named `claude-plugins.json` (yields EISDIR on read --
          `await fs.mkdir(path)` is portable; do NOT use chmod tricks
          for read-failure portability).
        * `writeMarketplaceConfigEntry` partial patch on absent
          marketplace at `config-write-back.ts:58, :177` casts: pin
          saveConfig's loud refusal.
  </behavior>
  <action>
    Sequence:
    1. Write T1 tests (apply + enable-disable test files).
    2. Write T3 test (apply test file).
    3. Write T4 test (apply test file) -- exercise Plan 04's
       `dangling-reference` variant from Y2.
    4. Write T5 matrix test (plan test file).
    5. Write T6 sub-tests (apply test file, config-io test file,
       config-write-back test file).
    6. Run `node --test <new tests>` -- all should be GREEN on first
       run since the behaviour already lands in Plans 01-05.
    7. Run catalog-uat byte gate. MUST be GREEN with NO catalog edits.
    8. Run `npm run check` until GREEN.
    9. Run `pre-commit run --files <changed>` until GREEN.
    10. Commit atomically. SKIP=trufflehog inside a worktree. Suggested
        title: `test(reconcile): pin load-time enable + cascade arms + predicate drift`.

    Test-only commit. ZERO source edits. ZERO catalog edits.

    Important fixture notes:
    - For T6's EISDIR sub-test, `await fs.mkdir(path)` portably yields
      EISDIR on subsequent `readFile`; this avoids chmod-tricks for
      portability.
    - For T1's load-time ENABLE arm, the seed shape is exactly the
      WR-09 disable-axis fixture inverted (disabled record + config-
      enabled, instead of populated record + config-disabled).
    - For T4's dangling-reference variant, the fixture matches the new
      Y2 variant signature from Plan 04 (`cause: "dangling-reference"`,
      `plugin` field required).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    - All T1, T3, T4, T5, T6 tests exist and are GREEN.
    - catalog-uat byte gate GREEN with NO docs/output-catalog.md edits.
    - `npm run check` exit 0.
    - `pre-commit run --files <changed>` GREEN.
    - One atomic Conventional Commit on `features/v1.12-config-files`.
    - Zero source files edited in this commit.
  </done>
</task>

</tasks>

<verification>
1. `npm run check` GREEN.
2. catalog-uat byte gate GREEN with NO docs/output-catalog.md edit.
3. T1: load-time ENABLE test renders `(installed)` row + second reconcile
   silent; orchestrated enable-success test asserts `status: "enabled"`.
4. T3: direct pluginsToUninstall bucket renders one `(uninstalled)` row +
   WR-06 converged-row-drop.
5. T4: applySourceMismatches dangling-reference variant renders the
   plugin child row through applyReconcile.
6. T5: predicate-drift matrix agrees across populated/empty x installable
   true/false.
7. T6: lock-held arm, EISDIR (directory-named claude-plugins.json),
   writeMarketplaceConfigEntry partial-patch saveConfig loud-refusal --
   all GREEN.
8. ZERO source-file edits in the commit diff (test files only).
9. One atomic Conventional Commit on `features/v1.12-config-files`.
</verification>

<success_criteria>
- T1, T3, T4, T5, T6 are CLOSED by new tests.
- Commit is byte-neutral and test-only.
- `npm run check` GREEN.
</success_criteria>

<output>
Append per-finding closure notes to
`.planning/quick/260612-bcs-fix-pr-51-five-agent-review-findings/260612-bcs-SUMMARY.md`
recording which commit closed T1, T3, T4, T5, T6.
</output>
