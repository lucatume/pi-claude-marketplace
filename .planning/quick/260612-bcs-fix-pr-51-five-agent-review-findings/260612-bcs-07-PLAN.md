---
quick_task: 260612-bcs-fix-pr-51-five-agent-review-findings
sub_plan: 07
type: execute
wave: 7
depends_on: [260612-bcs-01, 260612-bcs-02, 260612-bcs-03, 260612-bcs-04, 260612-bcs-05, 260612-bcs-06]
files_modified:
  - CLAUDE.md
  - src/orchestrators/reconcile/README.md
  - src/orchestrators/reconcile/apply.ts
  - src/orchestrators/reconcile/preview.ts
  - src/orchestrators/reconcile/plan.ts
  - src/orchestrators/reconcile/notify.ts
  - src/orchestrators/reconcile/types.ts
  - src/orchestrators/reconcile/apply-outcomes.ts
  - src/orchestrators/plugin/install.ts
  - src/orchestrators/plugin/uninstall.ts
  - src/orchestrators/plugin/enable-disable.ts
  - src/orchestrators/marketplace/remove.ts
  - src/persistence/migrate-config.ts
  - src/persistence/config-merge.ts
  - src/persistence/config-write-back.ts
  - src/shared/notify.ts
autonomous: true
byte_contract: byte-neutral
requirements:
  - D1
  - D2
  - D3
  - D4
  - D5
  - D6
  - D7
  - D8
  - D9
  - D10
  - D11
  - S1
  - D-MIG

must_haves:
  truths:
    - "reconcile/README.md is rewritten in domain terms (purity discipline, 7-bucket model, sentinel contracts) with NO Phase/Plan/Wave/Task references and NO process-history voice; every claim checks out against the post-Plan-06 source."
    - "All forbidden process-history comments listed under D11 are stripped or rewritten to inline rationale; decision/requirement IDs are preserved as the only allowed traceability."
    - "persistence/migrate-config.ts:30-33 contract comment documents the first-run-migration silence as deliberate (NFR-2) and informational (D-MIG); no notify call added."
    - "CLAUDE.md NFR-10 enumerates the new sanctioned write paths (claude-plugins.json, claude-plugins.local.json) (S1)."
    - "D2-D10 textual corrections applied with symbolic refs (file.ts::symbol) replacing hard line-number cross-refs; every replaced symbol grep-verified at edit time."
    - "ZERO source-logic edits; ZERO rendered-byte changes; catalog-uat byte gate GREEN with NO docs/output-catalog.md edit; npm run check GREEN."
  artifacts:
    - path: CLAUDE.md
      provides: "NFR-10 enumeration adds claude-plugins.json + claude-plugins.local.json (S1)."
    - path: src/orchestrators/reconcile/README.md
      provides: "Rewritten in domain terms (D1); no process-history voice; all claims source-grounded."
    - path: src/persistence/migrate-config.ts
      provides: "Contract comment at :30-33 marks the silence as deliberate and informational (D-MIG)."
    - path: src/orchestrators/reconcile/apply.ts
      provides: "D2 corrections + D10 symbolic refs + D11 ID strips."
    - path: src/orchestrators/plugin/install.ts
      provides: "D5 resolvePluginVersion + 3-tier precedence (plugin.json > entry.version > hash) docstring; D10 symbolic refs; D11 ID strips."
    - path: src/orchestrators/plugin/uninstall.ts
      provides: "D4 JSDoc cleanup + variable-ref fixes; D10 symbolic refs; D11 ID strips."
    - path: src/orchestrators/marketplace/remove.ts
      provides: "D8 Flow header rewritten to current implementation; D11 ID strips."
    - path: src/shared/notify.ts
      provides: "D3 tuple-position claims corrected (`disabled` is last); D11 ID strips."
  key_links:
    - from: src/orchestrators/reconcile/README.md
      to: src/orchestrators/reconcile/{apply,plan,preview,notify,types,apply-outcomes}.ts + plan.ts:285-295/:375-382
      via: every README claim grep-verifiable against post-Plan-06 source (7-bucket model, sentinel contracts, pluginsToEnable populated, empty-resources marker)
      pattern: "pluginsToEnable|sentinel"
    - from: CLAUDE.md NFR-10
      to: src/persistence/locations.ts:133
      via: sanctioned write-paths enumeration includes claude-plugins.json + claude-plugins.local.json
      pattern: "claude-plugins\\.(local\\.)?json"
---

<objective>
LAST plan in the split. Pure comment / docs / CLAUDE.md cleanup over the
post-Plans-01-06 source. ZERO behavior change; ZERO rendered-byte change.

Closes: D1 (reconcile/README.md rewrite), D2-D10 (textual + symbolic-ref
corrections across reconcile + plugin orchestrators + persistence),
D11 (planning-artifact ID strip across all listed sites), S1 (CLAUDE.md
NFR-10 enumeration), D-MIG (LOCKED: first-run migration contract comment
at migrate-config.ts:30-33 only -- no notify call).

Purpose: This must land LAST because every prior plan edited comment
neighbourhoods. Doing the scrub last means we document the FINAL code,
not stale references.

Output: One atomic Conventional Commit on `features/v1.12-config-files`,
comments/docs only, byte-neutral, `npm run check` GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260612-bcs-fix-pr-51-five-agent-review-findings/260612-bcs-CONTEXT.md
@CLAUDE.md
@.claude/rules/typescript-comments.md
@docs/output-catalog.md
@docs/messaging-style-guide.md

@src/orchestrators/reconcile/README.md
@src/orchestrators/reconcile/apply.ts
@src/orchestrators/reconcile/preview.ts
@src/orchestrators/reconcile/plan.ts
@src/orchestrators/reconcile/notify.ts
@src/orchestrators/reconcile/types.ts
@src/orchestrators/reconcile/apply-outcomes.ts
@src/orchestrators/plugin/install.ts
@src/orchestrators/plugin/uninstall.ts
@src/orchestrators/plugin/enable-disable.ts
@src/orchestrators/marketplace/remove.ts
@src/persistence/migrate-config.ts
@src/persistence/config-merge.ts
@src/persistence/config-write-back.ts
@src/persistence/locations.ts
@src/shared/notify.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite reconcile/README.md in domain terms (D1)</name>
  <files>
    src/orchestrators/reconcile/README.md
  </files>
  <action>
    Rewrite the file end-to-end in domain terms. Anchor every claim to the
    POST-Plan-06 source. Required corrections from CONTEXT.md D1:

    - `pluginsToEnable` IS populated (see plan.ts:285-295 and :375-382 after
      Plan 04's Y1/Y2 cuts settle); the README's claim that it is
      "structurally empty" is FACTUALLY WRONG.
    - There is NO `state.disabled` field; the disabled marker is empty
      `resources.*` + `installable: true` (the README's prediction of a
      `state.disabled` field was deliberately never built).
    - The directory has 7 files; the README lists 5. Enumerate all 7 with
      a one-line role for each.
    - The voice is forbidden process-history (Phase 53/54/55, Plan 01/02
      tables). Replace with domain content: purity discipline (the
      planner is pure; the applier is the side-effect boundary), the
      7-bucket model (marketplace adds / removes / plugin installs /
      uninstalls / toggles / source-mismatches / read-pass throws), and
      sentinel contracts (the empty-resources disabled marker; the
      tri-state samePlannedSource sentinel from Plan 04's Y1).

    Voice rules:
    - Follow `.claude/rules/typescript-comments.md` (comment policy).
    - Follow the Google Markdown style guide (per ~/.claude/CLAUDE.md):
      sentence-case headings, no emoji decoration, plain prose.
    - Treat domain-language uses of the word "phase" as preserved
      unchanged only when describing CODE concepts (e.g. "two-phase
      commit", "phase ledger"). The reconcile README has none of those;
      every "Phase" reference there IS GSD history and goes.

    ZERO source-logic edits.
  </action>
  <verify>
    <automated>grep -nE '(Phase [0-9]+|Plan [0-9]+|Wave [0-9]+|Task [0-9]+)' src/orchestrators/reconcile/README.md ; test $? -eq 1</automated>
  </verify>
  <done>
    - reconcile/README.md mentions all 7 files in the directory with a
      one-line role each.
    - reconcile/README.md asserts `pluginsToEnable` IS populated and the
      disabled marker is empty `resources.*` + `installable: true`.
    - reconcile/README.md contains ZERO Phase/Plan/Wave/Task references.
    - reconcile/README.md voice is technical / domain-grounded; no
      process-history narration.
  </done>
</task>

<task type="auto">
  <name>Task 2: D2-D10 textual + symbolic-ref corrections; D11 ID strip; D-MIG comment; S1 CLAUDE.md update</name>
  <files>
    CLAUDE.md,
    src/orchestrators/reconcile/apply.ts,
    src/orchestrators/reconcile/preview.ts,
    src/orchestrators/reconcile/plan.ts,
    src/orchestrators/reconcile/notify.ts,
    src/orchestrators/reconcile/types.ts,
    src/orchestrators/reconcile/apply-outcomes.ts,
    src/orchestrators/plugin/install.ts,
    src/orchestrators/plugin/uninstall.ts,
    src/orchestrators/plugin/enable-disable.ts,
    src/orchestrators/marketplace/remove.ts,
    src/persistence/migrate-config.ts,
    src/persistence/config-merge.ts,
    src/persistence/config-write-back.ts,
    src/shared/notify.ts
  </files>
  <behavior>
    Apply each comment/docs finding precisely as documented in CONTEXT.md.
    ZERO source-logic edits in this commit -- only comment bodies,
    docstrings, markdown content, and CLAUDE.md prose.

    D2 (reconcile/apply.ts):
    - apply.ts:18: "drive the four orchestrators" -> "five" (the comment
      lists six actions).
    - apply.ts:533-534: rewrite the order-rationale paragraph --
      buildUninstallBucket (plan.ts:386-414) EXCLUDES plugins under a
      to-be-removed marketplace (apply.ts's own WR-02 comment at :251
      confirms). State the correct rationale.

    D3 (shared/notify.ts:163, :275-276): rewrite the tuple-position
    claims so they reflect `disabled` being LAST (these comments are
    byte-load-bearing reasoning for catalog gates; correctness matters).

    D4 (uninstall.ts:193-204): delete the orphaned JSDoc OR merge into
    the actual entrypoint doc at :348-351. Fix `:617` / `:630` variable
    refs that no longer exist (`outcome` -> `localOutcome`,
    `cascadeResult` -> `cascadeFailure`, etc.).

    D5 (install.ts:16, :494): replace `resolveInstallVersion` with the
    actual `resolvePluginVersion`; restate the 3-tier precedence
    (plugin.json > entry.version > hash) per `plugin/shared.ts:461-475`.

    D6 (reconcile/plan.ts:263-266): strip the line-number cross-ref to
    `install.ts::statePhase`; rephrase as "the only path that POPULATES
    resources.*" (vs disable emptying them). Use symbolic refs only.

    D7 (apply-outcomes.ts:145): fix the provenance -- CFG-03 reason is
    the hard-coded literal "invalid manifest" (never narrowProbeError);
    the state-load arm can also yield "lock held".

    D8 (marketplace/remove.ts:19-35 Flow + :111): rewrite the Flow
    header to match the current implementation --
    `resolveScopeOrNotifyNotAdded` + `withLockedStateTransaction` +
    `tx.save()`, include the CFG-03 gate and WB-01 write-back. Drop the
    pre-rewrite `resolveScopeFromState` / `withStateGuard` narration.
    Fix the stale ref at :111.

    D9 (reconcile/notify.ts:226-228, types.ts:9): change future-tense
    ("will land" / "future apply path") to present tense -- apply.ts
    exists.

    D10 (symbolic refs): replace hard line-number refs with symbolic
    `file.ts::symbol` form. Specific corrections from CONTEXT.md:
    - install.ts:1077 / :1212 -> point at `notify.ts::renderScopeBracket`
      (actual location notify.ts:1475) symbolically.
    - install.ts:1078 self-ref: drop.
    - install.ts:807 and uninstall.ts:633: replace wrong
      output-catalog.md section refs with the correct section names.
    - uninstall.ts:465 "shared.ts:339": replace with
      `shared.ts::<symbol>`.
    - install.ts:1214-1215 "uninstall.ts:298-302": symbolic.
    - apply.ts:34 external-dep line numbers: anchor to dep version, not
      lines.
    - apply.ts:68 "(mirrors preview.ts:60)": symbolic.

    D11 (policy violations -- strip planning-artifact tokens):
    - enable-disable.ts:35-38 Pitfall 54-2 landing-sequence -> strip ID,
      keep rationale.
    - reconcile/notify.ts:14-16 DIFF-01->DIFF-02 evolution note -> strip
      evolution narration, keep the current rule.
    - notify.ts:274 / :321 and apply-outcomes.ts:96 "RESEARCH Pattern 5
      Option A" -> strip planning-artifact ref, keep the rule.
    - apply.ts:31 "RESEARCH Assumption A1" -> strip.
    - preview.ts:69 "RESEARCH Security Threat Pattern" -> strip.
    - plan.ts:271-272 "Rule 2 deviation from the original plan's
      behavior block" -> strip.
    - plan.ts:25 "Pitfall 53-?" + apply.ts:91 "Pitfall 54-N" -> strip.
    - Bare "Pitfall 1/2/3/4/5/8" + "Pattern 3/4" in apply.ts and
      install.ts -> strip dead IDs; keep rationale.
    - uninstall.ts:70/:132, remove.ts:75/:125, enable-disable.ts:85/:146
      "byte-identical to today" -> "matches standalone behavior".
    - config-write-back.ts:30 "v1.12 config family" -> "schemaVersion-1
      config family".
    - config-merge.ts:139 "downstream phases" -> "downstream layers".
    - install.ts:1201-1217 and uninstall.ts:635-640 changelog-voice
      blocks -> one durable sentence each.

    D-MIG (LOCKED: persistence/migrate-config.ts:30-33): replace the
    comment with one that states the result is informational and callers
    intentionally discard it (NFR-2 load-time quiet). Do NOT add a
    notify call. Locate by symbol name, not by line number (line range
    may have shifted across prior plans).

    S1 (CLAUDE.md NFR-10 enumeration): add `<scopeRoot>/claude-plugins.json`
    and `<scopeRoot>/claude-plugins.local.json` to the sanctioned
    write-paths list so the config-file location does not read as
    containment widening (locations.ts:133 context).

    Comment policy preservation (.claude/rules/typescript-comments.md):
    KEEP requirement/decision IDs (`PRL-NN`, `AUTH-NN`, `DIFF-NN`,
    `ATTR-NN`, `RECON-NN`, `ENBL-NN`, `SPLIT-NN`, `WR-NN`, `CR-NN`,
    `UAT-NN`, `SNM-NN`, `TYPE-NN`, `SC-N`, `NFR-N`, `D-XX-NN`, `Mxx`).
    STRIP Phase/Plan/Wave/Task IDs and unresolvable Pitfall-N /
    Pattern-N / RESEARCH-* labels. The rule: planning artefact ->
    strip; specification anchor -> keep.
  </behavior>
  <action>
    Sequence:

    1. Walk D2-D10 in file order. For each, edit ONLY the cited comment
       span. Verify the symbolic-ref replacement actually exists in the
       target file at the time of edit -- don't trust the stale numbers
       in CONTEXT.md; grep the symbol name in the target file to
       confirm.
    2. Walk D11 across all listed sites. Keep all requirement/decision
       IDs (catalog above); strip Phase/Plan/Wave/Task + unresolvable
       Pitfall-N/Pattern-N/RESEARCH-* labels.
    3. Apply D-MIG comment edit -- locate the contract comment by
       symbol (`migrateFirstRunConfig` or `MigrateFirstRunResult`,
       whichever is closer).
    4. Update CLAUDE.md per S1: add the two claude-plugins paths to the
       NFR-10 list.
    5. Sanity grep before commit -- confirm NO surviving forbidden
       tokens in `src/`:
       `grep -RnE '(Phase [0-9]+|Plan [0-9]+|Wave [0-9]+|milestone v[0-9])' src/`
       Should return ONLY the whitelisted domain-language matches
       enumerated in `.claude/rules/typescript-comments.md`:
       - bridges/agents/stage.ts two-phase narration
       - "phase ledger" concept (install.ts neighbourhood)
       - "update phase 3" test-fixture strings (NOT in src/ -- ignore
         tests/)
       - URL version pins (e.g. #v1.0)
       Any OTHER match is a miss; fix and re-grep.
    6. Run catalog-uat byte gate. MUST be GREEN with NO docs/output-catalog.md
       edits.
    7. Run `npm run check` (GREEN -- comment-only edits don't change
       behaviour).
    8. Run `pre-commit run --files <changed>` until GREEN.
    9. Commit atomically. SKIP=trufflehog inside a worktree. Suggested
       title: `docs(reconcile): rewrite README + scrub planning-artifact comments`.

    ZERO source-logic edits in this commit. If you find yourself wanting
    to "just fix" a small logic issue, STOP -- that belongs in an earlier
    plan (and Plans 01-06 should have caught it).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    - `grep -RnE '(Phase [0-9]+|Plan [0-9]+|Wave [0-9]+)' src/` returns
      ONLY the whitelisted domain-language matches enumerated in
      `.claude/rules/typescript-comments.md`. Every Phase/Plan/Wave
      reference to GSD planning steps is gone.
    - persistence/migrate-config.ts contract comment states the silence
      is deliberate (NFR-2) and the result is informational (callers
      intentionally discard); no notify call added.
    - CLAUDE.md NFR-10 enumeration lists `claude-plugins.json` and
      `claude-plugins.local.json` alongside the existing sanctioned
      write paths.
    - catalog-uat byte gate GREEN; no rendered byte changed.
    - `npm run check` exit 0; `pre-commit run --files <changed>` GREEN.
    - One atomic Conventional Commit on `features/v1.12-config-files`.
    - ZERO source-logic edits in the commit diff (comments + markdown
      + CLAUDE.md only).
  </done>
</task>

</tasks>

<verification>
1. `npm run check` GREEN.
2. catalog-uat byte gate GREEN with NO docs/output-catalog.md edit.
3. `grep -RnE '(Phase [0-9]+|Plan [0-9]+|Wave [0-9]+)' src/` returns ONLY
   the whitelisted domain-language matches.
4. reconcile/README.md is rewritten in domain terms; every claim source-
   grounded against POST-Plan-06 code (`pluginsToEnable` IS populated; no
   `state.disabled`; all 7 files listed).
5. persistence/migrate-config.ts contract comment marks the silence as
   deliberate (NFR-2) and informational (D-MIG).
6. CLAUDE.md NFR-10 enumerates claude-plugins.json + claude-plugins.local.json
   (S1).
7. ZERO source-logic edits in the commit diff.
8. One atomic Conventional Commit on `features/v1.12-config-files`.
</verification>

<success_criteria>
- D1, D2, D3, D4, D5, D6, D7, D8, D9, D10, D11, S1, D-MIG are CLOSED by
  comment / docs edits.
- Commit is byte-neutral, source-logic-neutral, comments-only.
- `npm run check` GREEN; catalog-uat byte gate GREEN.
</success_criteria>

<output>
Append per-finding closure notes to
`.planning/quick/260612-bcs-fix-pr-51-five-agent-review-findings/260612-bcs-SUMMARY.md`
recording which commit closed D1-D11, S1, and D-MIG.
Then write the final block of the SUMMARY confirming all 53 finding IDs
have closure notes (cross-check against
`260612-bcs-INDEX.md` coverage table).
</output>
