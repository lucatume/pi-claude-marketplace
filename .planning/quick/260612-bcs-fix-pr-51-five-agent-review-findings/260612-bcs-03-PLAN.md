---
quick_task: 260612-bcs-fix-pr-51-five-agent-review-findings
sub_plan: 03
type: execute
wave: 3
depends_on: [260612-bcs-01, 260612-bcs-02]
files_modified:
  - src/orchestrators/reconcile/apply.ts
  - src/orchestrators/plugin/shared.ts
  - src/index.ts
  - tests/orchestrators/reconcile/apply.test.ts
autonomous: true
byte_contract: byte-neutral
requirements:
  - I6
  - S4
  - S6
  - Y7

must_haves:
  truths:
    - "reconcile apply classifies StateLockHeldError to 'lock held' and PluginShapeError to kind-mapped reasons (not-in-manifest / already-installed / unsupported); never falls back to {unreadable} for those cases."
    - "Three non-toggle orchestrated loops in apply.ts (at :245-248, :303-306, :349-352) adopt the import/execute.ts:613 fail-loud pattern when an orchestrated call returns no outcome; the cascade no longer silently swallows the row."
    - "synthesizeUndeclaredMarketplaceSource returning undefined either surfaces a row OR carries a decision-anchored comment at every call site referencing CONTEXT.md S4."
    - "index.ts:31 uses errorMessage(err) so non-Error throws render `reconcile aborted: <stringified>`, not `reconcile aborted: undefined`."
    - "All rendered bytes stay identical to pre-commit; catalog-uat byte gate GREEN with NO docs/output-catalog.md edit in this commit."
  artifacts:
    - path: src/orchestrators/reconcile/apply.ts
      provides: "classifyOrchestratorThrow handles StateLockHeldError + PluginShapeError; three loops adopt fail-loud pattern."
    - path: src/orchestrators/plugin/shared.ts
      provides: "Decision-anchored comment OR row surface for synthesizeUndeclaredMarketplaceSource returning undefined (per CONTEXT.md S4)."
    - path: src/index.ts
      provides: "errorMessage(err) at :31 (Y7)."
  key_links:
    - from: src/orchestrators/reconcile/apply.ts::classifyOrchestratorThrow
      to: src/import/execute.ts::dispatchFailedOutcome
      via: instanceof narrowing model for PluginShapeError + StateLockHeldError
      pattern: "PluginShapeError.*kind|StateLockHeldError"
    - from: src/orchestrators/reconcile/apply.ts (loops at :245-248, :303-306, :349-352)
      to: src/import/execute.ts:613
      via: fail-loud "returned no outcome in orchestrated mode" wording
      pattern: "returned no outcome in orchestrated mode"
---

<objective>
Close the remaining error-channel / silent-failure findings that DON'T edit
the enable-disable cluster (Plan 02): I6 (classifyOrchestratorThrow extension
for StateLockHeldError + PluginShapeError), S4 (synthesizeUndeclaredMarketplaceSource
silent undefined), S6 (three non-toggle outcome-less orchestrated loops in
apply.ts -- the fourth loop is covered by Y3 in Plan 05), Y7 (errorMessage at
index.ts:31).

Purpose: This is the reconcile-apply classification + small silent-failure
cleanup. Byte-neutral. Depends on Plans 01 + 02 having merged so apply.ts is
on the post-fix structure.

Output: One atomic Conventional Commit on `features/v1.12-config-files`,
byte-neutral, `npm run check` GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260612-bcs-fix-pr-51-five-agent-review-findings/260612-bcs-CONTEXT.md
@CLAUDE.md
@.claude/rules/typescript-comments.md

@src/orchestrators/reconcile/apply.ts
@src/orchestrators/plugin/shared.ts
@src/import/execute.ts
@src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests RED for I6, S4 (anchor), S6, Y7</name>
  <files>
    tests/orchestrators/reconcile/apply.test.ts
  </files>
  <behavior>
    Write one failing test per finding:

    - I6 pin: a config declaring a plugin absent from the manifest drives
      `classifyOrchestratorThrow` at `apply.ts:181-184, 420` to render
      `{not in manifest}`, not `{unreadable}`. Add a second sub-case for
      `StateLockHeldError` rendering `{lock held}`, and per-kind sub-cases
      for `PluginShapeError` rendering `{already installed}` and
      `{unsupported}`.
    - S4 anchor pin: at least one call site of
      `synthesizeUndeclaredMarketplaceSource` (`plugin/shared.ts:284-285`)
      asserts the chosen path (row OR documented decision comment present at
      both call sites). If the test asserts a row, the row must use existing
      catalog tokens.
    - S6 pin: a fixture that forces undefined return at ONE of the three
      non-toggle loops (`apply.ts:245-248`, `:303-306`, or `:349-352`)
      renders a row, not silence. The fourth loop (`:470-473`) is the
      setPluginEnabled toggle path -- it lands in Plan 05 with Y3.
    - Y7 pin: throw a non-Error (e.g. the literal string `"boom"`) through
      the path reaching `index.ts:31`. Assert: the rendered text contains
      `reconcile aborted: boom`, NOT `reconcile aborted: undefined`.

    Tests RED -- DO NOT touch source yet.
  </behavior>
  <action>
    Write tests using node:test + the existing harness. Reuse seed shapes
    from `apply.test.ts`. For PluginShapeError, mirror the seed used by
    `import/execute.ts::dispatchFailedOutcome` instanceof narrowing.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/reconcile/apply.test.ts 2>&1 | grep -E 'fail|pass' | head -20</automated>
  </verify>
  <done>
    - Failing tests for I6 (all 4 sub-cases), S4 (anchor), S6, Y7 exist.
    - Tests RED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement I6, S4, S6, Y7 (GREEN, byte-neutral)</name>
  <files>
    src/orchestrators/reconcile/apply.ts,
    src/orchestrators/plugin/shared.ts,
    src/index.ts
  </files>
  <behavior>
    - I6: extend `classifyOrchestratorThrow` at
      `reconcile/apply.ts:181-184, 420` to handle:
      * `StateLockHeldError -> "lock held"`
      * `PluginShapeError -> kind-mapped reasons` (`not-in-manifest`,
        `already-installed`, `unsupported`)
      Model: `import/execute.ts`'s `dispatchFailedOutcome` instanceof
      narrowing. Renderers reuse existing tokens (`lock held`,
      `not in manifest`, `already installed`, `unsupported`).
    - S4: in `plugin/shared.ts:284-285`, EITHER skip the config write and
      surface a row when synthesis returns undefined OR add a
      decision-anchored comment at the call sites referencing CONTEXT.md
      S4 + the existing :250-257 doc. The test in Task 1 anchors whichever
      path is taken. Prefer the surface path if structurally feasible
      within this PR's scope; otherwise document with reference to S4.
    - S6: for the THREE non-toggle loops at `apply.ts:245-248, :303-306,
      :349-352`, adopt the fail-loud pattern from `import/execute.ts:613`
      ("returned no outcome in orchestrated mode") so the row never
      silently vanishes. Reuse the import/execute.ts:613 wording verbatim
      so all four loops converge on identical text once Plan 05 lands
      the fourth (toggle) loop fix.
    - Y7: in `index.ts:31`, replace `(err as Error).message` with the
      existing `errorMessage(err)` helper.

    Byte rule: ZERO catalog edits. If any rendered byte shifts, that's a
    bug -- fix BEFORE commit.
  </behavior>
  <action>
    Sequence:
    1. Implement I6. Get all four I6 sub-cases GREEN.
    2. Implement S4 (prefer surface; fall back to documented decision
       comment if surface is structurally infeasible). Get S4 anchor test
       GREEN.
    3. Implement S6 for the three non-toggle loops. Get S6 test GREEN.
    4. Implement Y7 -- one-line swap. Get Y7 test GREEN.
    5. Run catalog-uat byte gate. MUST be GREEN with NO catalog edits.
    6. Run `npm run check` until GREEN.
    7. Run `pre-commit run --files <changed>` until GREEN.
    8. Commit atomically. SKIP=trufflehog inside a worktree. Suggested
       title: `fix(reconcile): classify lock/shape throws + surface silent loops`.

    Do NOT touch enable-disable.ts (Plan 02) or any catalog files.
    Do NOT touch type-design cuts (Plans 04, 05).
    Do NOT touch comments/docs (Plan 07).
    Do NOT touch the fourth toggle loop at apply.ts:470-473 -- that's
    Plan 05 with Y3.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    - All Task 1 tests GREEN.
    - `npm run check` exit 0.
    - `pre-commit run --files <changed>` GREEN.
    - One atomic Conventional Commit on `features/v1.12-config-files`.
    - catalog-uat byte gate GREEN with NO docs/output-catalog.md edits.
  </done>
</task>

</tasks>

<verification>
1. `npm run check` GREEN.
2. catalog-uat byte gate GREEN with NO docs/output-catalog.md edit.
3. I6 not-in-manifest / already-installed / unsupported / lock-held tests
   pass.
4. S4 call sites either render a row OR carry a CONTEXT.md S4 comment.
5. S6 forced-undefined fixture at one of the three loops renders a row.
6. Y7 string-throw renders `reconcile aborted: boom`.
7. One atomic Conventional Commit on `features/v1.12-config-files`.
</verification>

<success_criteria>
- I6, S4, S6, Y7 are CLOSED by code + tests.
- Commit is byte-neutral.
- `npm run check` GREEN.
- IL-2 preserved.
</success_criteria>

<output>
Append per-finding closure notes to
`.planning/quick/260612-bcs-fix-pr-51-five-agent-review-findings/260612-bcs-SUMMARY.md`
recording which commit closed I6, S4, S6, Y7.
</output>
