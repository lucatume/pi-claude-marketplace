---
quick_task: 260612-bcs-fix-pr-51-five-agent-review-findings
sub_plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orchestrators/marketplace/remove.ts
  - src/orchestrators/marketplace/autoupdate.ts
  - src/orchestrators/reconcile/apply.ts
  - src/persistence/config-io.ts
  - src/shared/notify.ts
  - docs/output-catalog.md
  - tests/orchestrators/marketplace/remove.test.ts
  - tests/orchestrators/marketplace/autoupdate.test.ts
  - tests/orchestrators/reconcile/apply.test.ts
  - tests/persistence/config-io.test.ts
  - tests/shared/catalog-uat.test.ts
autonomous: true
byte_contract: catalog-amending
requirements:
  - I1
  - I2
  - I5
  - S2
  - S3

must_haves:
  truths:
    - "Orchestrated partial marketplace-remove cascade renders every successfully-uninstalled plugin row AND every per-plugin failure (not just the first failure)."
    - "Autoupdate write-back skips are surfaced as failed/skipped rows; no name in finalResult.changed renders success when its write-back was silently skipped."
    - "loadConfig diagnostic detail (EACCES / JSON-parse / schema-key) reaches the rendered row's cause trailer instead of being flattened to {invalid manifest}, with absolute paths stripped (T-53-02-02)."
    - "reconcile-driven installs surface result.postCommitWarnings the same way import/execute.ts does."
    - "When the read-pass throw at apply.ts came from saveConfig inside migrateFirstRunConfig (EACCES on claude-plugins.json), the failure row names claude-plugins.json basename, not state.json."
    - "docs/output-catalog.md and tests/shared/catalog-uat.test.ts move in lockstep with every rendered-byte change in this commit; catalog-uat byte gate stays GREEN."
  artifacts:
    - path: src/orchestrators/marketplace/remove.ts
      provides: "Partial arm carrying unstaged successes + per-plugin failures from the tx outcome (CMC-31 PARTIAL shape)."
    - path: src/orchestrators/marketplace/autoupdate.ts
      provides: "writeAutoupdateBack returns skipped names; honest failed/skipped rows for skipped write-backs."
    - path: src/orchestrators/reconcile/apply.ts
      provides: "applyMarketplaceRemoves renders N rows for N plugins (not 1); postCommitWarnings surfaced from reconcile installs; read-pass attribution names the actual failing config file."
    - path: src/persistence/config-io.ts
      provides: "loadConfig diagnostic detail threaded to consumers via result.error; basename-only after T-53-02-02 redaction at the renderer."
    - path: docs/output-catalog.md
      provides: "Catalog amendments for any row bytes added/changed by I1 (multi-row partial), I2 (skipped name), I5 (cause trailer)."
  key_links:
    - from: src/orchestrators/marketplace/remove.ts
      to: src/orchestrators/reconcile/apply.ts::applyMarketplaceRemoves
      via: partial arm carries unstaged + per-plugin failures
      pattern: "applyMarketplaceRemoves.*partial"
    - from: src/persistence/config-io.ts::loadConfig
      to: src/orchestrators/reconcile/apply.ts (and other consumers)
      via: result.error threaded into row cause trailer (paths stripped)
      pattern: "loadConfig.*error.*cause"
    - from: docs/output-catalog.md
      to: tests/shared/catalog-uat.test.ts
      via: lockstep byte-equality amendment for I1/I2/I5 row additions
      pattern: "catalog-uat"
---

<objective>
Close the catalog-amending review findings from PR #51: I1 (orchestrated partial
marketplace-remove cascade), I2 (autoupdate skipped names rendered honestly),
I5 (loadConfig diagnostic threading through cause trailer), plus S2
(postCommitWarnings surfaced from reconcile installs) and S3 (read-pass
attribution names the actual failing config file).

Purpose: These findings share a single property -- they add or shift rendered
bytes. Grouping them keeps docs/output-catalog.md + catalog-uat amendments in
ONE commit instead of scattered across many. Every other plan in this split
stays byte-neutral.

Output: One atomic Conventional Commit on `features/v1.12-config-files` with
behavior fixes + matching tests + docs/output-catalog.md + catalog-uat
fixtures moved in lockstep. `npm run check` GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260612-bcs-fix-pr-51-five-agent-review-findings/260612-bcs-CONTEXT.md
@CLAUDE.md
@.claude/rules/typescript-comments.md
@docs/output-catalog.md
@docs/messaging-style-guide.md

# Finding-relevant source files (load only the spans you are editing)
@src/orchestrators/marketplace/remove.ts
@src/orchestrators/marketplace/autoupdate.ts
@src/orchestrators/reconcile/apply.ts
@src/orchestrators/reconcile/notify.ts
@src/orchestrators/plugin/install.ts
@src/persistence/config-io.ts
@src/persistence/migrate-config.ts
@src/shared/notify.ts
@src/import/execute.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests RED for I1, I2, I5, S2, S3</name>
  <files>
    tests/orchestrators/marketplace/remove.test.ts,
    tests/orchestrators/marketplace/autoupdate.test.ts,
    tests/orchestrators/reconcile/apply.test.ts,
    tests/persistence/config-io.test.ts
  </files>
  <behavior>
    Each finding gets at least one new failing test pinning the post-fix
    contract.

    - I1 pin: a multi-plugin marketplace where N-1 uninstalls succeed and the
      Nth fails renders N rows (N-1 success + 1 failure), not 1. Seed the
      transaction outcome shape that `marketplace/remove.ts:268-277` produces
      and drive it through `applyMarketplaceRemoves`
      (`reconcile/apply.ts:268-275`). Assert every successfully-uninstalled
      plugin gets a success row AND the failing plugin gets a failed row.
    - I2 pin: autoupdate where a name in `finalResult.changed` cannot have its
      source synthesized renders a failed or skipped row, not silent success.
      Seed `writeAutoupdateBack` (`autoupdate.ts:484-501`) to return a non-empty
      `skipped: string[]`; assert the final notify
      (`autoupdate.ts:343-347`) demotes those names out of success.
    - I5 pin: at least one `loadConfig` consumer (pick EACCES via chmod-0 path
      OR malformed schema key) renders a cause trailer carrying the granular
      reason (`permission denied` / `JSON parse error` / specific schema key)
      AND basename-only path tokens. Drive through any consumer surface listed
      in CONTEXT.md I5 (`reconcile/apply.ts:152-169` is the easiest to wire).
    - S2 pin: a reconcile-driven install whose
      result carries a `postCommitWarning` renders that warning row through
      `applyReconcile`, mirroring `import/execute.ts:699-703`.
    - S3 pin: claude-plugins.json is unwritable so `migrateFirstRunConfig`'s
      inner `saveConfig` throws EACCES; the resulting failure row names
      claude-plugins.json basename, not state.json (`apply.ts:596-603`).

    Use node:test + the existing harness/fixtures. Mirror seed shapes used in
    neighbouring tests already in the same file. Tests run RED at this stage
    -- DO NOT touch source yet.
  </behavior>
  <action>
    Write one test per finding listed above. Each test:
    1. Seeds a fixture that drives the finding's code path.
    2. Asserts the post-fix observable behavior (rows rendered, basename-only
       paths, no silent skip).
    3. Runs RED today (no production code edits yet).

    For I5, use the existing T-53-02-02 path-redactor helper found in
    `shared/notify.ts` (search for the basename-only enforcement) as the
    contract: the cause trailer keeps the diagnostic but strips absolute paths.

    For S3, use `await fs.chmod(path, 0)` portably on the directory containing
    `claude-plugins.json` to provoke EACCES on save; remember to restore mode
    in the test cleanup so the suite stays portable.

    Do NOT run `npm run check` yet -- it should fail.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/marketplace/remove.test.ts tests/orchestrators/marketplace/autoupdate.test.ts tests/orchestrators/reconcile/apply.test.ts tests/persistence/config-io.test.ts 2>&1 | grep -E 'fail|pass' | head -20</automated>
  </verify>
  <done>
    - One new failing test exists per finding (I1, I2, I5, S2, S3).
    - Each test fixture matches the seed shape documented in the relevant
      neighbouring test in the same file.
    - All five tests are RED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement I1, I2, I5, S2, S3 source changes (GREEN) + catalog amendments</name>
  <files>
    src/orchestrators/marketplace/remove.ts,
    src/orchestrators/marketplace/autoupdate.ts,
    src/orchestrators/reconcile/apply.ts,
    src/persistence/config-io.ts,
    src/shared/notify.ts,
    docs/output-catalog.md,
    tests/shared/catalog-uat.test.ts
  </files>
  <behavior>
    Implement each finding to flip the Task 1 tests to GREEN:

    - I1: add a partial arm to `applyMarketplaceRemoves`
      (`reconcile/apply.ts:268-275`) mirroring the standalone CMC-31 PARTIAL
      shape, carrying both `unstaged` (success rows) AND per-plugin failures
      from `marketplace/remove.ts:268-277`'s tx outcome. Render one success
      row per unstaged plugin + one failed row per failed plugin.
    - I2: `writeAutoupdateBack` (`autoupdate.ts:484-501`) returns a
      `skipped: string[]` of names whose source could not be synthesized.
      Demote those names from `finalResult.changed`
      (`autoupdate.ts:343-347`) into honest failed or skipped rows.
    - I5: at every consumer surface listed in CONTEXT.md I5
      (`reconcile/apply.ts:152-169`, `reconcile/preview.ts:72-80`,
      `enable-disable.ts:507/618`, `install.ts:1019`, `uninstall.ts:296`,
      `marketplace/remove.ts:543`, `marketplace/autoupdate.ts:373`,
      `migrate-config.ts:151-158`), thread `loadConfig`'s `result.error` into
      the rendered row's `cause` trailer AFTER stripping absolute paths via
      the existing T-53-02-02 redactor in `shared/notify.ts`. Do NOT strip
      the parse/permission detail itself.
    - S2: in `reconcile/apply.ts:389-433`, surface
      `result.postCommitWarnings` the same way `import/execute.ts:699-703`
      does.
    - S3: in `apply.ts:596-603`, when the throw came from `saveConfig` inside
      `migrateFirstRunConfig` (EACCES on claude-plugins.json), attribute the
      failure to the config basename, not state.json.

    Catalog rule (closed-set, mandatory in THIS commit):
    - If I1, I2, OR I5 changed rendered bytes -- and they almost certainly
      will -- amend `docs/output-catalog.md` (relevant section) AND
      `tests/shared/catalog-uat.test.ts` fixtures in the same commit.
    - Prefer reusing existing tokens before introducing new ones. If a new
      token is unavoidable, add it to the catalog and pin it in catalog-uat.
    - Every other rendered byte stays identical. catalog-uat byte gate must
      stay GREEN.
  </behavior>
  <action>
    Sequence:

    1. Implement I1 first -- modify the partial arm in
       `applyMarketplaceRemoves`. Get the I1 test GREEN. Update catalog if the
       multi-row partial introduces new fixture bytes.
    2. Implement I2 next -- thread skipped names through `writeAutoupdateBack`
       to the final notify. Get the I2 test GREEN. Update catalog if a new
       skipped-row token appears.
    3. Implement I5 across all consumer surfaces listed above. Get the I5 test
       GREEN. Update catalog for the cause-trailer bytes.
    4. Implement S2 -- mirror `import/execute.ts:699-703`. Get S2 test GREEN.
    5. Implement S3 -- correctly attribute the basename in the read-pass throw
       arm. Get S3 test GREEN.
    6. Run the catalog-uat byte gate explicitly:
       `node --test tests/shared/catalog-uat.test.ts`. It MUST be GREEN.
       If any rendered byte shifted without a docs/output-catalog.md update,
       fix the catalog first.
    7. Run `npm run check` until GREEN (typecheck + ESLint + Prettier + full
       test suite).
    8. Run `pre-commit run --files <changed files>` until GREEN.
    9. Commit atomically (single Conventional Commit). If inside a worktree,
       prefix `SKIP=trufflehog`. Suggested title:
       `fix(reconcile): render every cascade row + thread loadConfig detail`.

    Do NOT touch C1, I3, I4 (those edit enable-disable.ts -- Plan 02).
    Do NOT touch I6, Y7, S4, S6 (those are Plan 03).
    Do NOT touch any type-design cuts (Plans 04, 05).
    Do NOT touch comments/docs (Plan 07).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    - All Task 1 tests GREEN.
    - `npm run check` exit 0.
    - `pre-commit run --files <changed>` GREEN.
    - One atomic Conventional Commit on `features/v1.12-config-files`.
    - docs/output-catalog.md amended in lockstep iff I1/I2/I5 shifted bytes;
      tests/shared/catalog-uat.test.ts byte gate GREEN.
    - No raw absolute path appears in any rendered cause trailer (I5
      contract preserved).
  </done>
</task>

</tasks>

<verification>
1. `npm run check` -- typecheck + ESLint + Prettier + full test suite GREEN.
2. Catalog-uat byte gate GREEN (tests/shared/catalog-uat.test.ts).
3. I1 multi-row partial test renders N rows for N plugins (not 1).
4. I2 skipped-name test renders a row, not silent success.
5. I5 cause-trailer test carries the granular reason AND basename-only paths.
6. S2 postCommitWarnings test renders the warning row.
7. S3 EACCES-on-claude-plugins.json test names the config basename.
8. One atomic Conventional Commit on `features/v1.12-config-files`, title
   <=72 chars, body lines <=80 chars, pre-commit hooks GREEN.
</verification>

<success_criteria>
- Every finding in this plan (I1, I2, I5, S2, S3) is closed by code + tests +
  any required catalog amendment.
- docs/output-catalog.md + catalog-uat byte gate move in lockstep ONLY for
  the row changes introduced here; everything else stays byte-identical.
- IL-2 preserved (output through notify); T-53-02-02 preserved (basename-only
  in cause trailers); NFR-6 preserved (`npm run check` GREEN).
</success_criteria>

<output>
Append a per-finding closure note to
`.planning/quick/260612-bcs-fix-pr-51-five-agent-review-findings/260612-bcs-SUMMARY.md`
(create if it does not exist) recording which commit closed each of I1, I2,
I5, S2, S3 and any catalog rows added.
</output>
