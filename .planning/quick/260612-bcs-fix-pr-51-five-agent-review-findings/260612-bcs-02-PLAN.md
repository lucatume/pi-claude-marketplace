---
quick_task: 260612-bcs-fix-pr-51-five-agent-review-findings
sub_plan: 02
type: execute
wave: 2
depends_on: [260612-bcs-01]
files_modified:
  - src/orchestrators/plugin/enable-disable.ts
  - src/orchestrators/plugin/update.ts
  - src/orchestrators/plugin/reinstall.ts
  - src/orchestrators/marketplace/remove.ts
  - src/orchestrators/marketplace/autoupdate.ts
  - src/edge/handlers/plugin/enable-disable.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
  - tests/orchestrators/plugin/update.test.ts
  - tests/orchestrators/marketplace/remove.test.ts
  - tests/orchestrators/marketplace/autoupdate.test.ts
  - tests/edge/handlers/plugin/enable-disable.test.ts
autonomous: true
byte_contract: byte-neutral
requirements:
  - C1
  - I3
  - I4
  - S5
  - D-UPD
  - D-NCF
  - T2

must_haves:
  truths:
    - "setPluginEnabled honors its never-rethrows contract even when state.json is corrupt in either scope; the edge handler emits a notify row, not a raw throw."
    - "Disable cascade partial failure folds dropped artefacts into state before surfacing the failure (state.json never claims artefacts that are gone)."
    - "Enable failure renders rollback-partial recovery rows by threading InstallFailureCapture through runInstallLedger."
    - "plugin update and marketplace autoupdate refresh a disabled plugin's record (version/source pin) but keep the plugin disabled with empty resources (D-UPD)."
    - "marketplace/remove.ts narrowCascadeFailure maps AgentsUnstageFailureError to 'source mismatch' (aligned with uninstall.ts ATTR-09, D-NCF)."
    - "reinstall.ts:1090-1092 and update.ts:1032-1034 no longer silently skip the write-back on invalid config (S5)."
    - "T2 update-vs-disabled is pinned with a disabled-record seed test in update.test.ts AND an autoupdate cascade test."
    - "All rendered bytes stay identical to pre-commit; catalog-uat byte gate GREEN with NO docs/output-catalog.md edit in this commit."
  artifacts:
    - path: src/orchestrators/plugin/enable-disable.ts
      provides: "try/catch wrap of resolveCrossScopePluginTarget routing through classifyTransactionThrow; InstallFailureCapture threaded on enable; dropped-fold on disable partial cascade."
    - path: src/edge/handlers/plugin/enable-disable.ts
      provides: "catch routes through notify rendering the failed outcome; no raw throw escapes to the caller."
    - path: src/orchestrators/plugin/update.ts
      provides: "Disabled-record guard: refresh record (version/source pin) without re-materialization; resources.* stay empty (D-UPD)."
    - path: src/orchestrators/marketplace/autoupdate.ts
      provides: "Disabled-record refresh keeps the plugin disabled in the autoupdate cascade (D-UPD)."
    - path: src/orchestrators/marketplace/remove.ts
      provides: "narrowCascadeFailure AgentsUnstageFailureError -> 'source mismatch' (D-NCF)."
    - path: src/orchestrators/plugin/reinstall.ts
      provides: "Invalid-config arm at :1090-1092 surfaces the failure (not silent skip) consistent with the sibling CFG-03 abort (S5)."
  key_links:
    - from: src/edge/handlers/plugin/enable-disable.ts
      to: src/orchestrators/plugin/enable-disable.ts::setPluginEnabled
      via: try/catch wrapping resolveCrossScopePluginTarget routes through classifyTransactionThrow
      pattern: "setPluginEnabled.*classifyTransactionThrow"
    - from: src/orchestrators/plugin/update.ts
      to: src/orchestrators/reconcile/plan.ts::isRecordedButDisabled
      via: disabled-record guard before re-materialization
      pattern: "isRecordedButDisabled|isCurrentlyDisabled"
    - from: src/orchestrators/marketplace/remove.ts::narrowCascadeFailure
      to: src/orchestrators/plugin/uninstall.ts (ATTR-09 mapping)
      via: AgentsUnstageFailureError -> source mismatch
      pattern: "AgentsUnstageFailureError.*source mismatch"
---

<objective>
Close the enable-disable behavior findings (C1 never-rethrows, I3 dropped-fold,
I4 InstallFailureCapture thread) along with the two locked decisions that edit
the same neighbourhood (D-UPD update-vs-disabled, D-NCF narrowCascadeFailure
align) plus S5 (reinstall/update invalid-config consistency) and T2's
update-vs-disabled behavior test.

Purpose: This is the enable-disable / update / autoupdate edit cluster. It
MUST land before Plan 05's setPluginEnabled overload (Y3), because Y3 builds
on the try/catch structure C1 introduces. The whole commit is byte-neutral --
no catalog edits permitted.

Output: One atomic Conventional Commit on `features/v1.12-config-files`,
byte-neutral, `npm run check` GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260612-bcs-fix-pr-51-five-agent-review-findings/260612-bcs-CONTEXT.md
@CLAUDE.md
@.claude/rules/typescript-comments.md
@docs/output-catalog.md
@docs/messaging-style-guide.md

# Finding-relevant source files
@src/orchestrators/plugin/enable-disable.ts
@src/orchestrators/plugin/install.ts
@src/orchestrators/plugin/uninstall.ts
@src/orchestrators/plugin/update.ts
@src/orchestrators/plugin/reinstall.ts
@src/orchestrators/plugin/shared.ts
@src/orchestrators/marketplace/remove.ts
@src/orchestrators/marketplace/autoupdate.ts
@src/orchestrators/reconcile/preview.ts
@src/orchestrators/reconcile/plan.ts
@src/edge/handlers/plugin/enable-disable.ts
@src/shared/notify.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests RED for C1, I3, I4, D-UPD, D-NCF, S5</name>
  <files>
    tests/orchestrators/plugin/enable-disable.test.ts,
    tests/orchestrators/plugin/update.test.ts,
    tests/orchestrators/marketplace/remove.test.ts,
    tests/orchestrators/marketplace/autoupdate.test.ts,
    tests/edge/handlers/plugin/enable-disable.test.ts
  </files>
  <behavior>
    Write one new failing test per finding, pinning the post-fix contract.

    - C1 pin (orchestrated + edge handler):
      * Orchestrated: seed a corrupt state.json (invalid JSON) in either scope
        and call `setPluginEnabled` in orchestrated mode. Assert: returns a
        FAILED outcome row (no throw escapes), exactly zero process.stdout
        writes, and basename-only path tokens in the rendered cause trailer
        (T-53-02-02).
      * Edge handler: same fixture driven through
        `edge/handlers/plugin/enable-disable.ts:48-57`. Assert: exactly one
        notify call rendering the failed row; no exception escapes.
    - I3 pin: drive a disable cascade that produces a non-empty
      `cascade.dropped` AND a partial-failure outcome
      (`enable-disable.ts:252-259`). Assert: after the call, state.json no
      longer references the dropped artefacts AND the failed row still
      renders. Mirror the TR-03 fold from `uninstall.ts:468-484`.
    - I4 pin: drive an enable failure where `runInstallLedger`'s
      rollback-partial recovery captures non-empty residue
      (`enable-disable.ts:206-213`). Assert: the failed row renders the
      rollback-partials trailer (matching install/uninstall pattern via
      `composeInstallFailureMessage` -- `install.ts:343-352`, used at
      install.ts:810/:967), not a bare failed row.
    - D-UPD pin (T2 behavior side): in
      `tests/orchestrators/plugin/update.test.ts`, seed a disabled record
      (populated config entry + empty `resources.*` + `installable: true`).
      Call `pluginUpdate`. Assert: the record's version and source pin
      refresh BUT `resources.*` stay empty AND the rendered status reuses an
      existing success-shape token (NO new catalog token). Add an analogous
      autoupdate cascade test in
      `tests/orchestrators/marketplace/autoupdate.test.ts` with the same
      disabled-record seed.
    - D-NCF pin: in `tests/orchestrators/marketplace/remove.test.ts`,
      seed a cascade that throws `AgentsUnstageFailureError`. Assert:
      `narrowCascadeFailure` (`remove.ts:159-165`) renders
      `"source mismatch"` (matching uninstall.ts's ATTR-09 mapping), not
      `"not in manifest"`.
    - S5 pin: in `tests/orchestrators/plugin/update.test.ts` (or wherever
      the reinstall/update invalid-config path is exercised), assert the
      invalid-config arm at `reinstall.ts:1090-1092` and `update.ts:1032-1034`
      no longer renders success when the write-back was skipped. Pick the
      same shape as the closest sibling CFG-03 abort.

    All tests RED -- DO NOT touch source yet.
  </behavior>
  <action>
    Write each test using node:test + the existing harness. Reuse seed shapes
    from neighbouring tests in the same file. Tests run RED.

    For the corrupt-state.json fixture (C1), write the literal string
    `{ not json ` (no closing brace) so `loadState` throws on parse and
    `resolveCrossScopePluginTarget` propagates pre-try/catch.

    For I3, seed an `agent-staging/` directory that triggers cascade.dropped
    on disable by leaving an orphan agent .md in user scope (matches the
    TR-03 fold seed in `tests/orchestrators/plugin/uninstall.test.ts`).

    For I4, seed an enable path where the install ledger produces a partial
    rollback residue (match the install.ts:810/:967 seed in
    `tests/orchestrators/plugin/install.test.ts`).

    For D-UPD, the disabled record seed is exactly: config entry present
    + `state.records[name].resources.*` all empty + `installable: true`.

    Do NOT run `npm run check` yet.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/enable-disable.test.ts tests/orchestrators/plugin/update.test.ts tests/orchestrators/marketplace/remove.test.ts tests/orchestrators/marketplace/autoupdate.test.ts tests/edge/handlers/plugin/enable-disable.test.ts 2>&1 | grep -E 'fail|pass' | head -30</automated>
  </verify>
  <done>
    - Tests RED for: C1 (orchestrated + edge), I3, I4, D-UPD (update +
      autoupdate cascade), D-NCF, S5.
    - Seed shapes match neighbouring tests in the same files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement C1, I3, I4, D-UPD, D-NCF, S5 (GREEN, byte-neutral)</name>
  <files>
    src/orchestrators/plugin/enable-disable.ts,
    src/orchestrators/plugin/update.ts,
    src/orchestrators/plugin/reinstall.ts,
    src/orchestrators/marketplace/remove.ts,
    src/orchestrators/marketplace/autoupdate.ts,
    src/edge/handlers/plugin/enable-disable.ts
  </files>
  <behavior>
    Implement each finding to flip Task 1 tests GREEN.

    - C1: wrap `resolveCrossScopePluginTarget`
      (`enable-disable.ts:292-297`, BEFORE the existing try/catch) so a
      throw from `loadState` is routed through `classifyTransactionThrow`
      (already at `enable-disable.ts:480`) to a `(failed)` row / typed
      outcome. Model the try/catch ladder on
      `reconcile/preview.ts:158-170`. In
      `edge/handlers/plugin/enable-disable.ts:48-57` add a catch that calls
      `notify` with the existing severity routing -- NO new branches in
      notify.ts. Rendered bytes for the failed row reuse existing tokens.
    - I3 (disable dropped-fold): when `cascade.dropped` is non-empty on
      disable partial failure (`enable-disable.ts:252-259`), fold the
      dropped artefacts into the record, save the shrunken record, THEN
      surface the failure -- mirror the TR-03 fold from
      `uninstall.ts:468-484`.
    - I4 (enable InstallFailureCapture thread): pass a fresh
      `InstallFailureCapture` as the 4th arg to `runInstallLedger` on the
      enable branch (`enable-disable.ts:206-213`) and render
      `rollbackPartials` into the failed row via the existing
      `composeInstallFailureMessage` (`install.ts:343-352`) call shape used
      at install.ts:810/:967.
    - D-UPD (LOCKED decision): in `plugin/update.ts` and the autoupdate
      cascade path, when the target plugin is currently disabled
      (`isCurrentlyDisabled`: populated config entry + empty `resources.*`),
      refresh the record (version pin / source pin) but DO NOT
      re-materialize artefacts; `resources.*` stay empty. Render the
      existing success-shape token (NO new catalog token).
    - D-NCF (LOCKED decision): in `marketplace/remove.ts:159-165`, the
      `narrowCascadeFailure` switch on `AgentsUnstageFailureError` returns
      `"source mismatch"` (ATTR-09 align with uninstall.ts).
    - S5: in `reinstall.ts:1090-1092` and `update.ts:1032-1034`, replace
      the silent skip-of-write-back with the loud CFG-03 abort the
      siblings perform (or at minimum append a warning row). Pick the same
      shape as the closest sibling.

    Catalog rule: ZERO new tokens here. If any rendered byte shifts, that's
    a bug -- fix the renderer to preserve bytes BEFORE commit. The
    catalog-uat byte gate must be GREEN with NO docs/output-catalog.md
    edits in this commit.
  </behavior>
  <action>
    Sequence:

    1. Implement C1 first -- introduce the outer try/catch and the edge
       handler catch. Get C1 tests GREEN.
    2. Implement I3 -- the disable dropped-fold mirroring uninstall.ts. Get
       I3 test GREEN.
    3. Implement I4 -- thread InstallFailureCapture. Get I4 test GREEN.
    4. Implement D-UPD in update.ts -- introduce/reuse a disabled-record
       guard (refresh-only, no re-materialization) and ensure
       `resources.*` stay empty. Apply the same guard in the autoupdate
       cascade path. Get D-UPD tests GREEN.
    5. Implement D-NCF -- the one-line switch change in
       `marketplace/remove.ts:159-165`. Get D-NCF test GREEN.
    6. Implement S5 -- align reinstall.ts:1090-1092 and update.ts:1032-1034
       with the sibling CFG-03 shape. Get S5 test GREEN.
    7. Run catalog-uat byte gate:
       `node --test tests/shared/catalog-uat.test.ts`. MUST be GREEN with
       NO catalog edits.
    8. Run `npm run check` until GREEN.
    9. Run `pre-commit run --files <changed files>` until GREEN.
    10. Commit atomically. SKIP=trufflehog inside a worktree. Suggested
        title: `fix(plugin): close enable-disable cascade + update-disabled gaps`.

    Do NOT touch I1, I2, I5, S2, S3 (Plan 01).
    Do NOT touch I6, Y7, S4, S6 (Plan 03).
    Do NOT touch type cuts (Plans 04, 05).
    Do NOT touch comments/docs (Plan 07).
    Do NOT add Y3's setPluginEnabled overload -- the overload depends on
    C1's try/catch structure being merged first; Y3 lands in Plan 05.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    - All Task 1 tests GREEN.
    - `npm run check` exit 0.
    - `pre-commit run --files <changed>` GREEN.
    - One atomic Conventional Commit on `features/v1.12-config-files`.
    - catalog-uat byte gate GREEN with NO docs/output-catalog.md edits in
      this commit.
    - No raw throw escapes `setPluginEnabled` in orchestrated mode; no
      leaked absolute paths in the failed-row trailer.
  </done>
</task>

</tasks>

<verification>
1. `npm run check` GREEN.
2. catalog-uat byte gate GREEN with NO docs/output-catalog.md edit.
3. C1 corrupt-state test confirms no throw escapes setPluginEnabled in
   orchestrated mode (and zero stdout writes through the edge handler).
4. I3 test confirms state.json drops the cascade.dropped artefacts before
   the failed row renders.
5. I4 test renders the rollback-partials trailer matching install/uninstall.
6. D-UPD (T2 behavior side) confirms refresh-but-keep-disabled with
   `resources.*` empty in both `update.test.ts` and `autoupdate.test.ts`.
7. D-NCF test pins AgentsUnstageFailureError -> "source mismatch".
8. S5 test confirms invalid-config no longer renders silent success.
9. One atomic Conventional Commit on `features/v1.12-config-files`.
</verification>

<success_criteria>
- C1, I3, I4, S5, D-UPD, D-NCF, T2 (behavior side) are CLOSED by code + tests.
- Commit is byte-neutral (no catalog edits).
- `npm run check` GREEN; catalog-uat byte gate GREEN.
- IL-2 preserved (output through notify); NFR-3 preserved (fail-clean state
  via the dropped-fold); T-53-02-02 preserved (basename-only paths).
</success_criteria>

<output>
Append per-finding closure notes to
`.planning/quick/260612-bcs-fix-pr-51-five-agent-review-findings/260612-bcs-SUMMARY.md`
recording which commit closed C1, I3, I4, S5, D-UPD, D-NCF, T2 (behavior).
</output>
