---
quick_task: 260612-bcs-fix-pr-51-five-agent-review-findings
sub_plan: 04
type: execute
wave: 4
depends_on: [260612-bcs-01, 260612-bcs-02, 260612-bcs-03]
files_modified:
  - src/domain/source.ts
  - src/orchestrators/reconcile/types.ts
  - src/orchestrators/reconcile/apply-outcomes.ts
  - src/orchestrators/reconcile/apply.ts
  - src/orchestrators/reconcile/plan.ts
  - src/orchestrators/reconcile/notify.ts
  - src/persistence/migrate-config.ts
  - tests/orchestrators/reconcile/apply.test.ts
  - tests/orchestrators/reconcile/plan.test.ts
autonomous: true
byte_contract: byte-neutral
requirements:
  - Y1
  - Y2
  - Y4
  - Y5
  - Y6

must_haves:
  truths:
    - "samePlannedSource returns 'same' | 'different' | 'unknown-stored' (no truthy-coercion misreads a corrupt record as a source match)."
    - "PlannedSourceMismatch is widened to four causes (source-mismatch / unknown-stored / dangling-reference / malformed-plugin-key) with per-cause variants; rendered output stays byte-identical."
    - "InvalidBlockOutcome's basename-carrying field is renamed (no longer punned `marketplace`); the renderer follows."
    - "MigrateFirstRunResult.error exists only on the existing-invalid arm of the reason discriminant."
    - "PluginToggleAxes.successStatus is derived from `enable` inside applyPluginToggles; the redundant axis is dropped."
    - "All rendered bytes stay identical; catalog-uat byte gate GREEN with NO docs/output-catalog.md edit in this commit."
  artifacts:
    - path: src/domain/source.ts
      provides: "samePlannedSource tri-state result; callers updated."
    - path: src/orchestrators/reconcile/types.ts
      provides: "PlannedSourceMismatch widened to 4 causes; sentinel `<marketplace not declared>` replaced with discriminant."
    - path: src/orchestrators/reconcile/apply-outcomes.ts
      provides: "SourceMismatchOutcome propagates new variants; InvalidBlockOutcome field rename."
    - path: src/orchestrators/reconcile/notify.ts
      provides: "Renderer arms at :143 derive original strings from the new variants (byte-identical)."
    - path: src/orchestrators/reconcile/apply.ts
      provides: "Renderer arm at :515 derives original strings from the new variants; applyPluginToggles derives successStatus from enable (Y6)."
    - path: src/persistence/migrate-config.ts
      provides: "MigrateFirstRunResult error only on existing-invalid arm (Y5 discriminant cut)."
  key_links:
    - from: src/domain/source.ts::samePlannedSource
      to: src/orchestrators/reconcile/plan.ts:138, :192
      via: tri-state switch replaces `=== true`
      pattern: "samePlannedSource.*same|samePlannedSource.*unknown-stored"
    - from: src/orchestrators/reconcile/types.ts::PlannedSourceMismatch
      to: src/orchestrators/reconcile/apply-outcomes.ts::SourceMismatchOutcome
      via: per-cause variant propagation
      pattern: "dangling-reference|malformed-plugin-key"
---

<objective>
Apply the type-design cuts that DO NOT touch enable-disable.ts (those are
Plan 05). All five cuts here are renderer-preserving: rendered output stays
byte-identical to pre-commit. Commit is byte-neutral.

- Y1: samePlannedSource tri-state union
- Y2: PlannedSourceMismatch widening to 4 causes
- Y4: InvalidBlockOutcome.marketplace -> basename rename
- Y5: MigrateFirstRunResult discriminant cut
- Y6: PluginToggleAxes.successStatus derivation

Purpose: Y1 in particular is a high-value safety cut -- truthy-coercion of
the existing `boolean | "unknown-stored"` shape silently misreads a corrupt
record as a source match. The TypeScript compiler enumerates every shadow
caller; that's the value of doing Y1 early in this commit.

Output: One atomic Conventional Commit on `features/v1.12-config-files`,
byte-neutral, `npm run check` GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260612-bcs-fix-pr-51-five-agent-review-findings/260612-bcs-CONTEXT.md
@CLAUDE.md
@.claude/rules/typescript-comments.md

@src/domain/source.ts
@src/orchestrators/reconcile/types.ts
@src/orchestrators/reconcile/apply-outcomes.ts
@src/orchestrators/reconcile/apply.ts
@src/orchestrators/reconcile/plan.ts
@src/orchestrators/reconcile/notify.ts
@src/persistence/migrate-config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Renderer byte-equality tests for Y2 variants (RED until cuts land)</name>
  <files>
    tests/orchestrators/reconcile/apply.test.ts,
    tests/orchestrators/reconcile/plan.test.ts
  </files>
  <behavior>
    Add per-variant renderer tests asserting the same output strings the
    previous fused shape produced. For each new Y2 cause -- `source-mismatch`,
    `unknown-stored`, `dangling-reference`, `malformed-plugin-key` --
    assert the rendered row bytes match the pre-cut output for that cause
    (the fixtures in catalog-uat already encode the pre-cut bytes for the
    fused 2-discriminant shape).

    For Y1, add a unit test of `samePlannedSource` asserting exhaustive
    switch coverage: the three return values map to the existing pre-cut
    behaviour at every comparison site (`plan.ts:138, :192`).

    For Y5, add a narrowing test asserting that reading `error` on a
    non-`existing-invalid` arm is a TYPE error (use `// @ts-expect-error`
    inside the test file).

    For Y6, no new test required (axis-derivation is a refactor that the
    existing toggle tests cover end-to-end).

    Tests RED until cuts land.
  </behavior>
  <action>
    Mirror seed shapes from `apply.test.ts` and `plan.test.ts`. The Y2
    per-variant tests should be a parameterized table over the four
    causes asserting byte-equal rendered output.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/reconcile/apply.test.ts tests/orchestrators/reconcile/plan.test.ts 2>&1 | grep -E 'fail|pass' | head -20</automated>
  </verify>
  <done>
    - Y2 per-variant byte-equality tests exist.
    - Y1 tri-state switch test exists.
    - Y5 `@ts-expect-error` narrowing test exists.
    - Tests RED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply Y1, Y2, Y4, Y5, Y6 (GREEN, byte-neutral)</name>
  <files>
    src/domain/source.ts,
    src/orchestrators/reconcile/types.ts,
    src/orchestrators/reconcile/apply-outcomes.ts,
    src/orchestrators/reconcile/apply.ts,
    src/orchestrators/reconcile/plan.ts,
    src/orchestrators/reconcile/notify.ts,
    src/persistence/migrate-config.ts
  </files>
  <behavior>
    - Y1 (samePlannedSource tri-state): `domain/source.ts::samePlannedSource`
      returns `"same" | "different" | "unknown-stored"` instead of
      `boolean | "unknown-stored"`. Update every call site -- including
      `plan.ts:138, :192` where `=== true` exists today -- to switch on the
      new union. Truthy-coercion of `"unknown-stored"` becomes a type
      error.
    - Y2 (PlannedSourceMismatch widening): in `reconcile/types.ts:137-150`,
      widen `cause` to
      `"source-mismatch" | "unknown-stored" | "dangling-reference" | "malformed-plugin-key"`
      with per-cause variants:
      * `plugin` REQUIRED on `dangling-reference`
      * `rawKey` (NOT `marketplace`-punned) REQUIRED on
        `malformed-plugin-key`
      Propagate to `SourceMismatchOutcome` in
      `apply-outcomes.ts:130-134` and the renderer arms at
      `notify.ts:143` and `apply.ts:515`. Replace sentinel string
      `"<marketplace not declared>"` with the discriminant. Renderers KEEP
      byte-identical output by deriving the existing strings from the new
      variants.
    - Y4 (InvalidBlockOutcome.marketplace rename): in
      `apply-outcomes.ts:138-147`, rename the field that carries a
      basename to `basename` (or `configBasename`). Update the renderer to
      match.
    - Y5 (MigrateFirstRunResult cut): in `migrate-config.ts:41-53`, cut
      the result type along the existing `reason` discriminant so `error`
      exists ONLY on the `existing-invalid` arm. Update consumers to
      narrow on `reason` before reading `error`.
    - Y6 (PluginToggleAxes derived successStatus): in
      `apply.ts:435-450`, derive `successStatus` from `enable` inside
      `applyPluginToggles`; drop the redundant axis.

    Byte rule: catalog-uat byte gate MUST stay GREEN with NO catalog edits.
    If any rendered byte shifts, that's a renderer bug -- fix the renderer
    BEFORE commit.
  </behavior>
  <action>
    Sequence:

    1. Apply Y1 first. Let the TypeScript compiler enumerate every call
       site that breaks. Update each site to switch on the new union
       (delete `=== true` patterns at plan.ts:138, :192). This surfaces
       shadow callers via the type system.
    2. Apply Y2. Propagate through SourceMismatchOutcome + the renderers.
       Per-variant renderer tests must turn GREEN with byte-identical
       output.
    3. Apply Y4 rename. Update the renderer. Run catalog-uat to confirm
       no byte shift.
    4. Apply Y5 discriminant cut. Update every consumer to narrow on
       `reason` before reading `error`. The `@ts-expect-error` test from
       Task 1 must turn GREEN.
    5. Apply Y6 -- a one-line helper inside `applyPluginToggles`. The
       axis field comes off the call sites.
    6. Run catalog-uat byte gate. MUST be GREEN with NO catalog edits.
    7. Run `npm run check` until GREEN.
    8. Run `pre-commit run --files <changed>` until GREEN.
    9. Commit atomically. SKIP=trufflehog inside a worktree. Suggested
       title: `refactor(reconcile): widen source-mismatch + cut migrate result`.

    Do NOT touch enable-disable.ts (Plan 02 + Plan 05's Y3).
    Do NOT touch S7-S10 / catalog (Plan 05).
    Do NOT touch comments/docs (Plan 07).
    Do NOT touch any test gaps T1-T6 (Plan 06).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    - Task 1 tests GREEN (byte-equality, exhaustive switch,
      `@ts-expect-error` narrowing).
    - Type system rejects truthy-coercion of `"unknown-stored"`; rejects
      reading `error` outside the `existing-invalid` arm of
      MigrateFirstRunResult.
    - catalog-uat byte gate GREEN with NO docs/output-catalog.md edits.
    - `npm run check` exit 0.
    - `pre-commit run --files <changed>` GREEN.
    - One atomic Conventional Commit on `features/v1.12-config-files`.
  </done>
</task>

</tasks>

<verification>
1. `npm run check` GREEN.
2. catalog-uat byte gate GREEN with NO docs/output-catalog.md edit.
3. Y1 truthy-coercion test (TypeScript rejects bare `if` on the tri-state).
4. Y2 per-variant renderer tests confirm byte-identical output for all
   four causes.
5. Y4 renderer test confirms basename-rename leaves output unchanged.
6. Y5 `@ts-expect-error` test confirms `error` narrowing.
7. Y6 confirms `applyPluginToggles` derives `successStatus` from `enable`
   without an axis field (existing tests cover end-to-end).
8. One atomic Conventional Commit on `features/v1.12-config-files`.
</verification>

<success_criteria>
- Y1, Y2, Y4, Y5, Y6 are CLOSED by code + tests.
- Commit is byte-neutral.
- `npm run check` GREEN.
</success_criteria>

<output>
Append per-finding closure notes to
`.planning/quick/260612-bcs-fix-pr-51-five-agent-review-findings/260612-bcs-SUMMARY.md`
recording which commit closed Y1, Y2, Y4, Y5, Y6.
</output>
