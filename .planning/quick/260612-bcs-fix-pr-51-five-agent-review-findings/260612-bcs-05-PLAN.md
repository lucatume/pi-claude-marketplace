---
quick_task: 260612-bcs-fix-pr-51-five-agent-review-findings
sub_plan: 05
type: execute
wave: 5
depends_on: [260612-bcs-02, 260612-bcs-04]
files_modified:
  - src/orchestrators/plugin/enable-disable.ts
  - src/orchestrators/reconcile/apply.ts
  - src/orchestrators/reconcile/plan.ts
  - src/orchestrators/reconcile/notify.ts
  - src/persistence/config-io.ts
  - src/persistence/config-write-back.ts
  - src/shared/notify.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
  - tests/orchestrators/reconcile/apply.test.ts
autonomous: true
byte_contract: byte-neutral
requirements:
  - Y3
  - S7
  - S8
  - S9
  - S10

must_haves:
  truths:
    - "setPluginEnabled has an overload pair: opts.notifications.mode === 'orchestrated' returns Promise<EnableDisablePluginOutcome> (no | undefined); apply.ts:470-471 has no silent `if (!outcome) continue` guard (Y3 + S6's fourth loop)."
    - "isDeclaredEnabled(entry) helper exported from persistence/config-io.ts replaces every `enabled !== false` repeat at every call site (S7)."
    - "MarketplaceBlock.status (reconcile/notify.ts:51-58) is narrowed to the 5 statuses the projection assigns + undefined; the defensive runtime throw at :118-126 is deleted (S8)."
    - "cascadeSeverity structural-subset param in notify.ts:1865-1873 has status tightened to the closed union (S9)."
    - "config-write-back.ts:58 cast comment points at saveConfig's validator backstop (S10) -- no code change if the cast already lands inside the documented backstop chain."
    - "All rendered bytes stay identical; catalog-uat byte gate GREEN with NO docs/output-catalog.md edit in this commit."
  artifacts:
    - path: src/orchestrators/plugin/enable-disable.ts
      provides: "setPluginEnabled overload pair (Y3); mode-narrowed return type for orchestrated mode."
    - path: src/orchestrators/reconcile/apply.ts
      provides: "Dead `if (!outcome) continue` removed at :470-471 -- now a type error (Y3 also closes S6's fourth loop)."
    - path: src/persistence/config-io.ts
      provides: "Exported `isDeclaredEnabled(entry)` one-line helper (S7)."
    - path: src/orchestrators/reconcile/notify.ts
      provides: "MarketplaceBlock.status narrowed; defensive runtime throw deleted (S8); cascadeSeverity param tightened (S9)."
    - path: src/persistence/config-write-back.ts
      provides: "Cast comment at :58 points at saveConfig's validator backstop (S10)."
  key_links:
    - from: src/orchestrators/plugin/enable-disable.ts::setPluginEnabled (overload)
      to: src/orchestrators/reconcile/apply.ts:470-471
      via: orchestrated-mode return type forbids `| undefined`
      pattern: "setPluginEnabled.*orchestrated.*Promise<EnableDisablePluginOutcome>"
    - from: src/persistence/config-io.ts::isDeclaredEnabled
      to: src/orchestrators/reconcile/plan.ts (and other consumers of `enabled !== false`)
      via: single shared predicate replaces the open-coded comparison
      pattern: "isDeclaredEnabled"
---

<objective>
Land the type cuts that DEPEND on Plan 02's enable-disable structural changes
having merged: Y3 (setPluginEnabled orchestrated overload pair -- removes the
silent continue at apply.ts:470-471 and closes S6's fourth loop in one move),
plus the four remaining type-tightening Suggestions (S7-S10).

Purpose: Y3's overload signature is byte-neutral but type-load-bearing; it
removes the only remaining `if (!outcome) continue` silent vanish in the
reconcile cascade. The Suggestions are localized type tightenings that keep
the same byte-neutral contract.

Output: One atomic Conventional Commit on `features/v1.12-config-files`,
byte-neutral, `npm run check` GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260612-bcs-fix-pr-51-five-agent-review-findings/260612-bcs-CONTEXT.md
@CLAUDE.md
@.claude/rules/typescript-comments.md

@src/orchestrators/plugin/enable-disable.ts
@src/orchestrators/reconcile/apply.ts
@src/orchestrators/reconcile/plan.ts
@src/orchestrators/reconcile/notify.ts
@src/persistence/config-io.ts
@src/persistence/config-write-back.ts
@src/shared/notify.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests RED for Y3 + S7-S10 type-tightening contracts</name>
  <files>
    tests/orchestrators/plugin/enable-disable.test.ts,
    tests/orchestrators/reconcile/apply.test.ts
  </files>
  <behavior>
    - Y3 pin: in `tests/orchestrators/plugin/enable-disable.test.ts`,
      assert (via `// @ts-expect-error` on a deliberate misuse) that the
      orchestrated-mode overload signature forbids `undefined` in the
      return shape. Add an end-to-end test that exercises a successful
      orchestrated enable through `applyReconcile` -- the cascade row
      must render and not vanish under the previous silent-continue.
    - S7 pin: a small unit test of `isDeclaredEnabled(entry)` covering
      the three relevant entry shapes: `{enabled: true}`, `{enabled: false}`,
      `{}` (absent flag); confirm the helper returns the expected truth
      values matching `enabled !== false` semantics.
    - S8 pin: a `@ts-expect-error` test confirming `MarketplaceBlock.status`
      cannot be assigned a value outside the closed 5-status union (+ the
      defensive runtime throw is deleted so coverage tools no longer flag
      it).
    - S9 pin: a `@ts-expect-error` test confirming `cascadeSeverity`'s
      structural-subset param's `status` is the closed union, not
      `string`.
    - S10 pin: no behavioural test required; confirm the comment update
      via grep in the verification step.

    Tests RED until cuts land.
  </behavior>
  <action>
    Mirror seed shapes from existing tests in each file. Tests using
    `@ts-expect-error` need to compile cleanly under tsc -- run
    `npx tsc --noEmit` to confirm the expected error lights up.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/enable-disable.test.ts tests/orchestrators/reconcile/apply.test.ts 2>&1 | grep -E 'fail|pass' | head -10</automated>
  </verify>
  <done>
    - Y3, S7, S8, S9 tests exist; tests RED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply Y3 + S7-S10 (GREEN, byte-neutral)</name>
  <files>
    src/orchestrators/plugin/enable-disable.ts,
    src/orchestrators/reconcile/apply.ts,
    src/orchestrators/reconcile/plan.ts,
    src/orchestrators/reconcile/notify.ts,
    src/persistence/config-io.ts,
    src/persistence/config-write-back.ts,
    src/shared/notify.ts
  </files>
  <behavior>
    - Y3 (setPluginEnabled orchestrated overload): in
      `plugin/enable-disable.ts:287`, add an overload pair so
      `opts & { notifications: { mode: "orchestrated" } }` returns
      `Promise<EnableDisablePluginOutcome>` (no `| undefined`). Mirror
      the pattern in `AddMarketplaceNotifications`. In `apply.ts:470-471`,
      remove the dead `if (!outcome) continue` -- the type now forbids it
      (this also closes S6's fourth loop in the same edit).
    - S7 (isDeclaredEnabled helper): export a single one-line predicate
      `isDeclaredEnabled(entry)` from `persistence/config-io.ts` next to
      ScopeConfig. Use it at every `enabled !== false` site. Search
      `grep -RnE '\.enabled\s*!==\s*false' src/` to enumerate; replace
      each with the helper call.
    - S8 (MarketplaceBlock.status narrowed): in `reconcile/notify.ts:51-58`,
      narrow `status` to the 5 statuses the projection assigns + undefined.
      Delete the defensive runtime throw at `:118-126` (now structurally
      unreachable).
    - S9 (cascadeSeverity closed-set param): in `notify.ts:1865-1873`,
      tighten the cascadeSeverity structural-subset param's `status`
      from `string` to the closed union.
    - S10 (config-write-back.ts:58 cast comment): point the cast comment
      at saveConfig's validator backstop (per type-review note). No code
      change required if the cast already lands inside the documented
      backstop chain; the edit is comment-only on that one line.

    Byte rule: ZERO catalog edits. catalog-uat byte gate must stay GREEN.
  </behavior>
  <action>
    Sequence:

    1. Apply Y3 first. Update apply.ts's call sites to use the
       orchestrated overload. Remove the dead `if (!outcome) continue` at
       apply.ts:470-471. Get Y3 tests GREEN.
    2. Apply S7. Export `isDeclaredEnabled` from `persistence/config-io.ts`.
       Grep `\.enabled\s*!==\s*false` across `src/` to enumerate every
       site; replace each. Get S7 test GREEN.
    3. Apply S8. Narrow the type; delete the defensive runtime throw at
       notify.ts:118-126. Get S8 test GREEN.
    4. Apply S9. Tighten the structural-subset param's `status` to the
       closed union. Get S9 test GREEN.
    5. Apply S10. Update the cast comment at config-write-back.ts:58 to
       reference saveConfig's validator backstop.
    6. Run catalog-uat byte gate. MUST be GREEN with NO catalog edits.
    7. Run `npm run check` until GREEN.
    8. Run `pre-commit run --files <changed>` until GREEN.
    9. Commit atomically. SKIP=trufflehog inside a worktree. Suggested
       title: `refactor(plugin): tighten setPluginEnabled overloads + narrow types`.

    Do NOT touch type cuts already in Plan 04.
    Do NOT touch comments/docs (Plan 07).
    Do NOT touch any test gaps T1-T6 (Plan 06).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    - Task 1 tests GREEN (Y3 + S7-S9).
    - `apply.ts:470-471` no longer has a runtime `if (!outcome) continue`
      guard.
    - `grep -RnE '\.enabled\s*!==\s*false' src/` returns ZERO matches
      (every site uses `isDeclaredEnabled`).
    - catalog-uat byte gate GREEN with NO docs/output-catalog.md edits.
    - `npm run check` exit 0.
    - `pre-commit run --files <changed>` GREEN.
    - One atomic Conventional Commit on `features/v1.12-config-files`.
  </done>
</task>

</tasks>

<verification>
1. `npm run check` GREEN.
2. catalog-uat byte gate GREEN with NO docs/output-catalog.md edit.
3. Y3 overload `@ts-expect-error` test + applyReconcile end-to-end test
   pass.
4. `grep -RnE '\.enabled\s*!==\s*false' src/` returns ZERO matches.
5. S8 narrowing + defensive-throw deletion confirmed.
6. S9 closed-union param confirmed.
7. S10 comment update confirmed via grep.
8. One atomic Conventional Commit on `features/v1.12-config-files`.
</verification>

<success_criteria>
- Y3, S7, S8, S9, S10 are CLOSED by code + tests.
- Plan 03's S6 fourth-loop coverage is now complete (Y3 removed the silent
  continue at apply.ts:470-471).
- Commit is byte-neutral.
- `npm run check` GREEN.
</success_criteria>

<output>
Append per-finding closure notes to
`.planning/quick/260612-bcs-fix-pr-51-five-agent-review-findings/260612-bcs-SUMMARY.md`
recording which commit closed Y3, S7, S8, S9, S10.
</output>
