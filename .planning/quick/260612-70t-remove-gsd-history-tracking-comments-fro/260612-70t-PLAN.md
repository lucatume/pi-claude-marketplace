---
phase: quick
plan: 260612-70t
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/**/*.ts
  - tests/**/*.ts
  - .claude/rules/typescript-comments.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "No TS comment under extensions/ or tests/ contains GSD process-history bookkeeping (Phase NN / Plan NN / Plan NN-NN / Wave N / Task N / milestone vX.Y)."
    - "No test title under tests/ contains GSD process-history bookkeeping prefixes."
    - "Every decision ID (D-xx), requirement ID (PRL-xx, AUTH-xx, DIFF-xx, ATTR-xx, RECON-xx, ENBL-xx, SPLIT-xx, WR-xx, CR-xx, UAT-xx, SNM-xx, TYPE-xx, SC-xx, NFR-xx, Mxx), and GitHub issue reference (#xxxx) present before this plan is still present after this plan."
    - "Domain-language uses of 'phase' (two-phase commit narration in stage.ts, the phase-ledger transaction concept, fixture strings like 'plugin update phase 3 failed', URL fragments like #v1.0) are preserved unchanged."
    - "`npm run check` passes (typecheck + ESLint + Prettier + tests) at the end of every task commit."
    - "`.claude/rules/typescript-comments.md` exists with YAML frontmatter scoping it to `**/*.ts` and codifies: comments and test titles describe what the code does (and non-obvious why); GSD process-history bookkeeping is forbidden; decision IDs and requirement IDs are allowed as traceability anchors."
  artifacts:
    - path: ".claude/rules/typescript-comments.md"
      provides: "Claude Code rule scoping TS files to enforce the comment policy"
      contains: "paths:"
  key_links:
    - from: "extensions/**/*.ts comments"
      to: "decision/requirement ID catalog"
      via: "in-comment traceability tokens"
      pattern: "D-[0-9]+|PRL-[0-9]+|AUTH-[0-9]+|DIFF-[0-9]+|RECON-[0-9]+|ENBL-[0-9]+|WR-[0-9]+|NFR-[0-9]+"
    - from: "tests/**/*.ts test titles"
      to: "requirement IDs"
      via: "test() name strings"
      pattern: "test\\([\"']([A-Z]+-[0-9]+|M[0-9]+)"
---

<objective>
Strip GSD process-history bookkeeping (Phase NN / Plan NN / Wave N / Task N / `milestone vX.Y` parentheticals) from every TypeScript comment under `extensions/` and from every TypeScript comment AND test title under `tests/`, while preserving all technical substance, decision IDs, requirement IDs, and GitHub issue references. Then add a `.claude/rules/typescript-comments.md` rule file that codifies the policy for future TS work.

Purpose: Comments must describe what the code does (and non-obvious why); git owns process history. The v1.0–v1.12 GSD lineage in comments is dead-weight bookkeeping that confuses future readers and makes diffs noisy. Traceability IDs stay because they anchor comments to PRD/REQUIREMENTS rows, which IS still useful.
Output: Cleaned `.ts` files across `extensions/` and `tests/`, plus the new rule file. Three atomic commits, each leaving `npm run check` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md

# Sampled patterns the executor must handle (illustrative, not exhaustive)

# Pure history that gets DELETED or merged into surrounding prose:
#   extensions/pi-claude-marketplace/edge/handlers/shared.ts:3
#     `// Phase 56 Plan 01 Task 2 -- cross-cutting edge-handler helpers shared by`
#     → strip the `Phase 56 Plan 01 Task 2 -- ` prefix; keep `cross-cutting edge-handler helpers shared by...`
#   extensions/pi-claude-marketplace/edge/handlers/shared.ts:11
#     `// \`edge/handlers/plugin/enable-disable.ts\` (Phase 54 frozen). Plans 02 and`
#     → `// \`edge/handlers/plugin/enable-disable.ts\`. Subsequent helpers add to`
#       (delete the `(Phase 54 frozen). Plans 02 and` narrative; rewrite for fluency)

# ID present, parenthetical removed:
#   `// RECON-01..05 (Phase 55 Plan 02): apply the load-time reconcile BEFORE`
#     → `// RECON-01..05: apply the load-time reconcile BEFORE`
#   `// WB-01 / Phase 56 Plan 02: extract \`--local\` BEFORE positional parsing`
#     → `// WB-01: extract \`--local\` BEFORE positional parsing`
#   `* WR-05 (Phase 56 review): the flip never writes state.json --`
#     → `* WR-05: the flip never writes state.json --`
#   `// ATTR-07 (Phase 48): \`addMarketplace\` now routes precondition failures`
#     → `// ATTR-07: \`addMarketplace\` now routes precondition failures`
#   `* config (claude-plugins.json) is the new source of truth after Phase 51's`
#     → `* config (claude-plugins.json) is the new source of truth.`
#       (re-flow the paragraph; drop the dangling `after Phase 51's` clause and whatever historical
#       continuation followed it)

# Test titles — prefix stripped:
#   `test("Phase 8 / PRL-10 replacePreparedSkills can rollback to previous skill bytes", ...)`
#     → `test("PRL-10 replacePreparedSkills can rollback to previous skill bytes", ...)`
#   `test("Phase 32 initiateDeviceFlow: AUTH-01 happy path returns ok+cred+authAttempted", ...)`
#     → `test("initiateDeviceFlow: AUTH-01 happy path returns ok+cred+authAttempted", ...)`
#   `test("AUTH-09 (Phase 32): domain/github-auth.ts never interpolates ...", ...)`
#     → `test("AUTH-09: domain/github-auth.ts never interpolates ...", ...)`
#   `test("import-x/no-restricted-paths defines exactly 8 zones (one per folder) -- D-11 (Phase 21 retired presentation/)", ...)`
#     → `test("import-x/no-restricted-paths defines exactly 8 zones (one per folder) -- D-11", ...)`

# FALSE POSITIVES — must NOT be touched:
#   extensions/pi-claude-marketplace/bridges/agents/stage.ts ~line 307:
#     `* Phase 2: remove old target files (only safe-to-overwrite ones), rename`
#     → KEEP unchanged. This narrates the two-phase commit algorithm of staging.
#   tests/transaction/phase-ledger.test.ts:
#     The phase ledger is a transaction-domain concept. Body text like
#     `// 4 phases, phase 3 throws` describes the feature under test → KEEP.
#     ONLY clean up GSD-style title PREFIXES if any exist
#     (e.g. `test("Phase 8 / PRL-10 ...")`); a bare `phase` referring to the
#     ledger's own phases stays.
#   tests/shared/errors.test.ts:
#     Fixture strings like `"plugin update phase 3 failed"` → KEEP (they are
#     test data, not GSD bookkeeping).
#   tests/domain/source.test.ts:
#     URL fragments like `#v1.0` → KEEP (version pins in URLs, not milestone refs).

# WR-04 reference comment that must NOT lose its meaning:
#   `// WR-04 (Phase 56 review): explicit-save transaction so the abort arms`
#     → `// WR-04: explicit-save transaction so the abort arms` (keep the why intact)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean GSD history bookkeeping from extensions/ TS comments</name>
  <files>extensions/**/*.ts</files>
  <action>
Edit every `.ts` file under `extensions/` to remove GSD process-history bookkeeping from comments while preserving all technical substance and traceability IDs.

Procedure:

1. Enumerate candidate lines once with:

   ```
   grep -rEn '(\bPhase [0-9]+\b|\bPlan [0-9]+(-[0-9]+)?\b|\bWave [0-9]+\b|\bTask [0-9]+\b|milestone v[0-9]+\.[0-9]+|v1\.1[0-9] milestone|\((Phase|Plan|Wave|Task)[^)]*\))' extensions/ --include='*.ts'
   ```

   Treat the output as a worklist, not as an exhaustive specification — re-grep after each batch of edits to catch what wraps across lines.

2. For each match, classify and act:

   - **GSD-only sentence** (the entire comment line/paragraph is process bookkeeping with no technical content). Delete the comment entirely. Example targets: leading file banners like `// Phase 56 Plan 01 Task 2 -- shared edge scanner unit tests.` when the next line(s) already describe the file.

   - **Mixed comment** (GSD parenthetical embedded in a technical sentence). Strip ONLY the bookkeeping segment; keep the rest verbatim. Patterns to strip:
     - ` (Phase NN)` / ` (Phase NN Plan NN)` / ` (Phase NN Plan NN-NN)` / ` (Phase NN review)` / ` (Phase NN frozen)`
     - ` / Phase NN Plan NN` / ` / Phase NN review` (slash-joined process anchors)
     - `Phase NN Plan NN: ` / `Phase NN: ` / `Plan NN: ` / `Plan NN-NN: ` when used as a leading bookkeeping prefix on a comment line
     - `Phase NN's ` / `Phase NN frozen.` / `Plans NN and NN` when used as a narrative continuation
     - `milestone vX.Y` / `vX.Y milestone` references and their UAT-decision parentheticals like `(v1.12 milestone UAT decision 2026-06-11)`

   - **Domain-language `phase`** (two-phase commit narration in `bridges/agents/stage.ts`, any `phase ledger` reference if it occurs in extensions/). KEEP unchanged.

   - **Decision IDs and requirement/finding IDs** (`D-NN`, `D-NN-NN`, `D-NN.N-NN`, `PRL-NN`, `AUTH-NN`, `DIFF-NN`, `ATTR-NN`, `RECON-NN`, `ENBL-NN`, `SPLIT-NN`, `WR-NN`, `CR-NN`, `UAT-NN`, `SNM-NN`, `TYPE-NN`, `SC-N`, `NFR-N`, `MN`, `Pitfall N`, GitHub `#NNNN`). KEEP every occurrence verbatim.

3. After editing, re-flow any paragraph whose grammar broke (e.g. `the new source of truth after Phase 51's reconcile planner` becomes `the new source of truth` and may need a period). Do not introduce new claims; the goal is to make the surviving sentence read naturally.

4. Run after the edits, in this order, and fix any failures before considering the task done:

   ```
   pre-commit run --files <changed files>
   npm run check
   ```

   In particular, watch for architecture tests under `tests/architecture/` that grep source patterns or assert on specific comment shapes (`no-split-01-cast-reads.test.ts`, `notify-grammar-invariant.test.ts`, `manifest-read-seam.test.ts`, etc.); if any fail, the assertion's regex/byte-form needs the same stripping treatment — but DO NOT touch test files in this task. If a test references a removed comment and cannot be salvaged by tightening its regex without touching tests, surface the file path and the failing assertion text instead of editing tests; the executor will address those in Task 2.

5. Commit:

   ```
   git add extensions/
   git commit -m "refactor(extensions): drop GSD process-history from TS comments"
   ```

   The commit body should briefly note: "Strips Phase/Plan/Wave/Task/milestone bookkeeping from comments under extensions/. Preserves decision IDs, requirement IDs, and the two-phase commit narration in bridges/agents/stage.ts. Source-policy enforcement codified separately."

Hard guarantees the executor MUST verify before committing:
- `grep -rEn '(\bPhase [0-9]+\b|\bPlan [0-9]+(-[0-9]+)?\b|\bWave [0-9]+\b|\bTask [0-9]+\b)' extensions/ --include='*.ts' | grep -v 'bridges/agents/stage.ts'` returns no GSD-bookkeeping matches. Hits inside `bridges/agents/stage.ts` are allowed (the two-phase commit narration).
- `grep -rEn 'milestone v[0-9]+\.[0-9]+|v[0-9]+\.[0-9]+ milestone' extensions/ --include='*.ts'` returns no matches.
- Every decision/requirement ID that existed in `extensions/` before this task still exists after (spot-check via `grep -rEn '\b(D-|PRL-|AUTH-|DIFF-|ATTR-|RECON-|ENBL-|SPLIT-|WR-|CR-|UAT-|SNM-|TYPE-|NFR-)[0-9]' extensions/ --include='*.ts' | wc -l` before and after; the after-count must be ≥ before-count).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
- All GSD process-history bookkeeping is removed from `extensions/**/*.ts` comments.
- The two-phase commit narration in `bridges/agents/stage.ts` is unchanged.
- Decision IDs and requirement IDs are preserved (count is ≥ pre-task count).
- `npm run check` is green.
- One atomic commit on the current branch with a Conventional Commits title.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean GSD history bookkeeping from tests/ TS comments and test titles</name>
  <files>tests/**/*.ts</files>
  <action>
Edit every `.ts` file under `tests/` to remove GSD process-history bookkeeping from BOTH comments AND `test(...)` / `describe(...)` titles, while preserving all technical substance and traceability IDs.

Procedure:

1. Enumerate candidates separately for comments vs titles:

   ```
   # Comments and other body lines:
   grep -rEn '(\bPhase [0-9]+\b|\bPlan [0-9]+(-[0-9]+)?\b|\bWave [0-9]+\b|\bTask [0-9]+\b|milestone v[0-9]+\.[0-9]+|v1\.1[0-9] milestone|\((Phase|Plan|Wave|Task)[^)]*\))' tests/ --include='*.ts'

   # Test/describe titles (catch both styles):
   grep -rEn '\b(test|it|describe)\(\s*["\x27].*\b(Phase [0-9]+|Plan [0-9]+(-[0-9]+)?|Wave [0-9]+|Task [0-9]+)\b' tests/ --include='*.ts'
   ```

2. For each comment match, apply the same classify-and-strip procedure as Task 1 (delete pure GSD-only comments; strip bookkeeping segments from mixed comments; preserve decision IDs and requirement IDs).

3. For each title match, strip the GSD bookkeeping while keeping the rest of the title and any embedded IDs:

   - `"Phase 8 / PRL-10 replacePreparedSkills can rollback ..."` → `"PRL-10 replacePreparedSkills can rollback ..."`
   - `"Phase 32 initiateDeviceFlow: AUTH-01 happy path ..."` → `"initiateDeviceFlow: AUTH-01 happy path ..."`
   - `"AUTH-09 (Phase 32): domain/github-auth.ts ..."` → `"AUTH-09: domain/github-auth.ts ..."`
   - `"import-x/no-restricted-paths defines exactly 8 zones (one per folder) -- D-11 (Phase 21 retired presentation/)"` → `"import-x/no-restricted-paths defines exactly 8 zones (one per folder) -- D-11"`
   - When the only thing left of a `(Phase NN ...)` parenthetical is whitespace-only fluff, drop the parenthetical entirely (don't leave `() ` artifacts).
   - Preserve every `D-NN`, `D-NN-NN`, `PRL-NN`, `AUTH-NN`, `DIFF-NN`, `ATTR-NN`, `RECON-NN`, `ENBL-NN`, `SPLIT-NN`, `WR-NN`, `CR-NN`, `UAT-NN`, `SNM-NN`, `TYPE-NN`, `SC-N`, `NFR-N`, `MN`, `Pitfall N`, and GitHub `#NNNN` reference verbatim.

4. **Domain-language `phase` — KEEP unchanged:**
   - All body text in `tests/transaction/phase-ledger.test.ts` that uses `phase` to refer to the ledger's own phases (e.g. `// 4 phases, phase 3 throws`). Only clean GSD-style title PREFIXES if any exist.
   - Fixture strings under `tests/shared/errors.test.ts` like `"plugin update phase 3 failed"` (test data, not GSD).
   - URL fragments under `tests/domain/source.test.ts` like `#v1.0` (version pins).
   - Any other `phase` that names a domain concept rather than a GSD planning step.

5. **Architecture-test self-references — CRITICAL.** Several `tests/architecture/*.ts` files greppattern-match against source files or against catalog docs. After cleaning, run `npm run check` and inspect failures:
   - If a failing assertion is matching a literal source comment that this PR just changed, update the assertion to match the NEW form (do this in the same task).
   - If a failing assertion is matching `docs/output-catalog.md` byte-form or the REASONS tuple, that is NOT in scope here — those files are not touched by this plan. If such a test fails, it indicates a different, pre-existing issue; surface the failure and STOP.
   - If `tests/architecture/no-split-01-cast-reads.test.ts` or `notify-grammar-invariant.test.ts` greps source and a regex pattern still matches AFTER the cleanup (i.e. no false negatives), no test-side change is required.

6. Run after the edits:

   ```
   pre-commit run --files <changed files>
   npm run check
   ```

   Fix any failures before committing.

7. Commit:

   ```
   git add tests/
   git commit -m "refactor(tests): drop GSD process-history from comments and titles"
   ```

   Body: "Strips Phase/Plan/Wave/Task/milestone bookkeeping from comments and test/describe titles under tests/. Preserves decision IDs, requirement IDs, the phase-ledger domain language, and fixture strings."

Hard guarantees the executor MUST verify before committing:
- `grep -rEn '\b(test|it|describe)\(\s*["\x27].*\b(Phase [0-9]+|Plan [0-9]+(-[0-9]+)?|Wave [0-9]+|Task [0-9]+)\b' tests/ --include='*.ts'` returns no matches.
- `grep -rEn '(\bPhase [0-9]+\b|\bPlan [0-9]+(-[0-9]+)?\b|\bWave [0-9]+\b|\bTask [0-9]+\b|milestone v[0-9]+\.[0-9]+)' tests/ --include='*.ts'` returns no matches EXCEPT inside the domain-language carve-outs listed in step 4. If anything else survives, it must be addressed before committing.
- The traceability-ID count under `tests/` is ≥ pre-task count (spot-check the same regex used in Task 1).
- `npm run check` is green.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
- All GSD process-history bookkeeping is removed from `tests/**/*.ts` comments and test/describe titles.
- The phase-ledger domain language and fixture strings are unchanged.
- Decision IDs and requirement IDs are preserved (count is ≥ pre-task count).
- Architecture tests still pass (assertions updated if they referenced renamed titles).
- `npm run check` is green.
- One atomic commit on the current branch with a Conventional Commits title.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add .claude/rules/typescript-comments.md codifying the policy</name>
  <files>.claude/rules/typescript-comments.md</files>
  <action>
Create the rule file `.claude/rules/typescript-comments.md` so Claude Code automatically applies the policy when editing TypeScript.

Use this exact structure (YAML frontmatter + body):

```markdown
---
paths:
  - "**/*.ts"
---

# TypeScript comment policy

Comments and test/describe titles describe **what the code does** and, when
non-obvious, **why**. They do not record which GSD milestone, phase, plan,
wave, or task produced the code. Git history owns process history.

## Forbidden in comments and test titles

- `Phase NN`, `Plan NN`, `Plan NN-NN`, `Wave N`, `Task N` references to
  GSD planning steps.
- `milestone vX.Y`, `vX.Y milestone`, and UAT-decision parentheticals such as
  `(v1.12 milestone UAT decision 2026-06-11)`.
- Parentheticals like `(Phase 56 review)`, `(Phase 54 frozen)`,
  `(Phase 55 Plan 01)`.
- Any other phrasing whose only purpose is to record which planning artefact
  authored the line.

## Allowed (and encouraged) as traceability anchors

- Decision IDs: `D-01`, `D-21`, `D-54-01`, `D-15-11`, `D-17.1-01`, etc.
- Requirement and finding IDs: `PRL-NN`, `AUTH-NN`, `DIFF-NN`, `ATTR-NN`,
  `RECON-NN`, `ENBL-NN`, `SPLIT-NN`, `WR-NN`, `CR-NN`, `UAT-NN`, `SNM-NN`,
  `TYPE-NN`, `SC-N`, `NFR-N`, `Mxx` matrix IDs.
- GitHub issue/PR references like `#2916`.

These anchors link the code to a specification row, not to a planning step.

## Domain language is not GSD history

Words like `phase` that name domain concepts in the code itself are
**not** GSD references and must be preserved unchanged. Examples:

- The two-phase commit narration in `bridges/agents/stage.ts`
  (`Phase 2: remove old target files ...`).
- The `phase ledger` transaction concept and any body text under
  `tests/transaction/phase-ledger.test.ts` that talks about the ledger's
  own phases (e.g. `4 phases, phase 3 throws`).
- Fixture strings such as `plugin update phase 3 failed`.
- Version pins inside URLs such as `#v1.0`.

## Examples

Forbidden -> Allowed:

```
// Phase 56 Plan 01 Task 2 -- shared edge-handler helpers ...
```
becomes
```
// Shared edge-handler helpers ...
```

```
// WB-01 / Phase 56 Plan 02: extract `--local` BEFORE positional parsing
```
becomes
```
// WB-01: extract `--local` BEFORE positional parsing
```

```
test("Phase 8 / PRL-10 replacePreparedSkills can rollback ...", ...)
```
becomes
```
test("PRL-10 replacePreparedSkills can rollback ...", ...)
```
```

After creating the file:

1. Verify the YAML frontmatter parses (open it in the editor or run a quick `head -5` and confirm the `---` fences and `paths:` array). The body uses markdown fenced code blocks; the frontmatter is ONLY the top three-line `---paths---` block. Do not let the example fences get confused with frontmatter delimiters.

2. Run:

   ```
   pre-commit run --files .claude/rules/typescript-comments.md
   ```

   If the pre-commit Markdown formatter rewrites the file (trailing newlines, list-style normalization, etc.), accept its changes and re-stage.

3. Commit:

   ```
   git add .claude/rules/typescript-comments.md
   git commit -m "docs(rules): add TypeScript comment policy rule"
   ```

   Body: "Codifies that TS comments and test titles describe what the code does. GSD process-history bookkeeping (Phase/Plan/Wave/Task/milestone) is forbidden; decision IDs and requirement IDs are allowed as traceability anchors. Scoped to **/*.ts via Claude Code rule frontmatter."
  </action>
  <verify>
    <automated>test -f .claude/rules/typescript-comments.md && grep -q '^paths:' .claude/rules/typescript-comments.md && grep -q '"\*\*/\*\.ts"' .claude/rules/typescript-comments.md</automated>
  </verify>
  <done>
- `.claude/rules/typescript-comments.md` exists with YAML frontmatter scoping `paths: ["**/*.ts"]`.
- The body codifies: forbidden GSD bookkeeping patterns, allowed traceability IDs, domain-language carve-outs, and worked examples.
- The file passes pre-commit hooks.
- One atomic commit on the current branch with a Conventional Commits title.
  </done>
</task>

</tasks>

<verification>
After all three tasks are committed:

1. `npm run check` is green.
2. `grep -rEn '(\bPhase [0-9]+\b|\bPlan [0-9]+(-[0-9]+)?\b|\bWave [0-9]+\b|\bTask [0-9]+\b)' extensions/ tests/ --include='*.ts' | grep -v 'extensions/pi-claude-marketplace/bridges/agents/stage.ts' | grep -v 'tests/transaction/phase-ledger.test.ts' | grep -v 'tests/shared/errors.test.ts'` returns no matches.
3. `grep -rEn 'milestone v[0-9]+\.[0-9]+|v[0-9]+\.[0-9]+ milestone' extensions/ tests/ --include='*.ts'` returns no matches.
4. The traceability-ID count under `extensions/` and `tests/` is ≥ pre-plan count.
5. `.claude/rules/typescript-comments.md` exists and parses.
6. Three commits exist on the current branch, each one a clean atomic step (extensions cleanup; tests cleanup; rule file).
</verification>

<success_criteria>
- All three tasks committed, each with a passing `npm run check`.
- No GSD process-history bookkeeping survives in `extensions/**/*.ts` or `tests/**/*.ts` outside the documented domain-language carve-outs.
- Every decision ID, requirement ID, finding ID, and GitHub reference that existed before is still present after.
- `.claude/rules/typescript-comments.md` codifies the policy and is scoped to TS files.
</success_criteria>

<output>
No SUMMARY required for quick tasks. The three commits on the current branch are the deliverable.
</output>
